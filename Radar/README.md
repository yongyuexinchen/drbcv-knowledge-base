# 体系演化雷达（System Evolution Radar）

> 记录**我认为哪些系统正在失稳、迁移、重构**，以及我准备如何响应。
> 创建：2026-08-06 ｜ 定位：**个人版系统动力学推演引擎**（不是机会收集箱，不是趋势追踪器）

## 理论底座（哲学源头，2026-08-06 确认）

Radar 不是从零设计的——它是混沌-确定性世界观（见 [[确定性是压缩模型_混沌是更新机制]]）的**工程化**：

```
混沌 → 人类提取规律 → 确定性 → 建立体系文明 → 路径依赖僵化 → 新变量进入 → 新混沌
```

- KOS = 保存已提取的确定性
- Self = 记录观察者如何形成确定性
- **Radar = 监控确定性何时被混沌突破**（本库）

对应关系：前提三（体系可被打败）← 循环的"僵化→新变量"环节；disruption 字段 ← "改变规则空间/让优化目标失效"；purpose 字段 ← "体系最初解决什么问题，现在还能解决吗"。
完整框架见 [[2026-08-06_混沌确定性与Radar理论底座]]（事件源）。

## 第一性原理（2026-08-06 用户提出，三条前提）

1. **高维系统对低维系统具有短期预测能力**：宏观长期不可预测，短期可预测。
   → Radar 的单位不是"预测未来"，而是"**监控当前状态，在有限窗口内判断变化方向**"。
   → 长期判断 = `scenario`（场景），短期判断 = `forecast`（预测）。状态机判据只依赖短期可观测指标。
2. **任何体系都有目的性**：存在本身为解决特定矛盾。**把握存在目的是推演体系变化的关键**；不知目的可通过历史运作反推。
   → 目的反推验证纪律：反推的目的必须能生成可证伪预测——"如果目的是 X，条件 Y 出现时体系应做 Z"。兑现→推断有效；落空→推断作废。禁止 just-so story。
3. **体系可以被打败且失效**：对确定且牢固的体系，引入混沌是最好的办法。
   → 不要在对方规则内竞争，改变状态空间（汽车不是更好的马车）。disruption 只记录**攻击体系目的实现方式**的外部变量，其他不录。

## 三层认知架构

```
              世界
               |
       ----------------
       |              |
    KOS知识层       Radar演化层
       |              |
 "现在是什么"     "未来会怎么变"
       |
    Self认知层
       |
 "我为什么这样判断"
```

长期演进（**Strategy OS 暂缓**，Position 卡是雏形，YAGNI）：

```
Knowledge OS → Self OS → Radar OS → Strategy OS
  (世界是什么)   (我是谁)   (往哪变)    (发现≠获得：学什么/投多少/何时进)
```

## 核心模型

```
             System
                |
        ----------------
        |              |
     Purpose       Structure
        |
        ↓
    Contradiction（核心矛盾）
        |
        ↓
    Pressure（压力：来源/指标/影响）
        |
        ↓
   Transition Point（转换点）
        |
   ----------------
   |              |
Internal      External Chaos
Evolution     Disruption
   |
   ↓
Position（布局）
```

| 实体 | 定义 | 形式 |
|------|------|------|
| System | 长期运行的系统：**目的** + 结构 + 核心矛盾 | `Systems/` 卡 |
| Contradiction | 体系存在的目的所对抗的矛盾（目的无法实现时=系统要变） | 体系卡字段 |
| Pressure | 正在积累的压力（来源/指标/影响） | **正文结构，不单独建实体** |
| Transition | 压力突破阈值后系统进入什么状态。**一张卡=一个可监控的短期预测** | `Transitions/` 卡（核心） |
| Position | 判断正确时我该学什么/投什么/做什么项目/建什么关系 | `Positions/` 卡 |

## 转换点状态机（每个状态有可观测进入判据，推进必须在卡上写依据）

```
seed → observing → strengthening → triggered → positioning → resolved
   ↘                                              ↘ invalidated（任意状态可进入）
```

| 状态 | 含义 | 进入判据 |
|------|------|---------|
| seed | 刚出现单一信号 | 一个信号源出现 |
| observing | 多信号源支持 | ≥2 个独立信号源，指标可观测 |
| strengthening | 越来越明显 | 指标连续 2 期增强，且无反证 |
| triggered | 关键阈值突破 | 卡内 trigger 量化条件满足 |
| positioning | 开始行动 | Position 卡激活，动作有截止时间 |
| resolved | 结果出来 | 转换完成 / 兑现或错失 |
| invalidated | 判断失效 | 卡内 invalidation 条件出现，记录原因，不删卡 |

## 转换点卡模板

```yaml
---
type: transition
system: "[[体系名]]"
status: observing            # 6态之一
window: short_term           # short_term(≤6月) | medium_term(6-24月) | long_term(>24月=scenario)
trigger: "可观测量化阈值"
invalidation: "出现则失效"     # ← 铁律：无失效条件 = 无法证伪 = 不是预测
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

## 前提（Premises）      # CAO 铁律：无前提不成立
## 现状（含状态机依据）
## 压力（Pressure）      # 固定三字段：来源 / 指标 / 影响
## 触发阈值
## 转换方向（含 disruption 评估）
## 时间窗口
## 失效条件
## 布局 → [[对应Position卡]]
```

## 体系卡模板

```yaml
---
type: system
name: "体系名"
purpose: "存在目的（缓解什么矛盾）"       # ← 第一性原理字段
core_contradiction: "目的对抗的核心矛盾"   # 目的无法实现=体系要变
disruption: "攻击目的实现方式的外部混沌"   # 只记攻击目的的，其他不录
created: YYYY-MM-DD
---
# 体系名
## 组成
## 稳定机制
## 脆弱点（Pressure 清单）
## 监控指标
## 转换点（Transitions/）
```

## 第一批五个体系（不贪多）

1. **AI产业体系** — 职业输入侧
2. **软件工程体系** — 吃饭的手艺
3. **中国经济体系** — 经济机器怎么转（实体+政策）
4. **资本市场体系** — 钱往哪流（价格+资金）
5. **个人成长体系** — 自己也是一个系统

边界纪律：利率政策→中国经济；DR007资金面→资本市场。一个压力只记一处，跨体系交叉引用。

## 与 Self 的闭环（个人战略操作系统）

```
世界变化 → Radar（哪些系统在变？）→ Self（我如何理解和行动？）
    → Project（我如何进入这个变化？）→ Experience → 更新 Self
```

## 维护纪律

1. 转换点卡必须有失效条件；状态推进必须写**依据**（哪个指标变了）
2. **每周五巡检**（绑定 weekly_check）：逐卡推进状态机 + 更新信号 checkbox
3. triggered 后 24h 内激活 Position 卡，动作带截止时间
4. invalidated 不删卡，记录原因（失败也是资产）
5. 不追求数量：5 体系起步；长期判断只进 scenario，不进状态机
6. **雷达审视雷达**：Radar 也是体系。目的=在转换前获得认知与进入机会；失效条件=状态机无人推进 / 维护成本 > 产出
7. git 纪律：本目录单独 `git add Radar/`，禁 `-A`（单仓库混跑多任务）

## 目录

```
Radar/
├── README.md
├── Systems/       体系卡（5 张，含 purpose/core_contradiction/disruption）
├── Transitions/   转换点卡（核心）
└── Positions/     布局卡（动作清单）
```
