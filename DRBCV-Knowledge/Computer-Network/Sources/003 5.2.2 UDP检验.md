# 003 5.2.2 UDP检验

> Source: 003 5.2.2 UDP检验_原文.docx

003 5.2.2 UDP检验_原文

2026年07月19日 12:19

发言人   00:00

各位同学大家好，在这个视频中我们要探讨UDP检验这个考点。所谓的UDP检验就是说数据的发送方给数据的接收方发送一个UDP数据报之后，接收方如何去检验出这个UDP数据报当中是否有比特错误。其实也就是UDP协议的差错控制，就这个意思。

发言人   00:26

好，接下来我们先提出一种新的差错检测方法。假设左边这个A结点要给右边这个A结点发送32比特的二进制。好，为了让B结点在收到这些数据的时候，能够检测出其中是否发生了这个比特错误。我们需要在这个原始数据当中再携带一些校验信息，然后把原始数据校验信息一起发给这个接收方。这样的话数据的接收方就可以根据这些校验信息去检查到底有没有出错了。

发言人   01:03

好，接下来看这个校验信息应该如何生成。我们看这个式子，假设我们最终要生成的校验信息总共是16个比特，那么我们可以把原始数据以16个比特为一组，让这些二进制数进行二进制加法。那最低位两个一相加，本位和等于零往更高位进一个一好，接下来次低位0加0，再加上来自低位的这个进位0加0加1，本位合就等于一，同时往更高位进0。好，那么二进制的加法我们在计算机组成原理里边已经训练了很多了，具体的细节就不再赘述。总之把这32比特的原始数据拆分成每16比特为一组，然后让这些分组进行二进制加法。相加的结果就是这样的一串数字，大家可以自己算一下。好，最后我们再把这个加法的运算结果逐位取反，也就是一变00变1逐位取反。取反之后得到的这16个比特就作为我们的校验和。

发言人   02:17

好，所以这32比特的原始数据再加上这16个比特的校验和数据一起发给接收方B结点。那么B结点在收到这些内容之后，就可以用类似的方法去检查有没有发生比特错误。显然如果说没有任何一个比特发生错误的话，那么这三组16比特的数依次进行二进制加法，最终得到的结果肯定是全一。

发言人   02:50

这是没有发生比特错误的情况，很好理解。因为我们的这个校验和它是把前边的这些原始数据加和的结果逐位按位取反对不对。所以如果数据的接收方把这些16比特数依次进行加法运算的话，肯定就是16比特的权益。那假如这三组数相加的结果不是全一，那么是不是就说明了其中肯定发生了某些比特错误？

发言人   03:26

好，这就是UDP校验的一个核心的思想。在刚才这个例子当中，原始数据我们假设只有32比特。那如果原始数据更多一些，其实也是同样的道理。比如说原始数据假设有48比特，那么同样的在计算16比特校验和的时候，我们把这48比特的原始数据拆分成16比特为一组，然后让这些16比特的数依次进行这个二进制的加法。

发言人   03:55

首先是前2个16比特数进行二进制的加法，那现加的结果是这样的。接下来再把这个加法的结果和第3个16比特进行相加的运算，加法运算得到的结果是这样的那在这个加法运算当中出现了一个比较特殊的情况，就是最高位的这个二进制加法运算产生了往更高位的进位。当我们遇到这种情况的时候，需要把往更高位的进位让他加回到最低这一位，我们把这个过程称为回卷，把最高位的一加回到最低位，那么一加一本为何等于0？往更高位进一，一加一本位和等于零往更高位进1 0加1本位和等于一往更高位进0。也就是说再往前的这些比特就不会再发生改变了。

发言人   05:00

好，这是最高位回卷的意思。好，现在这个运算结果就是原始的3个16比特数依次相加，并且考虑了回卷得到的一个计算的结果。那么把这个计算的结果再进行逐位的取反，零变一一变零，逐位取法。这样的话就得到了16比特的校验和。好，那么发送方就可以把这个原始数据携带上16比特的校验和，一起发给B结点。

发言人   05:34

对于接收方来说，他去检测比特错误的方法也是雷同的。反正就是把它收到的这些内容以16比特为一组依次进行二进制的加法。但是需要注意我们这儿说的二进制加法，如果最高位产生了进位的话，需要把它回卷到最低位，要考虑这个运算。显然如果没有任何一个比特发生错误的话，那么前3个16进制数相加的结果肯定还是这样的一个结果，对吧？那最终再加上16比特的这个校验和，那得到的运算结果肯定就是16比特的权益。好，所以如果接收方加法运算的这个结果为16比特权益，那么就说明没有发生比特错误。而如果不是16比特权益，那就说明发生了比特错误。好，所以从这个例子当中我们可以看到，我们前边设计的这种方法，即便原始数据变多也是完全适用的。另一方面在这个例子当中，我们强调了进行二进制加法的时候，一定要注意最高位产生的进位需要回卷。

发言人   06:47

好，那么理解了这种差错校验的方法之后，接下来就可以正式的介绍UDP校验它背后的一个过程了。首先在UDP数据报的首部这个位置，末尾的两个字节表示的就是UDP数据报的校验和，两个字节对应的就是16个比特。在计算校验和之前，数据的发送方会首先在UDP数据报的前面增加12个字节的尾首部。展开来说，这12个字节包含四个字节的源埃批地址，四个字节的目的埃批地址。然后一个字节的全0，一个字节的无符号数十7，最后还有两个字节表示的是UDP长度，为首部当中的UDP长度这个字段和真正的首部当中的这个UDP长度字段，二者是相等的。而原埃批地址和目的埃批地址这两个字段的信息就取决于发送方的埃批地址和接收方的埃批地址，就把它填进去就行了。关于为什么要添加为首部，大家不用纠结，反正你只需要记住在计算校验和之前会添加12个字节的尾首部，一起去进行这个校验和的运算，知道这一点就行了。

发言人   08:09

好，那么添加了为首部之后，接下来就可以计算16比特的校验核。计算的方法和刚才介绍的如出一辙，我们会把尾首部首部还有UDP的这个数据部分，反正把所有的东西全部用于校验和的计算，以16比特为一组，依次进行二进制的加法。再次强调在进行二进制加法的时候，最高位如果产生了进位的话，那么需要回卷到最低位，加回最低位就是这个意思。好，经过这一步处理，最终就得到了16比特的加法运算结果。

发言人   08:46

那我们还需要把加法运算的结果逐位取反，取反之后就得到了16比特的校验和。然后把这16比特的校验和填到UDP的首部这个位置。好，那么校验和确定好之后，发送方的传输层就可以把刚才临时添加的这个尾首部给去掉，把它给拆掉。拆掉之后再把整个UDP数据报交给网络层，然后让网络层封装成埃批数据包进行传输。好，这是发送方的传输层要做的事情。比较特别的点就在于计算校验和之前需要先添加一个临时的为首部。而计算完校验和之后，再把这个尾首部给拆掉。所以这个为首部实际上并不会在网络层进行传输。

发言人   09:39

好，那接下来看接收方的传输层要如何去进行UDP校验，接收方的传输层会收到一个完整的UDP数据报，其中包含了首部和数据这样的两个部分。在首部的末尾16个比特携带了校验盒的信息。接下来接收方会检查整个UDP数据报有没有发生比特错误。

发言人   10:04

显然由于发送方在计算校验和的时候添加了一个为首部，所以接收方在进行插座检测的时候，也需要把这个为首部重新给构造出来。那这个为首部当中包含的信息，和发送方那边构造的为首部是一模一样的。好，临时添加了这个尾首部之后，接下来就可以把尾首部首部还有这个UDP的数据部分，把所有的这些部分全部放在一起进行这个差错的检测。具体来说就是以16个比特为一组进行二进制加法，最高位产生的进位需要回卷。如果最终加法的结果为全一就说明没有比特错误。于是就可以接收这个UDP数据报，并且把UDP数据报的数据部分，根据目的端口号的信息交付给应用层当中的某一个进程。

发言人   11:04

另一种情况，假如差错检测没有通过，也就是刚才说的那个加法的结果不是全一的话。那么就说明这个UDP数据报在传输的过程当中发生了比特错误。此时接收方的传输层就会直接丢弃这个UDP数据包，那它里边携带的这个数据也就不会再交给应用层。好，这是接收方的传输层进行UDP检验的一个过程。

发言人   11:32

好，最后2024年的真题39题，这个题目大家可以暂停来练练手，自己动手算一遍，把二进制带进去，一个比特一个比特的去计算，那你算出来的结果是什么呢？显然应该是C选项。其实24年的39题这个题目就是我刚才这个例子的来源。所以24年的这道题目检验和的计算方法和刚才罗列的这个式子如出一辙。如果算错的同学可以结合课件再检查一下是哪个环节出了问题。

发言人   12:07

好的，在这个视频中我们介绍了UDP检验的方法。首先我们介绍了一种新的差错检验的算法。首先我们需要用原始数据去计算检验和具体来说就是把原始数据以16比特作为一个组，把各组数据进行二进制加法。在加法运算的过程当中，如果最高位产生的进位需要进行回卷，加回到最低位，那加法运算的最终结果我们再把它逐位取法，就得到了16比特的校验和，这是校验和的计算。在检测差错的时候也是类似的方法，接收方会把收到的内容以16比特为一组，同样的也是进行二进制的加法。如果加法的结果为全一就说明没有差错，如果加法的结果不是全一就说明有差错。基于这样的差错检测算法，我们又补充了UDP校验当中的一些要点。

发言人   13:06

UDP校验的核心思想和上面介绍的这种差错检测的方法是如出一辙的。只不过在UDP校验当中，发送方在计算校验和之前需要先添加一个临时的尾首部，在计算完校验核之后再把为首部拆掉。那相应的对于数据的接收方来说，在进行差错检验之前，也需要添加这个为首部，让为首部一起参与差错的检验，检验完了之后再拆除为首部。对于数据的接收方，如果说检测出这个比特错误，那么就直接把UDP数据报给丢弃。如果说没有比特的错误，那么就会接收这个UDP数据报。并且根据UDP数据报当中的目的端口号去决定这个数据包当中携带的这些数据应该交给应用层当中的哪个进程。好的，这就是UDP检验这个考点。

发言人   14:06

最后我们回顾一下网络层。当我们在介绍IP数据报的时候，我们说IP数据报的首部这个位置添加了首部校验和，同样也是16个比特。埃批数据报在传输的过程当中，只检查手部当中有没有发生比特错误。检验的方法其实和这节课介绍的UDP的这种检验方法如出一辙。具体来说就是把埃批数据报的整个首部分别以16比特为一组进行二进制加法。在进行二进制加法的时候，也需要考虑到回卷这种情况，也就是如果最高位产生进位的话，那么需要把最高位的进位加回到最低位，那嘉禾的结果最终在逐位的取反。就可以得到埃批数据报的首部校验和这16个比特的信息。

发言人   15:02

注意IP数据报的首部校验和的计算，只有IP数据报的首部会参与。也就是IP数据报的数据部分，我们是不参与到首部校验和的计算当中的。并且在IP数据报当中计算首部校验和的时候，不会在IP数据报的前面再添加一个为首部，没有这样的多余步骤。所以IP数据报的这个首部校验和，它的计算方法本质和UDP是一样的。但是背后的细节有一些区别，UDP会添加一个尾首部，另外UDP的校验范围是包含了首部和整个数据部分。好的，那么大家可以再消化一下。