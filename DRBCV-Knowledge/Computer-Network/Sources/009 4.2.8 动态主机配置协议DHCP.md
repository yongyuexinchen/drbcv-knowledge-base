# 009 4.2.8 动态主机配置协议DHCP

> Source: 009 4.2.8 动态主机配置协议DHCP_原文.docx

009 4.2.8 动态主机配置协议DHCP_原文

2026年07月19日 12:21

发言人   00:00

好的，在这节课中我们要探讨动态主机配置协议，英文缩写叫DHCP。首先直接给结论，所有的DHCP相关的重要考点，我都在这个思维导图当中用彩色字体给大家标注出来了。我们要了解DHCP的一些基本概念，以及要理解DHCP背后工作的一个过程，主要分为1234这样的四个步骤。一会儿我会结合具体的例子带大家理解背后的过程。

发言人   00:35

好，首先看DHCP它的作用。当一台主机刚接入网络的时候，它肯定是还没有埃批地址的对吧？所以我们需要给这种刚接入网络的主机分配一个埃批地址。当然除了埃批地址之外，还需要给它配置默认网关，配置子网掩码这些东西。所有的这些和网络相关的这些参数的配置，都是通过DHCP协议来自动的完成的。

发言人   01:05

好，首先来看一下DHCP在协议栈当中处于什么位置。可以看到DHCP协议它其实属于应用层，对吧？那为什么我们的考研大纲把DHCP协议放在网络层这个部分来学习呢？因为DHCP协议它的存在主要就是为了给这个网络层所需要的一些东西进行初始化的工作，所以才把DHCP放在网络层这边来学习。但是我们要知道它其实属于应用层。那么应用层的DHCP协议会使用传输层的UDP协议提供的服务，UDP协议又会使用网络层的埃批协议提供的服务，而埃批协议又会使用数据链路层当中的某一种就是链路层的协议提供的服务。所以当DHCP协议相关的数据在网络当中传输的时候，其实需要经过层层的封装。

发言人   02:04

需要先把应用层的这个DHCP协议所定义的这个数据结构，也就是DHCP报文作为一个整体封装到UDP里边。那UDP协议封装的这一整坨数据又要交给网络层再封装成埃批数据报。网络层的埃批数据包再交给链路层封装成mac真好。

发言人   02:27

接下来用一个具体的例子来感受DHCP协议工作的过程。首先在这个例子当中，学校这个单位它划分了两个局域网。右边是一个局域网，左边是一个局域网。换句话说这是两个相互独立的子网，对吧？那么两个子网，他们可以分配的这个IP地址是不一样的，对吧？为了管理每一个子网当中的这个埃批地址资源，我们通常来说会给每一个这种局域网配备一个DHCP服务器。你可以理解为这是一台专门的主机。

发言人   03:10

好，那么不同的子网当中的这个DHCP服务器，他所负责管理的IP地址资源显然是不同的那在这个例子当中，我们可以看到学校的这两个子网它的网络前缀总共是占17个比特。左边这个子网它的网络前缀应该是这样的一串。前16个比特是166.1，第17个比特是零。因此左边这台DHCP服务器，它有权去分配的埃批地址资源，就应该是这个子网当中的所有的埃批地址。比如说他给自己分配了116.1.0.9，他给自己分配了这样的一个埃批地址。然后给H一分配了点0.1，给H二分配了点4.2。所有的这些埃批地址都是这个DHCP服务器从自己的埃批地址池里面拿出来的，分配给这些主机。

发言人   04:13

类似的道理对于右边的这个子网来说，我们也会给这个子网安排一台DHCP服务器。这台服务器它负责管理的埃批地址池的资源就是前17个比特是哒哒哒哒哒第17位等于一对吧？跟左边那个子网不一样。然后末尾的这15个比特，就是可以由这个DHCP服务器自己去分配的。当然了末尾的15个比全0和全一这两个特殊的地址是不能分配给某一个特定的主机去私用的对吧？这一点我们之前强调过很多次。好，这是DHCP服务器在一个局域网内的地位。

发言人   04:55

好，接下来我们做这样的一个训练。假设H三这台主机是刚接入网络的主机，接下来我们会分析DHCP协议工作的一个过程。好，我们来看一下这个图，H三这台主机是新接入网络的那它本身拥有一个mac地址，但是他此时还没有被分配埃批地址，没有被配置子网掩码，也没有被配置默认网关这些东西对吧？好，那么当它新接入网络之后，他会向DHCP服务器请求给自己分配自己需要的这些东西。具体来说分为四个步骤。

发言人   05:35

我们看这张图，左边这条线client表示的是客户，也就是刚加入这个网络的主机。右边server表示的是服务器，它对应的就是DHCP服务器。那么动态配置埃批地址的这个过程需要经过四个步骤。

发言人   05:55

第一个步骤是啊这个客户，也就是新接入网络的这台主机，要给服务器发送一个DHCP的discover，也就是DHCP的发现报文，这是第一步。第二步服务器收到这个DHCP发现报文之后，要给这台主机返回一个DHCP offer。Offer这个词一般来说是翻译为提供，就是服务器给客户主机返回DHCP提供报文。第三步客户主机要给服务器发送DHCP request报文。Request一般来说翻译为请求，也就是DHCP请求报文。第四步服务器又给客户端返回DHCP acknowledge，这个一般翻译为DHCP确认报文。好，这是一个总的流程，分为四个步骤。接下来我们展开看一下每一个步骤的背后发生了哪些事情。

发言人   07:00

先给大家看最终的这个图做一个解释。接下来大家会看到1234会看到这样四块很大的东西。那这儿的12三四分别对应的就是刚才我们提到的发现报文、提供报文、请求报文和确认报文。并且每一个DHCP报文它都需要被封装到UDP协议里边，然后再交给网络层封装成埃批数据报。网络层再交给这个链路层，最终会封装成mac真好。所以1234这几个报文我们都是详细的描述了它从应用层到传输层，到网络层再到链路层，分别是怎么层层封装的。

发言人   07:59

考试中比较喜欢考察的是各个DHCP报文的作用，以及这些DHCP报文被封装成埃批数据报的时候，这个目的地址原地址应该怎么填。还有就是在电路层被封装成mac帧的时候，目的地址和原地址又应该怎么填？所以我在这儿拓展的这些细节，虽然看起来很细碎，但是考试考的就是这么深。因此还请大家耐下心来，我们一起把内部的这些细节给抠一抠。

发言人   08:33

好，现在我们回到这张图。H三是新接入网络的一台主机，它此时还没有被要分配埃批地址，没有被配置子网掩码和默认网关这些东西。好，那么显然应该给H三提供这个埃批地址分配服务的应该是右边这台DHCP服务器，对吧？那在这台DHCP服务器上会运行着一个进程，我们姑且就把它称为DHCP服务器进程。这个进程对应的这个通信端口号是67点，而H三这台主机里边也会运行一个进程，叫做DHCP客户端进程。它所对应的端口号是68。接下来H3内部的这个客户端进程会和DHCP服务器的这个服务器进程二者相互通信，最终完成埃批地址、子网掩码还有默认网关的配置。

发言人   09:31

好，先来看第一步，客户主机首先要给DHCP服务器发送一个DHCP发现报文，discover DHCP发现报文，发现报文长这个样子。那么在这个DHCP发现报文当中，H三这台主机需要说明自己的这个mac地址等于多少，相当于给这个DHCP服务器。自报家门对吧？把身份证号告诉他，我的身份证号是什么？AABBCCDDEE33，这是我的身份证号。初来乍到新接入这个网络，请给我分配个埃批地址。

发言人   10:08

好，这是DHCP发现报文当中需要包含的比较重要的一个内容。由于这个DHCP报文它属于应用层，对吧？它属于应用层，因此接下来这个应用层又会交给传输层的UDP协议。UDP协议把这个报文封装成一个UDP数据报。

发言人   10:31

在传输层需要关注的是端口号的信息。H三和这个服务器的通信本质上是H3上面的一个进程和DHCP服务器上面的另一个进程的通信，对吧？那么H三的这个进程它的端口号是68，而DHCP进程它的端口号是67点。所以在这个DHCP发现报文当中，目的端口号应该指向的是DHCP服务器当中的那个进程的端口号67。而原端口号应该指向的就是H3，它的这个进程所对应的端口号68。好，这是传输层的封装。

发言人   11:11

接下来重点来了，传输层又交给网络层封装成IP数据报，由于这个H3结点此时暂时还没有IP地址，对吧？所以在IP数据报当中，这个源地址就只能先填0.0。还记得全灵这个特殊的埃批地址有什么作用吗？它表示的是本网络上的本主机，如果一台主机暂时还没有自己的这个埃批地址的时候，他就只能说我我的埃批地址是0.0。I am nobody, 对吧？我谁都不是我现在还暂时没有埃批地址。好，所以这是原埃批地址需要注意的地方。接下来目的IP地址应该写成全一，那么全一的这个IP地址它表示的是这个IP数据报希望向本网络内的所有节点去广播。

发言人   12:11

好，思考一下为什么我们想一下，因为H三刚加入这个网络对吧？他连自己的埃批地址都不知道，那他有可能知道DHCP服务器的这个埃批地址吗？也不知道对不对。所以当他想要给DHCP服务器发送这个DHCP发现报文的时候，他就只能用广播的形式去寻找，对不对？

发言人   12:37

好，所以这个目的埃批地址要写成广播埃批地址，32比特全一好，那么这是H3主机的网络层做的一个封装。接下来网络层又需要交给数据链路层，对吧？网络层又要交给链路层，把埃批数据包封装成mac帧。好，那么来看一下在这个mac帧当中，首先原地址不用说了，H3本身就拥有自己的mac地址，所以原地址就写H三自己的那目的地址这块会写成48比特全一的。目的mac地址表示的是这是一个广播帧对吧？这是一个广播帧，那道理是一样的。因为H3不知道这个DHCP服务器它的mac地址等于多少，它的这个埃批地址等于多少。所以我只能广播去找，没办法。

发言人   13:28

好，所以这样的话H三这台主机就完成了第一个报文的封装。我们详细阐述了第一个报文从应用层到传输层，再到网络层，再到链路层，每一层分别应该如何封装好。由于最终是被封装成了一个广播mac帧，因此这个报文它会被广播到网络当中的各个节点对吧？每一个节点都会收到携带着DHCP报文的mac帧。但是最终只有DHCP服务器的这个服务器进程才会真正的接收这个DHCP发现报文。

发言人   14:13

好，来解释一下背后的过程。我们先分析H六这台主机，当它收到这个mac帧的时候，它是怎么处理的。首先H6主机它的链路层收到这个mac帧之后，显然会把这个mac帧接收下来，对吧？因为它的目的地址是全依，全音意味着这是一个广播帧，网络中的所有的这个节点的链路层都会接收它。好，接下来H6的链路层收到这个mac帧之后，首先把这个mac帧掐头去尾，然后就得到了埃批数据报对吧？那检查IP数据报发现也比较奇怪，这个目的IP地址也是一个广播地址，对吧？那广播地址是不是意味着我的网络层也应该把这个IP数据报给接收下来？好，接下来继续去拆解网络层，接收这个IP数据报之后，把IP数据报的首部给去掉，然后就得到了这个UDP数据报，也就是到了传输层。

发言人   15:22

从UDP数据报的首部当中可以看到，这个UDP数据报它的目的端口号是67点。但是H6这台主机上并没有任何一个进程绑定着67这个端口号。所以到达传输层这一步，H6终于发现问题不对了。这个UDP数据报肯定不是发给我的，对吧？因为我没有端口号为67的进程，于是到达传输层之后，这些数据就会被H六给丢弃。好，那你可能会觉得奇怪，H6不能把67这个端口号绑定到自己的某一个进程上吗？那结论就是这个特殊的端口号是不可以被使用的。

发言人   16:06

因为在互联网技术当中，一些比较重要的特殊的这种进程，会专门被分配一个特定的这种端口号。这是互联网的技术标准去制定的一个规范。也就是说67这个端口号它只能被DHCP服务器进程这种特殊的进程去使用，而不能被一台主机上面自己编写的这个普通进程去使用。好，所以这就确保了普通的这些主机，当它收到这个UDP数据报并且检查目的端口号的时候，在他自己的内部肯定找不到一个和67相互绑定的进程。好，所以虽然所有的这些主机节点都会收到这个mac真，但是最终只有DHCP服务器会把这个报文给接收下来，并且做出响应。

发言人   17:01

好，那么现在来到DHCP服务器这边，它从这个mac帧当中，就像剥洋葱一样一层一层的剥开，最终就播到了这个DHCP发现报文。好，从这个发现报文当中，DHCP服务器知道，现在有一个新人加入，这个新人的mac地址是什么？三三这个mac地址，那么接下来我会尝试着给他发一个offer，发一个offer就是给他提供一个埃批地址。

发言人   17:32

好，于是就到达了第二步。那么DHCP服务器它从自己管理的这个埃批地址池里边找到一个暂时还没有被分配出去的空闲埃批地址。比如说128.2这个埃批地址现在还空闲，那么接下来会尝试着把128.2这个埃批地址分配给这台新主机H3好，那么来看一下这个DHCP提供报文里边需要指明的一些东西。首先肯定要指明给这个新主机分配的埃批地址等于多少，另外还需要指明这个埃批地址的租用期等于多少。租用期的意思是说这个埃批地址你可以使用多长时间。所有的主机对埃批地址的使用都是有这个时间限制的。如果租用期快到了，租用期快到了，那么一台主机可以续租这个埃批地址，也就是需要再跟这个DHCP服务器打一个招呼，你给我分配的这个埃批地址，我希望续租，续租一年或者续租几个小时，类似于这样。好，所以租用期指的就是给这个新主机分配的埃批地址，它可以使用多长时间，就是这么个意思。

发言人   18:45

接下来还需要告诉新主机你所处的这个子网子网掩码等于多少。来看一下，在学校的右边这个网络当中，我们可以看到它的末尾的15个比特是主机号前边的更高位17个比特是网络前缀对吧？所以在这个网络当中的所有的节点，它的子网掩码肯定就是17比特的1加15比特的0，对不对？那用十进制表示的话就是255.128.0。

发言人   19:22

好，另外还需要告诉这个新主机默认网关的埃批地址等于多少。默认网关是B2这个接口，它的埃批地址是128.1。好，所以这个东西也需要告诉他。

发言人   19:37

这是DHCP提供报文里边包含的内容，接下来需要把应用层的这个数据交给传输层进行封装。传输层同样使用的是UDP协议，UDP协议会把这些数据封装成一个UDP数据报，并且写入目的端口号和源端口号。由于这个报文是要从服务器发给这个客户端的，所以源端口号应该写的就是67，目的端口号是68，就是这个样子。好，接下来传输层再把这些数据交给网络层，封装成IP数据报。由于此时这个H3结点暂时还没有埃批地址，所以牧的埃批地址就只能先写成广播。那另一方面，原埃批地址直接写DHCP服务器，它自己的埃批地址就行了，对吧？虽然对方还没有埃批地址，但是我自己是有埃批地址的。所以原埃批地址写自己的目的IP地址写成广播IP地址。

发言人   20:38

好，接下来又到了链路层，链路层这个地方出现了区别。由于由于这个H3在刚开始的时候已经告诉DHCP服务器自己的mac地址等于多少了，对吧？所以这个DHCP服务器它可以直接把目的mac地址写成H3的地址，而原mac地址就是写自己的这个mac地址。也就是说这个DHCP提供报文，它在网络层是被封装成了一个广播埃批数据报。但是在链路层它其实是被封装成一个单波，真的好。所以对于这个单波帧来说，它经过这个交换机的转发会精准送达H3，其他结点都不会收到这个单波帧，对吧？

发言人   21:33

好，这样我们就完成了第二步。DHCP服务器给这个客户主机发了一个DHCP offer。好，现在H3收到了这个DHCP提供报文。

发言人   21:46

接下来要进入第三步，需要给DHCP服务器发送一个request，也就是请求报文。那么第三步是这个样子。好，首先来看一下请求报文如何构造。在DHCP请求报文当中，H三这台主机需要说明，我是谁谁谁，我接受的埃批地址是多少？这个埃批地址128.2是刚才这个DHCP服务器给他返回的这个报文当中告诉他的对吧？128.2你就去用这个IP地址。好，所以H三要告诉这个DHCP服务器我是谁，我接受的埃批地址是多少。接下来又到了传输层，封装成UDP数据报，这就不再赘述。

发言人   22:32

好，重点看网络层和数据链路层到网络层这个部分。由于H3现在他虽然说已经在走这个埃批地址申请的这个流程了，但是这个埃批地址直到第四步完成之前，这个IP地址还暂时不属于H3。所以H3现在其实还是一个nobody。所以在网络层这个地方，H3封装的这个IP数据包源地址还是只能先写成全00.0，那么目的埃批地址要写成全11个广播数据报。为什么要这么做？我们一会儿再来解释。大家先记住这个结论，这个有可能在选择题或者大题当中作为一个小问来考察。

发言人   23:21

好，最后mac帧这个地方也需要把它包装成一个广播帧，广播mac帧也就是目的mac地址需要写成48比特全一好，你现在可能会觉得有点奇怪，这个DHCP服务器在第二步给H三返回的这个报文当中，难道就不能告诉他自己的埃批地址和mac地址等于多少吗？为什么非要让H3在第三个步骤的时候还是去进行广播呢？我点对点的发送不好吗？好，原因是这样的，DHCP服务器这个东西，对于一个比较大型的局域网来说，比如说在你们学校有可能会有很多台DHCP服务器去负责IP地址的分配。

发言人   24:12

好，那么在这种情况下，我们来思考一下，如果一个学校内部有多台DHCP服务器，不同的DHCP服务器，它所负责管理的埃批地址资源各不相同。比如说这个服务器它负责管理1000个埃批地址，另一台服务器负责管理另外的1000个IP地址，每个服务器的管辖范围各不相同。在这种情况下会发生的一种情况就是当一台新主机在第一个步骤广播这个DHCP发现报文的时候，这个DHCP发现报文会被网络内的所有DHCP服务器所收到，对吧？那么每一台DHCP服务器都以为新节点是在向自己申请埃批地址，因此所有的DHCP服务器都会给这个新节点返回DHCP offer报文。就是我给你分配一个IP地址，另一个服务器说那我也给你分配一个IP地址。好，所以对于H3来说，它有可能会收到多个DHCP服务器返回的这个提供报文，对不对？好，那在这种情况下，H三这个节点是不是得广而告之，告诉这些服务器我最终选用的埃批地址是多少，不然的话就乱套了，对吧？每一台服务器都以为自己已经把埃批地址分配出去了，但是事实上H3最终选用的埃批地址只有一个。

发言人   25:52

那这也是为什么这个DHCP请求报文需要用广播的形式告诉全网的原因。因为在现实的这个网络当中，同一个局域网内部可能会存在多台DHCP服务器，所以这个新节点需要广而告之，告诉所有的DHCP服务器。我最终接受的是谁给我分配的埃批地址。好，就是这个原因。好，所以这个地方埃批地址要写成广播埃批地址，目的mac地址也要写成广播的mac地址。好，这是第三个步骤。

发言人   26:29

好，接下来就可以进入到第四个步骤。这台DHCP服务器收到了第三个报文，接下来要返回第四个报文，就是确认报文。第四个报文长这个样子，其实第四个报文和第二个报文它俩的内容基本上是一样的。相当于再把这个给H三分配的埃批地址、租用期、子网掩码、默认网关这些东西再一次给H3发回去。就相当于告诉他，我现在确认了这些东西都是你的，这些东西都是你的。

发言人   27:06

好，那么这个确认报文被封装到UDP里边，然后又被封装到这个IP数据报里边。好，原埃批地址写的是服务器自己的埃批地址，而穆的埃批地址写的还是广播地址，原因和之前一样对吧？H3现在正在走申请埃批地址的流程，但是这个流程还没走完，所以他其实还暂时没有拥有埃批地址。好，因此第四个报文它所对应的这个埃批数据报目的地址也只能先写成全一好。

发言人   27:41

最后到了这个链路层，链路层会把这个IP数据包封装成一个单波帧。因为这个DHCP服务器它本来就知道新主机的mac地址等于多少，对吧？所以就只需要用一个单波帧把它发回去。所以第四个报文被封装成帧之后，先交给交换机，交换机精准送达给到H3好。H3在收到这个确认报文之后，他就真正的拥有了埃批地址，拥有了子网掩码，拥有了默认网关的配置。好，这就是DHCP协议在背后工作的过程。

发言人   28:20

大家可以再结合这张图进行回顾。对于考试来说比较重要的东西，我都总结在了这张思维导图里边。DHCP协议属于应用层，它是基于UDP的。并且它使用了客户服务器模型，也可以缩写为CS模型。C对应的是client，s对应的是server CS客户服务器模型。

发言人   28:43

DHCP的客户指的就是新接入网络的那台主机，也就是希望获得埃批地址的那台主机。而DHCP服务器的话就是负责分配埃批地址的那台主机。每一台DHCP服务器会负责管理一系列的埃批地址池。刚才我们说在一个大型网络内部，有可能会有多台DHCP服务器，这个是可以被允许的。只是不同的DHCP服务器，它所负责管理的埃批地址池也各不相同，各管各的那值得一提的是，在家庭网络当中，通常是由我们的家用路由器去兼职。DHCP服务器的一个作用就相当于你们家的这个路由器，它的内部其实正在运行一个DHCP服务器进程，内部正在运行这样的一个进程。所以我们家用路由器它的功能其实是很综合的。

发言人   29:40

DHCP这个部分一定要记住这四个过程，每个过程使用的DHCP报文类型不一样。第一个步骤是客户端给服务器发送DHCP发现报文。第二个步骤是服务器给客户返回DHCP提供报文。在这个提供报文当中需要给客户主机指明我给你分配的埃批地址是多少，租用期是多少，子网掩码是多少，这个默认网关需要配置为多少，这些信息都在DHCP提供报文里边，要说明清楚。接下来第三步，客户会向服务器，发送这个DHCP请求报文，相当于要告诉整个网络内的所有的DHCP服务器，我最终选择使用的埃批地址是多少。好，最后第四步，服务器给客户端返回DHCP确认报文。

发言人   30:35

在这四个步骤当中，网络层和链路层的原埃批地址、目的IP地址、源mac地址、目的mac地址，每一个步骤应该如何去设置？这个是很喜欢去考的一个点，所以还请大家注意。好的，以上就是DHCP的所有内容。