# 022 4.4.4_2 OSPF的基本工作原理

> Source: 022 4.4.4_2 OSPF的基本工作原理_原文.docx

022 4.4.4_2 OSPF的基本工作原理_原文

2026年07月19日 12:22

发言人   00:00

各位同学大家好，在这节课中我们要继续探讨OSPF协议的基本工作原理。通过之前的学习，我们知道对于一个使用了OSPF协议的自制系统来说，每一台路由器可以直接探测到自己和哪些节点相连，以及自己和相邻的这个节点之间的边权值等于多少。这些都是一台路由器可以独立探测到的信息。比如说R一探测到这样的一些信息，R6探测到这样的一些信息，每一台路由器会把自己探测到的这个链路状态信息通过宏方法迅速的广而告之。所有的路由器都这么做，最终就会使得各台路由器都能够迅速的收到，其他路由器发来的这些链路状态信息。

发言人   00:54

把所有路由器探测到的链路状态信息全部都整合起来之后，就形成了我们非常熟悉的邻接表。邻接表是数据结构课程当中学习的一种图的存储方法。接下来我们把邻接表这个术语换一个名字。在OSPF协议当中，这个邻接表会被称为链路状态数据库，英文缩写叫LSDB links state data base。链路状态数据库其实就是把每一个路由器探测到的这些信息全部整合在一起，就组成了一个完整的链路状态数据库。我们知道由于使用了这种宏饭机制，所以每一台路由器都可以迅速的建立一个完整的链路状态数据库。

发言人   01:48

好比如说R一这台路由器，它构建好自己的链路状态数据库之后，就可以基于这个链路状态数据库运行迪杰斯特拉算法。从而就可以算出从R一出发到达net 0的最短路径路径的总长度是二，到达net一的最短路径是直达，那这条边的权值等于一好，再比如到达net 4，这个网络最短的路径是R1，先到R3，然后再到24，最后到达net 4，就是这样的一条转发路径。好，再比如到达net 6，这个网络可以找到的最短路径有两条。首先是从R1到R3再到R5R6最后到达net 6，这是第一条可选的路径。还有一条路径是从R3到达R4，R4到R6最后再到net 6。也就是说从R一出发到达net 6这个目的网络，我们可以找到两条等价的路径。不过对于R一来说，无论走哪条路径，下一跳路由器总是R3这台路由器好，那么到达其他目的网络的最短路径，这儿就不再赘述。

发言人   03:11

对于迪杰斯特拉算法它的运行过程，忘了的同学可以回顾数据结构相关的内容。好，总之迪杰斯特拉算法可以计算出从一个单一节点出发，到达每一个目的网络的最短路径等于多少。那么基于迪杰斯特拉算法运行的这个结果，接下来R一路由器就可以构建自己的路由表。那R一的路由表最终应该长这个样子。可以看到OSPF协议它所规定的路由表格式至少包含这样的三个关键信息。首先是目的网络，然后是下一跳路由器以及到目的网络的总距离。那目的网络这个部分我用了net 1、net 2，什么net 0和这张图是对应的这是一个简写。

发言人   04:03

本质上这样的一个目的网络，我们是用IP地址和子网掩码这两个信息去表示的。比如说net一这个目的网络，我们可以用这样的一个IP地址加上子网掩码26比特的1，用这种方式去表示。由于R1和net一是直连的，所以从路由器R一出发到达net一的下一跳路由器，我们没有写任何路由器的地址，它属于直接交付这种类型。好，再看对于net二这个目的网络，R1先要把IP数据包转发给R2，R2再转发给net 2，总距离是3加1等于4，所以这个地方距离信息写为了4，并且下一条路由器是R2。好，再看最后一行net 0这个网络。我们把它对应了自治系统外部的整个互联网，可以用一个默认路由去映射net 0这个网络。

发言人   05:09

如果路由器R一收到一个埃批数据报，那么它会根据这个IP数据报的目的，IP地址分别去对比这些目的网络号是否能对得上。如果所有的目的网络号都对不上，那么最终一定能和这个默认路由相匹配，那最终这个埃批数据包就会被发往net 0。那么在net零这边通常是会有一个其他自制系统的路由器和本自制系统的这个R一路由器会有直接相连。所以下一跳路由器的信息通常会记录为与之相连的外部路由器它的埃批地址。由于这个图当中没有画出这个外部路由器，所以我们就直接记为外部路由器，大家知道我想表达的意思就行。

发言人   06:04

好，从这个路由表当中可以看到，虽然运行迪杰斯特拉算法之后，路由器R一它完全可以知道从自身到达任何一个目的网络的完整的路径应该怎么走。但是在路由表当中并不需要存储完整的路径，只需要存储下一跳路由器的地址就可以了。比如说到达net 6这个目的网络有可能是走R3、R5、R6这条路，有可能是走R3、R4、R6 net 6，也有可能是这条路。但是对于二一来说，这个IP数据报最终到底要往下走还是往右走，他其实不用关心。对于R一来说，无论走哪条路，反正他只需要把IP数据报丢给R3这台路由器即可。那这两条路路径的总长度总距离都等于9。

发言人   07:00

好，这是值得注意的一点。虽然迪杰斯特拉算法的运行结果会得到完整的转发路径，但是路由器只记录下一跳的信息，而不会存储完整的路径。另一方面，在这个自制系统内，一旦发生了网络拓扑的变化，比如说链路的权值发生了改变，或者某些链路断开了，某些路由器坏掉了。一旦发生这些事情，那这种网络拓扑结构的变化都会立即引发红饭。无论是链路的变化还是路由器的变化，这些变化总能被自己直接相邻的这个路由器所探测到，对吧？那么一旦这个邻居探测到变化之后，它就会立即红饭，从而就会引起每一台路由器它的LSDB，也就链路状态数据库发生变化。而一旦这个链路状态数据库发生了变化，那么路由器就会重新运行。迪杰斯特拉算法会基于最新的网络拓扑结构去重新计算最短路径，并且更新路由表。

发言人   08:10

好，现在我们已经了解到了，对于一台路由器来说，他要构造自己的路由表，第一步是要先构造本地的这个链路状态数据库。换句话说就是要构造自己的邻接表。基于自己的邻接表再来运行迪杰斯特拉算法，计算出最短的路径，最终再根据迪杰斯特拉算法的运行结果来构造我们的路由表，就是这样的一个过程。

发言人   08:41

好，在这个地方有一个细节值得跟大家提一嘴。我们在数据结构课程当中学习的迪杰斯特拉算法，就是基础版的迪杰斯特拉算法，他只能找到从起点到达这个终点的一条最短路径。但是在这个地方你会看到，我们OSPF协议它运行的是改进版的迪杰斯塔算法。这种改进的算法可以支持找到多条等价的最短路径。这个地方有必要跟大家解释一下，以免大家回去看数据结构的时候产生疑惑。

发言人   09:19

好，接下来我们考虑这样的一个问题。我们说自制系统内的每一台路由器，都需要在本路由器内部保存一份这种链路状态数据库。我们举的这个例子，闲鱼电信这个自制系统，它里边的节点链路的数量不算多，对吧？所以这个邻接表的大小看起来还能够接受。但是如果是一个超大型的自制系统，里边就会有很多很多的路由器。比如说一些公有云服务商，他们构建的自制系统规模内部可能包含5万甚至10万台的路由器。想一下这么大的自制系统，如果我们用一个统一的连接表来表示它的拓扑结构的话，是不是会让这个数据库非常庞大，并且每一台路由器都要存储一份数据库的信息，这显然会让路由器们有点压力，对不对？

发言人   10:25

除了数据库会变大会变复杂之外，其实自制系统变大之后，红泛的压力也会变大。因为我们说红泛法本质上就是一个路由器把自己探测到的邻居的信息一传十，十传百传到全网，对吧？但是如果全网有5万到10万台的路由器，那么每一台路由器探测到的信息都需要全网广播。那这就会使得自制系统内的这个红泛消息数量变得特别巨大。所以自制系统太大了之后，红泛的压力也会变大。另一方面迪杰斯特拉算法它的运算开销也会变大，对吧？

发言人   11:11

那么对于这个问题，OSPF协议是如何解决的呢？说来也简单，OSPF协议支持区域划分的这种功能。比如说外面的这个方框是闲鱼电信构建的自制系统在整个自治系统内使用了OSPF协议。那么OSPF协议支持我们把自制系统划分成多个区域。比如说在这个例子中，我们划分为了区域0、区域一、区域2、区域3，划分成了总共四个区域。每个区域都会用一个32比特的区域标识符去标记。那么划分区域之后，红方法需要交换的链路状态信息的范围就局限在了各个区域的内部。

发言人   12:06

比如说R2它要把自己探测到的链路状态信息红饭出去。那么他在红饭这个信息的时候也会指明自己所属的区域号，区域标识符等于多少。那么他红泛的这个信息显然会被R3收到，也会被R1收到。R3收到这个红泛信息之后，由于这个宏观信息仅在区域一的内部进行广而告知。所以R3虽然它同时也和区域零相接，但是他并不会把这个红泛信息往区域零这边发送，并不会因此红泛的链路状态信息被局限在了这个区域的内部。这样就可以减少宏饭带来的网络通信量。

发言人   12:57

另一方面也可以让每个路由器的LSDB更小。比如说对于R2这台路由器来说，他只需要关心本区域内的网络拓扑结构就行。换句话说，R2内部存储的连接表只需要包含这个区域内的各个节点，以及节点之间的边的权值，只需要包含这些信息就行了。至于其他区域的拓扑结构是怎么样的，R2路由器并不需要关心。这样就使得R2路由器的LSDB变小，那相应的迪杰斯特拉算法的运算效率是不是也可以得到保证？

发言人   13:39

好，接下来对这张图的一些信息进行解释。首先我们会看到零号区域，这个区域比较特殊，它是所谓的主干区域。在整个OSPF自制系统当中，主干区域只会有一个它处于中心的位置。在有的教材中，主干区域也会被翻译为骨干区域。因为它英文术语是backbone array，backbone就是击鼓的意思对吧？中枢骨干。

发言人   14:11

从这张图当中可以看到主干区域它的作用是连通其他的非主干区域。可以看到我这标了高亮绿色的这几台路由器，它们既属于主干区域，同时也属于某一个非主干区。那R3、R4、R七这几台路由器，我们就可以把它称为区域边界路由器，英文缩写叫ABR。在一个非主干区域内，如果一台路由器想要把IP数据报转发到其他的区域，那么就可以把IP数据报转发给本区域的边界路由器。好，所以对于一个非主干区域来说，至少会有一台区域边界路由器和主干区域直接相连。

发言人   15:05

好，另外值得一提的是，任何一个非主干区域通常都是和主干区域直接相连的。假如这儿有个区域4，它用一个路由器R10和区域三相连，这种结构是不被允许的。如果要在这个自制系统内新划分一个区域，那么这个区域四同样它也只能和主干区域直接相连。好，所以区域划分的越多，显然这个主干区域它的压力也会越大对吧？因为如果区域和区域之间相互传递IP数据报的话，那么这个IP数据报肯定得通过主干区域进行转发。所以非主干区域的划分也不能无限制的往上增长。在现实世界中，一个大型的自治系统内部可能会划分为30到50个这种普通的区域以及一个主干区域。

发言人   16:04

好，这是一个小小的拓展。刚才我们说过划分区域之后，区域内部的路由器，比如说R1或者R2这2台路由器，他们只需要知道本区域内的节点，以及节点和节点之间的边。当然R3也属于这个区域。另一方面R3这台特殊的路由器它也属于主干区域，它脚踏两只船，两边的情况都需要了解。好，所以像R3、R7、R4这种区域边界路由器，他们既需要知道自己直连的这个区域，它的网络拓扑是什么，与此同时也需要知道主干区域的这个网络拓扑结构是什么。换句话说，区域内部的这种路由器它构建的LSDB更小，它的邻接表更小，只需要包含这些信息对吧？但是这种区域边界路由器，它构建的LSDB就要更大好，这是给大家拓展的一些信息。

发言人   17:12

我们来总结一下OSPF协议，它支持把自制系统的内部划分为多个区域，其中主干区域只有一个，非主干区域可以有多个。根据路由器它所处的位置不同，大致可以分为这样的三类路由器。首先是自制系统边界路由器，比如说这儿的R6，如图所示，自制系统边界路由器会和其他的自制系统相连，会负责本自制系统和外部自治系统之间的IP分组的路由转发。好，这是最特殊的一个路由器。如果说这个自制系统内和其他自制系统的网络通信量很大，那完全可以设置第二台。第三台就是可以有更多台的这种自制系统边界路由器，这种设置是被允许的。好，接下来第二大类的路由器，就是我们这镖了高亮绿色的，叫做区域边界路由器。这种路由器的作用刚才说过，这儿就不再赘述。

发言人   18:20

最后第三类的路由器叫做区域内部路由器，也可以称为非边界路由器。也就是我这儿没有高亮的这些R1、R2、R5、R8、R9这些都属于区域内部路由器。这些路由器只负责本区域内的IP数据包的路由转发。如果要发往其他区域的话，那么IP数据报会先转发给本区域的边界路由器，然后由这些边界路由器再选择合适的路径继续往外转发。

发言人   18:57

好的，在这节课中我们进一步探讨了OSPF的工作原理。首先我们探讨了使用OSPF协议的路由器怎么产生路由表。首先第一步就是需要先构建LSDB。每一台路由器会把自己探测到的链路状态信息用洪泛法迅速的广而告知发给其他的路由器。并且一旦探测到身边的链路有变化，就会立即再一次红翻。由于宏方法一传十，十传百，可以迅速的通告全网。所以每一台路由器都可以迅速的构建自己的LSDB，也就是链路状态数据库。本质上链路状态数据库你可以理解为是图的邻接表存储。

发言人   19:45

每一台路由器都会独立的根据自己的LSDB去运行狄杰斯塔算法。从而就可以计算出从自身出发到达每一个目的网络的最短路径应该怎么走。基于最短路径的运算结果，最终就可以生成路由表。在路由表当中并不需要保存完整的最短路径信息，只需要记录目的网络以及下一跳路由器，然后再记录到达目的网络的总距离是多少就可以了。

发言人   20:18

好，另外OSPF协议为了防止每一台路由器的LSDB过大，以及为了防止宏犯法导致的网络通信量过大，所以提出了区域划分的这种功能。OSPF支持把自制系统划分成一个主干区以及多个非主干区域，这样在降低了宏观压力的同时，也让每一台路由器它的LSDB变小了。LSDB变小也就是图的邻接表变小了，那狄杰斯他算法的运算速度也可以得以保障。好，引入区域划分的这个概念之后，在自制系统内部的路由器就出现了几种类别，分别是自制系统边界路由器、区域边界路由器和区域内部路由器。这几类路由器我们结合图示可以一目了然，迅速回顾。

发言人   21:16

最后给大家拓展几个可能会出现的术语。这几个术语是容易混淆的，LSDB、LSA、LSI, 它们都有一个共同的前缀。LS也就是链路状态。LSDB链路状态数据库，它指的就是这整个邻接表，LSA它指的是链路状态通告，这是一种OSPF协议定义的一种数据结构。比如说R一路由器，它探测到的自己身边的这些链路状态信息，就会用一个LSA这种数据结构来保存。从逻辑上看，它其实就是一个链表，对吧？每一台路由器会把自己探测到的这个LSA通过OSPF分组，用宏方法迅速的广而告之，发送给其他的路由器。好，这是LSA这个术语。

发言人   22:14

最后还有一个术语叫LSI，I指的是information，也就是链路状态信息这个术语。它是一个广义的概念，你可以说R一探测到的这些信息，它是LSI你也可以说R1、R2、R3他们探测到这些信息是LSI你也可以说整个LSDB它们都属于LSI，反正都是在描述链路状态信息。好，所以LSI这个术语它是一个广义的概念，甚至你可以认为某一条边的信息它也是链路状态信息。做一个类比，LSDB就像是一本书，而LSA就像是书里的某一个章节，而LSI泛指书的内容，你可以说书里的一个字是书的内容，可以说书里的几个章节是书的内容，也可以说一整本书它也是书的内容，对吧？好，这几个术语大家在看书做题或者去网上查资料的时候可能会遇到。比如说2014年的一道真题就遇到了LSI这个术语，那这几个缩写都是值得注意的。

发言人   23:36

接下来最后再拓展一个东西，让大家看看LSDB更完整的样子。你会看到在这个自制系统内，除了路由器节点之外，其实每一个网络都会被我们视为一个节点，对吧？但是不知道大家有没有发现，之前我们在讲的时候，其实这个邻接表它是不完整的。我们只描述了从路由器出发到达与之相邻的其他节点，就是往外走的这些边权值等于多少，对吧？在之前给出的这些信息当中，并没有表示从一个网络节点到达与之相邻的路由器节点，它这条边权值等于多少。作为拓展知识，可以看这个图，可以看到我们对这个临界表进行了扩充，我们增加了net 1到net 6，从这几个结点出发往外走的各条边它的权值的信息。比如说net 1到达R一这条边的权值，这条有向边的权值，OSPF协议会把它视为零，一个直连网络到达与之相邻的路由器这个方向的边通常都会视为零。

发言人   24:54

好，所以如果LSDB，我们把它画的更完整一些，应该是这个样子。但除了我这画的信息之外，其实LSDB还有更多的信息。不过最主要的还是我们前面给出的这部分邻接表。因为对于一台路由器来说，从自身出发到达其他任何一个目的网络的这个最短路径，其实通过这个部分就完全够用了，完全可以基于这部分的信息去运行狄杰斯塔算法。好，这儿只是作为拓展学习，简要了解即可。好的，以上就是本节课的所有内容。