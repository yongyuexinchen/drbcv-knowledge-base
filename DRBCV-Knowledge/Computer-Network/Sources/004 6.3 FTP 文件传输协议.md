# 004 6.3 FTP 文件传输协议

> Source: 004 6.3 FTP 文件传输协议_原文.docx

004 6.3 FTP 文件传输协议_原文

2026年07月19日 12:19

发言人   00:00

各位同学大家好，在这个视频中我们要学习FTP文件传输协议。FTP这个协议它诞生的年代已经很久远了，1970年代初就被发明。然后在1985年开始，FTP进入了黄金时代。因为在那个时候TCPIP协议迅速的普及，并且逐渐的统治了互联网世界。

发言人   00:25

那么在TCPIP构建的互联网世界当中，人们需要一个统一的文件传输方式。比如说下载一个软件或者共享文件。我们现在传输文件一般就是用微信QQ或者百度网盘之类的那现在的这些应用传输文件通常是使用HTTPS协议去实现的。不过在上世纪的八九十年代，那个时代还没有像现在这么方便的这些工具。在那个年代，如果两台主机之间想要共享一些文件数据，那么通常就是使用FTP协议去实现相应的功能。

发言人   01:05

我们观察这张图，左边这个部分是FTP的客户端，你可以理解为是你的电脑。而右边这个部分是FTP的服务器端，你可以理解为是你舍友的电脑。假如你的舍友要给你传一个文件，那么在1980年代他可以把他自己的电脑变成FTP的服务器。然后你的电脑作为FTP客户端，你们俩之间可以通过FTP来共享一些文件。

发言人   01:35

具体来说，FTP可以支持这样的一些功能。首先它可以提供不同种类的主机系统之间的这种文件传输能力。怎么理解呢？比如说左边这是你你的这台电脑使用的是微软的dos系统。那在那个年代dos系统的文件系统，它采用的是FAT结构。学过操作系统的同学应该知道FAT是什么东西。

发言人   02:00

好，另一方面你80年代的这个舍友，他当时使用的是unix的操作系统，其中的文件系统又是UFS的格式。所以你们俩的这两台电脑，这两台主机之间软件硬件的体系结构是有巨大的区别的。但是在FTP协议的支持之下，你们俩的这个电脑之间也可以正常的去传输文件。好，另外FTP可以以用户权限管理的方式提供用户对远程FTP服务器上的文件管理能力。

发言人   02:35

怎么理解这句话呢？假设在你舍友的这个电脑上，它的根目录下面有一个文件夹学习资料，其中有一个文件叫葵花宝典点TXT。那你的这台电脑如果想要去访问你所有的这个电脑，它共享的这个文件的话，你需要先提供用户名、密码，去登录这个FTP服务器。它这边验证了你的身份之后，会根据这个用户的身份去判断当前访问的这个用户对于这个文件来说有没有读权限，有没有写权限。如果有读权限，那么说明你就有权去下载这个文件到自己的本地，对吧？而如果说有写权限，就意味着你这边就有权利往这个学习资料文件夹里面去上传一些东西。好，所以FTP是支持以用户权限管理的方式，对这些文件或者文件夹进行这种权限的分类的这跟操作系统的文件保护其实是一个逻辑。

发言人   03:45

而另一方面FTP还可以支持以匿名FTP的方式，提供这种公用文件共享的能力。这种公共的文件共享能力说白了就是用匿名登录的方式是连接这个FTP服务器。比如说在你设有的这个电脑上，它的根目录下面有一个文件叫做所有人可看点TXT。正如其名，这个文件可以允许所有的人，哪怕是匿名登陆的人去下载。那这样的话，在八九十年代那个时候，其实就可以实现一个文件对全世界范围内的这种共享。这件事情放在那个年代其实还是非常厉害的。

发言人   04:31

好，现在我们对FTP它的功能它有什么作用？已经有了比较感性的认知。好，最后有一点值得补充的是，FTP服务器可以同时为多个客户端服务。好，现在我们已经对FTP的功能和作用有了比较感性的认知。接下来我们要学习FTP的具体的工作原理，并且引入控制连接、数据连接这两个很常考的概，我们来梳理一些要点。

发言人   05:02

首先FTP是应用层的协议，它采用CS客户端服务器模型，并且基于TCP连接的这个可靠传输。在FTP客户端和FTP服务器之间需要建立两种类型的TCP连接，分别是TCP控制连接和TCP数据连接。两种连接分别有什么作用？我们一会儿再展开探讨。

发言人   05:27

好，现在我们先观察FTP的客户端这边它的组成。首先客户端这边这个圆柱体以及服务器端这个圆柱体就是这台电脑的磁盘。我们知道电脑的文件都是存储在自己的这个磁盘里面的。如果客户端要从服务器这边下载一个文件，那本质上就是把服务器的磁盘里边的某一个文件，通过这个数据传送进程，以及通过数据连接传送给客户端这边的数据传送进程。然后这个数据传送进程再把服务器传过来的数据，也就是那个文件写到自己的本地磁盘里。那这就完成了文件的下载。那文件的上传就是逆向的一个过程，这就不再赘述。好，这是这个圆柱体的含义，控制进程的含义。

发言人   06:21

我们现在先不展开，然后再往上看，这有个用户界面，就是你在电脑上看见的那个软件的界面。比如说使用windows电脑的同学，里边有一个文件资源管理器，也就是你打开文件夹看文件的那个东西，那个东西就是你的用户界面。Windows的文件资源管理器，它本身就支持FTP协议。所以说你电脑上的那个文件浏览器，它就可以作为FTP客户端的这个用户界面。那相应的使用mac的同学就是访达，访达也可以作为FTP的用户界面。

发言人   06:59

好，这是客户端这边的组成，用户界面控制进程和数据传送进程。好，再来看服务器端这边的组成，其实这张示意图还画少了一个东西，服务器端这边有主进程和从属进程这样的两大部分组成。其中主进程在整个服务器端只有一个，而从属进程可能会有多个。像这张图当中的控制进程以及数据传送进程，它们都属于从属进程。并且每个已经登陆FTP服务器的用户都会给他新建一个对应的控制进程，以及零个或者一个数据传送进程。

发言人   07:44

为什么控制进程必须有一个，而数据传送进程可能是零个或者一个呢？这就涉及到这个控制连接和数据连接的问题了。上面这个控制连接用于发送控制信息，所以把它称为控制连接。从这张图当中可以看到，控制连接就是客户端的控制进程和服务器端的控制进程。他们俩之间的TCP连接。这个控制连接是持久的，也就是在整个FTP会话期间，上面这个连接它都会保持不中断。

发言人   08:20

什么叫FTP会话呢？就是说你登录这个FTP服务器，你登上去之后，你可能要下载或者上传好多个文件，对吧？那当你的上传下载所有的东西都做完了之后，你再登出这个服务器。那么从你登录服务器开始，一直到最后登出服务器，整个过程就是FTP的会话期间。

发言人   08:43

在登录和登出的这段时间内，你可能会有多次下载文件或者上传文件的这种请求。每下载一次文件或者每上传一次文件，这个数据传送进程以及对应的这个PCP数据连接都会不断的被建立以及被销毁。比如说你先发出了第一个下载请求，那为了完成一个文件的下载，这个控制进程就会新建一个数据传送进程。然后这个数据传送进程会和客户端的数据传送进程建立起TCP数据连接。而当这个下载任务完成之后，这个数据连接就会断开，以及数据传送进程也会被销毁。如果下一次还有下载的请求，那数据传送进程以及这个数据连接又会再次的被重新创建。

发言人   09:41

好，所以我们说数据连接它是非持久的连接。每一次文件传输之前会建立，结束之后就会关闭。相对应的这个数据传送进程也一样。所以在这个地方我们说每一个已登录的用户，在整个FTP绘画期间肯定会有一个控制进程会持续存在。以及根据当前到底有没有在传输文件，可能会有零个数据传送进程，也有可能会有一个数据传送进程正在工作，就是这个意思。好，接下来我会给大家详细梳理FTP的客户端和服务器在登录阶段以及数据的传输阶段，以及最后数据传输完成之后还需要登出。在这三个阶段大致需要完成哪些事情，我们逐个阶段带大家分析梳理一遍。

发言人   10:36

登录阶段的目标是FTP的服务器要验证这个连接的客户端它的身份。好，那么服务器端这边我们说有一个主进程，这个主进程它会持续的监听21号TCP的21号端口，这个端口号是固定不变的。好，现在客户端这边如果它要连接远方的这个服务器，那么客户端这边会新建一个控制进程，这个控制进程可以随机分配一个端口，比如说8888这个端口。接下来客户端的控制进程通过8888这个端口向服务器端的21号端口发起这个TCP连接的请求。也就是像6.6 21这个IP和端口号发出TCP连接请求。

发言人   11:32

现在主进程完成了TCP连接的建立之后，这个主进程它会新建一个子进程，而新建的这个子进程就是这张图当中的控制进程。由于这个控制进程是由主进程创建的那学过操作系统的同学都知道，一个子进程它可以从父进程这边继承一些资源，对吧？比如说父进程这边刚才创建好的这个TCP连接就可以让这个控制进程去继承。这就相当于客户端的这个控制进程，它最后是要和服务器端的控制进程的21号端口建立这个TCP的控制连接。虽然刚开始是和主进程建立连接的，但是这个主进程建立完了之后，会把这个连接继承给下面这个进程。好，那么到这一步相当于TCP的控制连接就建立完成了。

发言人   12:31

这个控制连接本质上就是一个普通的TCP连接，只不过这个TCP连接接下来会用于发送各种各样的控制信息。好，那什么样的控制信息呢？接下来客户端会向服务器通过这个控制连接去发出自己的FTP登录请求。比如说把自己的用户名密码发给这个服务器的控制进程。好，那服务器端这边的控制进程会验证证他的用户名和密码。如果验证通过，那么服务器这边就可以给客户端这边返回一个FTP的响应报文。相当于就是告诉他用户名、密码验证通过或者验证不通过，类似于这种信息。那么到这步为止，所谓的登录阶段就已经完成了。

发言人   13:22

那这个地方有必要跟大家解释一下，很多教材包括谢欣仁老师的教材，都没有去详细的解释这个主进程背后的这个细节。所以在很多教材当中并不会给大家描述客户端的控制进程。先连接这个主进程，然后主进程再把这个连接继承给控制进程。就是教材里面不会描述背后这些细节，可能会简化的描述为客户端会请求与服务器的控制进程21号端口去建立TCP连接。不管是看书还是做题，如果看见这样的表述，我们不需要去钻牛角尖，我们认为这种表述是对的就行了。

发言人   14:05

好，现在用户已经登录成功了，接下来就可以开始进行数据传送。数据传送阶段可以采用两种传送的模式，分别是主动模式和被动模式。这两种模式的区别说来也简单，就是说服务器端的数据传送进程和客户端的数据传送进程之间，他们在建立TCP数据连接的时候，如果是由服务器这边主动的发起这种建立请求，那么就是主动模式。而如果服务器这边是被动的等待客户端发起这种建立连接的请求，那就是所谓的被动模式。那客户端这边在正式开始数据传送之前，也就正式开始上传下载文件之前，客户端必须发送port指令或者passive这个指令去告诉服务器说接下来的这个数据传送，我希望采用主动模式还是被动模式。所以这种数据传输模式的选择权，其实是在客户端这边的。如果主动模式的话，相当于就是客户端告诉服务器说接下来你来联系我。而如果是被动模式的话，就是客户端告诉服务器说接下来你等着我联系你，就这个意思。

发言人   15:29

好，接下来我会用两个例子，带大家详细的感受这个主动模式和被动模式背后的区别。先划重点，考研当中如果题目没有特别强调的话，那么默认采用主动模式。好，接下来我们开始进行数据传输。还记得数据传输之前，我们已经做了这个登录的动作，对吧？客户端和服务器已经建立好了控制连接，并且通过控制连接给服务器这边发送了用户名、密码这些登录相关的消息。现在服务器这边已经知道，和他通信的这个用户他的身份是谁了。根据用户的身份是不是就可以去决定这个用户对于某一个文件或者对于某一个文件夹，它的读写权限到底是什么？

发言人   16:23

好，我们看背后具体的过程。首先第一步，客户端会通过控制连接向服务器这边指明我这一次数据传输希望采用主动模式。那所谓的主动模式就是客户端希望服务器这边主动的向客户端发出这个TCP连接的建立请求，对吧？既然如此，客户端这边是不是得让服务器知道我这边的数据传送进程，它的IP地址以及端口号到底是多少。你想让对方主动联系你那你得把地址告诉对方，对吧？

发言人   17:05

好，所以在这个第一条信息当中，除了指明自己希望采用主动模式这种传输数据，与此同时还要把客户端这边的埃批地址，以及客户端的这个数据传送进程给它分配的随机端口号。我们假设是9855，把这个IP和端后也通通告诉对方，告诉对方说接下来你来联系我，我的地址是7.7，端口号是9855好。现在服务器端这边已经知道客户端的意图了，对吧？如果他同意客户端协商的这个事情，那么服务器的控制进程就会给客户端的服务进程返回一个响应，相当于就是同意你协商的这个东西。好，这是第二步。

发言人   17:58

好，接下来第三步，客户端会向服务器发出这个下载文件的请求，并且要指明自己要下载的是哪个文件。比如说指明我想要下载你服务器这边根目录下面的C下面的D这个文件。好，所以下载文件的请求是通过控制连接发送的。好，那么服务器这边收到这个下载请求之后，它是不是得验证当前的这个用户，他对于D这个文件有没有下载的权限，有没有读的权限，对吧？那如果说有求允许它下载，那接下来第四步控制进程又会给对方返回一个，针对于下载请求的响应，相当于就是同意你下载这个意思。好，这是第四步。

发言人   18:52

接下来到了第五步，刚才客户端的控制进程已经告诉服务器的控制进程，说我的IP以及我的端口号是多少。接下来请你主动的和我产生连接，对吧？所以接下来第五步，服务器端这边会新建一个数据传送进程，这个数据传送进程会使用20这个固定端口号，主动的向客户端建立这个TCP的连接。这条TCP连接就是接下来用于传输数据的，所以把它称为数据连接。

发言人   19:31

好，刚才客户端这边，指明的是要下载某一个文件，对吧？所以服务器这边就会把用户指明的这个文件从磁盘读出。然后通过这个数据传送进程，它的数据连接分成TCP报文段，一段一段地发给客户端这边。客户端这边收到文件的这些分段之后，他又会把这个文件组装起来，写到自己的本地磁盘。

发言人   20:08

当服务器端的数据全部发送完之后，服务器端会主动的发起第一次挥手。那么两边经过四次挥手之后，这个数据连接就会彻底的断开。与此同时客户端和服务器端这边的数据传送进程也会被终止。所以可以看到数据传送进程以及数据传送进程之间的这个数据连接，它们都是一次性的。它们的出现是为了完成某一个特定的传输任务。而当这个传输任务完成之后，数据连接和数据传送进程就都可以退出舞台了。

发言人   20:51

好，在这个例子当中，我们体会到了主动模式的工作过程。核心就是客户端首先要告诉服务器，你接下来主动连我的哪一个IP和端口，要把这个信息告诉对方。而服务器这边是通过固定的20端口去发起这个TCP的数据连接。这个固定的端口号也是考试常考的一点，大家需要特别注意。

发言人   21:21

好，接下来我们换一个例子感受被动模式工作的过程。我们假设客户端和服务器的控制连接已经建立好了，并且用户名、密码登录的这个动作也已经完成，那接下来就可以开始传输数据。假设这次客户端想要采用被动模式，那么首先客户端的控制进程会向服务器的这个控制进程通过控制连接发送这一次传输的模式。如果采用的是被动模式，那么就是发送PASV passive这个命令。好，这个命令不重要，反正就是告诉服务器说接下来这个数据传输我希望采用的是被动模式。那被动意味着服务器这边被动的等待客户端这边建立TCP连接对吧？

发言人   22:14

好，那么对于服务器来说，如果他同意对方被动模式的这种请求，那么首先服务器这边它是不是得先新建这个数据传送进程，并且给这个数据传送进程分配一个随机端口。注意，被动模式下，服务器的数据传送进程分配一个随机端口就行了，不需要给它分配固定端口，20不需要。好，那么在这个例子中，我们假设给这个进程分配的是2111这个随机端口号。那么接下来服务器要被动的等待客户端对自己发出连接请求，对吧？那服务器肯定得把这个端口号的信息告诉对方。好，所以你会看到在第二步，服务器会给客户端返回一个响应，就是针对于刚才第一条消息的一个响应。

发言人   23:11

就是说我同意你的被动模式的这种请求，那接下来我会等待你的连接。我这边的随机端口号是2111，我这边的IP地址是6666。就是把这些信息告诉客户端。

发言人   23:29

好，接下来第三步，假如客户端接下来是想要上传一个文件到服务器的C这个目录下面，那么上传文件的这个请求就是这条FTP报文也是通过控制进程发送的。底层是使用了这个控制连接，其中要指明我要做什么，我要上传一个文件，那传到哪儿，我希望传到你的C这个目录下面。好，服务器这边是不是得验证这个用户对于C这个目录，这个文件夹有没有写，有没有上传的权限，对吧？那如果说有这个权限，我允许你上传，那么服务器就会返回一个响应，也就是四这条消息。同样的这个响应也是通过控制连接完成这条响应，相当于就是告诉他说你刚才说要上传的这个事情，我同意。你好，可以看到1234这几个步骤都是通过这个控制连接完成的那接下来要真正的开始数据传输了，这次数据传输的方向应该是从客户端传向服务器，对吧？因为是上传一个文件。

发言人   24:43

好，接下来第五步就可以开始建立数据连接。客户端这边会新建一个数据传送进程，并且给这个传送进程分配一个随机的端口号。比如说1234。那么这个进程会向服务器之前告知他的2111这个端口，发起TCP连接建立的请求。经过三次握手之后，就完成了数据连接的建立。

发言人   25:11

好，有了这个TCP连接，接下来是不是就可以开始传输文件数据了？这一次客户端是要上传一个文件，客户端的这个数据传送进程会从磁盘里读出本地文件，然后把这一大个文件进行分段拆分成TCP报文段。紧接着利用这个数据连接，也就是这条TCP连接分段去传送这个文件数据。好，服务器这边的数据传送进程收到文件的分段之后，最终把这些文件的分段给它写到C这个目录下面。比如说这个文件我们把它命名为F好，那么整个文件的传输完成之后，接下来就可以断开数据连接了。

发言人   26:00

不好意思，这个地方我想打的是C指向S就是说上传这个动作完成之后，是客户端主动向服务器发出了断开数据连接的第一次挥手。其实这个数据连接的断开，第一次挥手到底是由服务器发出的，还是由客户端发出的，这个和主动模式、被动模式没有任何关系。主要就是看数据到底是从左边传向右边，还是从右边转向左边。如果是左边客户端传给服务器，那么客户端这边一旦数据传输完成之后，就会由客户端主动的发起第一次回收，因为客户端他自己才知道这个文件传完了，对吧？那么一旦文件传完了，就可以第一次挥手主动的断开TCP连接。那如果是相反的，方向也一样的，也就是下载文件的时候，这种时候是服务器给客户端，传这个文件，一旦服务器这边他知道这个文件传送完成了之后，就会有服务器的这个进程。主动的发起第一次挥手。

发言人   27:08

好，所以断开数据连接到底是由谁主动的断开的，这个和主动被动模式没有关系。对于考试来说，大家不需要去理解这么深，在这儿我只是给大家做一些拓展。我们在细节层面能够把这些流程给串起来，更有利于大家的记忆。好，这是被动模式的示例。在一次FTP绘画当中，用户可以多次切换主动模式或者被动模式，甚至每一次数据传输之前都可以单独的指明本次的数据传输到底要采用主动模式还是被动模式。

发言人   27:46

好，当这个客户端上传下载文件，所有的事情都完成了之后，最后就可以登出这个服务器。于是来到最后一个阶段，登出这个阶段很简单，首先第一步，客户端向服务器通过这个控制连接发出这个登出的命令，告诉对方说我的事情办完了，我要走了。那具体来说是quit这个命令，接下来服务器会向客户端返回一个响应。这个响应是对一这条消息的一个回复，相当于告诉他说，我知道了，你要走了。好，这两个动作完成之后，接下来服务器会主动的发起第一次挥手。我们知道TCP总共需要经过四次挥手，四次挥手结束之后，整个控制连接就彻底的断开了。那么FTP的绘画也到此结束。好，通过这个例子我们就可以清楚地感受到FTP它背后工作的一个过程。

发言人   28:48

最后需要给大家补充一些关于FTP的拓展。首先第一点，FTP可以很方便的实现文件的上传或者下载。但是如果要修改一个文件，效率就非常低。比如说服务器端这边，C下面的D这个文件。假如客户端想要修改这个文件，那么客户端就只能先把这个文件下载到自己的本地。然后在自己的本地改完之后再重新上传回这个服务器。需要把文件整体的下载和上传，也就是说对于这个文件的局部修改是不被允许的，只能整体的下载再上传。这是FTP的一个缺点。

发言人   29:33

好，另外补充一个概念，我们可以看到FTP把控制信息和数据信息这两类信息的收发分别用2个TCP连接来完成，分离了控制和数据。也正因如此，我们可以称FTP的控制信息是带外传输的。所谓的带外传输就是不用数据连接去传输，额外的用另一个连接来传输这个控制信息，就这个意思。这么做的优点是控制命令不会因为数据传输而阻塞。

发言人   30:07

考虑这样的一种情况，假如客户端正在下载一个很大的文件，比如说这个文件D它可能高达10GB，那么这个文件的下载需要耗费很长的时间。如果说这个文件下载到一半，客户端这边不想要继续下载了。那此时很简单，只需要让这个控制进程给服务器这边的控制进程发送一个命令，就告诉对方说我要停止下载。那么服务器端就可以及时的做出响应，对吧？就不再继续传输文件的数据。

发言人   30:43

假如我们数据连接和控制连接没有分开，而是用同一个TCP连接去实现的话。那在这种情况下，如果这个文件持续的传输，当这个文件下载还没有完成的时候，这个TCP连接的传输就会被一直占据，对吧？那么客户端进程和服务器进程之间如果要传输一些控制信息的话，就可能会因为数据传输还没有结束而发生阻塞性。好，所以这种控制和数据相分离的这种策略是有作用的。

发言人   31:15

好，接下来还有一点值得补充的是，我们刚才说客户端登录服务器之后，他会向服务器请求说我想要下载你的根目录下面的C下面的D对吧？那不知道有没有同学有这样的疑问，客户端是怎么知道服务器这边的这个目录结构的那这个问题需要使用到list这个FTP命令去完成。这个命令是通过控制连接发出的，也就是请求服务器返回文件列表。比如说客户端通过控制连接向服务器发出list这个命令，问他根目录下面有哪些文件。

发言人   31:56

好，服务器端的控制进程收到这个命令之后，接下来有意思的来了。这个根目录下面包含ABC这几个文件对吧？那么这些文件列表的信息需要新建一条数据连接来传输回去。所以服务器端返回文件列表这个事情最后是通过数据连接完成的。但是请求查看文件列表这个动作又是通过上面的控制连接完成的那这一点给大家特别强调一下，以防出题老师偷袭。

发言人   32:34

好，最后还有一点需要说明的是，我们介绍数据传输的主动模式和被动模式。Rfc的官方文档，也就是FTP协议最权威的那个官方文档，它默认是使用这个主动模式。所以我们考研也会跟着这个官方文档，默认去考这种主动模式。

发言人   32:57

但是在现实应用当中，由于主动模式无法穿透NAT，所以在现实应用当中反而会默认采用被动模式，主动模式无法穿透NAT这个问题怎么理解呢？如果采用NAT技术，那么客户端这边它会有一个内网地址，对吧？然后这个客户端连接了NAT路由器之后，这个NAT路由器对外会有一个统一的公网IP地址。那如果采用主动模式的话，意味着客户端需要主动的等待服务器，向客户端发出这种TCP连接的情请求，对吧？但是这个客户端和其他的很多台主机共享同一个公网IP地址。所以如果这个NAT路由器不支持客户端，专门给它分配一个公网的端口号的话，那么想让服务器这边主动的连接客户端这件事情就几乎不可能实现。

发言人   33:59

在现实应用当中，一个FTP的服务器，它通常都会有一个自己的公网埃批。所以在现实应用当中，服务器被动的等待客户端向自己发出这个连建立连接的请求，这种方案是更容易落地的。好，所以官方的默认模式和现实当中实际的默认模式它是有区别的。大家在网上查资料的时候可能会看到默认采用被动模式这样的说法。现在你知道了默认被动这个是事实层面的一个标准，而官方文档的标准是默认主动模式。我们考研以官方文档为主，除非题目特别说明，否则我们就认为采用的是主动模式。

发言人   34:46

好的，在这个视频中我们介绍了FTP文件传输协议，我们介绍了FTP的功能和特点。FTP采用的是CS模型，C对应的是client客户端，S对应的是server服务器。FTP的服务器有一个主进程和多个从属进程，控制进程和数据传送进程都属于从属进程。整个FTP服务器有且仅有一个主进程，这个主进程会负责监听21号端口。那如果说有客户端的控制进程向21号端口发出建立连接的请求，那么这个主进程会完成TCP连接的建立。然后把这个TCP连接继承给控制进程。那对于一个已登录的用户来说，在整个FTP会话期间都会有一个专属的控制进程为他服务，为这个用户服务。

发言人   35:42

服务器的控制进程和客户端的控制进程之间，建立的这个TCP连接叫做控制连接。控制连接是持久的这就意味着在整个FTP会话期间，控制连接都会一直保持不会断开。控制连接主要是用于传输一些控制类的信息，不会传输文件。

发言人   36:04

具体的数据控制连接是客户端的某一个随机端口，和服务器端的21号端口建立起的一个TCP。好，这是控制进程之间的控制连接。接下来服务器这边的数据传送进程和客户端的数据传送进程，他们之间会建立起数据连接。数据连接是暂时性的，是非持久的。数据连接用于传输文件的具体数据，包括上传下载一个文件或者传输文件的列表，这些都是通过数据连接来进行的。在传输开始之前会建立数据连接，传输结束之后会立即关闭这个连接。所以数据连接是非持久的，一次性的。

发言人   36:50

在传输数据的时候，传输模式有两种，一种是主动模式，一种是被动模式。官方默认的是主动模式，在主动模式之向服务器的数据传送进程会向客户端的数据传送进程主动的发起TCP连接的建立请求。这就意味着客户端需要提前开放一个随机端口，并且把这个随机端口告诉服务器，就告诉他说你主动来连我，那么你要连我的哪个端口，需要把这些信息交代清楚。那服务器这边会采用20这个固定端口，主动的和客户端告知的这个随机端口建立连接，这是主动模式。好，那如果是被动模式的话，就是服务器要开放一个随机端口，并且要提前把这个，随机端口告诉客户端。接下来服务器就可以被动地等待着客户端发起这个数据连接。好，那么控制连接、数据连接以及比较特殊的这两个固定端口，这些很喜欢考察，大家需要注意。好，以上就是FTP文件传输协议的所有内容，大家可以再结合这一页课件来进行回顾。