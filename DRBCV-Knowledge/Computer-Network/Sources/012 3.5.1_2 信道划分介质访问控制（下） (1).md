# 012 3.5.1_2 信道划分介质访问控制（下） (1)

> Source: 012 3.5.1_2 信道划分介质访问控制（下） (1)_原文.docx

012 3.5.1_2 信道划分介质访问控制（下） (1)_原文

2026年07月19日 11:53

发言人   00:00

好的，继续来看使用信道划分的方法去实现介质访问控制。在这个视频中我们重点探讨码分复用技术，英文缩写叫CDM。先介绍码分复用技术的这个呃现实背景，在曾经的2G3G移动网络时代，也就是0几年到10年，差不多我上高中那会儿。那个时候我记得我们的手机可以选择网络的格式GSM或者CDMA。GSM和CDMA就是两种网络通信的技术，其中CDMA技术在后来的三季时代也曾经被广泛的使用。CDMA技术可以用于实现节点和节点之间的通信。CDMA技术的背后，它的底层原理其实就是码分复用技术，英文缩写叫CDM。

发言人   00:55

好，为了方便理解这个码分复用技术，接下来我们引入一个模型。我们假设有这样的三个通信基站，分别叫A站、B站、C站。这几个节点之间使用CDMA技术进行通信。那在这种技术的支持下，A站和B站可以同时给C站发送信号。当然如果有更多的站点，比如说D站一站想要同时发送数据，这个也OK都没问题。比如说A站发出了101，B站发出了110这样的三个比特，那两个站点发出的信号在传输的过程当中会相互影响、相互叠加。C站会接收到相互叠加的这个信号。但是在码分复用技术的支持下，C站有办法可以把来自各个节点的信号值给分离出来。

发言人   01:51

好，那具体是怎么做的呢？接下来我们展开来探讨一下。在这儿给大家做了一个码分复用技术的总结。接下来我会结合这张导图去介绍码分复用技术每一步需要做什么。

发言人   02:05

好，首先第一件事，我们需要给各个节点分配一个专属的码片序列。那一个码片序列会包含M个码片，也就是包含M个信号值，我们可以理解为M维的向量。同时这个M维向量的分量通常为了方便计算会取一或者负一。好听起来很抽象对不对？没关系，我们直接来看例子。

发言人   02:33

假设现在我们用四维的码片序列去做这样的设计，给A这个结点分配一个码片序列。那这个码片序列当中包含四个分量，分别是1111。每一个分量就是一个所谓的码片，四个分量结合起来就是所谓的码片序列，我们可以用线性代数的向量去理解，这个所谓的M维码片序列其实就是M维的向量，对吧？而这个向量的每一个分量，我们要么取一，要么取负一。至于为什么要这么取，我们一会儿再来填这个坑。

发言人   03:13

好，这是A站点的专属码片序列。同样的道理，B站点和C站点也会有各自专属的码片序列，并且各个站点的码片序列是不同的。好，接下来看一下这个码片序列有什么作用。假设现在A站要给C站发送101这样的三个比特，那么A这个结点如果想要发送二进制的一就需要发出四个信号值1111，也就是和它的码片序列保持一致。如果要发送二进制的0，那么就会发出四个信号值，-1-1-1-1，也就是和它的码片序列刚好完全相反。好，总之我们会用M个信号值去表示一个比特，那对于其他站点也是一样的。

发言人   04:05

假如B站点要给C站点发送一个二进制的一，那么它应该发出四个信号值，1-1 1、负一。如果要发送二进制0，那么它发出的四个信号值就应该是全部取反，负一一。负一一如果用线性代数来解释的话，相当于二进制一对应的是M维的这个向量，二进制零对应的是负的M维向量。好，那么在这个例子中，101这几个二进制对应的信号值就应该是这样的三组。接下来C站收到了这样的三组信号值，每一组有四个分量组成。

发言人   04:49

在码分复用技术当中，各个站点会提前知道彼此的这个码片序列。也就是说C站点它是知道A站点的码片序列是多少，也知道B站点的码片序列是多少。那么当C站点收到这样的一些信号值的时候，经过对比发现1111这个是A站点的码片序列，而-1-1-1-1这个是A站点码片序列的取法。所以他就知道这三组信号肯定是A站点发过来的，而且它对应的是101这样的三个比特。好，那么回到这张导图，刚才我们说相互通信的各个节点会知道彼此的码片序列等于多少。

发言人   05:35

好，另外一点需要补充的是，在CDM技术当中，我们要求给各个结点分配的这个码片序列，也就是每个结点的M维向量必须相互正交。在线性代数当中我们学过，如果A和B两个向量相互正交，那么这两个向量的内积应该是等于0，对吧？好，所以在刚才这个例子当中，大家可以自己暂停来验算一下，就是ABC这几个向量两两内积结果都是0，这就意味着这三个向量相互正交。那为什么要保证这些码片序列要相互正交呢？一会儿你会感受到它的作用。

发言人   06:21

好，我们让故事继续。刚才这个例子中只有A正在给C发送数据。接下来我们让情况变得复杂一点，让B站点也同时发送数据。假设在某个时刻A正在给C发送比特1B正在给C发送比特0。那么B站点发出的四个信号值就应该等于向量负B而A站点发出的四个信号值应该对应的就是向量A好，那么此时对于C站点来说，它会收到两个信号的叠加，也就是要把这两个向量进行一个加和加起来。所以C站此时收到的信号值就是0202。

发言人   07:09

这组信号值的数学本质就是向量A加上负的向量B好，那么接下来C站点如何从这个叠加信号当中把A和B发过来的数据给分离出来呢？这就需要使用到一些数学手段。好，我们先尝试着把结点A发过来的这个比特给分离出来。我们可以这么做用结点A的这个码片序列和我这个接收方收到的这个叠加信号进行一个内积，然后再乘以M分之1，这儿的M等于4。因为是四维向量，那我们可以把这个运算称为规格化内积。

发言人   07:58

好，那么结合线性代数的知识我们知道，由于向量A和向量B相互正交，所以这个式子应该等于M分之一A向量和A向量的内积。再加上M分之一A向量和负B向量的内积。那刚才说AB两个向量是正交的对吧？所以后面这部分就应该等于0。

发言人   08:25

那这部分去掉就只剩下式子的前半部分，就应该等于M分之一乘以A向量和它自身的内积。由于A向量的每一个分量都是一，所以它和它自己的内积应该是等于一加一加一加一也就等于M对吧？那再乘以一个M分之一就等于一好，如果规格化内积的结果等于一，那么蘸点C就可以确定结点A给我发过来的这个比特应该是一好。

发言人   09:04

接下来看结点B做法也是一样的，我们可以用节点B的这个向量去乘以节点C收到的这个叠加信号。同样的道理，B和A相互正交，所以二者相乘应该等于0。最后就应该得到这样的一个式子，B和负B进行内积。由于向量B的每个分量要么是一要么是负一，所以这个内积的结果肯定是负的M那再乘以前面的这个M分之一，运算的结果就应该等于负一。那计算结果等于负一意味着结点B发过来的这个比特应该是0。

发言人   09:46

好，我们再整体梳理一遍，A站要给C站发送一个比特一，那么它需要发出的信号值就应该等于A这个限量。B站要给C站发送比特林，那么它发出的信号值就应该等于负B这个向量。A和负B这两个向量或者说这两组信号值在传输的过程当中相互叠加。C站收到的信号值叠加的信号值从数学的角度看就应该是等于A向量加上负B这个向量。接下来C站需要从这个叠加信号当中把A和B两个节点发过来的这个数据进行分离，分离出A结点发来的数据的方法就是用A结点的码片序列和我收到的这个信号值进行规格化。内积运算的结果等于一，表示A结点给我发了一个比特一。

发言人   10:47

同样的道理要把B结点发过来的这个数据分离出来。那么C站点需要做的事情就是把C站点收到的这个叠加信号和B站点的这个码片序列进行规格化。内积运算的结果等于负一，表示B站点发来了一个比特0。好，没跟上的同学可以暂停消化一下。

发言人   11:11

接下来我们再举个例子，如果A和B同时发送三个比特，那么A站发出的信号就是向量A然后负的向量A再来一个向量AB站点发出的信号就是向量B向量B负的向量B那C站点这边收到的信号就应该是这样的，大家可以自己加一下。第一组向量对应的是A加B第二组对应的是负A加B第三组对应的是A加负B接下来C站点要从这几组信号值中分辨出结点A发来的是什么数据。他会用节点A的码片序列和这三组向量分别做规格化内积。第一组和向量A规格化，内积等于一，表示这是比特一。第二组和向量A规格化，内积等于负一表示比特0。第三组和向量A进行规格化，内积结果等于一，表示这是比特一。所以C站点就可以确定A站点给我发送了三个比特，分别是101。好，那么分离B站点发来的这个数据也是完全一样的道理，这儿就不再赘述。

发言人   12:26

好的，刚才讲解的这个例子其实是来自于2014年的真题。第37题说站点ABC通过CDMA共享链路ABC的码片序列分别是，这样这些数据和我刚才举的例子一模一样。然后C站点从链路上收到的这个序列是巴拉巴拉，反正就是有四组信号，每组信号有四个分量组成。

发言人   12:51

好题目问我们C站点收到A站点发送的数据是多少？那大家可以代入具体的数值去算一下，用这四组信号值分别和A站点的这个码片序列做规格化内积。第一组运算得到的结果是一表示比特1，第二组信号得到的结果是负一，表示比特0。第三组得到结果是一，表示比特1。所以A发过来的数据就是101这个题目选择B选项。作为训练大家也可以把B站点发过来的数据尝试着分离一下，反正就是和B站点的这个码片序列做规格化内积。相比于线性代数要考察的那些内容，这个计算简单多了对吧？

发言人   13:38

好的，在这个小节中我们介绍了用信道划分的方式去实现介质访问控制。首先我们学习了时分复用技术，时分复用技术就是会把信道的使用时间拆分成固定大小的时间片，然后各个节点依次分配一个时间片轮流去使用。时分复用技术对于这个时间片的分配是不灵活的，因此我们提出了统计。时分复用技术会在十分复用的基础上，给各个节点按需分配时间片。你的传输需求大，那么我就给你多分配几个时间片。采用这样的策略就可以大幅提升这个信道的利用率。

发言人   14:20

好，紧接着我们介绍了频分复用技术。频分复用技术的思想就是会把一个信道的总频带拆分成几条互不相交的子频带。并且子频带和子频带之间会加入隔离瓶带，用于防止这个子频带之间相互干扰。每一个发送节点会被分配一条子频带。类似的原理，当我们对光信号进行这种频分复用的时候，我们可以把它称为波分复用技术。因为本质上光的这个波长和光的频率是有关的，所以光的波分复用本质上就是光的频分复用，原理和上面这种技术是类似的。

发言人   15:06

好，最后我们介绍了码分复用技术，英文缩写叫CDM。在CDM技术当中，首先我们需要给各个节点分配一个专属的唯一的码片序列。一个码片序列会包含M个码片，也就是M个信号值。我们可以把码片序列看作是一个M维的向量。为了方便对这个向量进行处理，也就是为了方便做这个规格化内积，所以我们会规定这个M维向量的每个分量要么取一，要么取负一好另一方面为了从叠加信号当中分离出每个发送方发来的这个数据，所以我们要求给各个结点分配的码片序列，或者说给各个结点分配的这个M维向量必须相互正交。那这是由向量的数学特性决定的。

发言人   16:01

相互通信的各个节点需要提前知道彼此的这个码片序列。那每个节点有了自己专属的码片序列之后，它就可以开始发送数据。如果要发送比特1，那么就发送和码片序列相同的M个信号值。如果是要发送比特林，那么就要发出和码片序列相反的M个信号值。如果多个节点同时发送信号值，那么这些信号会在传输过程当中相互叠加。信号值的相互叠加，它的数学本质就是M维向量的加法。

发言人   16:39

好，最后接收方收到这个叠加信号之后，要从叠加信号当中分离出各个发送方发来的数据。这就需要使用一些数学手段，接收方会用他收到的这个叠加信号与某一个发送方的码片序列进行规格化内积。如果规格化内积的结果为一，表示它受到的是比特一。如果规格化内积结果为负一，那么表示它受到的是一个比特0。好，大家可以再结合这页PPT进行回顾，以上就是这个小节的全部内容。