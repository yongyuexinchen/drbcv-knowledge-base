# 012 5.3.6_1 TCP拥塞控制（慢开始和拥塞避免）

> Source: 012 5.3.6_1 TCP拥塞控制（慢开始和拥塞避免）_原文.docx

012 5.3.6_1 TCP拥塞控制（慢开始和拥塞避免）_原文

2026年07月19日 12:20

发言人   00:00

好，接下来看TCP这个部分的最后一个考点，拥塞控制在大纲当中处于这个位置。在TCP协议中实现拥塞控制的算法主要有这样的三种。对于考试来说最重要的是慢开始算法，还有拥塞避免算法。往年出现过的考点相关的题目几乎都是这两个算法。除了这两个算法之外，解决拥塞控制还有一种算法叫做快恢复算法。这种算法的触发条件是当发生了快重传的时候，也就是我们之前介绍过的三个冗余ACK那种情况。当发生快重传的时候就会触发快恢复这个算法。在这节课当中我们主要探讨前两个重点算法，而后面的这个快恢复算法我们放在下节课再来探讨。

发言人   00:51

好，首先初学者很容易把拥塞控制和流量控制混起来。接下来我们会探讨这二者的区别与联系。来看这样的一个例子。假设这是一个比较复杂的网络，网络当中有很多路由器组成，那我们知道一个网络上面可能会连接很多台主机在这儿我们用ABCDEF来表示。假设现在A主机和B主机上面的两个进程正在进行这个TCP连接。那么流量控制这个功能就是要控制TCP连接的双方，给彼此发送数据的这个速度。也就是说流量控制这个功能，它只管局部的问题。

发言人   01:38

好，再来看什么是拥塞控制。拥塞控制它要控制的是整个网络当中每台主机的数据发送量。当发生拥塞的时候，希望尽快的降低网络中这些路由器的负载。所以拥塞控制它是一个全局的问题。

发言人   01:55

不知道大家平时上网的时候有没有遇到过，突然一下子就是家里的网络特别卡，或者整个学校的网络都特别卡。有没有遇到这种情况？假如假如这是你们学校的网络，在高峰期的时候，比如说睡觉之前，可能整个学校所有人都在用这个网络刷视频，打游戏之类的那在这种时候，整个学校的这些主机是不是都在疯狂的给这个网络注入新的数据，那此时网络中这些路由器的负载就会变得很大。如果说这些主机注入的数据量太大，超出了整个网络的传输能力的话，那么网络当中就会经常出现丢包的情况。此时你就会感受到整个网络变得很卡，对吧？那当你感受到整个网络好像都变得很卡的时候，这个时候我们就说发生了拥塞。

发言人   02:51

对了，有时候我也会把这个词读作拥塞。因为平时我们说堵车塞车的时候都是念塞，但是正规的读法应该是读拥。在接下来的讲解中，我可能偶尔会把它念作拥塞。

发言人   03:04

好，那么回到这个网络，当这个网络出现拥塞的时候，我们应该如何去缓解这个拥塞的情况呢？最直接的办法就是迅速的减少各台主机往这个网络上发送的这些数据量，对吧？只要所有的这些主机发送的数据量都往下降的话，那么整个网络的负载是不是就可以变低，网络的拥塞就可以得到改善。

发言人   03:31

对于TCP协议来说，我们应该如何去迅速的减少一个进程，或者说一台主机它发送的数据量？是不是可以用我们之前学过的发送窗口这个东西去作为限制，一个进程的发送窗口越小，那么就意味着这个进程往网络上发送这个TCP报文段的速度也会受到限制，对吧？那之前我们在学习流量控制的时候说过，发送方的这个发送窗口受到接收方的接收窗口的大小限制。那接收窗口和发送窗口这二者配合就可以实现收发双方的这个端到端的流量控制。

发言人   04:16

现在我们还想用发送窗口去解决网络拥塞的问题。因此我们引入一个新的概念叫做拥塞窗口。这个拥塞窗口是每一个发送方都会维护的一个变量，数据的发送方它的发送窗口大小不能超过接收方的接收窗口大小，也不能超过拥塞窗口的大小。接收窗口这个值反映的是数据的接收方它接收数据的能力，对吧？而拥塞窗口这个值反映的是当前这个网络它传输数据的能力。

发言人   04:57

那有了拥塞窗口这个变量之后，我们就可以怎么做呢？当数据的发送方检测到网络拥塞之后，它就迅速的降低自己的拥塞窗口。那拥塞窗口降低就意味着自己的发送窗口也会降低，对吧？这样的话相当于就限制了自己往这个网络上继续丢数据的这个速度。

发言人   05:24

显然网络拥塞这种问题并不是一台主机的问题。当一台主机检测到网络拥塞的同时，其他主机也会同时检测到这个网络拥塞。那每台主机在检测到网络拥塞之后，都主动的降低自己的这个拥塞窗口。那所有的主机都这么做之后，整个网络的负载是不是就降低了？就像那首歌唱的，只要人人都献出一点爱世界将变成美好的人间，对不对？好，那每一台主机他们都降低自己发送的这个数据量，那么整个网络就会变成一个美好的不拥塞的网络。

发言人   06:09

好，现在拥塞控制的总体思路我们已经捋出来了。接下来我们应该思考的就是如何去判断网络有没有发生拥塞。首先看第一种情况，假设对于一台主机来说，它的传输层TCP这边发出的每一个TCP报文段都能够顺利的收到ACK都没有超时。那这种情况下数据的传输是很顺利的对吧？此时说明整个网络肯定没有发生拥塞。好，再看第二种情况，如果说一台主机的这个传输层，它的TCP发出的报文段出现了超时重传。那么此时我们可以认为整个网络发生了比较严重的拥塞。

发言人   06:55

通常来说发出一个报文段之后，没有按时收到AK导致这种现象的主要原因是路由器发生了丢包的状况。什么意思？比如说C这台主机它发出了一个TCP报文段，那这个TCP报文段在网络层是被封装成一个埃批数据报的对吧？那假如这个埃批数据包到达中间的某一台路由器的时候，由于这台路由器此时负载很大，所以它来不及接收这个主机发来的这个TCP段。那这样的话，对于C主机而言，它发出的这个TCP段是不是就会迟迟收不到ACK？那路由器丢包说明此时路由器的负载已经很重了，因此我们可以判定为发生了严重的拥塞。好，所以如果检测到严重拥塞的时候，这个数据的发送方是不是就应该迅速的尽可能快地缩小自己的这个拥塞窗口，也就是需要迅速的把自己的这个数据发送量给压下来。好，这是严重拥塞的情况。

发言人   08:03

再来看另一种情况，假如数据的发送方收到冗余ACK，也就是之前我们学习的快速重传那种机制，收到冗余ACK那么此时我们可以判定为网络可能有一点拥塞。收到冗余ACK意味着什么？意味着数据的发送方可能发出了4个TCP报文。其中3个TCP报文可以顺利的到达，另外一个TCP报文可能是丢失了，对吧？那么在这种情况下，既然发出四个可以到三个，只丢了一个的话，我们可以判定此时网络可能有点拥塞。但是还没有拥塞到特别严重的情况，对吧？好，所以如果发送方收到冗余AK并且引发了快速重传，那么此时就判定为网络有点拥塞。那既然网络有点拥塞，接下来这个数据的发送方是不是应该自觉的适当的缩小一下自己的这个拥塞窗口。

发言人   09:05

这就是拥塞控制的总体思想。首先数据的发送方要判断当前这个网络到底是没有拥塞，还是发生了严重拥塞，又或者是有一点拥塞。当发生严重拥塞的时候，数据的发送方应该自觉的迅速缩小自己的拥塞窗口，从而迅速的限制自己的这个数据发送量。而如果此时网络有点拥塞的话，那么数据的发送方就适当的缩小自己的拥塞窗口。那么慢开始这种算法可以用于网络严重拥塞的时候，这种算法可以迅速的把拥塞窗口变小。好，另外快恢复这个算法，它用于解决当网络有点拥塞的时候，适当的降低这个拥塞窗口的大小。而拥塞避免算法主要是在网络还没有发生拥塞的时候，小心翼翼的去调大这个拥塞窗口。

发言人   10:08

好，这节课我们主要讨论的是慢开始和拥塞避免这两种算法。大家可以翻开王道书找到这张图，只要我们把这张图理解清楚了，那么拥塞控制这个考点就难不住你那为了辅助大家理解这张图，我制作了一个和这张图完全配套的一个另外的一张图。我做的这张图是用大家更熟悉的这种方式，带大家看到背后发生的一些过程。接下来的讲解我会尽量的贴合真题风格，所以我认为有必要先跟大家解释一下拥塞控制的考题特点。因为接下来的例子也会尽可能的去贴合真题的这种考题特点。

发言人   10:53

好，首先第一个特点，棕色控制这个考点它经常与流量控制综合考察。也就是说在真题当中发送窗口的大小，我们得综合考虑接收窗口有多大，以及拥塞窗口有多大不过在这节课的例子中，为了聚焦于这个拥塞控制相关的功能，我们接下来讨论的这张图会假定接收方的接收窗口足够大会基于这样的前提条件去进行探讨。也就是说在这节课的例子中，我们可以认为发送窗口的大小等于拥塞窗口的大小。那关于拥塞控制和流量控制是怎么综合来考察的，这个我们在学完之后会用一道真题来感受。

发言人   11:40

好，接下来拥塞控制的第二个考题特点就是在历年真题当中以及在绝大多数的教材当中，探讨拥塞控制问题的时候，通常只涉及单向传输。也就是说TCP连接的双方通常只有其中的一方在发送数据。那这样去考察的话，相当于考察的难度就不会过高，也不会变得太复杂。所以在这节课要讲的这个例子当中。我们也会假设只有客户进程在给服务器进程不断的发数据。服务器进程没有数据要发给客户端，它只会返回纯粹的ACK报文段。好，这是真题的第二个特点。

发言人   12:23

接下来真题的第三个特点，通常来说会默认每个TCP报文段都是以最大段长，也就是满载数据的。比如说像09年的这个题目，它就说一个TCP连接总是以1KB的最大段长去发送TCP段。然后后面巴拉巴拉给了一堆条件，最后考这个拥塞窗口的大小应该等于多少。类似的15年这道题，他说甲乙双方建立了TCP连接，然后甲向乙发送数据，始终以这个最大段长等于一KB去发送这个TCP报文段。最后问甲的发送窗口等于多少？好，所以拥塞控制这个部分，几乎所有的真题都会给大家这样的一个条件。每一个TCP报文段都是满载数据的。

发言人   13:16

接下来真题的第四个特点，拥塞窗口的大小，通常来说是以最大段长的倍数作为单位。比如说假设最大段长等于1KB而拥塞窗口的大小等于2。那等于二的意思是说这个拥塞窗口的大小是2KB2倍的MSS是这个意思。那在接下来的讲解中，我们也会用这样的方式去记录拥塞窗口的大小。你要知道拥塞窗口的大小，一二这个值表示的是它是最大段长的多少倍，是这个意思。

发言人   13:56

好，最后一点，在所有的拥塞控制相关的考题当中，我们都会默认接收方收到一个报文段之后会立即确认。哪怕收到失序报文段也会立即返回一个ACK。那这就会导致什么？就会导致冗余ACK对吧？最终就会引发我们之前学习的快重传。好，这是真题的第五个特点。接下来我们也会默认数据的接收方它采用立即确认的这种策略，而不会推迟确认。

发言人   14:33

好，接下来观察这张图。在这张图当中横坐标表示的是传输轮次，本质上它体现的是时间的概念。它以往返时间作为单位。比如说二这个刻度表示的是从零时刻开始到两倍的RTT，这个时间点应该是什么样子。然后六这个刻度表示的是到6倍的RTT这么长的时间的时候，这个拥塞窗口的大小是什么样子。好，所以横轴表示的是时间，它是以RTT的倍数作为单位的。

发言人   15:13

接下来看纵轴，纵轴这边表示的就是拥塞窗口的大小。比如说在四倍的RTT这个时间点，拥塞窗口的大小是十6。那根据刚才的解释我们知道拥塞窗口等于16，它表达的是拥塞窗口等于16倍的MSS，对吧？它想表达的是这个意思。

发言人   15:37

好，接下来看左边这个地方，你会看到一串很奇怪的英文缩写，叫做SSTHRESH。这个东西正规的翻译应该把它翻译为慢开始门限。但是在真题当中通常把这个值称为拥塞控制的阈值。所以接下来我会用阈值这个术语去指代SSTHRESH去指代这个东西。

发言人   16:04

好，我们来解释一下这个阈值有什么作用。可以看到这儿对应的是阈值的初始值等于16，这就意味着拥塞窗口大于16还是小于16，这是一个分界线。当拥塞窗口的大小小于这个阈值的时候，我们会让拥塞窗口的增长速度尽量的快一点。而一旦拥塞窗口大于这个阈值，我们就需要采用一种更谨慎的做法。我们不能无限制的让拥塞窗口快速增长。一旦超过这个阈值，那么拥塞窗口每经过一个RTT就只能让它加一好。所以这个阈值是拥塞窗口的大小如何变化的一个分水岭一个分界线。

发言人   16:54

好，那么现在我们假定这个阈值的初始值等于16。接下来我们看零这个时刻到4倍的RTT这个时刻。在这段期间我们会采用慢开始算法。

发言人   17:09

顾名思义，当一台主机或者一个进程，它刚开始往一个网络当中去发送数据的时候，刚开始不能发的太快，对吧？就像一个从来不锻炼身体的人，他如果刚开始想要尝试锻炼的时候，这个运动量不能太大。如果说第一天的运动自己能承受，那么第二天翻一倍，第三天运动量再翻一倍，第四天运动量再翻一倍，第五天运动量再翻一倍。那假如你是那个锻炼的人，每天你的运动量都会在前一天的基础上翻一倍，那么这么翻下去你很快就会累死，对不对？好，所以你作为运动的人，你会给自己设置一个阈值，就是当你某一天的运动量达到16这个阈值的时候，再往后你就不能每天翻一倍了。如果前一天的这个运动动量你能承受的话，那么你会选择后一天这个运动量再加一对吧？逐步的往上走，就是这样一个意思。

发言人   18:12

好，那么对于TCP协议数据的发送方来说也是一样的。当自己刚开始往一个网络当中发送数据的时候，因为刚开始自己还不确定这个网络到底有没有发生拥塞，对吧？所以刚开始得谨慎一点，先把拥塞窗口的值设置为一从一开始接下来每收到一个ACK就让拥塞窗口的值加1。那这种策略就会导致每过一个RTT，这个拥塞窗口的大小就会翻一倍。

发言人   18:48

好，我们用这张图换一个视角来理解。我们说当一个TCP节点刚开始发送数据的时候，首先会把自己的这个拥塞窗口的值设置为一，这是零，这个时刻设置为一。另外我们假设这个初始的阈值等于16，好，拥塞窗口设置为一意味着此时这个进程的发送窗口大小也等于一。别忘了我们假设的前提条件，我们是假设接收方的接收窗口足够大对吧？那基于这样的条件，发送窗口的值就永远等于拥塞窗口的值。

发言人   19:26

好，所以零这个时刻数据的发送方可以给数据的接收方发送的这个数据量。如果按照满载数据这种形式来发送的话，它就只能发送一个TCP报文段，对吧？所以在这个图当中你只看到一条线，这条线对应的就是一个满载数据的TCP报文段，其中携带的数据就是MSS。这么多好，那么数据的接收方收到这个TCP报文段之后，它会立即确认，于是返回一个ACK。

发言人   20:00

刚才我们说采用慢开始算法的时候，我们每收到一个ACK就让这个拥塞窗口的值加1。由于这个时刻收到了一个ACK对吧，所以拥塞窗口的值就在原有基础上加了一就来到了二这个大小。拥塞窗口变为二意味着发送窗口也变为了2，对吧？那这就意味着接下来这个数据的发送方，它可以连续发出2个TCP报文段，每个报文段都是携带MSS这么多的数据。好，所以在这个地方你会看到两条线对吧？

发言人   20:38

好，接下来到接收方这边，由于采用的是立即确认的这种方式，所以每收到一个TCP报文段，它就会返回一个ACK。因此返回了2个ACK之后，在二倍RTT这个时间点，发送方这儿它的这个拥塞窗口是不是又可以加二了，每一个ACK会加一对吧？那么2个ACK就加2。好，现在拥塞窗口的值变成了4，那接下来发送方就可以连续的发出4个TCP报文段，相应的接收方也会返回4个ACK对吧？连续返回4个ACK好，那么在三倍rt t这个时间点发送方收到了4个AK于是拥塞窗口又可以加四到了八这个值。所以在这个时间点发送方可以连续发出八个段，那相应的接收方这边又会返回8个ACK。好，所以到4倍RTT这个时间点，发送方的这个拥塞窗口又会加八到达16这个值。那十6刚好就是我们之前设置的那个初始阈值。

发言人   21:53

好，那么拥塞窗口的大小到达这个分界线之后，再往后就得切换为拥色避免的算法。接下来拥塞窗口的增长我们得谨慎一点，不能每隔一个RTT就让它翻倍。来看一下拥塞避免算法规定在一个RTT内即使收到多个ACK，我们也只能够让拥塞窗口的值加1。当拥塞窗口大于等于阈值的时候，就会使用这个拥塞避免算法。相比之下，前边这个慢开始算法，它每隔一个RTT拥塞窗口就翻倍，对吧？

发言人   22:32

如果一直这么翻倍下去，那拥塞窗口可能会变得特别大，这就有点害怕对吧？好，所以你会看到在四倍的RTT这个时间点，由于此时拥塞窗口的值等于16，所以发送方可以连发16个报文段，而接收方也会连回16个ACK。由于此时我们已经切换成了拥塞避免算法，所以虽然收到了16个ACK，但是在这一个RTT内我们只能让拥塞窗口加1，所以这个地方让它增加的慢一点，谨慎一点。好，所以到5倍RTT这个时间点，拥塞窗口的值只增加了一来到了17这个值。好，那接下来就可以连发17个报文段，然后又会连续收到17个ACK。同样的由于拥塞窗口已经超过阈值了，所以虽然说到多个ACK我们也只能让它加一好，那后面的就以此类推，一直到12倍的RTT的时候，在这个时间点发送方收到了23个ACK，但是这个拥塞窗口的值只能加1，变成24这么大好，现在又可以连续发出24个报文段，此时故事发生了，在12倍的RTT到13倍的RTT这段时间内，发送方发出的某一个或者某几个报文段发生了超时。

发言人   24:00

按照之前我们分析的逻辑，一旦发生了超时重传，说明此时网络可能发生了严重的拥塞，对吧？而当网络发生严重拥塞的时候，我们应该干什么？应该迅速的缩小拥塞窗口，也就是应该迅速的压制自己发送的这个数据量。好，所以在这个图当中你会看到在12倍的RTT这个时间点，一旦发生了网络拥塞，或者更具体一点来说是发生了超时重传这个事件。那么当这种事件发生的时候，我们会迅速切换成慢开始算法。而慢开始算法规定这个拥塞窗口的值是从一开始的对吧？所以你会看到拥塞窗口的大小从24这么大一下子回归到了一这个起点。在这张图当中你也会看到这一点，在13倍的RTT这个时间点，拥塞窗口的值又恢复到了一。

发言人   25:00

除了拥塞窗口的值要发生改变之外，门限这个值也应该发生改变。我们会把门限的值设为拥塞窗口值的一半。由于发生这个超时重传之前，拥塞窗口的值是24，对吧？那24除以2等于12。所以发生超时重传的时候，我们会把这个门限值让它从16变成12。在这张图当中对应的就是这个东西。

发言人   25:30

当发生网络拥塞的时候，我们迅速的减小了拥塞窗口，也适当的减小了这个门限的值。其实这个门限值降低本质上也是为了缓解拥塞，对吧？因为这个门限值越小，意味着接下来这个拥塞窗口的增大趋势会变得更慢一些，对不对？

发言人   25:50

大家也需要在王道书上补上这几个字。这个点它对应的网络拥塞指的是超时重传这种事件。只有超时重传的时候，会一下子把拥塞窗口变成一。如果不是超时重传，而是收到冗余ACK，那么这个拥塞窗口的值不会下降的这么快。好，所以要在自己的书上补上这几个字。

发言人   26:15

好，现在在13倍的RTT这个时间点，我们又回到了慢开始算法。在接下来这段时间内每收到一个ACK就会让拥塞窗口加1。这就意味着在接下来这段时间内，每隔RTT这么长的时间，拥塞窗口的大小就会翻倍。

发言人   26:34

好，回到这张图，首先发出一个TCP返回一个ACK，每收到一个ACK就会让这个拥塞窗口加1。所以在14这个时间点拥塞窗口到了2，接下来发出2个TCP报文段，然后返回2个ACK，每个ACK导致拥塞窗口加1，所以15这个时刻拥塞窗口变成了4。接下来发出四个报文段，返回4个ACK在16这个时刻拥塞窗口变成了八好，接下来不一样的东西来了，16这个时刻可以发出八个报文段，然后返回8个ACK那照理来说我们现在是在慢开始算法的阶段，对吧？照理来说8个ACK就应该对应拥塞窗口加八好，但是这儿有个特殊的情况，由于刚才我们已经把阈值降为了12，所以当发送方收到这8个ACK的时候，他这次的拥塞窗口不能加8，只能加4，也就是不能超过阈值的限定。好，所以在这张图当中你也会看到这一点。

发言人   27:46

照理来说17倍的RTT这个时间点应该是在16这个时间点的基础上，优塞窗口翻倍对吧？但是翻倍之后会直接超过这个阈值，所以这个翻倍的动作会被限制。因此17这个时间点拥塞窗口的大小再一次来到了这条分界线。

发言人   28:07

那既然到了这条分界线，再往后是不是又得切换成拥塞避免算法了。接下来拥塞的窗口的增大我们得谨慎一点，每一个RTT最多只能让它加一好，所以从17这个时刻开始就切换为拥塞避免算法。根据拥塞窗口的大小可以发出12个段，返回12个ACK。那么在18这个时刻虽然收到了12个ACK，但是由于采用的是拥塞避免算法，所以我们只能让拥塞窗口加1，来到了13这个值。再往后就不再赘述，反正每隔一个RTT最多只能加1。最终到21这个时刻，拥塞窗口的大小到达了16，对应这张图就是来到了这个终点。再往后的过程就不再赘述，都是一样的。

发言人   29:00

好，来总结一下，当一个TCP节点刚开始传送数据的时候，需要执行慢开始算法。慢开始算法会把这个拥塞窗口的初始值设为一，接下来每收到一个ACK就让拥塞窗口加1。这就意味着在慢开始阶段，拥塞窗口每经过一个RTT就会翻倍。那么当拥塞窗口的大小大于等于这个阈值的时候，就需要切换为拥塞，避免算法每一个RTT最多只能让这个拥塞窗口加1。

发言人   29:38

如果某一个时刻发生了超时重传这种事件，此时说明网络当中出现了严重的拥塞。那在这个时刻首先我们得重启这个慢开始算法，会把拥塞窗口的值重新回归到1。另一方面我们会把这个阈值设为发生拥塞的时候，这个拥塞窗口的大小的一半，设为这样的一个值。再往后的过程就是类似的拥塞控制。

发言人   30:10

这个考点最重要的就是这张图，把这张图理解透了，任何题目都难不住你好，接下来用两个真题来练手，看09年的39题说一个TCP连接总是以1KB的最大断肠去发送TCP段，发送方有足够多的数据要发送。当拥塞窗口为16KB的时候发生了超时。问发生了超时之后，接下来的4个RTT时间内所有的TCP段传输都是成功的。那么问当第4个RTT时间内发送的所有的TCP段都得到肯定的应答的时候，拥塞窗口的大小应该是多少？

发言人   30:55

好，来整理一下MSS等于1KB我们用小写字母C代表这个拥塞窗口的大小。拥塞窗口的大小等于16倍的MSS。在这个时刻发生了超时，那超时这个事件会导致什么？首先阈值我用小写的S来代替，预值会等于当前的这个拥塞窗口值的一半。那当前拥塞窗口值是16，除以2就等于8对吧？

发言人   31:24

阈值变成了8，另外拥塞窗口要变成一好，那么这就是发生超时的时候会做的一个调整。那我们不妨把超时的这个时刻记为零时刻，零时刻拥塞窗口等于一，阈值等于8。好，那么在一倍的RTT以后，这个拥塞窗口是不是会翻倍，变成二对吧？S等于8，接下来再经过一个RTT，在两倍的RTT的时候，优塞窗口又会翻倍变成4，阈值同样等于8。接下来在第3个RDT的时候，拥塞窗口变成8，阈值同样等于8。好，那么在第3个RDT这个时刻，拥塞窗口的值已经到达了这个阈值，对吧？所以到第4个RTT的时候，虽然会收到8个ACK，但是这个拥塞窗口只能再加一了，对不对？

发言人   32:21

因为我们强调过，只要它超出了这个阈值，那么每隔一个RTT最多只能加1，所以到4倍RTT这个时刻，拥塞窗口就变成了9，阈值同样等于8。所以这个题目选择C选项对吧？从零这个时刻开始，这是第一个RTT，这是第2个RTT，第3个RTT，第4个RTT。那题目问的就是在第4个RTT时间内发送的这些TCP报文段都得到肯定应答。也就是都收到ACK之后，拥塞窗口的大小应该等于多少？再次强调C等于九的意思是拥塞窗口是九倍的MSS。假如这个题目假如这个题目它把MSS设置为2KB那么答案应该等于多少？就应该等于2乘以9，就应该是18KB就得选这样的选项。好，所以不要忘了拥塞窗口的这个数字和MSS之间是有联系的，这些细节不注意很容易掉到坑里。

发言人   33:26

好，这是09年的题目，在这个题目当中发送方的发送窗口它只受到这个拥塞窗口的限制。因为这个题目没有给任何条件，告诉你说这个数据的接收方，它的接收窗口的大小是怎么变化的对吧？它没有给这样的条件，所以在这个题目当中，发送窗口的大小就只受限于这个拥塞窗口的大小。

发言人   33:53

那接下来我们再看15年这道题，这个题目我们就得把拥塞窗口和接收窗口同时考虑进去。好，来看一下说主机甲和主机乙建立了一个TCP，然后甲的拥塞窗口初始阈值为32KB然后甲项乙始终以MSS等于1KB的大小去发送数据。好，我们先记下来，MSS等于1KB初始的阈值等于32倍的MS好，继续往后说。这个主机甲一直有数据发送，主机乙为该连接分配了16KB的接收缓存。接收缓存的大小会影响什么？是不是会影响接收窗口？

发言人   34:41

好，再往后读说这个主机乙会对每一个数据段进行确认，什么意思？它这想表达的就是它采用的是立即确认对吧？而不是推迟确认。

发言人   34:51

好，并且告诉我们忽略段的传输实验，传输实验怎么理解？大家有没有发现，当我们在画这个TCP的传输过程这个示意图的时候，我们所有的这些线它都是一根很细的线，我们没有画出这条线的宽度，对吧？如果这条线有宽度，比如说一条线画这么宽的话，那么这个宽度其实就对应着TCP报文段的传输实验。一般来说我们在探讨TCP的这个传输过程的时候，我们只考虑TCP报文段的传播时延传播实验，而不考虑TCP段的传输实验。历年真题也基本上都是这样。好，所以真题说话还是很严谨的，他把该提到的边边角角条件都会给你交代清楚。

发言人   35:45

好，接下来说。如果主机已收到的数据全部存入缓存并且不被取走，那么问主机甲从连接建立成功的时刻起，并且没有发生发送超时的这种情况。那么经过400RTT之后，甲的发送窗口应该等于多少？

发言人   36:07

好，我们回头琢磨一下这句话说主机甲收到数据存入缓存之后不被取走，不被取走是什么意思？就是数据放到缓冲区里，但是暂时不交付给应用层，是这个意思对吧？也就是说在这道题目当中，我们可以认为这个接收缓存它只会往里边填入新的数据，而不会去清空这个缓存。这意味着16KB的接收缓存剩余的空间会越来越小，对吧？

发言人   36:40

那接收缓存的剩余空间变小会影响的是什么？是不是就会影响接收窗口的大小？好，所以从刚建立起连接的那个时刻开始算起，我们把这个时刻记为零。在这个时刻阈值等于32，接收方的接收窗口大小等于多少？我们用小写的R来表示接收窗口大小。只有16对吧？整个接收缓冲区就只有这么大好那么刚开始传送数据的时候，主机甲的拥塞窗口应该设置为多少？应该设为一对不对，这是慢开始算法规定的。那么在零这个时刻，主机甲的发送窗口大小就应该等于拥塞窗口和接收窗口当中更小的那个值，对吧？

发言人   37:29

现在我们得综合的考虑接收窗口和拥塞窗口的限制了。好，既然发送窗口等于一，因此在零这个时刻，主机甲是不是只能给主机乙发送一个TCP报文段？那此时主机甲的拥塞窗口就会加一C就变成了2，对吧？那这个阈值肯定没有发生改变，同样等于32，那接收窗口现在还剩多少公斤？16的接收窗口收到了一个报文段，因此接收窗口的剩余容量就只剩下了15。好，所以在一这个时刻，发送窗口的大小就应该等于2。那发送窗口等于二意味着甲可以向乙发送2个TCP报文段，对吧？

发言人   38:16

好，所以到两倍的RTP这个时刻，S的值没有发生改变，接收窗口的值会从15变成13。因为主机意会把两个报文段的数据放到缓存里，并且这些数据是不交付给这个应用层的。好，那主机甲的拥塞窗口是不是会变成4？又翻了一倍，那此时发送窗口就变成了4。

发言人   38:43

好，下面到第3个RTTS的值不变，R的值变成了13减4只剩下九的大小。而拥塞窗口经过一个RTT之后再次翻倍变成8。那此时发送窗口的大小是不是就等于8？那此时意味着主机甲还可以给主机乙连发八个报文段，因此在四倍的RTT这个时刻阈值S不变，主机乙的这个接收缓冲区只剩下一的空间了，对吧？九个空位当中又消耗了八个空位，只剩下一个空位。

发言人   39:23

好，另一方面主机甲的这个拥塞窗口再次翻倍，因为这个拥塞窗口还没有超过阈值，所以每隔一个RTT它总能翻倍。好，那么到4倍的RTT这个时间点，发送窗口的大小等于多少就等于一了。因为我们说发送窗口的大小同时受限于接收方的接收窗口大小以及拥塞的窗口大小，要取更小的那个值对吧？好，所以经过4个RTT之后，甲的发送窗口A选项。好，这是15年的这个题目。

发言人   40:01

之后大家做拥塞控制分析的这种题目，也可以参考我这儿的这种写法。首先一定要提醒自己，把MSS专门写下来。因为MSS的值决定了这个CRS它们真正的值等于多少，我们最终都要把这些数字乘以MSS。在分析的过程当中，我们从零这个时刻开始，每隔一个RTT你都要梳理出这个阈值，接收窗口、拥塞窗口和发送窗口，要梳理出这些东西。这样顺着往后分析，最终你就可以得到题目，要你求的这个答案。

发言人   40:42

好的，那么在这个视频中我们主要介绍了慢开始和用塞避免。截至目前所有的历年真题都只考这两个算法，还暂时没有考过快恢复算法。而关于这两个算法的复习，其实只要理解这张图就完全够了。好，所以这节课就不写思维导图了，用这张图复习比什么都好好的。以上就是这节课的全部内容。