69 5.3_2 数据通路-专用通路结构_原文

2026年08月04日 00:24

各位同学大家好，在这个小节中我们要继续探讨数据通路这个问题，我们会着重探讨专用数据通路的结构。上小节中我们学习了单总线的结构，如果采用这种结构就意味着同一时刻只允许两个部件之间进行数据的流通。如果说多个部件之间想同时交换数据，那单总线结构是不能支持的。但事实上我们指令执行的过程中有的部件，它们之间的数据交换逻辑上是可以同时进行的。如果能够让多个部件之间同时进行数据的流动，那么显然我们指令执行的速度也可以进一步的得到提升。所以基于这样的需求，我们可以建立多总线，用多个总线来同时传输多组数据。

另外还有一种方法就是可以建立专用的数据通路，在任何两个需要进行数据流动的部件之间，都建立起一个专用的数据通路。比如说可以用这样的方式来连接各种各样的寄存器。别看这个图现在看起来复杂，但是其实它的原理和单总线的结构没有太大的区别。这些什么C2、C5、C9指的是控制信号。当这个控制信号有效的时候，就意味着它所控制的这条数据通路可以进行数据的流通传送，是这样一个意思。所以和上小节的内容其实并没有太大的区别。只不过这个图看起来好像更复杂了一点，大家不要被吓到。

好，接下来我们基于这样的一种专用数据通路的方式来探讨一下，如果我们想要取出一条指令，那么需要发出哪些控制信号。首先指令的存放地址肯定是在PC当中的，所以我们应该让PC的值先传到MAR当中，因此就需要给出C零这个控制信号。好，这是第一步要做的事情。好，接下来这个指令的存放地址，这个信息是不是还应该通过外部的地址总线传送给给主存，传送给主存，所以我们应该接通C一这个信号，这是第二步要做的事情。好，那现在主存接收到了地址信息，那我们还应该给主存发送一个读控制信号，我们想要读取那条指令的数据。好，所以接下来要发出的一个控制信号是啊这样的一个控制信号。

好，现在主存已经收到了地址，也收到了读的信号。那么主存是不是应该通过这个外部的数据总线，把这个数据传送给MDR。因此我们应该给C二这个信号有效，这样我们就读入了这条指令的数据。那接下来这条指令要执行之前，是不是还应该把它送给IR指令寄存器。所以接下来我们应该让C三这个控制信号有效。

好，这样我们就大致完成了取指令所需要做的工作。不要忘记我们每取出一条指令都需要让PC加1，这个题目并没有告诉我们PC加一的控制信号是哪一个，所以我们直接描述PC加E的过程就可以。好，接下来我们是不是还应该把这个指令的操作码部分，把它送给CU进行指令E对吧？

好，所以接下来应该让C四这个控制信号有效，把指令的操作码部分送给CEO进行指令一码。这个地方我们用了灰色的字体来标注这个过程。原因在于有的题目不会给你一个专门的控制信号来控制这个指令寄存器神器到CU的数据通路。如果题目中没有给出这个控制信号的具体名字，那我们只需要写前边这个部分就可以，后面这个部分就不用管好。这就是基于专用数据通路的接够在取指周期所需要做的一系列事情。

好，接下来给大家一个小小的练习。如果说我们当前刚才取出的这条指令是一条加法指令，然后这个加法指令指明了某一个地址，就是操作数在主存当中的存放地址。然后另一个加法的操作数已经提前存到了ACC寄存器当中。那么接下来这条加法指令的执行过程应该是什么样的？每一个步骤需要发出哪些控制信号？这个问题留给大家自己分析，简单说两句。

这个算术逻辑单元其中的一个输入来自于ACC累加寄存器，然后另一个输入可以直接来自MDR。那这是不是意味着我们可以先把这个加法指令指明的操作数，先把它取到MDR当中，然后把MDR和ACC同时作为输入给到ARU就可以。好，具体的就不再展开，留给大家自己去练习。

好，接下来我们用一个课后习题来感受一下这个部分的考察形式。好，这个题目给了我们一个图，它是一个简化的CPU与主存连接的结构示意图，图中省略了所有的多路选择器。多路选择器我们之前有给我介绍，就是如果采用这种专用数据通路的连线方式，那么有的寄存器，比如说这个地方C这个寄存器它有可能同时会接到多个输入。那么我们可以在这一端接上一个多路选择器，用来选择到底让哪个输入信号通过。除了多路选择器之外，之前我们还说过也可以使用三态门来分别的连上三个输入的线路。只有微操作发生器给某一个三态门发出了有效信号之后，这个输入的通路才会被导通，所以多路选择器和三态门这个是我们之前提到过的一个小知识，这儿再带大家回忆一下。

好，继续往后读题。这个CPU内部有一个累加寄存器ACC已经给出了。然后还有一个状态数据寄存器，也就是这个地方状态寄存器是不是就是PSW程序状态字寄存器，也有的地方会把它称作标志寄存器。好，除了ACC和PSW之外，还有其他的四个寄存器，也就是标出的ABCD4个。然后这四个寄存器分别是MARMDRPC和IR。

然后第一个小问是让我们推测出ABCD这四个寄存器的名称分别是什么？也就是要把MAR、MDRPC和IR填到合适的位置。好，那来看一下首先很明显的一个特征，这儿有个加一对吧？每一次取出一条指令，PC的值都必须自动的加一，所以D肯定是PC好，那我们继续顺着这个线索进行推测，PC有一条数据通路直接的指向了CPC里的数据应该是会被输入到MAR对吧？因为我们需要根据PC所指明的的地址去找出接下来要执行的这条指令，因此C一定是MAR。

好，继续。这个地方我们看到B这寄存器，它和V操作发生器直接有一个连线，需要把数据直接输送给这个V操作发生器的是不是只有指令寄存器？我们需要把指令的操作码部份送给微操作发生器。因为其中包含了指令译码器用来解析操作码，所以和微操作发生器直接相连的一定是IR指令寄存器。

好，那最后剩下的A肯定就是MDR对吧？直接从这个数据通路的连线也可以推测出它是MDR。因为你看它和主存M有直接的连线，和主存直接相连的寄存器只有MAR和MDR2个，而MAR里边存放的地址数据只有可能是单向的传送给主存。而由于我们有可能对主存进行读，也有可能进行写，所以MDR里的数据和主存应该是有可能进行双向的交互的。好，所以从这个角度也可以推测出A一定是MDR。

好的，那这样我们就完成了第一个小问，关键在于我们需要熟悉哪些寄存器之间会有数据流动。只要熟悉这一点，那么我们就可以根据他这儿给出的连线的箭头的信息来推测出哪个位置应该是哪个寄存器。好，接下来第二个小问，让我们简述取指令的过程当中数据的流向是什么样的。首先我们要取得这条指令的地址是被放在PC当中，所以应该先让PC的值把它放到MAAR。好，由于这个题目并没有明确的指出这个数据通路它所对应的控制信号到底是什么样一个名字，所以我们就不需要再说明这个控制信号。

好，接下来我们应该从主存当中读出MAR所指向的数据，然后读出的数据应该放到MDR当中。所以接下来一个步骤应该是MMAR，然后传递给MDR。同样的之前我们一直强调说，如果要进行行读操作，那么必须说明就是CEOO要给主存发送一个读信号。我们强调过这个问题对吧？但是这个题目当中，显然他想着重考察的并不是我们对这个控制信号的理解，而是想要着重考这个数据通路数据的流向。所以我们就不需要再写出控制信号。

好，现在指令已经取到MDR了，接下来是不是就可以把MDR的这个指令信息把它转存到IR指令寄存器当中。所以下一步就应该是MBR打一个括号，取出其中的内容放到IR当中，这就是取指令的一个过程。当然了在很多教材当中，所谓的取指令的阶段还包含了分析指令的过程。所以如果大家不放心的话，还可以再进行下一步的说明，就是把操作码部分送给微操作信号发生器。可以这么写，OP指的是Operator就是操作码。然后IR里边存放的是指令，指令的操作码部分把它传送给这个微操作发声器，这个汉字比较多。不放心的同学也可以再加上这样的一步，表示自己知道还有这样的一个步骤。

好，另一个方面，当我们取出一条指令之后，我们之前强调过很多次，一定会让PC的值自动加一对吧？所以这个地方如果不放心的同学，也可以再写上PC加1，然后再存回PC。当然了这个例题当中我们只写了前边这几步，就是希望和我们的课后答案保持一致。如果大家不放心，那么还可以在后面加上PC加1，还有IR的这个操作码部分送给微操作发生器。再补上这样的两个步骤。

好，这就是第二个小问好，接下来第三个小问，让我们描述数据在运算器，也就这ARU和主存之间的一个存取访问的数据通路。首先必须明确的是，运算器ARU它并不是寄存器，这个部件并没有数据存储的功能。那这运算器想要使用的数据会被我们放到ACC累加寄存器当中。所以这个题目虽然问的是运算器和主存之间的这个存取的数据通路，但事实上他想问应该是ACC和主存之间的一个数据存取的数据通路。

好，首先我们来看一下，从主存当中取一个数据到ACC里边。那么我们先假设此时我们要读取的那个数据的地址已经被放到MAR里边了。那么取数据的过程就是把主存当中MAR所指明的那个数据先给放到MDR，也就是这样的一个步骤。

接下来看一下怎么从MDR把数据转存到ACC？可以看到ACC和MDR的这个连线看起来是个单向的，只能由ACC流向MDR，但是不能由MDR直接流向ACC。好，所以我们只另辟蹊径看一下这个MDR它可以往下传送数据，然后这个数据又可以经过ARU从这边流出。然后如果说通过这条路再给它传送到ACC，那这样的数据通路是可行的。所以这个题目就像是小时候玩的迷宫游戏，要你找到从起点到终点的一条可行的路径。

那有同学可能会有疑惑，ARU不是对两个数进行运算，然后把结果进行输出吗？但事实上ARU肯定也能支持，就是把一个输入的数据原封不动的让它传出去，对吧？实在不行也可以理解为左边输入的这个数据和一个零进行一个加法操作，然后再把这个加法结果给它存回ACC，这样是不是也可以？好，总之MDR里的数据流过ARU，然后原封不动的再流向ACC，这个数据的流动是可以实现的。那么我们可以这么来描述这个过程，MDR里的内容取出来，然后流向ARU。由于ARU它不是寄存器，所以这地方我们不需要打括号什么的，然后ALU再流向ACC。好，这样我们就完成了从主存读一个数据到运算器，也就是读一个数据到ACC里面。

好，接下来再看存一个数据，现在要存的数据已经放在ACC里边了，我们想要把它存回主存。那么首先是不是应该把ACC里的这个数据先给放到MDR，然后接下来用MAR指明我们想要存的地址是在什么地方，然后再把MDR的数据存回主存就可以。由于我们之前已经假设就这个数据的读写地址已经提前放到MAR里边了。所以这个地方我们直接描述清楚数据的流动过程就可以。好，这就是第三个小问好，接下来456这3个小问，我们先把它们连起来看一下。

第四个小问是要我们简述这样的一条指令，它的一个数据流动的过程。这个指你的意思是说我们要把X所指明的主存单元里的数据，把它取到ACC里边。好，接下来第五个小问是让我们描述一个加法指令，这个加法指令做的事情是说要把ACC里边存储的内容和Y所指向的那个数据进行一个相加，相加的结果再给放回ACC。所以4和5这2个小问，其实我们应该把它们连起来理解。

第四个小问先把X放到ACC里面。第五个小问是要取出一个数Y然后让X和Y相加，再把相加的结果再给存回ACC这是第五个小问好再连起来看第六个小问，第六个小N要完成的这条指令是要把ACC里边的内容存回Z所指向的主存地址当中。这就相当于把之前计算的X加Y的一个结果，把它给存回主存存到Z所指向的这个地址。好，所以456这3个小问其实是一个整体。

好，接下来依次分析这三条指令的一个执行过程。从这些题目给的描述不难看出，它其实已经默认了这条指令此时已经存放到IR指令寄存器当中了。好，所以现在我们可以直接进入这个指令的执行阶段。我们要读出X所指向的主存单元，所以我们应该把地址X把它放到MAR里边，或者也可以换一种描述方式，就是把这条指令的地址码部分给它放到MAR当中。由于这个指令的地址码部分就是他这给出的X所以我们也可以用这样的方式来描述。注意X是一个值而不是一个寄存器，所以在X的外面我们不用打括号。

好，现在地址信息已经给出了，那接下来是不是就可以读出这个地址所指明的主存单元数据，应该先从主存放到MDR当中。那么如何把MDR的数据转存到ACC？这个我们在上一个小问已经讨论过，就是经过ARU再流向ACC是这样的一个过程。好，那这样我们就完成了这条取数指令的执行。

接下来进入加法这个步骤。刚才我们已经把X所指向的那个加数放到ACC里边了。现在这条加法指令已经被放到IR指令寄存器当中，那么它的地址码Y指明了我们要读取的主存地址，所以我们可以直接把Y把它放到MAR。当然了，和之前一样，我们也可以用ADIR放到MAR，用这样的方式描述也是OK的。

好，接下来我们同样需要先把Y所指向的这个数据给读出，这个数据会被放到MDR当中。好，所以此时加法操作的X和Y所指向的这两个数都已经被我们取到CPU里边了。那接下来ARU是不是需要进行这个加法运算？我们观察一下它的连线，ACC里的这个X可以直接输送给ARU。然后MDR里面存放的第二个加法的操作数，也可以直接通过另一条专用的数据通路把它输送给ARU。好，所以如果站在控制器的角度来看，就是要把这两条线路的一个控制信号让它有效让这两条数据通路连通。好，那接下来控制器是不是还应该给ARU发送一个加法的控制信号？当然了这个题目没有给出这个加法控制信号的一个具体的名字，所以我们也可以不用描述。

总之就是ALU把这两个输入的操作数进行加法操作之后，最终的加法结果又给存回ACC。所以经过这样的操作，就导致ACC里边保存了X所指向的数据和Y所指向的数据，它们的一个加和好。这地方大家会发现ARU的输出端并没有接一个暂存寄存器，但严格来说应该是要接一个暂存寄存器，先把这个输出信号让它稳定之后，再把运算结果存回ACC。好，但是这个题目没有画暂存寄存器，所以我们就不用管好，这样就完成了加法操作。

好，接下来第六步，我们要把加法的结果存回Z所指向的主存单元当中，所以首先我们要把加法的结果从ACC先给存到MDR里边。同样的，此时这个指令已经被放到IR指令寄存器当中了，所以我们可以直接从IR指令寄存器里边把接下来要存的那个地址给它传到MAR里边，就是把Z放到MAR，同样的也可以使用ADIR，然后存到MAR，也可以使用这样的方式来记录。好，现在我们已经指明了地址，那我们还需要把刚才加法得到的这个结果从ACC放到MDR里边。所以接下来一步应该把ACC的内容传到MDR。好，最后把MDR里边的数据写到主存里边MAR所指向的那个位置，这样我们就完成了存储的一个操作。好的，这就是这个题目的一个做法，不是很难对吧？

好的，那么上个小节和这个小节我们介绍了数据通路相关的知识。上个小节我们着重探讨了单总线结构的这种数据通路，而这个小节我们着重探讨了专用数据通路这样的一个结构。这种连线方式意味着但凡两个部件之间需要有数据的流动，那么我们都需要在这两个部件之间有一个专门的数据通路，因此它的造价成本会比较高。但是由于各个部件之间的这种数据传送，它们的通路都是相互独立的。因此如果说我们当前执行的指令中间的某几个数据流通的过程可以并行的执行。那么我们就可以让多个专用的数据通路同时工作，这样就可以提升指令的执行速度。所以它比单总线的结构肯定是要效率更高一些。但是世上难得两全法，你要有更高的效率就意味着需要付出更多的成本。

好，另一个方面，我们的每一个寄存器或者说每一个部件采用专用数据通路的话，就意味着有可能会有多个输入。那么到底要让哪个输入信号？通过这一点，我们可以连接一个多路选择器，或者给每一个输入的线路都连一个三态门，用这样的两种方案来解决。然后CEO通过控制信号来决定让哪一个输入输信号有效就可以。

好，无论是单总线多总线还是专用数据通路，这个部分的考题通常都是给大家一条指令，让大家描述各个阶段的一个微操作序列。其实就是要弄明白每一个微操作它的一个数据流是什么样的，以及这些微操作应该发出哪些控制信号来完成控制。考察的方式相对固定。再次强调我们学习这门课不能只看只听，一定要动手做课后习题。只有自己动手做过，才可以把这些做题技巧还有知识给巩固。好的，以上就是这个小节的全部内容。