31 2.3.1_3 浮点数的表示范围、几种特殊状态（上）_原文

2026年08月03日 23:46

各位同学大家好，在这个小节中我们要探讨754浮点数的表示范围以及几种特殊的状态。这个部分是比较常考的，并且内容比较多，所以我们会把这个小节拆分成两个视频进行讲解。首先我们会分析规格化浮点数的表示范围。确定了第一个问题之后，我们就可以进一步的探讨，如果浮点数的运算结果超出了规格化浮点数可以表示的范围，那计算机一般会怎么处理？在i triple e754标准当中，如果浮点数的阶码全0或者阶码全一。那这样的特殊状态的浮点数，我们对它的数值的解读方式也要特殊的处理。我们会在这个小节当中穿插着把所有的特殊浮点数全部过一遍。

好，通过之前的学习，我们已经知道了754标准定义的单精度浮点数以及双精度浮点数分别长什么样子。这两种浮点数都由符号阶码和尾数这三个部分组成。只不过双精度浮点数double型，它的阶码和尾数的长度会更长。另外float和double的这个阶码偏置值也不一样。

在之前的学习中，我们一直在探讨的是规格化浮点数i triple e754是这么规定的。如果说阶码部分不全为零，也不全为一的话，那么表示这个浮点数是一个规格化的浮点数。通过之前的学习，我们已经知道了如何去解读一个规格化浮点数的真值。首先符号这个部分我们按零正一负去解读，而阶码的真值我们可以这么去转换。首先我们把N比特的解码，先按照无符号整数的这种方式去把它解读，然后在这个基础上再减掉偏置值。好，这是阶码的解读。好，最后尾数部分对于规格化浮点数来说，我们默认在尾数的这个小数点前面会隐含一个一。那通过之前的学习，相信规格化浮点数的这个解读方法大家已经掌握的差不多了。

好，现在我们再补充754标准的一个新的规定，就是阶码部分假如N个比特全为零或者全为一的话，那么这样的浮点数就不是是规格化的浮点数。我们就不能按照前边介绍的这种方法去解读它的数值。在这张表格当中给大家总结了阶码全0或者阶码全一的时候，我们会遇到的所有的情况。看见这张表格相信你已经汗流浃背了，没关系，接下来我们会详细的探讨每一种情况。好，首先观察一下这个表格的表头，左边这一溜就这个部分我们罗列的是单精度浮点数阶码全0或者阶码全一的时候，应该如何去解读它的真值。而右半部分这罗列的是双精度浮点数阶码全为零或者阶码全为一的时候，我们应该如何去解读它的真值。

除了阶码之外，尾数这个部分是否全为零也会影响到我们对真值的解读。比如说前边的这四种情况，当阶码全为零的时候，如果尾数全0，那么我们就把这样的浮点数看作是增值的正零或者负不0。而如果尾数部分不全为零，那么我们会按照非规格化的浮点数去进行解读。非规格化浮点数的一个显著特点就是尾数的小数点前面，这我们隐含的不是一而是零。一会儿我们会用具体的例子来展开探讨。

好，再往下，下面的这三行罗列的是当阶码全为一的时候，我们如何解读。如果阶码全为一，而尾数部分全为零的话，那么这样的浮点数表示的是无穷大。根据符号位的正负性可以分别表示正无穷或者负无穷。好，另外如果阶码全一，但是尾数不全为零的话，那么表示这是一个无定义丨非丨通常用英文NAN not a number用这个缩写去表示。比如说当我们对一个复数进行开根号的时候，运算的结果就会用not a number这种特殊的状态去记录。

好，现在我们脑子里形成了一个框架。阶码全0、阶码全一这两大类情况的后面还需要继续拆分尾数全0或者尾数不全零这样的两种子情况。由于考研中主要考察的是单精度浮点型，所以在这节课当中我们主要以单精度浮点型为例去进行分析。双精度浮点型的分析方法分析思路一模一样。在这节课的最后我也会给大家留下双精度浮点型的训练，以防出题老师突然偷袭。

好，现在我们先把阶码全0或者阶码全一的这种特殊的浮点数放在一边。我们先回到规格化浮点数。刚刚说了规格化浮点数的阶码不可以全为零，也不可以全为一。那么在这样的条件限制下一个规格化浮点数，它的表示范围应该是多少呢？我们以单精度浮点型float为例。好，首先尾数部分总共23比特，在这23比特的前面我们还会默认的隐含一个一。另外阶码部分，由于它是用移码的这种编码规则去表示的，所以我们需要把移码转换成真值。

首先按无符号整数去解读这八个比特，然后再减掉偏是指好。那么巴比特的无符号整数，它本来可以表示的范围应该是0到255，对吧？但是i trip e规定规格化浮点数的阶码不可以全为零，也不可以全为一。那么去除全零和全一这两种状态之后，规格化浮点数的8比特阶码所能表示的这种范围是不是就落到了1到2544，就只能表示这样的一个范围对吧？

好，现在我们是基于无符号整数的这个角度去去分析这个阶码的取值范围。最终这个阶码的真值还需要再减掉偏置值，对吧？那float型的偏置值是127，所以阶码的真值取值范围最终就落在了负的126到正的127这个区间内。这个地方要跟上，1减127等于一负的126，25 4减127等于正的127。

好，确定了规格化浮点数的这个阶码的范围以及尾数的这种形态之后，接下来我们就可以分析规格化浮点数它最大可以表示多少，最小可以表示多少。这张图是一个数轴，右边是正数的方向，左边是负数的方向，中间这儿是真值0。好，接下来我们先分析规格化的单精度浮点数，它可以表示的最大值等于多少。通过之前的学习，我们已经知道规格化浮点数的真值，可以这么去表示，尾数一点叉叉叉叉去乘以2的阶码次方，这样就可以得到规格化浮点数的真值，对吧？

好，既然我们想让这个规格化浮点数表示的值尽可能的大，那么首先尾数数部分我们是不是应该让这个尾数尽可能的大？那最大的尾数当然就是1.111，就是尾数全为一这个情况好，尾数先确保它是最大的。然后阶码的部分刚才分析了，最大值可以取到的是127。所以当阶码等于127，尾数是全一的时候，这个规格化浮点数就来到了最大的值。那阶码的增值127对应的二进制状态应该是7个11个0。不熟悉的同学可以慢下来自己验算一下，7个11个零先按照无符号数去解读，它应该等于254。那么254再减掉偏置值127就得到了真值127，也就是得到了规格化浮点数的阶码最大的一个值。

好，我们把刚才分析的结论放在这张图里。规格化浮点数可以表示的最大的正数就应该是这样的一个值。如果用二进制表示就应该是这样的那下面这个题方我们是用二进制的科学计数法来表示的。1.11乘以2的127次方，意味着这个小数点实际上需要往后浮动127位。好，这是float型可以表示的最大的一个正值。那相对应的float型可以表示的绝对值最大的一个负数，就应该是在刚才这个正数的基础上取一个负就行了。除了符号位从零变成一之外，这个阶码和尾数部分都和刚才这个分析的结论保持一致。

好，接下来我们再梳理这个规格化浮点数可以表示的最小的一个正数等于多少。那么对规格化的浮点数来说，尾数可以取到的最小值就应该是1.0000，就是后面总共跟23个0。除了小数点之前隐含的这个一之外，后面的这些全部取0。好，尾数先确保它是最小的。

好，另外阶码我们也应该取最小值。那通过之前分析，我们知道阶码的最小帧值可以取到负的126。我们说了规格化浮点数，它的阶码不可以全为零。那刨除全零的这种状态之外，阶码可以取得最小值，就应该是7个01个一。

可以验算一下，先按照无符号数去解读这个解码。那这个无符号数的值就应该是一。无符号数的基础上再减掉偏置值127，那么就得到了负的126。这就是阶码部分可以取到的最小的一个值。好，那么结合尾数和阶码的信息就可以得到最小的正数1.0乘以2的负126次方。那相对应的绝对值最小的负数就应该是在刚才这个基础上把符号位从零变成一，然后阶码尾数保持不变，负的1.0乘以2的负126次方。

好，需要注意的是我们这一行使用的是二进制科学计数法。在做题或者看书的时候，你可能会遇到10进制表示的这几个值。我们来解释一下，比如说最大的这个正数，如果用十进制来表示的话，可以把它记为这样的一个值。首先乘以2的127次方这个部分我们保持不动。然后1.111这个定点小数，它的值等价于2，减掉2的负23次方，怎么来的呢？

思考一下，1.111这个值，如果它加上0.0000很多个0，最后这个比特我们取一，那么我们主。位的相加，两个一相加，本位由零往高位进一个一。刚才进过来的这个一又会持续的往前传递，对吧？那么这个进位不断的往前传递之后，最终得到的加和结果是不是就应该是10.0000，后面全部都是零，对吧？那么10.00000这个数值把它转换成十进制不就是对应二吗？小数点前的这一位渭全等于一，小数点前面的第二位位全等于2。好，所以如果用二这个值减掉0.0001，那么相减的结果刚好就对应了1.1110.00001。

这个值为什么等价于二的负23次方？这个很好理解，对吧？小数点的后一位它的位置是二的负1次方，小数点后的第二位是二的负2次方。那小数点后面的第23位，它的位置不就是二的负23次方。好，所以这个最大的数值我们把二进制科学计数法转成十进制记法可以写成这个样子。这个地方有可能在看书做题的时候产生疑惑，所以进行了一个解释。

好，接下来我们看一个真题，18年的14题。这个题目问单精度浮点数它可以表示的最小的规格化正数是多少？刚才分析过了，应该是这样的一个值，对吧？答案选择A选项，我们要理解刚才分析的过程，不要去背这些数值，背没有意义。你在考场上要能够自己分析这个规格化正数它的最小值、最大值应该等于多少。只有理解了背后的逻辑，才可以以不变应万变。有18年这种考法，不难想到未来有没有可能这么去考。单精度浮点数它可以表示的最大的正数等于多少，或者它可以表示的绝对值最大的负数等于多少。刚才分析的这几个关键的数值边界都有可能作为命题的角度。

好，接下来我们探讨浮点数的溢出问题。刚才我们已经确定了这个规格化浮点数，它可以表示的最大帧数以及绝对值最大的负数分别等于多少。如果说现在两个浮点数，比如说进行加法运算，但是运算的结果已经超出了这个最大值所能表示的范围，那么此时我们说就发生了正上一。那相应的如果两个浮点数进行运算之后，运算结果已经比绝对值最大的这个负数还要更小，那么此时我们就说发生了负上一，结合这个数轴应该会很直观，反正就是说运算的结果落在了外面这个区间。好，那么正上溢和负上意可以统称为上译，英文术语对应的是over floor。Overflow也可以直接翻译为溢出。

那么当浮点数发生上溢或者说发生了溢出的时候，这个计算机会怎么处理呢？I trip e定义了浮点数的两个特殊的数值，分别可以用于表示正无穷或者负无穷。假如浮点数的运算结果发生了正上亿，那么i triple e754标准规定运算结果要记为正无穷。那相应的如果这个运算结果落在了负上亿的这个区间，那就会用负无穷这种特殊的值去记录。

好，来看一下怎么表示无穷大这数值。首先阶码应该全部置为一，而尾数应该全部置为零。那正无穷这个值我们需要把符号位写为正，也就是零，而负无穷的符号位是一好。所以对于float型变量来说，如果要表示正无穷的话，它的二进制应该是符号位为零。然后阶码全为一，尾数全为零，对应16进制应该是这个样子。负无穷只需要把符号位改为一就行了。解码同样全一尾数全0。那double型的正负无穷的表示和float型规则一模一样，只不过阶码和尾数的位数会有所区别。

大家可以动手写一下这个小节的末尾，我会公布答案。好，回到之前这一页课件，我们举一个发生正上意的例子。假如有两个float型的变量A和B它们的值都是规格化正数所能表示的这个最大值。那么当我们把A和B相加的时候，显然加和的结果肯定就会落在正上方的这个区间，对吧？那么此时它们加和的结果就会用正无穷这个特殊的值去记录。

当浮点数运算发生溢出的时候，除了把结果置为正或者负无穷之外，CPU还会把浮点数一出一长，这个标志位置为一。比如说在叉86处理器当中，浮点数运算单元FPU里边有一个标志位叫做OE，它是overflow exception。这个标志位就会被用于记录浮点数的溢出异常。那754标准规定默认不响应浮点数的溢出异常。也就是说即便发生浮点数提出，也不会中断当前这个程序的运行。那这样的规定就确保了容错性比较好。当然程序员可以手动的去开启这种浮点数溢出异常的响应。如果没有手动开启的话，浮点数溢出就会直接被忽略。

为了让大家感受的更直观一些，这儿写了一个程序示例。在这个程序实例当中，我会用两个最大的规格化正负点数进行相加，那结果肯定会发生上亿。Float点H这个头文件当中直接定义了一个这样的一个常量FRT杠max。这个常量的值就是最大的规格化正负点数，也就是对应这个值。那之前分析过它的二进制应该是什么样。好，然后下面这一句我把两个最大的规格化正数相加，用result这个变量记录加和结果。然后在这一句输出这个相加的结果，你会发现输出的结果是INF。INF指的是infinity，表示无穷大这个意思。

紧接着在后一句代码当中，我们可以手动的去检查刚才的这个运算结果是否发生了溢出。如果刚才的运算结果is infinity是无穷大的话，那么就说明发生了溢出。如果我们不做这种手动的检查，那整个程序是不会中断执行的，它还会继续往下运行。好，这样你就理解了刚才这句话。

754标准规定就是默认不响应浮点数移出异常。它会用正无穷或者负无穷这样特殊的值去处理浮点数的溢出，这样可以保证程序的容错性比较好。有兴趣有时间的同学可以在电脑上跑下这段程序，甚至你可以把这个最大的规格化浮点数，把它的16进制打印出来看一下，是不是我们之前分析的这个结果。这段程序不重要，只是想给大家做一个拓展，让浮点数的上亿这个事情变得更直观一些。好，探讨完上意之后，接下来我们要探讨浮点数的下意识。