44-头条项目-用户注册-生成Token_原文

2026年07月21日 12:54

发言人   00:01

Hello, 各位同学，上一小节咱们创建了用户了。那这一小节儿咱们顺序向下来生成访问令牌响应给前端。我们说这个访问令牌，它是一个用户是否登录的一个凭证，如果有这个令牌，那说明登录了就可以看个人信息，修改个人信息。但如果没有令牌说明没有登录就不能看，不能修改个人信息，那这是访问令牌的作用。

发言人   00:27

那我们该怎么生成访问令牌呢？注意抬头看啊，借助UUID模块，咱们生成一个临时的token就可以。其实这个token它就是个字符串。那具体做法就是UUID这个模块下调用UUID4这个方法，在用STR方法转成字符串，把这个字符串响应给前端就OK了。这个是咱们生成token的方法，以及token的一个作用。

发言人   00:53

那么在这儿，因为咱们这个token是不是明显涉及到前后端的配合工作？那我在这儿展开给各位同学讲解一下token。首先token是服务器发给客户端的一个字符串。一个字符串是用来干啥的？就是用来证明是不是已经登录过了。那为什么非得要有token呢？它有啥作用呢？注意HTTP是无状态的，就是你第一次登录了，服务器是不是让你登录了。

发言人   01:23

但接下来你比如说要看个人信息，那又去发起个人信息接口的请求，那服务器知不知道你登录过了呢？不好意思，他不知道，那怎么办呢？就需要我们在每次请求当中添加上这个token，进行自我身份的一个证明。

发言人   01:41

我已经登录过了，你得让我看个人信息，那具体的流程给各位同学稍微画了一下。比如说用户在前端登录或者是注册，一旦登录注册后端就会生成token，就是一个字符串。生成了这个头肯之后，由后端返回给前端。前端收到投肯了，他要保存起来。

发言人   02:04

每次请求的时候，比如说在请求个人信息，修改个人信息，请求这些接口的时候，都会在请求头里边携带着这个token，就是证明大哥，我已经登录过了，你得让我看信息，得让我修改。好，那这个token在请求的时候携带了，后端收到这个token会验证，偷看对不对？对，就能看个人信息，能修改个人信息。如果是错的那你还没有登录，那得去重新登录才行。

发言人   02:36

这个是前后端关于token的一个执行流程，各位同学稍微了解一点。那在这儿刚刚我提到一个关键点，给各位同学稍微展开说一下。我说前端在发起每次请求的时候，是不是要在请求头当中携带着这个token，那前端是怎么携带的呢？注意标准的写法后端开发人员要了解。

发言人   03:02

那我们一块儿来看一下，注意token在请求中的位置是请求头，那携带的方式就是authorization，冒号bear空格写token。Idealization就是专门用来放身份信息的那咱这个token是不是就是用户的身份信息？接下来放一个字符bear表示持有者令牌，就是令牌的意思。那接下来空格是不是放了一个token，这个token就是咱们后端生成的响应给前端的一个字符串token。这个是前端在每次请求的时候，请求头里边携带token的这个标准写法。咱们作为后端开发人员，稍微了解一下，这跟我们个别的代码稍微有一点点关系。好，那现在我们就知道了这个token前后端交互是不是做用户身份认证的一个凭证。

发言人   03:55

那这个token由咱们后端去生成，那这一小节咱们就来看一下，咱用户注册了是不是也要生成响应一个token给到前端？那我们来写代码来试一下，来，我们回到拍场当中来。首先我们要生成token，生成token是不是要存起来，也存到咱们的数据库表里边，对不对？大家还记得吗？

发言人   04:18

我们的数据库里边有一个user杠token表，那么这个表打开来看，有一个user ID它是外键，那就是这个token他是哪个用户的？是不是关联的外键，是user表里边的user ID。好，那接下来token字段存的就是那个token字符串。

发言人   04:38

好，再有expires at，那这个是过期时间。那我们的过期时间一般设置为七天，就是从比如说我们登录这个时间点开始算，加上七天，七天之内证明都是登录的状态，都可以直接访问个人信息，修改个人信息。但是如果是七天之后不好意思，过期了，是不是让他重新登录去，那这个是咱们的user token表，一会儿生成了token，是不是要往这个表里边来存？

发言人   05:07

当然咱们是ORM的方式来增删改查操作数据库是不是肯定也得有模型类？那大家还记不记得模型类咱们是直接复制过来的。在我们的models里边有一个user来展开给各位同学看一下。我把上面的user折叠来看user token表，那这里边是不是有ID user ID它是一个外键是不是？好，再有token expires ad还有create at，那这个模型类直接有了叫user token，那我们就直接用它了。好，那我把这个models直接关掉了，那接下来咱就直接封装方法来生成token，是不是就OK了。

发言人   05:49

我们回到CRUD里面，CRUD里面的user，我们向下来一点，允许我加一个小注释，我们现在要生成生成这个TOKEN token，那我直接tab一下了。注意来看DF create token是不是也没问题，我就用这个方法名了。好，那DD数据库绘画一来项这个参数肯定要，user name我就不要了。因为咱们的那个token表里边是不是有一个外键是user ID，那我就用user ID了，那这个user name干掉，咱们写一个user下划线ID，ID是不是就是int类型来着？

发言人   06:32

好，那接下来我们往下，我们要干啥呢？是不是生成偷看设置过期时间。然后我们看看数据库里边关于这个用户有没有token，如果要是有，我们重新更新一下。没有是不是存一下就OK了。所以我在这儿打一个注释，我们先去生成token，加设置这个过期时间，设置过期时间。

发言人   06:59

下一步我们就要查库，查询数据库，当前用户是否有这个token，是否有token，那我们紧跟着，如果有token要干啥？是不是就更新？那如果要是没有token当前用户没有token，我们是不是就是添加就OK了。

发言人   07:24

好，那我们现在先第一步是不是生成token，大家还记得吗？PPT里边怎么生成token，UUID是不是？好，那接下来我就直接写了，那我们来一个tab一下，但这句不要，它不要它来干掉，我们就是UUID点儿，UUID4UUID4这个方法。好，另外往上看啊，import是不是已经导进来了，那接下来我们往下来，我们是不是得STR把它转字符串。好，那接下来我剪切一下，STR转个字符串，然后不是把它存一下，比如说就叫token了，等号赋值。

发言人   08:03

好，那现在我们的字符串token已经有了，接下来下一步是不是还得设置一个过期时间，那我们起个名字，比如说就跟那个字段名一样了，EXPIRES艾特，等于什么呢？是不是当前的时间加上七天？好，那就是data time点儿闹一下。好，那我们这个data time我得给它导包导一下，导入。好，接下来还有一个time delta，那我们也给它导包导进来，那我们导入一下。好，那接下来我解释一下这个time deal，它它是返回一个时间长度，那我说一下它的这个全量写法，我control c一下，在这儿打一个小注释。

发言人   08:49

Time delta我这儿是不是写了一个days等于7，但其实它还可以这么写，来逗号HOURS，大家猜一下是啥？比如说写个等于二是不是就是小时，然后还可以写MINUTES。比如说我等于一个三十，这个数值是我随便写的。好，那再逗号隔开写一个seconds，等于比如说一个十，那这个意思就是七天、2小时、30分钟、十秒之后是不是过期？那当前的过期时间是不是就是系统当前时间，咱们这儿设置的是不是就是七天的有效期？那现在过期时间是不是也设置好了？

发言人   09:32

下一步查库，看看当前这个用户有没有token，是不是那查库我不太会写，大家帮我写。对，当前通义灵码写的是不是还挺不错，那我们直接tab一下了。Select查表user token这个模型类没问题，那我们把它import导进来。接下来威尔模型类的user ID等于是不是就是行user ID那就是看看当前用户有没有这个token，对不对？

发言人   10:05

好，接下来我们来一个result DBXQ的执行。它执行完之后，是不是我们就提取结果就可以了，那我就这么提取了scala one or nine是不是有可能有有可能没有，那如果有我们说那就更新一下呗，如果没有就添加是不是就OK了。好，那接下来是不是该咱们的这个if判断了？好，那我写一下if我太一下，如果有token，是不是把token更新一下，过期时间更新一下IOS是不是就是没有token的意思？如果没有token，是不是就是重新创建了一个对象，那把我们的新的token是不是给它存进去，我们检查一下user token模型类字段，user ID token过期时间是不是都有了。好，那接下来我们下一步是不是就是I的添加就行。接下来我们缺commit来执行它，提交它。

发言人   11:07

好，那执行提交好之后，我们这个create token方法还没完，要最终return一个咱们刚刚定义好的这个token，生成好的token，return一个。为什么要在这return一个呢？因为一会儿咱是不是要在响应结果里面拿到最新的token。所以我们把刚刚生成的这个新的token是不是作为函数返回值返回。那在调用这个方法的位置，我们是不是就能拿到这个新的token了，就能响应给前端了。

发言人   11:38

好，那现在咱们的生成token的方法就已经封装完了。容我把这个方法截一个屏钉在屏幕上。那这个方法是不是在咱们路由处理函数当中来调用一下就可以了。那回到咱们的路由里面，注意来看这是不是在创建用户，创建完用户紧跟着就要干啥，是不是生成token了。

发言人   11:59

好，那接下来我一下await users里面的create token，调用生成token的方法，把数据库绘画依赖项是不是传到函数里面，把user ID是不是也传到函数里面。那我们调用了这个函数了，是不是就能拿到函数的返回值，所以存一个变量叫token。那接下来我们是不是把这个变量token保存的结果响应给前端就OK了。好，那接下来关于data响应的数据，这个写死的字符串用户访问令牌我们给它干掉，直接变成token这个变量。好，那现在咱们的生成token的代码是不是就已经写完了？那接下来我们来验证一下，来回到咱们的浏览器里边来，允许我先刷新一下，那try IT1下，填个用户名。

发言人   12:48

大邱比如说这回111，密码123456，接下来我们excute来提交一下来看看是不是注册成功。同时token这里按一串字符串，咱的token是不是就生成且响应回来了。那我们现在通过交互式文档测试，咱用户注册这个接口是不是已经OK搞定。那接下来咱就这样拿着写好的前端界面，咱们来注册一下来试一下。来回到咱们的最后这个页面里边来。

发言人   13:20

那回过来之后，我们先刷新一下，找到我的。现在没登录，那我们点去注册，换个账户名了，咱就排着来。大邱222，三个二，密码123456，确认密码123456来注册来看注册成功。接下来回到我的来看看大邱222是不是已经登录成功了。那登录之后，接下来咱们回到拍摄当中，来检查一下token表里边有没有我们刚刚注册的用户。来点开咱们的databases插件，点开user token表来看看。我先现在是不是两个用户有了token？ID是23、24的那我们再从uc表里边看一下，ID23的24的大邱111、大邱222是不是都有了token。

发言人   14:14

那说明咱们的用户注册接口的功能是不是就已经实现完成了，那实现了接口功能了。接下来咱们回来到代码当中，稍微把思路整理一下。首先咱们刚刚做的事儿是生成token，是不是返回token给到前端，那这个token怎么生成的呢？是不是用UUID这个模块，我们设置一个过期时间，然后同时查询数据库里边当前用户有没有token，有的话，那我们就走更新，没有是不是就新增它就好了。那在我们的路由处理函数当中，调用这个生成token的方法，拿到最新的token响应给前端是不是就OK了？这是我们这一小节完成的。

发言人   14:55

当然整个注册接口，大家还记得吗？我们要实现注册是不是得先看看用户是不是存在的。如果存在说明重复了，那是不是不让他注册，让他换一个名字。好，如果数据库里边没有这个用户名，我们再让他去创建用户。其实就是把用户输入的用户名密码写入到数据库，是不是就OK？

发言人   15:16

好，接下来我们再用这一小节的生成token的方法，把token凭证来生成响应给前端。最后响应结果给到前端。咱们这里前后端约定的响应结果，就是用户登录凭证以及用户的关键信息，这个是咱们整个注册的逻辑。那接下来就该各位同学的时间了，大家动手来使用UUID这个模块生成token响应结果给到前端，大家来动手来试一下。另外试完之后不要走开，下一小节咱们把成功异常的响应结果来整体抽取封装一下，让咱们的项目更加工程化一些，我们下一小节儿不见不散。