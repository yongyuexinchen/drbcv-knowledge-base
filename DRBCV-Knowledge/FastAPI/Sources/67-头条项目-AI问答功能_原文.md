67-头条项目-AI问答功能_原文

2026年07月21日 12:53

发言人   00:01

Hello, 各位同学，这一节咱们一块儿来实现AI问答功能。有了这个功能了，我们就可以在前端界面输入文字，点发送按钮，就可以收到千问大模型回复的内容了。那么这一节咱们一块来实现一下这个AI问答功能，那这个功能该怎么实现呢？来咱们一块儿来看一下。

发言人   00:20

首先咱们这个功能是在前端界面调用千问大模型，那怎么调用呢？注意先登录到阿里云百炼模型广场，在模型广场里面找需要的模型，点API参考。注意重点重点获取这个大模型HTTP请求地址以及APIK，把这两个关键信息在前端界面当中配置上就可以。那另外在哪里配置呢？来咱们一块儿来看一下，注意前端界面要调用模型，找到前端项目SRC里面，注意有一个config配置文件夹，找到APIGS。咱们这里有对应的OpenAI这个API地址，那我们未来直接粘贴。当然咱的项目当中这个请求地址我是粘好的，咱们直接替换自己的APIK就可以。

发言人   01:15

好，那也就是说我们要调用这个大模型，是不是就在模型广场里边找模型重点显示，看请求地址跟APIK趴替换到前端界面是不是就OK了。当然我这里温馨提示，咱们这个项目的AI问答功能是调用了阿里百炼的千问答模型。我们只是用这个大模型实现对话功能就可以。未来咱们还会带着同学们去训练模型，把这个模型服务化。这样的话我们不光会调用别人的模型，是不是还能训练自己的模型服务化给别人来用。当然我们先学调用别人的。

发言人   01:54

好，那接下来我们来试一下。首先注意点PPT当中蓝色的文字，去阿里百炼模型广场。那我点一下，按control键点一下，稍等打开模型广场，温馨提示看右上角，我是不是已经登录过了，那大家也用自己的手机号注册登录一下。那我们找模型来看通义千问三是不是比较新的模型。那鼠标放上来，找到API参考这个按钮，我们点一下，稍等一下，注意有三个选择来看，我们要用的是对话功能，所以选择OpenAI，我们点一下，稍等跳转界面。在这里来看HTTP请求地址，请求方法post，第一个关键信息是不是已经找到了。但我提示这个请求地址我已经在前端代码里面粘好了，大家不用管了，直接用就可以了。

发言人   02:52

再往下还有一个获取APIK，这个我们在PPT当中提到它是不是一个关键的配置，这是密钥，我们要得到它，那怎么得到？我们待会儿重点说。在右下角这儿有一个代码的写法，就是使用千问大模型前端该咋写代码，给大家稍微看一下。在这里我说一下，本来是应该前端写代码，对不对？但是我们没有学过前端，如果直接选择前端代码的话，对我们来说难度是不是有点高。所以我们直接看python的关键提示就可以了。

发言人   03:26

注意来看这里是不是有模型的地址，然后往下是配置，那怎么配置的呢？注意model，就是模型，你选择的是哪个模型。那再有message，message里边是列表，列表里面套字典，字典的属性。Row表示角色是系统的还是用户的？Content就是内容，无论是用户发的消息还是模型回复的消息，都是一个字典的格式来存储的。一个字典里边有肉有康弹的，那这个代码大家稍微看一下就行，我们也不用写，因为我的前端界面里边是写好的。

发言人   04:07

那我们只需要干啥？就像刚刚PPT里边说的，找请求地址，找APIK就行。而且请求地址是不是我已经粘好的，咱直接关注APIK这个密钥就可以了。

发言人   04:20

好，那接下来我们点获取APIK来试一下，来点稍等一下。如果你要获取APIK要前往密钥管理，这里是不是有三个地址，那一会儿我们就选择北京了。当然底下它还有一个提示说如果是新开通的账号需要实名认证。如果提示您尚未进行实名认证，那大家就按要求填写认证信息就可以了。好，我这里是提前填好的。好，那认证好之后，说在界面的某个位置就会有一个弹窗，我们可以去创建APIK，其实是点右上角的一个按钮就能弹窗了。好，那接下来注意，选择归属的账号，默认的业务空间最多支持20个密钥，最多支持20个。好，那再往下有了密钥了，点击密钥右边旁边有一个小图标代表复制，复制好之后粘到咱们的前端代码就可以了。

发言人   05:24

好，那接下来咱们来试一下。首先点密钥管理，稍等一下，注意来看，我这里是不是有一个密钥，这是我之前创建的。那接下来我点右上角的创建密钥这个按钮，注意来看最多20个，我现在是不是有一个。好，那接下来我点绑架，直接选择默认的这个账号，然后默认的业务空间。接下来点右下角的确定，注意来看是不是又来了一个密钥，那我们复制它是不是就OK了，找到密钥旁边的这个复制按钮，来点一下，注意看提示。

发言人   06:00

制成功了，那接下来咱们要做的是不是到前端代码当中来粘贴？来回到咱们的拍场本当中来。我是提前把咱们的前端项目，前端代码在拍top当中打开了。我们要做的就是找到项目根目录当中SRC文件夹，找到config文件夹。

发言人   06:20

配置的意思，展开找到API点GS注意来看API地址就是那个HTTP的请求地址，模型的请求地址。APIK这是不是咱们要粘贴的，来control v粘贴一下。那往下是不是有一个模型的名字，我也是提前写好的，模型名称。好，那接下来我们粘完了APIK了，注意大家也只需要粘贴这个APIK。那粘好之后，我们先保存代码，先保存一下。最好把前端的服务重启一下，因为咱是不是改了配置文件，万一不生效，咱们重启一下。

发言人   06:58

好，那接下来回到终端，我们control c然后向上重新运行NPMRDEV。运行好之后，接下来回到浏览器里面了，回到浏览器里边找到咱们的项目，把AI问答刷新一下。那我们先试一下，先发个，你好你你好，稍等一下。很高兴见到你能收到这个回复。那说明咱们前端界面调用千问模型是不是OK成功了，但是提示测试这个功能要联网。好，那我再来一句，比如说介绍一下fast API框架来稍等，当然可以，fast API是什么什么什么。我们能收到这个消息，说明咱们前端调用千问模型哦耶成功了。

发言人   07:51

那成功之后，咱们稍微总结提炼一下这一节都干啥了。首先我回到这个大模型服务平台，大家还记得吗？要用千问模型得来，阿里云百炼的模型广场是不是？右上角提示先登录，先登录，找咱们的某个模型，找API参考，API参考。那找到之后，来我们点进来，重点来了，我们要的是对话功能，来点一下，我们要请求地址APIK。那如果未来假如说咱们在python里边或者是前端里边，如果说想自己手写这个对话功能的代码，那就参考右下角这个代码，是不是就OK了，提示这里，这是重点model，还有message，这就是用户跟模型的消息，它的格式是不是在这儿了。好，那接下来这不是咱们的重点，往上看APIK是不是重点。

发言人   08:50

点进来之后，我们是不是要创建APIK？创建它复制这个K到咱们的代码当中是不是就ok那大家还记得吗？这个K咱们粘到了config的APIGS里边，找位置一粘贴，测试前端是不是能调用成功就OK了。

发言人   09:09

这是咱们改写了前端代码，变成了自己的APIK。是不是实现调用了阿里云百炼的千问大模型。我们这个是不是调用的是别人的模型？我们说后续课程，咱们还有模型的训练，模型的服务化，是不是就是自己训练模型，把它提供服务。这样的话，别人的前端代码是不是也能调用咱的大模型？那我们后边慢慢来学习模型训练和模型部署。接下来该同学们的动手时间，在阿里云百炼模型广场创建自己的账号，注意实名制，得到APIK粘到APIGS，config APIGS里边来。调用一下千问大模型，看看能不能使用用AI问答功能，那大家来动手来试一下。