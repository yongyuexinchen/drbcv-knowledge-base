请求体传参

请求体传参_原文

2026年07月22日 14:53

发言人   00:00

好，各位小伙伴们大家好，我是老肖。接下来我们开始讲第三种前后端分离中的一种传参，叫请求体传参。这种实际上是最常用的，尤其是当你要传一些大量的数据的时候，一定要采用请求体传参好吧。当然如果你不是做前后端分离的项目，你是做传统的项目，就是前后端不分离的。那么还有一种传参方式就是表单传菜，好吧。

发言人   00:25

请求体传参说白了就是把参数放到request的body里面，放到这个请求对象中传过来的，就是request body里面，通过request body来携带这个参数。它有一个特点，它携带的参数可以比较大。比如说你整个文件，整个一个几百兆的文件上传过来，那你必须需要采用这个请求题穿插或者是表单提交。当表单提交的只能是基于那些前后端不分离的项目，但我前面就给大家说过了，fast的一篇，他一生下来就是做什么？就是做这个前后端分离项目，所以表单提交你了解一下就可以了，好吧？我们重点是要讲这个请求体传上好。

发言人   01:12

请求体传参一般来说是传一个阶层格式传一个阶层格式。这个阶层格式的可以是很复杂的，大概率都是很复杂的，那么这种很复杂的参数，一般来说我们在写代码的时候，后端的接收参数的时候，往往是往往首先要定义一个模型的，定一个模型的。通过这个模型来去接收参数最合适，通过模型内去接受探索最合适，你说老师我能不能单独一个一个的定义了？你单独一个的定义也是也不是不行，也不是不行，那你得需要用到这个包底好吧？当然这个太这样太麻烦，大部分情况下我们都是使用第一个模型来。

发言人   01:51

好，你看我们怎么写，比如说我们要新建一个真正的员工了，新建一个真正的员工了，我们还是一样，这个换一个views，我们在这儿，我还没接到，忘记了。新建一个python pid叫CH03回车，然后把这个views的拷贝到03里面来，这个叫CS3了，一样的4S3，这个不叫一二号船上叫请求题，请求解双菜，这些都不要。这个先留着，等会我会改。

发言人   02:40

我们首先要定一个模型类，这个模型类叫怎么写呢？Class EMP好，它继承一个副类，这个副类叫base model，这个模型是专门来接收参数的一个模型的，不是说我们之前可能很多同学学过了这个江狗，江狗里面有一个基于OM框架的模型类。当然这不是，这是是接收参数以及响应参数的一个模型类。它的特点是把请求这个阶层格式的参数把它变成对象，也可以把对象变成这个阶层格式的数据。好，我们要打爆是来自于这个PY dan，当然PUI dink了，我说过了，可以把把它改成什么fast API好吧？这个发财API没有，就是PY的那个。

发言人   03:29

好了，然后里面就可以定义你要你接受的参数不是多个，对吧？这多个里面到底包含哪些，是不是？比如说我们要介绍一个name，name是什么类型的，你就写写类型sdr看到没？可以了好，可以了。当然你要写你要加一些描述。如果假设你要加描述，那就是这样写feel。

发言人   03:51

在请求题里面如果你要做一些描述的话，用field表示了它是一个段的意思，是一个月的意思。比如说这里面我们可以加什么，这个default对不对？Default叫什么呢？我们叫它叫员工的名字。

发言人   04:15

那你还可以加其他的一些验证，比如说智能表示了对吧？你还可以做其他的一些校验，还有长度的校验都可以。之前我们讲过的，其实这个field跟我们之前讲的Perry对象用法是一样的，只是这个field是用在这个模型类里面的，代表是一个字、一个属性，一个字段的意思。好，这是第一个。我们还有一个edge，edge是int，表示员工的年龄。

发言人   04:50

好在control d还可以有，比如说员工所在的，出生日期，对吧？出生时间，那这个出生的时间的话，一般你就不能是int，不能是int应该是that对吧？带下来一下，带起来这不用带太，因为它只需要到年月日就可以了，所以服装data中的data，当然你也可以给他一个默认值，好吧？像这个怎么叫default了，应该叫desperation description。

发言人   05:29

复制。那加一下默认值，意思就是说你可以有可以没有，知道吧？可以有，可以没有，我不加默认值，说错了，加默认值。对，加默认值是代表可以有，可以没有。你比如说什么都没有，就比如说这个员工的出生时间，出生日期的出生。七它就可以没有对吧？你可以不传，但是这两个是必须要传的对吧？

发言人   05:59

OK好，其他的我先暂时不做校验先接下来我们还有这个员工的什么呢？员工的我们做一个复杂的，比如说这个员工还有地址，对吧？就写写个注册。使员工请求参数的模型。我现在我做一下格式化没错。那这个员工里头我还可以加什么呢？可以定义这个员工他所在的地址，那我再定一个类ADDR。好，它也是继承，只要是模型的都是继承best model的。

发言人   06:49

好，这是代表区域详细地址。然后详细地址里面有几个属性呢？有三个属性，比如说第一个属性provence。不是with它是独创类型的sdr sdr人家老说我看到，我不想再写描述了，其实你就写到这儿就行了然后所在的城市STR以及所在的县，因为我们这个我们到我们一般是三级的，对不对？然后country country country STR好，这代表详细地址。那这个员工他肯定也是有一个详细地址的对吧？

发言人   07:38

好，所以在员工里头有一个ADDR的属性。这个a ddr属性是哪个类呢？是我们前面定义好的IDDR的这个类。当然你可以写写完整dress。对吧写完整，那这里复制在这粘贴一样的。这里也可以跟费用的description、devot，等于就是你可以不传员工的地址，然后加一个description描述员工的详细地址。好了，你看这就是两个模型类的嵌套了。

发言人   08:22

好，然后接下来我这里面是就是要传一个员工，对吧？你要传给员工可以，EMP那么它是什么类型的呢？是我们前面定义的模型类的类型，这样子写就可以了，写完了没有吗？写完了，然后我把这个打印一下，如果有参数，我把这个参数打印一下。EMP, 整个把EMP打印一下，它是POS的请求，因为它是添加员工post。我叫搜索添加好名，这个函数的名字可以也可以改一下，叫create。

发言人   09:08

好，然后我们还有一个不着急，还有这个麦里面。先在导入的时候加一行代码，这个大家应该知道我要写什么代码。CS3这边是33分之第43。

发言人   09:26

好，然后接下来来启动。启动之后一样的打开接口文档，先打开这个文档，刷新一下。刷新之后我们有一个叫请求底穿插，对吧？这边有个添加员工，添加员工里面你看需要传一个，它就是请求体传插了。请求体传参你看传的是一个阶层，然后这个传阶层的话，它还有一个什么？还有一个这个一个样例，1 example value一个样例。好，点击添加alt你接下来你就可以往里面去修改，他他会默认给你一个样例，对吧？

发言人   09:59

你像有一些必传的，哪些参数是必传的，其实在这里可以看到。在哪里？在这还看不到对吧？在这还看不到哪些是必传的，其实我们这里面是有的，对不对？点scheme对，在这里可以看到，scheme schema里面，你看这边告诉你了，星号橙色的信号是必传的对吧？Name是必传的，edge是必传的对吧？

发言人   10:22

后面两个这个boss以及ADDR都是可传可不传的，对不对？其中ADDR是一个对象对吧？是一个节省对象，具体传值就这样说这样传。

发言人   10:35

好，我们点击创一个，然后我们这里面传一个张3。中文也可以的。年龄的话我穿一个23岁，日系我就不变了。ADDR这里面你可以不穿，也可以穿，好吧？比如说我先不传对吧？我这个我把它剪切掉，解析了，这解决掉，这是不传。那这里不传了，最后这个逗号你要把它删掉，这个地这个boss后面的这个逗号要把它删掉。你看这整个就是不是一个阶层，整个这是是不是一个阶层对象没错吧？

发言人   11:09

好，然后点击is good走，对吧？正常接收了，它是通过请求底杠D通过请求体参数传进去。你看这是不是打印了，打印的每个参数每它整个参数它是一个对象，把每个对象的属性都给你打印出来了，对吧？张3 23以及时间，时间是data类型的，然后ADDR你没有传，那就是那对不对？

发言人   11:33

如果传了，那我们我们再来一下，在这好重新再重新把它还原一下。比如说这里是湖南省，然后长沙。然后，什么长沙县。好，点击一次再点一次也是正常的。

发言人   12:04

然后我们在这看是不是可以看到完完整整的对吧？在它有一个ADDR属性，a ddr属性是一个address类型的对象，里面包含有湖南省长沙市以及长沙县。所以我们我们传的是Jason，对不对？但是接收的时候已经变成python对象了。

发言人   12:20

那谁帮我们做这个事情呢？其实就是我们这个模型内来帮我做这个事情，所以我们前面讲过这个模型它是干嘛的呢？是负责接收对象，它实际上也可以什么，它响应的对象也是这样子的。你看我现在响应的不再是OK了，就是一个EMP对象，你看这时候会出现什么效果，当然了，我们这个代码改了之后，还是重启一下来刷新。刷新之后我们还是点击try IT out，这个没粘贴，我还是写一下。张三，22岁。我们。CS长沙长沙县。

发言人   13:06

好，我把这个还是复制一下，以便于等会儿又没了，复制一下，点击excute走来这边。响应的数据里面再看，这是response body响应的数据里面是不是也是接的。但实际上我们响应的是一个什么？我们响应的不就是一个对象，EMP对象。但它响应的是什么？是一个整个是一个节省的数据，对吧？这个节省数据，你是实际上你还可以这样子，直接返回这个品，把整个EMP不要前面加MSG也行，好吧，我们再来运行一下。好，点击excuse走。

发言人   13:45

你看这个时候就是整个这这是一个阶层对象了，看到没，响应的数据也是一个整个阶层格式的对象。就是你刚刚传的这些参数，我原封不动的返回给你，对吧？好，我们还可以在这里面做一些特殊的处理。比如说我在这里面我们做一个什么呢？做做一些校验。比如说年龄必须是18到60岁之间，那在这里加一个GGE，大于等于18岁对吧？这不是校验吗？之前讲过的大于等于18，其实这个feel跟我们之前讲的Carry对象用法是一样的，小于60岁对不对？

发言人   14:25

那这个name我想做一些非常复杂的校验，那你还可以怎么做？如果你对某一个字段还想做一些非常复杂的校验，那这个非常复杂的校验的话，那我可能要写一个函数，对不对？我们叫什么呢？叫very very day。Name，为了the name。

发言人   14:46

然后接下来我们传一个参数，其实这里面应该不是写CL，应该是类，然后传个参数，8.1个值。好，这是干嘛的呢？这是校验我们的一个员工的名字，复杂的教练。

发言人   15:10

好，但是还不能生效，必须加上一个装饰器。那我这里先加一个张火符，加一个装饰器，否则他不知道你加的哪一个，加一个艾特符号，这个艾特符号后面接什么呢？接field的，然后下划线value very day。Value data就校验器的意思，完了dea校验器的意思，那你要校验哪一个字段呢？你就写那个字段名，比如说我要校验这个name，这里面就写name。好的，我先把这个该导的报道一下。啊啊啊到了这里，这里注意这是校，你校验哪个，你就参数就写哪个。

发言人   15:53

自定义一个复杂的校验器。好吧，然后针对哪个字段，针对哪个？对，是要字段或者属性做校验的校验。

发言人   16:24

好，那你就写这个字段的名字，这个字段名字我就写name。假设你要针对a ddr做校验，你就写a ddr针对这个edge做校验你就写edge，以此类推。好，然后把它格式化一下。然后接下来我们开就开始在里面写一些复杂的校验的代码，对不对？对吧？

发言人   16:44

复杂校验的代码，那这个复杂校验的代码我们怎么个写法呢？通过断言去写，这个估计是很多人学了，应该学过测试，对吧？学过测试学过测试的话，这个asset assert就是断言什么？断言是做校验，但你这里面还可以做一些其他的判断，都可以的，知道吧？

发言人   17:08

你其实你不用断言也可以。比如说if这个value是你传过来的值，对吧？等于加3。假设我规定你不能长长三，对不对？我那我就可以干嘛抛出一个校验的错误，还记得race一个value。

发言人   17:29

Metal date error. 这个校验的错误了，要导一下包，validate exception。exception. 它是来自于，但是这样子的话不行，这个还要看着异常，还得写一个我们后面再讲的一个专门捕获异常的东西。

发言人   17:56

好，那这里我们还是用断言算了，这里最好还是用我们就用这个怎么断言了。比如说当断言后面接一个接一个布尔类型，接一个布尔类型，比如说我用智能表达式了可以吗？对吧？我用这表达式那也行，认识那那你现在先在前面写，先拿这个value先导入RE模块是不是？在IE模块里面我就night对吧？

发言人   18:29

然后我之前写的一个真的表示这样写的是这样写的对吧。尖顶号中括号A到Z是不是？大写我就不管了，再加一个下划线，再是斜线W然后画框括起来画，然后是5到15，对不对？5到15，然后加一个多罗符号，多了符号表示结束是不是？

发言人   18:57

然后这里面接什么直接关掉，把你是个字符串。好，然后得到一个result结果给result结果，那你这个result的结果如果为空，对吧？Result意思，那如果为空，那就意味着什么？意味着你这是报错了，对不对？是不是？好，然后接下来我们就这里一直拉着空，not应该这样，就是not not not。这老师应该写前面。

发言人   19:46

这样写，反正总而言之，这个断言后面必须接一个倍儿类型，好吧？好，最后我返回这个value，那这样脏了这个代码，你还可以自己去写，格式化一下。这里面有一个波浪线，这个波浪线它说是birth类型，not什么什么帽子接什么什么，看起来没有错。先这样。

发言人   20:14

好，现在我们来跑起来，跑起来之后，我们回到这儿刷新。点击check out，那我现在我乱写一个，比如说我这是全是素的，我原来复制粘贴好了吧，我现在全，这其实就不行，对吧？这就不满足。点击，按道理说果然是的。对，报422对吧，是不是？他告诉我叔叔的江山有误，看到没？所以其实就是我们的这个表达式生效了。好，然后看我们控制台，控制台上打印不了，这里面也出现422，422其实就是你输入的这个数据有误，知道吧？

发言人   20:56

那为什么出为什么这里报错了呢？我为什么校验通过了？其实就是因为我们这里加了一个自定义，一个复杂校验器，对吧？当然这里面代码你可以自己去学，自己去做，你实际上还可以有一个抛出异常，抛出一个验证错误的异常。当然这个讲的异常的时候，到时候我们再去把这个代码改造一下，知道吧？因为如果你只是抛出异常，但是你没有开启的话，这还是不行的，否则你就没办法定义什么，定义那些提示信息，知道吧？最好这里面是定义一个自定义的异常。

发言人   21:29

好，关于请求体的传参我们就讲到这儿了。所以大家发现没有请求体的传参，其实就是把整个节省格式的参数当成一个对象，在在我们的API接口中去接收就可以了，对吧？好，谢谢大家，再见。