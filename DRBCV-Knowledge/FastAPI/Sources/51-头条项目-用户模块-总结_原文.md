51-头条项目-用户模块-总结_原文

2026年07月21日 12:53

发言人   00:01

Hello, 各位同学，截止到现在，咱们用户模块的核心功能就已经实现完了。那我们一块来总结提炼一下重点。我们回忆在用户模块咱们涉及到的核心接口是不是有注册、登录查看个人信息，修改个人信息，修改密码等等等。这些接口大家回忆实现过程是啊对跟咱们新闻模块基本是一致。是不是先有模块化路由，接下来定义按ORM模型类。因为我们将来操作数据库里边的数据是不是采用ORM的方式，所以我们先有模型类，下一步是不是就可以增删改查操作数据库里边的数据了。当然我们这个增删改查操作数据是不是都是封装的方法。那这个方法，在咱们路由处理函数当中来调用，得到结果return响应是不是就OK了。

发言人   00:57

这个接口的开发流程，其实跟我们上一章节的新闻模块是不是基本还是一样的。包括我们后续再来开发其他的接口的话，这个接口开发流程也是基本一样的。好，那接口的开发流程有了，在开发接口的过程当中，咱们用户模块是不是也涉及到了一些新的小的知识点，我们也来整体来提炼一下。首先大家回忆我们从新闻模块到用户模块，甚至包含后面的模块，成功的响应格式是不是都是统一的，code message data对不对？我们不希望每次都return一个大括号字典，我们封装了一个方法，把这个通用的成功的响应格式是不是封装成了一个函数。那未来我们再响应直接调用这个函数是不是就OK了？

发言人   01:48

具体咱们怎么做的呢？分三步。首先第一步封装函数return统一的响应格式。我们定义了一个函数叫success response，成功的响应对不对？参数有message和data，这个data默认值我们给了一个将来是返回新闻数据还是用户数据，具体的data我们再具体设置，是不是就OK开了。

发言人   02:14

好，接下来在函数内部，我们规定了这个通用的成功的响应格式，包含code message和data。那这个响应结果我们是不是通过杰森response的方式，特人返回了。这里我们没有用这个fast API的默认的，因为我们说将来在这个地方，我们可能得到的是fast API的对象，python dented的ORM对象，是不是都有可能？我们希望都能序列化成我们想要的code message data这样的格式。所以我们是不是导入了杰森response，用杰森response做的序列化？

发言人   02:53

这是我们的第一步，是不是相当于把这个code message data这样的一个格式给它规定好了。好，那统一的格式有了，但有一个问题，就是我们这个data是不是不固定。它有可能返回的是新闻，有可能返回的是用户，对不对？那这个data的类型不一样，那怎么办呢？为data定义模型类，比如说拿咱们用户模块来说，我们需要的有啊token，有这个user info是不是就是用户的关键信息，那咋办的？我们要的data是不是要携带token和uf info用户关键信息。所以我们就定义了一个python单体壳模型类，用这个模型类来构建我们想要的data数据是不是就OK了？

发言人   03:42

好，当然在这里我们还加了一个model config就是模型类的配置，写了一个from attributes等于true。那这句的意思就是允许从O2M当中提取属性值，当然提取啥输入性值呢？是不是包含那是user info我们需要的用户关键信息。好，这是咱们定义模型类，这个模型类是不是就是用来提取data相关的数据的。

发言人   04:12

好，那有了模型类了，下步在路由处理函数当中调用我们第一步封装的success response来响应结果就OK了。那是不是就是在路由处理函数当中写return正常响应结果，但响应的不再是大括号字典，而是调用success response，按照我们接口的要求传入message的值和data的值是不是就OK了，那这个data的值是关键。我们在这里是不是用第二步定义的这个模型类来构建我们想要的数据，是不是就OK了。

发言人   04:45

好，在这里大家看还有一个细节，基于一个模型类，第二model validate括号里面填的是一个ORM对象。那这句的意思是啥？从user这个ORM对象当中来提取属性值。这句的意义就在于，我们从数据库里面得到的user OM对象，它其实包含的这个数据是非常多的。比如说包含ID、用户名、密码、头像、性别等等等等，是不是有很多数据。但password，比如说这个密码，我们要不要return响应呢？其实不要，这个不太安全，对不对？

发言人   05:29

那我们要的从外层看，注意是不是有token，有user info。当然user info是不是有我们想要提取的关键信息。所以在这个地方，我们是不是就定义了user info对应的类型，基于我们要的用户的关键信息来model validate，来提取user ORM对象当中的数据，是不是就OK了。这样的话那个password就是我们这个user info这个模型类里边没有定义的属性，我们那个值是不是就不提取了？还自然而然那password是不是就不会提取了。

发言人   06:06

这里大家需要注意的就是from attributes等于true和model validate它俩是不是配合起来的。上边等于true代表允许了，底下model validate是不是才能真正的提取。这个是我们封装的通用的成功的响应格式。封装完之后，大家还记不记得再往后写接口return的时候，是不是都是直接调用supersize response来响应，是不是就OK了，就不用我们return大括号自己写code message和data了。好，这是统一的成功的校验格式。

发言人   06:40

当然大家回忆成功有了统一的格式了，失败是不是也有。所以紧跟着我们是不是就定义了这个全局的异常处理器。大家还记不记得这个所谓的异常处理器，它就是函数，我们再把它注册到fast API实例上，也就是注册到全局。所以是不是咱们叫它为全局的异常处理器。它可以捕获比如说通用的业务层、数据库层、系统层这样的异常。那我们再以统一的这个格式返回给前端，是不是就OK了？

发言人   07:14

那我们课上实现这个全局的异常处理器就分成两步。第一就是定义异常处理器，其实就是函数定义好之后，我们再把它全局注册。全局注册涉及到一个新的方法，基于fast API实例点儿add exception heller这个方法参数一是异常类，参数二是第一步，就是上一步定义的这个异常处理函数的名字是不是就OK了。这样的话，当无论咱们的哪个接口发生了异常之后，我们是不是就能捕获到对应的异常，响应给前端了。那以后我们再写接口，就关注这个成功的逻辑是不是就OK。那异常的交给这个全局的异常处理器是不是就OK了？所以做到咱们这个用户模块，给大家讲了这个成功和失败的统一响应格式，这是基于咱们的这个工程化来抽取封装。以后咱们同学们做项目的时候也要考虑这一层。

发言人   08:14

好，统一的成功和异常处理器搞定之后，下一步就是到一些细节知识点了。比如说咱们密码是不是加密的那咱怎么做的？帕斯lab加密，哈希加密对不对？那分三步，第一步安装，第二创建加密上下文，第三调用方法加密或者是校验密码是否一致。是不是就OK了。那我们看细节，安装的时候，我安装的是1.7.4这个版本，因为我们说我安装的是相对稳定的版本，是不是？好，接下来第二步创建加密上下文。我们这里是不是设定了这个加密的算法以及这个新旧版本自动兼容。

发言人   08:56

好，有了这个密码加密上下文了，下一步调用哈希方法实现明文加密。调用very fine方法是不是做密码的校验，就是两个密码是否是一致的。当然这个pass lab加密的这些代码，我们是不是也把它抽取到了UTS里面，那对应的哪个模块需要导入直接使用是不是就OK了，这也是工程化的思想。

发言人   09:23

好，那有了加密了再往下，还有最后一个就是用户访问令牌token。那有了这个token了，我们是不是相当于就有了登录凭证了。登录之后可以查看个人信息，可以修改信息。如果没有登录，那就不能查，不能改，是不是好，用户访问令牌token，它其实就是一个字符串，就是登录凭证。

发言人   09:46

为啥要有这个登录凭证呢？就是我们的HTTP请求。HTTP它无状态，就比如说我请求了八次，是不是？第一次我发请求登录了，但第二次我想查个人信息，不好意思，默认无状态。那咋办呢？再次发请求的时候，前端是不是就可以携带这个token，就证明大哥我登录了，你是不是得让我去看个人信息，它是一个登录凭证，那我们具体前后端怎么配合？注意来看，后端生成token，我们是不是用UUID生成的一个临时token，其实就是一个字符串。我们在数据库表里边又存了一个过期时间，对不对？好，OK, 那后端生成了token返回给前端，那前端再次发请求的时候，为了证明我已经登录过了，那怎么办？他把token添加到请求头里面，那后端想要看他有没有登录，是不是就从请求头里面获取这个token，那咱们后端的重点工作就来了，怎么获取的呢？

发言人   10:52

Fast API有一个header的参数，它是专门用来从HTTP请求头当中获取数据用的header参数。这个是咱们的token用户登录凭证，那咱们用户模块涉及到token。包含咱们后续做这个收藏，浏览历史，还都仍然需要校验token证明用户已经登录了，才能看他的收藏，才能看他的浏览历史记录。当然这个token的验证我们是不是也已经抽取过了，抽取过方法，我们封装成了一个函数。那其他的模块再想要用这个用户登录凭证，那我们直接导入来使用是不是就OK了？我们刚刚复习的这些就是咱们用户模块的核心知识，以及涉及到的一些新知识。好，那现在咱们用户模块的核心功能以及重点知识，咱们就提炼完成了。那下一小节儿，咱们开始收藏模块，我们下一小节儿不见不散。