65-头条项目-设计缓存策略_原文

2026年07月21日 12:53

发言人   00:01

Hello, 各位同学，上一节我们封装了读取跟写入缓存的通用方法了。那么这一节咱们向业务靠拢一下，调用方法把新闻数据缓存一下。这里新闻数据的缓存，其实我们有一个操作原则，这个原则就是遇到请求不再直接查数据库，而是先查缓存。缓存里边有数据，那就返回缓存里面的数据。如果缓存里边没数据再来查数据库，同时把查库得到的这个数据写入到缓存。另外就是一旦发现数据库里边的数据变化了，缓存的数据要同步更新甚至是删除。因为咱们要保证缓存里边的数据跟数据库的还是一致的。

发言人   00:48

咱们这个读写原则是开发当中的一个缓存策略，叫旁路策略。这里大家留意，旁路策略它是一个比较常见的缓存策略，那具体该怎么实现呢？咱们这样拿着一个功能来分析一下。比如说咱们就拿一个比较简单的获取新闻分类来分析一下。那大家回忆，咱们原来获取新闻分类，进入到请求是不是直接查询数据库，看看数据库里边这个分类是啥样的，是不是把它return回去。但我们有了缓存了，不直接查库，先尝试从缓存当中获取。如果缓存有数据，直接return。一旦缓存当中没有数据了，才执行查询数据库的数据。那么这个查询数据库的数据，是我们原来就写过的，这里不用变。就是在这之前是不是先尝试从缓存里面查了一次。

发言人   01:44

好，那接下来继续往下，一旦缓存没有数据是不是查库了？那我们查库了，是不是得到数据库里边的数据了，那下一步怎么办呢？如果有数据写入到缓存一份儿，接下来return这个查出来的数据是不是就OK了。

发言人   02:02

像我们刚刚这一种，就是用旁路策略实现这个新闻分类的CRUD方法，这个思路各位同学OK吗？其实就是在查库的前面是不是先查了缓存，查完库是不是写入了一次缓存。那这个思路如果OK的话，我温馨提示一点，我们这里查缓存写入缓存的方法注意，最好把它封装一下啊不要直接把这个代码放到CRUD里面，要不CRUD是不是太多太乱了。那我们封装一下，既能复用，是不是还遵循工程化的思想。所以待会儿咱们先封装查询新闻分类缓存，写入新闻分类缓存的这两个方法，然后再去改造CRUD。

发言人   02:52

那接下来咱们来封装方法来试一下。我们回到拍产品当中来，首先咱们是不是要封装新相关的缓存操作方法，那我就得这么办了，右键新建一个文件夹，比如说我就叫缓存，CACHE，那关于缓存相关的IPY文件都放到catch这个文件夹里面来。那我们现在是不是针对新闻模块来做缓存相关的操作？那我新建一个python file，起个名字news CACHE catch。在这里我打一个注释，这个PVI文件它存放的是新闻新闻相关的缓存方法，比如说举例子，新闻分类的读取和写入。那么未来像新闻列表的读取跟写入，是不是也放到这个PY文件里面来？这个文件的功能各位同学清楚了没有？清楚了吧？好，那清楚之后，那咱就规划读取跟写入的方法是不是就OK了。

发言人   03:55

当然大家冷静，我们大家还记得吗？我们现在是redis缓存存数据是不是都是通过键值对的方式，那就是k value的方式。那问大家我们要存新闻分类，要读取新闻分类，这个缓存K有了吗？是不是还没有呢？所以我们是不是得自己构造一个K名，就自己设计就可以了。比如说我举个例子，我起个名字，CATE charter JORIES下划线来一个K然后等号赋值。注意K名是字符串的方式，所以我打一个引号。比如说我起个名字就叫news，冒号冒号factories，冒号CATECATEGORIES。

发言人   04:43

好，那现在K名已经设计好了，那下一步咱们来封装方法是不是就可以了？这里我要封装两个方法，一个是获取新闻分类缓存的方法。好，敲回车。那我们再来一个就是写入入新闻分类缓存的方法，我们一共要两个，那我们从上来先来获取，那异步的，think一下DF起个名字，我们要获取那我就叫get下划线，是不是获取缓存里边的那我就CACHED杠charactery。偷个懒，这个大单词直接来一个control c复制了，control v粘贴小括号是不是冒号一下，那我们要干啥？获取缓存数据是不是？那怎么获取？对我们上一节是不是封装了获取缓存数据通用方法，我们在这儿调用传入我们刚刚设计的这个K名，那是不是就查这个K对应的数据了？

发言人   05:45

好，至于上一节封装的那个方法名，如果大家不记得了，就到这个配置文件里面来找，是不是就OK。那大家来看看，第一个读取处理的是字符串格式的数据。第二个读取处理的是列表或者是字典格式的数据。那这两个大家挑一个呗，底下这个是不是为啥？对，因为咱们有很多个分类，这个要的数据格式它是列表，而且每个分类它是一个字典。所以咱们得可以处理这个列表字典这种格式的数据是不是才OK，所以我们要底下的这一个。

发言人   06:27

好，那我把这个方法名直接control c复制一下，回到咱们的news catch里边来，我们来调用它。好，那我就await异步的方式control v粘贴一下小括号，小括号里边把咱们的factories k传进来就OK，那么获取的是不是就是character's k对应的缓存数据了。好，OK, 那么接下来我们把这个方法名给它import导进来。同时这是获取缓存里边这个K对应的数据。我是不是要把数据得到之后给它return，返回到这个函数调用的位置。

发言人   07:03

好，那现在获取新闻分类缓存的这个方法是不是已经封装完了？那我们来到下边，是不是还有一个写入呢？那我们再来一个方法，a think异步的DEF1下，咱起个名字呗，我就叫set CACHE，杠CACATEGORIES characterises小括号一下，那我打一个冒号。

发言人   07:29

首先代码我来一个pass，我先问大家这个方法需要啥参数吗？那我们想他是不是最起码需要缓存的数据，是不是得有，而且我们还要有一个过期的时间，因为我们上一节提到过，不同类型的数据过期时间是不是不一样的，那我们需要把不同的这个数据单独设置过期时间。这两个参数咱们分析出来了，那下一步咱们开始是传参。比如说我简单点来，首先这个数据我就直接叫一个data了，我是不是要传类型，那我们新闻分类它是不是一个list列表？按一个list列表，列表里边是啥？是不是它是一个字典来着？那我就DICDICT1下，当然中括号一下，它的K是不是一个STR，就是K名是不是有这个分类名字分类ID啥的逗号隔开，那它的值我来一个any了。好，那接下来我依次导入list import给它导入一下，导入typing里面的dict，也把它import的导入导入typing里面的还有一个安妮，还是一样把它import的导入好，那现在咱们的这个缓存的数据这个参数是不是已经搞定了？

发言人   08:45

那我们逗号隔开，我说是不是还有一个过期时间，那我就叫expire EXPIRE，冒号一下它是不是应该是一个int类型。接下来等号赋值。对于缓存的是新闻分类，那我来一个值了，比如说我来一个7200秒，7200秒是啊2个小时是不是？在这儿我稍微给同学们扩展一下，就是我们在日常开发当中，有一个大家习惯的过期时间的数值，来跟各位同学说一下。比如说如果是分类或者是一些配置啥的，那么通常过期时间就写7200就可以了。当然如果是列表数据，比如说后期我们要缓存这个新闻列表，那我们就给他来一个时间。比如说是六百，那就是十分钟十分钟。再来一个，如果是详情，将来我们还要缓存这个新闻的详情数据，那么我就来一个，比如说1800秒，一千8是不是就是30分钟？当然比如说还有像验证码，验证码的话这个时间非常短，我来个120是不是就两分钟就过期？那这里我们的原则就是注意我说一下数据数据数据越稳定，越稳定缓存越持久，数据要是变化快，那你的缓存时间就越短，就OK了，这是一个原则。

发言人   10:13

通常我们就按照刚刚注释里边去配置就行，所以我们这儿是一个分类，那我就设置7200。因为咱们这个项目的这个分类基本不变，所以我就写7200了。好，现在我们set catch h charactery这两个参数咱们搞定了，那接下来就到代码，代码要干啥？我们考虑功能，是不是把新闻分类数据写入到缓存，那是不是就调用上一节封装的那个设置缓存的通用方法，传参是不是就OK了？

发言人   10:48

好，那回到咱们的这个配置里面来，往下找设置缓存的方法名是set catch，那我把这个方法在参数这里，我要截一个屏了，因为它。参数三个，咱们顺序是不是不能传错？好，那我截一个屏。另外把set cash这个方法名直接control c回到咱们的PY文件里边。我们采用异步的方式control v来调用这个方法。这个方法我们把它import给它导进来。

发言人   11:18

好，那导进来之后下一步传参来看，首先第一个参数是不是咱们要的是K名，那我们的K是啊在这里characterises k，这是我们的新闻分类对应的这个K。那我就是calculate ies k逗号隔开，下一个是啥？来看value是不是？那我们的value是啥？要存的数据是不是这个data，那我就直接写data了。好，待会儿隔开再来expire，过期时间是不是？好，那我就把咱的参数expire给它传进来。

发言人   11:53

好，那现在咱们写入缓存的这个方法是不是就已经调用完了？那同时我return一下，把这个方法的结果给他return的方法set catch characterises调用的位置。好，那现在咱们向业务靠拢的获取新闻分类缓存，写入新闻分类缓存，这两个方法是不是就定义完了？

发言人   12:16

那我们下一步要干啥？到CRUD里面是不是根据旁路策略的原则来改造代码，接下来我把这两个方法给它截一个屏，给它钉在屏幕上，截一下，钉缩小搁到右下角来，右下角来，那回到注意CRUD里面，CRUD我们是不是要改到这个news蛋蛋？我讲课有一个习惯，就是写好的代码我要留存一份这个news，我直接control c control v1份。我们现在是不是带缓存功能的news CRUD方法，所以我给它加一个CACHE缓存。好，我敲一个回车。

发言人   12:58

那我现在要改造的是CRUD里边的news杠catch，那我们是不是就找到获取新闻分类的这个CRUD方法？大家来看，我们现在这个方法里边就三句代码，是不是查库返回数据库ORM结果的。那么我们要怎么改造？不是上来就查库了，获取新闻分类不是上来就查库，我们要先干啥？先尝试从缓存中获取数据。好，那如果缓存当中没有，是不是才走这一步？就是查库。

发言人   13:39

那查出来了最新的数据了，我们要干啥？一会儿我们不是直接return，是不是写入缓存，最后干啥？是不是返回就OK了？返回数据是不是就OK了？那这个思路各位同学OK吗？OK的话，我们从上来先尝试从缓存中获取数据。各位同学找方法帮我来调用。对，调用get catch的factories，那方法名我直接来复制了，回到咱们的catch文件夹里边，找到咱们这个获取新闻分类的缓存数据control z一下，回到CRUD里面，control v粘贴小括号一下，小括号一下。当然我们要import是不是把它导入一下，当然我们是不是await一下异步的方式。

发言人   14:32

好，那这里是不是在获取新闻分类缓存数据，那有没有呢？我们先把它存一下，比如说我叫到CACHED，杠charactery CATEJORIES等号来一个赋值，那这里边儿是不是有可能有有可能没有，那我们加个条件，if如果有，那干什么？直接return它是不是就可以了catch的那现在是不是就完成了从缓存当中获取，如果有是不是就直接返回了？当然如果没有进这个衣服那就是缓存当中没有，是不是照常查库。那当然查了库了，不要直接返回了。因为我们这儿return的话，底下写缓存的代码是不是不执行了，所以我在这里不直接return了，哪个变量我把它存一下了，这里是分类，那我就JORIES等号来个赋值。那未来返回，我先把这个返回写了，return factory是不是仍然是要return的？

发言人   15:33

当然中间是不是我们要写入缓存，就是把这个charactery是不是写入缓存好，那怎么写呢？调用刚刚封装的方法是不是就OK了？好，那接下来回到catch这个PY文件里边，把咱们set设置新闻分类缓存数据的这个方法control c一下，回到咱们的CRUD，那怎么写？我加个if判断，如果charactery里边有数据，是不是再写入就可以了，那我就await调用刚刚封装好的那个把新闻分类写入缓存的方法，是不是就OK了。

发言人   16:05

好，那这个方法名我来一个control c复制，set的这个control c回到咱们的CRUD control v粘贴，同时我先把它import导进来，小括号一下，那小括号里边传谁啊？我第一反应是不是就是characterises，但我说一下，有问题，因为charactery是ORM结果，这里边这里边打个注释，charactery是ORM结果。那我们刚刚的这个方法能不能接收ORM数据呢？不能，他接收的，各位同学来看一下，是list，里边是dig，那怎么办呢？别急，别急转一下，怎么转？我有办法，杰森able AABLE杠encoder ENCODER小括号一下，我允许我把它in号的导一下，导入一下，把我们的factories给它搁进来。注意这个方法它可以把我们比如说这个ORM对象，像这种复杂对象，就变成我们字典、列表、字符串，像这种杰森能认识的格式。

发言人   17:18

那我们把转完的这个结果传入到写缓存的这个方法，让它存是不是就OK了。那我来一个变量把它存一下了，那我就不叫别的名字了，还叫characterises了，那我把factors传进来写入缓存。各位同学，现在咱们旁路策略先从缓存当中取，如果有就返回，没有就查库，写入缓存瑞特人结果返回数据。

发言人   17:50

这个流程OK不那实现完之后，下一步我们要改造一下路由的位置。我们现在最新的代码是不是在news catch里边？那大家想我们的路由发请求是不是调用CRUD方法，调用的是哪一个里面呢？对，news里边的那俺们最新的又在news刚开始里边，那能行吗？铁定不行是不是？所以找到这个reuters找到news点PY。那接下来我们来看，获取新闻分类这个接口调用的CRUD方法是不是就是news里面的，那不行，我们是不是得要news catch里边的，我们把它导一下，从上面from CRUD import news cache，那下一步这一句代码是不是变成news cache里边的get charactery。那这样的话调用到的是不是就是咱们最新的获取新闻分类这个方法了？

发言人   18:52

好，那现在咱们的路由是不是改造完了？那接下来我们要做实验。但做实验咱是不是要看结果，就看缓存里边有没有数据，是不是缓存成功了，那怎么办？我说一下，咱们还是借助database这个插件，添加一个redis数据源来点data source，选择redis，选择redis。那在配置这个界面大家要注意一点，就是我们没有用户名密码。是不是安装redis到现在从来没有涉及到过用户名和密码，所以我们把它点开选到no OS，选完之后啊不要着急，不要着急点OK，这里边有一个driver驱动，这个驱动我说一下，选择到1.5这个版本啊不要选1.6。

发言人   19:41

我记得最高目前最高是1.6，选1.6连不上啪啪啪啪的报错，选1.5，注意选1.5好，那选完之后点OK，我把这个local host现在是不是一个redis的图标给它展开，注意是不是有一个数据库编号是0，咱是不是就是零。当然现在展开没有缓存，为什么？咱都没有获取新闻分类，没有发这个请求，所以是不是这里肯定是空的。接下来，咱们把后端服务跑起来，去前端界面发请求，咱们来看看能不能加入缓存。

发言人   20:21

来我们启动后端服务，接下来切到咱们的浏览器里边。大家想我们只要刷新这个界面，获取新闻分类这个请求是不是就重新发一次。一旦发走咱的代码流程，先从缓存当中取，没有是不是查库，查了是不是写入到缓存。

发言人   20:43

好，那我们现在刷新来试一下，就是让这个新闻分类重新发请求，来刷新，刷新之后，那我们回到拍场当中来，咱们现在的这个redis这里，点一个刷新来看多了点啥，是这是啥呢？来看看news factories，双击展开来看看是不是咱们的分类分类数据全在这儿喽。那现在缓存都已经有数据了，那当当回到咱们的这个catch里面来，当我们再一次发请求调用这个CRUD方法的时候，是不是先走缓存？缓存里面有直接return了，还会走下边的查库吗？就不走了。那这样的话，咱数据库压力是不是就变低了，响应速度是不是也就变快了？

发言人   21:42

这个就是咱们缓存的流程了，那功能实现了，咱们把这一节的重点来总结提炼一下。这一节咱们是封装了这个新闻分类的缓存操作方法，跟业务结合了。那一个是获取新闻分类缓存，一个是把新闻分类写入缓存。那这两个方法大家来看难吗？不难，是不是？就是我们自己去设计一个K设计好K之后，调用上一节封装的这个通用方法来获取跟写入缓存，是不是就OK了。

发言人   22:16

好，这两个方法封装完之后，我们就改造这个CRUD，实现这个旁路策略。就是先尝试从缓存当中获取，这个获取是不是调用的是方法。好，如果缓存当中有直接return，缓存当中没有往下走，是不是去查询数据库里边的数据？把数据库里边的数据写入到缓存，是不是为下一次请求提速用的。那么最后return数据是不是就OK了？

发言人   22:43

另外在代码的实现过程当中，有三个小细节需要大家去注意一下。一个就是写入缓存的时候，大家回忆这个redis它支持ORM结果的写入吗？他不支持，所以咋办？用Jason able encoder是不是缓了一下，转成这个列表、字典、字符串这样的格式，再去传入写入缓存是不是就OK了，这是第一个细节。第二个细节就是咱们这个databases插件这里，关于redis的这个驱动大家还记得吗？版本选择到1.5就别选那个最高的，它现在不太稳定，我们选择稳定版本就OK了。

发言人   23:22

好，那这个完事儿之后，切回到咱们这个缓存news catch里面来。就是我们涉及到一个过期时间，不同类型的数据过期时间是不一样的。数据越稳定缓存越持久，越不稳定的就越不持久。你比如说这个验证码，那这个缓存时间不能太长，我们来个120秒两分钟，那像我们的这个分类，我们设计的就是7200秒，那就是2个小时，对不对？咱们开发当中的原则就是不同类型的数据缓存时间不一样。那这样做，我说一下有一个好处，它可以避免所有的K同时过期，引起有一个词儿缓存雪崩。那么大家想，如果所有K同时过期，同时查数据库，数据库的压力突然暴增，这个事儿是不是还挺可怕的。所以大家在日常开发的时候，不同类型的数据缓存的时间咱们配成不一样的，避免缓存雪崩。

发言人   24:28

好了，这个就是我们这一节新闻分类数据读取跟写入缓存的流程了。接下来大家也把自己的项目来实现一下，测试一下有没有缓存成功。另外实现完之后不要走开，下一节咱们再来把新闻列表也来实现读取跟写入缓存的操作。下一节我们不见不散。