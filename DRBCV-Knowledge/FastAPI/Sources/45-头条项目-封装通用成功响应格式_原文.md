45-头条项目-封装通用成功响应格式_原文

2026年07月21日 12:54

发言人   00:01

Hello, 各位同学，上一节咱们完成了注册接口的功能了也能够正常响应结果了。那这一节咱们一块儿来抽取封装一下通用的成功的响应格式。那有什么用呢？大家来看，就拿咱们的用户模块来说，注册、登录等等接口成功的响应结果都包含code message和data。只不过每个接口这个data具体数据是不是有差异的那咱们现在的实现方式是每个路由处理函数return一个字典，这个字典是不是咱们写好的code message和data，这个字典大家想想是不是重复的那当我们站在工程化的角度来讲，我们是不是可以把这个通用的成功的响应结果来抽取封装一下，做到复用。那这样做是不是更满足工程化思维，那具体该怎么做呢？来我这里给大家整理了三个步骤。

发言人   00:56

我们首先要抽取通用的成功响应结果，那怎么抽取呢？注意抽取成一个工具函数，让它能包含code message和data。这个工具函数因为每次响应的这个message有可能不一样，data是不是也是有差异。所以message和data我们作为参数来出现，等待调用这个工具函数的时候，传入具体的message消息和data数据是不是就OK了。我们这个函数return返回值设置为Jason response，就是返回一个Jason格式的我们需要的数据。

发言人   01:34

当然这里我还用了一个方法叫杰森able encoder。这个方法的功能就是把任何的对象，比如说像fast API的对象，python单体的对象，ORM对象，都能转换成我们需要的。比如说还有code message data的对象。当然这里的data大家想想是不是还包含很多属性，那我们是不是也得提前规定类型？所以第一步抽取响应结果，但是这个响应结果里面，我们是不是需要给这个data准备类型，所以紧跟着我们下一步就定义数据类型。

发言人   02:09

这个数据类型就跟咱们的接口文档呼应，包含我们接口要的数据，比如说token，user info是不是就是用户的关键数据了。当然这个类有一个model gun config是不是我们原来没有写过的那这个是模型类的配置，重点看注意from attributes等于true。这个参数的作用是可以自动化序列ORM对象其实大白话来讲，就是允许从O2M属性当中取值。在咱们这里，就比如这个user info是用户的关键信息，来自于哪儿？是不是数据库里边查出来的user关键信息？那数据库里面查出来的是不是ORM对象？我要从ORM对象当中提取值，需要这一句属性取值为true，意思是允许从ORM对象当中取值的意思，这一句咱们要给它加上。这是咱们这个data对应的数据类型，咱是不是已经写好了？

发言人   03:13

当然在这儿还有一个user info，user info是不是还包含很多属性，我们也需要为user info定义一个类，那一会儿这个user info这个类咱就直接写代码了，就不在这儿看了。好，定义好了数据类型了，最后一步我们要在路由处理函数当中调用第一步抽取的函数响应结果就可以了。在这里我们调用的时候是不是就要传入我们需要的message？有可能是注册成功，有可能是登录成功，有可能是查询信息成功，对不对？

发言人   03:46

好，接下来再有data，data是不是我们需要的关键数据，那怎么得到呢？我们用第二步定义好的data的这个数据类型来构建一个对象。这个对象包含token，包含user info。这里大家重点看user info，来自于用户关键信息的这个类点儿，model y date这个方法。

发言人   04:13

小括号里的user就是ORM对象。那这个model validate是啥意思？这里大家注意了它的功能就是从user这个ORM对象当中取值。这里之所以能够取值，这里是取值。之所以能够取出结果，是因为咱们在第二步，比如说用户的关键响应结果和user info对应的类里边开了一个from attributes等于true，就是允许从ORM对象当中取结果。这一句跟model validate他俩是配合起来的。如果类里面没有允许，那我们直接调用model validation方法会报错的。

发言人   04:56

这个是我们站在工程化的角度来抽取成功响应格式的这个工具函数的做法。先抽取函数，第二步为我们函数响应的结果准备数据类型。下一步在路由处理函数当中调用函数，turn响应结果是不是就OK了，就不用我们return趴写个大括号，写个字典了。那这是咱们封装通用的成功响应格式的思路，接下来咱们到代码当中来试一下。

发言人   05:25

来，咱们回到pyo p当中来。首先在咱们的retour users里面，咱们刚刚写完的是不是注册接口？这个注册接口，目前还是return一个字典的方式。那我们现在把return直接写字典的方式直接给它注释掉了。那咱们整个路由处理函数就没有响应结果了。那我们先保存，可以直接到浏览器当中去试一下，来回到咱们的浏览器里边来找到交互式文档。

发言人   05:52

我们先刷新一下，那找到register try一下，那我们输入个username，比如说大邱这回该333了。密码我们来一个123456，来一个excute提交，来看响应结果是不是为now了。接下来我们要做的就是响应结果不是直接return一个字典，而是把它抽取封装一下，再来调用响应结果。

发言人   06:15

接下来我们回到拍top当中来试一下。首先各位同学我们是不是要把这个格式要把这个格式抽一个函数？各位同学我要抽通用的工具函数，放到哪里比较合适呢？对，放到youtube里边是不是比较合适？我新建一个python文件，咱们抽取的是成功的响应结果，对不对？那我起个名字，响应RESPONSE response，那我在这里定义函数，把那个成功的响应结果给它封装进来。好，那我定一个函数，DEF，成功的那我就叫circus ze了。SUCCESS circle size，RESPONSE response.

发言人   06:58

我先打个pass。各位同学问大家，我们这个方法要有啥参数？提示消息message是不是不同，响应的结果是不是不一样？我们要有俩参数，message MESSAGE message冒号类型我就写一个STR了，当然这个message我给一个默认值，比如说我就要success成功。要是传了，那你传啥我就响应啥，没传我就直接是supersize，因为咱这是成功的响应结果的格式。

发言人   07:31

好，那接下来再来data，data的话我要有默认值危难，但这个类型我就不写dict了，直接干掉也行，或者冒号写安妮也行。那我这儿直接不写了，直接这么空着就可以。Data是啥类型呢？我不确定将来你传什么结果进来，我data是不是就是什么类型。

发言人   07:52

好，OK, 那我把这个空格干掉一下，那现在咱的参数是不是就搞定了？搞定之后，我们为的是这个函数是不是把那个成功的响应结果给它封装一下。按道理我是不是要return来一个字典。但有一个问题是我就不直接写一个python的字典了，因为我们将来得到的这个对象，有可能是fast API的，有可能是python denticles，有可能是ORM。我们不直接写字典让fast API去转换了。咱们调用fast API的一个包，Jason response让他帮咱去转换，那这样的话我们更可控，我们需要什么样的格式？转什么样的格式是啊那当然在这儿需要我去先导入一下。我们往上from fast API点response，import Jason response.

发言人   08:44

下一步我们要return返回，返回的内容我们也是Jason response之后的结果。在这儿还是要跟大家说一嘴，我们有一个目标我们有一个目标有一个目标，目标是啥呢？就是我们要把任何的，比如说fast API的对象，或者是python python单体壳的对象，或者是ORM的对象，是不是都要都要照正常去响应。那这个响应的结果，是不是我们说要包含code，包含message，再包含data，这是不是我们响应的需要的这个格式，那我们怎么办呢？我可以提前把这个格式定义一下，然后借森response来转换就OK好，那我定义一下，那在这里我来一个比如说content等号赋值为一个对象，那这个对象里边肯定就是包含我们接口文档里边规划好的code message和data。

发言人   09:47

好，那这个content是我们最终要响应的格式，那放到杰森response里边怎么去转换呢？来注意加参数content。直先不要着急，不直接按通义灵码的写，通义灵码是不是就直接就让我们写content了，就是调用上边这个对象了。

发言人   10:06

那不是的，不是我们不这么干，那该怎么写？要不说咱将来可能得到的是fast API的对象，python dente ORM的对象，是不是都要正常响应成这个格式？那怎么确保它能正常转换呢？我们借助一个包儿，让我导一下for some fast API点encoders import Jason able encoder，我们用这个Jason able encoder去转换。那也就是说cantine的这个参数它的值是杰森able encoder，那要转换成什么样的呢？就是content上面这个对象存储的格式，携带code message和data的。

发言人   10:49

那现在咱们这个通用的成功结果，这个工具函数是不是就已经抽取完了？那我们这里return的结果是杰森response之后的结果，我们没有用fast API。他自动隐式转换的方式，我们来指定了杰森response来转换。为了确保fast API python 3和ORM对象都能正常转换，且是包含code message data的格式。所以我们调用了一个Jason able encoder来转换。

发言人   11:16

好，抽取完了这个工具函数了，下一步咱们要干啥来着？我们这里复杂的是data，data将来传入什么类型，咱data是不是就收什么类型。那咱拿用户这个接口来说，是不是得把这个用户的data格式先给它定义出来。所以接下来我们定义用户的这个格式，它属于python单体的类型。那我们来到schemers里面找到users，因为我们这个users是不是用户模块相关的python单提供类型，那我们在这儿来定义。

发言人   11:53

好，首先允许我敲个回车，我们刚刚第一步就是这个data，它是不是得有有自己的数据类型，那我们是不是得定义一个class，来个class，那我起个名字，比如说就叫user，但是我们是验证相关的，对不对？我叫OSRESPONS1括号冒号一下，我先写一个pass。那么我这个类型是不是要继承？注意base model。因为我们是python单体的类型，所以继承base model，大家我们这个类型里边要包含啥属性？对，是不是要包含token，包含user info？好，那我来一个TOOKEN token，token是不是就是STR类型？再有user info，那我们这个是属性，我得按咱们python的语法规则，user下划线info我就不驼峰了，注意冒号类型。

发言人   12:48

这里大家注意，我们不能直接写dict因为我们是不是得有我们需要的什么用户名、昵称、性别、简介等等等等这些属性。那直接写dit是不是不太合适？那咋办呢？

发言人   13:02

一会儿是不是咱们再去把用户信息定义一个类，是不是就OK了？当然这里大家还要留意一点，咱们那个接口，我不知道大家还有没有印象，它的响应结果user info是驼峰式的，就是这个I是大写的，没有这个下横线。所以一会儿咱是不是还得把这个属性起一个别名？

发言人   13:21

好，那我们一会儿再说，我们先把这个user info对应的类是不是给它搞出来。好，那我们往上了，允许我再敲一个回车。那么定义啥呢？我复制一下，是不是就是user info对应的类是不是这么干？好，那我来一个class，不叫叉叉，咱们好点起名字，那我就叫user info，user info INFO，再来一个response RESPSPONSE。好，括号一下，冒号允许我先打一个pass。好，那接下来问大家，我们这个user info对应的类已经有了ctl c一下，底下这个叉叉我是不是就给它替换成user info response，是不是就OK了。

发言人   14:08

那接下来等号我们给起个别名，这别名儿怎么起？大家还记得不？我有一个field，FIELLLD，那我们把它导包导一下，impart它导的是python单体里边的field的。括号一下。首先必填是不是肯定得要着的？接下来别名是不是alliance？那我们要的别名就是接口里边规范好的这个user info。好，那user info的类型也指定好了，别名也指定好了。

发言人   14:38

接下来还没完，我们要添加模型类配置，模型类的配置那怎么写？注意model model model杠config等号赋值值，注意我用config CONFIGDICT小括号一下，当然我们倒包把它导进来，好，import一下，找到python dental里边的config dict。那这里边填参数填啥？我们肯定是要开启一个允许从ORM对象当中取值的，当然还要开一个啥，就是liars别名跟这个字段名互相兼容的。

发言人   15:18

那怎么开？注意我敲个回车给大家写。首先我要有一个populate or by name等号赋值来一个true，这是别名跟字段名兼容的。好，那逗号隔开还要有一个就是from attributes，等于演示出允许从ORM对象当中取值的。那我在这儿稍微加一点注释，注释上面populate or by name，那这个就是a liars和这个字段名字段字字字段名兼容用的。好，那么下边这一个注释一下，就是允许从ORMORM对象属性中取值，开了这个才允许从ORM对象属性当中取值。

发言人   16:15

好，那现在咱们这个data对应的数据类型是不是已经搞定了？当然咱们往上还有没完成的，就是data里面的user info的类型，咱是不是还打了一个pass还没写？那我们把这个user info的类型也给它整理一下。那么大家这个user info的类型是不是要尽可能兼容咱们的所有接口，把我们要的用户的各种信息尽可能的是不是都给它包含进去。比如说像昵称、用户名、密码、头像、简介等等等，是不是都要包含。

发言人   16:50

那么这种情况怎么办？那我这么来user info对应的类，我让他先有一个基础类基础类。这个基础类里边就包含用户的基础信息，像那些可选的，比如说什么昵称、阿瓦塔头像，大家还记得吗？它都是可选的。我一会儿把那些可选的就放到这个基础类型里面。

发言人   17:12

这个基础类型定义好之后，我们的user info response继承这个基础类型，那是不是就也能拿到基础类型里边的属性字段了？像ID，user name啥的，我把它放到user info response里面来，我就分两个类来写，再加上一个info类。Info这个类就是类这个类里边，比如说我括号一下就包含ID这个用户名啥的。那像这个基础类，我这块儿放的就是一些可选的那些，这个代码稍微有点多，没有啥技术含量。那跟各位同学来提前汇报一下，我直接复制了这个模板的代码片段也给各位同学准备好了。

发言人   17:56

我们来到浏览器里边找到通用响应格式代码片段。我这里是不是有一个用户信息基础数据类型。定义了一个类叫user info base，它也是一个patent单体的类型。大家来看是不是包含昵称是optional可选的，头像可选的，真的性别可选的，BIO就是简介也是可选的。那么这些可选的我都放到基础类型里面了，那对于类型的限制注意failed，是不是允许为空，最大长度和描述是不是都给它加上了。

发言人   18:29

那么这个我直接复制走了，来复制一下回到咱们的代码当中来。这是不是就是我刚刚规划的基础类，那我在上边给它粘贴一下了。在这里我来一个control v敲一个回车，当然optional，是不是咱们得倒包一下来鼠标放上去，import导入typing里边的optional。好，那用户基础信息这个数据类型已经搞定了。

发言人   18:54

那接下来往下走，user info response是不是要继承我们上边的ser info base，就是这个基础的数据模型。好，那我来一个继承user info base。好，继承好之后，这个pass我们要干掉了，我们说刚刚提到的这个ID用户名是不是放到user info response里边来。好，那接下来我把pass干掉，那ID来一个肯定是int类型了，敲个回车还有一个user name，那spring类型是不是没问题？

发言人   19:28

接下来跟各位同学说一下，最好也包含上模型类配置，因为我们有的接口它没有token，到时候是不是就不用这个类来响应了，不用这个模型类来响应了，是不是就用它就可以直接响应这个用户信息。所以最好这个user info response也给它加上这个模型类配置，确保允许从ORM对象当中取值。至于这个alliance跟字段名兼容，一会儿我们把它干掉，那允许我偷个懒来一个control c复制，放到上边这个类里面来。当然字段兼容的这个我们把它干掉，在咱们的user info里面是不是不需要。

发言人   20:10

好，那现在咱们需要的数据的类型，这个数据模型是不是已经规划好了？重点是从data这里推导出来的。因为我们要响应的数据是code message data，那你的data是什么数据类型，什么模型，是不是给它定义出来？但定义data的时候，我们发现它里边有user info，是不是也得定义类型？这个user info的类型拆成了两个，一个base是不是一个ur info。

发言人   20:34

好，那现在咱们第二步定义数据模型是不是已经搞定了，那下一步咱们干啥了？模型都有了，那就在路由处理函数当中调用刚刚抽取封装的那个成功响应格式的函数来响应结果是不是就可以了？好，那接下来咱们找到register，咱们往最后写了，之前写死的return字典我就保留着了。好，那接下来敲个回车，return注意千万别顺手直接写字典了。

发言人   21:05

我们是不是要调用方法？就是我们刚刚在response里面定义的是不是success response，允许我复制一下，control v是不是小括号，当然要倒包把它导进来来import的导包，导入之后是不是要传入message跟data？好，那我来一个写message等于也行，直接写也行。Message咱这儿提示注册成功，那data不能写字典，我们要写啥？是不是这个类型的？这个类型构造出来的对象，所以允许我control c一下，control c回到路由里边儿。那我就这么来了，我就叫瑞斯。

发言人   21:46

PONSE response data它等于走你括号，那就是由data这个数据模型构造出来的对象，是不是才是咱们真正要用的data数据？好，这个大括号data等号大括号我们直接换成response data是不是就OK了。那这个数据模型user of response，我们要把它import导入一下。

发言人   22:11

这个user of response回到咱们这个模型类里面，大家来看是不是包含我们要的token和user info。接下来回到路由处理函数，token等于往上找，咱是不是变量名也是token，所以我就参数token等于我们的用户token值。那除了token我们刚刚说还要有啥？对user info是不是允许我tape一下？User info的值这里大家注意，不是直接返回ORM对象，我们是不是目标是提取user里边的属性值，且是我们需要的。

发言人   22:51

我们刚刚需要什么属性呢？来回到user schemers里面，是不是需要的是走你这样格式的那就是user info response这个类构造出来的对象。好，那我们直接control c一下，回到rotor里面来，那我们把这个user干掉，control v粘贴。当然还没完，我们是不是要从user这个ORM对项当中提取属性值。所以model validate一下小括号填上ORM对象。当然我们这个右侧info response这个模型类，我们要把它import导入一下。那我们现在需要的data里边的数据，token user info是不是都有了。

发言人   23:31

那接下来我们就到浏览器里边实验一下，然后回到浏览器里边来，我们换个账户，比如说这回我们来个444，密码还是123456了。XQ的提交来看，注册成功。那我们现在又能拿到这个成功的响应结果了，是不是说明封装的通用的成功结果，响应格式是不是就已经封装完了，已经成功了。

发言人   23:53

那接下来咱们回到代码当中梳理一下思路。来回来。首先我们封装成功的响应结果这个格式，其实本意就是抽取了一个函数，把我们原来直接return字典的写法是不是抽到了咱们response这个工具函数，circus ze response里面，它有一个返回值，是我们需要的code message data这样的成功的响应结果。那我们抽取了一个函数了，再从路由处理函数当中调用这个函数，是不是就拿到了我们需要的响应格式，再return给前端，是不是就OK了？

发言人   24:33

但只不过我们说这个data它是不是要有自己的类型。比如说有token，有用户关键信息等等，那怎么办呢？我们是不是在steamers里边定义了对应的类型，那我们一步一步考虑，是不是先考虑的是data对应的数据模型，data对应的data对应的就是token和user info。只不过user info它更复杂一些，是不是包含用户的关键信息。这些关键信息我拆成了两个类，一个是基础类，就是那些optional可选的那另一个就是像ID，user name这些必填的。当然我们在定义这个数据模型类的时候要注意我们有一个模型类配置。这是我们第一次见model gun config重点有一个属性叫from attributes等于true，他的意思是允许从ORM对象当中去取值，就啥意思？来还是回到路由当中来。

发言人   25:26

大家来看，我们用户信息是不是在ORM模型类里边。因为这个create user是不是操作数据库咱们得到的ORM对象。那这个ORM对象我们是不是要把它转成python单体和类型，那怎么办的呢？是不是基于python dente类点model validate。所以我们是不是就从ORM对象这个user当中提取了我们想要的值，像ID、用户名、头像等等等信息，当然因为我们定义了这个模型类里边包含的是脱敏之后的这些用户信息，像密码啥的不在这个类里边，所以是不是自动就给它忽略掉了。

发言人   26:06

这个是我们站在工程化的角度把这个成功的结果，因为各个接口都比较类似，咱们给它抽取封装了一下。那现在我们能够拿到抽取之后的这个成功的响应结果。那后续再写接口的话，我们是不是就不用再写return字典了，而是直接调用success response来return响应结果，是不是就可以了？好，那我们一块儿实现了这个抽取封装了。接下来各位同学也来动手把自己的代码这个成功结果的响应格式来抽取来试一下。抽取完之后不要走开，这一节抽取成功的，下一节咱们来抽取异常的。下一小节咱们不见不散。