39-头条项目-获取新闻详情-增加浏览量_原文

2026年07月21日 12:49

发言人   00:01

Hello, 各位同学，上一节新闻详情这个接口，咱是不是可以响应新闻的核心数据了。这一节咱们来增加浏览量，我们说浏览量是不是每次进入新闻详情都要加一？其实这个加一是不是就是把数据库里边浏览量这个字段修改一下就OK了。涉及到数据库的修改，我们说方法是update，where找到这条新闻values设置字段等于什么值，是不是就OK了。那这一句代码，咱是不是要先在CRUD里边封装一下，在路由当中调用是不是就OK了。

发言人   00:40

接下来咱们到pym当中来试一下，来我们回到py top里边来。首先我们仍然是数据库的增删改查操作，所以我们放到CRUD里面。我现在来新增一个方法，我来到最后，数据库操作的我们就直接a think异步操作了，那DEF定义一个方法，我们这里是增加浏览量，那我起一个名字increase INCREASE，increase这个单词就是增加增长的意思。好，那我直接news下划线VIEWS views是不是浏览量的意思？那我直接先tab一下。好，那tab之后，底下的这些代码，功能代码我们直接先给它pass一下，干掉pass一下。

发言人   01:24

那我们先分析一下参数，参数有DB数据库。绘画依赖项是不是肯定得要的。好，接下来还有一个news，ID是不是就是新闻的ID因为咱是不是得找到这一条新闻才能update更新它，所以这里有一个新闻ID这个参数。好，那参数搞定了，下一步咱是不是开始更新就行了。

发言人   01:45

我们刚刚在PPT里面看到这个更新，我们可以怎么写？是不是update一下？update。好，那接下来update的小括号一下，点YWHERE是不是按条件找到它，接下来再点values是不是就OK了。那这里update报错是因为得倒包，import一下导入circle alchemy里边的update。好，那导进来之后小括号我们就开始填参数了。首先update小括号，填的是模型类，那我们要增加的是新闻表里边的浏览量，所以我们的模型类就是new。

发言人   02:26

接下来外耳条件，是不是就看看表里边谁的ID跟我们的news ID形参值是一样的，是不是就OK了。那就是表模型类似news点儿ID判断相等跟形参news ID好，那接下来values怎么写呢？写字段等号来赋值，那这个字段咱是不是得参照数据库表了？打开database注意看news，这里有一个VIEWS news，它就是浏览量。好，那接下来我们要修改的字段是不是就是VIEWS等号来个赋值，是不是就OK了？那值就是基于views加一个是不是就OK。

发言人   03:14

这条语句儿咱们挺长的，先给它存一下。那么存完之后，下一步咱是不是得去执行它。但是执行完之后，在这里我还想写一个DB commit是不是提交的意思？那为啥我在这儿要加一个提交？有同学可能想到，我们之前在数据库绘画依赖项里边不是已经写过committed提交了吗？那为什么在咱的CRUD更新这里还要写一次提交呢？那我说一下，是因为数据库绘画依赖项，这里是当前绘画的所有操作，执行完是不是才会commit。那我这里我不放心，我是不是希望你子弹更新了就要提交数据库，是不是就OK了，更新立刻提交到数据库，所以这里我采用稳妥的方式，我这儿再写一遍commit了。好，那现在我这个增加浏览量的方法看起来是不是写完了，但提前跟同学们说，这里我要扩展一个知识点，其实还没写完，还可以写的更完美一些。好，那我们先这么放着，先看功能，只要功能有了，大致思路是不是就没问题了。

发言人   04:20

好，那接下来思路拉回来，我把我们定义的增加浏览量这个方法，截屏定在这里。这个方法是不是要在路由当中调用起来，来回到路由，那上边是获取新闻核心数据的那我们往下敲回车，是不是要把浏览量这个方法给它调用起来。好，那我tape一下调用news里边的increase news news DB数据库绘画依赖项是不是要传参news ID是不是也要传参传到咱们的增加浏览量这个方法里面来？那在这儿写路由处理函数的news ID没问题，当然写我们获取到的新闻数据里边的ID是不是也没问题。好，那我就用咱们获取到的了，那就是news detail里边的这个ID好，那现在咱们增加浏览量的方法已经调用起来了，我们是不是就可以来浏览器里边验证一下了？好，那接下来我们保存，回到浏览器里边来。我先来excute一下，ID为二的，然后数据找出来了，注意帮我看一下浏览量是8904。那我要是再提交一次，注意提交，那我们来看看浏览量就变成了28905，那这个增加浏览量确实是不是就成功了。

发言人   05:46

功能已经完成了，接下来我们回到排查我刚刚说是不是在这个update这里我要讲一个小扩展，让这个功能尽可能的更完善一些。那我要扩展的是啥，允许我在这儿加一个注释，就是我们这里是数据库的更新操作数据库的更新。那这个更新操作之后，我们通常要干啥？检查数据库是否真的命中了，数据库啥意思？问大家这句update a一定能把一条数据的浏览量加一吗？不一定，我们说一个最简单的场景，这个条件一定能成立，找到一条数据吗？那不一定，那万一用户乱七八糟输入一个，那咱就没有这条新闻，对不对？那更新是不是就不成功？那提交又有啥意思呢？所以我们一般这种更新操作，我们要在最后来检查一下数据库是不是真的命中数据了。

发言人   06:55

那命中了，我说一下，命中了那我反回一个，比如说TRUE处没命中，那我就返回一个false。那这样的话我们就可以根据这个函数的触或force的返回值是不是来提示用户就可以了。所以这就是考虑到各种各样的报错可能性，我们把这个检查工作给他做一下。

发言人   07:20

好，那这个怎么做？我说一下，首先看这儿XQ的这里它是不是会返回一个结果的那就是ORM对象是不是？那我把它存一下，这里边我可以提取出来是不是命中了，我来个result存一下。

发言人   07:36

好，那存完之后result RESSULT，result点儿肉point肉是不是行的意思？Coin是不是数量的意思？这个属性可以获取到命中的行数，那假设这个update执行了，是不是命中了一行，那我就这么写了，result点point大于0。比如说我真的更新了一条数据，这里是不是就真的是大于零的，是不是就是一个处了？否则是啊false。好，那我把这个值给他return出去，那return到哪里？当然是不是这个方法调用的位置，那也就是陆游这里，那我们是不是就可以拿到这个true或者是false来提示用户了？

发言人   08:30

那我就这么干了，比如说我就叫它为VIEWSRES view的结果是不是好？那我写一个if判断if直接tab一下了。Note views RES是不是？如果没有命中，那没有命中咱就抛异常呗。那在这里我来一个404，底下有详情，我偷个懒儿，就跟上面一样了，新闻不存在。那这个写完之，咱们的更新操作就完成了。那我们可以来试一下，回到浏览器还是正常数据，那我来一个excute执行，8906浏览量是不是又加了一个。但是同学们可能说咱们来一个错的ID呗，那这个错的ID我说一下，咱这儿就不是了，为什么呢？

发言人   09:19

因为一旦ID错误是不是会先进第一个if，所以咱这儿就不实验了。我们说之所以加这个if判断是不是为了是万一再有其他的地方没有用到获取新闻详情，只用获取增加浏览量，那这个时候是不是就能拿到咱们这个抛出的异常了。大家要知道的就是在数据库更新操作的时候，我们通常要检查一下数据库是不是真的命中了数据，那这个写法儿把excute的结果存一下，基于excuse的结果点肉point是不是可以检查命中的数据。那现在更新浏览量咱是不是完成了，那接下来该各位同学的表演时间，大家也来动手把自己的浏览量增加功能来实现一下。那下一小节儿，咱们来获取相关推荐新闻，下一小节儿咱们不见不散。