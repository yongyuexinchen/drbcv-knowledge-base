66-头条项目-缓存新闻列表_原文

2026年07月21日 12:53

发言人   00:01

Hello, 各位同学，上一节咱们实现了新闻分类的缓存操作。那这一节咱们继续向下来把新闻列表的缓存操作来实现一下。当然咱们这里还是使用旁路策略，也就是当遇到请求不是直接查数据库，而是先查缓存。如果缓存里边有数据直接返回，缓存里边没有数据再查库。把查出来的最新的数据写入，缓存也返回就OK了。那这是旁路策略。咱们上一节实现新闻分类的缓存操作，是不是也是按照这个流程来的？那我们就把同样的流程在新闻列表这里再来实现一次。

发言人   00:43

当然大家回忆，我们上一节操作新闻分类的缓存的时候，关于新闻分类查缓存、写缓存这两个功能，咱们是直接写在CRUD里面，还是封装了方法封装了方法。咱们站在工程化的角度，缓存操作的代码是不是独立出来了一个IPY文件？好，那咱们也是一样，把新闻列表读写缓存的操作，咱们也封装方法。在CRUD里面调用方法来实查和写的功能是不是就OK了。

发言人   01:16

那大家还记得吗？这两个方法咱要干啥？是不是规划缓存的K然后调用前面咱们封装好的redis那个读写的方法来读写关于新闻列表的缓存数据，是不是就可以了？当然我们前面提到redis缓存都是k value的方式去存储。那咱是不是得先设计K然后value就好办了，是不是就是新闻列表就行。

发言人   01:44

那么关于K，注意这里我有一个设计原则，要保证唯一性。就啥意思？比如说举个最简单的例子，我们上一节有一个news factors，这个K那我这一节存新闻列表还能叫news charteris吗？肯定不能，名字冲突了，数据是不是就乱套了。而且大家考虑，咱有很多个新闻，那是不是会有很多页儿，那存的时候这个K名每页能冲突吗？也不能是不是，所以咱们要保证这个K的唯一，那这个咱怎么设计？

发言人   02:23

注意我这么考虑，我先带一个前缀news list，代表新闻列表。接下来因为我们有很多个分类，带上分类ID，再带上页码，就代表你不是一个列表吗？你这个列表是第几页，再有每页的数量，当把这些一家问大家，咱的当前这个列表是不是K名是唯一的了，应该是了。那我就这么规划了，确保唯一大家以后开发当中也是一缓存的K需要咱们去设计，确保唯一性，要不然数据冲突。

发言人   03:00

好，那K确定了下一步是不是就是Y6？Y6好办，是不是就是新闻列表就OK。好，那按咱们这么分析，我们是不是先封装关于新闻列表读取缓存、写入缓存的方法，按照对应我们设计的K存新闻列表数据，读新闻列表数据是不是就OK了？

发言人   03:22

好，那么封装好了方法了，我们就改造CRUD，按照旁路策略的原则来改造是不是就ok好，那接下来咱们来写代码来试一下，回到咱们的排场当中来。首先我是不是要找到缓存里边的news开始，因为这个是不是咱们封装的关于新闻模块的这个缓存读取写入的这么一个PY文件。好，那我们来到news catch，是缓存里面的，那我们接下来是要干啥？定义新闻列表数据，读取跟写入缓存的方法是不是？好，那接下来我加注释。首先我们要定义方法，比如说咱们要有写入缓存的方法，当然咱们这里写入缓存是不是关于新闻列表的，对不对？有写入还有啥？还有读取缓存的方法，当然也是关于新闻列表的。

发言人   04:18

好，那我先来写这个写入缓存的这个方法了，那我们来一个异步方法，a link DEF定义一下，比如说我们是写入缓存，我就叫set，catch new's list这个名字是不是还不错，那我直接tab一下，当然代码我pass一下，待会儿咱们自己写。这个参数我也把它干掉，咱们自己来分析，自己来写。那需要哪些参数呢？咱们要考虑功能。

发言人   04:50

首先大家回忆，我们说这个缓存K咱们前边是不是设计过这个K大致是咋写的？我说确保唯一性，那我来一个news list这样的一个前缀。然后接下来我冒号是不是跟上啥分类的ID是不是还冒号跟上啥？页码就是你当前是第几页儿，再冒号跟上每页儿的数量，每页数量我们刚刚是不是这样规划的？

发言人   05:20

好，基于这个规划问大家，你说我们这参数学要啥？分类ID要不要？要页码要不要？要每页数量要不要？要，当然还要啥？我得有新闻列表数据，那是不是得有一个数据的形参，当然是不是还得有过期时间，是不是也得要？因为咱们缓存的设置还是要设置过期时间的。

发言人   05:49

好，那也就是说我们的这个参数基本上这一坨要着还要有再加上，加上啥呢？就是咱们的列表，列表数据再加上过期时间，是不是这么来？好，那我可开始了，咱们就从左开始来。首先分类ID比如说第一个分类ID那我就character ID了，JORY，下划线ID，冒号给个类型。比如说这里我就来个可选了，optional OPTIONAL，中括号来个int当然我把这个option给它import导入一下，稍等一下，import导入拍屏里面的。

发言人   06:30

好，那我现在第一个参数分类ID是不是搞定了？那逗号隔开还要有啥？就是这个页码，页码的话比如说我就叫配置了，那它的类型是不是就是int类型，好多会隔开，还要有啥？对每页的数量是不是？那你就要配置size或者我直接叫size了，是不是？也OK那冒号还是in的类型，那再逗号隔开，还要有啥列表数据对不对？那我叫data也行，或者直接叫news list是不是也OK，那么这个类型允许我tab一下了，是不是？首先它是列表里面包字典，字典的K是STR值，是不是？有可能是任意类型，所以我写一个any了。

发言人   07:16

好，那再往下expire这个是不是就是过期时间？那过期时间的话这里是不是有一个默认值？当然这个默认值我们可以把它去掉，或者是我们按照前面规划的，大家还记得吗？咱们是不是根据不同的类型时间设置成不一样的。因为都一样的话，一下全都过期是不是会造成缓存雪崩？所以我们的原则就是如果你这个数据越稳定，那我们缓存时间就越持久数据变化越快，缓存时间就越短。那我们之前规划，大家看是不是我们说这个缓存时间，像列表，我们是不是可以规划成1800秒，那就是30分钟。好，那我就直接这么着了。

发言人   07:56

好OK那我们现在这个参数是不是就写完了？那我们下一步干啥？是不是写功能？那问大家这个功能是干啥？是不是就是调用前面封装的redis的设置的方法，存存这个新闻列表到缓存是不是就OK了？

发言人   08:23

那是不是就是调用前面的方法就行了？那我是不是await一下，大家还记得方法吗？如果忘了的话，是不是去catch config里边看一下，那是不是就调用set cache分别传入k value，还有过期时间是不是就OK好，那回来回来到咱们的代码当中，那我就调用set catch传入K那K我是不是还没有呢？待会儿我是不是要设计一下，给它拼接一下，逗号隔开传Y6。Y6是啥？对，news list那逗号再隔开，还有expire是不过期时间。

发言人   08:59

好，那现在是不是就差K了，那咱给它提前规划一下呗。K比如说我等号赋个值，咱们规划的是不是噔噔噔噔长成这个样子，那咱就拼一拼呗。首先我需要news list前缀，然后后边拼上咱们这些形参是不是就OK了？那我这么干，把这个news list这个前缀儿这个前缀儿，我把它放到上面，作为一个变量出现了。来我control z一下，那往上来，咱们来一个小变量名，常量名儿，比如说我就叫大写了news list下划线list，然后再来一个前缀prefix PREFIX，等号赋值来news list。那未来关于新闻列表这个缓存K是不是都是以news list冒号开头的？

发言人   09:50

好，那接下来咱们开始往下拼接，那我就来个F字符串拼接是不是先放上一个大括号，找到咱们那个news list和prefix。那接下来是不是放ID，但这个ID我打算处理一下，因为这儿是不是有一个optional，那代表可选，对不对？因为有可能会出现什么情况？就是某些新闻，比如说它没有分类ID那没有分类ID的话，你这总不能是一个乱码或者没有，那就不合适了。所以我把它提前处理一下，我怎么处理？比如说我叫起个名字，character改一改，PPPA character part就是分类部分等号来个赋值，那我来个if条件判断，如果有ID那就直接用ID否则比如说我就来个就代表是所有分类之下的。好，那我就这么写。

发言人   10:42

If character ID这样is not not is not NONEN。如果不是难，也就是有这个character ID那咱就这个character分类这个部分。就直接取值就是这个ID当然如果是难了没有，那就直接是这个单词就可以了。所以也就是说这个分类ID这一部分是不是有可能是具体的。比如说一这个分类，二这个分类，就是这个数字是不是也有可能是哦啊好OK，那这个分类ID这一部分搞定了，那我们下一步好办了，是不是来个大括号放上capture part，然后接下来我拼一个冒号，拼一个冒号是不是就到页码还有这个每页的数量了。那这回直接用行参是不是就OK了，那就是配置，然后冒号再来一个size是不是就OK搞定。

发言人   11:42

好，那这个K的拼接各位同学OK吗？OK。好，那有了K吗？我们是不是就设置这个缓存？当然设置会有一个结果，我们把它return一下，是不是return到未来这个方法调用的位置就可以了。

发言人   11:57

好，那现在关于新闻列表写入缓存的方法是不是咱们封装完了，那再往下是不是还有一个读取，那我们来a think DEF起个名字，这个名字我觉得也可以get cache获取缓存。谁的呢？News list的。好，那接下来往下，这个参数我还是给它干掉，待会儿咱们分析具体的功能代码我先pass一下。

发言人   12:22

好，那问大家我现在是不是要获取新闻列表的缓存数据？那我存的时候是不是按照这样的K存的那我问大家读的时候是不是也按照这样的K来读？所以这个K这个K的拼接的代码我偷个懒，control c一下是不是可以放到读取缓存这个方法当中来。那一放进来，我们对应的参数也就差不多了。

发言人   12:50

那问大家character ID要不要？要配置要不要？要，代表是页码size，要不要？要代表的是啊这一页有多少条数据，是不是？好，接下来这个参数问大家出来了没有，是不是就是分类ID带上页码，带上每页的数量，我们给它control c，回到读取的这个方法，control v哦耶是不是搞定？

发言人   13:20

好，那参数搞定了，接下来读缓存是不是就OK了，那怎么读？调用咱们在配置里面封装好的get缓存的方法。Get catch，get get on catch用哪一个？杰森的是不是？好，那我回来，到咱们的这个新闻，到咱们的这个news cache里面来，那我要读，那我来个await。因为咱们是异步的，get Jason开始传进去咱们的K当然返回的结果我们给它return一下，返回到这个方法就是读取新闻列表缓存数据方法调用的位置是不是？

发言人   14:01

好，那现在咱们关于新闻列表读写缓存操作的俩方法是不是封装完了？各位同学下一步干啥？实现缓存策略，旁路缓存策略是不是就OK了？

发言人   14:16

好，那去哪里实现？对CRUD里面，但注意我们是不是来到CRUD里面的news cache里面。好，那我们找到CRUD的news开始，是不是找到获取新闻列表这里。那目前是不是直接查库，就return结果了。我们说那你不要直接查库了，我给它存一下，存一个news list这个变量。未来早早晚晚是不是都要return这个news list？

发言人   14:48

当当整个流程应该加啥？先尝试从缓存当中获取，没有查库，写入缓存return是不是就ok？那我们来加入注释，注意先上来先尝试从缓存获取新闻列表，如果有那就直接return了。当然如果没有是不是照常查库，查库的代码不变，然后接下来还要干啥？写入缓存写入完了return是不是就OK了？这个是咱们旁路策略的原则。

发言人   15:26

好，那我们就尝试获取一下呗。那咋办呢？各位同学，调用方法是不是就OK好，那我偷个懒儿，把名字直接复制了。那我获取的control c复制回到咱们的CRUD，来一个control v粘贴，我来来个括号，调用input给它导进来。当然我在前面加个await，是不是要异步的方式？

发言人   15:51

好，那这个方法鼠标放上来是不是得传？我不能截屏，大家来看是不是得传ID传页码，传这个每页的数量，对不对？所以在这里我是不是要传这个分类ID然后还有页码还要传入这个每页的数量，是不是要传这些，我把它注释一下。

发言人   16:18

好，那大家来看看，分类ID咱这里是不是已经有了页码有没有，注意看是不是没有，一会儿咱们得自己计算，每页的数量有没有？有是不是limit。那这个页码这儿页码该怎么算？大家不要头疼，在这儿我推导一下，就是咱们原来这个skip他是不是跳过的数量，跳过的数量，数量是不是现在的scheme？大家还记得这个skip咋算的吗？注意，是不是页码减1，乘这个每页数量是不是得出来的那问大家，如果有了这个公式了，我有skip跳过的数量，还有limit每页的数量，那这个页码能不能算小学知识，是不是就能搞定？

发言人   17:17

来吧，兄弟们上帮我推导一下，来推导一下怎么算页码应该等于什么。不好意思，通义灵码是不是给咱算出来了？就是type一下跳过的数量，除以每页数量加上一是不是就OK。当然我在这儿，我写整除，整除就是因为这儿它是不是出不来小数，以免咱们这个排查母不能识别，所以我这用整除了。

发言人   17:50

好，那问大家这个页码的算法大家OK吗？如果OK的话，这三个参数是不是就集齐了？集齐是不是就能真的调用它了？

发言人   18:00

好，那我们先算这个页码。比如说我来一个配置，这个变量保存等于skip除每页的数量加上一个这个配置，就是页面是不是就算出来了。那我这个方法是不是就能调用了？来can to c一下了，回来V粘贴一下分类。ID是不是就是形参character ID页码换啥？页码是不是就是上面的变量配置每页的数量，各位同学换啥？是不是就是咱们上面的形参limit？

发言人   18:35

好，那这里是不是会得到结果，那我给它存一下，这就是缓存的那个列表，是不是CACHED，下颌线等号来个赋值，当然是不是如果有数据就return了，那怎么办？If catch the list冒号，我特别想return RETUTURN cache list，按逻辑是不是这样解就OK了，但但no no no不行，为什么？这里是不是从缓存当中读出来的，它是缓存数据对不对？缓存是不是咱们前边的阶层格式，但我这里return返回的应该是什么格式？这里返回的，注意它不能直接这么写。我要的打个注释，要的是ORM格式的，咋整？转他一下是不是就可以了？

发言人   19:34

Tap一下，我还是return来个列表推导式，for item in catch the list。那前面大家还记得关于新闻的模型类是啥吗？News NEWS小括号一下，构造一下，当然是不是把咱们的item放进去，当然item是一个大字典，心馨儿是不是给它展开一下。那这样做的话，是不是就是把咱们从缓存当中读出来的杰森格式的数据转成了ORM格式的那在return返回是不是就OK了？

发言人   20:10

好，那么现在读取缓存里边的新闻列表是不是搞定了？来逻辑往下走，如果没有进这个if，说明缓存是不是没有数据。所以查库查完库写入缓存是不是就OK了，那就是调用咱们前边封装的那个set缓存那个方法是不是？那这里当然我们得是news list里边如果有数据是不是在写，没有写了也没用是不是？所以就if news list接下来是不是调方法来写入。

发言人   20:40

当然写入的时候大家还要注意一件事儿，你看咱这个代码是不是提示我一个转换的方法，就是我们的缓存要的应该是啊这个字典、列表这种格式的那不能把ORM news list是不是直接给它写进去，所以是不是这里提示我转了一下？大家对这个方法有印象吗？咱们上一节那个新闻分类写入缓存是不是就用它了？但是这里我就不用了，给大家换一个方法。

发言人   21:12

但是这里大家逻辑上知道，我打一个注释，就是先把这个ORM格式的这个数据是不是得转换成这个字典，是不是才能写入缓存，所以咱们这儿得先转，是不是才能存？那这怎么转？我在这儿给大家一个思路，首先咱们要把这个ORM转成python单体壳类型，就是我们指定一个具体的类型去转换，就想要哪些字段就拿哪些字段，这样更严谨。好，那转成python单体格类型了，再把这个python体格类型再转为这个字典，再转为字典。咱们这么来，最终写缓存。

发言人   22:00

那咋办呢？注意在这里，我就这么来了，我来一个news data这么一个变量存一下，还是来一个列表推导式，那就是for item，item比如说in咱的news list，但前面要干啥呢？是不是把这个item暗示每一项，是不是转python单体和类型，转一个新闻对应的python单个类型，再转字典是不是就OK了。也就是说这里我会给大家用两个方法，但是首先就是python单体和这个类型咱们要么重新写，要么在原来的找。当我提示一下，这个我们原来写过，允许我找一下咱们原来的skimmer，有一个贝斯手。Base里边儿是不是有一个news item base来看是不是新闻相关的，我们就用它就OK了。好，那我把它直接control c过来，回到咱们的CRUD，那就是这么来control v粘贴空格一下，然后我先把它input给它导一下，导入好，导入之后接下来怎么写？

发言人   23:07

调用方法点儿model validate是不是从ORM当中提取属性值，从哪一个ORM对象就是咱们item这个变量。好，那这么写，是不是就把item转成了python dente类型，是news item base这个类型的，按它按里边的字段来转换。好，那转好之后，现在是不是已经转成python的类型了？好，那再下一步转自电那怎么办？

发言人   23:36

继续调方法，model dump括号一下。那我们是不是转格式为Jason，那我来一个mode等于引号Jason，当然我再来上一个参数，叫by a liars，取值我为false，就是不用这个别名，为啥我要加上它？来允许我回忆一下这个base。大家来看我们这个news item base这个类型里边是不是属性名是python格式的，但它有别名是不是captain ID那我们这个缓存数据是不是传给后端用的？那我们要保持python的风格，就不启用这个别名，不用别名。

发言人   24:18

好，OK回到咱们的CRUD，允许我给大家加点注释，尤其是这一句，by a liars等于false，来注释一下control v它是不使用别名，要保持这个python的风格，要保持python的风格。因为因为打个因为因为咱们redis里面的数据，redis数据是给后端用的，所以我们直接不用别名还是python的风格，这样咱们后期是不是还好处理？好，那现在咱们这个news data是不是就是我们转换之后的了，那下一步就好办了干啥？存是不是就可以了？

发言人   25:03

那我就await一下，await set catch news list是不是好像就是这个名字。那我先tab一下，导入看看有没有。来有import一下。好，导入capture ID，配置limit，还有就是我们刚刚准备好的转换好的这个news data是不是就OK，顺便我检查一下是不是这个名字。

发言人   25:27

来回来，such cache news list是不是就是它？好OK那我们现在写入缓存的功能是不是也实现完了，那我们下一步是不是就return就OK了？那现在咱们关于新闻列表的旁路策略是不是就已经设计好了？

发言人   25:43

那我们下一步，各位同学我们要干啥？注意还不是测试，一定要记着。就是咱们路由里边大家还记得吗？调用的它是不是仍然是那个news里边的获取新闻列表这个方法，咱们是要把它改成news catch里面的。所以回到咱们的reuters news模块，news模块，找到这个获取新闻列表是不是在这里。那大家来看看，咱们调用的get news list是不是这个news里面的，现在要把它改成news cache里面的，来control s一下。

发言人   26:22

接下来各位同学，咱是不是要测试了，那测试是不是就是刷新前端界面，然后看database，默认现在是上一节课的那个缓存的新闻分类，看看这儿是不是会多出来新闻列表，是不是就OK了。好，那我们回到浏览器，先刷新前端界面，来回到浏览器刷新，那回到咱们的排场M，刷新一下database插件，刷新一下。注意了，看news list是不是已经来了，那前缀是news list，然后接下来是不是就是分类ID是一，当前页码是一，每页是不是十条数据。

发言人   27:03

那我们展开来看看，来看看第一页的新闻是不是全在这儿了。那缓存当中已经有这一页新闻列表的数据了，那我们下次再去发请求，是不是直接请求的就是缓存里边的那就不会回到CRUD，先执行缓存的查找，如果有是不是就在这里直接return了，没有是不是才查库。那我们服务器数据库的压力是不是在新闻列表这里又降低了？好，那这个是咱们新闻列表的缓存操作就已经完成了。

发言人   27:40

那咱们一块儿总结提炼一下这一节的重点。我们这一节仍然是沿用旁路策略，实现了新闻列表的读取缓存跟缓存的写入。当然咱们缓存列表这里涉及到咱们自己去涉及K这个K比上一节的那个新闻分类是不是稍微复杂一点？我们为了确保唯一性数据不冲突，这个K设计成了带分类ID带页面，带每页数量的情况。这样保证每一页新闻都是有自己独立的缓存的。

发言人   28:13

另外在写入数据的时候，写入缓存的时候大家要注意，这个格式要转换。因为我们说redis它不能直接存IORM数据，那怎么办呢？把ORM转成我们需要的具体的严格的类型，就是需要哪些属性，就转一个python单体，可再转为字典。那么这里我们调用的方法大家来看一下，使用某个python单体壳类型就是我们之前定义好的，对不对？I news item base这是python单体和类型。Model validate是不是从ORM对象item当中提取属性值，那这样的话是不是就转成了python单体和类型了？好，再调用model dump是不是转成Jason格式，这样的话咱们才能把这个Jason格式的news data是不是存入到redis缓存当中。

发言人   29:02

那么这个写入缓存这个地方稍微有一点复杂，大家在课下练习的时候稍微注意一下。当缓存的写入功能也实现了，大家切记是不是咱们的rotor里面调用的方法不再是news里面的get news list，而是news cache这个缓存里面的。那把如改完之后，大家就刷新前端界面，是不是看看database这个插件里边能不能看到新闻列表的缓存数据就OK了，那这个就是咱们新闻列表的缓存操作流程，大家也来动手来试一下。

发言人   29:36

另外我还有一个大胆的想法，我们一起做了新闻分类和新闻列表的缓存功能。后边还有啥跟大家说，还有新闻的详情，还有相关推荐。那这两个功能大家想想是不是还是一样。先封装关于比如说新闻详情的读取写入缓存的方法，然后改造CRUD，按照旁路策略来改造，把这个路由里边调用的方法不再是news里边的，而是news catch里边的，是不是就OK了。所以这个思路是一样的。那交给大家来独立完成新闻详情跟推荐内容的缓存功能，好不好？那就该大家的表演时间了。大家先把新闻列表的缓存功能来实现，实现完之后再把这个底下新闻详情跟新闻推荐的缓存功能也来实现一下，看看谁能把旁路策略运用的最熟练。