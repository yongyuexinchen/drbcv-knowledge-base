54-头条项目-取消收藏_原文

2026年07月21日 12:53

发言人   00:01

Hello, 各位同学，这一节咱们来完成取消收藏功能。我们还是一样，先看接口要求，写路由，再来分析具体该怎么实现取消收藏的功能。首先看接口要求，接口地址，这里请求方法是delete。因为我们所谓取消收藏，是不是就是在收藏表里边把这一条新闻的收藏记录给它干掉，是不是就OK了。所以咱们请求方法，这里是delete，路径是斜杠remove移除的意思。另外仍然需要认证请求头，登录了才能取消收藏，否则那你取消谁的？好，另外news ID就是你要取消的是哪一条新闻的收藏记录？我们需要一个news ID。

发言人   00:50

好，另外再来看响应结果仍然是统一的，格是code message和data。Data直接就是now默认的，咱不用管了，是不是就了？好，那这是咱们取消收藏这个接口的要求。那有了要求了，咱路由是不是基本上就能写了？来，接下来咱们先写路由，再来分析具体该咋取消。好，回到咱们的pym里边来。那我们要好再来一个接口了。好，那我们来一个艾特router，点儿delete一下。那我们说路径是不是我们要求是remove 2EMOVE，那接下来写路由处理函数，i think DEF, remove favorite这个名字还OK。

发言人   01:32

底下的代码我们先给它pass一下，接下来我们看这个参数，首先我们接口要求news ID是不是得要着，因为news下划线ID是python的写法，那我们将来写参数是不是得是news驼峰式的ID所以我们导入query，查询参数a liars来个别名是不是就OK了。接下来认证token的方法，把它dependence注入数据库绘画依赖项是不是也来注入一下参数，这三个是不是也正好是咱们需要的？好，那我就直接用着了。那接下来pass这里我先写上一个return，那我们是不是还是用supersize response，那我直接来一个删除收藏成功，那个data就不用管了，因为我们直接data是一个now，那就是默认的那个那那个值是不是就OK，这个不用管了。

发言人   02:26

好，那现在我们的基础路由写好了，下一步我们是不是就该分析一下这个具体咋去取消收藏呢？来我们回到PPT里边来分析一下，回到PPT里面来，想要取消收藏仍然是进入请求的时候需要认证投肯。我们携带了一个news ID这个参数，将来按news ID找到咱们这条收藏记录，把它移除了是不是就ok进入请求认证token携带了参数了，那我们下一步验证用户是否登录了，就是验这个token有没有效，是不是？如果没有登录，那咱就报错提示登录了，是不是就能往下走去取消收藏了？

发言人   03:08

那取消收藏，其实就是把咱们收藏表里边当前这条新闻记录是不是给他删掉。那删掉注意调用ORM方法delete，移除就OK了。那移除完之后，注意我们要检查一下命中的数量，就是你移除是不是有可能成功，有可能失败。如果移除的数量，比如说大于零了，那就是一条，那是不是代表真的取消了一条新闻的收藏？如果是零是不是代表没有命中所以如果不大于0，我们要抛异常，那大于零了是不是才代表真的？取消了一条新闻的收藏，那我们来响应结果是不是就OK了？

发言人   03:52

好，这个是咱们取消收藏的思路，其实核心就在哪里？是不是要delete当前这条新闻的收藏记录，然后检查一下命中数量是不是就OK了。接下来这个思路各位同学OK吗？我们OK的话就来写代码来试一下了。

发言人   04:12

回到咱们的playtime当中来，我们是不是封装CRUD方法？那我们切到CRUD favorite，那我们来往下定义一个方法，我多敲一点回车了，在这里来一个a think DEF，稍等一下，remove news favorite。这个函数名是不是也OK？那接下来底下的这个代码，咱们先给它pass，待会儿咱们分析自己来写。

发言人   04:40

其次看看参数，数据库绘画依赖项各位同学要不要？肯定是要的。User ID要不要？要news ID是不是铁定也得要？因为我们是移除当前用户的这一条新闻的收藏记录，所以user ID news ID是不是都得要着。那咱们这三个参数现在就搞定了。那我们把这个代码功能来给它实现一下，把pass干掉了。

发言人   05:09

那我来一个delete DELETE，delete显模型类where，WHERE，写你的筛选条件。那delete我们要把它import导进来，导入circle alchemy里面的那删的是哪个模型类？Favorite favorite，咱们的模型类的名字给它搁进来。那你要删的是什么样的记录呢？是不是就是看看当前用户的这一条新闻，对应的记录要删掉，那怎么写？对，favorite点user ID等于形参user ID逗号隔开。

发言人   05:50

Favorite点news ID等于形参news ID这是一条ORM语句，比如说我就叫STMT把它存一下，那下一步我们是不是excute来执行它？执行完之后commit去提交，当然提交完之后我们说要干啥，检查命中的数量是不是？那我就直接return一个，咱们把这个执行的结果给它存一下，那我就叫RESULT，等号赋值。那return result点儿什么来着，row point是否大于0，是不是就return一个或者true或者force到这个函数调用的位置了。那接下来咱们调用这个函数拿到true force为触，代表真的移除了一条新闻的收藏。那我们正常响应结果，否则就rise抛异常是不是就OK了。好，接下来回到咱们的路由里面来，那我们就开始调用方法了。Date favorite里面的remove news favorite，我tape一下，传DB数据库绘画依赖项，传user ID news ID是不是OK没问题。

发言人   07:09

传完之后，我们这个函数正常被调用了，是不是会响应一个结果回来？那这个结果是不是就是那个布尔值？那我来一个变量result把它存一下了，那这个result值是不是有可能是true，有可能是force。如果是true，正常return响应结果，那如果是force，按咱说抛异常是不是？那我写一个，if not result rise抛出异常，那HTTP exception我们把它import导入一下，导入fast API里面的那接下来再有status，我们再把它import导一下。如果是为false，就是这个result为force，是不是代表当前这个记录是不存在的，没有找到这样被收藏的新闻，对不对？所以那我就直接是404，收藏记录不存在了。

发言人   08:00

那现在咱们这个rem取消收藏的功能，就已经写完了。那我们就回到浏览器里边来试一下，回来到咱们的浏览器里边来，先刷新一下，当前这条新闻，小星星是橙色的代表，添加了收藏了，那我给它点一下试试，点星星不成了，那是不是就取消了收藏了。当然咱也不确定就是这一条新闻，发射卫星的这个是不是第三条新闻。那我们去那个favorite表里边来看一下这个IDV3的新闻是不是不在收藏表里边了，是不是取消收藏了呢？

发言人   08:43

来回到咱们的patch当中来，找到data base这个插件，找到favorite这个表，来看看果然收藏的新闻是不是只有12，那个ID为三的是不是没有了，被干掉了。好OK那干掉了这条收藏记录了，说明咱们取消收藏的功能。这个接口是不是已经OK搞定了？

发言人   09:07

那实现了功能了，咱们把整个remove取消收藏的实现思路来整体回顾一下。首先来看，我们按照API接口规范文档的要求，是不是传入news ID新闻的ID在认证token。接下来我们封装了CRUD的方法，这个方法是不是delete移除，那移除的条件是不是就是看看表里面是不是当前用户，是具体哪一条新闻，找到之后把它delete。当然这个delete，因为我们最终要看看是不是真的删了，真删了是不是才响应成功，否则就抛异常。所以我们瑞特了一个命中的数量是否大于零这样的一个布尔值。那么我们在调用这个CRUD方法的时候，是不是就拿到了这个布尔值了？如果布尔值为处，那就正常race pose响应删除成功。那如果response，那rise抛异常是不是就OK了，这个就是咱们取消收藏的大致实现思路了。

发言人   10:12

我们一块儿实践完了，接下来该同学们的表演时间了。大家把自己的项目取消收藏的功能来实现一下。另外实现完之后不要走开，下一节咱们来实现获取收藏列表的功能，下一小节咱们不见不散。