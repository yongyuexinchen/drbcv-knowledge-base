29-FastAPI进阶-ORM总结_原文

2026年07月21日 12:50

发言人   00:00

零基础学AI月薪过万就来黑马程序员。Hello, 各位同学，截止到现在，circle alchemy ORM工具常用的知识点咱就已经学完了。知识是不是稍微有点多？咱们一起来整体回顾一下。在2个小时之前，咱们开始了ORM的课程。我们提到有了ORM工具是不是可以不写circle语句了，可以用面向对象的方式是不是来操作数据库了？经过漫长的学习，大家体会到O2M面向对象的编程方式了吗？历历在目是不是？

发言人   00:38

那咱们回顾一下细节，我们说咱学的ORM工具，是不是叫circle alchemy？ORM为啥学他呢？因为circle阿米是企业里边用的最多的，社区最活跃的。咱是不是挑着这个第一来学的那想要用circle alchemy，咱是不是得先安装一下，安装完建库建表，然后是不是就能增删改查操作数据了。

发言人   01:05

首先安装的话，大家还记得吗？安装几个包派两个，一个是circle alchemy，当然是异步的是不是好？另一个是mysql le的异步驱动。那安好了包了，下一步咱是不是就能建库建表了？那建库咱是不是还用circle就行，就是create database是不是好，那有了库了是不是可以建表了？那建表的代码各位同学回忆是不是稍微有点多来着？那坐稳了，咱们回忆一下。首先要件表分三步，第一步先创建异步引擎，数据库引擎，最重要的就是你的数据库是mysql cl，还是这个阿帕奇，还是circled，地址是啥，用户名密码是啥？我们要create or think engineer的方式，是不是创建一个咱自己的数据库引擎？

发言人   01:59

那有了数据库引擎了，下一步定义模型类。为什么要定义模型类呢？因为我们说ORM，它是不是用面向对象的方式来操作数据库了。所以面向对象是不是有class类？咱们一般要先有一个鸡肋，是不是再有一个字节表对应的模型类。那鸡肋的话有要求，是不是要继承一个类，declare base。那这个鸡肋里边一般放的是不是就是共用的属性字段？好，那有了鸡肋了，咱想创建啥表，是不是得有一个对应的模型类，这里边要负责表明你的字段是啥，分别是啥类型，对不对？

发言人   02:43

那有了模型类了，是不是就可以基于模型类来建表了？那注意咱们是不是要在fast API启动的时候建表，那建表的核心写法就是run think调用咱们的鸡肋。注意这是鸡肋的名字，基于鸡肋调用原数据，meta data，接下来create 2去建表是不是就OK了。

发言人   03:07

那有了表了，下一步咱是不是就可以增删改查操作数据了。但大家回忆，我们增删改查操作数据是不是一般都是在一个路由，在一个接口里边操作。所以要增删改查操作数据，咱们第一步是不是把数据库绘画通过dependence依赖注入的方式注入到咱们的路由里面来。那咱们这个路由是不是就可以使用数据库绘画，然后增删改查操作数据了。

发言人   03:37

那增删改查，咱们就按讲课的顺序了。比如说查询是不是核心是select，当然是不是得exclude执行。它执行完之后是不是就可以基于这个ORM对象来提取结果了，是不是？好，那这是查询，那除了查询还能增加，增加的核心方法是爱的小罐是不是填一个ORM对像那怎么得到ORM对象呢？咱们用前边的模型类把它转一下是不是就可以了。

发言人   04:07

好，那增加完，咱们学的是更新。当然更新跟删除的思路非常像，是不是都是先查找，找到了就能更新或者是删除了，最后commit一提交是不是就OK？好，那更新的话其实就是找到之后把属性是不是重新赋值就行，等号重新赋值就可以了。好，那删除就更简单了，直接delete小括号里边把咱们找出来的这个对象给删掉，是不是就OK。

发言人   04:35

同学们这些是不是咱们经过2个小时学到的circle alchemy的常用知识了，大家感觉如何每个人都是独立的个体，那大家的感觉可能不同。有的同学感受可能还好，我能记住。那么我想对你说，你很棒，点赞，咱们一起向后学习项目课程，咱们一块加油。当然有的同学可能感觉妈呀知识有点多，我记不住。那么别怕，我想对你说，别担心，因为有我，咱们一起加油。项目课程里边还会重复使用这些知识，你肯定能行的这都是小意思。同学们坐稳了，咱们一起开始项目课程喽。