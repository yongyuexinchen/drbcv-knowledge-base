63-头条项目-安装和配置Redis客户端_原文

2026年07月21日 12:53

发言人   00:01

Hello, 各位同学，上一节咱们安装了redis的服务端了。那这一节咱们来安装配置基于python连接的redis客户端。有了客户端了，我们用客户端连接服务端，实现redis的操作。比如说像读取缓存了、写入了、删除了等等等等。

发言人   00:21

好，那我们一块儿来看一下，那我们分两步，一个是安装，是不是？一个就是配置。安装的话命令是PIP install redis。当然在这儿提示，我们安装的是redis，要使用它的异步支持。当然异步也不需要安装额外的包。因为在目前redis的官方关于python支持的这个客户端，它内置了异步的支持了，我们就直接安装redis。当然使用的时候，咱们注意要导入的是I异步的就OK了。

发言人   00:54

那我们就顺序往下来看看怎么导入，怎么去配置。首先导入导入异步的，redis点a think IO因为这名字比较长，我们可以起别名。好，那导入之后我们说要配置客户端，其实这里的配置客户端就是创建一个redis的连接对象。像后续的读取了、写入缓存了等等操作，都是基于这个redis的连接对象。

发言人   01:20

那我们来看看创建这个连接对象的细节。首先基于咱们导入的redis，调用类redis小括号来配置host注意服务器的地址。那再往下pot端口号6379。上一节我们是不是看过redis的端口号就是6379。好，那再往下DB默认的数据库编号，那这是啥意思？就是咱们redis它默认提供了16个逻辑数据库编号，0到15。但是在实际项目当中，我们通常就使用零就行，除非有极其特殊的这种隔离需求，所以一会儿咱们写代码的这个DB数据库编号，我们就写零了。

发言人   02:00

好，那最后加一个参数decode response。这个参数它的作用就是返回的数据要不要从字节流转换为字符串。为啥非得需要这个参数呢？是因为redis的底层它存的是字节数据，如果我们要不解码，它返回给我们的就是B开头的一大串字符，我们看不懂，不方便操作。所以我们添加这个参数置为true，意思就是把字节流转换为字符串。

发言人   02:31

那这样咱方便，那这个就是咱们redis安装跟配置的方式了，接下来咱们也来试一下。首先我们回到这个py top里边来，找到终端是不是先来安装的命令，这就极其简单了，因此到直接是redis敲回车，稍等一会儿来看。因此到安装成功，那安装完之后，下一步我们说干啥？配置是不是？那我问大家，写配置在工程化的角度往哪里写比较好？Config里面原来已经有一个DB config，是不是DB数据库的配置了，那我们就再来一个缓存的配置了，那我新建一个python file缓存CACHE cache，接下来CONF敲回车，那我们就导入创建连接对象，是不是就OK了？

发言人   03:24

Impart redis点儿，think IOE部的起个别名redis好，那我们下一步是不是就是创建redis的连接对象。因为我们说将来操作缓存是不是都要基于这个对象那我们创建这个对象，那怎么创建呢？对，基于导入的redis调用redis这个类，那这样的话是不是就是创建了连接对象了？那我给这个连接对象起个名字，因为我们说后续这个操作缓存是不是就依赖于这个对象了。我们起个名字，比如说我就叫redis下划线client CLIENT，是不是redis客户端的意思？

发言人   04:14

那接下来咱们写配置参数是不是就OK了。我们说这个配置参数是不是涉及到主机地址、端口号、数据库编号等等等等。其实我们现在通义灵码提示的是不是就是正确的，我不太不不采纳他，一会儿咱们自己写。

发言人   04:35

那接下来我们是不是要写host主机地址。那这个主机地址的话问大家是不是有可能是local host也有可能是IP或者将来可能是啊我们服务器的IP地址，这是不固定的。所以我打算怎么办？注意先有一个变量，把咱们服务器的这个地址给它存上。将来万一地址变了，直接改变量值是不是就OK了，那我就这样来了，直接叫redis REDIS，下划线来一个HOST。那我们的地址是啥？打个引号，我们目前就是本机，所以我就写local host了。HOST那我们的这个配置参数，这里host主机地址直接我就写上面的变量，redis host是不是就OK了？

发言人   05:26

那我逗号一下，那再往下是不是还有port端口号？我们说默认端口号是6379，对不对？那万一将来比如说有变化，那我们老规矩，存变量将来有变化，改变这个变量的值是不是就OK了？好，那我就叫redis pot 6379。那port参数的值就是我们上面的redis port是不是就OK了？

发言人   05:49

那逗号隔开再来一个啥？那个逻辑数据库的编号等于我们说默认就写0，是不是就OK了？当然它默认是0到15，是不是一共16个逻辑数据库？那万一将来需要极其特殊的这种隔离情况？我没有改编号，所以老规矩我还是把它存一个小变量。Redis DB默认咱们就用这个零了。好，那DB的值我就等于上面的变量，redis杠DB好，逗号再隔开，就是decode response等于一个true，我们要把自解解码为字符串。

发言人   06:28

好，那这个就是咱们创建redis连接对象的方式，其实就是配置方式。我们把这几个小参数稍微加点注释。首先host地址是不是？那就是redis服务器主机地址没问题。好，那接下来下一个part端口号。好，那再往下DBDB我们说是不是就是那个redis数据库的编号，那它是0到15，是不是一共16个，我们这就取零就可以了。

发言人   06:58

好，那再往下decode response它是啥意思？是否我说是否是否将字节数据解码为字符串，那我这儿为true就是要解码，对不对？把字节解码为字符串，咱们方便查看和操作。

发言人   07:19

好，那这个就是咱们创建redis连接对象，配置redis客户端的方式了。我们稍微回顾一下，首先我们这一节是安装配置redis客户端，对不对？安装的命令大家还记得吗？PIP install redis，我们用的是异步。当然不用担心是不是我们安装的是redis，它已经内置支持了这个异步了。我们要注意的就是使用的时候用异步导入redis，点a think IO起个别名下。

发言人   07:50

一步就基于导入的这个redis，调用类redis来创建连接对象。那我们说后续的写入缓存、读取、删除缓存等等等，是不是都是基于这个连接对象的？好，那有了连接对象了，我们要配置参数，写redis服务器的这个地址，端口号6379是不是，DB默认的这个逻辑数据库编号，我们就写零就OK。最后再加一个decode response，把字节解码为字符串，方便咱们操作和查看就OK了。

发言人   08:23

好，那我们现在是一块儿把redis客户端安装配置了。那接下来大家也把自己的项目redis来安装配置一下。另外配置完之后不要走开，下一节咱们来封装缓存操作，就是写入缓存、读取缓存、删除缓存等等等。下一节我们不见不散。