20-FastAPI进阶-ORM操作数据-查询数据_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，上一节咱们在路由当中注入了数据库绘画，尝试过了一个查询功能。那从这一节开始，咱们来使用ORM来操作数据了。那么提到操作数据，无非就是四大类，增删改查是不是？那咱肯定都要学，那咱们一个一个来先学查询，查询的关键点是select的小括号，那具体怎么用呢？来咱们看细节。首先看select小括号，小括号里边是不是填模型类。那这里的模型类就是将来要查询哪个表，这个表对应的模型类叫什么名字，填啥就行。

发言人   00:39

这个slack的小框是一个ORM语句，我们要基于数据库绘画去excute执行它。执行之后会返回一个ORM对象，我们的数据也就在这个对象里面了。当然咱们得来提取，可以提取所有，也能提取单条。那咱们分别来看一下，首先提取所有基于上一步的O2M对象，scale as点2就获取所有了。

发言人   01:08

好，那我们说能获取所有是不是也能获取单条？我列举了两个获取单条的写法，咱们分别来看一下。首先基于ORM对象scales点first first是不是第一个，那这是一个获取单条的写法那么另外一个写法就是get，这个get它是根据主键的值来获取单条数据。它的写法跟上边这两种稍微有点差异，它不需要基于ORM对象，而是基于DB这个数据库会话，直接点get来获取就行。

发言人   01:42

参数一写上模型类，那就是将来要查哪个表，然后逗号隔开放上主键的值就可以了。这个是咱们查询语句的写法。我这里列举了三个从ORM对象提取数据的方式，当然提前说提取数据还有其他的写法。那么我们在这里先尝试这三种，后续还会给同学们介绍其他的写法。因为查询在咱们开发当中很常见，是不是？

发言人   02:09

好，接下来言归正传，了解了数据库操作查询的写法了，咱是不是要写代码来试一下？我们回到拍项目里面来，在python里边我沿用了一个接口books。处理函数get books list是不是已经dependence注入了数据库绘画了，那接下来咱们直接查询是不是就可以了？好，接下来我把这个pass给干掉。

发言人   02:33

查询的话咱是不是把查所有查单条都来试一下。大家还记得吗？查询的核心语句是啊slack的小块是不是放上模型类。那么在咱们现有的代码结构当中，咱有的表是书籍，模型类的名字是book。所以咱这个查的话也写上book这个模型类的名字。是不是就OK了？

发言人   02:58

当然我们说它是一条ORM语句，对不对？这就一个select，比较简单。那我就直接excute提交它了，允许我剪切一下。好，那么await数据库操作是不是采用异步的方式？Await基于数据库绘画这个DB点儿execute来执行select小括号。

发言人   03:20

那我们说它是不是会返回一个ORM对象，那能返回咱给它存一下，比如说我就叫result，结果给它存上，那基于这个ORM对象是不是可以来提取数据了？好，那我就result点儿，scales点儿，是获取所有。那获取过来之后，我是不是要给它存一下，将来想回去，比如说我就叫book了。好，那return这个book给到客户端。那接下来咱们的一个获取所有图书的接口是不是封装完毕，那我们保存一下，咱的项目也是正在运行的状态，咱直接看结果是不是就可以了。那我们回到浏览器当中来，先刷新一下book这个接口有咱们直接try IT excute来执行。来往下来看数据库里边的数据是不是全都给咱查出来了，来看很多条是不是？那不瞒大家说，在大家不知道的情况下，我偷摸的插入了几本图书进来。

发言人   04:21

好，现在我们查到了所有数据了，那还没完，咱是不是还有单条的那回到咱们的pym里边来，我把这一句给它注释掉。那注释掉之后，这一句它是不是所有？那我打一个注释，它是获取所有，对不对？

发言人   04:41

好，那我们继续来，我是不是还有单条的？那单条的有一种写法是基于这个ORM对象点儿scales点儿first是不是可以的？好，那我们同样就还给它赋值到book里边。因为我们底下响应的是books，就省着咱们改代码了。

发言人   05:00

好，保存一下，回到咱们的浏览器里边来先刷新一下，还是基于刚刚的books这个接口，try eat一下，执行excute来往下来看，获取到了好几条数据，但first是不是提取到了第一条数据，那first成功了，那我们回到py top里边来，刚刚说是不是还有一个get？好，那大家回忆，我们说这个get他还需不需要slack啥的？是不是不需要？那我把上面这两句直接注释了，直接注释。

发言人   05:32

那这个get怎么用呢？是不是它是直接基于这个DB数据库绘画点get就OK了。那参数一放模型类，参数二是不是加主键？比如说我来一个主键是5，就是我那个ID是5，我有这个ID。好，那它是不是会返回一本书给我们，那我还就叫book，等号来赋值。因为我们底下是不是响应的是book。好，那接下来保存，回到咱们的浏览器里，接下来刷新一下，try IT excute来看有没有一本书，也有。那我们通过get获取单条数据是不是也成功了。

发言人   06:10

接下来咱们把刚刚这几句代码回顾一下，加一点小注释。首先select DB execute select的这一句他是不是做查询动作的？这个是查询。那咱们这里是不是会返回一个ORM对象？那基于ORM对象scales点2是获取所有的。Scales点first它是获取单条，当然这里它是获取第一个数据，是不是？好，接下来再有就是get，这个get的话它也是获取获取单条数据获取单条数据，但是这个get它是根据主键来获取。好，这个是咱们查询的基本写法了。

发言人   07:01

那我们一块练习完了，接下来是不是该同学们来动手了。大家跟我一样，先来得到一个ORM对象，基于它来获取所有获取第一个再试试get根据主键来获取单条数据。接下来大家来动手来尝试一下。尝试完之后可以把咱们的代码粘到评论区，我们在评论区里边对答案。