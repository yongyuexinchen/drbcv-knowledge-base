58-头条项目-添加浏览历史-思路分析_原文

2026年07月21日 12:53

发言人   00:01

Hello, 各位同学，从这一小节开始，咱们来到浏览历史模块了。浏览历史模块涉及到的核心功能有添加浏览历史记录。那什么时候添加呢？就是在用户访问这一条新闻的详情界面的时候，那么就会在浏览历史表里边把这条新闻添加进来。好，那加进来之后，当我们进入到个人中心浏览历史这个界面的时候，就会调用获取浏览历史列表这个接口。当然每一条新闻的右面有一个小叉子，一点就把这条浏览历史记录删除了。当然右上角还有一个清空，这个清空就是清空浏览历史列表。

发言人   00:42

看到这儿之后，大家有没有发现点啥？对，有点熟。咱们的浏览历史有添加浏览记录、删除浏览记录、获取浏览历史列表、清空浏览器历史列表。刚刚做完的收藏，添加收藏、取消收藏、获取收藏列表、清空收藏列表，这两个模块的功能是不是非常的类似。

发言人   01:13

我有一个大胆的想法，我不做你来做。当然大家也不需要担心，万一大家一时没有思路了，我准备好了配套无聊来帮助大家。那我准备的配套物料有示例代码，大家如果想不起来用哪些知识，什么思路了，可以参考示例代码。另外我还准备了讲义，讲义里边有每个接口详细的思路分析，我们可以根据这个思路分析来实现功能。除了讲义，我还有思路分析视频，我们一块儿分析思路。大家把它用代码实现出来是不是就OK了？另外我们知道因为咱们都是ORM的方式操作数据库，那像浏览历史对应的模型类，我已经提前准备好了。大家到配套物料的素材代码片段里边来查找，复制粘贴就可以。

发言人   02:11

我大胆一点喽，浏览历史我们一块儿来分析思路，代码的实现交给大家喽。接下来我们来分析第一个接口，就是添加浏览记录。思路分析我会带着大家分析接口规范文档，推导出来路由的写法，然后再带着大家来分析功能具体实现思路。

发言人   02:35

好，我们看接口的要求，接口地址是post。那么大家记得一旦看到post如果有参数是不是就得是请求体参数，是不是就得先定义python单体个模型类才能写请求体参数。好，那接下来请求方法post地址，我们直接写I的前缀API history，放到模块化路由的前缀里边是不是就OK了？

发言人   03:04

好，那么另外请求头需要认证，这个我们还用担心吗？不用了，是不是？我们已经定义好了这个认证请求头的方法，那还是dependence注入来使用，是不是就OK了？好，那接下来再往下来看，请求参数，参数是news ID当然我们配合着post请求方法，那是不是就是请求体参数就可以了？好，那接下来再往下看啊，响应结果，响应结果仍然是统一的格式code message data。那这个data是不是就是收藏相关的信息了，包含收藏的ID，收藏的用户ID、新闻ID，还有就是浏览的时间。

发言人   03:44

那看到了接口的要求了，我们往右来看路由，大家看看这个路由自己会写不，先导入API rotor，创建API rotor实例，写前缀，定义好分组名儿。那接下来写请求的路径，直接写I的是不是就OK了。定义路由处理函数，我们这里需要认证token，需要数据库绘画依赖性。

发言人   04:10

这个data大家帮我看看是啥，就是我们要求的这个请求体参数。但为什么我不叫news ID呢？因为它是一个请求体参数，它的类型是不是应该是一个python单提款类型。那news ID在哪里呢？是不是行参data点news ID就可以了？这个路由大家OK吗？我有信心，大家应该没问题。好，分析完路由了，接下来如果同学们担心自己忘，在这儿就可以暂停一下视频，先把基础路由给它搭出来了。

发言人   04:44

好，那假设我们基础路由已经搭完了，那我们接下来是不是就要分析一下该怎么添加浏览历史记录呢？该怎么实现呢？来注意来看，当我们进入请求的时候，是不是要认证token参数？我们说是请求体参数，那么进入请求之后，我们就要验证用户有没有登录过。他要是没登录，那我们就给他报错登录了，是不是才能往下来添加浏览记录？当然这个添加，注意我们要先检查一下这条新闻当前这个用户有没有浏览过。那如果他没有浏览过，那就直接把这条新闻加到浏览历史表，是不是就OK了，就是添加浏览历史记录。当然那如果他浏览过，更新一下浏览的时间就行了，把系统当前的时间更新到浏览时间，是不是就OK好。

发言人   05:45

那无论是走添加还是更新，最终是不是都要响应结果给到前端？当然按照以往的经验，咱们写完一个接口，是不是要拿着前端界面来访问这个接口来试一下了。但是在这温馨提示，咱们目前前端界面还没有地方能够测试有没有添加成功这条浏览记录。那怎么办？注意我们到pat tom当中打开database插件，找到history表来看看。

发言人   06:14

如果访问一条新闻，那来看一下能不能在浏览记录当中添加一条。那我们现在刚做到添加浏览记录，就先在数据库表里面去看有没有添加成功。等未来咱们做了这个浏览历史记录列表了，是不是浏览一条新闻就能到列表里面看到了。那我们目前先迂回一下。

发言人   06:37

好，这个是咱们添加浏览记录的实现思路。各位同学还OK吗？如果OK的话，大家就可以暂停来实现一下这个接口的功能喽。如果在实现的过程当中，大家遇到问题，我们可以在评论区里边讨论，或者是找到配套素材里边的示例代码，实现的思路等等等来辅助自己。我知道这是大家第一次独立开发接口，大家不要怕打球，会一直陪着你的。遇到问题我们在评论区见，好不好？大家加油。