路由和启动方式

路由和启动方式_原文

2026年07月22日 14:58

发言人   00:00

好，各位小伙伴们大家好，我是老椒。接下来我们开始进入到fast API的第二节。第二节我们主要讲fast API的路由以及请求参数的问题。

发言人   00:10

首先第一个fast API的路由，路由我们如果学过这个江歌的话应该知道，包括flask里面也有路由这个概念。路由其实就是一种映射关系。路由就是把客户端请求的UL地址与我们的视图函数进行绑定映射的一种关系。路由里面有三个非常重要的概念。第一个是请求路径，就是请求的URO地址，URO地址也称之为叫URL路径，是一个意思。你看我们上节课的时候回到代码中来，这个代码虽然是我是自动生成的，但是你要知道像这个引号引起的里面的内容，就是请求的路径或者请求地址。

发言人   00:49

斜线表示跟地址，跟地址其实就是IP地址加端口号后面的后面的一个斜线。其实斜线的话在很多浏览器中是默认是忽略了，是隐藏了的，不是忽略了。你比如说像在我们的浏览器中，默认就隐藏了。在这个地方你写先不要不写这样的能访问的，因为默认的这个斜线它会帮你隐藏。

发言人   01:10

好啊，然后接下来继续请求方法，解决方法就是我们的那个路由前面那个那个单词，就路由前面那个单词，你看就这个单词是get完之后get请求，好吧？我们前面讲过，一般来说有五个，一个是get，一个是post，还有一个是update，还有一个是put，还有一个delete，对吧？有这五个非常重要的请求方法，分别是增删改、查查两个查。增删改大家都知道，像这个post主要是做新增的好吧，布置了做修改的，update也是主要做修改的好吧。说错了，是一个叉叉的话是get，他一般不管是查单个还是查所有的都是get，那删除了就是滴滴，一共只有五个，不止五个，还有pad还有open等等。其实常用的是这五个，但是一共加起来了好像有九个还是11个。当然后面的不太重要，一般不怎么用，五个够了。因为我们我们的接口无非就是获取数据，获取数据其实包括数据的增加感加强，好吧，就是这个呃接下来还有一个是函数，函数就是这个啊就是DM后面定义的这个函数。

发言人   02:27

老师为什么前面加了一个ASIC，是因为我们说过fast API它是个义务的。之前我们在讲这个python的网络编程及并发里面，讲携程的时候说过，你要想定一个异步函数，那函数前面加一个ASYNC这就可以了，这个函数就是一个if函数，知道吧？那其实fast API的官网上也告诉我们了，最新版的fast AP的官网上告诉我们了。如果你不知道这个函数应该定成异步还是定义成同步的函数，你直接不用加这个ASONC就可以了。

发言人   03:00

因为你不加这个AASYNC，那你这个函数其实也是异步的，知道吧？OK, 你加上去那也没关系，你加上去的话，这就是一个异步任务，没错。那异步任务里面如果你想要做切换的话，里面需要用到await，之前我们讲携程的时候说过OK，还是那句话，如果你你这些携程掌握的不好，没关系，fast API里面帮会帮你处理，你直接正常的定义函数就行了。所以如果如果我们是一个在企业里面这个大大项目的研发的话，是90%的函数，直接按这样定义就行了好吧。有一些极个别的函数，考虑到这个函数的并发，考虑到这个函数未来会有大量的IO操作，那么才有可能去在这里面去加OH做切换。加OH做切换的话，那你前面必须有这个ASIC，所以大家只要知道，这就是一个函数就行了，好吧？

发言人   03:58

好，回到文档中来，也就是说什么是路由呢？路由就是把路径、请求方法以及函数三者把它结合在一起，形成一种映射关系好吧？那这种映射关系形成好之后，当浏览器发送一个请求，请求路径正好和我们这个路由中的请求路径一一对应相同。那么它就会找到当前这个函数，找到的当前这个函数的话，就会调自动调用你这个函数。所以你看刚刚我们给大家举了个第一个fast的API的程序里面，我们通过浏览器发送的请求，然后这个函数就自动被执行了。

发言人   04:38

好，接下来搞清楚了路由之后，接下来我们再给大家写一个，好吧？写一个之后，我会给大家讲讲这里头还有什么东西可以可以再添加进去的。好，我先对我们对我这个代码来做一下格式化，在这儿。再写一个，比如说我现在是第一个函数叫test，这个函数什么都没有，没什么其他作用，就是模拟一下，打一句话执行了开始好吧。

发言人   05:13

你想返回什么东西，那你直接return，一般来说返回的是一个字典，好吧？当然了你要返回一个整数行吗？行，返回一个字串也可以，返回一个对象也行。

发言人   05:23

其实很很灵活的，在fast的API中非常的灵活。你任意返回什么东西都可以，它会自动把你要返回的内容转换成阶层格式。如果你访问字典，那它也会把字典转换成阶层格式。如果你访问一个数字，那它直接访问一个数字，以此类推。如果你返回一个对象，它会把你这个对象变成一个阶层格式，这个中间转换过程是自动帮你转换的，知道吧？我们这里还是返回一个字典，这个叫我们叫MSG，叫OK。

发言人   06:02

对，好了，我就写，你看我就定义了一个函数，那这个函数上有加路由，没有加路由。路由是这样子的，是APP，这个APP前面的这个APP是什么？这个变量名字是什么就是什么。如果这个变量名字改了，那不好意思，你这里面也跟着改好吧。

发言人   06:19

APP点，假设我这个请求必须只能是POS的请求，那我就写POS的。假设我这个请求必须只能接受这个push请求，那我就写put，包括update delete其实都有好吧，delete看是吧？好，那我这里面我就写post，post这个请求里面第一个参数去接pass，pass就是请求的UR地址。假设我这个请求UR地址是斜线test的，表示什么？表示浏览器只要是发一个斜线test的请求，那么它就会找到这个函数，当然了你的请求方法必须也是POS的。OK好，那我们来试一下呗，现在我们把这个服务器停掉，再执行。再执行之后，我们回到我们的浏览器。

发言人   07:06

其实我刚刚我们我们我们有一个新的路由对吧，新路由里面的UL地址是斜线，我看我这个斜线test对吧，然后回车，回车之后发现出错了，出现个什么是是什么这个delta的错误。Data其实错误的信息是什么？Method not around函数的不允许掉好吧？这里面也有一个这样的method，not around, 就是函数不允许掉。那也就是说他找是找到我这个函数了，但是不让我们调。为啥？一定是这里面不匹配，一定是不匹配。

发言人   07:40

我们这个路由你看请求路径的，对不对？浏览器中是实现test的对吧？我们这个代码中也是实现test。这个路径是对的，请求URD是对的。但是请求函数不一，请求方法不一样。我们这请求方法规定是POS的请求。但是浏览器中你通过这个浏览器地址栏的回车只能是电子请求，没办法，是POS请求，那怎么办呢？

发言人   08:08

那我们在这个DOCS，我们在这个在哪里呢？我们在这个接口文档中去发请求就可以了，在这儿找到我们这个test。好，然后点击try IT out，点击excute它会自动发送post请求。你看这是表示发送一个post的请求，请求地址是这个地址，对吧？最后得到一个正常的访问相应结果，message OK。同时你还可以看到正常打印的这句话，是不是？你看三者每一个都得匹配，才能够正常调用我们这个函数，这就是我们的路由。

发言人   08:47

好，上节课我们其实还有一个支点没有给大家讲，就是什么呢？就是这个fast API的项目的启动问题，faas的API的项目启动了，它有三种启动的方式。好，我还是给大家打开一下，回到我们的第一节课的这个文档里面去。直接往下拉送了，就是启动我们的bus的API有3种。一种是通过拍top的，点击它的启动按钮来启动，那这种通过点击启动按钮去启动的话，只是在本地测试的时候，就是开发模式下在本地开发模式下你才会去销售的。就包括我们前面的课程中，前面这几节课，其实一直是点这个按钮来启动，好吧，那点这个按钮启动必须你这个排场，是是是专业版的排场，而且是2021版。2020年的四月份之后的应该也有。最好是2021以后的，那才会有这个fast的API的项目。

发言人   09:54

有了fast API的项目，然后你才能点击这个向右的绿色的箭头，才能启动我们的fast API的项目，好吧？好，这是第一种。这种是在开发模式下，生产模式下不会用它。那么生产模式用下面这两种知道吧？第一种是比较常，第二种是比较常用的，是通过这个UV chrome UV chrome这个命令来启动。好吧，那这个命令应该怎么敲了？来，我在这告诉大家，你看我现在采用第二种启动了。第二题的话敲mini，所以打开telemeter，记住，打开这个telemeter，打开telemeter之后，在这儿敲mini，敲什么命令了？UVUVUV chrome，UV chrome后面接my冒号APP。

发言人   10:44

好，给大家解释一下，这个may是什么？就是你的入口PR文件的名字，点PR你不用写，直接写这个mad就可以了。而且你这个main一定要是在项目目录下，在当前目录下，就是你敲命令的这个目录，你敲命令这个目录不就是在项目目录下吗？所以你这个卖点PY也必须在项目录下，在不在？在的，所以这个目录要选对。如果是项目下面的项项目下面的其他目录下，你还要进到那个子目录，然后再敲这个命令，知道吧？好，总而言之这个卖就是整个项目的入口PY文件的名字。好吧，一般情况下我们都取名叫卖，卖点PY这块就不用写APP就是这个fast的API这个变量的名字。

发言人   11:38

第四行这个代码我们不是创建了一个fast的API的变量吗？那它的变量名字我给它指名叫APP，那OK你这个地方也叫APP。如果这个地方第四行这个变量名字改了，这个mini也要跟着改，好吧？

发言人   11:53

好，然后接下来后面这个加不加无所谓，有一个杠杠reload好吧，这个加不加无所谓，不加也可以好吧。其实在生产过程中一般不加杠杠reload。看到没？刚刚reload是让服务器在更新代码后重新启动，好吧，什么意思呢？就是当你写更新完代码之后，它会自动帮你重启，知道吧？但是实际上在市场环境中不会加这个，如果你在本地测试的话，你可以加，没问题，我们就这样回车就行了。你看一样的也启动了，你看到这句话吗？Start server process，表示已经启动成功了，其他的正常访问我就不访问了，我只是告诉大家，这是第二种启动方式，知道吧？

发言人   12:41

好，我那我要停止怎么办？在光标光标在这一闪一闪的这个位置，确定要注意这个光标首先是在我在这个teleme里面，你接下来才可以停止。你比如说光标在这儿，那肯定不能停，好吧？好，光标在这儿一闪一闪的，然后按一下control c，它就可以停止了。Count c就你就把它当成命令就可以了，停止命令是一样的。

发言人   13:04

然后还有第三种启动方式，第三种启动方式是在这个卖点PY里面加定义一个函数，定一个卖函数，通过运行这个my函数去启动它的代码，你看我在这儿都有的。好，我给大家写一下。这是第三种，是备用的，知道吧？一般不用就是在这个入口点评完文件里面写一个卖函数入口函数。

发言人   13:29

这个入口函数里面干嘛呢？导入我们的手动，通过写代码方式手动启动这个UV crown，导入UVC run，执行银行代码，run这这行代码这行代码第一个参数是什么呢？是APPAPP的名字，第二个参数是host，host你写什么呢？一般的就是你看到它有默认值的耗死的它有默认值的这B你看house有默认值的，段号其实有默认值，看到没？默认是127.0001就是本地，端口是八千，所以其实我们不指定端口，它也可以的。所以到这儿就可以了，写到这儿就行了。

发言人   14:09

然后鼠标右击选择运行，这样也可以，这样子也可以运行。你看又运行了，然后访问的话是一样的，我们在在这儿刷新一下，看是不是正常可以访问的。这个接口文档正常可以访问的对吧？

发言人   14:26

好吧，这三种题的方式，你看看你喜欢用哪一种。用我个人的话，我觉得在我自己的电脑中，由于我有这个专业版，所以我在开发模式下我还是喜欢用这个。好吧？好，当然这三种其实你都要知道，好吧，都要会好了。

发言人   14:45

那关于陆游，关于路由以及三种启动方式，我就先讲到这儿。路由是什么意思？这个路由还没给大家讲完，不好意思，稍等一下路，我们回到这里录音。

发言人   14:58

我们还可以加很多东西的，比如说这里面还可以加个什么呢？Tax, tax一定要是个列表，这里写什么呢？点这是一个这是一个标签，是一个区分其他接口的标签，其他是是给接口分组的标签。有接口分组的标签，如果标签相同，大家都分到这一组知道吧？

发言人   15:27

好，还可以写什么呢？还可以写summer，那么就是叫什么呢？是文档总结，就是这个接口的一个总结概要。好吧？比如说我这个接口是说什么接口？是一个测试接口，测试的接口，测试的接口对吧？还可以继续写，还可以写description，那就是描述接口的详细描述，接口的详细不要说OK那还可以继续写，比如说还可以写一个叫response，description响应结果，响应数据的详细描述，就你这里面可以写中文，对吧？好，差不多，就这些。好，现在我们再启动一下，启动一下之后，我会给大家看一下我们接口文档，刷新一下走，你看这个地方就不一样了，你看给接口分组的标签在这显示的好吧？那这两个接口在default这一组，这个test的接口在给接口分组标签这一组，所以现在没有。

发言人   16:38

这个text其实就是给接口分组，因为接口特别多，你肯定是要分好一组的对吧？100个接口，这20个接口是一个一个模块的，另外20个接口是另外一个模块的，那我怎么分模块了或者分组呢？就使用这个tack来分组，那你现在可以看到那个萨姆就在这儿显示测试的接口，就是这个接口的概要，明白吗？然后把这个点进去，这就是接口的详细描述，在这儿会显示看到没？然后还有一个接口的响应，秒响应表的时候，在这会显示看到没看到。所以也就是说我们在这个路由里面加的那些属性的值，其实是为了丰富我们的这个接口文档的展示的，知道吧？它不影响我们的功能知道吧？好，这节课我们就正式到这儿了，谢谢大家，再见。