34-头条项目-获取新闻分类-模块化路由_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，上一节咱们准备好了数据库跟ORM工具了。那有了数据了，是不是就可以写功能操作这些数据了？我们把操作之后的结果响应给前端，前端拿到响应结果了，界面当中是不是就能展示对应的内容了。从这一小节儿开始，咱们就来逐步开发项目功能操作数据响应结果给前端。

发言人   00:23

我们从第一个模块开始，就是咱们的新闻模块。这个新闻模块是咱们整个项目当中最简单的一个模块。我们从简单的入手，逐步来提升难度。

发言人   00:34

好，新闻模块是咱们的第一个攻克目标了，那咱们来看看有哪些功能？其实这些功能咱们老早是不是就见过了，比如最上面是不是有新闻分类，我们可以获取到头条分类、社会分类、国内国际等等分类内容种。好，从分类往下看啊，我们眼睛看到的是不是就是好多条新闻的简介信息。这个叫新闻列表，这里展示的是新闻简介，我们如果想要看完整内容咋办？前端操作是不是就是点击一条新闻就能跳转页面到详细内容页面，那这个叫详情新闻详情。当然详情的底下还有一个相关推荐，这个相关推荐就是同类新闻。也就是说咱们新闻模块的核心功能就三个，新闻分类、新闻列表、新闻详情跟相关推荐。

发言人   01:31

那咱是不是得一个一个来，那我们就从最上先获取新闻的分类了。那我们马上要开发接口了，该怎么开发呢？这里大家要先明确接口的功能是啥，这里的接口不是某一个接口，是所有的接口。前端向咱接口发请求接口增删改查操作数据库里边的数据响应结果给前端是不是就OK了。好，那也就意味着咱的接口得有路由能操作数据库是不是能响应结果。

发言人   02:07

那具体的思路我给各位同学整理了一下，咱们来体会一下。首先咱得有路由，那做项目咱说是不是得是模块化路由，工程化思维开发是不是？好，至于模块化路由路径咋写？方法咋写？参数咋写，响应结果咋写？不同的接口是不是肯定是不一样的，那咱们得具体去分析。

发言人   02:31

好，有了路由了，那前端是不是能发请求了？发请求我们目的是操作数据库里边的数据，那我们就通过ORM的方式去操作。ORM核心是面向对象，那咱就得有啊模型类。至于这个模型类该定义成什么名字，表名是啥？字段名是啥？属性是啥类型，那我们是不是得参照数据库表，那一会儿咱们具体去分析。

发言人   02:57

好，有了模型类了，下一步是不是就能增删改查操作数据库里边的数据了。当然我们说做项目要有工程化思维。那么对于数据库增删改查操作，我们可以抽取封装一下，封装到CRUD文件夹里面。那咱们这里是获取新闻分类，是不是就是查询？那我们就要把查询的代码封装到CRUD里面。好，那封装好之后，是不是最终路由要调用这个方法？因为我们在路由里边是不是增删改查操作数据库里边的数据。所以我们在路由里面调用前面的增删改查的方法，然后响应结果是不是就可以了。

发言人   03:43

这个是咱们接口开发的大致思路，定义模块化路由器要有模型类，接下来封装CRUD方法，在路由当中去调用方法响应结果是不是就OK了。那这个接口的时流程基本上适用于所有接口，后续咱们逐步来体会。那么有了接口开发思路了，那下一步是不是参照着咱们的物料，把咱们这四步逐步来实现就可以了。首先第一步是不是模块化路由？我们说模块化路由参照啥来写？API接口规范文档是不是？那咱们就来分析一下咱这个新闻分类，来上接口新闻分类列表，请求方法是get地址API news character。那这个API news是不是可以定义为模块化路由的前缀？

发言人   04:38

好，那接口地址方法有了，下一步来看参数。Skip limit都是int类型，跳过的记录数默认0，limit是返回的记录数，限制就是每一页的条数，默认值100。那这两个参数是不是配合起来做分页的？好，那看完了参数了，再往下响应结果示例。响应结果code状态码200表示成功message super size是不是成功的意思？当然这message我们是不是可以换成，比如说获取新闻分类成功是不是也可以？好，那接下来重点看啊前端要显示的数据在data里面，中括号是不是咱这里是一个列表，因为新闻分类列表它里边是不是有很多分类，那我们用一个列表来整体管理一下。

发言人   05:27

这个是咱们前后端约定好的接口规范，我们是不是对照着这个规范写模块化路由器？但是这个路由大家还记得吗？我们讲模块化路由基础知识的时候，是不是写过一个简易版来拿出来？首先API router实例前缀是不是咱也写了，tag分组news是不是体现在交互式文档里边。好，那接下来艾特root是不是艾特的是API router这个实例，不再是艾特APP了。Get factory这get是不是咱们前边约定好的请求方法get。

发言人   06:05

好，那接下来处理函数，处理函数这里我们是不是带俩参数了？原本讲基础的模块化路由是不是没写这俩参数来着？一会儿咱们也给它补上。

发言人   06:14

好，skip int类型默认值0，limit int类型默认值100。好，那接下来return结果，这个return的结果就不能乱写了，是不是按照前后端约定的这个格式来写？只不过咱们这个data暂时还没有去查询数据库，所以是不是先写死为新闻分类列表。那未来查完了把这里替换是不是就OK了？

发言人   06:38

这个是咱们获取新闻分类列表的模块化路由的写法。咱们一步一步来，是不是把咱的模块化路由先给它补全一下，咱其实就差参数跟这个结果，是不是要细化一下。好，接下来咱们回到pat cham里边，把这个模块化路由来完善一下，来到pym里边来。首先咱们的模块化路由API router是不是已经有了？装饰器有了，接下来补全参数，参数是不是一个skip，什么类型来着？Int默认值咱们来一个零。

发言人   07:12

好，各位同学在这里先暂停一下，注意来看我的拍叉M今天跟前面是不是不一样，多了啥？他怎么这么贴心，怎么能帮我生成呢？而且来大家来看看这个图标是啥？通义灵码AI辅助插件是不是？

发言人   07:39

好，那到项目课了，我把我的通义灵码插件给它安装上且登录了。那写代码是不是有小助手了？来，我先把它tab一下，是不是他帮我生成了？好，那生成之后，大家跟着我的鼠标往右边来，大家也把自己的排插按摩通义灵码让它生效。但注意要登录一下，来看我这里是不是已经登录了。那大家用自己的阿里云账号来登录一下，就跟我一样，写代码就有小助手了。好，那我把我的这个侧边栏先给它关掉，我们现在暂时先用它的一个提示功能，后边如果出现bug啥的，我们再让他帮咱去调bug。

发言人   08:19

好，那思路拉回来，我这里是不是补全了两个参数。那一个是skip它的默认值是零，然后另一个limit默认值是100。好，那参数补齐了，我们说这个响应结果是不是稍微整理一下，按前后端约定好的来。那我们这里是不是得有一个code，我们要的是200逗号，是不是再来一个message MESSAGE。这里你写supersize，写获取新闻分类成功都可以。好，那接下来再逗号隔开还得有一个data，那么这个data我们暂时还没有数据，所以我就先写死为一个字符串，这个就是新闻分类列表。

发言人   09:08

那现在模块化路由咱是不是基本上已经完善好了，那项目也是正在运行的状态。接下来咱们就到这个浏览器当中来测试一下这个接口，那回到咱们的浏览器里边来，我们刷新一下，那找到character aries，点开try IT1下。参数默认值是不是直接填充了，我就不改了，直接提交来看一下。响应结果是不是OK拿到了。

发言人   09:35

那我们现在的第一步，模块化路由是不是就已经搞定了。那后续咱们是不是还要有模型类，还要有查询数据库，还要去调用查询的方法响应正式的新闻分类内容。那我们先一步一步来，这一小节咱们完成模块化路由就可以，我们把这一小节的核心知识来整理一下，其中我们刚刚在开发这个接口的时候，是不是给各位同学整理了一个接口实现流程。我们打一个注释，我们有一个接口的实现流程，那具体怎么做的，大家还有印象吗？

发言人   10:16

第一步咱是不是定义模块化路由？那这个模块化路由咱们参照啥来定义？API接口规范文档来定义。

发言人   10:29

好，有了模块化路由了，我们说下一步要干啥？是不是定义模型类？因为咱们都是ORM的方式来操作数据，所以我们要定义模型类模型类。那这个模型类参照啥去定义呢？那我们这里就参照数据库表结构去定义。当然真正开发当中，咱是不是有那个数据库的设计文档，数据库设计文档这个位置去定模型类就可以了。

发言人   11:01

好，那接下来第三步干什么？是不是在CRUD这个文件夹里面创建文件来封装操作数据库的，是不是方法就OK了。好，那接下来第四步，第四步咱们是不是得在路由处理函数当中调用CRUD里边的方法，我们是不是就能拿到结果，就能响应结果给前端了？

发言人   11:30

好，那接下来咱们第四步，在在路由处理函数里面。调用CRCRUD封装好的方法，然后响应结果是不是就OK？这个是咱们接口的实现流程，每一步该参照啥？是不是给各位同学列一下。这个实现流程基本上适用于所有接口。

发言人   11:58

咱们现在完成的是不是第一步？模块化路由参照API接口规范文档，是不是写了新闻分类的模块化路由器。我们一块写完了，接下来是不是该各位同学动手了，大家来动手把自己的新闻分类那个模块化路由来完善一下。完善好之后，大家把自己的代码粘到咱们的评论区，咱们核对答案。那下一小节我们就来往下从模型类开始，参照数据库表定义咱们的新闻分类这个模型类了。我们下一小节儿不见不散。