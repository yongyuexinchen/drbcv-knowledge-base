46-头条项目-全局异常处理器_原文

2026年07月21日 12:54

发言人   00:01

Hello, 各位同学，上一节我们封装了通用的成功响应格式了，能够让所有接口成功响应格式都是统一的那这一节我们来解决另一个同样重要的问题，就是统一的异常处理。那么截止到现在，大家回忆咱们做过了新闻模块，还有用户模块的注册功能。我们考虑到的异常情况都是比较简单的，涉及到的都是业务层面的异常。就比如查新闻不存在咱们报错，注册的时候用户名重复也报错。

发言人   00:31

但其实大家想想，开发项目的时候，难道只是业务层面的异常吗？不是的，它有可能会出现各种各样的异常情况。就比如说circle错了，外键关联失败了，数据库连接异常，提交事务失败等等等等。那这些错误情况，要命的是每个接口是不是都有可能产生，那我们咋办？在每个接口这边都写一遍try except来捕获异常吗？显然不合理，这不符合工程化是不是？那我们该怎么办呢？

发言人   01:09

封装全局异常处理器，其实就是异常处理函数。我们可以把业务层面的、数据库层面、系统层面的异常都定义成异常处理器。无论哪个接口出现了异常，都由咱们这套全局异常处理器来统一接管，返回统一格式的错误信息给到前端。那也就意味着啥？咱们有了这套全局的异常处理器，在路由里边考虑成功的业务逻辑就可以了。

发言人   01:40

异常的不用管了，只要发生异常，咱们这个全局异常处理器是不是马上接管？那大家说咱要不要异常处理器，那肯定是要好，是不是？那接下来咱们就一起来看一下，在fast API里边是怎么实现全局异常处理的。

发言人   02:00

注意想要实现全局异常处理器，我分成两个步骤去说。第一就是定义异常处理器，就是把我们可能项目当中遇到的异常情况定义成异常处理器，然后把异常处理器注册到全局。怎么注册到全局？其实就是注册到fast API实例上。那这样的话所有接口只要异常，是不是就都可以由这套异常处理器来接管了。这个就是咱们的全局异常处理器以及实现方式。

发言人   02:33

那接下来咱们来写代码来试一下，当然我提前说一下，这个异常处理器这个函数，每个项目涉及到的异常还是比较类似的。所以我准备好了一套代码片段，一套模板代码，咱们分析一下就直接用了。来回到我的素材里边来。

发言人   02:53

04异常处理器代码片段。我在这里是封装了业务层面的异常，数据库完整性层面的异常，以及circle alchemy数据库错误异常。还有就是最后一个兜底的，就是如果不符合以上这几种情况的，但凡是出现异常了都走出处理器。好，那这个代码允许我先复制，咱们到这个拍场当中再给大家去解释一下重点。

发言人   03:20

来，我来复制代码，那回到咱们的拍场景当中来，注意重点来了，我是不是要找地方来粘贴代码，找哪里粘贴呢？我们这个是一个通用的函数，通用的工具函数对不对？那就对了。You to来咱们新建一个python file，那起个名字，咱们这是成功以外的，那我就叫exception了，EXCEPTION exception。好，我直接control v粘贴一下。那接下来咱们返回到最上，把每个异常都稍微看一下，解释一下。

发言人   04:00

好，那上面的倒班就不说了，先看第十行，第八个mode等于true。这句的意思就是按开发模式，那项目开发过程当中上线之前置为true，好处就是我们可以返回详细的错误信息。那我们错误信息详细了，排bug是不是就效率更高了？我们也可以在开发过程当中把这个debug mode置为true，上线之后置为false错误信息，详细的咱自己看啊。

发言人   04:29

好，这是一个第八个mode。接下来往下第一个异常处理是业务层面的，HTTP exception。我们是不是也引入了一个HTTP exception这个异常类，那返回值是啥呢？注意Jason response格式，不是fast API自动转换的，我们用Jason response来转换。那涉及到的响应结果，比如说状态码message是不是就是错误信息，来自于HTTP exception里边的detail，捕获到物信息是啥，那我们就放到message这里，响应给前端是不是就OK了。好，再有data，因为我们都异常了，是不是就没有正常的数据给到前端去渲染了。所以data值置为那就OK。

发言人   05:13

好，这是第一个业务层面的异常。那接下来再往下，往下有数据库完整性约束错误。比如说唯一性，咱们项目是不是要求用户名唯一，那检测到不唯一了，重复了是不是就报错，外键关联失败。如果不符合以上这两点详细的错误信息，那我们就直接返回数据库约束冲突是不是就OK了。那接下来往下，如果是debug开发模式，是不是有了一个详细的错误数据，里边包含错误的类型、详细信息，错误的URL，那最终return返回值还是Jason n response格式，包含code message详细的错误信息。这里的data是不是就是给咱们开发人员看的，他返回的就是上面的详细的错误信息了，包含错误的类型，详细信息，还有就是哪个位置发生了错误，是不是好OK，这是第二个，就是咱们数据库完整性约束。

发言人   06:20

好，那如果不是这样，数据库方面明确的错误的，我们还有第三个第三个异常处理器异常函数处理的是circle alchemy数据库错误。那么这里用到的异常类似circle alc MI error。那来看如果debug mode是true，就是如果是开发模式，这个错误信息超详细来看错误的类型，错误的详细信息，而且把错误的详细信息这一句话是format格式化成字符串了，方便咱们去排错或者是打印日志。那么这个我稍微加一个注释，它是格式化异常信息为字符串，按方便日志记录跟调试。

发言人   07:04

好，那接下来pass就是哪里错误了，具体是哪个UL出现了异常。好，那接下来return response，涉及到的属性、状态码、详细信息data是不是就是享版的异常内容。好，这是第三个异常处理函数。那接下来再往下，如果上面这三个都是详细的具体的错误，对不对？那这个详细的我们用前三个异常处理函数来解决。当然我们说这个异常是不是五花八门的那在最后咱们来一个兜底的，就是除去前三个这个明确的异常的，但凡再发生异常，那是不是就走第四个异常处理函数就OK了。那异常信息跟上面的是一样的类型详细信息格式化成字符串，方便我们去记录和调试，最后就是打印出异常的路径是不是？最后通过Jason response格式来返回是不是就OK了。

发言人   08:10

这个是我给大家准备好的异常处理器，四个异常处理器，四大类，四大类异常。各位同学可以把这个代码留好啊，充其量到我们业务层面或者是数据库完整性约束的位置稍微改一改，是不是就OK。好，那我们这里就直接复制过来了。那相当于我们现在的第一步定义异常处理器，这个异常处理函数是不是就已经完成了。

发言人   08:36

那我们说下一步干啥？是不是注册？那我们想注册的时候，我是不是有四个异常处理函数，那是不是都要添加到fast API实例？那我想想，是不是就得fast API实例点调用方法，是不是来注册？但注册四遍是不是有点麻烦，所以我咋办？把这四个的注册封装一个函数，在从main里边来调用，只调用一次是不是就一次性注册了四个异常处理函数了，那这样我们更方便一点。

发言人   09:08

好，那接下来怎么办？允许我在UTS里边新建一个python file，这个python file是定义方法，来注册这四个异常处理函数。我把这个方法待会儿到main里边，一定要用。就OK了。那我起个名字，exception，EXCEPTION exception.

发言人   09:31

当然我不能直接叫exception，因为我前面的异常处理器函数这里是不是叫exception了。那这儿我来个集合，handler，DLERS handlers，那我在这儿想要干啥？就是DF比如说我来一个REJISTR register杠，exception SEPA，直接tab一下了。Exception handlers我传入一个参数是fast API实例，这个类型类类型我不要啊，直接把它干掉。那我在这儿干啥？就是来注册来注册。将来这个方法我到main里边一定要用，传入我真实的fans和API实例。是不是就相当于把我们的异常处理函数全都给它注册册进去了。

发言人   10:14

那我这么来，好，那接下来该怎么注册？注意这个通义灵码的代码，我们给它干掉了，这个不太对劲儿。好，那我怎么写？我用我的行参APP将来调用函数的时候，是不是得真的传范斯API实例？好，那接下来我就是APP点儿注意I的exception handler er这是我们注册异常处理器的方法，I的exception handler。那接下来这个方法，参数一，你注册的异常处理器类型是啥？异常处理器的名字是啥？这参数二就是那个函数名，那我们把它分别import导入一下。

发言人   10:57

HTTP exception是不是来自于fast API？咱们这个HTTP exception handler在哪里？我说一下在我们刚刚定义的exception点PY里面，大家来看我们的函数名是不是就是HTTP exception handler。那这个跟我通义灵码提示出来的是不是一样的？如果同学们的提示不一样，就直接，比如说在这边复制一下。

发言人   11:22

到handlers里边去粘贴一下，是不是就OK好，那我们把它import来导一下，导入的是youtube里面的，直接导入。那现在我们的第一个业务层的是不是搞定了？我在这儿加一个注释，咱们这个是业务层的报错，业务层面。

发言人   11:40

好，我敲个回车，还没完，是不是还有这个数据完整性的？好，那我来APP还是爱的exception handle，我直接tab那类型，这不对了，这个不对，我们把它干掉。注意数据完整性约束的是谁啊。来回到exception里边，我把这个第一个就折叠了，就这个单词，那我们直接复制这个回到咱们的handler里面，control v粘贴，逗号隔开，逗号隔开。那我们是不是把咱的方法名拿过来，那方法名就是在这里，arrow handle ler。

发言人   12:18

好，回到咱们的handlers里面，control v粘贴。这两个一个类是不是要import来把它导进来？当然我们这个异常类来自于circle acme，那我们的函数名import是不是来自于咱们的youtube里面？那这儿我们也加一个注释，这个是数据完整性约束，数据完整性约束。

发言人   12:42

当然不是完整性的详细错误信息了，是不是还有一个数据库层面的错误？好，那我还是直接tab一下，都是基于fast API实例，是不是I的exception hand，那错误的类型跟方法名先干掉。那我们去哪儿找？去咱们刚刚的exception里边儿，折叠第二个，现在来到第三个类型，是不是circle alchemy error control c一下回来到咱们的handlers里面，control v先粘贴类型，然后逗号隔开去找咱们的方法名SQL alchemy ero handler control c回到handlers里面control VA粘贴。那分别导包，首先类型import导入函数名导入UTS里面的那同理我们加一个注释，这是数据库层面的错误。

发言人   13:29

好，那接下来再往下还有最后一个，是不是就是那个兜底的，来我直接tab一下。I的exception handler方法没问题，类型exception是不是就是异常？方法名儿我们来检查一下，回到exception，找到第四个，control c一下，回来到handel's里面control加V粘贴好像是一样的，来我们import把它导入一下。那么这个是干啥的，兜底的就是默认的，兜底的默认那不符合上面三个的全都走。

发言人   14:03

最后一种情况，而且在这儿温馨提示一下，大家在注册的时候也按照这个顺序走。那就是业务的数据完整性数据库和最终默认兜底的这一个。其实在这儿注册的时候，咱们业内有一个小原则，就是子类在前，具体的是不是要在前面？父类在在后，子类在前，父类在后。好，那并且还有具体的在前面在前面，抽象的就是不具体的是不是在后面，在后面，这是我们在注册全局的异常处理器的时候，大致的一个顺序，大家知道一下。

发言人   14:46

好，那现在我们的register exception handler这个函数已经写好了。其实这是不是就是在注册全局异常处理器了，只不过这个方法我们没调用，是不是还没生效？那我们回到魅影全局的位置，把这个方法调用上是不是就生效了？来我control c一下，回到main里边来，我就基于APP这个fast API实例了，control v粘贴一下小括号，小括号传参是不是把咱的fast API实例APP给它传进去。那当然这个方法我们要import导入ut里面的，给它导进来。那我们这一句就是真的在注册异常处理器，是不是异常处理器异常处理器，那这个函数调用了，函数里边的功能才生效。

发言人   15:33

好，那现在我们定义注册了全局异常处理器，如果真的有异常，那就由他来接管喽。那我们来试一下好不好？来，我们回到浏览器里边来，把咱们的交互式文档我刷新一下，还是拿咱们的register注册，try IT1下。比如说咱们来一个这个用户名重复的，大邱，比如说444，咱是不是注册过，1234556来execute，来看这回的异常信息，是不是用户名已经存在data是now是不是就是咱们的全局异常处理器给它接管了？好，另外再有就是像circle错误，我们也能看见异常信息。

发言人   16:16

那这个我怎么给大家演示？大家注意我接下来要演示的就是出异常的情况，回到咱们的拍场当中来，比如说我们找到这个CRUD，CRUD是不是都是关于circle操作？那大家看啊我这儿是不是有get用户名？其实你任意找一句excute执行的是不是都应该是一句circle action语句。但接下来我一不小心把这个query，他本来是个变量名，对不对？我咔嚓给他打了个字符串，exclude执行啥？执行个字符串是不是肯定有问题？

发言人   16:50

那接下来咱们来试试，来回到咱们的浏览器当中来。这回我换个用户名，换个咱们没有注册过的，比如说五用户名密码是不是没问题，我去注册来走。妈呀。数据库是不是操作失败，想版的错误信息是不是全给到我们了。这个是我们拿着这个register注册接口是不是做了实验。但是我们说它叫全局异常处理器，是不是？那啥叫全局？所有接口只要出异常，他是不是都管？

发言人   17:26

咱们大胆一点儿，咱们把这个register测完了，咱们再试试这个新闻模块的好不好？比如说大家回忆我要查新闻是不是得按ID来查，那我们就整一个不存在的ID咱们来试一下。比如说我来个一万excuse来稍微瞅一眼，404新闻不存在data是不是we now了。那大家也可以动手，就是把咱们新闻模块里边的某个circle语句是不是给他改错，然后来看看详版的错误信息是不是也OK。那我这里就不演示了。当然大家千万别忘了，我刚刚那个注册接口那个circle是不是我给改错了，咱们是不是得给它改回来，要不待会儿出事儿了，来回到咱们的拍照P当中来，我刚刚把execute执行的这个circle本来是变量名，我是不是加了个字符串，我们把它control z一下，避免待会儿我们再写功能的时候出问题。那现在全局的异常处理器，咱们已经实现好了，那以后咱们在路由里面，注意在路由里面只考虑正常的成功的逻辑就OK了。异常的不用管了，是不是全都由这套全局的异常处理器来接管？

发言人   18:41

我们这里关于异常处理器，我们定义了四个异常处理函数，分别是业务层的、数据库完整性的，还有就是数据库层面的，还有就是一个默认兜底的那这个代码大家把它留存好，充其量比如说到这个数据库完整性，那么这种唯一性的约束等等位置，是不是可能需要咱们去微调一下，大家可以留好，后续项目当中也可以拿来复用。好，那有了这个异常处理函数了，咱是不是要注册，我们这里怎注册的？是不是定义了一个方法，把四个异常处理函数统一注册，再把这个方法到main当中一定要要用，是不是就OK了？这里有一个关键的函数，基于fans的API实例I的exception handler，参数一放异常类，参数二放异常处理函数名，是不是就OK了？那整个register exception handles这个函数其实就是在注册全局异常处理器了。只不过我们需要调用它，在哪里调用呢？Main点PY我们调用这个方法传入真实的fast API实例，是不是就OK了，全局异常处理器就生效了。

发言人   19:52

那截止到现在，咱们统一的成功响应格式封装好了，统一的异常处理也搞定了。那我们后端接口统一的对外协议成功和失败是不是全搞定了？那后续咱们就关注在业务层面来实现功能是不是就OK了。好，那接下来该各位同学来动手了，把咱们自己项目的异常处理器来统一封装注册一下。注册完异常处理器不要走开，下一小节我们继续向后来开发用户模块的功能，下一小节儿咱们不见不散。