19-FastAPI进阶-ORM-在路由中使用ORM_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，上一节咱们使用ORM创建了数据库表。那这一节咱们来学习一下在路由当中使用ORM。那问题来了，我们为啥要在路由当中用ORM呢？咱们拿着现实的例子来分析一下。比如说我有一个搜索功能，用户点搜索的时候是不是应该完成的是数据库查询的工作。那我要注册用户，是不是应该点注册按钮的时候完成数据库新增用户的功能，那这里点按钮完成某个功能，是不是就是点按钮的时候要向一个接口发请求，接口负责查询、新增等等工作是不是就可以了。大家回忆一下自己用过的别人的项目，是不是绝大多数情况都是这样，我们要通过一个接口来完成数据库增登山改查的操作。

发言人   00:54

那这么重要，咱也得来看一下。在路由当中使用ORM核心是依赖注入。我们要把数据库绘画注入到路由处理函数当中。路由处理函数有了这个数据库绘画了，就可以基于这个绘画进行数据库增删改查操作了。那具体该怎么做呢？来咱们详细看一下写法。

发言人   01:17

首先依赖注入，依赖注入是不是得先有一个依赖项，再dependence注入。那这个依赖项，咱们要获取数据库绘画，那想要获取数据库绘画，先要有绘画工厂。那怎么创建绘画工厂呢？来往下看啊，通过一个think session mark这个工厂函数，绑定数据库的引擎。那这个引擎咱是不是在上一小节儿就给它定义好了？好，那有了数据库引擎了，下一步绑定一个绘画类，这个绘画类是a think session异步的，那最后一个参数inspire on赋值为false，这个false的意思就是绘画对象不过期，不过期的好处就是不会重新查询数据库，那咱性能是不是会更好一些？

发言人   02:07

好，我们现在有了绘画工厂了，那么在依赖项里边就可以通过这个绘画工厂拿到数据库绘画了。那我们在依赖项里边，如果没有异常，就返回这个数据库绘画给路由处理函数。同时没有异常咱们是不是要提交事务？当然如果有异常回滚保证数据的一致性，那在最后咱们要关闭会话，避免连接的泄露。那这样的话，咱有了一个数据库绘画这样的一个依赖项了。那有了之后好办了。

发言人   02:40

下一步咱是不是在需要的路由处理函数里边dependence把它注入就可以了。那注入了数据库绘画是不是就可以进行一系列的增删改查操作了？好，在路由当中使用ORM的核心是不是有依赖项，注入到路由处理函数就可以了。

发言人   03:00

了解了写法了，接下来咱们也来试一下。我们回到市场当中来，先允许我打一个注释，就是明确一下咱们的需求。需求就是我要定义一个查询功能的接口，那这个接口咱们查查，大家抬头来看我们上一小节儿是不是定义了图书表。那咱查也就查图书就可以了。查询图书当然肯定是ORM的方式，对不对？那我们要在路由当中通过ORM的方式查询图书了。

发言人   03:34

我们的核心思想是是不是依赖注入的方式？那这个依赖注入该怎么做呢？是不是先创建依赖项，这个依赖项要获取数据库的绘画。那有了数据库的绘画了，下一步要干啥？是不是dependence？咱们把它注入注入到路由处理函数是不是就可以了？好，这个是咱们的需求跟实现的思路，各位同学清晰了吗？

发言人   04:04

好，清晰的话咱们要上手开始做了。首先咱们的依赖项是不是要获取数据库绘画？那这个数据库绘画咱们是不是得用工厂函数来创建？这个工厂函数咱们往上边先给它导包导进来，要基于circle alchemy EXT点a think IO来导入。那咱就直接逗号隔开，逗号隔开工厂函数是sink session mark。当然一会儿咱们还要指定一个绘画类，这个绘画类是a think session，咱就一块儿都把它导进来了。

发言人   04:38

好，那倒包之后，接下来咱们往底下走，是不是开始写代码？首先咱要创建异步绘画工厂，那是不是通过think session mark来创建？创建了绘画工厂了，咱给它保存一下。比如说我就叫a think NC，然后session SSSION session local，本地的是不是？我把这个local大写一下，咱们驼峰一下，好，等号赋值。那这样的话我的一个异步的绘画工厂是不是就有了？当然那小括号里边咱还没完，还得继续写。

发言人   05:16

我要办的绑定，绑定啥呢？数据库引擎，数据库引擎在哪儿找？往上走啊走，think engineer这是不是就是咱们的异步引擎？好，那接下来往下来，我们的think engineer给它拿过来，逗号一下，我们在这儿也加一个注释，这个是不是就是绑定数据库引擎？

发言人   05:41

好，那绑定了引擎了，是不是还要指定这个绘画类？那绘画类是glass，注意它带一个小下划线等号。那我们的绘画类是a think session，是不是？导进来了。

发言人   05:54

好，逗号哥看一下，打个注释，这个是指定绘画类，写错了绘画类好，教会车。最后还有一个expire on commit等于false，那我们说这里为啥写false来的？False打一个注释，它是提交后这个绘画它不过期，那不过去有啥好处？不会重新查询数据库，那性能是不是就会更好一些？

发言人   06:26

好，那现在咱们的异步绘画工厂是不是有了？有了之后，依赖项里边是不是就可以基于它来获取数据库绘画了？好，那接下来咱们开始依赖项了。依赖向大家来说，我来写，其实就是个函数，是不是？那我们异步一下，因为涉及到数据库的提交，回滚等等操作，咱们异步操作。好，DEF, 比如说我就叫get DD或者是get database是不是都可以？DATABASE小括号冒号一下。

发言人   07:00

那我们是不是要获取数据库绘画，那异步的方式，拿到咱们的这个位置一下。拿到咱们的think session local这个绘画工厂，那as比如说咱们就叫session了，绘画SESSION来冒号一下。好，那我们尝试执行，因为有可能出问题，是不是串一下。

发言人   07:23

好，那业务的返回谁呢？Session没有问题返回，然后await session是不是要提交，commit一下。好，那接下来如果有问题一下是不是就可以了。Except我们把这个异常类exception给它拿过来，如果有异常那咱怎么办？是不是要wait一下，await，那我要回滚了，session点儿roll back就可以了。好，那还要rise。因为rise的话咱也不涉及到抛出具体的什么异常信息，就直接rise空着了。

发言人   08:00

好，那最后finally一下就别管成功了还是失败了。那我问他，咱要把这个绘画给它关闭掉，要不然连接泄露，session点儿close一下。好，那我们加点注是啊在这里这个业务的session是不是它是一个返回啥呢？数据库绘画给这个路由处理函数，它是返回数据库绘画给谁呢？给路由处理函数。

发言人   08:29

好，那commit它是提交提交事务。提交事务当然没有问题，是不是再提交提交事务。好，那roback是不是就是有异常了，有异常那么回滚回滚，保证数据的一致性。最后close是关闭回话，关闭回话就可以了。

发言人   08:52

好，那现在各位同学，咱们这里是不是这个依赖项就已经有了，依赖项有了之后咱们可以干啥好了，注入到路由处理函数是不是？那咱是不是就得模拟一个路由出来？那这个路由提前说咱们模拟的功能就是查询图书，查询图书。好，那接下来咱们照常定义路由就可以了。那来个路由，大家来说我来写，艾特APP点儿，比如说获取图书，那我就get了。咱们来一个路径，比如说就是不可斜杠，不可思，有可能是多本书。好，当然当然温馨提示咱的表里边是不是空的，没有书。好，那没关系，大不了查出来的是空的呗，大不了咱们待会儿加一条呗，对不对？

发言人   09:39

好，接下来言归正传。A think DEF比如说就是get不可利斯特是吧？有可能是多本书，小括号冒号一下，小括号里边要干啥，dependence注入是不是好，那我就DB一下，DBDBDB1下冒号。比如说我跟一个类型，这个类型就是a think session，是不是异步绘画类型，等号赋值dependence DEPENDS，当然是不是得导入一下，稍等impart fast API的dependence小括号一下倒谁，是不是就是上边这个依赖项。好，那接下来get data base小括号要还是不要啊不要是不是删掉。

发言人   10:28

好，那现在咱们dependence依赖注入是不是完成了，那我们要看一下有没有注入成功，是不是？那怎么办呢？在这儿咱们提前用一下查询的功能。当然提前说查询的操作咱是不是没讲过一会儿会有一个专门的主题来讲查询。咱们在这儿先用那么一下下，就是为了看看这个dependence他有没有依赖注入成功。

发言人   10:55

好，那接下来我们来试一下，那怎么查询？首先我们要查询要调用select。这个select当然是circle alchemy的。来咱们把它导一下，找到circle alchemy的select，那select的小括号填上模型类。就是你本质上是不是要在某个表里边找数据，那你这个表对应的模型类是啥？直接写模型类就可以。

发言人   11:22

咱的模型类是不是就是book来往上翻一下，不可不可不可以？是不是在这里呢？啊，数据库表是不是通过这个模型类创建出来的？好，那接下来往下来这个是select book，就是查询book类创建出来的表里边的数据。

发言人   11:40

好，当然它是一句查询，那我是不是将来要给数据库来执行，那咋办？注意允许我先剪切一下，那么这一句select是不是要交给数据库去完成？那就是我们的数据库绘画对象，我们找到DB点儿excute来提交啥呢？查询操作，当然数据库的读取、写入，咱们要异步的方式来完成。

发言人   12:09

好，这一句只要提交了，它会返回一个结果。那这个结果比如说我就先用result把它存上，那这个result它存的是返回的ORM结果，因为这是不是就是ORM操作？好，这个ORM结果里边咱是不是要提取出来我们想要的，比如说是一本书，还是多本书，对不对？那result点儿scales，注意scales点儿。二是获取所有，查询所有。那他是不是会返回一个结果给我们？那这个结果其实它是输，对不对？

发言人   12:45

比如说我来一个变量book来收一下，那接下来我return响应，响应谁呢？不可，那现在咱们这个查询功能的接口就已经写完了。那在这儿再温馨提示一下，就这个查询的发，咱们别着急，后边会有专门的一个主题，咱们来学查询。现在大家知道就OK。

发言人   13:07

好，那接下来查询功能看起来写完了，咱是不是就要验证一下了。那接下来注意力我们的项目是正在运行的状态。那接下来咱们到浏览器当中向这个接口发请求来试一下。那我刷新一下，现在是不是多了一个接口。好，那接下来点开try IT，没有参数直接提交来注意来看有没有响应的结果。

发言人   13:31

有啊，注意有响应的结果，只不过这个结果是一个空的列表。为啥是空的？因为咱数据库里边是不是没有数据，所以查回来的是空的。那如果有数据，它真的能返回数据，那接下来咱们回到拍照B里边来，咱们来试一下，加一点数据来看看。

发言人   13:51

来打开咱们的插件database，双击展开book，把这个侧边栏关掉了。那我们点注意上边操作按钮，这儿有一个加号，按I的肉就是添加一行。比如说ID是一书名，咱们来一个，大家看啥书？比如说红楼梦，作者是谁？曹雪芹是不是价格，咱我随便填一个了，出版社就是黑马出版社。创建时间啥的，你就直接双击点进去，然后再把光标失焦拿出来，这个时间就有了。

发言人   14:29

好，那现在我是不是有一条数据，我要把它保存到数据库，来super meat提交一下。好，那接下来我的数据库里边有一条数据了，那回到咱们的浏览器重新刷新，咱们再去查询一次来看一下。注意同学们来看刚刚添加的这本书是不是查出来了。那能查出来，是不是说明咱们通过依赖注入的方式，把数据库绘画注入到咱们路由里边，是不是已经成功了？那这一节查询不是重点，来回到咱们的代码当中来，各位同学告诉我什么是重点，依赖注入数据库绘画对象是不是重点？能查询出来说明注入成功了。

发言人   15:13

好，那我们捋捋思路，刚才干了啥了？来思路还在这里。那我要模拟一个在路由当中使用ORM查询，是不是？那怎么做的？依赖注入那怎么依赖注入？先创建依赖项，依赖项里边是不是要获取数据库绘画？

发言人   15:32

Dependence注入，那我们是不是创建依赖项，但我要有数据库绘画，那怎么办呢？通过工厂函数a think session mark，那这个section mark我们加了注释了，绑定数据库引擎。这个数据库引擎往上找，往上找，你的数据库引擎叫什么名字就写啥，是不是就OK了。好，那再往下来，下一步是不是绑定这个绘画类，就是我们导进来的异步的session，a think session。好，再有一个参数就是提交后绘画它不过期，它不会重新查库。

发言人   16:09

好，那接下来有了这个绘画工厂了，那我们基于绘画工厂是不是就能获取数据库绘画了？能获取到数据库绘画，我们是不是没有问题，就把它返回给路由处理函数来提交事务。有问题那就回滚，最终别管有没有问题。那这个绘画是不是要关闭，避免连接泄漏。好，那依赖项都有了，下一步dependence注入来使用它是不是就OK了。这个就是咱们在路由当中使用ORM的方式。那接下来大家也来动手在路由处理函数当中dependence注入数据库绘画来试一下。

发言人   16:48

大家可以跟我一样，就是模拟一个查询的动作。那这个查询能查出来是不是说明注入就没问题，数据库绘画是不是也没问题？接下来大家来动手来试一下。试完之后，大家可以把查询的结果放到咱们的评论区，咱们在评论区不见不散。