56-头条项目-清空收藏列表_原文

2026年07月21日 12:53

发言人   00:01

Hello, 各位同学，这一节咱们一块儿来实现清空收藏列表这个接口的功能。咱们老规矩，先对照着API接口规范文档来分析基础路由，再来实现具体的清空收藏列表的功能。好，那我们先看接口的要求。首先接口地址这里请求方法是啊delete，因为咱们要清空，所以是啊删除delete。好，那请求的路径我们直接写科利尔是不是就OK了。那再往下看啊，请球头仍然需要认证，那也就是登录了可以清空自己的收藏列表。否则没登录那就不能清空了。

发言人   00:40

好，那再往下，我们看到直接就是响应示例了，是不是说不定没有请求参数，那我们看响应结果仍然是统一的格式，code message和data，data直接是啊now因为我们把所有的收藏数据是不是全都清空了，那data也就不需要返回啥，当然也返回不了啥了。只不过大家留意一下这个message，跟之前的接口不太一样，它是删除了某某条收藏记录，意思是不是就是删除了十条，这儿就写10，删除了100条，这儿是不是写100？好，因为我们message这里需要一个删除的数量，那我们一会儿是不是就得在清空的时候想办法得到清空的数量才行？好，这个是咱们的接口要求。这个接口对于咱们来说是不是so easy了？咱们大胆一点，这个路由咱们暂时先不写，分析一下怎么清空收藏列表，咱们就一块儿来写代码了。

发言人   01:46

好，那我们往下来分析清空收藏列表的实现思路。注意来看，当进入请求的时候，认证token就可以了，没有其他的参数。那认证token的方法，咱们是不是已经封通过了，看一下用户有没有登录，他要是没登录，咱是不是就报错提示登录了，是不是才能清空咱们当前这个用户的收藏列表？

发言人   02:10

那清空，直接调delete方法是不是就OK了，把当前用户这个收藏列表给它delete删除掉是不是就ok那删除完之后，我们是不是要响应结果？当然大家还记得吗？我们说响应的结果，是不是要一个删除的数量。那我们是不是就得在前一步清空的时候，delete的时候是不是得到这个数量，那怎么得到呢？我有办法，咱们基于excute执行这条delete语句，是不是会得到一个ORM对象。那么基于这个对象，调用row point就会得到命中的数量。我们就返回这个数量是不是就OK了？一个ORM对象点row point一下就OK。

发言人   02:57

好，那这个是咱们清空收藏列表的了，执行delete清空当前用户的收藏的新闻。在row point一下是不是拿到命中的数量响应给前端是不是就OK了。这个思路各位同学还行吗？那我们来试一下了，那回到咱们的拍摄当中来，我们先写路由，来我艾特ROUTER1下点儿，是不是来delete方法，那路径是不是就是可丽儿来着？接下来think DEF，比如说我就叫可利尔favorite，还不错，可利尔favorite。那user是不是认证用户token的？这个咱们需要DB数据库绘画依赖项，咱是不是肯定也得要着它？那接下来底下这个result调用CRUD方法这一行，我们先给干掉，因为咱还没有待会儿封装好了再说。好，那supersize暂时要着默认的清空成功。

发言人   03:56

好，那现在基础路由是不是就写完了，那下一步咱是不是封装那个清空的方法就可以了。那回到咱们的CRUD里面来，敲一点回车，我往上滚动一下，think一下DEF，我是不是要移除我所有收藏的这个新闻，那我就叫remove奥FA。这个名字不错，我tap他一下，那代码我直接给它干掉。来一个pass参数来看一下，DB要不要？数据库绘画依赖项那是肯定得要，要不然咋删，对不对？

发言人   04:36

好，那接下来再来看user ID问需要吗？不需要干掉了，需要，为什么需要？我在这儿打一个注释，清空收藏列表，清空的是啥呢？是不是当前用户的收藏列表？那怎么表示当前用户呢？User ID是不是就OK了？

发言人   05:01

假设我得到了一个用户ID是比如说一，那我是不是就到咱们那个收藏表里边找到用户ID唯一的那些收藏记录全给干掉，是不是就OK了？这是咱们的user ID，咱们需要因为要清空当前用户的收藏列表。好，OK搞定。

发言人   05:21

那现在参数完事儿了，代码pass干掉，咱们就可以写功能了。那我们调用ORM的delete方法，DELETE delete方法。当然删的时候是不是带一个条件，删的是当前用户的那当然delete怎么用呢？是不是就是我们得写上这个favorite，因为我们是不是要清空收藏表，favorite模型类搁在这儿，那where怎么写？看看favorite这张表里边哪个用户ID跟我们得到的参数user ID是一样的，找到这个当前用户的给他delete掉是不是就OK了。

发言人   06:02

好，这是一条语句，比如说我STMT给它存一下，接下来是不是开始excute执行它。那执行完切记不要忘commit commit commit提交一下，当然问大家，写完了没有？还没有，大家要注意，我是不是要返回一个删除的数量，那怎么办？那么就return result是不是咱们XQ的执行后的结果，点row point一下是不是就OK了。当然我在这儿，我还可以写一个二或者0，就是有具体的数量，你就返回这个数量，否则就直接返回0，是不是就OK了，这样我们可控。

发言人   06:43

好，那现在咱们的清空收藏列表这个小方法已经搞定了。那允许我把这儿截一个屏，截一个屏，钉在屏幕的右下角来。这个方法要干啥？路由当中调用一下是不是就可以了？

发言人   06:59

来回到路里面来，那我要调用，那我await一下favorite里面的remove all favorites DB和user ID是不是要传参好，那传好之后，这里是不是会拿到remove all favorites函数返回值，是不是就是咱们的数量？那在这儿，比如说我起个名字，叫叫啥呢？叫point或者叫result都可以。那拿到了这个数量了，接下来我们干啥，message这里给他用上，那一般是不是就得这么说，清空了多少条收藏记录，是不是清空了某某条记录。问大家，我想用一个变量怎么整？格式化呗对不对？F一下，然后这某某我就给它来个大括号，变成变量point是不是就OK了。

发言人   07:57

那接下来注意，我们的清空收藏列表功能看起来是不是实现完了，那接下来咱们来试一下，那我们control s保存回到浏览器里边来，注意，我刷新一下，现在我有两条收藏记录，清空一下试试啊不要眨眼清空。你确定要清空吗？确认没有了。好，接下来咱们检查一下，检查一下收藏表里边还有没有。来回到咱们的拍照目当中来，打开database插件，打开favorite，表是不是被清空了？表能清空说明咱们当前这个接口的功能是不是就OK搞定了。

发言人   08:44

搞定之后，咱们把这个功能稍微复习一下。那咱们的接口要求要求要一个清空的数量。所以所以回到咱们的CRUD方法里面来，我们delete删除当前用户的收藏记录，怎么得到数量呢？基于XQ的执行后的结果，得到了一个ORM对象。那我们基于这个对象row point一下是不是就能拿到命中的数量了。好，OK, 这个是咱们清空收藏列表这个接口的实现思路了。咱们一块儿实现完了，接下来各位同学也来动手，把自己的收藏列表清空功能来试一下。