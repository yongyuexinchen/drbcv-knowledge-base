48-头条项目-获取用户信息_原文

2026年07月21日 12:54

发言人   00:01

Hello, 各位同学，这一节咱们来实现用户登录之后获取个人信息这个接口。老规矩，咱们还是先基础路由再来分析实现思路。我们看一下这个API接口规范文档，分析一下这个路由的关键信息。

发言人   00:17

首先我问大家，这个路由请求方法是i get地址是i info。那只不过往下注意来看，请求头是不是需要咱们认证，这是咱们第一次做，那注意这是认证的啥。大家回忆我们说之所以能看个人信息，是不是他得是登录了，那怎么代表他登录了呢？对对对，咱们有token没过期，说明登录了是不是就可以看个人信息。否则就不能看，就得报错提示，对不对？至于这个请求头该怎么认证，咱们一会儿在讲解具体的实现思路的时候再跟大家说。

发言人   00:57

咱们先搞定这个基础路由。好，接下来我们继续往下看这个API接口规范文档。再往下是不是就是响应结果了？响应结果仍然是统一的格式，包含code message data，只不过data里边大家看啊只包含用户信息了。那我们这个响应的python单体模型类是不是就得换一换，换成user info对应的那个模型类是不是就OK了。

发言人   01:22

这个是我们从这个API接口规范文档里边获取到的路由关键信息。接下来咱就先把这个基础路由来搭建一下。来咱们回到拍场面当中来找到rooters，找到咱们的users用户模块。接下来我们敲个回车，大家帮我写这个路由该咋写。对，at root一下点儿是不是get请求方法，那我tap一下路径是不是也就是info好，那接下来路由处理函数，那我tap一下a think定义函数是get user info是不是就是获取用户的详细信息，那这参数我说一下，这儿我先把参数都给它干掉，一会儿需要啥咱们在思路里边具体来分析。

发言人   02:08

好，那接下来实现的代码我们也给它干掉了，干掉我只保留一个只保留一个return，是不是我们也是用抽取好的success response来返回统一的格式，是不是就OK了。Message我就写获取个人信息成功了，那这个data我先给它干掉，因为用户信息我是不是还没有？那我先把这个data给干掉。好，现在我们这个基础路由真的很基础，是不是？就已经写完了。那接下来我们到交互式文档里边来看一下这个路由能不能用，来回到咱们的交互式文档里边，我先刷新一下，来看现在是不是有了一个info。那我try IT1下execute来看，获取用户信息成功，那咱们这个基础路由现在是不是就搞定了。

发言人   02:55

接下来同学们重点就来了，我们来看看这个个人信息到底该怎么获取，思路是啥？跟着我一块儿回到PPT里边来，来回到PPT。大家这里始终要记着一点，用户登录之后是不是才能看个人信息？所以是不是需要认证一个请求头，这个是我们这一节的关键点。那接下来我们就要解决两个问题。第一，前端他大概是怎么携带的请求头。第二我们怎么获取，这是重点。

发言人   03:31

好，接下来咱们往下走，我们说当进入请求的时候，其实前端一旦发起请求，他会在请求头里边添加了这个token。它怎么添加的，那我说一下，这是HTTP的标准写法，authorization冒号bear写token。这个token是不是咱们后端创建的响应给前端的那前端携带好了，咱后端接收到请求是不就得获取这个投。因为我们后边得判断有没有token，过没过期，是不是好，那怎么获取？

发言人   04:06

注意这是一个新知识点。在咱们fast API里边有一个header的参数，它专门用来从请求头当中获取数据，咱们可以直接导入来使用。那我们将来就可以把咱的方法加一个参数。比如说我就叫authorization，是不是名字就对应起来，token就是string类型，等号赋值是从请求头当中来提取。当然这块儿咱们要起一个别名，因为前端存的时候，它是不是authorization a大写，那我们起一个别名a liars，还是a liars起别名是A大写的authorization。那我们就可以把前端设置的authorization bear token给它获取到了。

发言人   04:49

那我们获取到前端存的这个token了，那我们下一步要干啥？检查咱这个token的有效性。那这个有效性怎么查？无非就是我看看数据库里边有没有这个token，它过没过期。所以现在就两种情况了，一就是token有效，二就是token过期了或者没有token，是不是就是无效token？那如果是无效的，没有这个token或者是过期了，那我们就return一个难，证明没有，那我们是不是就可以解if rise抛异常了。

发言人   05:21

当然还有可能有一种情况就是有令牌而且没过期，是不是就说明用户暗示登录的状态？是登录的状态我们就可以干啥了，从数据库里边查找到这个用户，当然我们要获取数据库里边用户的最新信息，是不是查找到他。当然在这儿一涉及到查找就有可能有两种情况，大家说是啥嗄查到和没查到，对不对？假如说没查到，那我们就抛异常，如果查到了要干啥？

发言人   05:52

对，响应结果给前端是不是就OK了？查出来哪个用户，把这个用户的结果响应给前端就可以了，这个是咱们获取用户信息的思路。进入请求之后，我们获取到请求头当中的token检查是否有效。如果有效，我们就来查对应的用户响应结果。这个思路各位同学OK吗？应该没问题是不是？

发言人   06:15

好，但是在这儿提前跟大家说一下，大家还记得吗？获取个人信息是不是要走检查令牌有效性查用户。那我问大家，如果是修改个人信息，要不要检查令牌的有效性？要因为也得登录了，是不是才能修改个人信息，那也就意味着啥？咱们的注意检查有效性，查用户信息这个流程在获取用户信息，在修改用户信息，甚至是修改密码，是不是都需要用它？那大家能想到啥？抽取个工具函数，接下来哪个接口要用，咱直接来导入使用，是不是就OK了。

发言人   07:03

那具体该怎么做？就是关于检查令牌的有效性，查用户这个是不是还是数据库的CRUD。我们照常封装CRUD方法，封装好之后，我们把这个功能整合到一个工具函数里面。整合好之后，在路由处理函数当中直接导入使用整合好的工具函数是不是就OK了。那这个写法咱们稍微变换一下，这里是为了其他接口也能复用这个逻辑。好，那接下来咱们回到拍场当中来，实现一下来回到拍场面当中来，我在info这个上边打一个注释，就是我们现在要做的获取用户信息，我要干啥？是不是先要查token，查token，然后是不是获取用户，获取用户查token，查用户是不是？那我要把它干啥？

发言人   07:54

正常去封装CRUD方法，优点方法。只不过下一步我们把这个方法的功能功能整合成一个工具函数。整合成工具函数之后，哪个接口要用直接导入来使用，是不是就OK了。那这样的话，我们用户认证这个逻辑是不是就可以高度复用了。我再往下写一句，就是我们的路由导入使用是不是就OK了。

发言人   08:25

接下来咱们先来实现第一步，就是封装CRUD方法，是不是查token来查用户？好，接下来我们回到CRUD里面找到user这个模块，那我们往下来往下来准备封装方法。那我们封装的方法是啥？是不是就是根据token来查询用户。那想要根据token来查，是不是先验证token的有效性，然后干什么？如果有效是不是再查询用户，是不是就OK了？

发言人   08:58

好，来我们定义方法，think一下get user by token是不是没问题？这个方法OK，DB数据库绘画依赖项咱要着token string类型是不是也要着？因为咱们就是要根据token来查用户。好，那接下来再往下，首先我们是不是来一个select查询，根据user token这个模型类，那这个模型类大家还记得不，在models里边咱们当时是直接复制过来的。User token是不是有对应的ID外键，user ID token过期时间啥的。

发言人   09:33

好，那接下来回到咱们的CRUD里面，select user token where表里边的token等于形参token那语就是写完了，咱是不是得执行它。所以我来一个await DB execute，那执行之后是不是会有一个结果？所以我tap一下这个结果有有可能没有，所以我用scala one or none去提取。那我存到一个变量，这个变量我稍微改改，我叫DB token，是不是就是获取到的数据库里边的token，对不对？好，那这个token获取到之后，问能直接查用户吗？验证有没有token，过没过期是不是？

发言人   10:15

当然如果要是没有token或者是过期了，是不是就不要执行查询用户了，所以我先在这儿写一个if判断，if not DB token，DB同肯，那我冒号一下，是不是我return一个呢？Return难了，后续的比如说我再往下哒哒哒写查询用户的代码，是不是就不执行了？但是查询用户的代码我等会儿写，我这个token的if还没写完，我只没有还不行，我再加一个奥尔，或者是不是就是。

发言人   10:46

过期了，那这个过期该怎么判断？就这么来的。AADB token，DB杠token它有一个expires at是不是就是过期时间。那接下来我tape一下，小于系统当前时间，这是啥意思？就比如说数据库里边存的过期时间，假如说是2222年12月22号，但现在已经是23号了，那说明他是不是就已经过期了？那如果过期了是不是也return一个呢？那既然return了，后续的查询用户的逻辑是不是就不用执行了？好，当然如果没有进这个衣服，是不是就说明有token且没有过期，那就得把用户是不是给咱查出来。

发言人   11:35

好，那接下来我们往下是不是开始查用户了，那查用户是不是还是一个select查的表，是不是就得是user这个模型类对应的表了。好，那接下来where写查询条件，那这个条件怎么写？大家还记得吗？刚刚我们说这个模型类，这个user token里边，他是不是有一个外键user ID那我这个token都查出来了，点user ID是不是能得到用户ID那我们就按ID来查用户是不是就OK了。

发言人   12:04

来回来，那大家帮我写怎么写？按user模型类里边的ID如果等于DB token对应的user ID那接下来我们来一个QUERY这个语句，把它存起来。那下一步我们是不是就execute去执行它。但是执行的时候，我别直接return，我来一个RESULT result存一下。那我要return返回的是啥？基于ORM这个对象sky了one or难，为啥又是one r难呢？

发言人   12:42

因为有可能你这是成功查出来，用户是不是有可能查不出来所以我用scala one or nine去提取。好，那现在就是根据token查用户的方法，咱是不是已经封装完了。那接下来我们是不是把这个功能整合到一个个工具函数里面，方便后续的接口是不是去复用它。那接下来接下来允许我把这个参数的位置截个屏，待会儿方便咱们去传参。截屏稍微缩小一点点，放到右下角来。

发言人   13:15

那问大家，我要封装工具函数来整合这个功能，然后写if判断要是没有这个用户是不是就得rise抛异常。那么大家工具函数往哪里写？UTS是不是？现有的文件咱就不用了，因为咱们接下来做的是用户认证。那我右键新建一个python file，我起个名字就是奥斯，认证的意思。来我敲一个回车，那在这儿要干啥？其实它就是根据整合整合根据token TOKN token查询用户，最终返回用户的功能。

发言人   14:00

那我来写代码了，那我先我think一下，DF定一个方法。这个名字我还挺喜欢，容我tape一下，get current user是不是就是获取当前用户的意思？代码跟参数咱不用它，我先pass一下这个代码参数，我给它干掉，不对劲儿，来干掉。好，那干掉之后，我们大家来分析一下这个函数里边，待会儿是不是要调用我们刚刚封装好的CRUD get user by token，那接下来我们是不是需要DB，数据库绘画依赖项是不是还需要token？那DB直接dependence注入是不是就OK了？那头跟对对对，hider那个hider参数是不是？好，那接下来我写一下这个hider参数的话，我就根据那个HTTP标准写法来起名字了。

发言人   14:57

Authorization ation容我type一下，这单词我要着好，那后边死定类型没问题，这个DBDB是不是也没问题？那只不过值我们得改一下，改成啥？H大写HEADER小括号，那接下来点点点表示必填，我来import导入一下，导入fast API里边的header。稍微整理一下这个注释，咱们得贴着这个方法，要不然不太好来control v粘贴一下。好，那接下来我说是不是还得给它起个别名，因为咱们HTTTP的标准是不是authorization的A是大写，那我来一个alliance，那这个别名就是大写的authorization，容我换个行好不好？这样的话大家能看得清楚一点。

发言人   15:45

好，接下来再有DB，DB的类型，我们硬盘的把它导入dependence依赖注入导入fast API里边的get DB是不是从配置里面把它import导入好，导入好之后，现在问大家参数是不是搞定了？那搞定之后，我们本质在这里是不是就要调用底下我们截图的这个方法？那允许我导入一下，那就是f ROM咱们的CRUD，CRUD import user user。好，那我要调用方法，是不是就是users点儿get user by，别着急，是不是token，那我是不是要传入DB跟这个token了当当。

发言人   16:30

但现在有一个问题来了，DB能传了token呢？同学说那直接用authorization，不不不，还不行。大家还记得吗？前端的是咋写的，是不是什么bear啥开头的，所以这个标准写法，大家最好这么写，tape一下，最好怎么写？注意就是我把这个来个小变量名，token other ization我type一下，注意spelt以空。因为它是不是那个bear bear它是不是这么写的，允许我我这个往前缩进一下，好像不太好看是不是？好，他那个是不是这么写的，什么B大写的B，BEARER，然后空格是不是加上某某某这个token，它是不是它是不是这么写的？

发言人   17:25

好，那我就用split是不是把它分割一下，以空格分割，是不是这儿有个空格，那就会返回一个列表，列表的值一是bear值，二是token。那我们按下标一是不是取到我们真正要用的token，对不对？这是一种写法。好，那接下来我注释掉。

发言人   17:44

还有一种写法是啥？就是token，等号赋值还是authorization，是不是我们的形参？Replace一下，我tap一下，把bar空是不是替换成空字符串，其实就是是不是把前边儿这一坨给它干掉了，还是只有我们的这个token，所以这两种写法都OK，那我就后来者居上了，保留最后一个了。

发言人   18:10

好，那底下的这个get user by token，我们给它解锁注释，DB数据库绘画依赖项有了，然后接下来是不是把这个token给它传进去。那当然我是不是得我胃他一下，数据库操作await他一下那这块儿是不是他就有可能通过get user by token这个方法得到一个用户，或者是看是不是没得到。好，因为我们是scale 123提取的，那我来一个ur变量给它存一下。当然在这儿我们要考虑异常，就是如果没有用户是不是要抛异常了。所以我来一个if，如果not user我type一下rise HTTP exception导包，导fast API里边的，然后statues，导个包，把它import一下。好，那底还有我来一个无效的令牌或已经过期的令牌是不是就OK了。

发言人   19:07

好，那接下来rise的抛异常搞定了，我们还没完，重点是如果查到用户了，我们是不是要return这个用户，所以我return一个user，那也就是说如果查到了用户了，那就直接return这个用户。没查到就rise抛异是不就OK了。那么现在咱们这个整合的工具函数是不是也完成了？那我们下一步要干啥呢？是不是我们要在路由处理函数当中拿到这个user响应给用户？好，那接下来允许我把这个代码截一个屏，我稍微这么一截截屏，然后缩小，放到右下角来。那我们是不是回到reuters里面？

发言人   19:54

好，那问题来了，这里边儿怎么用呢？那我们第一反应是不是input的导入，然后在这里边调用。我说一下，其实不需要。我们还可以走一个dependence依赖注入，就是依赖注入是不是能拿到它的参数，且能拿到它的AI执行逻辑和返回值我们依赖注入一下就OK了，那怎么办？在这儿我先打一个注释，我们采用依赖注入的方式，那怎么写？注意我直接来一个形参，user等号赋值值是dependence，是不是依赖注入？那注入的是谁呢？是不是就是咱们刚刚的这个方法。

发言人   20:38

那大家要回忆这个方法在哪里？对，是不是在UTS的OS里面，那我们去上面把它导入一下，来for from youtube的OSS里面import是不是get current user？好，那接下来往下，dependence依赖注入的，偷个懒儿走，是不是就是get current user就OK了。那我把这个小空格，参数这儿的小空格删一下。好，那接下来注意注意我们就这么写，它其实就能拿到get current user的参数逻辑以及返回值，dependence依赖入就行。但是在这儿我最好还这么写一下，这个user我给个类型，它的类型是不是ORM类型。所以我来一个模型类user，来我们得把它import导入一下，import this name是models里面的，加了这个类型了，再把空格还得给还回来，OK搞定。

发言人   21:42

好，那现在关于咱们这个user就已经得到了。那接下来我们要干啥了，响应结果是不是就OK了？所以在success response这里，我们逗号隔开来写一个data，然后注意data的值是不是要提取。2M对象当中的值。所以基于某个python dental模型类是不是调用model validate传入我们的user，这里的user是不是就是dependence的get current user得到的。

发言人   22:15

好，那接下来这个模型类对不对呢？User info我们来检查一下好不好？大家还记得模型类在哪里吗？对，schemers里面，点开user大家来看，我们有一个user info response，他继承了user info base。是不是就是上面的这个可选的这些什么昵称、头像、性别简介等等，然后还附加了ID和username。我们用它对不对？它那可不能再用这个user us了，因为它里边是不是有token，认证的有token。好，我们用上面的user info，用user info对应的这个python单体模型类，来回到咱们的root or里边模型类OK没问题。

发言人   22:59

基于这个模型类提取ORM对象user当中的属性值赋给data响应给前端是不是就OK了。那么现在咱们这个info获取个人信息这个接口，各位同学就已经开发完成了。而且这个return响应结果已经拿到了。那我们是不是就可以来试一下了。

发言人   23:18

接下来回到咱们的浏览器里边，在这儿跟大家说，不能用交互式文档了，因为交互式文档它得不到IHTTP请求头当中的那个token。那咱们怎么办？直接用咱的前端项目来获取。那在这儿跟大家说一下，我们在哪里能拿到个人信息？注意就是我我先刷新一下，注意就是我的界面上边这里是不是有我登录的账号，点这个区域，点就能跳到个人信息这个界面，就看个人信息界面有没有内容，会不会弹窗报错就可以。

发言人   23:55

那我们点一下，注意来看是不是打开了大邱的个人信息这个界面。但是同学们可能说大邱我私下做实验，我好像直接进来一，他也能看见用户名ID这个人很懒啥的那这是咋回事儿呢？我说一下，就是因为我默认前端写了路由跳转，前端也写了默认值，所以你能看到。但大家如果不放心，你可以这样测试，回到咱们的拍照P当中，你把这个路由咱的user info这个接口给它注释掉，给它注释掉。那注释掉之后，大家再来看前端界面的反应。来回到浏览器，允许我刷新一下，注意来看干啥了，获取用户信息是不是失败了，为什么失败？因为咱是不是没有那个info这个接口，那你访问个人信息人家是不是就报错了？那接下来我点个返回，回来把这个info我给它取消注释。

发言人   24:52

有了接口了control s，回来到咱们的浏览器里边，我再刷新一次，点个人用户名，这里点是不是就不再报错了？那能正常拿到最新的信息，而且不报错，说明咱这个info这个接口是不是OK没问题。好，那这个接口的功能实现了，接下来咱们来总结提炼一下重点。我们先回到代码当中，把这个思路来整理一下。这个思路首先我们是不是验证用户是不是登录了，就是有没有token，过没过期。如果登录了，那是不是就查用户响应结果？那没登录那是不是就报错提示了。所以我们说关于查用户查token，我们要封装CRUD方法。

发言人   25:37

那这个方法所有的功能我们整合到一个工具函数，为啥要整合来着？因为后续的修改是不是也要来验证用户身份，所以我们整合一个工具函数是不是整合到off里面了。那接下来在路由当中采用依赖注入的方式，是不是来使用就OK。

发言人   25:54

至于这个CRUD，相信大家应该没问题。是不是？就是根据获取到的token，看看数据库里边有没有判断有没有过没过期。然后如果有没过期，我们就查用户是不是就OK了。好，这个功能其实就是在查用户了。

发言人   26:11

那接下来我们在OS里边是不是整合功能，我们用header是不是获取请求头当中的数据参数。好，接下来我们就把这个header里边的这个token，是不是给它获取到传入到get user by token，就是通过token获取用户的这个方法里面。如果有用户我们就返回，没有用户是不是red抛异常。

发言人   26:35

最后是我们原来没这样写过，在root or里边，我们没有是直接import导入，而是干啥了，依赖注入的方式dependence是不是把咱们整合好的那个验证用户的工具函数，是不是给他dependence注入了。那注入之后，我们是不是就能拿到它的参数，它的返回值了？那接下来响应结果是不是就OK了？

发言人   26:58

这个是咱们获取户个人信息这个接口的实现流程，获取了请求头当中的token，且抽取了一个工具函数来验证用户是不是登录了。接下来各位同学也来动手把自己的这个项目获取个人信这个接口来实现一下。实现完之后不要走开，下一小节咱们就开始修改个人信息喽。我们下一小节不见不散。