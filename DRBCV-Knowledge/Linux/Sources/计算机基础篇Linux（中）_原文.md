计算机基础篇Linux（中）

计算机基础篇Linux（中）_原文

2026年07月22日 14:53

发言人   00:00

OK同学们，接下来我们来讲date这些指令，这也是我们经常会用到的date指令。我们随便用一个项目给大家讲解一下这个指令它到底是干啥用的，其实在大部分的项目中，大家都一定会遇到这只料，这个后面跟着空格，然后clone再跟着一个网址。好，这条指令是什么意思呢？Date加特隆加网址。就相当于是把别人的项目，把别人的仓库直接车轮过来，直接拿过来搬过来，就相当于是我们复制这些项目复制到咱们的机子里。再通俗一点讲，就是把别人程序员写好的代码复制到我们机子里。就比如这些项目里有一条dt指令，你可以看到它也是date加筹论加一个网址，这些指令它的指令作用就是复制别人的项目到我们机子里来，我们可以执行一下，试一下。我们可以看到，这样就把别人的项目成功复制到我们的机子里了，对不对？我们可以看一下项目文件就已经过来了，我们可以用CD进去，这样就进入到项目里来了。

发言人   01:12

第一个指令讲完以后，我们来讲这个LFS这个指令。在执行这个LFS之前，你需要安装LFS工具，怎么安装呢？前面给大家讲过拥有速度，空格APP加上空格install再加上软件包的名字，也就是date杠LFS，这是软件包的名字。我们先安装一下，复制回车OK安装好了。这些工具它是干什么的呢？如果我们在克隆一些项目的时候，项目里有一些很大的文件，就比如说是一个1GB2GB的文件，那它是不太容易直接车拢过来的那这时候我们就需要用到date正LFS工具了，这里报错了，报错的话我们就先需要更新一下版本，因为我们如果不使用工具最新版本的话，它可能会报错，所以我们先更新一下，我们再来安装一下。好，这里多打了一个波浪号。好，工具安装好了。

发言人   02:21

D的空格LFS空格data，空格LFS空格炮指令，一般是用在我们拉取模型的文件的时候会用到它。就比如说这个love GPT项目里的模型，我们怎么把它成功的拉起不来呢？首先我们需要先安装LFS软件包，我们刚才已经安装好了，我们还要初始化一下它。这条指令你是一定要执行的，你要问他有什么含义的话，它就相当于帮你把这些工具准备好的意思。你可以看到它输出了这样一条信息，那它就证明成功了。接着我们就需要去拉取模型，复制这一条指令。我们可以看到拉取模型的时候，用的也是这个空格可动，然后再空格再加一个网址，因为模型下载的地址就是这里，我们直接把它复制过来回车。

发言人   03:21

好，开始拉取代码。但是他在拉取的时候，他只会拉取项目中的一些小文件，他不会把大文件拉进来。那我们怎么去拉大文件呢？我们先按control加C得退出来，按control加C的前提是这里出现了百分百，我们再进到刚才拉取项目目录底下，我们首先LS查看当前目录有哪些文件，这是我们刚才拉起不来的目录，我们把它复制一下CD然后加文件名进入到目录里面。

发言人   03:57

接下来就是我们用date LFS poll命令的时候，我们可以用这指令来拉取项目里未拉取的大文件。这样就会拉起大文件，我们可以稍微等待一下，这个文件太大了，我们就不等待了，我们直接尝试加C取消掉拉取。假如是没有取消的话，你想查看一下大文件完整性完不完整，那怎么办？那我们就用这杠LFSLS杠files这个指令，我们把它复制过来。它作用就是查看刚才拉取的大文件，它有没有拉取成功，验证文件下载的完整不完整，你可以看到这里出现了三条文件，文件的前缀都不一样。如果这些文件前缀是问号，证明文件它下载安装完了。如果是一个减号就证明文件它不完整。这条也是减号，下载的不完整。

发言人   04:55

如果下载不完整怎么办呢？啊不要急，我们就先返回到上一级目录CD空格加两个点给大家说一下。就是每当你执行的指令的时候，每一个单词和每个单词它之间需要有空格的。同学们，比如说CD要空格加两点date，然后空格掣动再空格项目是这样的，一定要坚持空格。同学们好，我们返回到上级目录以后，把这个目录直接给删除掉。先LS看一下这个目录的名字，因为这个太长了还记不住，把它复制一下RM杠R加目录的名字，就相当于直接删除这个目录底下所有文件，回车，这样我们就可以重新下载模型文件了。

发言人   05:38

那里给大家讲一下这窗大指令状态指令它是一个虚拟环境的一个工具。有些同学可能不太理解虚拟环境是什么意思，我给大家打一个比较形象的一个比喻。咱们如果要在家做饭的话，你首先得先进到厨房里，得进到对应环境来做饭，对不对？不能在你客厅做饭，你也不能在卧室里做饭，你也不能在楼道做饭，你必须在厨房里做饭。也就是说你要干什么活，你要做什么事，就要在你对应的房间里来干，在对应的环境下来看。比如说你上厕所，你只能在卫生间上，那你总不能在厨房上，对不对？而且别的房间可能没有对应的工具，这项目也是一样的，你如果想启动和执行项目，你必须先到这个对应的环境里来启动。

发言人   06:25

康大就是用来管理这个虚拟环境的工具。你用了这个康大工具以后，相当于你在你的屋子里又开了一个房间。然后你在房间里站着工作，不影响其他房间里的事情，这样一个意思，我们来用另外的服务器给大家讲讲这些康大工具。因为腾讯云它没有安装康大，我们用奥特DL创建服务器。

发言人   07:09

创建好之后，我们点进来点终端。首先我们要看一下它有没有康大用。这些指令可以看到他这输出了康纳的版本，那就证明他安装了康纳。那我们来给大家讲一下这三指令，DD指令创建虚拟环境。

发言人   07:28

还用刚才的比喻，相当于是你在你的屋子里就开了一个单独的房间。这个房间企业的名字，这个房间它是专门来干这些项目的，或者说干某些活儿的，我们来创建一个虚拟环境，用康大加create加斜两个斜杠name，name后面的真正参数就是你给这房间他起名字，你可以随意起，但是要用合法的字符，也就是字母这些不能用这些汉字儿，你还可以指定它的python版本，python版本什么意思呢？同学们，python很多同学听说过，有很多学没听说，对不对？Python它是一个编程工具，那python版本就是编程工具版本。就打比方，类比说到你这个屋子里的话，你可以把它类比成你这屋子里的环境，有可能它是毛坯房，对不对？有可能是精装房。这样就创建了一个超大虚拟环境，python版本指定的是3.10版本，然后名字叫做MYNV，也就是你在屋子里又开了一个屋子，这个屋子专门用来执行一个任务，这里就问你要不要继续输入yes就可以。

发言人   08:42

这样一个康大环境就创建好了，也就是在屋子里开辟的新房间，咱那咱们怎么激活呢？或者说怎么进入到这个房间里来呢？咱们首先要用这条指令初始化一下，让这房间他准备好，就把门先打开，那我们才能进去，对不对？OK初始化完成了，我们怎么进去呢？我们先把这个终端关掉，然后新建的终端可以看到，这里跟刚才是不一样的，出现一个base之前是root在前面，没有括号base。这是你想要进新房间的一个必要条件。

发言人   09:17

到目前步骤你还没有进入到你的屋子里，那我们怎么再进去呢？那我们就需要下一条指令，激活虚拟环境，强大加activate，加刚才创建房子名，也就是虚拟环境，可以看到激活后这里有一个变化。这就证明你成功进到这个房子里了也就是你可以在这个环境里干些事儿，它不会影响到别的房间，它是一个独立的房间，这就是窗大制定。

发言人   09:46

接下来是PIP指令，PIP指令他意思就是要用来安装一些环境依赖。有的同学可能会问了，环境依赖是什么意思？我给大家讲一下，还是用屋子举例。假如你现在在你屋子里用康大的工具成功开辟了一个新的房间，对不对？这房间你打算用它当厨房，但这个房间里什么都没有，没有锅碗瓢盆，也没有一些做饭必备的一些用具。那怎么办呢？我们是不是要上街来买这些锅碗瓢盆这些工具，这时候我们就要用到我们的PIP指令了，我们需要用PIP安装相应的依赖。

发言人   10:28

如果你想在这里执行你的项目的话，也就是必须先有这些锅碗瓢盆，也就是这些依赖它是必要条件。没有它你做不了饭，没有依赖那项目跑不了一个道理。同学们我们用PIP空格加install安装一些依赖，后面跟着的是依赖的名字。就比如有些项目要安装fair这些依赖，你可以把它理解为厨房里锅碗瓢盆必备的工具。你如果想启动这些项目，那你必须得用PIP install来安装一些必要的东西，这也是依赖的含义。

发言人   11:04

这是PIP指令，接下来是python指令，这什么意思呢？就是执行python脚本。同学我们来给大家写python脚本看一下，写脚本的时候也可以顺便复习一下刚才知识，用VI编辑器来创建一个python脚本，VI加一个当前目录下不存在的文件名，再加点PY，创建pad文件，在这里写入我们的python代码。写好之后我们按ESC输入英文的冒号，WQ保存，这样一个python脚本就成功创建出来了。而且我们写入了python代码，我们要来执行代码就需要用passion指令来用python加刚才的文件。

发言人   11:56

我们写的python脚本和python指令在我们项目中也会使用到，就比如说auto DL部署long chain杠check这些项目。教程中第四指令python加一个start up点PY，你可以把后面的杠A忽视掉。你可以看到它就是用passenger令加文件名来运行脚本，这是passenger令一般在启动项目或者是直接写脚本的时候就会用到python指令。如果你用python指令它执行不了的话，那你就需要用python 3加文件名来执行python脚本。这里就不演示了，同学们只需要在python后面加一个三，然后加文件名就行了。

发言人   12:42

其实这个环节大家很多同学可能就都搞不懂。不过没有关系，这些东西大家就是播一遍，就播一遍。因为咱们后面带大家跑线的时候，也会一步一步的来大家实操的。到时候你再配合的反复来观看这些，我教你这些指令，大家就能明白这每一个操作是什么意思。啊不要着急，同学们一点一点来。好，这节课的指令部分就先到这儿，咱们下节课还继续讲。指令总共分了三个环节，是从简单到难。这节课的难度属于中级的。好，同学们，我们下节课再见。