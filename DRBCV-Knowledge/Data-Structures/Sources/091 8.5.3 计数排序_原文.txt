091 8.5.3 计数排序_原文

各位同学大家好，在这个视频中我们要探讨技术排序这个算法，英文叫counting salt。技术排序在408考研大纲当中其实并没有要求我们掌握。但是在过去几年的真题当中，有那么一些真题我们需要使用到技术排序的这种思想和技巧。所以技术排序这个考点虽然在大纲当中大家找不到，但是我们还是会进行补充和学习，以免接下来出题老师来骗来偷袭。

首先我们会探讨技术排序的算法思想，紧接着我们会基于这种算法思想去实现这个算法，并且给出C语言代码。接下来我们会分析算法的性能，包括时间复杂度、空间复杂度，当然还有算法的稳定性。最后我会结合两道历年真题，展示技术排序在考试当中的应用。

好，首先我们用一个例子带大家感受计数排序的算法思想。好故事是这样的，假设在某小学某班级，英语老师给这个班的学生们做了一次英语测试。好，那么现在的问题是这样的，在这个班里有一个同学叫咸鱼，他这次英语考试考了16分。请问如果让你判断咸鱼他在这个班排名第几，那么你会用什么样的方法呢？直觉告诉我们，想要知道16分排在第几，我们只需要关注在所有的这些学生当中，小于等于16分的这个人总共有多少个，对吧？好，那么从上到下快速的扫一眼，小于等于16分的成绩只有卧龙凤雏。算上咸鱼它自身，所以小于等于16分的元素总共有三个，对吧？因此闲鱼的这个班级排名应该排在第三，当然这个是倒数第三。

好，讲到这里你已经体会到技术排序算法的核心思想了。刚才我们想要确定16这个元素应该排在第几个位置。我们的做法是快速的扫一遍统计，小于等于16的元素总共有几个。总共有1233个元素的值小于等于16，所以16这个元素肯定就排在第三个位置。

好，类似的道理如果我们想要知道，震南这个同学它83分排在第几位，那我们可以快速的扫一遍统计小于等于83的元素总共有几个。阿呆的分数是小于等于正南，他自己也是小于等于83。再往后是卧龙、咸鱼、凤雏。好，总共有12345，总共有五个元素小于等于83，因此正南它应该排在第五位对吧？好，那么我们观察右边这个排序的总体的结果，可以看到正难排在第五位。好，这就是基数排序的核心思想。

接下来我们用一个更严肃的例子来探讨技术排序的实现。先给出一个待排序的数组，我们观察一下这个数组当中的各个元素的值，其中出现的最小值是0，出现的最大值是4。好，由于整个待排序数组当中所有的元素值只有可能落在0到4这个区间。那接下来为了实现计数排序，我们设置一个辅助数组C这个辅助数组它的总长只有5，所以它的下标就落在了0到4这个区间。由于待排序数组当中只有可能出现0到4这个范围内的元素，所以接下来我们可以遍历这个待排序数组，统计各个元素值出现的次数。

具体来说就是这么做，先遍历A数组的第一个元素，它的值为二。好，那么出现了值为二的元素，那我们就可以对辅助数组的二号下标这个位置进行加一的操作。表示值为二的元素，目前总共出现了一个。好，继续往后处理下一个元素，下一个元素的值等于4。好，所以我们对辅助数组C的四号下标对应的这个位置进行加一的操作，说明四这个值出现了一次，继续往后下一个元素是3。那么对C三这个位置加1，说明三这个元素出现了一次。好，继续往后零这个位置下一个元素的值为零，所以对C0这个位置加1，说明值为零的元素出现了一次。

好，再往后下个元素的值为二，接下来要对C二这个位置再加1，说明二这个元素又出现了一次，对吧？好，那么再次加一之后，C二这个值就变成了2，说明值为二的元素总共出现了两次。好，类似的道理，再往后三这个元素又出现了一次，那么C3就变成了2，再往后零这个元素又出现了一次，那么C零这个位置再加1，最后这个元素值为三，那么C三这个位置再加1，最终C3的值来到了三好。我们再把C一这个值默认为零把它填上去。遍历完这个待排序数组之后，我们利用辅助数组统计出了零这个元素出现了两次，一这个元素出现了零次，2三四分别出现了231次。好，所以经过第一步处理C这个数组就变成了这个样子。好，接下来我们要基于这个中间结果，再次对辅助数组C进行下一步的处理。

最终我们要追求的效果是什么呢？我们希望用CI去表示小于等于I的元素总共有多少个。比如说C二这个位置，我们希望用这个位置的值去表示，在原始的待排序序列当中，值小于等于2的元素总共有多少个。我们只要统计出这个信息就可以知道值为二的元素应该排在第几位。对吧？这就跟之前给闲鱼排名的那个例子是同样的道理。

好好怎么做这一步的处理呢？很简单，基于之前得到的结果，我们知道值为零的元素总共有两个对吧？好，那么最终我们希望C零这个位置表示的是小于等于0的元素总共有多少个。显然C零这个位置肯定也等于2，对吧？小于等于0的元素只有两个。

好，接下来C一这个位置，我们希望用它表示小于等于一的元素总共有多少个。好，现在我们已经知道小于等于0的元素总共有两个，那再结合原先C一这个位置，我们知道值为一的元素有零个，对吧？所以小于等于一的元素总计应该有2加0，也就是二个。

好，继续往后，接下来C二这个位置，我们希望用它统计出小于等于2的元素总计有多少个。现在我们已经知道了小于等于一的元素总计有两个。之前我们知道值为二的元素总共有两个，对吧？所以值小于等于2的元素总计应该有2加2等于四个。好，后面三同样的道理要统计小于等于3的元素总共有多少个。我们现在已经知道了小于等于2的元素总共有四个。我们再观察值为三的元素有三个对吧？好，所以小于等于三的元素的总数应该等于4加3等于七个。

好，最后我们要统计小于等于4的元素总共有多少个。小于等于3的元素总共有七个，值为四的元素只有一个。所以小于等于4的元素总数应该是7加1等于八个。好，可以看到我们利用之前得到的这个中间结果再次进行处理，得到了这个统计数组。那经过第二步的处理之后，辅助数组的值就变成了22478。

注意，经过第一步处理和第二步处理，虽然我们都是用辅助数组C去表示，但是它们的含义已经发生了变化。左边这个中间结果CI这个元素表示的是在原始的这个待排序数组当中，元素值等于I的总共有多少个。而经过第二步处理之后，CI这个值表示的是在原始数组当中小于等于I的元素累计出现了多少个，没跟上的同学可以暂停消化一下。

好，接下来完成第一步和第二步的这个预处理之后，第三步我们就可以利用这个辅助数组去完成排序。好，接下来我们对排序的过程进行演示。A是待排序数组，C是我们经过两步处理得到的辅助数组。除此之外我们还需要一个数组B它的长度和A相同，B数组用于暂存A排序的结果。好，另一方面为了让大家观察到计数排序的稳定性，我会用不同的颜色去区分相同的几个值。

好，接下来我们对数组A中的元素进行排序。我们会从后往前依次处理各个元素。从后往前的这种处理可以保证技术排序是稳定的那一会儿大家会感受到它的工作原理。

好，既然是从后往前处理，那么第一个被处理的就应该是A七这个位置的元素。当前元素的值等于3。观察C三这个位置，C3等于七说明在原始的这个数组当中，值小于等于3的元素总计有七个。所以当前处理的这个元素三，它应该放在最终这个数组的第七个位置。由于数组下标是从零开始的那它的第七个位置就应该对应数组下标六对吧？好，所以我们对C3进行减一的操作。减一之后就确定了当前元素它应该放在哪一个数组下标处，应该放在B6这个位置。好，所以蓝色的三放在B6。

好，现在我们处理完了第一个元素，接下来第二个元素处理的逻辑是一样的。第二个元素的值等于0，那观察C0，首先我们对C0的值减1，这样就确定了当前元素它存储的这个数组下标的位置应该存在B一好，所以我们把黄色这个零放在B一的位置。再往后接下来这个元素我们观察C三这个位置它的值等于6，那说明当前扫描到的这个三应该放在目标数组的第六个位置。所以我们同样的先对C3减1，然后再把这个当前处理的元素放在B5，放到B5这个地方。好，后一个元素它的值等于2，观察C2，C2等于4，说明当前这个元素应该存储在B这个数组的第四个位置。那第四个位置对应的应该是B3好，所以我们先对C2进行减1，减一之后等于3。接下来减一之后的这个值就指向了当前处理的这个元素，它应该存储在哪一个数组下标处。好，所以灰色的这个二把它放在B3这个位置。

好，接下来的处理是一样的。我们加速后一个元素的值等于0，观察C0，先对C0进行减一的动作，减一之后的这个值指向了当前元素的最终存放位置，所以当前元素应该存储到B0。好，再往后下个元素的值等于3，观察C3，先对C3进行减一的动作，减一之后的这个值就指向了当前元素应该存储的最终位置。好，所以当前的这个元素应该存储在B4。截至目前我们已经把所有的值等于三的这几个元素都放到最终位置了。可以看到按照我们刚才设计的这个逻辑来进行处理，可以确保这个算法是稳定的对吧？白色的、红色的、蓝色的，这是原先的相对顺序。经过排序之后，白色的、红色的、蓝色的它们的相对顺序没有发生改变，说明计数排序是稳定的。

好，继续往后下一个元素的值等于4，观察C4，先对C4进行减一的操作，减一之后就确定了当前元素最终存储的位置，因此当前元素应该存储在B7这个地方。最后一个元素值为二，那观察C2，先对C2进行减1，减一之后就确定了当前元素它存储的最终位置，应该把它放在B2这个地方。好，那么我们从后往前处理了每一个待排序元素，每处理一个元素就会把这个元素放到它最终的位置。好，这样我们就完成了技术排序。

好，所以经过第三步的处理，我们的这几个数组就从最开始的这个样子变成了这个样子。B数组用于存储最终的排序结果，C数组在排序的过程当中，各个位置的值会减1。所以C数组在排序之后它的值也会发生改变。

好，我们回到刚开始这个地方。首先我们需要确定待排序数组当中，它的这些元素的取值范围应该落在零到K这个范围之内。像这个例子当中，它的元素取值范围就应该是0到5。接下来第一步处理，我们需要定义一个长度为K的辅助数组C辅助数组C的下标范围应该是零到K减1，刚好就可以对应上这个原始数组当中各个元素的值。接下来我们会遍历原始数组，统计出各个值出现的次数，然后再进行第二步的处理。经过第二步处理之后，CI的值表示的就是小于等于I的元素总共有多少个。

然后第三步我们利用之前得到的这个辅助数组进行排序。好，那么代码实现就是这个样子。我们传入数组A辅助数组B以及数组A数组B的这个长度，然后给出数组A的这个取值范围，应该是零到K取值范围K决定了这个辅助数组C它的长度应该定义为多少。好，接下来后面这个部分，这个for循环，我们对这个辅助数组C进行了初始化的赋值，把整个数组都赋为零。好，再往后一步就到了刚才提到的step 1。

步骤一我们会遍历待排序数组，也就是A这个数组，然后统计A这个数组当中每个关键字出现的次数。由于A这个数组当中每个关键字的取值范围只有可能是零到K减一对吧？所以任何一个关键字的值都可以让它对应到C这个辅助数组，所以这一个for循环实现的就是step 1第一步操作。接下来我们还需要对这个辅助数组进行下一步的处理。还记得我们怎么做的吗？经过上一步的处理，我们知道C0等于2，说明小于等于零的这个元素它总共肯定就是两对。所以C零这个位置我们是不需要去改变上一步得到的这个值的那对于C一的值，我们需要做的就是把当前这个C0的值和C一的值进行相加，对吧？那2加0等于2。

好，再往后一个是处理C2，要统计小于等于2的元素总共有多少个。我们只需要把目前已经知道的小于等于一的元素的个数加上值为二的元素个数，对吧？也就是说用目前的C1加上目前的C2，那C1加C2就是2加2等于4。

好，那回到代码可以看到我们是从C一那个值开始处理，C一的值等于原先的C1加上原先的C0。好，C一处理完了之后，I加加就会开始处理C2。那C2的值又等于原先的C2值加上C一的值，也就是刚才我们梳理的那个逻辑代码。基础不好的同学可以自己用手算的方式模拟一遍。好，总之经过这一步的处理之后，CI保存的就是小于或等于I的元素，总共有多少个？保存的就是这样的一个信息。

接下来就可以进入第三步，我们利用辅助数组C进行计数排序。在之前的演示当中，我们强调对各个待排序元素的处理是从后往前进行的。所以下面这一段for循环可以看到I的值是从N减一开始。

好，还记得我们是怎么处理AI这个元素的吗？我们会根据当前处理的这个元素的值去查找C这个辅助数组对应的位置。比如说当前处理的元素AI它的值等于3，那么我们就应该去查找CAI这个位置对吧？我们需要对C数组的值进行减一的操作，那我们可以这么写减1，减一之后又把它复回给c ai可以这么写对吧？当然如果简单一点你也可以写CAI减，这样也是OK的逻辑上是等价的。

好，那C这个数组的值减一之后，减一之后的这个值指向了当前处理的这个元素。Ai它应该存储在B数组的哪个下标处，对吧？所以进行这一步处理之后，下一步就应该是把AI这个元素放到数组B当中。那放到什么位置呢？应该放到数组B的CAI。应该放到这个位置对吧？

理解背后的过程，其实这个代码就没那么复杂。好，所以经过这样的两步处理，其实就处理完了AI这样的一个元素。那再往后是不是就可以让I减减，然后处理下一个元素了。好，回到这段代码，现在再来观察for循环里边的这两句代码，你应该知道是什么意思了。跨考的同学可以自己在手绘梳理一遍。

好，接下来我们对这段代码的复杂度进行分析，先探讨空间复杂度A这个数组是原始数据，为了实现计数排序，我们定义了一个输出数组B它的长度等于N所以B这个数组带来的空间复杂度就是ON好，另外我们还需要定义数组CC的长度取决于K这儿的K反映了原始数组A它里边的元素的一个取值范围。好，所以数组C带来的空间复杂度是OK好，所以综合来看数组B数组C它们都属于为了实现这个计数排序而定义的辅助数组。因此整个算法的空间复杂度就应该是ON加K好，再来看时间复杂度。

第一个for循环对C这个数组的各个元素进行了初始化的赋值。由于C数组总共有K个元素，所以这段代码它的复杂度是OK再往后需要遍历待排序数组，统计这个数组当中各个值出现的次数。由于A这个数组它的长度是N所以第二个for循环它的复杂度就是ON接下来第三个for循环需要对C这个辅助数组进行再次的处理。结合C数组的长度可以知道，第三个for循环时间复杂度OK好，最后一个for循环我们会从后往前去处理待排序数组A当中的各个元素，对每一个元素的处理只需要经过常数级的时间，总共有N个待排序元素。所以最后这个for循环它的时间复杂度也只有ON好，那么把所有的这些时间复杂度加起来之后，可以知道整个技术排序它的时间复杂度就应该是O的两倍N加K那这个常数我们可以把它去掉，所以综合来看，技术数排序的时间复杂度也等于O的N加K好，我们不难得出这样的推论。假如K的数量级和N的数量级相同的话，那么技术排序的时间复杂度就应该等于ONK和N同等数量级，那么前面这个O的N加K就等价于O的2N。而当我们表示这种数量级的时候，我们可以把常数项去掉。那把常数项去掉之后，这个时间复杂度不就回到ON了吗？

显然在这种条件下，计数排序它的时间效率要优于快速排序和堆排序这种N倍的log 2N要优于这种算法。而假如K的数量级大于等于N倍log 2N的话，那么技术排序的时间复杂度就会大于等于O的N加上N倍log 2N对吧？那一个低阶加上一个高阶，我们可以把低阶这个部分给去掉。这是我们在第一章学过的东西，加法原则，不记得的同学可以回去复习一下。好，所以如果K的数量级大于N倍log 2N那么技术排序的这个时间复杂度也会大于N倍log 2N那在这种情况下，技术排序的时间效率就不如快排和堆排序好。

所以上面这个推论反映出了什么情况下比较适合用技术排序去解决问题，关键取决于K它的数量级等于多少。假如这些待排序元素它的取值范围刚好就是零到N对吧？小于等于N如果落在这个区间的话，那么就说明K它的数量级是不是和N是相同的。此时我们就可以考虑能不能采用基数排序。

如果用技术排序，我们得到的时间效率会比快排堆排都要更高，当然我们也需要付出一些空间代价，因为我们需要定义一个辅助数组B和辅助数组C对吧？付出了空间的代价换取了更高的时间效率。所以我们说技术排序它是一种典型的空间换时间的这种算法思想，牺牲空间复杂度去换取更高效的这个时间复杂度。

好，所以说当我们在考试的时候，如果考题让我们去排序的这个数组，它的取值范围K比较小。所谓的比较小就是K和N同等数量级。那么在这种情况下，我们就可以考虑采用技术排序的思想。比如我们有两道真题来感受技术排序在考试当中的应用。13年的41题，他说有一个整数序列AA0一直到AN减1。其实它这的整数序列就是一个长度为N的数组。紧接着题目告诉你说整个数组当中的每一个元素，它的取值范围大于等于0，小于N好，然后说如果存在这样的一些元素，这些元素的值都相等，并且这些元素的总个数M大于二分之N满足这个条件的时候，我们称X是A的主元素。这句话有点绕，但他想表达的意思就是说，如果整个数组当中有超过一半以上的元素，它们的值都相等的话，那么这个值X就是数组A的主元素。

题目怕你看不懂，还给了你一个例子。比如说这儿有个数组，在这个数组当中五这个值就是它的主元素。因为整个数组当中出现了一个五、两个五、5554个5、五个五，总共有五个5对吧？整个数组的长度是8，其中的五个元素它们的值都等于5，所以五这个元素就是A这数组的主元素。后面这个例子，同样的数组长度为八，但是五这个元素总共有一个、两个、三个、四个，五这个元素的出现次数没有过半，所以在A这个数组当中就没有主元素，这是后一个例子。

好，然后题目让我们巴拉巴巴拉设计一个算法，要找到数组A当中的主元素。如果存在主元素，那么就输出这个元素，否则输出负一。你看我们需要统计A这个数组当中各个元素值出现的次数，对吧？并且题目告诉我们A这个数组当中各个元素的值肯定小于N好，那么这个题目我们是不是就可以利用技术排序的那种思想去解决。

我们可以定一个辅助数组，C辅助数组的长度是N恰巧所有的元素它的取值范围不会超过0到N减1。那我们是不是只需要把A数组给遍历一遍，一边遍历一边在C这个数组当中统计各个元素值出现的次数，然后最终看一下有没有哪个元素值它的总数是过半的那这样的话这个题目是不是就解决了？好，所以这个题目的解决我们只需要把计数排序step p也就是统计各个元素值出现的次数，只需要把这一步做完，基本上这个题目就可以解决了。具体的训练留给大家，当然这道题目并不只有计数排序一种解法，它有很多种解法。但是采用技术排序的这种思想，我们可以得到一个挺不错的分数。因为时间复杂度很优秀，可以达到ON这个数量级。

好，再看一个题，18年的41题说，给定一个含有N个整数的数组，设计一个在时间上尽可能高效的算法。它只要求在时间上尽可能高效。也就是说在空间复杂度上，它这个题目不会有任何的要求，对吧？一定程度上它是在暗示你这个题目可以采用空间换时间的这种思路。

好，继续往后读。他让我们找出这个数组当中没有出现的最小的正整数。比如说他给了一个例子，这儿有个数组，在这个数组当中没有出现的最小的正整数是一。我们看一下零不是正整数，对吧？所以未出现的最小正整数就是一好，后面又给了一个例子，在这个数组当中出现了1233个正整数。那在后面这个例子当中没有出现的最小的正整数就是4。好，后面就是让我们设计一个算法，去找到一个数组当中没有出现的最小正整数。

大家思考一下，这个题目可不可以采用基数排序的思想去解决，好像不行对吧？因为题目并没有给出每一个元素的取值范围。比如说它不像13年的这道题，13年的这道题它明确的告诉我们，就是每个元素的取值范围不大于N也就限制了这个K的数量级。但是在18年这个题目当中，它可能出现负数，可能出现正数。而且题目没有给任何的条件去限制这些元素它的取值范围。那是不是就用不了计数排序了？

好，非也。我们换一个角度来思考。题目让我们找出的是数组当中未出现的最小的正整数，整个数组当中只有N个元素。好，那我问你在长度为N的这个整型数组当中，有没有可能同时出现01234一直到N加1，有没有可能出现这么多个值？不可能对吧？因为它的长度只有N，所以在一到N加一这个范围内，这其中一定至少会有一个数是这个题目给的数组当中没有出现过的对吧？

好，接下来有思路了吗？我们假设题目给的这个数组它叫AN那我们可以定义一个辅助数组，叫做C这个辅助数组的长度，我们把它设的长一点，长度为N加2。那长度为N加2的话，这个辅助数组C它的下标范围就应该是0到N加一这个范围，对吧？好，接下来我们是不是可以遍历这个题目给的这个数组AN每遍历到一个数组元素的时候，比如说我们遍历到四这个值，那么我们就把C4的值加1。我们在数组A当中遍历到二这个值的时候，我们就把C二这个值加一好。那么把所有的数组元素都遍历完之后，C这个辅助数组是不是就回到了计数排序的那种逻辑？CI反映的是I这个值出现了几次，对吧？

好，那么进行完这种统计之后，我们最终要找到的是原始数组当中没有出现的最小正整数。那可以怎么做？是不是可以从C一这个元素开始依次的往后找。找到第一个CI等于零的地方。

比如说当C一等于0的时候，是不是说明值为一的元素在原数组当中出现了零次，对吧？那么一这个元素就是原数组当中没有出现的最小正整数。明白我意思吧？如果C一这个元素非零，那么就继续往后找呗，反正就从左往右找，找到辅助数组C当中第一个值为零的位置。那么这个位置所对应的这个下标，这个下标就是原数组当中没有出现的最小正整数。

好，所以18年的这个题目也可以利用计数排序的思想去解决。只不过这个题目对计数数组的这种应用还是比较灵活的。在考场上我们可能不太容易想到，训练的问题留给大家。

技术排序也不是这个题目的唯一解，只不过采用技术排序的这种方法可以得到很不错的分数。因为这个题目只是在时间上提出了这种要求，在空间复杂度上我们可以肆意妄为，对吧？而技术排序的这种思想，这种处理的方式，它的时间复杂度只需要ON这个数量级是非常优秀的。

好，通过13年和18年这两道题，希望可以带大家感受到基数排序的思想在考试当中的应用，抓住这个核心要点。如果我们发现题目给的这个待排序数组，或者我们要处理的这些数据，它的取值范围K比较小的话。那么采用技术排序的这种思想就可以获得很优秀的一个时间复杂度，也可以帮助我们在考试当中拿到更高的分数。好，在这个视频中我们介绍了计数排序这种算法。它虽然在大纲的要求之外，但是在考试当中偶尔还是会用到。

对于技术排序，我们首先要关注它的适用条件。首先待排序元素它的关键字必须是整数型，并且待排序元素的取值范围应该是零到K并且K的值要比较小，最好K和N是同等数量级。因为如果K的数量级大于N倍log n那么我们就不推荐使用技术排序。当取值范围K和ON是同等数量级的时候，技术排序的这个时间复杂度和空间复杂度都只有ON并且技术排序是稳定的那关于技术排序的算法实现，大家可以再结合这个思维导图以及课件再进行回顾，这儿就不再赘述。

最后还需要跟大家说明的是，我们在这个视频中给出的这段参考代码是改写自算法导论这一本经典教材。这段代码写的比较简洁，目的是便于大家理解。这段代码的风格比较适合初步的学习以及考试。但是在实践当中你可以对这个代码的功能进行一些拓展，让技术排序更具备通用性。我会给大家附上技术排序代码的这个PDF附件。

在实战当中如果要使用技术排序的话，我们其实不需要严格的约定。这个待排序数组当中的元素值是零到K减1，不需要做这样严格的约定，我们可以尝试着去找到数组当中的最大值和最小值，然后用辅助数组C的。比如说C零这个位置据统计最小值出现的次数，然后CK减一这个位置去统计最大值出现的次数。那C数组中间的一些元素就可以统计最小值和最大值之间的其他元素出现过多少次，对吧？好，所以只需要进行一些变通，就可以让这个技术排序它更具备通用性。

具体的代码实现，我会把这个PDF的附件发给大家，这段代码你可以直接copy到自己的本地去运行。这样的测试样例也给大家写好了。你看在这个测试样例当中，我给的这个待排序数组，它的值就没有限制在零到K这么死的一个范围，对吧？它可以出现负值，可以出现正值。反正我们的这个技术排序的代码可以灵活的去应对这种情况。有兴趣有能力的同学大家可以再看一下，我给大家的这段代码。

你要知道有时候考试和实战还是有一些区别的。在学习的时候，考试的时候怎么简单怎么来。但是在实战的时候，我们需要考虑到各种通用性、健壮性等等。好的，以上就是技术排序这个补充考点。