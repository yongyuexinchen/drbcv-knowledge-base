097 8.2.1+8.2.2 插入排序_原文

好的，这个小节中我们会学习第一个排序算法插入排序。这个算法的思想很简单，就是我们会不断的把一个一个的待排序的记录，按照关键字的大小插入到前面已经排好序的子序列当中，直到全部记录插入完成为止。好，我们结合动画来理解。刚开始我们会从第二个元素，从这个元素开始入手，我们会认为当前处理的这个元素之前的这些部分是已经排好序的。好，现在我们需要把当前的这个元素38和之前已经排好序的元素依次进行对比，比当前处理的元素38更大的，那些元素我们都需要将它依次后移。目前只有49这个元素，4十九大于38，所以把49后移，然后把38插入到49前面。

好，接下来要处理的是65这个元素，65比49更大。所以如果我们是按递增就是增序排列的话，那么我们并不需要把65放到49或者38之前。因此我们把65放回来。那接下来处理的是97，它比前边的这个65还要更大，所以97应该是放到它原有的这个位置。好，接下来处理的是76这个元素，在76之前的这些元素是已经排好序的那我们依次往前检查，97要大于76，所以76应该插入到97的前面，因此我们需要把97往右移一位。好，再往后要检查的是65，由于7十六大于65，所以它应该插入到65的后面，因此我们会把76插入到这个位置。

好，接下来要处理的是13这个元素，依次检查它前边的这些元素，97比13更大，又以76比13更大右移，65比13大右移，49更大也要右移，最后是38也比13大还是右移，好，现在再往前已经没有别的元素了，所以13应该是放入到零这个位置，也就是这样。好，接下来处理的是27这个元素。二十七之前的这些序列此时已经排好序，那处理的方式也是一样的。我们需要把27前边这些已经排好序的，并且关键字的值大于27的这些元素全部右移，给27挪出一个位置来，也就是这个样子。所以27应该插入到一这个位置。

好，最后要处理的是49这个元素。注意我们这儿的49画了一个下划线，是为了和三这个位置的49做1个区分。那么处理的方式和之前一样，我们要把之前的已经排好序的这些元素当中关键字的值大于49的全部右移一位，然后把49插入到四这个位置，也就是这样。注意刚才我们只是把比它更大的这些元素全部用一，那和它相等的这个元素是没有优异的那这么做可以保证算法的稳定性。好，所以插入排序的思想是很简单的那代码实现也不难。

我们假设这儿我们进行排序的就是一个简单的int型的数组，其中总共有N个数据元素。我们会定义一个变量I指向当前需要插入到前边那些序列的一个元素。好，接下来是这个if语句。只有当前处理的这个元素比它前边的这个元素更小的时候，我们才需要移动它前边的这个元素。所以我们会把当前I所指的元素这个值用一个中间变量temp把它保存下来。这是为了防止移动元素的时候把原有的这个数据给覆盖掉。

好，接下来就到了第二层的或循环，我们会从I的左边这个位置开始，依次往左检查它前边的这些元素是否要比38当前元素的值要更大。所有比它更大的元素我们都需要把它给右移。好，所以这个for循环导致的结果就是所有的大于38的元素都会右移一位。那每有一位元素都会J减，当J小于零的时候，这个循环就会停止。所以最后我们还需要把之前保存下来的这个temp值给它复制到J所指的右边这个位置，也就是把38复制到0。好，这是第一轮的for循环，接下来是不是可以让I的值加加，开始处理65这个元素，那后续的执行就不再分析，这个原理都很简单。

好，现在我们来看一下我们课本当中给出的另外一种直接插入排序的实现方式。它用了带哨兵的这种方法来实现。还记得哨兵吗？我们在顺序查找那个算法当中提到过什么是哨兵。这个地方的哨兵原理也是类似的。就是说我们实际的这些数据元素，我们是从一这个位置开始存储的，而零这个位置我们会空出来把它作为哨兵。

这种实现方式其实本质上的原理和之前的那种实现方式没有任何的区别。和之前一样，只有当前指向的元素比它前边的元素要更小的时候，我们才需要移动它前边的这些元素。和之前那种实现方式不一样的是，我们会把当前的处理的这个元素，把它复制到A零这个位置。而之前我们的实现方式是专门的定义了一个temp变量对吧？

好，接下来到第二层的for循环，和之前一样，我们会检查当前所指元素左边的所有的这些元素。如果说比当前前所指元素更大的那些，我们都会把它们往右移一位。所以刚开始处理的是49，49要比38更大，所以49往右移。好，接下来会J减减让J等于0。但是由于现在J所指的元素刚好是指到了A0，那么它和A零是相等的，所以这一层的for循环就可以顺利的跳出。

跳出for循环之后，我们还需要把A0所指的这个元素，把它复制到J加一这个位置，也就是变成这个样子。好，所以其实我们加了哨兵的好处在于，我们不需要再像之前那样，每轮循环都还要判断一遍J是否大于等于0。看一下之前的这种实现方式，这个for循环里边我们每一次J减之后，我们还需要判断一次J是否大于等于0。满足这个条件的时候，我们再来判断J所指的元素和我们当前处理的元素的大小关系，就需要进行两次判断。而加了哨兵之后，我们就不需要再进行这样的判断，所以可以让我们这个循环的执行效率更高一点点。但是这个提升其实很小。我个人是不太喜欢这种带哨兵的实现方式，因为我觉得之前的那种实现方式，逻辑上好像更清晰一些。

所以大家在实际考试当中，如果让你写插入排序的算法，那你可以根据自己的喜好，自己的习惯来，不一定按照我们课本上给出的这种方式来写。好，接下来我们来看一下这个插入排序的算法效率如何。空间复杂度不用说了，是O一这样的一个数量级。因为其实我们也就是需要定义IJ这样两个用于循环的变量。另外带哨兵的这种实现方式，我们会用A零这个位置把它空出来，作为一个算法执行的辅助空间。而之前的那种实现方式，其实我们也就是定义了一个temp变量。所有的这些辅助变量所所需要的空间都是常数级的，和问题规模N没有关系。所以这个算法的空间复杂度就是O一这个数量级。

接下来我们来看时间复杂度。在进行插入排序的时候，我们都是从第二个元素开始依次往后处理。每一个元素需要进行这样的一轮for循环。所以总体来看我们总共需要处理N减一个元素，也就是需要进行N减一烫的处理。每一趟的处理都需要进行关键字的对比，也就是这个if判断也有可能需要进行元素的移动。我们的时间复杂度主要的开销其实就是来自于这两个部分。

先来看一下最好的情况，如果说我们原始的这个表当中，各个元素的排列本身就是已经有序的那这种情况下我们每一趟的处理都只需要对比一次关键字，而不需要移动元数。好，所以总共N减一烫的处理，那么我们就只需要进行N减一次的关键字对比。因此总体来看我们最好的时间复杂度应该是ON这个数量级。好，这是最好的情况。原本这些元素就是有序排放的。

接下来看最坏的情况，如果说原本的这些元素它们是逆序排放，在这种情况下我们每一趟的处理都需要把当前元素和之前的所有的元素都进行一次关键字的对比。并且把之前已经排好序的元素全部都往后移动一次。比如说当我们进行到最后一趟排序的时候，我们要处理的是十这个关键字，那我们需要把当前所处理的关键字移动到A零这个位置。接下来这个for循环当中，我们还需要把当前的这个元素和前边已经排好序的所有的元素都进行一次关键字的对比。并且把这些元素都依次的后移一位。好，总之整体来看对比关键字的次数，还要移动的次数，我们把这些值进行一个加和，肯定都能够达到ON的平方这样的一个数量级。

好，这是最坏的情况。原本这些元素都是逆序存放的那这种直接插入排序算法的平均时间复杂度也会达到ON的平方这样的一个数量级。我们的课本当中是把最好时间复杂度和最坏时间复杂度加和然后除以2，用这样的方式来确定了一个平均时间复杂度。上一个小节我们说过，当我们在关注一个排序算法的时间和空间复杂度的时候，我们也需要再关注这个算法的稳定性如何。直接插入排序显然是一个稳定的排序算法。

好，现在我们对刚才的这种实现方案进行一个优化。之前当我们在处理某一个元素的时候，我们都是用顺序查找的方式依次往前找，找到它应该插入的位置。但是由于当前处理的元素，它之前这些元素其实已经是有序的，并且在我们这个例子当中是用顺序存储的方式来存储的。所以不难想到我们可以用折半查找的方式来更快的找到当前处理元素应该插入的位置。

好，来看一下怎么做。首先我们在A零这个位置把当前处理的元素给保存下来，防止它被覆盖。好，接下来我们会在当前这个元素前边的这个区域内，尝试着找到它应该插入的位置。好，接下来我们会判断low加high除以二这个位置的元素和我们当前处理的元素大小关系如何。在这个例子当中，50要小于55，所以55这个元素应该插入到50这个元素右边的区间内。因此我们接下来会在它右边的区间内进行查找，也就是让low等于幂的加一好。

接下来检查的元素泌的应该是low加high除以2，也就是70这个元素。那70比55更大，所以55如果要插入的话，应该是插入到70的左边。因此接下来我们会在70左边的区间内进行查找，也就是让I等于M的减一好，接下来mid等于low加high除以2，要检查的是60这个元素，60要比55更大，所以55要插入的位置应该是在60的左边。因此接下来我们会让I等于M的减一好。

到了这一步，我们的low已经大于high了，那这个时候我们就应该停止折半查找此时low所指的位置，右边的这些元素都是要比当前处理的元素55更大的。所以接下来我们应该把这个区间内的元素全部都右移一位，给55腾出一个位置，也就是这个样子。好，最后我们再把之前保存下来的这个值给它复制到low所指的位置。这样的话我们就完成了55这个元素的插入。

好，接下来处理下一个元素，要处理的是60，我们要在low和high之间寻找60需要插入的位置。第一个被检查的元素泌的应该是50，50要比60更小，所以60插入的位置应该是在50的右边区间内。所以我们会让low等于密的加一好，接下来密的等于low加high除以2，所以检查的是60这个元素。

好，现在问题发生了，我们当前处理的元素60和这儿已经有的元素六十它的值是相等的那按照我们之前设计的折半查找规则来看，当我们找到一个和目标关键字相同的关键字的时候，我们就会停止这个折半查找。但是在这个地方我们为了保证插入排序的稳定性，当我们发现一个和当前处理的元素值相同的这种元素的时候，我们还应该往这个元素的右半区间内继续查找，来确定当前元素的插入位置。所以到这一步我们并不会让这个折半查找停止，我们还会继续在密的所指的这个位置右边的区间内，来尝试着查找60这个元素应该插入的位置。所以接下来我们会继续让low等于幂的加一好，继续密的等于low加high除以2。那当前密的所指的元素70要比60要更大，所以我们会让high等于幂的减一好。到这一步low大于high，那我们就可以停止折半查找。和之前的处理一样，我们需要把low到I减一这个区间内的元素全部往右移一格，给当前的这个元素腾出位置，所以执行的结果就是这样的那这样我们就完成了60这个元素的插入。

好，接下来要处理的是90这个元素，同样的我们会在前边这个区间内尝试着找到当前元素应该插入的位置。具体这个折半查找的过程我们就不再一步一步的分析。按照我们之前提出的这种处理逻辑，最终会导致的结果是low，这个指针会指向high的，右边这个折半查找同样是会停在low大于嗨的位置。不过由于此时low大于I减1，所以在这种情况下我们并不需要移动任何一个元素。

好，这是对90这个元素的处理。最后一个要处理的元素是10，和之前的处理逻辑一样，我们会尝试在它前边这个区间内找到它应该插入的位置。经过一系列的处理之后，最终这个折半查找会停在high指向0，low指向一这样的一个状态。同样的也是low大于high，在这个时候停止折半查找。

处理逻辑和之前一样，我们需要把low和I减一之内的这些元素全部都右移一位，也就是这个样子。最后再把A零这儿保存下来的副本给它复制到low所指的位置。好，总之当我们在确定当前元素应该插入的位置的时候，由于前边的序列都已经是有序的状态，所以我们可以用折半查找的方式来更快的确定应该插入的位置。最终折半查找都会因为low大于high而停止。我们要做的就是把low和I减一之内的元素全部优于一位。

课本里面的代码的写法是high加一到I减一这个区间内的元素全部优一。因为最终high肯定是在low左边相邻的这个位置，所以high加一和low其实是一样的意思。另外我们为了保证这个算法的稳定性，当我们找到和当前处理的元素关键字的值相同的元素的时候，我们还应该继续在密的所指的位置右边来寻找插入的位置。

就像刚才60这个例子一样，缕清楚之前的过程，这个代码的原理就很简单了。这是我们课本当中给出的代码。这个while循环就是实现了我们刚才说的折半查找的过程，需要注意这个else条件。这个else条件里边已经包含了我们刚才说到的，保证算法稳定性的一个逻辑。如果幂的所指的元素和当前我们要插入的那个元素值相同的话，那我们应该继续在密的所指位置右边来寻找插入的位置。所以我们会让low等于mid加1，这个地方是和我们之前学习的折半查找稍微有一点点区别，大家需要注意的地方。

好，另外这个for循环实现的就是把所有的这些元素都统一的依次的右移一位。这种折半插入排序比我们之前提出的直接插入排序相比，其实就是我们比较关键字的次数会减少一些。但是最终我们移动元素的次数没有发生什么改变。因此整体来看折半插入排序它的时间复杂度依然是ON的平方这样的一个数量级，并没有什么质飞跃。

好，最后我们再来看一下插入排序算法，这种思想其实也可以用于对于链表的排序。我们之前的讨论一直都是基于顺序表，对吧？也正是因为基于顺序表，我们才可能实现这种折半查找。如果这些数据元素采用链表存储的话，那显然折半插入排序就是实现不了的那对于链表的插入排序，具体的代码怎么实现，这个留给大家自己去练习。

和顺序表的插入排序不太一样的是，如果采用链表存储的话，当我们要移动一个元素的时候，其实只需要修改几个指针就可以了，并不需要像顺序表那样把很多元素依次的右移。所以这种情况下移动元素的次数会变少。不过由于我们只能顺序的来依次比较各个关键字，用这样的方式来确定插入的位置。所以关键字的比较次数其实依然会保持ON的平方这样的一个数量级。因此整体来看是时间复杂度还是ON的平方。总之只要我们采用的是插入排序这样的一种实现方式，那最终我们的时间复杂度肯定都会达到ON的平方这样的一个数量级。

不过需要注意的是，如果说我们给出的原始的那个元素序列，它本来就是有序的，或者说基本上接近有序。那在这种情况下，其实插入排序也可以得到一个很不错的执行效率、执行效果。插入排序的思想和实现其实都是比较简单的，唯一大家可能需要注意的就是如果我们采用折半查找的方式来确定插入位置的话，那这个折半查找的过程需要特别的注意一下。注意这样的两个问题。如果当前密的所指的元素和我们当前处理的那个元素值相等的话，那我们应该怎么处理才能保证稳定性，这是第一点要注意的。第二点应该注意的就是我们最终折半查找算法停止之后，我们应该在什么位置是插入当前处理的这个元素。把这两个小细节注意到了，那折半插入排序的实现也会变得很简单。好的，以上就是这个小节的全部内容。