如何调用单体agent以及如何使用

如何调用单体agent以及如何使用_原文

2026年07月22日 14:57

发言人   00:00

好，今天给大家带来如何使用必胜这个项目，然后创建自己的单体agent以及具体如何使用的一个教程。那我们就直接开始，你可以按照之前教程进行登录就可以，我们用管理员账号进行登录。

发言人   00:29

登录进来之后，我们新建一个模板。

发言人   00:52

在进到画布开始设计之前，我们我先要给大家介绍一下，A的它是什么样的一个东西。大家可以看到agent它的中文翻译是代理人的意思。对，其实agent它就是代理人，他可以帮我们去代理一些事情，去帮我们去执行，去完成一些事情。给大家讲一下一些人工智能的一些大牛，他们是如何定义的这个agent。

发言人   01:16

Agent是一个以大元模型为核心，可以调动各种各样的工具。它可以拥有记忆，也可以自主策划自己的行为，然后来帮助用户完成他的目标的一个一套流程。也就是说agent是一个以大元模型为核心，可以自我反省，可以自己使用这种工具来帮助用户完成一个特定目标的一套流程，这就是agent。

发言人   01:40

我们先看到左边，我们先来左边，在这里创建一个自己的agent。我们今天的目标是创建一个具有简单计算功能的agent。因为这个大元模型可能它在计算能力方面不是很出色，计算一些复杂的式子的可能会出错。这里我们就需要给他一个工具，这边的这个agent是各式各样的agent。这个zero shot agent就咱们这个项目里一个最通用的一个agent，就是你可以让他做任何事情，其他的都是一些具有特定功能的A枕头。咱们先不用管，我们再去做一个大语言模型，也就是这个agent的核心。

发言人   02:28

我们就使用ChatGPT就行了，ChatGPT它效果会好一点。像chat GM3现在就是有点还是有点带不动，还是需要官方把这项目再更新一下。因为这个项目就通过lunch的框架来调用的agents。因为他这lunch有点版本有老，所以就是稍微的有点带不动。等后面项目更新一下，然后大家就可以去跟企业合作的时候，就可以正常的去调用XGM3了，就不用GP4了。我们现在还需要一个计算工具，把它拖过来，好，三个组件就拖过来了。Agent是以大语言模型为核心，然后运用多种工具来进行自我反省，自我策划的一个能力，最终来完成用户目标的一套流程。这是agent它的核心，这是它的工具，这里这些提示词就它自我反省的一些逻辑写在这里，我们把它连接一下。

发言人   03:40

这样子他们就创建了一个简单的具有计算能力的一个，给大家说一下，再重复一遍，这里带这个红心就是你必须要和它连接，是必填项。前面也解释过了，你的组件它可以连接很多的组件，这个点也可以连接别的LM的连接点，也就是别的大语言模型连接点。我们再把一些必要的参数给填一下，APIK。

发言人   04:17

我们测试一下OK，没有报错，我们就可以去试一下了。

发言人   04:31

刷新一下，然后在这里直接上线。上线之后，我们就直接使用就可以了。我们可以问他一些问题，请帮我计算。

发言人   05:04

然后问它等于多少，可以看到agent它已经开始运行了。好，最终他得出了他的答案。如果大家看不明白的话，咱们可以去翻译一下。

发言人   05:28

你可以看到他在这里输出的是他一些操作，有一些想法，他需要执行一些运算操作。他就调用了这个计算的工具，把这些内容输出给了工具，工具计算答案得到9927这样一个答案，然后再反馈给大语言模型。大语言模型它知道最终答案是9927以后，然后再把这个答案输出给我，这就是这样一个非常简单的一个agent，最简单的一个计算型agent。好，接下来给大家上一些难度。

发言人   06:21

我们可以使用项目官网的一些模板给大家介绍一下。大家也可以自己去到这个文档里找到这些应用。你可以看到它的应用案例，合同审核、招股书分析。我们今天用这个招股书分析，如果你想直接用他的这个例子，你也可以在这里点技能市场。他给你提供了下载导入的功能，可以在这儿点下载。下载好了之后，咱们在这点击导入。

发言人   07:00

然后把咱们刚刚招股书分析打开，这就是官方弄好了一个模板，我给大家介绍一下这个agent是怎么样的一个工作流程呢？

发言人   07:32

首先你得先有一个招股书的PDF，在这里通过文件的节点输入进来，在这儿对这个PDF进行加载。加载好之后因为这个PDF很大，大家看一下有多大。

发言人   08:01

这是我随便下载了一个PDF，它总共有四百多页，这些数量就非常大了。大语言模型它不可能一下读完四百多页PDF的，它就需要有一个文本分割器，把PDF分割成小块的PDF，然后挨个塞进这个大语言模型。分割好之后，放到这个向量数据库里就行了。如果你需要进行检索的话，就用这个组件进行检索就可以了。用这个open I的evening模型，它的作用就是把用户的问题向量化，然后发到这里进行检索。因为把这个PDF先进行的向量化，存在的存在这个数据库后，用户的问题也需要进行一个向量化，再到向量数据库里检索，只有向量才能跟向量进行检索，所以我们需要这个模块。

发言人   09:03

我们可以探讨这些组件，它是用来获取检索内容，检索完的内容和大元模型连接在一块，用这些组件。就是把检索内容返回给大元模型。大家可以把这三个组件看成大部分时间都连接在一块的组件。这些组件其实也可以换成别的。因为不一定只有这一种向量储存的方式，也可以用其他的向量储存。如果有的话，你就可以把这替换掉。

发言人   09:44

我们再看一下这一堆组件这一堆组件完成了一个对PDF进行向量检索的一个功能。我们可以把它预设成一个工具，因为只有工具节点才可以连接agent这个工具节点。这些节点它是不能连接到这里的。我们需要一个中间人来告诉agent我们整块的这堆组件是实现什么样的一个功能，让这个agent的知道我们该在什么时候调用这些工具。我们可以看到它这里预设了一些词，government agent这个工具，使用这些工具能达到的一些目标。

发言人   10:27

我们再看到其他地方。这个就是大圆模型，我们在这其实可以把它替换掉，换成x GPT的比较好。拆GBTBTB4的话，它的调用A指的能力还是独一无二的。如果大家没有A就是GB4的API的话，可以来咱们的代码包的文档里来获取一下。给大家放了一个，这两个是等价的。等一会儿咱们介绍完之后就把它替换掉。可以看到它这里还连接了一个计算工具。因为招股申请书审核的时候，可能会涉及到一些计算的问题，就需要用agent来调用计算的工具，可能也要用到搜索的工具，这里是搜索的工具。

发言人   11:20

他们最终连接到这个zero shot agent这些组件。我们可以看一下，agent是一个以大语言模型为核心，这就是大元模型调用这种工具。如果你想获取公司的信息，就调用这个工具。如果你想要计算一些功能，就可以调用这个工具。如果需要联网搜索，就调用搜索工具，使用工具来达到用户的最终目的。

发言人   11:57

这里是预设的一些问题。也就是说这个agent以大语言模型为核心，调用这三个工具来回答用户的这些问题。就比如说该公司比就比如说该公司的主营业务，盈利模式，agent它还会有一个行为的方式，知道他在什么时候该干些什么，调用什么工具该调用什么工具，这是进行的一个预设。咱们可以看一下。咱们可以看一下，你可以告诉他这个格式，给他一些例子。这也是指导他的一个行为方式。我们可以看到下一个参数就是问题的关键词，这个是获取到答案的关键词。

发言人   12:54

这里是用户输入的关键词，这里是返回答案的关键词。这些关键词要在这个input输入变量，这个关键字就代表用户的问题，这个关键字就代表最后返回的答案。这是一个复杂一点的一个单体A检查，我们来试一下，报错了，缺少了密钥，再填一下。

发言人   13:26

填一下，再测试一下。OpenAI组件我们还没有调用，那我们就把这个删除了，直接删除掉，把它连接到大语言模型的节点。给它连接上。

发言人   14:02

测试一下，他还说缺少一个APIT。

发言人   14:19

再测试一下OK好了，跑通了，我们去具体的体验一下。

发言人   14:32

大家在这里可以预设一些想要问的问题，相也相当于是预设一些提示词工程，就是不用再去手动的去问了。比如你可以设定一些公司比较好奇的一些问题，常问的一些问题，就经常会问的一些问题，还有一些就是比较不关注的一些内容，都可以来预设一下，然后保存一下，保存成功。

发言人   14:59

然后刷新一下。把招股书上线，把另外一个下线，这样会快一点。

发言人   15:16

大家可以看到，输入框是不能输入问题的，只能在这儿上传文件，因为咱们定义了文件的上传节点。把周五书上传进去。开始解析文件，它这个过程可能需要很久很久，大家可能需要等一下。

发言人   15:42

好，已经开始执行了。我们可以看一下输出内容，要查找公司的主营业务。在这里查找失败了，无法确定。很有可能就是招股书上写的不是很明白，盈利模式也失败了。销售模式咱们看一下。销售模式有直销、经销贸易商，有多少知识产权？有这个45项授权专利，是一项发明专利，这里也没有提及，他没有分析出来该公司主要股东持股比例主要股东持股为百分之百，剩下的就是一些细节了，他在这里是有一些问题的，没有分析出来。我们看一下哪些参数会影响这个生成的结果的一个质量。

发言人   17:05

这temperature也就是回答的随机性，我建议大家在这种严谨的应用场景下，用agent的话尽量调成0。如果你真的想让它很好用的话，如果是企业自己来用的话，那也可以用4。如果是不差钱的情况下，但我还是建议大家等后面这项目更新了以后，咱们去调用拆M3。但拆GM3，它效果是一定不如这批次的。对，但是这GP4，它就会很贵，一条问题可能要八毛钱一套A证下来可能要十块钱。

发言人   17:56

然后再就是数据集的治疗，数据集要包含问的问题，其他的话就没有什么可以修改了，大家退出保存一下就可以。今天这个单体agent创建的教程以及使用的教程，就先到这里。招股数据agent大家可以私底下去试一下，好好试一下。然后BB4的GPT4的API也给大家放出来了，大家去体验一下就行了，体验一下你就知道了。因为这个东西相对来说就比较复杂，你如果只是看的话，你可能就是看不太懂，必须要咱们自己去自行体验几遍的，然后就不浪费大家时间了。我希望大家一次性把这些所有的都体验完啊不要再浪费服务器的钱。同学们，然后这执行过程确实是非常慢的，大家到时候自己去体验一下就可以了。好，就先到这，下节课咱们来讲一下多体agent大家如何去调用和实现。