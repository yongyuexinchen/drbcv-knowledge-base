提示词深度讲解

提示词深度讲解_原文

2026年07月22日 14:53

发言人   00:00

OK同学们今天给大家带来如何编写提示词的教程。接下来我会从这个理论层面给大家讲明白这些提示词是什么。然后咱们下节课再到实践层面来带大家实操编写一些经典的提示词。

发言人   00:17

好，我们就直接开始，先给大家讲一些理论部分。首先我们要知道这个大模型它到底是什么？大模型大家可以看到是在指人工智能领域用大量数据进行训练，拥有了庞大参数量的深度学习模型。它可以处理自然语言任务，图像识别等任务。我们目前使用的这些像XJM或者说chat PT轻微星火这些就可以被称作为自然语言处理大模型。大模型还有一个英文名字叫做LLM，大家看到LLM的时候就知道这是大模型。如果你看到NLP大模型的话，你就知道它就是自然源处理的大模型，也就是咱们所说的这些ChatGLM ChatGPT这些都属于自然源处理的大模型。

发言人   01:17

OK讲明白这些大模型之后，我们看一下这里提示词到底是一个什么东西。大家可以这样理解，提示词它就是你在使用这个大模型的时候，你跟大模型所说的一些话，这就是提示词。它很简单，文当你写的很长，我们就可以把它直接理解为你给大模型输入的内容就是提示词。就比如说我在这里说你好你好这个词，它就是第二次，也就是我们所说的prompt。

发言人   01:52

接下来是大家在编写prompt可能会存在的一些问题。第一个是模型它可能存在的问题，有一些大模型它在训练的时候可能用的数据集，或者这个模型性能的因素，可能就会导致模型理解不了提示词的内容，就会导致这个输出质量的下降，这是模型自身的原因。还有一种是使用者的原因。如果没有学过这个提示词编写的话，自己写的提示词它就可能缺乏系统性，全部都是依赖个人的经验。这些提示词也没有一个固定的方法，别人可能也看不懂你的提示词，分享给别人，别人也不知道该从哪里修改。而且不同的模型之间也会存在一些差异。总的来说大家平时写提示词的过程可能存在的问题可能是啊模型自身的原因，模型质量可能不太行，不能精准理解提示词。第二个就是使用者的原因，没有学过如何编写提示词，这样的话和大模型互动时输出的内容它就会不符合预期。

发言人   03:00

接下来为了解决prompt存在这些问题，我们就要引入这个提词工程。提尔斯工程是什么呢？它是采用一种工程化的思想，还有这个指导方法来编写提尔斯。大家可以看到工程化编写提示词的过程和机器学习的过程类似，都是需要对这些提示词一直进行迭代。你可以看到当你有一个编写提示词的idea，也就是想法的时候，要先去把咱们的想法付诸实践。你开始写，第二次写之后就要测试一下，测试出来就要进行分析，看一下能不能达到你的预期。如果不行的话，我们要继续进行迭代，进行循环，直到产出令你满意的prompt为止。

发言人   03:50

接下来就给大家介绍一下如何用工程化的思想来编写提示词的一个理论指导的方法。Prot它在构建的时候会遵循两个原则，一是你要给大模型一个清晰和明确的指令。二是对模型思考的时间。我分别给大家讲一下这两个原则是什么意思。

发言人   04:20

写的大模型碟子，你要尽可能的清晰明确，要避免这种模糊性的歧义。假如你想让模型给你生成一个文章，你不能只是说写一篇文章，你要写出明确的指令。我给大家举个例子，写一篇文章。这个是ChatGLM大模型。咱们可以看到，我只说了写篇文章的要求，大模型开始随便写了，对不对？它就开始指向人工智能领域。但其实我们可能让他写一篇。中学生春游的作文儿。

发言人   05:24

这就是一个清晰明确的指令。要告诉大模型，你要写一篇文章是关于中学生春游的主题。你可以看到他就按照你的要求来写。其实你这个原则清晰明确的指令，就跟咱们平时人和人说话之间一样。你也可以把大模型想象成为这个人，大家跟别人在沟通的时候，要提出你的要求，也就是要尽量的用清晰明确语句来告诉他你想干些啥。就比如说领导给下属布置任务的场景，领导直接说下面给大家派的任务，但没说任务是什么，大家就会听不懂对不对？所以领导也得说明白任务是什么，任务该怎么干你不能只说吃，对不对？也就是提示词得有明确的意义。

发言人   06:13

其实大模型你可以在一定的程度上把它理解成一个人。你写pmp t的时候，就像是你跟一个人在沟通的一个过程。然后下面是一个例子，同学们。咱们在编写时除了语言上要清晰，你还可以使用一些标点符号来给这些模型更更清晰的指定。可以使用中文的双引号，也可以使用尖括号，英文的双引号，还有一些特定的这种符号。咱们需要把想输入的内容用符号标记起来。模型就更有可能会理解前面这段话和引号里括起来这段话，他们不是一个内容的。我给大家举个例子试一下。

发言人   07:18

可以看到，模型它可以理解你说的意思，他知道这些引号里包含了什么内容。就比如我们大家在编写一些复杂的提示词时，你有一大段话想要给传输给模型的时候，你就需要用引号来和你的要求分得开。尽量是要把任务要求。和用户输入用分隔符号。

发言人   07:53

Let do that. 在编写的时候，你要用这个引号。来把用户输入的问题来给隔开。这样大模型它就会清楚的知道任务要求是任务要求用户输入是用户输入，这是另外一个使用分隔符是让指令更清晰的一个方法。

发言人   08:18

第三个方法我们也可以用一些示例来指引模型的输出，来规范它输出的格式。如果你想让大模型输出一些特定风格或者格式内容的时候，你也可以给它一个例子。就比如咱们接下来的例子，请你按照以下格式输出内容。

发言人   08:55

这可用一个标点符号，用引号把这里遮开。问题。

发言人   09:11

回答。用户书。中国的首都是哪里？大家可以看到，这个是给这个大模型的一个任务指令。后面这段话是给它的一个样例，你可以把它理解为任务要求，这是用户问他的问题，咱们可以试一下。可以看到模型按照样例返回了问题，这就是少量样例。

发言人   09:50

指引他输入的格式。在咱们一些实际的操作过程中，一般不会是这种简单的格式。咱们一般是要求用Jason格式来输出内容。

发言人   10:13

使用js方式来让大模型输出内容。知道这个prompt构建的第一个原则，用清晰明确的指令。我们总结一下，第一，你要把语言描述的很清晰。第二，你可以用一些工具来让你输入的提示词显得很清晰。第三，你可以用一些样例数据来指导模型输出，让它输出的很清晰。这是第一个原则。

发言人   10:47

第二个原则是给模型一些思考的时间。有的同学可能会问了，这个时间该怎么去理解呢？你可以把它理解为要给模型足够的信息，让它来充分的理解上下文。就比如咱们在和人之间沟通的时候，你也可以想象这个场景，说一句牛头不对马嘴的话，你说我很慌或者说我很害怕，就没有上下文了。别人就不知道你怎么了，你为什么害怕，你为什么慌，你必须得说清楚，对不对？比如说你现在准备去蹦极，我很慌，你要给别人提供这种足够的情景信息。模型真人一样，它需要足够的上下文。

发言人   11:27

第二时间还有第二层的意思，你要分步骤来引导模型进行思考。因为模型它在执行一些复杂的任务中，它可能不会像人一样，它有一个思考的过程。大模型型的输出是按概率来输出文字的，所以在解答一些复杂的问题的时候会出错的。咱们就需要让模型沿着正确的思考路径来引导他得出正确答案。对，我们分别来看一下这两个例子。第一个是给动模型上下文，让模型来回应。第二个例子是分布营造模型。

发言人   12:09

这些上下文怎么理解呢？我们可以看一下这个例子。开头告诉模型你是一个智能助理，模型的任务是记录信息，接着告诉他用户的输入是什么什么一个内容。下面是要求。

发言人   12:32

在这个例子中的上下文，第一个不定你的角色，第二个是给你任务，第三个是设置任务要求，这就是整个上下文。我们往下看，在复杂的任务当中分步骤引导模型。咱们可以看一下这些事例。

发言人   12:56

让这些大模型来解一道数学应用题。如果直接把这些题目对着大模型，模型就很有可能会出错。那怎么让模型得出正确的答案呢？你就要告诉他人类是怎么来思考这个问题的，你需要把人类思考的方式焦虑大模型来一个模型可以看到这里，你需要按照这个方式解题。这个是题目，告诉他计算的过程是一个什么样的，最后得出一个答案，然后接着把新的题目给他，让他按照上面的计算过程来思考这个题目怎么解。可以看到模型就得出了答案，这就是分步骤引导模型。

发言人   13:43

我们来总结一下第二个原则，对模型的思考时间，在原则里它有两个内容。一个是你要给模型足够的上下文信息，让模型知道他自己是一个什么样的角色，他是干什么吃的，他要做什么任务，任务有什么要求。第二条是在复杂的步骤中分步骤来引导模型。在引导的过程中大家就要学着人类的思考方式，把自己的思考方式写给自己的大模型，让他按照这个步骤来思考。这就是prompt提示词编写的两条原则原则。

发言人   14:18

看完之后，我们来看一下题词的结构。大家在写提示词的时候，也可以按照这个节奏来撰写你的提示词。第一个节奏就是上下文，而上下文里有角色任务和知识。第二节奏是命令，就是你要给他下达的一个指令步骤思维链示例。第三个是输入数据，也就是用户输入的一些内容。最后是输出的是要求大模型，它要以什么格式来输出。咱们可以看一下这个例子，这是一些对应的提示词，红色的部分是上下文的节奏。

发言人   15:04

告诉模型你是一个机械工程师，你的任务是开发这个文本模型。决策任务。该模型可以将电影评论分为正面和负面两类，告诉他怎么分类，接着是指令这个节奏，必须告诉模型的任务是什么。请根据这个上下文输入，对文本进行分类，并且给出相应的输出类别。这里是它的实例，这儿它是没有步骤的，因为这些任务不需要很复杂，思维量和步骤。接下来是输入文本，因为大模型要对文本进行分类任务，这就是用户给他的文本，在这里让大模型输出，这是输出类别。你可以在这儿不一定模型以什么格式输出，以Jason格式输出或者是其他格式输出都可以。咱们看一下这个结构上下文指令，指令带着这个示例，这个是输入，这也是输出的格式要求，这里没有写，这是一个经典的prompt编写的一个结构。

发言人   16:18

我们接下来看一些企业生产环境中使用的铁。可以看到业务一，比如说某智能音箱，它需要用大模型替代传统自然语言处理的模型，可以从用户输入的内容中提取出结构化的信息。就比如说大家要跟智能设备进行交互，例如小爱同学还有siri对不对？你要告诉模型一件事儿，要从你的句子里提出一些有用的东西。业务的要求是模型能处理用户输入的内容，以固定格式来输出内容，以便于接触解析提示词。输入内容要供后段的代码来进行一个解析。如果这个大模型输出了一段不符合规范内容的话，那后端程序就无法解析内容，后端就会出错。这就是为什么大家都会要求大模型输出Jason格式，这也是方便后端接触来进行解析。

发言人   17:17

OK我们来看一下它是怎么设计提示词的节奏的。首先用户输入会固定一个提示，将用户输入的内容部分留空。当再有用户输入的时候，他就会把用户输入内容补充到流通的部分，拼接成一个完整的提示词，再提交给大模型。接着在这儿还得大模型定义了角色身份来帮助模型理解。背景说明照片模型上下文，字段说明照片模型要提取哪些内容，输出示例告诉模型一个输出的示例，这是提示词内容。咱们回到上面这儿，你可以看到它这定义了身份，上下文定义了角色。这里定义了角色，还得说明你上下文，海底的指令，也就是必选的面料，这个节奏里说的内容它这里没有留，其实就是用户输入内容流通的部分，其实也有输入的节奏。

发言人   18:27

第四节奏就是输出的示例，要告诉大模型要用什么格式输出。我们来看一下这个提尔森。这告诉模型的角色，模型任务是什么？要告诉模型用户可能会输入一个什么样的一个指令，并要求记录信息。这是大模型的任务，要提取出信息。

发言人   18:51

这里是给大模型一些清晰明确的规范的要求，告诉模型输出完之后直接结束啊不要新增内容。其实这句话还是很有必要的。大家平时在写提示词的时候，大模型可能会胡乱输出，对不对？你可以像这样一样，采取一些非常严厉的这些处分来告诉大模型，你就是不能输出其他内容，你必须要按照这个格式来输出或。你也可以看到这是按条来规定的这也体现了咱们的第一个原则，对模型的指令要清晰明确，条例要清晰，要用1234高达模型。

发言人   19:32

那我们来看一下结果，可以看到这是咱们的提示词，这是用户输入，告诉模型昨天例假结束了，大模型后端就开始记录了。话题是月经，目的是记录。用户又输入了一句，今天我生日，急着花输出这个话题是生日，目的是记录。这个提示词讲完了，它可以相当于是一个实体提取类的题词，后面也会搭一些作业来让大家练习一下，这儿可以看到心得。流程太多会增加这种大模型出错的概率，我们要尽量减少，不能强制大模型输出某些内容，要根据语义输出适合的每句话。

发言人   20:26

这句话其实也不对，这句话大家可以不看。在我们测试的过程当中，有时候必须要强烈的语气来告诉大模型要干什么。在咱们实际的生产过程中有的时候语气一定要强烈一些。大模型它就会知道该怎么干，都用肯定句。你要告诉他干什么，要结合功能设计。这个帖子啊不要期望于大模型的交互，一次交互解决所有问题。

发言人   21:00

这句话怎么理解呢？告诉大家，就比如智能音箱的例子。智能音箱它这个业务场景是需要用大模型来提取出用户语句的一些关键内容，然后来通过这个关键内容达成一定的功能。你不能指望着你对大模型说了一句话，他就把这件事情全干完了。实际上大模型它并没有那么强，它背后的逻辑是这样的。同学们当大模型接收到用户说的第一句话的时候，首先第一步把这个用户的输入传给大模型，解析到结构化的数据，把这个数据传到后端，让后端来执行这些操作。

发言人   21:45

后端在执行操作的时候可能需要和大模型在交互一次，就是这样一个逻辑。所以说你不能指望这大模型一次干成你所有要求的事儿，尽量让这个模型来分布处理这个内容。这是第一个实例。

发言人   22:08

我们来看第二个事例。业务要求是给主播撰写短视频文案，将这个指定文本转化成指定视频主播风格的短视频文案，也就是将一段文字抓成一定风格的文案。它的业务要求是这风格很难以描述，两个风格差距较大。我们来看一下设计过程，他这设计了两版，第一版和第二版。第一版他没有给出样例，他在这儿留出了用户输入的位置，给了模型一个任务，给了一个角色，给了要求，但没有给样例，这样效果可能就不会很好啊。因为你光这样说话的话，大模型它是没有办法理解到用户到底是以什么样的风格来说话的，大模型也不能很好的理解这风格。

发言人   23:01

我们来看第二段，这里就给了大模型的样例，让这个大模型从这个样例中学习到这个主播说话的风格，因为风格这些东西有的时候你用言语说不清，只有给一些例子让大模型看了之后，他才能领悟到。你可以看到他样例给完之后。给了他一个角色，告诉模型要参考样例，这是他的任务改写原文，这是用户说的地方。总结一下第二版，第二版基于full shot，就是少量势力的意思。

发言人   23:55

告诉模型来参考样例来生成这个文案，我们可以看下效果。

发言人   24:07

在这里就给了样例123。接着是要求，这是原文模型输入之后就很像上面的这种样例，我们可以看一下心得。

发言人   24:31

可以看到风格类的代谢尽量使用少量样例。因为这个大模型它很难通过文字get到具体的内容。风格是一种很难说清的一个特质，只有看到了具体的例子才知道这就是让力的作用。然后接下来给大家留了三个作业，一个是实体抽取的一个作业，你需要通过这段文字来提取出品牌的中文名、英文名、创业时间，主打商品。这些作业跟前面的业务一它是有点像的。

发言人   25:16

你可以看到，你也可以自定义一个角色，告诉我模型以什么样一个节奏来输出内容。你也可以告诉模型输出节奏是什么，大家下去也可以试一下。是实体抽取的一个任务。

发言人   25:35

第二个是破解任务。破写德芙巧克力味道很不错。这个句子他的风格要求是淘宝优质点评的质量。这个主题就有点像第二业务。大家要编写这些作业的时候，要尽量可能去抽选一些淘宝优质点评的样例给到模型，再用大模型仿照事例来编写点评。

发言人   26:00

第三个是张大模型升题目，也就是大模型的总结能力。这些题目大家可以自由发挥，结合前面两个学到的东西自由发挥就可以了。好，然后咱们今天的提示词理论课就先到这里了。这节课主要给大家讲解了一些提示词的理论指导，这就是咱们今天的全部内容了。然后咱们同学可以切换到下一课，下节课来带大家实操一下提示词。好，同学们，我们再见。