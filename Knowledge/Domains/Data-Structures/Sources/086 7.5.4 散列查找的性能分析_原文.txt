086 7.5.4 散列查找的性能分析_原文

好的，在这个视频中我们会以线性探测法为例去探讨散列查找的性能分析。在历年真题当中，但凡考到散列查找性能分析的，有80%左右的题目都是基于线性探测法，所以我们会以这种方法为例。好，首先我们会探讨如何计算散列表的ASR，也就是平均查找长度。这个是考研的重点，在大题和小题当中都有考察过ASL的计算。其次我们会引入两个概念，分别叫做装填因子以及聚集现象，又叫堆积现象。这两个因素都有可能会影响散列表的查找性能。

好，首先我们探讨第一个问题，如何计算散列表的ASL。直接上例题，假设这儿有一个散列表，长度是十六，那么对应的数组下标就是0到15。这个散列表初始为空，并且设计了一个散列函数，我们用关键字对13取余数，用这样的方式可以确定一个关键字的初始散列地址。当发生冲突的时候，我们会采用线性探测法去处理这种冲突。好，后面给出了这个关键字的插入顺序。

第一个小问让我们画出所构造的这个散列表。好，首先插入关键字19 19，对13取余数等于6，所以19这个关键字它的初始散列地址应该在六这个位置。插入到这个地方，接下来14，它的初始散列地址计算等于一，插入后一个关键字23，初始散列地址等于10，插入到十这个位置。好，再往后现在出现了不一样的情况，一这个关键字它对十三取余等于一，然而现在一这个位置已经被其他的元素所占领了，对吧？也就是说发生了冲突。

那么当发生冲突的时候，题目说让我们采用线性探测法。那线性探测法的规则很简单，如果当前这个位置发生冲突，那么就往后面往右边再探测后一个位置。好，那后一个位置二这个位置此时为空，对吧？所以一这个关键字插入到二这个地方。好，再往后68这个关键字它应该插入到三这个位置，再往后20插入到7 84这个关键字，对，13取等于6。然而此时六这个位置已经有别的元素了，也就是产生了冲突。那么往后探索一个位置，再往后探索一个位置，八这个位置此时为空，所以84这个元素应该插入到8这个位置。

好，继续后一个关键字，27，它的初始散列地址等于11，这个位置已经被占用。那么按照线性探测法往后探测二这个位置也已经被占用，三这个位置也被占用。直到探测到四这个位置的时候，这个位置为空。

因此27插入到四这个位置，接下来55这个元素初始散列地址等于3，这个位置已经被占用，探测4，再往后探测到5，因此55这个元素应该插入到五这个位置。好，接下来11这个元素插入到11这个初始散列地址，十这个关键字初始散列地址等于10，发生冲突，往后探测一个单元，再往后探测一个单元，12这个位置为空，所以十这个元素插入到12这个位置。接下来最后一个元素79，它的初始散列地址等于11，这个位置已经被占用发生了冲突。那么就会依次的探测后面这些个地址，直到探测到9这个地址的时候，这个位置为空。所以79插入到9这个地方。好，这样我们就把这些关键字都依次插入到了这个散列表当中，并且构造出了整个散列表的样子。

接下来第二个小问，它让我们计算出在等概率的情况下查找成功和查找失败的平均查找长度ASL。好，首先来看在等概率情况下查找成功的这个平均ASL应该如何计算。好，既然要查找成功，那我们要查找的关键字肯定是已经存在于这个散列表当中的某一个关键字，对吧？那么在等概率的情况下，我们认为我们查找其中的任何一个关键字的可能性都是相等的那总共有12个关键字，因此任何一个关键字被查找的概率都是12分之1。

好，假如我们要查找的是14这个关键字，我们是怎么做的？首先我们会以此次要查找的这个关键字去计算它的散列地址，对吧？计算它的初始散列地址也就是对十三取余，那么十四的初始散列地址等于一。因此我们会在一这个位置去去对比这个元素，它的值是不是等于我们的目标关键字14。如果等于，那么OK我的查找就已经成功了。所以对于14这个元素来说，我们查找它就只需要对比一个关键词，对吧？

好，再看第二个元素。比如说假如我们要找的是一这个元素，那么我们首先会用这个元素去计算它的初始散列地址等于多少。那么一对于十三取余等于一，所以首先我们会去一这个位置去查找我们的目标关键字。由于这个初始散列地址和我们要查找的这个目标关键字不一样，所以我们会探测后一个地址对吧？因此查找一这个元素总共需要付出的查找长度或者说关键字的对比次数，就是对比了一次两次，直到对比第二个关键字的时候，我才可以确认查找成功。

好，后面这些元素它的查找长度的分析都是类似的。事实上我们之前插入这些元素的时候，计算得到的这些探测序列刚好就是我们查找这个元素所需要经过的这个对比次数，比如说79这个元素，如果我们要查这个元素的话，那么首先它的初始散列地址等于？一这个位置没有对上，那么往后探测后一个元素没有对上，继续往后，直到探测到九这个位置的时候，它和79这个关键字能够匹配上，那此时才可以确定它查找成功。所以查找79这个元素总计需要付出的查找长度代价应该是多少呢？应该是123456789，应该是九的查找长度，对吧？

好，所以综上所述，我们可以得到这样的一个计算公式，等概率情况下的平均查找长度。整个散列表当中总共包含12个元素，因此每个元素被查找的概率是12分之1。那19这个元素它的查找长度是14这个元素它的查找长度是一，23这个元素它的查找长度是一一这个元素它的查找长度是2。好，后面的以此类推，那么把所有这些元素的查找长度都相加，然后再乘以12分之1，就得到了查找成功的ASL等于2.5。好，那么这是查找成功的ASL。

接下来我们再看查找失败的ASL怎么计算。好，那么计算方法是这样的，先观察这个散列函数，这个散列函数有可能得到的初始散列地址总共有13种，对吧？就是说你查找任何一个关键字，它的初始散列地址肯定只会落在0到12这个区间内。好，那么我们假设在查找失败的这种前提下，这个关键字的初始散列地址落在这13个位置处的概率都是完全相等的。比如说假如某个关键字它的初始散列地址落在了零这个位置，那么由于零这个位置本来就是空的，对吧？所以要确认这个关键字查找失败所需要对比的关键字数量是不是只有一个，就对比零这个位置就OK了对吧？

好，所以初始散列地址为零的这种关键字，从开始查找到确认查找失败所需要付出的查找长度就是一好再看如果我们的这个关键字它的初始散列地址落在了一这个位置。那么假如这个关键字最终要确定它查找失败，我们是不是得先对比一这个位置，一这个位置的这个关键字和我们查找的这个目标不匹配，那么往后对比二这个位置，二这个位置也不匹配，还需要往后对比三三不匹配，再到4567，一直对比到12还是不匹配。直到对比到13这个位置的时候，发现这个位置已经空了，此时我们才可以确定初始地址为一的这个关键字查找失败。那么整个过程当中，它的查找长度就应该是十三，对吧？就应该是十三。

好，那后面的情况是不是完全类似的这种分析思路？假如初始散列地址是二这个位置，那么这个位置不匹配，得往后依次的去探测，每个位置都不匹配。那么只有探测到13这个位置的时候，也就是说只有探测到第一个空位置的时候，才可以确认查找失败。所以对于一个初始地址为二的这种关键字来说，确认查找失败所需要付出的查找长度就应该是12。从这个位置到这个位置。好，所以后面这我们又加了12。好，再往后的是同样的道理，这就不再赘述。好，刚才说了，由于初始散列地址总共有13种情况，那每一种情况出现的概率都是13分之1，所以相加之后还要除以13，最后得到的结果就是7。

这儿有必要提醒大家的是当我们在计算查找失败的ASL的时候，很多同学容易犯这样的经典错误。很多同学会认为整个散列表的长度是16，对吧？所以他会认为一个关键字它的初始散列地址落在0到15任何一个位置的概率都是相等的，也就是它会乘以一个16分之1。但事实上这是错误的对吧？我们散列函数计算的这个初始散列地址，有时候和散列表的长度是并不相同的。计算查找失败的ASL的时候，要以这个散列函数可以覆盖的这个范围为准，而不是以散列表的总长度为准。这是考试当中很容易出错的点。

上面这个式子就是正确的计算方法，下面这个式子就是错误的示范。好，这是查找成功和查找失败的平均查找长度。好，这是第二个小问。

接下来第三个小问，我们让问题变得更复杂一点。在之前得到的这个散列表当中，我们删除元素20，然后再计算等概率情况下查找成功和查找失败的平均查找长度ASL。好，那么这种情况为什么要单独拿出来强调呢？因为很多同学会犯这样的错误，就是说20这个元素它既然已经被删了，那么就意味着数组下标为七的这个地方它就空了，对不对？比如说假如我们误认为七这个位置已经为空，那么当我们查找一个关键字，这个关键字的初始散列地址如果说直接就是指向了7，那假如七这个位置本身就为空的话，是不是只需要对比一次关键字就可以确认查找失败了，对吗？好，这是一个标准的错误，这是一个标准的错误码。

记住我们在之前的学习中强调过，采用开放定址法解决冲突的散列表当中，如果我们删除一个元素，那么我们进行的是逻辑删除，而不是物理删除。逻辑删除和这个位置为空完全是两个概念，对吧？当我们查找到一个逻辑删除的这种位置的时候，我们不能认为查找失败。因为这个元素被删除了，那么后面还有可能会存有同义词，对不对？还需要往后探测。好，所以这是删除元素之后很容易踩的一个坑。一定要注意我们删除元素的时候是逻辑删除，而不是物理删除。好，那么在这种情况下查找成功的平均查找长度，我们计算逻辑是一样的。

目前在整个散列表当中依然存在的元素总共只有11个了，对吧？我们把查找其中的每一个元素所需要付出的这种查找长度全部都加起来，然后除以11就可以得到查找成功的ASL。分析的方法和之前是雷同的。我们以79这个关键字为例，这个关键字它的初始地址是一，所以79这个元素首先会在一这个位置去查找。好，匹配不上，于是会探测二这个位置还是匹配不上好，继续往后探测，探测到七这个位置的时候，这个位置虽然没有存任何元素，但是它是逻辑删除对吧？并不是这种空单元格。那就意味着探测到这个位置的时候我还不能停，还得往后继续探测。好，所以接下来还会探测八这个位置没匹配上，再探测九这个位置，OK匹配上了。好，所以79这个元素它查找成功所需要付出的查找长度就应该是九这么多。

刚才这个例子我们也感受到了逻辑删除的这种位置对查找过程的一个影响。它和这种空位置是完全两个概念。好，刚才探讨的是查找成功的ASL，查找失败的ASL分析的话方法也和之前是雷同的。

同样的根据散列函数可以知道有13种初始散列地址。在等概率情况下，我们认为任何一个初始散列地址出现的可能性都是13分之1，概率都是相等的那我们依次算出每一个初始散列地址从开始查找一直到确认查找失败，它的查找长度应该是多少。把这些值相加，然后除以13就可以得到查找失败情况下的平均查找长度。

好，分析的方法和之前是类似的，这儿就不再赘述。重点要注意删除一个元素的时候，我们进行的是逻辑删除，而不是直接置为空。好，那么大家可以把这个例题当中的1233个小问都自己动手做一遍，把这三个小问搞定。那么考研真题当中分析ASL的题目，大部分应该都难不住大家了。

接下来我们会引入两个概念，分别叫装填因子以及聚集现象。接下来要讲的这两个东西会影响到散列表的查找效率。先来看什么叫装填因子，很简单，就是用散列表中的记录总数N除以散列表的总长度M它反映了散列表满的程度。比如说刚才得到的这个散列表，整个散列表的长度是16，而整个散列表当中总共有12个记录，12个元素。所以这个散列表的装填因子就是16分之12，也就是等于0.75，那这就是这个散列表的装填因子。当装填因子阿尔法等于一的时候，是不是说明整个散列表都已经填满了，对不对？显然当一个散列表它被填得越满的时候，或者说当一个散列表的装填因子越大的时候，我们就越容易发生冲突，对不对？从而就会导致插入一个新元素或者查找一个元素的时候，这个效率会降低，也会使得ASL增大。

这个结论不难理解。就以之前得到的这个散列表为例，此时这个散列表它的装填因子已经比较大了。那假设我们要插入一个新元素，这个新元素的值是X那么这个新元素它的散列地址是不是只有可能在0到12这个区间内，总共有13种可能。

我们观察现在这个散列表，如果这个新的元素它的初始散列地址刚好是插入到零这个位置，那OK我没有发生冲突，直接就插入成功。而只要这个初始散列地址是落在1到12当中的任何一个位置，那么这个插入操作的过程一定会发生冲突，对吧？比如说初始散列地址为一的话，那么它会发生好几次冲突。直到探测到13这个位置，才可以把这些新元素插入到散列表当中。所以可以看到装填因子很大的时候，我们进行插入操作，这个效率是很低的。因为大部分的位置都已经被使用了，所以在插入的过程中就极有可能发生冲突。

查找操作的效率也会因为装填因子过高而降低。这一点我们根据前面这个例题的第二小问已经有体会了。你看整个散列表的元素，很多的时候，查找其中的某些元素有可能会发生很多很多次冲突才可以找到。所以查找效率降低，从而导致这个ASR整体上升，对吧？好，这是装填因子这个概念，装填因子会影响散列表的查找效率。接下来再引入一个概念叫做聚集现象，也叫堆积现象。

用一个例子辅助大家理解，假设我们有这样的一个散列表，长度是27，散列函数是用关键字对23进行取余，并且我们采用线性探测法来处理冲突。那假设我们要插入6295、275、98121以及144。假如我们接下来要依次插入这些个关键字，那么这些关键字对23取余都等于6。也就是说这些关键字它们的初始散列地址都映射到了同一个地方。

好，那么在这种情况下，六是第一个被插入的元素。那接下来29这个元素，由于这个初始散列地址已经被占用了，对吧？所以只能往后探测一个位置，因此29要放到7这个地方。那52这个元素它的初始散列地址也等于六六这个位置占用了往后探测再往后探测它得放到8这个位置。好，后面的类似75就得探测到9这个位置，98得到十这个位置，121得探测到11这个位置，144得探测到12这个位置。好，那么这是用线性探测法处理冲突的时候，这几个元素最终的一个存储地址。

好，此时我们看到有一堆元素扎堆聚集在一起，或者说堆积在一起。那这种聚集堆积的现象会导致什么情况呢？我们思考13这个地址13这个地址，这个位置此时为空，可以插入一个新元素。但是如果这个新元素它的初始散列地址是在6 7 8 9 10 11 12 13，在其中的任何一个位置处，这个元素它最终都会放到13这个位置，那这就是所谓的聚集现象。

我们看到在处理冲突的过程当中，如果几个初始散列地址不同的元素争夺同一个后继散列地址，那么这种现象就称为聚集或者堆积。就像刚刚我们说的，如果我们插入一个新元素的话，那么不管这个新元素它的初始散列地址是6 7 8 9 10、11、十二还是十三，就是这么多个初始散列地址的新元素都需要共同的去争抢13这个空位。此时就发生了聚集或者叫堆积。显然这种聚集现象它不是什么好的现象，对吧？它也会影响到这个散列表的工作效率，会导致散列表的查找效率下降，ASR提升，这个不难理解。

你看一堆元素它扎堆聚集在一起，那假如我要查找一个关键字，这个关键字最后它会查找失败。那刚开始它的初始散列地址等于6，那从六这个位置开始查找，直到确认查找失败。我们总共是不是需要对比一次、两次、三次、四次、五次、六次、七次、八次，得对比八次关键字才可以确认查找失败。这就是一堆元素聚集在一起的一个缺点。假如其中的某一个位置我们把它挖掉，把这些元素挖掉，就是让它保持为空。那么我们从六这个位置开始查找，直到确认查找失败，是不是就不需要八次关键字对比，只需要从初始散列地址开始往后找到第一个空的这种单元格，我就可以确认它查找失败了。好，所以如果一堆元素都聚集在一起，中间都没有空位，那这种情况对于查找效率是有比较大的影响的那不幸的是线性探测法它的特点就会导致很容易出现这种聚集堆积的现象。因为在线性探测法当中，如果一个位置它发生了冲突的话，那么我们总是往后探测相邻的后一个单元，就很容易导致一堆元素聚集在一起，对吧？

好，那么既然这种聚集堆积的现象会对散列表的查找效率产生负面的影响，那我们怎么去缓解，怎么去减轻这种聚集现象呢？还记不记得我们在介绍开放定址法的时候，我们介绍了几种常用的探测序列。线性探测法只是其中的一种，对吧？

我们还介绍平方探测法、双散列法、伪随机序列法等等。接下来我们尝试着把线性探测法改成平方探测法，看一下能不能缓解聚集的现象。好，还是同样的例子，散列表的长度以及散列函数保持不变，我们把线性探测法改成平方探测法，用这种方法去处理冲突。

同样是那几个元素，那六是第一个元素，它可以顺利的插入到初始的散列地址。接下来29这个元素和六这个位置发生了冲突，那初始散列地址这个地方发生冲突的话，接下来会探测到从初始散列地址往右偏移一个单位这个位置。也就是七这个地方。好，那这个地方为空，所以29插入到7，接下来52它的初始散列地址等于六发生冲突。接下来探测从初始散列地址往右一格，这个位置还是发生冲突。接下来探测从初始散列地址往左一格，也就是减一这个位置，那这个位置为空，所以52插入到5这个地方。

接下来75这个元素初始散列地址这个地方发生了冲突。接下来往右探测一格，这个位置也发生了冲突。接下来往左探测一格还是发生冲突，再往后就是探测初始散列地址往右偏移四格那个位置。好，这是初始散列地址往右偏移1234，也就是十这个地方此时为空，所以75插入到10这个地方。

好，接下来98这个元素，显然它刚开始探测的这几个位置都已经被占满了。所以直到探测到初始散列地址往左偏移四个单位，也就是1234，也就是二这个地方才可以找到插入的位置。再往后121这个元素，它探测前面这几个地址都会冲突，对吧？直到探测到初始散列地址往右偏移九个单位，也就会插入到实物这个地方。

好，最后144这个元素前面这些个地址都已经被占用了。直到探测到负三的2次方，也就是从初始散列地址往左偏移九个单位的那个位置。好，我们来数一下，这是初始散列地址对吧？往左偏移123456，往这边789，往左偏移九个单位，24这个位置此时为空，把它插入。好，到此为止我们用平方探测法把相同的几个关键字插入到了散列表当中。接下来我们观察它的聚集现象，肉眼可见这些元素并没有像线性探测法那样全部聚集在一起，对吧？所以采用平方探测法去处理冲突，肉眼可见的减少了聚集现象。

还记得聚集现象它的定义是什么吗？就是说多个初始散列地址，不同的元素争抢同一个位置，这种现象就是聚集现象，对吧？那么在这个例子中，比如说我们看八这个位置，看起来八这个位置周边已经被很多元素包围了，但事实上这个位置此时只会被初始散列地址为78的2类元素去争夺。如果插入的新元素，它的初始散列地址等于七这个位置发生冲突的话，那接下来它会往右探测一个对吧？也就会探测到八这个位置，所以初始散列地址为七的元素会争抢八这个位置。除此之外大家可以去分析一下，其他任何一个初始散列地址的元素都不会来争抢八这个位置。所以从肉眼上看，元素的聚集现象大幅度的减轻了，以及从逻辑理性上去分析，这种聚集的现象也确实减轻了。

所以我们得到的结论就是，如果采用线性探测法去处理冲突，那么更容易导致聚集或者叫堆积现象。而采用其他的方法处理冲突，比如说用平方探测法，或者采用设计合理的双散列法，以及设计合理的伪随机序列法，采用别的这些方法在一定程度上可以缓解聚集现象的发生。而聚集现象减轻就意味着散列表的查找效率可以提升。

好，那么在这个视频中我们首先用一个例题带大家学习了如何计算散列表的ASL查找成功和查找失败的计算方法，略有区别。对于查找成功的情况，我们首先要确定的是散列表当中到底存在多少个元素。在这个总结式子当中，我把它记为N散列表中总共有N个元素，其中I这个元素查找成功所需的这种关键字比较次数是CI那么把所有的这些已存在的元素它的查找长度相加，然后再乘以N分之一这个相等的概率就可以得到查找成功的ASL。

好，另外在计算查找失败的ASL的时候，我们需要注意的是这儿的R这儿的R指的是散列表的取值个数，也就是初始散列地址有可能出现多少个值。散列函数的这个取值个数和散列表的长度有可能是不相同的这一点是做题的时候容易错的地方，需要特别注意。这儿的CI指的是散列函数取值为I的时候查找失败所需要的比较次数。计算散列表的ASR，这是考研的重点，有一半以上的这种散列表相关的考题都是要求计算ASR的，有可能考小题，也有可能考大题。大家可以结合王道的课后习题趁热打铁进行训练。

散列表的ASR反映了它的查找的效率。我们在这个视频的后半段引入了装填因子和聚集现象这样的两个概念。装填因子过高也会影响查找效率，会使得ASR增大。另外对于线性探测法处理冲突的这种散列表来说，比较容易出现聚集现象。而一旦出现聚集现象就也会影响查找效率，导致ASL提升。我们采用其他的方法，比如说平方探测法或者伪随机序列法，用其他的方法去处理冲突，就可以把元素稍微的打散一些，就别让他们聚集的太厉害。减少了这种元素聚集的现象之后，就可以使得散列表的查找效率得到提升。好的，以上就是这个视频的所有内容。大家可以趁热打铁，把王道书的课后习题，特别是历年真题好好的感受一下。