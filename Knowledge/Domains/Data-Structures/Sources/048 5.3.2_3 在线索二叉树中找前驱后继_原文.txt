048 5.3.2_3 在线索二叉树中找前驱后继_原文

好的，上一小节我们学习了怎么把二叉树线索化。别忘了我们建立线索的初衷就是为了更方便的从一个节点开始找到它的前驱或者找到它的后继。所以这个小节我们要探讨的是，当我们有了线索二叉树之后，怎么找到它的前驱和后继。我们会对这三种线索二叉树分别进行探讨。

首先看中序线索二叉树，先来看怎么找一个指定结点P的中序后继。如果指定结点的右孩子指针已经被线索化，也就是r tag等于一的话，那么它的右孩子指针本来就是指向它的中序后继的，所以这种情况下找后继很方便。但是如果这个节点它的右指针没有被线索化的话，那来看一下怎么找到它的中序后继。好，现在由于它的r tag等于0，那是不是就意味着这个指定结点它肯定是有右孩子的，也就是说这个指定节点的右子树非空。好，那既然找的是中序后继，也就是说我们要找的是按照中序遍历来看一个节点，它后面一个被访问的节点，那中序遍历的访问顺序应该是左跟右，对吧？

现在我们已经知道指定的结点P它肯定有右孩子，所以按照中序遍历的规则访问P结点之后，需要在中序遍历右子树。那按照中序遍历的规则，它的右子树当中第一个被中序遍历的节点就应该是P的后继。好，现在假设P只有一个右孩子，并且这个结点它就是最终的叶子结点，也就是它没有下一层，那显然这个结点就应该是P的后继。但是如果这个结点是一个分支结点，也就是它还有下一层的结点的话。那么对这个右子树进行中序遍历，也就是左跟右这样的规则来看的话，那它最左下角的这个结点就应该是P的右子树当中最先被访问的结点。好，接下来再假设这个结点它依然是分支结点。那么同样的按照左跟右这样的顺序把它展开的话，那这个结点的左孩子就应该是在P结点之后被访问的第一个结点点。好，所以规律就是P结点的右子树当中，最左下角的这一个结点就是P的后继结点。

当然这指的是中序后继而这是找后继的问题。好，来看一下怎么实现这个代码。首先我们可以实现这样的一个函数，这个函数做的事情就是从指定的节点出发。如果它的下一层还有左孩子的话，那么就会一直顺着左分支找到最左下角的这个节点。对于任何一棵树来说，按照中序遍历的规则，它的最左下角这个节点肯定是这棵树当中第一个被中序遍历的结点。所以如果要找到指定结点P的后继结点，如果它的右指针没有被线索化，我们就应该找到它的右子树当中第一个被中序遍历的节点，也就是实现这个逻辑。否则它的右指针已经被线索化，那么我们就可以直接返回它右指针所指向的节点。好，这是在中序线索二叉树当中找到一个指定结点P的中序后继。既然可以找到任意一个节点的后继，那我们其实就可以利用这个函数对中序线索二叉树进行中序遍历。

实现的方法很简单，我们传入我们要遍历的这棵树，它的根结点的指针T然后首先我们要找到在这棵树当中第一个被中序遍历的节点，也就是利用这个函数。那其实就是从根出发最左下角的这个节点。好，然后访问这个节点。当访问完这个节点之后，是不是就可以利用刚才我们定义的这个函数，很方便的找到这个节点的中序后继。所以用这样的一个for循环就可以实现对整个中序线索二叉树进行中序遍历。这个遍历算法并不需要递归调用函数，所以它的空间复杂度只需要O一这个数量级就可以。好，这是中序线索树当中找中序后继的问题。

再来看怎么找到它的前驱，分析的方法是类似的。如果左孩子已经被线索化，那么它的左指针所指的这个节点就是它的前驱。而如果没有被线索化，那么就说明他肯定是有左孩子的那按照中序遍历的规则，我们的这个结点P它的前驱肯定是左子树当中按照中序遍历最后一个被访问的节点。如果左子树只有一个节点，那显然这个节点就是它的前驱。而如果它还有下一层的分级的话，那么按照中序遍历左跟右这样的规则，它的右孩子节点显然是最后一个被访问的节点。好，如果再往后展开的话也是一样。总之从这个节点出发，最右下的这个节点才是左子树当中最后一个被访问的节点。因此P的左子树当中最右下的这个结点就是它的前驱。

有了思路我们就可以很方便的实现找前驱的这个代码。首先这个函数可以做的事情是可以找到一棵树当中按照中序遍历最后一个被访问到的节点。刚才我们说了，肯定是从根出发最右下角的这个孩子结点。所以如果左指针没有被线索化，那么我们只需要找到P这个结点，它的左子树当中最后一个被中序遍历的结点。用刚才定义的这个函数，这个结点就是P的前驱。而如果左指针本来就是线索的话，那么我们只需要返回它的左指针所指向的结点就可以。好，那有了找前驱的操作，我们是不是就可以实现这样的一个事情，就是对中序线索二叉树进行逆向的中序遍历，这就不再赘述。

好，这是中序线索二叉树关于找前驱和找后继相关的一些考点。接下来我们看先序线索二叉树，先来看怎么找它的先序后继分析思路是相同的。如果右指针被线索化，那么直接返回它的后继线索就可以。而如果没有被线索化，那说明P结点它肯定是有右孩子的。但是它有没有左孩子，这个我们还不确定。我们先假设这个节点它既有左孩子，同时也有右孩子。那么按照先序遍历跟左右这样的规则来看，P结点的后继结点肯定是它左子树当中第一个被访问的结点。显然按先序遍历的规则，P的左孩子这个结点肯定就是它的左子树当中第一个被访问的结点。因此如果P他有左孩子的话，那么他的先序后继就是它的左孩子。

好，这是第一种情况。我们假设它有左孩子，再来看第二种情况，如果没有左孩子的话，他肯定有右孩子。按照先序遍历的规则，左跟右左子树为空，所以遍历的顺序就应该是跟右。因此这种情况下P结点它的后继肯定就是右子树当中第一个被先序遍历的结点。显然无论它的右孩子是叶子还是分支结点，这个结点肯定都是它的右子树当中第一个被访问到的结点。因此如果P它没有左孩子，那P的先序后继就肯定是它的右孩子。这是找先序后继的一个，希望同学们有了思路之后，能够自己动手实现一下相关的这些代码，多动手练习，不要只停留在思考这个层面。

好，接下来来看怎么找先序前驱。我们只探讨它的左孩子没有被线索化的情况。当air tag满足这个条件的时候，可以证明一点就是P它肯定有左孩子。好，但是按照先序遍历的规则，我们按照跟左右这样的遍历顺序，P的左子树和右子树当中的所有的结点肯定都只可能是P的后，继而不可能是P的前驱。因此我们不可能在它的左右子树当中找到它的前驱，而我们的线索二叉树它只有指向孩子结点的指针，不可能往回找，对吧？所以在这种情况下，我们是找不到P的先序前驱的那除非用我们之前介绍从头开始遍历的土办法，再重新进行一次完整的先序遍历，用这种方式来找到它的前驱。

好，现在找不到P的前驱，这个结论我们是建立在每一个节点，只有指向孩子结点的指针建立在这个基础上得到的结论。但是我们说过，其实我们可以把二叉链表把它改成三叉链表，对吧？也就是给各个节点设置一个指向它父节点的指针。好，那来看一下如果能找到它的父节点的话，能不能找到它的先序前驱呢？先来看第一种情况，假设我们能找到它的父节点，并且P结点是父结点的左孩子。那么按照先序遍历根左右这样的规则来看的话，P结点肯定是在根结点，也就是它父结点之后就被访问的一个节点。因此它的这个父节点一定就是它的前驱，这是第一种情况。

第二种情况，假设P它是右孩子，并且它的左兄弟是空的。那么按先序遍历跟左右的这样的顺序，左子树为空，因此遍历的顺序就应该是跟右。那么同样的P的这个父结点一定就是它的先序前驱。好，再看第三种情况，如果P它是右孩子，同时它的左兄弟非空。那么按照先序遍历跟左右这样的遍历顺序，P这个结点的先序前区一定是它的左兄弟子树当中按照先序遍历的顺序最后一个被访问到的节点。

好，来看一下一棵树里边最后一个被先序遍历的节点应该是什么样一个节点呢？因为先序遍历是根左右，对吧？所以从根节点出发，如果它有右子树的话，那么右子树当中的节点肯定是最后被访问到的。因此从根节点出发，我们应该尽可能的按照向右的这个路径来找到最右下角的这个节点。

好，现在如果最右下角的这个节点它还有一个左孩子的话，那么按照根左右的顺序来访问。这个左孩子又成了最后一个被先序遍历的节点。好，如果这个节点它还有很多个左孩子，那么最下面这个孩子就应该是最后一个被访问的那如果这个节点它还有一个孩子，那是不是又得往右边下面找？所以一棵树里边按照先序遍历的顺序，最后一个被访问的节点，我们应该从这个根节点出发，优先的往右边的路走。如果右边的路没有了，那我们应该往左边的路走，然后用这样的方式找到最下面一层的这个节点，这个节点就是先序遍历当中最后一个被访问的节点。好，有一点绕，大家再自己体会一下。

好，回到这个问题，找先序前驱，我们在这儿已经探讨了，可以找到P的父结点，然后P作为左孩子和右孩子所有有可能出现的情况。还有一种情况我们没探讨，就是P没有父结点。也就是如果P是根结点的话，那根左右这样的访问顺序P肯定是第一个被访问到的结点，因此它是没有先序前驱的。好，这就是找先序前驱的四种有可能出现的情况。再次强调可以找到先序前驱，这个结论我们是基于允许逆向的找到P的父结点，基于这个前提来探讨的。

好，最后来看后续线索二叉树怎么找，后序前驱已经有线索的情况我们就不探讨了。来看没有线索的情况，air TAG等于0，那么说明它它一定是有左孩子的。但是它有没有右孩子，这个我们还不得而知。我们先假设它有右孩子的情况，那么按照后序遍历左右根这样的遍历顺序可以看到P这个结点。它的后序前驱一定是它的右子树当中按照后序遍历最后一个被访问到的结点。那显然P的右孩子一定是最后一个被访问到的，因此P的右孩子就是它的后序前驱。

好，再看下一种情况，如果P结点没有右孩子，那么按照左右根的遍历顺序，右子树为空，那就是左根P这个结点的前驱。应该就是左子树当中按照后序遍历最后一个被访问的结点，那显然就应该是它的左孩子。好，这是找后序前驱的方法，有了思路可以自己实现这个代码。

好，再来看找后序后继，我们只探讨没有被线索化的情况，这种情况说明他肯定是有右孩子的。但是按照后序遍历左右根这样的规则来看，结点P的左子树和右子树当中所有的节点一定只有可能是它的前驱，而不可能是它的后继。所以我们没办法在它的左右子树当中找到它的后继。因此我们只能利用土办法，就是重新进行一次完整的后序遍历，用这样的方式才有可能找到它的后续后继。当然还有一种情况，如果我们用三叉链表来存储这个线索树，也就是说我们可以找到P结点的父结点的话，那基于这样的条件来看一下。

好，先看第一种情况，假设P是它父结点的右孩子，那么按照后序遍历左右根这样的遍历规则，无论P结点它的下面还有没有孩子，P结点一定是这棵右子树当中最后一个被访问的结点。而在访问了P之后，就应该回头去访问它的这个父结点，因此P的后序后继就应该是它的父结点下种情况。如果P是左孩子，并且右兄弟为空的话，那么后续遍历就应该是左根。那显然无论如何，在访问完P结点之后，一定都是会紧接着访问它的父结点，因此这种情况它的父结点也是它的后继。

第三种情况，P是左孩子，并且它的右兄弟飞空。那按照后序遍历的规则来看，P结点的后继就应该是它的右兄弟。子树当中按照后序遍历的规则，第一个被访问的结点，一棵树里边按照后序遍历第一个被访问的节点，应该是我们可以从根结点出发，然后尽可能的往左走。如果往左的路没了，但是还有往右的路的话，那我们还要继续往右走。用这种方式找到最下面的一个叶子节点，它就是第一个被后序遍历的结点。好，再看最一种情况，如果P它没有父结点，它是根结点的话，那么后序遍历左右根的这种规则就决定了根结点一定是最后一个被访问的。因此这种情况下P是没有后继的。

好的，这个小节中我们介绍了在中序先序和后序线索二叉树当中找后继和找前驱的方法。这给了一个总结，内容很多，其实不需要被全部把它忘掉。我们重要的是要理解分析的这个过程，在考场上你能够临时推出来你想要的结果。

好，最后再进行一个总结，对于中序线索二叉树，想要找前驱和找后继都是可以的。所以在这种线索二叉树当中，给你一个指定节点，让你往前遍历或者往后遍历，一定都是可以实现的。而对于先序线索二叉树来说，我们找后继是可以的，但是找前驱不行。除非采用三叉链表可以找到指定节点的父节点，这种情况下是可以找到它的前驱的。

或者还有一种方法就是从头开始进行一次先序遍历，用这种方式找前驱。后序线索二叉树找前驱是可以的，找后继是不行的。可以找到后继的情况就是这儿提到的这两种情况。所以在先序线索二叉树当中给你一个指定节点，就可以从这个指定节点开始进行一个先序遍历。而对于后序线索二叉树来说，给你一个指定的节点，你只能从这个节点开始进行逆向的后序遍历。因为我们只能找到前驱，而不能找到后继。

好的，最近的几节我们介绍的是线索二叉树，考点无非就是怎么进行线索化，还有怎么在一个已经被线索化的线索二叉树当中找前区或者找后继。怎么线索化？这个问题最常考的还是手算。当然代码的实现其实也很简单，它无非就是进行一次中序或者先序或者后序遍历。大家还需要通过课后习题进行进一步的巩固。好，以上就是关于线索二叉树相关的所有内容。