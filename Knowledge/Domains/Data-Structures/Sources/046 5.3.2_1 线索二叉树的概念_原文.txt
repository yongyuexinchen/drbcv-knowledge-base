046 5.3.2_1 线索二叉树的概念_原文

各位同学大家好。在这个小节中我们会学习一个比较难也比较重要的一个考点，叫线索二叉树。我们会先用一个例子让大家体会线索二叉树和普通的二叉树相比它有什么作用。然后我们会介绍线索二叉树它的存储结构应该怎么实现，并且会介绍三种线索二叉树。

好，首先来看一下线索二叉树的作用。我们从这样的一个问题出发，我们之前学过普通的二叉树，我们可以对它进行中序遍历、先序遍历和后序遍历。对这棵二叉树进行中序遍历，应该可以得到这样的一个遍历序列。D是第一个被访问的节点，这是第二个被访问的节点。

经过中序遍历之后，本来这些数据元素之间他们呈现的是一个非线性的关系。但是从得到的遍历序列这个角度来看，它们之间其实是一种线性的关系。每一个元素有一个与之对应的前驱，还有一个与之对应的后继。当然和线性表一样，第一个元素是没有前驱的，最后一个元素是没有后继的。注意我们刚开始在讲树的时候，我们说过一个节点它有一个唯一的前驱，并且可能会有多个后继。这是从树本身的逻辑结构出发所定义的前驱和后继。但是刚才我们所说的一个数据元素，它的前驱和后继是基于它的遍历序列来定义的前驱和后继，所以不要和之前的前驱后继的概念相混淆，这是基于遍历序列所定义的。

好，那现在思考这样的一个问题，每当我们要对一个二叉树进行中序遍历的时候，是不是都得从根结点出发，只有我们知道根结点是哪一个，才有可能对整棵二叉树进行中序遍历？好，那现在的问题是，有没有可能在某些场景当中，我们只能知道某一个节点的指针，比如说G节点的指针，也就是说我们并不知道根结点是哪一个。那现在的问题是，如果只给你G结点的指针，你还能不能从G节点出发，对这棵树进行中序遍历呢？也就是遍历GBEAF这些节点，那显然这种操作是完成不了的。因为G节点只有指向它孩子的指针，并不可能从G节点往回找，找到他的双亲或者直接找到它的后继结点B好，所以这是普通的二叉树存在的第一个问题。就是要对它进行遍历的时候，我们每一次都必须从根结点出发，否则是完成不了中序遍历的那可以回忆一下线性表，假设这些元素它们是一个线性表的话，那给你其中任意一个元素的指针，你肯定都能找到这个元素后面的所有的元素。也就是说你对线性表的遍历并不是每一次都必须从头开始遍历的。你可以从任何一个元素开始往后遍历，但是对于二叉树来说，我们现在是做不到这样的事情的，我们每一次都只能从头开始遍历。

好，这是第一个问题。接下来思考第二个问题。假设现在给你一个指定的节点，给你这个节点的指针，那你有没有可能找到这个节点它的前驱呢？像F这个结点，它是第六个被访问到的那它的前驱就应该是A这个结点，我们这儿指的是中序遍历序列当中的前驱。好，如果只给你F这个结点的指针，那你肯定没办法找到它的前驱A因为每一个结点只拥有向下一层的指针，不可能向上找，对吧？这是找前驱的问题。

那如果要找到一个节点的前驱，我们可以这么做。我们要找的不是中序遍历序列当中的前驱吗？那我们可以对这整棵二叉树再进行一次中序遍历，也就是从根结点出发。然后在进行中序遍历的过程当中，我们用一个指针Q来记录当前被访问的节点，并且用一个指针pre用来记录当前这个结点的前驱。

中序遍历的代码当中，当我们在访问一个节点的时候，是不是会对这个节点执行visit操作。当前正在visit的这个节点其实就是我们这儿的Q中序遍历过程中，D结点是第一个被visit的结点。所以刚开始会让Q指向D而pre是指向none的。因为D这个结点它是没有前驱的，所以它的前驱指针现在指向no。好，现在我们可以对比一下，Q和P并没有指向同一个结点，所以我们还需要继续往后visit。在下一个结点被visit之前，我们需要把pre指针指向当前Q所指向的这个结点，然后Q指针指向下一个被visit的结点。好，现在pre指针所指向的这个结点就是Q指针所指向节点的中序的前驱。所以用这样的思路，我们可以不断的让Q指向后一个被访问的结点，然后pre指针也跟着依次的往后移，一直到这样的一个时刻，也就是Q和P指向了同一个结点，那么就说明此时pre所指向的结点就是P所指向结点的前驱，对吧？

好，所以在一棵普通的二叉树当中，我们要找到一个指定节点的前驱，我们只能从头进行一次完整的中序遍历才可以找到它的前驱。那如果要找到结点P的后继的话，是不是原理也是类似的。我们只需要让Q再往后移一次，也就是说我们判断是否找到的条件，我们可以判断P和P它到底是不是指向同一个结点。当它们指向了同一个结点的时候，Q所指向的结点就是P结点的后继。好，所以在一棵普通的二叉树当中，我们要从某一个节点开始往后遍历是很不方便的。同时如果指定一个节点要找它的前驱和找它的后继，我们必须从头开始重新进行一次完整的中序遍历，才可以找到它的前驱和后继，因此这些操作很不方便。所以如果在某些应用场景当中找前驱、找后继还有遍历这种操作会十分频繁的话，那这种普通二叉树的设计就会有一些缺点。所以就有人提出了所谓的线索二叉树。

那来看一下这棵二叉树的中序遍历序列。我们已经知道了我们这儿给各个节点标上了他被访问的一个序号123456。现在我们想要把这棵二叉树稍微改造一下，让它能够更方便地查找前驱，查找后继或者是进行遍历操作。那可以怎么做呢？我们之前说过，对于一棵有N个结点的二叉树来说，它总共有N加一个空链域，也就是这些空指针。我们可以把利用起来，用这些空链域指向各个节点的前驱和后继。什么意思呢？像G这个节点，它是第二个被访问到的那它前一个被访问到的，也就是它的前驱应该是D这个节点。所以我们可以让它的这个左孩子指针让它指向D这样的话我们通过它左孩子指针是不是就可以找到它的前驱了，后继也是类似的，B这个结点是G的后继，所以我们可以让它的右孩子指针指向B这个结点，这样的话我们就可以很方便的从一个节点出发，找到它的前驱和后继。

好，像D这个节点它是第一个被访问的节点，所以它没有前驱。我们可以让它的这个左孩子指针就是指向表示它没有前驱。那E这个结点它的前驱是B所以让它左孩子指针指向B右孩子指针指向它的后继A那F这个结点它的前驱是A所以左孩子指向A后G是C所以右孩子指针指向CC这个结点是最后一个被访问到的，所以它的右孩子指针我们让它指向no表示它没有后继。因此按照中序遍历的序列对这棵二叉树进行所谓的线索化之后，所得到的就是这样的一棵二叉树。那这棵二叉树就是所谓的线索二叉树，或者更准确的来讲，应该把它称为中序线索二叉树。因为它的这些前驱和后继指针，我们制定的规则是按照中序遍历中的前驱和后继来弄的那如果一个节点的左孩子指针和右孩子指针指向的是前驱和后，继而不是它的左右孩子的话，那我们就把这种类型的指针称为线索。指向前驱的就是前驱线索，指向后继的就是后继线索。好，这是线索的概念。

好，当我们把这棵二叉树线索化之后，现在再来看刚才的问题。如果给你一个节点G的指针，怎么找到节点G的后继结点呢？是不是很简单，只需要通过它的后继线索，它的后继线索指向的是B所以它的后继就是B因此线索二叉树想要找它的节点后继是很方便的那既然从一个节点出发能够找到它的后继，找后继很方便的话，那是不是也就意味着从一个节点出发往后遍历这件事情也是可行的？因为所谓的便利无非就是不断的找后继的过程，对吧？好，有同学可能会说你的这个节点G它的右孩子指针本来就是指向它的后继的，所以找后继当然方便了。但是如果对于一个右孩子指针指向的是它的右孩子，而不是它的后继。这样的一个节点要怎么找它的后继呢？这个问题我们留在后面的小节再来探讨。

总之这儿大家只需要知道，当我们把二叉树线索化，把它变成一颗线索二叉树之后，找节点的前驱后继变得更方便，并且遍历也更方便。这大家只需要体会到线索二叉树的这样一个作用就行了，也就是要知道Y建立这些线索的意义是什么？为什么要建立这些线索？

好，那接下来要看一下这种线索二叉树怎么存储它呢？和普通的二叉树相比有什么区别？在普通的二叉树当中，我们只定义了一个数据域data，然后还有左孩子指针和右孩子指针。但是刚才我们说了线索二叉树当中，我们的左孩子指针和右孩子指针，它有可能指向的不是左右孩子，而是指向它的前驱和后继。所以为了区别这样的两种状态，我们就需要增加这样的两个标志位，分别叫l tag和r tag。当l tag等于零的时候，表示的是它的左孩子，指针指向的真的是它的左孩子。而只有tag值等于一的时候，表示的是这个指针是一个线索。像左孩子指针充当的线索指向的就是它的前驱，右孩子指针充当的线索指向的就是它的后继。

在这个图当中我们用虚线表示线索，用实线表示左右孩子指针。黄色的虚线指的是前驱线索，紫色的虚线指的是后继线索。之前我们说过二叉树，我们也可以把它称为二叉链表。现在我们把它改造成线索二叉树的这种节点之后，我们可以把这种线索二叉树称为线索链表。好，所以这样的一棵中序二叉树如果从存储的视角来看的话，就是呈现出这个样子。只有当tag位为一的时候才表示与之对应的这个指针是一条线索。Tag位为零的时候表示的是与之对应的这指针指向的是它的孩子。

好，这是中序线索二叉树。也就是说我们是根据中序遍历的序列来确定每一个节点它的前驱和后继到底是谁。那既然有中序线索二叉树，是不是就应该有先序和后序线索二叉树？那原理是类似的，对于先序线索二叉树来说，我们同样的可以先在稿纸上给这些节点分别标上他们被访问的次序，ABDGECF, 这是先序遍历序列它的一个次序。好，现在我们可以按先序序列来确定各个节点它们之间的前驱和后继的关系，并且把这N加一个空链域，把它们变成线索。经过线索化之后所得到的先序线索二叉树就是这个样子。

其实原理和中序线索二叉树是一样的，只不过各个节点之间前驱后继的这个顺序会有所改变。像D这个节点，它的前驱是B而它的左孩子指针本来是空的，所以我们可以把它的左孩子指针变成现线索指向它的前驱，也就指向B结点。那G它的前驱是D所以我们让左指针指向D后继是E所以然后右孩子指针指向E其他就不再细聊。

总之最后我们可以得到这样的一颗先序线索二叉树。这是从逻辑视角看到一个样子，如果从存储视角来看的话，那它就应该是这个样子。还是一样的。只有tag值为一的时候表示这个指针它是一个线索，tag值为零的时候表示与之对应的这个指针，它是指向了孩子。好，这是先序线索二叉树。

最后再看后序线索二叉树，原理和之前一样，只不过我们是按照后序遍历所得到的这个遍历序列来确定各个节点它们的前驱和后继的关系。G这个节点是第一个被访问到的，所以它没有前驱，因此我们需要把它左孩子指针变成它的前驱线索。而由于它没有前驱，所以这个前驱线索应该让它指向，而它的后继应该是D这个节点，所以让它的右孩子指针指向D那D的左指针是空的，所以可以让左指针变成线索，让它指向它的前驱，也就指向G好，以此类推，其他的就不再列举。最后经过后续线索化之后，所得到的后续线索二叉树就是这个样子。好，如果从存储视角来看的话，这科后序线索二叉树就会呈现出这个样子。

好的，这个小节我们介绍了什么是线索二叉树，其实就是利用了二叉树本身所存在的N加一个空链域，把这些空链域把它们变成线索，也就是指向前驱或者指向后继。我们介绍三种线索二叉树，分别是中序、先序和后序线索二叉树。它们的区别在于我们在确定各个节点的前驱后继关系的时候，到底是按中序遍历还是先序遍历，或者是后序遍历来确定的那这儿还要补充几个大家可能在选择题中会看到的概念，比较容易混淆。

中序前区，中序后继。当我们在聊到一个节点，它的中序前屈的时候，其实指的是如果按照中序遍历这棵二叉树，那么在中序遍历序列当中，它的前驱到底是谁？所以像E这个结点，它的中序前驱就应该是B相应的它的中序后继就应该是A好，那先序后继也是类似的，就是指在先序遍历的过程当中，我们指定的这个节点，它在先序遍历序列当中的前驱到底是谁。那在先序线索二叉树当中不难发现，它的先序前区就是G而它的先序后继就是C好，后序前驱和后序后继的概念也是类似的，这就不再赘述。

当我们把一棵二叉树线索化之后，我们就可以很方便的找到一个指定节点的前驱，或者找到它它的后继。能够找到前驱后继的话，就意味着我们也可以很方便的从一个指定节点出发进行遍历操作。与普通的二叉树结点相比，我们需要在线索二叉树的这个结点的数据结构当中增加ARTAG和r tag这样的两个标志变量。当tag值为一的时候，表示与它对应的这个孩子指针是一个线索，而不是指向孩子。只有tag值为零的时候，才表示孩子指针指向的真的是孩子。

那一个比较重要的考点是大家需要能够手算来画出一颗线索二叉树。当然线索二叉树分为中序、先序和后序线索二叉树，所以在审题的时候一定需要注意那用手算的方式。线索化其实也很简单，我们先根据它的遍历规则确定各个节点被访问的顺序。然后像我们刚才那样，就是给各个节点边上一个它的访问编号，接下来再把那N加一个空链域，把它们分别连上前驱和后继，也就是完成线索化。这是选择题当中有可能经常会考到的一个点，当然这个点并不难。好的，以上就是这个小节的全部内容。