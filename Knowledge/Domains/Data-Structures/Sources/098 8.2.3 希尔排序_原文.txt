098 8.2.3 希尔排序_原文

好的，这个小节中我们会学习第二个算法，就是希尔排序。因为这个算法是一个叫希尔的人发明的那这个算法其实是基于上小节中学习的插入排序进行了一个优化。之前我们说当我们采用直接插入排序算法的时候，如果说你的这些数据元素原本就是有序的。那么在这种情况下，我们的直接插入排序可以得到很不错的一个执行的效率。或者我们再把条件放宽一点。如果你的这些元素能够保证基本上有序的话，那这种情况下直接插入排序的效率也会很不错。比如说像下面这个例子当中，当我们在处理30这个元素的时候，由于30要比20更大，所以我们不需要移动任何的元素。而当我们处理十这个元素的时候，我们只需要把20和30都分别往后移，然后把十放到一这个位置就可以，也就是变成12 13 10这样的一个状态。那后续再处理50、六十这两个元素的时候，也不需要移动任何一个元素。只有再往后处理到40的时候，我们才需要把50和60往后移，然后把40移到50原有的这个位置。

好，总之如果我们能够像第二个例子这样，能够保证各个元素之间它们基本上有序的话。那在这种情况下，我们的直接插入排序的那种策略也可以得到很好的执行效率。所以希尔排序的思想就是基于这样的一个考虑。我们会先追求表中的元素部分有序，然后再逐步的逼近全局的有序。

好，这个看起来很抽象，我们直接看一个例子来理解。我们会把表里的元素把它拆分成一个特殊的子表，怎么拆分呢？就是说在每一趟的处理当中，我们都会设置一个增量地。然后把相距距离为D的各个元素，把它们看作是一个特殊的子表。然后我们会把这些子表当中的各个元素进行直接插入排序。

好，我们结合动画来看一下是什么意思。假设我们第一趟的排序，我们设定这个增量D的值是4，也就是用元素的总数除以2。那总共有八个元素，所以8除以二等于4，这是第一的循环。我们设置的增量D的值，所有的相距距离为D的这些元素，我们都会把它看作是属于同一个子表的元素。也就是说对于49这个元素和它相距距离为四的元素，应该是1加4，也就是五这个元素，也就是七十六。而对于38这个元素和它相距为四的元素，应该是六这个位置，也就是十三。那后面的也是一样的，65应该和27把它们看作是同一个子表，而97和49看作是同一个子表，也就是这个样子。好，总之在第一趟的处理当中，我们会把相距距离为四的这些元素看作是同一个子表当中的元素。

接下来我们要做的事情是要对各个子表分别进行直接插入排序。对于第一个子表当中的这两个元素，它们本来就是有序的，所以直接插入排序执行的结果不会导致这两个元素的顺序有任何的改变。而子表二当中的两个元素，38要比十3更大。所以在对子表二的这两个元素进行直接插入排序之后，我们最终得到的结果应该是13放到前，38放在后面。子表三也是一样的，对这些元素进行直接插入排序之后，我们会让小的那个元素放在前面，大的放在后面，最后的子表嗣就是这个样子。好，这样我们就完成了第一趟排序的工作。我们把各个子表当中的这一些元素分别进行了直接插入排序。所以第一趟处理之后，这个数组当中各个元素的位置应该是这样的。

好，接下来进行第二趟的处理。第二趟的处理我们会缩小这个增量D的值，我们会在之前第一的基础上除以2。之前第一是四，所以4除以二等于2。因此第二趟的处理当中，我们会把相距距离为二的那些元素，把它们划分为同一个子表。对于一号元素与它相距距离为二的应该是3，那三再往后两个位置应该是57。所以49277665这几个元素组成了一个子表。那剩下的2468这几个元素，它们之间的相距距离也都是2。所以剩下的这些元素又会组成第二个子表，也就是这个样子。

好，接下来的处理还是和之前一样，我们会对各个子表分别的进行直接插入排序。对于第一个子表，我们对这四个元素进行直接插入排序，最终得到的结果应该是这个样子。第二个子表进行直接插入排序，最终得到的结果应该是这个样子。好，到此为止我们完成了第二趟的处理。表里的元素总体来看就是变成了这样的一个排列的顺序。

好，接下来第三趟的处理，我们会让这个增量的值继续的缩小，在第二趟的基础上除以2，也就是变成一。这就意味着所有的这些元素都会被划分为同一个子表。所以最后的这一趟的处理其实就相当于对所有的这些元素进行一次总体的直接插入排序。而这个时候其实经过之前的两趟处理，我们在这儿得到的这个元素序列其实已经达成了一个所谓基本有序这样的一个比较理想的状态。好，那接下来对所有的这些元素进行直接插入排序，得到的结果就应该是这样的。好，所以我们经过三趟的处理之后，就可以得到一个全局递增有序的一个排列。再把刚才的这几个过程给全部罗列一下，这是最初的一个状态。

在低碳处理当中，我们把相距距离为四的这些元素，把它们看作是属于同一个子表当中的元素。对所有的这些子表内部我们都会进行一次直接插入排序，得到的结果是这个样子。接下来在低碳处理的基础之上，我们会进一步的缩小增量D的值。所以在第二趟的处理当中，我们会把相距距离为二的这些个元素，把它们看作是同一个子表里的元素。把这些子表当中的元素进行一次直接插入排序，就可以得到这样的一个状态。好，最后基于第二这趟排序得到的这个结果，在第三趟的排序当中我们会让增量D的值变为一。

也就是所有的这些元素，我们都把它看作是从同属于同一个子表。对这个子表当中的这些元素进行直接插入排序，最终就可以得到一个全局的有序序列。好，这就是希尔排序的一个大致的实现思想。

在这个例子当中，我们选用的增量序列D是421这样的一个就是每一次缩小一半这样的一个序列。这也是希尔本人他自己建议的一种增量序列的一个选取方式。第一趟的处理中增量D我们可以选元素的个数除以二这样的一个值。然后在之后每一趟的处理当中，都会让增量D的值每一次减半，直到最后D等于一为止。

我们考研当中对于希尔排序的考察，通常也会出现其他各种各样的一个增量序列。比如说我们可以规定第一趟我们选择的这个增量D的值是3，第二趟选择一好。在这种情况下，在第一趟的排序当中，我们会把相距距离为三的这些元素看作是从属于同一个子表的元素。像这样我们不透明的这几个元素就是从属于同一个子表，对它们进行直接插入排序，得到的结果应该是这样的。好，接下来处理第二个子表当中的元素，对它们进行直接插入排序应该是这样的一个结果。好，接下来第三个子表只有这样的两个元素，对它们进行直接插入排序，得到这样的结果。好，那这就是第一趟的处理所得到的一个结果。

接下来按照要求第二趟的处理我们选择增量的值等于一，也就是对整体进行一次直接插入排序，最终就可以得到一个整体有序的序列。所以第二趟的处理结束之后，得到的状态就是这样子。这也是考试当中关于希尔排序最常考的一种考法。就是会给你一个特定的元素序列，然后告诉你说每一趟的这这个增量到底是多少，然后让你判断每一趟的排序之后所得到的这个序列是什么样的一个状态。类似的题目大家会在课后习题中遇到。

好，接下来我们来看一下希尔排序怎么用代码具体实现。我们会定义这样的几个辅助变量，一个变量是D它就是当前这一趟处理当中的增量。初始时在第一趟的处理当中，我们会让刚开始的这个增量等于元素的个数除以2，也就是四这样的一个值。好，接下来进行第一趟的处理，I等于D加1，也就是说I刚开始会指向4加1等于五的这个位置，是不是好奇怪？指向这个位置等于原因是因为我们在处理第一个子表的时候，按照直接插入排序的规则，其实我们只需要从第二个元素开始，就是检查这个元素是否需要把它插到前面就可以了。

好，所以刚开始我们会让I指向第一个子表当中的第二个元素这个位置。那接下来按照直接插入排序的规则，我们是不是需要对比当前指向的这个元素，还有它前边的这个元素，它们之间的大小关系。由于当前指向的元素要比49更大，所以我们并不需要改变76这个元素的位置，也就是这个if条件不满足。好，那接下来会进行第二轮的for循环，我们会让I的值加加，也就是变成I等于6。

现在我们要处理的是第二个子表，由于当前指向的这个元素要比它前边的这个元素要更小，所以我们会把当前指向的元素的值放到零这个位置。然后接下来更深层的这个for循环做的事情是我们会检查当前这个元素前边的这些元素是否要比当前处理的元素更大。如果更大的话，那么我们就会把J所指的这个元素给它后移。当然这个元素的后移并不是移到J加一这个位置。而是应该移到对于这个子表来说的后一个位置，也就是应该移到J加D这个位置。所以会把38移动到6这个地方。

好，接下来根据这个for循环的规则，我们会让J等于J减D其实也就是要检查当前这个子表当中前边还有没有其他的元素需要处理。好。由于此时D的值是4，所以J减D应该是等于负二这样的一个值。那到这一步J的值已经不大于零了，所以这一层的for循环会被跳出。接下来要执那是这一句，也就是会把刚才我们保存下来的这个数值放到J加D的这个位置，所以这波的处理会把13复制到2。好，这样的话我们就完成了对第二个子表的一个直接插入排序。接下来会继续I加加处理下一个子表。

对于这个子表的处理原理也是类似的。我们刚才已经把这些代码一句一句给分析了一遍，所以就不再赘述。大家自己看动画。

好，接下来继续让I加加处理最后一个子表，同样的我们会对这个子表进行一个直接插入排序。

然后继续I加加。好，那到这一步是不是我第一趟的处理就已经结束了。接下来第二趟的处理，我们会让D的值变为以前的一半，也就是让D等于2。

好，现在我们要对第一个子表进行一个直接插入排序。所以我们的I值刚开始会让它等于D加1。因为我们只需要从这个子表的第二个元素开始处理就可以。好，由于当前处理的元素27要比49这个元素要更小，所以我们需要把四十九往后移，然后把27放到49那个位置。

好，那现在对于这个子表来说，是不是前边的这两个元素其实是已经有序了。按照我们之前学习的直接插入排序，接下来我们应该处理的应该是76这个元素，对吧？但是这个地方的代码实现并不是这么做的。这儿我们会让I的值继续加加，也就是变为四。所以接下来我们会切换为处理第二个子表的这个元素。好，由于49要比13更大，这个if条件不满足，所以我们不需要对49和13的位置进行调整。接下来我们会继续让I的值加加，这样的话我们又回到了之前的那个字表，要处理这个子表的76这个元素。同样的由于不满足这个if条件，所以76这个元素的位置不需要做任何的调整。

继续让J加加，接下来又切换回了另一个子表，要处理38这个元素。由于38比49更小，所以根据直接插入排序的规则，我们需要进行一系列的调整。由于当前元素前边的这个元素值要比38更大，所以我们会把它前边的这个元素往后移一位。好，接下来会让J指向当前子表当中的再往前的一个元素，也就是会指向十三。由于13的值不比38更大，也就是不满足这个for循环的条件，所以接下来会跳出这个for循环执行之后的这一句，我们会把之前保存下来的数据，把它复制到J加D也就是四这个位置，处理之后就是这个样子。好，接下来又回到了这个地方，我们会让I的值继续加加，这就是要处理另外一个子表。处理的方法和之前类似，我们就不再赘述，大家自己看动画。

好，到此为止是不是第一个子表当中的所有元素都已经保证了有序。接下来继续让爱加加处理最后的一个元素。由于这个元素比之前的一个元素要更大，所以我们不需要做任何的处理。那到此为止我们就完成了第二趟的处理，我们保证了这两个子表都是有序的。好，最后我们就可以进行第三轮的处理，继续让D的值缩小一半，也就是D等于一。那之后的处理是不是就变成了我们最熟悉的那种直接插入排序。好，接下来过程就很简单了，最终排序得到的结果就是这个样子，得到了一个整体有序的序列。

好的，这就是希尔排序算法的一个代码实现。其实我们理解一段代码最好的方式就是自己搞一个例子，然后根据这个代码的执行一步一步耐下性子来分析。课本里边给的这种实现方式，我们是每次让I加加，也就是说我们会轮流的切换着来处理不同的几个子表。但是其实对我自己来说，我觉得逻辑更清晰的一种处理方式应该是我们可以连续的把从属于同一个子表的这些元素都一个接一个的把它处理完。然后再来处理另一个子表当中那这些元素。大家也可以自己尝试着实现一下。我们提出的第二种实现思路其实实现起来也不难。并且我认为这样的实现方式可能更符合我们对这个算法的一个理解。

好，接下来我们来探讨一下这个算法的算法性能如何。显然对于空间复杂度来说，我们只需要常数级的一个辅助空间，而对于时间复杂度的分析就要困难得多。比如说刚才我们举的这个例子，我们设计了这样的两种不同的增量序列。第一个增量序列是421，第二个增量序列是31。当我们采用不同的增量序列的时候，我们排序的趟数会受到影响。同时在每一趟排序当中，各个元素的对比，还有元素的移动，这些次数也都会受到影响。所以关于希尔排序这个算法的时间复杂度到底是怎么样的这个问题其实分析起来很困难。到目前为止都没有办法用数学手段来证明确切的时间复杂度。

对了，大家想一下，对于希尔排序来说，如果我们第一趟的这个排序当中，我们选择的增量D的值就直接等于一。那这种情况下算法是怎么执行的？想一下，其实这种情况会导致希尔排序算法退化为直接插入排序，对吧？

好，所以希尔排序在最坏情况下，它的时间复杂度也可以达到和直接插入排序一个数量级。因为就像之前我们说的，我们直接选择让增量D等于一，那它不就是一个直接插入排序吗？另外如果我们的数据元素数量不是特别大，在某一个范围内的话，那希尔排序的这个效率可以达到ON的1.3次方这样的一个数量级。相比于ON的平方其实还是提升了很多的。所以希尔排序比直接插入排序还是要优秀不少。只不过我们没办法用形式化的手段，数学的手段来确切的证明它的时间复杂度是多少。好，这是希尔排序的空间和时间复杂度。

接下来我们来看一下这个算法的稳定性如何。来看这样的一个比较简单的例子。假设我们原始有这样的一个序列，然后第一趟的排序当中我们选择增量D等于2。也就是说相距距离为二的这些元素，我们会把它划分为同一个子表。也就是说65和49这2个元素我们把它划分在同一个子表当中。对这个子表进行直接插入排序导致的结果就是49这个元素跑到了前面，65放到了后面。

好，接下来第二趟的排序，我们让增量D的值等于一，最终排序的结果就变成了这个样子。可以看到在原始序列当中，我们有下划线的这个49，它应该是在右边的。但是经过希尔排序之后，有可能它会到左边这个位置，所以这个算法显然是不稳定的那另一个方面，希尔排序的实现我们只能基于顺序表。因为我们需要用增量D来快速的找到与之相邻的从属于同一个子表的各个元素。所以必须要有随机访问的特性才可以实现这样的一个事情。因此希尔排序只能基于顺序表来实现，不可能基于链表来实现。

好的，这个小节当中我们学习了希尔排序这个比较神奇的算法。算法的思想就是我们要把整个序列把它划分成一个特殊的子表，然后对各个子表进行直接插入排序。每一趟的处理之后，我们都需要把增量D的值不断的减小，直到最后D等于一为止。在这个时候所有的元素都会被划分在同一个子表当中。

对于希尔排序的考察，通常会给出一个特定的例子，并且给大家一个增量序列，然后让大家分析每一趟的排序过程是怎么样的，排序之后的状态又是什么样的。而关于代码的考察频率不是特别高。当然了其实只需要理解了这个算法的一个实现的思想之后，我们考试的时候临场写代码应该也是难不住大家的。其实所谓的代码无非就是你对处理问题的逻辑，用计算机语言把它描述一遍，仅此而已。所以核心还是要理解这个算法的一个运行的逻辑，只要算法的逻辑捋清了，那代码其实就是逻辑的一个外化体现而已。好的，以上就是这个小节的全部内容。