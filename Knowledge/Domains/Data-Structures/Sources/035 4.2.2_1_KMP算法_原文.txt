035 4.2.2_1_KMP算法_原文

各位同学大家好。在上个小节中我们学习了朴素模式匹配算法。在这个小节中我们会基于上一小节提出的算法进行一个优化，从而得到KMP算法。好，这个算法的名字看起来也很吓人，但其实并没有什么它叫KMP的原因。只是因为发明这个算法的三个人，他们的名字分别带有KMP，所以叫KMP算法。好，所以不要被这个名字所吓到，其实它就是基于朴素模式匹配算法进行一个优化而得来的。

好，我们先回顾一下朴素模式匹配算法的思想。这个算法很简单，就是我们会把主串当中所有的和模式串长度相同的子串，都依次的和模式串进行一个对比，直到找到第一个完全匹配的子串为止。好，如果当前尝试匹配的这个子串在某一个位置匹配失败了，那么我们就会转而匹配下一个子串，并且是从下一个子串的头部位置开始尝试着往后匹配。对于现在这个图里面表示的这种情况来说，我们就是会转头来匹配以二号字符开头的这个字串。

那为什么我们会回头来匹配二号字符开头的子串呢？因为我们在主串里边的这些字符它到底是什么，我们并不知道，对吧？所以没办法，我们只能从这个子串的第一个字符开始依次往后匹配。好，如果此时二号字符开头的这个子串在某一个位置，同样也是匹配失败的话。那同样的原因，由于主串当中所有的这些字符我们都不知道，因此我们接下来只能尝试着匹配以三号字符开头的这个子串，并且我们只能从第一个字符开始依次往后匹配。因为主串里边包含了什么内容我们不得而知，对吧？这是朴素模式匹配算法的一个想法。

好，但是现在我们换一个思路，再把刚才的过程捋一遍。对于这个主串，如果第一个子串我们匹配到最后一个字符才发现它匹配失败。那么主串当中前边这些字符一定是和模式串对应的对吧？有没有想过这个问题？好，因此我们匹配到了第六个字符才发现它匹配失败。那这种情况下，其实主串前边这些字符的内容我们是已知的，而不是位置的，和模式串肯定是保持一致的，不然我们不可能匹配到第六个字符才适配，对吧？好，所以我们应该利用好这部分的信息，当模式串的某一个字符不匹配的时候，那么在主串的前边这些字符一定是和模式串保持一致的。因此主串里边的这些字符信息，虽然刚开始我们不得而知它里边包含了哪些字符，但是通过模式串的部分匹配，我们可以确定主串里边前边这几个字符到底是什么。

好，根据朴素模式匹配算法的思想，如果当前的这个子串匹配失败，那么我们只能开始匹配下一个子串，对吧？也就是这个子串。但是现在我们已经知道了这个子串前边几个字符的信息，而你会发现这个子串它的第一个字符和模式串就已经不匹配了。所以当前的这个子串其实根本没有必要再进行一个检查和匹配，我们可以直接跳过。

好，再来看下一个子串。现在的这个子串我们也知道了它前边几个字符的信息。第一个字符可以匹配，第二个字符不匹配，对吧？好，所以这个子串其实我们也没有必要检查，根本没有必要检查。

那么我们再看下一个子串，现在的这个子串我们同样的知道它头部的几个字符的信息。好，这个字符和模式串能够匹配，第二个字符和模式串也能够匹配。而现在主串里边这个字符到底是什么，我们还不得而知，对吧？好，所以对于现在的这个子串来说，前边两个字符肯定是匹配的，而第三个字符是否能够匹配还暂时不知道。因此对于现在这个子串来说，我们从这个位置开始检查，往后匹配就可以了。

好，跟上这个思路我们再来捋一遍。对于这个模式串T来说，当我们匹配到某一个字符，这个字符发生了失配的时候，那么这个字符之前的这些主串的信息我们是可以确定的，它和模式串一定是保持一致的。好在这个时候我们就没有必要再检查以二这个元素开头的子串，也没有必要检查以三这个元素开头的子串，而是可以直接对比以四开头的那个子串。而对于这个子串来说，我们也没有必要对比第一个元素和第二个元素。因为我们可以确定这两个元素肯定是和模式串能够匹配的。

现在暂时还不能确定的是我们的模式串第三个元素和主串里边刚才适配的这个元素能不能够匹配得上。因此对于这个模式串ABAABC来说，如果我们发现第六个元素匹配失败，那么我们可以让主串的指针I保持不变。这个I依然是指向刚才适配的那个元素，然后模式串的指针J让它等于3，也就是从模式串的第三个元素开始依次的往后匹配。好，因此可以看到我们利用好模式串本身带有的这个信息，可以跳过中间的好几个没有必要的对比。这样的话就可以使算法的效率得到提升。我们是直接跳过了中间的几个子串的对比，同时对于现在的这个子串来说，我们也跳过了开头的两个字符的对比，直接从第三个字符开始往后比较就可以了。好，其实这就是KMP算法的思想。

要利用好模式串本身隐含的一些信息，然后我们要确定模式串的某一个元素发生失配的时候，接下来我们应该如何处理。显然我们得到的这个结论它并不依赖于主串，只和模式串有关，和我们匹配的是主串的哪个位置并没有关系。好，我们再来验证一下，如果我们是从主串的第五个位置开始尝试着匹配，前面这些是什么我们都不知道。好，那么同样的，我们匹配到当前这个子串的最后一个元素的时候，发现这两个字符失配，那是不是一样的？前边这几个字符肯定和模式串是保持一致的。好，所以前面这些字符的信息已知，那根据刚才得到的结论，当第六个元素发生失配的时候，指针I保持不变，指针J让它变为三。也就是从这个模式串的第三个位置开始往后匹配，也就直接跳过了中间的几个子串的对比。然当前这个子串也是直接从第三个元素开始往后匹配。因为刚才发生失配的这个元素，它到底是什么内容我们不得而知。因此它有可能和我们的模式串第三个元素能够匹配得上，有这个可能性。

好，所以这个地方想跟大家强调的是，我们刚才得到的这个结论是具有通用性的。它只和模式串的内容有关系，和主串的内容以及主串里边我们匹配到了哪个位置并没有任何关系，它只依赖于模式串。好，目前为止我们探讨的是这个模式串的第六个元素。第六个元素发生适配的时候，我们应该怎么办？接下来顺着这个思路，我们再把其他的情况给考虑一下。

如果第五个元素发生失配了，那我们应该如何处理呢？好，来看一下现在第五个元素发生失配，那么前边的四个元素的信息我们是可以确定的，它和模式串肯定保持一致。好，那当前的子串匹配失败的话，下一个子串能不能匹配上呢？显然不行。第一个元素就匹配失败对吧？好，那这个子串也匹配不上的话，再看下一个子串，下一个子串能匹配上吗？显然也不行，你看这个位置是匹配不上的，这个子串不匹配，我们就只能再检查下一个子串。

接下来的这个子串你会发现它前边这些已知信息和我们的模式串是可以匹配得上的。而刚才发生失配的这些字符，它到底是什么内容，我们暂时不得而知。所以接下来我们可以从这个位置开始依次的往后检查就可以了。

好，所以得出结论，对于这个模式串ABAABC来说，当第五个元素发生失配的时候，我们可以让指针I主串的指针I保持不变，然后让模式串的指针J等于2，直接从模式串的第二个元素开始依次往后匹配。好，这是第五个元素适配的时候我们应该做的事情。好，继续探讨。如果第四个元素匹配失败，那么我们应该怎么做？现在第四个元素匹配失败，那么前边三个元素的信息我们是可以知道的，它肯定和模式串保持一致。而目前的第四个元素它到底是什么，我们暂时还无法得知。

好，同样的思路，现在的子串匹配不了，那看下一个子串，这个子串和模式串肯定是匹配不了的。第一个元素就对不上，那只能再检查再下一个子串，再下一个子串。目前就我们已知的这些信息而言，是可以和模式串匹配得上的，是可以和模式串匹配得上的。好，所以接下来的这个子串，我们从J等于二的这个位置再往后匹配就可以了。好，因此得出结论，当第四个元素发生失配的时候，我们让主串的指针I不变，让模式串的指针J等于2。好，这是第四个元素适配的时候我们要做的事情。

接下来再看第三个元素适配的时候怎么搞第三个元素适配，前两个元素的信息我们是已经知道的。好，当前的子串匹配失败，再看下个子串有没有可能匹配成功，不可能是吧？这两个元素就对不上。好，所以第二个子串我们没必要匹配，直接跳到第三个子串。好，因此第三个元素适配的时候，我们让主串的指针I不变，让模式串的指针J等于一就可以，然后继续往后匹配就可以。

好，接下来再来看，如果是第二个元素适配，那么怎么办？第二个元素适配，那么我们只能知道第一个元素的信息。好在这种情况下，刚才适配的元素到底是什么我们不知道，所以我们可以尝试着把下一个子串和模式串进行一个对比。好，因此当第二个元素发生失配的时候，主串的指针I保持不变，模式串的指针J等于一，然后再依次的往后对比就可以了。

好，最后一种情况，如果是第一个元素都直接匹配失败的话应该怎么办？这个时候我们只能尝试着匹配下一个子串，对吧？好，所以接下来我们要匹配下一个子串。

那在代码的角度我们可以怎么处理呢？刚才我们任何一个元素发生失配的时候，我们是不是都是让I保持不变，然后J的值让它等于一个某个值，对吧？J是等于某个值。好，未来写代码的时候方便，我们在这个地方可以保持和之前的处理逻辑类似，我们是不是可以先让J等于0，也就是指向模式串的第一个元素前边的一个位置，先让J等于0，然后I和J统一的加加统一的往后移。好，接下来我们再继续对比后面这个子串就可以了。

好，我们再把刚才分析的这6种情况进行一个整合。对于模式串ABAABC来说，每一个元素发生失配的时候，我们应该如何处理这个情况，我们都已经完全考虑进去了。好，利用这个结论，我们再把上一小节讲过的例子再来跑一遍。刚开始我们从主串位置为一的这个地方开始往后匹配。好，第一个元素可以匹配，第二个元素可以匹配，第三个元素可以匹配，第四个可以，第五个可以，第六个发现失配匹配失败。

好在这个情况下，按照上一小节学习的朴素模式匹配算法的思想，我们会让主串的指针I回溯，让主串的指针I回溯到下一个子串的起始位置，并且让J等于一对吧？之前我们是这么做的，但是现在我们已经有了刚才的结论，我们不用再这么搞。现在是第六个元素匹配失败，那我们让主串的指针I保持不变，然后让模式串的指针J等于三就可以。好，所以接下来我们干的就是这样的一个事情，直接跳过了中间好多字符的对比，然后从这个位置继续开始往后对比。好，现在I和J所指的元素可以匹配，依然可以匹配。到此为止是不是就直接匹配成功了？

优化之后我们的主串指针不需要再回溯，我们中国不是有一句话叫好马不吃回头草吗？那一个好的模式匹配算法，这个主串的指针I也不应该往回走。好，可以看到利用刚才我们分析模式串得到的这些结论，可以使得模式串和主串匹配的过程当中效率大幅度的提高。我们说过在模式匹配的应用场景当中，通常来说模式串是很短的非常短，而主串可能会非常非常的长。如果主串很长而模式串很短的话，用刚才我们的这种策略先分析模式串里边隐含的这些信息，然后再去进行模式匹配，就会显得非常的高效。

好，为了让大家更好的体会这种做法的一个威力，我们再把刚才的例子改一下，就是把第五个元素五个元素改成C好，在这种情况下，模式串和主串前面这些都是可以匹配的，直到第五个元素发生适配。而我们知道第五个元素适配的时候，让模式串的指针J等于二就可以。所以接下来我们从这个位置开始往后匹配。好，现在J等于二的这个位置和I所指的元素依然会发生失配，也就是第二个元素匹配失败，那这种情况下I不变，J等于一好，现在I和J所指的元素还是失败。那在这种情况下我们让J等于0，然后让I和J统一的加加，也就是移到了下一个子串开始进行匹配。好，接下来这个子串第一个能够匹配，第二个不匹配，那这个时候J等于一，I保持不变。

好，继续。I和J所指的元素能够匹配。第三个元素发生了失配，此时让J等于一，I保持不变。好，接下来继续尝试匹配，I和J所指的元素发生适配，而现在是第一个元素适配，因此让J等于0，然后I和J统一的加加。好，接下来继续让I和J所指的元素尝试着匹配，那你会发现后边这些元素是全部可以匹配的。最终算法会因为J超出了这个模式串的范围而停止。好，此时我们就完成了模式串的匹配工作。好，从这个例子中我们可以感受到整个过程当中主串的指针I不再回溯，不需要走回头路。这样的话相比于朴素模式匹配，算法效率就得到了大幅的提升。

好，那刚才我们说的这些逻辑如何用代码用程序代码来实现呢？其实我们刚才分析模式串得到的这些结论，是不是可以很方便的用数组来表示这个信息，我们把这个数组称为next数组。当第六个元素发生失配的时候，I是保持不变的，然后J的值要等于3。那我们可以让next 6的值等于3，就是表示第六个元素适配的时候J的值应该修改为多少。好，然后第五个元素适配的时候，J的值要修改为二，所以我们让next 5等于2。好，所以这个next数组是指明了当我们在某一个位置发生失配的时候，应该把J的值修改为多少应该把J的值修改为多少。好，比较特殊的是当第一个元素就发生适配的时候，刚才我们说是要先把J的值修改为零，修改为零，然后再让I和J统一来加加。

好，所以用代码表示刚才描述的逻辑。首先我们应该对比I和J所指的主串元素和模式串的元素是否相等。如果不相等，那么我们应该让J的值修改为next j把J的值修改为next j因为J表示的是当前匹配到了第几个元素，而next j刚好就是说当前匹配到的这个元素。如果适配，那我们应该把模式串的指针指向哪儿，对吧？好，所以用这样的一句代码就可以实现刚才所说的这一系列逻辑。比较特殊的是第一个元素匹配失败的时候，我们除了要把J的值修改为零之外，还需要让I和J同时加加。好，所以这个逻辑用一个if语句是不是就可以实现？

好，所以这就是所谓的KMP算法。是的，这就是考研数据结构当中难度可以排名前三的算法，并没有特别可怕对吧？那通过之前的分析可以感受到这个所谓的next数组它的值这个数组的值是和我们模式串的值是相关的。但是它和这个很长的主串没有任何关系。因此KMP算法的整体流程就是在进行模式匹配之前，需要先进行一个预处理。要分析模式串，然后求出和模式串相对应的这个next数组，然后再利用next数组来进行模式匹配。整个匹配的过程，主串的指针I是不需要回溯的。好，现在我们暂时不管next数组是怎么用代码来求的，反正经过之前的分析，我们用手工的方式也可以求出一个模式串的next数组。

好，现在我们先来关注后面这个模块，如何用代码来实现这个代码。很简单，这是KMP算法的模式匹配部分，要传入主串的值S和模式串的值T以及和模式串相对应的这个next数组。然后我们从主串的一和模式串的一这个位置依次开始往后匹配。如果说主串的当前元素和模式串的当前元素相等的话，也就是能够匹配的话，让I和J同时加加。另外刚才我们说过，当J等于零的时候也需要让I和J同时加加。好，如果不满足上面这个条件，说明I和J当前所指的元素是不匹配的，发生了失配的现象。那这个时候我们让J等于next j就可以。

好，我们把KMP算法的模式匹配这个部分的代码和上一小节的朴素模式匹配算法进行一个对比。你会发现改的地方也就是我们圈出来的这两个部分而已，以及要传入一个next数组。有了这个next数组之后，当主串和模式串不匹配的时候，我们不需要再修改这个主串的指针I不需要让主串的指针I回溯，只需要修改模式串的指针就可以。好，这就是KMP算法和朴素模式匹配算法的区别。从代码的角度来看，改动的地方并不是很多。

好，之前我们分析过朴素模式匹配算法最坏的时间复杂度是O的NM。最坏的情况就是每一个子串都匹配到最后一个字符，然后才发现匹配失败。然后主串的指针疯狂的回溯，回到下一个子串的第一个位置，然后再往后对比，这是最坏的情况。而对于KMP算法来说，整体来看它的这个最坏时间复杂度只会到达O的M加N这个量级。

其中我们求next数组，也就是预处理的部分，时间复杂度是O的M然后有了next数组之后，我们来进行模式匹配。模式匹配的这个过程最坏的时间复杂度只会到达ON这个数量级。因为主串的指针I是不回溯的，主串的指针I会一路高歌猛进不会回头，而主串的长度也就是N这么长，对吧？所以模式匹配的过程最快只会到ON这个数量级。好，所以整个KMP算法预处理的时间OM加上匹配的时间ON然后整体的时间复杂度就只会到达O的M加N这个数量级。可以看到比朴素模式匹配算法要好得多。

好的，这就是KMP算法。首先我们需要根据模式串的值来求出和这个模式串相对应的next数组。然后利用这个next数组进行模式串的匹配。整体的时间复杂度不会超过O的M加N这个数量级。

在我们408考研当中，大家只需要学会如何手动的求next数组就可以。这个部分的内容通常只会用选择题的方式进行考察，考察代码的概率不大。当然大家也需要记住KMP算法的整体时间复杂度，以及预处理的时候需要花多少时间，模式匹配的时候需要花多少时间。能够理解这个算法的大致思想，并且能够用手动的方式模拟算法的执行过程就可以了。好的，那么这个小节讲解结束。