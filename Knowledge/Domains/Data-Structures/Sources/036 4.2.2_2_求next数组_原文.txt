036 4.2.2_2_求next数组_原文

好的，经过上一小节的学习，我们知道了KMP算法的原理。首先我们得根据模式串的值，求出与这个模式串相对应的next数组。接下来在基于这个next数组来对模式串进行匹配，整个匹配的过程主串的指针是不回溯的。好，这个小节当中我们会再用几个例子，就是用更多的几个模式串尝试着求出其他模式串的next数组。因为求next数组这个步骤是很关键的对吧？而考研当中也要求我们掌握手算求next数组的这个方法。

好，来看这样的一个例子，现在有这样的一个模式，串google GOOGLE，我们要求google的next数组。首先这个next数组的下标，这些下标是不是应该和我们的字符串的这些字符的下标是一一对应的？字符串的下标是1到6，那么next数组也应该有1到6这样的一些元素对吧？好的，接下来我们尝试着先求next一再求next 2、next 3、next 4，也就是从左往右的这种顺序来求出这几个next数组的值。

好，首先来看next 1，next一的含义就是当模式串的第一个字符发生匹配失败的时候，模式串指针J应该指向什么位置，这就是next一的含义对吧？好，那来看一下，如果说此时第一个字符匹配失败，那么按照上一小节我们分析的逻辑，此时应该是让J指针先等于0，然后I和J同时加加对吧？也就是开始匹配往后的一个子串和这个模式串。那这个逻辑对于任何一个模式串都是一样的。任何一个模式串只要它的第一个字符发生了不匹配的情况，那么我们只能紧接着匹配下一个子串。所以以后当我们在求next数组的时候，所有的模式串next一这个值肯定都是零。

好，所以我们把next一等于零给写上，接下来我们来求next 2的值。Next 2的含义就是如果第二个字符匹配失败，那么接下来指针J应该指向哪儿。好，对于这个例子来说，如果此时第二个字符匹配失败，那么我们只能让模式串往右移动一位，也就是让J指向一这个位置，也就是这个样子。事实上对于任何一个模式串都是一样的，任何一个模式串它的第二个字符不匹配。那么我们就应该尝试着把I所指的这个字符和模式串的第一个字符检查一下它们能不能匹配。所以无论是哪个模式串，next 2的值肯定都是一，next 2的值肯定都是一。所以今后我们求next数组的时候，next一我们都可以直接写0，next 2我们都可以直接写一，这两个是确定的。对于任何模式串来说一定都是这样的。

好，接下来我们求next 3。Next 3的含义就是当第三个字符匹配失败的时候，我们应该让J指针指向什么位置。好，接下来我们可以这么处理，刚才画出的这条分界线，左边的这些内容是我们已知的，而右边的这些内容是我们未知的。好，所以我们可以尝试着让这个模式串一步一步的往往右移。然后我们再往右移动的过程当中，注意观察这条分界线左边的部分能不能匹配得上好，现在先往右移动一步，那往右移一步之后发现这个G和这个O对不上，对吧？无法匹配。

好，所以说明我们模式串往右移到这个位置是不够的，应该继续往右移。好，移动到这个位置的话，整个模式串就全部移动到刚才的这个分界线的右边了。而这条分界线右边到底是什么样的一些字符，我们目前是暂时无法确定的对吧？

好，因此接下来我们有必要来检查一下I所指的这个元素和J所指的这个元素，它们是否能够匹配。好，因此当第三个字符匹配失败的时候，我们可以直接让J的值等于一，也就是从这个位置开始往后匹配。好，所以这儿我们就可以确定next 3的值，next 3的值等于一好，接下来我们要求的是next 4，也就是如果模式串的第四个字符匹配失败的话，那么应该让指针J移动到什么位置。

好，来看一下，如果此时这个第四个字符匹配失败，那么和刚才一样的方法，我们可以在当前匹配失败的这个地方给它画一条线，那这条线的左边这些字符的值我们是可以确定的，而这条线右边我们暂时还不可以确定。好，那接下来我们让模式串一步一步的往右移移动一步，发现这个位置无法匹配，对吧？无法匹配好，所以得继续往右移再移动一步，发现这个位置依然无法匹配。好，所以我们还得往右移，直到整个模式串全部都跨过了刚才画的这条分界线。好，那就说明接下来我们应该对比的就是I所指的这个元素，以及第一个模式串的第一个字符。好，所以当这个模式串的第四个元素匹配失败的时候，我们应该让J的指针直接就指向一，因此next 4应该等于一好，接下来到了next 5，也就是如果第五个元素匹配失败的话，应该让指针J指向哪儿。

好，现在第五个元素匹配失败，那和刚才一样，我们在匹配失败的这个位置前面画一条分界线。这条分界线左边的这些元素肯定和模式串是一致的，然后右边的这些元素到底是什么，我们还不得而知。好，接下来我们让这个模式串一步一步的往右移，一边移动，我们要一边观察模式串和左边的这些主串能不能够匹配的上好，现在匹配不上对吧？显然匹配不上，然后再往右移，此时依然匹配不上好，再往右移匹配上了绿色的这条线，左边的这些部分是能够完全匹配的。好，所以接下来我们有必要检查一下I所指的位置和J所指的这个位置这两个字符能否匹配。好，因此对于这个模式串来说，如果第五个元素匹配失败的话，我们可以直接让J的值等于2，指向二这个位置。好，所以next 5应该等于2。好，最后我们还需要确定next 6，也就是第六个元素如果匹配失败的话，那么应该让J指针指向什么位置。

好，和刚才一样，如果第六个元素匹配失败，那么我们在当前匹配失败的这个元素，我们在这儿画一条线，然后这条线的左边这些字符我们是可以确定的，肯定和模式串能够对应上。但是右边的字符，这条线右边的字符是什么暂时还不能确定。好，还是一样的思路，我们把模式串一步一步的让它一步一步的往右移，然后每移动一步你观察一下，这上面和下面能不能匹配上。现在匹配不上，那么继续往右移。现在显然也匹配不上，所以再往右移。好，现在第一个字符可以匹配上，但是第二个字符匹配不了，对吧？所以这个模式串应该再往右移。好，现在这两个字符也匹配不上，因此模式串还得往右移，直到整个模式串都全部跨过了这个分界线为止。这就说明接下来我们有必要进行检查的就是I所指的这个元素和第一个字符能不能够匹配的上好。

所以对于google这个模式串来说，如果第六个字符匹配失败，那么我们应该让J指针指向一这个位置，因此next 6应该等于一好。那到此为止我们就用手算的方式确定了google这个字符串的next数组。确定了这个next数组，接下来我们尝试着使用这个next数组。好，我们有这样的一个主串，大家可以试一下，这个主串应该怎么念。Google GLE对吧？Google googoogling从这个里边要找到google这个模式串。

好，现在我们已经知道了google的next数组，这是我们刚才手算的方式求的好，从主串位置为一的这个元素开始依次往后匹配，发现这些元素都能够匹配。直到第六个元素匹配失败，O和E匹配失败。好，也就是说当J等于6的时候发生了匹配失败。好，在这个时候我们是不是应该让J等于next j也就是等于next 6，而next 6等于一。所以接下来我们应该让J的值等于一，然后I的值是保持不变的。这个我们上一小节分析过对吧？

好，J的值等于一，那就意味着接下来会从这个位置开始继续往后匹配。好，现在I和J所指的两个元素匹配失败，匹配失败，O和G对不上对吧？那匹配失败同样的当发生匹配失败的时候，我们就应该让J等于next j而此时J指向的是一。因此进行这步操作之后，J就会等于0。J等于零也就相当于指向了当前这个字符串前面一个位置。

好，上一小节我们说过J等于零这种情况我们会进行一个特殊的处理。如果检测到J等于0，那么让I和J都统一的加加也就开始匹配下一个子串。好，接下来这个子串，第一个字符能匹配，第二个能匹配，第三个能匹配，第四个能匹配，第五个匹配失败第五个匹配失败。所以也就是J指向5的时候发生了匹配失败的情况。那此时应该让J等于next j而next j的值应该是等于2，所以接下来J的值会被修改为二，而I的值保持不变。好，因此接下来应该从这个位置继续往后匹配。好，再往后的话，所有的这些字符对应的这些字符是不是都能够一一匹配？好，到此为止google这个模式串就匹配成功了，这就是对next数组的使用。

好，接下来我们再用这样的一个模式串来练手，求这个模式串对应的next数组。这个模式串它的长度是6，所以next数组也应该有next 1一直到next 6这样的几个值。好，刚才我们说过，对于任何一个模式串next 1和next 2，我们都可以无脑的写零和一对吧？

无脑的写好，接下来我们要求的是next 3。Next 3意味着如果第三个字符匹配失败，那么我们应该让这个模式出来的指针J指向什么位置。好，现在第三个字符匹配失败，那么我们能够确定的就是前两个字符是A和B那我们按照刚才的思路，让模式串一步一步的往后退，看一下退到什么位置可以使得这条分界线左边的部分能够完全匹配。好，来看一下这个模式串往右移一格，那么此时这个分界线左边的部分显然是对不上的。对不上的话我们就只能让模式串继续往右移。好，所以我们J指针应该是在这个位置，然后I指针应该是在这个位置，I指针保持不变，然后J指针是不是就应该让它指向第一个元素，就是这个意思。好，因此如果第三个元素匹配失败，那么应该让J指针指回到一对吧？应该让J指针指回到1，因此next 3应该是等于一的。

好，接下来next 4，next 4意味着当匹配到第四个元素的时候，发生匹配失败。好，在这种情况下，我们同样的在匹配失败的这个元素这左边这儿画一条分界线。然后接下来我们让模式串一步一步的往右移，移动第一步，现在对不上，然后移动第二步。好，现在分界线左边的这些内容对上了，能够匹配的上好，所以接下来就应该匹配主串的这个位置和模式串的这个位置，也就是应该让I保持不变，而让J指向模式串的第二个元素，吧？这个元素的下标应该是2。好，所以next 4的值就应该是2，就应该是2。好，我们把它填上去，接下来算next 5。

Next 5意味着当第五个字符匹配失败的时候，指针J应该指向哪？好，那第五个字符匹配失败的话，我们在这个字符左边这儿画一条线。好，接下来让模式串一步一步的往右移移一步，此时对不上再移一步。此时对上了，就这条分界线左边这些部分上面和下面都是可以对上的对吧？好，所以这就说明接下来是不是让I指针保持原位，然后让J指针指向这个元素就可以了。也就是说当第五个元素发生失配的时候，让这指针应该是指向这元素，也就是第三个元素对吧？指向第三个元素，这是第一个，这是第二个，这是第三个，指向第三个元素。好，所以next 5的值就应该是三对吧？

就应该是3，我们把它填上好，最后next 6。Next 6意味着如果匹配到第六个字符发现匹配失败，那么这个模式串应该往右移几个。好，同样的在匹配失败的这个位置，我们画一条线，画一条分界线，然后让这个模式串一步一步的往右移，移动一格没匹配上再移动一格，对上了。

好，这就说明接下来我们应该对比的就是这个元素和这个元素？I指针主串的I指针保持不变，然后J指针应该让它指向这个元素B这个元素，那这个元素的下标应该是1234，应该是四对吧？好，因此next的6就应该等于4。当第六个元素匹配失败的时候，我们可以直接让J指针指向第四个元素，然后I指针保持不变，然后从这个位置再依次往后进行匹配。好的，那么到此为止我们就求出了这个模式串它的next数组。

不知道大家有没有找到一点感觉，我这儿介绍的这种方法是我认为求next数组比较快的一种手算方式。这种方式和我们课本上给出的那种算法还不太一样，会更偏向于考试一些，可以帮你快速的做题。好，最后给大家留这样的一个例子，你可以暂停自己动手算一下这个模式串它的next数组应该是多少，可以暂停来算一下，然后我们公布答案321，你算它的结果是不是这样的。好，如果不是的话，自己去面壁。

好，对于KMP算法这个部分，你在第一次看书或者第一次看这个视频的时候觉得很懵逼，就是听得好像似懂非懂的，这个很正常。很少有人能够在第一次学习KMP的时候就直接把KMP完全理解，肯定都是要反复多遍的。同时你也得自己动手做一做课后习题来训练。因为你自己动手来模拟这个匹配的过程，以及动手求这个next数组，可以让你更清晰的理解算法背后一步一步的原理。