058 6.2.3+6.2.4 十字链表、邻接多重表_原文

各位同学大家好，这个小节中我们要学习图的最后两种存储方式，十字链表法和邻接多重表。十字链表会用于存储有向图，而邻接多重表是用于存储无向图的那上小节的末尾我们分析过邻接矩阵最主要的问题是它的空间复杂度太高。而邻接表最主要的问题是当它存储有向图的时候，要找某一个顶点的入边，也就是指向一个顶点的边是很不方便的，必须遍历整个邻接表才可以。那如果采用十字链表来存储一个有向图的话，就可以解决这两个问题。直接看图比较好理解，这个有向图当中总共有四个结点，ABCD.

这几个节点的信息会分别存在数组的0123这几个位置，也就是说每一个结点是有一个编号的那对于A结点来说，有一条湖是A指向B的，另一条弧是A指向C的。所以如果从A结点的绿色这个指针往右边找会发现它指向的第一条弧的信息是从零号结点指向一号结点，也就是A指向B也就是对应这条弧。然后我们顺着这个节点，绿色这个指针，也就是这条指针继续往后找，找到下一个节点，这个节点表示的是从零号指向了二号，也就是A指向C所以这个结点表示的是这条弧。好，那这两条弧是从A指向其他节点，然后还有两条弧是从其他结点指向A分别是D指向A和C指向A这两条弧的信息我们要顺着A这个结点的橙色这个指针往后面找。这个橙色指针是指向了这一个湖的结点，这个湖节点指明了这条湖是从二号指向00号，那二号节点就是CC指向A如果我们顺着这个弧结点的橙色指针继续往后找的话，那就可以找到下一条弧。这条湖是从三号指向零号，也就是从D指向A那除了C指向A和D指向A的这两条弧之外，已经没有其他指向A的壶了。所以下面这个弧结点的橙色这个指针是一个指向空，也就意味着已经没有其它的指向A的湖了。

好，所以用十字链表法来存储一个图的话，那么我们需要定义这样的两个结构体。其中一种结构体是表示顶点，另一种结构体是表示湖。如果我们通过绿色这一条指针线一直往后找各个节点的话，那么我们就可以找到从当前这个顶点往外发射的所有的这些湖这些边。而如果我们顺着橙色的这条指针一直往后找的话，那我们就可以找到所有指向当前这个顶点的湖。因此如果采用十字链表法，那我们想要找到和一个顶点相连的所有边，包括出边和入边都是很方便的那这些字段的含义在这儿已经用这种文字的方式给出这个部分的内容。最好是大家暂停对着这些文字还有这个图来进行理解，用语言的方式越解释越容易乱。

显然十字链表法的空间复杂度应该是这样的一个数量级。左边这个数表示的是顶点的个数，右边表示的是边的个数。那这个空间复杂度是不是和邻接表是同样优秀的，并不需要像邻接矩阵那样需要消耗V的平方这样的一个数量级机。同时使用十字链表法也可以解决邻接表当中找入边不方便的问题。我们只需要顺着橙色的这一系列的指针线路往后找，就可以找到所有的指向当前节点的入边。

再次强调十字链表法只能用于存储有向图。好，这是十字链表法。有兴趣的同学可以自己动手实现一下十字链表法的一个代码的定义。好，所以我们用十字链表法能够得到一个比较好的方案来存储有向图。

接下来我们再来看对无向图的存储可不可以有一个更优的方案呢？如果用邻接矩阵存储无向图问题是一样的，就是空间复杂度太高。而如果用邻接表这种方式来存储无向图的话，主要的问题是每一条边会对应两份冗余信息。比如说A和B之间的这条边就会对应这样的两份数据。所以如果我们要在这个无向图里面删除这一条边的话，那我们需要删除这样的两份数据是比较麻烦的。另外如果我们要删除其中的某一个结点，比如说删除A结点的数据，那么我们除了删除A结点，还有A结点后边的这些边结点之外，我们还需要找到这几条边的另外几个冗余数据，把这几个边节点的数据也全部删除。所以这就是用邻接表来存储无向图的时候存在的一个比较明显的问题。

当我们删除一个顶点或者删除一条边的时候很不方便，主要的原因在于每一条边会对应两份冗余的数据。好，那怎么解决这个问题呢？我们可以用邻接多重表的方式来存储无向图，还是一样的，直接给图比较容易理解。其实邻接多重表和刚才介绍的十字链表是很相似的。像这个无向图里边有五个顶点，那对应的就是这五份数据。同样的这些顶点的信息我们可以定义一个struct然后用数组的形式来顺序的存储这些顶点的信息。所以每一个顶点会对应一个数组下标的编号。

在顶点的信息当中，除了数据域之外，还需要有一个指针，是指向和当前这个顶点相连的第一条边。比如对于A这个顶点，它和BD之间有两条边，那我们顺着A结点的这个指针往后找，找到的这一条边就是零号和一号相连的一条边，也就是A和和B相连的一条边。在这个边结点里边，我们如果继续顺着i link这个指针往后找的话，就可以找到和爱好结点相连的下一条边。好，所以如果我们顺着橙色的这一条指针继续往后找，那找到的下一条边的信息就是零号和三号相连的边，也就是A和D相连的这条边的信息。好，那除了这两条边之外，已经没有其他的边再和A也就是和零号结点相连了。所以这个结点的i link指针是指向none，表示已经没有其他的边和A相连了。

好，再来看B结点，对于B来说有ACE这几个结点和它直接相连。那么我们通过B结点的这个指针往后找，找到第一条边的信息，这条边是A和B相连的一条边。由于这是一个无向图，所以B结点的编号一它到底是在左还是在右，其实逻辑上来看是等价的。

好，现在B结点的编号一是存在右边这个位置。所以如果我们要继续找到和B结点相连的下一条边的信息，那我们需要通过绿色这个指针，也就是j link来往后寻找，也就是找到这个结点。那这个边结点是指明了有二号和一号相连的一条边，也就是C和B相连的一条边。同样的B是存在右边这位置，所以我们需要通过绿色这个指针继续往后找。后面这个结点指明了这是四号和一号结点相连的边，也就是E和B相连的边。那到此为止我们就可以找到所有的和B结点相连的三条边的信息。当然了，如果你的这个无向图是一个带权图的话，那其实也可以在各个边节点里边增加一个字段，表示每一条边的权值。

好，总之用邻接多重表的这种方式来存储无向图，想要找到和某一个顶点相连的边是很方便的。同时每一条边只会对应一个边，结点只有一份数据，因此就不需要像邻接表那样同时维护两份冗余的数据。这种特性可以保证我们在删除一个结点或者删除一条边的时候，操作起来会方便很多。

比如现在我们要删除A和B相连的这条边，那这条边对应的边结点是这个节点。这个节点被删除之后，我们需要修改这样的两个指针，上边这个指针是指向和A相连的第一条边。那么我们是不是其实只需要在删除这个结点之前，顺着橙色这个指针来找到它的下一条边对应的是哪个结点就可以。所以删除这个结点之后，我们只需要让A结点的这个指针指向它后面的一个边就行了。

那对于B结点是不是也一样的？其实我们只需要通过绿色的这个指针找到和B结点相连的下一条边的数据，然后让B指向它就可以了。所以删除一条边是很方便的。

接下来看如果我们要删除一个顶点，比如说要删除E的话，那除了删除E本身的数据之外，我们还需要删除和E相连的所有这些边的信息。那么我们通过这个指针可以知道和E相连的边应该是这个边还有这个边。所以我们把这两条边对应的数据删除。

删除这两个节点会导致这两个指针的指向会出现问题。来看一下这两个指针的值应该修改成什么。比如说像左边这条指针，这条指针指向的是和一这个节点相连的下一条边。那从当前删除的这个节点来看，和一相连的再往后的一条边已经没有了。所以我们在删除下面这个节点之后，其实只需要把上边这个节点绿色的指针把它付为空就可以。右边这个节点也是一样的，只需要把下边这个节点的空指针付到这个位置就可以，也就是变成这个样子。

好，所以可以看到采用邻接多重表来存储无向图的话，那它的空间复杂度很优秀，甚至要比邻接表的空间复杂度还要更好。因为邻接多重表里边不需要存储冗余的边的信息。另外和邻接表相比，在邻接多重表当中进行边的删除或者节点的删除，这些操作会变得很方便。所以采用邻接多重表来存储无向图是非常好的一个解决方案。

好，再次强调邻接多重表只能用于存储无向图。好的，这个小节中我们学习了十字链表和邻接多重表这样的两种图的存储结构。由于这两种实现方式在实际写代码的时候，这个代码的逻辑会比较复杂。所以也不太可能在考研当中用手写的方式让大家实现十字链表和邻接多重表。因此这两种存储方式只需要理解它们各自的一些特性就可以。

十字链表法可以用于存储有向图，这种方案既解决了邻接矩阵空间复杂度太高的问题，同时也解决了用邻接表存储有向图的时候，找到有向图的路边不太方便，必须便利的这个问题。而邻接多重表可以存储无向图，同样的它既解决了邻接矩阵这个空间复杂度太高的问题，同时也解决了邻接表当中进行边的删除或者顶点的删除不方便，每一条边还会对应两份冗余数据这样的问题。另外对于一个给定的图，采用十字链表法或者邻接多重表来存储的话，那表示方式也是不唯一的。好的，以上就是关于图的存储结构相关的全部内容。