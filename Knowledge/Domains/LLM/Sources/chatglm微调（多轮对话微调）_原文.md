chatglm微调（多轮对话微调）

chatglm微调（多轮对话微调）_原文

2026年07月22日 14:57

发言人   00:00

好，同学们，这节课我们来进行一个ChatGLM3-6B的一个微调教程。要开始微调之前，我们先来看一下它的文档，看一下文档给我们提供的一些教程。第一步是先安装依赖，咱们这个文章里也写了，可以看到直接执行命令就可以了。执行这命令的前提是已经进入了虚拟环境，你需要先激活虚拟环境的。我已经激活了，你需要先运行这个命令，先初始化，然后再输入这个指令激活。激活后，然后安装依赖。

发言人   00:57

好，接下来我们返回到chat ChatGLM的微调官方示例看一下。Chat LM微调它官方提供了两种数据集的格式，一种是多轮对话的格式，还有一种是输入输出的格式。我们今天先给大家介绍多轮对话的格式。多轮对话格式你从它的名字中也能看出来，他可能会要使用一个多轮对话的聊天记录。

发言人   01:26

我们接下来往下看啊，它还提供了两种的多轮对话格式，一种是微调模型的对话能力，而非工具能力。另外一种是微调模型的对话和使用工具能力。这两种格式其实差别也不是很大。差别其实只有这里使用工具能力，这里会多一个工具字段，还有角色，这里会多一个工具，其他并没有什么差别。所以我们今天就从这个微调模型对话和使用工具能力这一种情况给大家讲解。

发言人   02:09

我们来看一下数据集，整个数据集。是被一个这样的方括号给括起来的。我们可以看到然后这一个数据集里面的会有很多条数据集，第一条、第二条、第三条，每一条数据集都被一个单独的划拨号铺到这里，一直到这儿给放起来。这里是一个完整的数据。我们看一下每一条数据里面它有什么关键字儿。第一个关键字是工具关键字，在这儿定义了这个工具是用来干什么的，还有一些参数之类的东西。在这里描述这些工具的话，格式是不受限制的。

发言人   02:57

我们看到第二参数，第二参数是conversation的参数。Conversation翻译过来就是对话的意思。你可以把它你可以把这个参数里面包含的内容理解为聊天记录的意思。用户和AI在使用上面的工具过程中，产生的聊天记录全部都会被记录在conversation这里面。

发言人   03:23

我们再去我们再继续看这个conversation里面有很多内容他这里会定义很多角色，一个是一个是system，一个是user，一个是还有一个是assistant。System角色它翻译过来是系统的角色的意思。这里你可以填入系统角色提示词，这也是提示词起什么作用。它是把ChatGLM，也就是AI的决策进行一个限定。你可以告诉他你是一个程序员，或者你是一个翻译官，你要帮我翻译用户的话，你要帮我翻译用户输入的内容，对不对？或者说你是一个审核员。这个提示词它的作用就是在时刻的不断提醒AI它到底是个什么角色，又处于一个什么样的环境当中，这是一个环境变量。通俗一点讲，这段提示词它会在AI每次输出内容的时候，不断的给AI重复说，你是是一个程序员你是一个程序员这样的。

发言人   04:30

接下来是user角色，user这个角色也就是用户角色，用户给模型的问题会被记录在这里。再接下来是assistant角色，assistant角色是祝福的意思，这里他记录了AI返回给用户的话。接下来是工具这个角色。我们来看一下工具角色，并不是ChatGLM3中的原生角色。工具在预处理阶段会将自动转化为一个工具角色，并且要默认计算损失值，它不是一个原生角色，是一个新加的角色，在这个角色里定义了工具的名字。还有工具的一些参数，还有工具的返回值。这是工具的关键字。

发言人   05:32

在接下来这儿这里就是AI返回的一段话，因为它是一个多轮对话的一个数据集，它的格式就是用户说完AI说AI说完用户再说。它还有一个提供了一种多智能体，也就是多个agent的一个样例。如果你有agent一个聊天记录的话，你还可以加进去用户的角色，还有AI助手角色都记录在这里。这就是一个多轮对话数据集格式，这个数据集是从这里来跳动，我们使用这个数据集就可以。首先第一步，返回到这个登目录。好，接着把项目拖拢过来。

发言人   06:30

如果克隆不过来，就用这个学术加速。加速完成之后，一定要取消学位加速。同学好，课程成功了。我们看一下这个项目的原始数据集，在这data里面有一个trying data，点开看一下。这就是他原始的数据集。接下来数据集下载过来了，我们要对数据集进行调整，我们的指令是分过来。

发言人   07:10

回车可以看到像这样的输出内容就证明这数据集已经调整好了，数据集就已经处理完成了。我们来修改一下这个参数，进入到这个目录，看一下目录的构成。刚才修改完的数据集存在这里，会增加这个文件夹。在这个目录里我们看下这个数据，这个修改后调整后的数据线是这样，它看起来会很很混乱很乱。我们把它下载到本地的编辑器里来看一下，这样就能看得很清楚了，可以看到它这个数据集，它这里的每一行都代表了一条数据。对了，这里就是英文，我翻译两行给大家看一下，方便大家去理解。

发言人   08:03

我们看一下这数据到底是怎么个事儿，我们来看一下，这是第一行，从这里到下面它都是第一行。第一个关键字是工具关键字，工具关键字从这儿到这儿。它定义了工具使用的方法，还有工具里必要的一些参数。这里是没有固定格式的这里是工具。

发言人   08:31

接下来是conversation关键字，也就是关键字，也就是对话的关键字。他记录了用户和AI之间交互的这些记录，也就是聊天记录。我们来看一下，第一个说话的是user，也就是用户，用户说话的内容是作者，我们来看一下，他说我正在解决一些请求问题，你能帮我发一个带有标题这个什么让我知道详细，让我知道相应的详细信息。紧接着就是AI，这个角色是AIAI他就说话了，他说我需要使用这个工具发送post的请求。也就是上面这个工具，然后工具就开始工作了。我们可以看到工具工作的一些内容在这里。工具响应它的响应之后，就把响应内容记录在了这儿。AI就看到了他的回应，就把这个消息给返回给用户说请求成功响应，包含了请求中发送的数据，这是其中的一条。

发言人   09:43

我们来看一下第二条，看完第二条应该会理解的更加深层一下。第二条是从这里，然后到这里我们来看一下第一个关键字，也就是态势工具，记录了一些特定的方法，还有必要的参数，也就是工具详细信息。可以看到一直到这里都是的，接下来是对话参数，我们可以看到这儿角色用户开始说话了，用户说我想查看我的家乡悉尼澳大利亚的卫星图像，位于这个经纬度，我的生日是这个，能帮我获取图像吗？就开始说话了，我需要使用这个工具来检索指定位置和日期的卫星图像。然后工具就开始响应了，这是工具响应的参数。最后得到了一个响应，他会把响应的内容再发给这个AI。最后AI就说已经检索到卫星图像的URL，也就是图像的网址。你把这个网址打开就可以看到用户请求的这个图像了，这就是整个数据集的它的一个构成，包括等后面我也会会给大家把文档里放一些书籍的标准格式，大家直接去套用就可以。准备好数据集之后，我们就修改参数，到这里，到这个目录底下。

发言人   11:26

在这个目录下，找到这个文件夹，打开。

发言人   11:38

把base model换掉，换成我们的模型路径。

发言人   11:50

把数据集的路径也换掉。

发言人   12:08

保存一下，保存完成之后，我们就退出去退出去，重新进入这个目录。

发言人   12:22

运行微调的面料。我们可以看到，现在正在加载模型，现在就开始微调了。它的整个过程可能要有个3个小时。我们可以看一下，它这里有一个预估的时间，3小时41分钟。大家都不用，大家就不用干等着了，微调完成之后，它会显示这样内容，显示这样的话它就证明微调已经成功了。微调结束了，但是你的界面可能跟我的不太一样，因为这个字段是按时间来算的。进入这个目录。

发言人   13:14

你看一下，也就是左边的这个目录项。

发言人   13:27

启动微调的模型，先到ChatGLM目录下，找到反弹demo目录下。找到output目录。

发言人   13:48

进来找到对应的文件夹，可以看到每个文件夹都只有一个文件，就去找那个包含很多文件的文件夹，找这个文件。你把这个文件名给复制过来到这里。但是你一定要在这里加上一个杠root杠，这样就OK了。我们启动一下微调的模型。

发言人   14:28

好，成功之后就可以开始工作了，我们还是用老办法，记得一定要带这个端口。在这里把这个改成8501，打开power ship，用SSH命令启动回车。

发言人   14:54

我调整一下ssh地址和密码。

发言人   15:25

输入yes。

发言人   15:32

好，然后输入密码。

发言人   15:38

回车，如果没有报错的话，就可以登录了。现在正在加载模型，我们看下后台有没有报错，如果没有的话，就证明我们加载成功了。好，加载成功之后，微调的多轮对话模型我就可以来使用了。

发言人   16:02

好，那多轮对话微调教程就先到这里。到后面也会为大家带来一些其他方式的微调，还有精调的这些方法。同学们咱们大家一定要好好跟着实操一下，不要怕麻烦，同学们再见。