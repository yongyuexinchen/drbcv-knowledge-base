Chatglm2+langchain（参数调节和配置）

Chatglm2+langchain（参数调节和配置）_原文

2026年07月22日 14:52

发言人   00:01

好，同学们，这节课来讲一下这个模型该如何去使用，参数如何调节。我们先讲这里，这个就是大模型的温度影响大模型的反馈精度。你是想让大模型反馈的内容更加严谨，更加死板，还是想让大模型反馈的内容更加随意、更加有想象力，都可以通过这里来调节。实际还是要根据业务场景和应用场景来调节，各有利弊，往左拉会更严谨，但也会更死板。往右拉更具有创造力，会更丰富，但是要牺牲一些专业性和严谨性。

发言人   00:38

我们举个例子，我们把这个数字拉到最左边续写这句话，我叫张三。我们可以看到他就重复这句话了，很死板。我们如果把这个数值拉的大一点，再试一下，复制一下这句话。你看这个时候他就把这句话进行扩展了。如果在企业的业务场景使用的话，数值最好不要设置太大，不然会牺牲掉严谨性。如果是客服场景的话，可以稍微大一点，但是不要很大，不然就走偏了。

发言人   01:20

下面历史对话论述，这里就是让大模型记录几轮对话。如果设置成3的话，每次交互和使用的时候，大模型会基于之前的三轮对话的内容继续反馈。超过三的话大模型的记录人数就会更多。这个参数就是让大模型具有记忆力的，不会存在聊着聊着就从头开始了。但这里的数值比较吃配置，大家最好不要设置的太大，最好不要超过十轮，超过十轮的话，这个机器可能就会崩了。大家也可以根据业务场景和应用场景来灵活调整，一般3到5轮基本就够用了。客服场景和销售场景的话，一般5到8轮也就够了。

发言人   02:06

好，我们再从头看一下每一项的参数。我们先讲一下选择对话模型。有普通的大模型对话，就是我们正常使用ChatGPT这种大模型交互对话。我们先选择大模型对话，下面这里我们可以选择大模型的底座。大模型的底座我前面也讲过，每一个大模型的反馈内容和推理性能是不一样的，但是都大差不差。这里除了GLM2，还有质谱的模型，还有微软的OpenAI的模型，还有OpenAI自己的模型，这里是cloud模型。下面这里就是又重复一遍，给咱们留了一个接口。

发言人   02:48

我们目前只能使用JLM和质谱，其他的暂时不能用，因为会涉及到网络环境问题，而且其他的模型在中文场景内肯定是没有GLM2和质谱的效果好的。毕竟国产大模型是通过训练中文语料训练出来的，所以在中文的应用场景发挥会更好一些。如果想使用这个质谱模型的话，就要去获取API的K如果只是使用JLM2的话，就不需要去配置。给大家演示一下质谱API的配置方法。我们先打开百度，然后去搜索智谱。

发言人   03:33

点进来。

发言人   03:43

点进来。

发言人   03:50

点击开始使用。正常来说你是要进行注册登录的。注册登录很简单，基本上30秒就注册好了。大家可以自行的注册登录一下。因为我已经登录过了，所以不需要再进行登录了。

发言人   04:05

好，注册完成后，我们点击查看APIK，我们复制APIK。如果这一栏没有APIK的话，那就重新添加一下就行。一般都会有一个系统默认的K复制以后我们回到服务器这里，就是部署服务器这个界面。

发言人   04:33

我们点击这里，回到最上层的文件夹，然后找到long chat，down chat chat这个目录下，然后找到conference文件点开，然后再找到model config，点PY文件夹，点开它。然后我们往下拉，我给大家简单的讲一下这些参数是什么意思。这里M3E杠base是设定模型的位置，前面这里是模型的名字，这里其实就是模型存在的位置。如果你跟着我在服务器上跑的话，你就不用管。如果你是在本地跑的话，这个位置是必须要修改的。这里是OpenAI的模型，如果你要使用OpenAI的模型的话，就在这里配置OpenAI的K咱们这个服务器节点是国内的，所以调用不了OpenAI的K剩下的就是些其他模型。如果你想去使用的话，要把模型下载到本地目录，然后把模型本地目录的位置填进来就可以，然后我们再往下拉。

发言人   05:48

这里是微软OpenAI的接口，如果你想使用这个模型的话，你可以去官网获取它的APIK。但基本上我们是用不到的，所以暂时不用管。下面这里就是OpenAI的模型，就是拆GPT，这里可以填GPT3.5或者GPT4。这里填密钥，就是OpenAI的密钥基本上是用不到的，除非你想去试一试。这里就是cloud模型一样，你想用的话可以把密钥配置过来，在某宝上也有卖的。

发言人   06:27

往下我们看到今天的主角质朴。把刚刚复制的密钥复制过来就行，粘贴到里面。因为我提前粘贴过了，所以我就不用动，你正常的粘贴到双引号里面就可以。

发言人   06:44

下面这里就是选择模型的版本，模型的值这个pro是最强的，但是非常的吃配置。我们就使用正常的版本，就推演下面剩下的模型也是需要用的话，可以去获取一下K进行配置，基本上是用不到的。这里他这里也给我们配置的地址了，我们去对应的地址获取一下就可以。

发言人   07:23

然后我们来到这个server config点PY文件，然后往下拉，这里有一个20002，下面这里有一个21001OK没问题。这里说一下，不能有和21001重复的端口，不然端口就会被占用，使用过程中就很容易报错。好，检查无误后，记得要保存这两个配置文件都要进行保存。你一点叉它就会提醒你保存，因为我已经保存过了。如果没有提示你保存，你就检查一下你填写的内容是否被保存了。

发言人   08:04

55后我们再回到这个终端界面，和大家说一下配置完成后要重启一下机器，给大家演示一下。

发言人   08:17

给大家演示一下，要control加C退出来。好，这个时候服务器就终止了。不管你在云服务器上跑任何项目，如果你想终止，你就CTRL加C就可以。然后我们把文档中这条指令复制过来，粘贴回车，把服务器启动起来，然后稍微等待一下。

发言人   09:02

等到它输出这个界面之后，说明就说明跑起来了。我们再回到这个交互界面，看一下是否能正常使用。如果一直刷新的话，你就在power share里面随便点两下。如果一直加载不出来东西的话，你就control加C把这个命令复制一下。

发言人   09:26

上节课也给大家讲了，粘贴过来连接一下就行了，还是一样输入我们的密码。再和大家重复一遍，输入密码的时候要注意大小写，你输入的过程中密码是不显示的，所以不要慌，虽然不显示，但它也输入进来了，一定要慢慢的输入进来，尽量不要出错。输出完成后回车等待几秒。如果没有报密码错误的话，然后再回到这个界面，我们再刷新一下这个页面。如果你能直接刷新出来的话，你就不用管。如果你的界面一直出不来，你就按照我的方法control加C重新来一遍就行。

发言人   10:11

好，我们选择质谱这个模型。如果你是使用chat GM2-6B的话，就不需要任何配置。质谱和GM2的效果差不多。好，我们和他对话一下，我们没有配置别的API的话，和他对话就没有反应。

发言人   10:40

我们再回到质朴。

发言人   11:04

我们来到下面提示词模板这里，它这里是默认没有定义提示词模板的。前面也和大家说过，提示词会帮我们优化大模型的性能，让大模型在特定领域发挥的能更好一些，能更加适合特定的应用场景和业务场景。我带大家过一遍。

发言人   11:29

我们继续来到long chat down chat，chat这个文件下，我们找到confidence点进来，然后找到这个prompt config点PY文件下点进来，我们看这里。大家可以把这一段给复制一下。然后粘贴到下面，因为我已经粘贴过了，所以我就不再继续粘贴了。

发言人   12:02

你如果没有粘贴的话，要重新进行粘贴了，然后这个值要和上面的值保持一致。Input, 这里就是我们用户输入的值。这里不用动，我们可以修改下面的东西。翻译助手修改就是这里的名字，能更好的知道每一个模板是做什么的。好，然后下面这里就是我们预设的提示词，你可以根据特定的场景来给它预设提示词，这样它就会在特定场景内发挥表现的更好。好，以这个翻译助手为例，我给他设的是你是一个聪明的翻译助手，请将下面的内容翻译成英语。你到时候根据业务场景灵活调整就可以了。智能销售、客服、产品经理等等都可以调。它和ChatGPT的提示词也不太一样，你不能说的过于详细。好，下面这里的input也是用户输入的值，还是这里比如说我们给他说的你好。

发言人   13:13

然后你好这两个字就会从后端传输过来，然后他就会进行思考和运作了。其他的暂时没有特别讲的了。这节课其实就是带大家调整一些API和配置，还有提示词模板的调整，大家可以直接切到下节课。下节课会给大家讲一下这个知识库问答。这里才是针对B端来使用的场景，可以针对企业的内部知识库和数据集进行检索和对话。好，同学们，我们下节课再见。