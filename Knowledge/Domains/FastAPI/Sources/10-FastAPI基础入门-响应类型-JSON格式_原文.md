10-FastAPI基础入门-响应类型-JSON格式_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，截止到现在，咱们已经学了fast API当中好多关于请求的知识了。比如说咱的路由、请求方法、请求参数等等等等。当然我们也有经验，如果服务器收到我们的请求了会干啥？对，给出来响应是不是？

发言人   00:23

在fast API里边支持的响应类型也是多种多样的那提前说fast API默认支持的响应类型是阶层格式。在这儿同学们可能会好奇，那不对，咱是不是没有写过杰森？那这个杰森哪来的呢？说到了这儿，咱就得看看目前响应的类型是啥样的。

发言人   00:45

注意我们写过的代码处理函数是不是直接return了一个python的对象。那在fast API里边，如果遇到响应的是python格式的对象的话，fast API它会自动把它转成杰森兼容格式，并且包装成杰森response返回给客户端。也就是说咱们写代码写的是python对象，背后的活儿都让fast API是不是给干了。那咱程序员只需要关注业务逻辑是不是就行了。至于转换这件事儿不用咱管，fast API自己就能干。

发言人   01:20

这个是fast API默认就支持的杰森格式的响应结果。当然除了杰森格式，fans的API也支持其他格式。我们来看一下，比如说HTMLS month，顾名思义就是返回HTM像plain text response，返回纯文本file response，那就返回文件。Streaming response它是流式的响应结果，通常处理几大文件，还有redirect response是重定向，这些都是fast API支持的响应类型。

发言人   01:55

当然这么多，我们不用都去记，咱们挑重点就可以。其中默认支持的Jason response，那咱肯定是要知道的。其次如果响应回HTML内容咱也要了解。再次涉及到文件下载的file response咱也要知道。那接下来咱就把这几种常用的或者可能用到的响应类型咱们来看一下。

发言人   02:20

首先咱们这一节先来看看Jason response，那代码不需要咱们再去写，因为咱已经是不是写过很多个接口，响应的都是python对象。Fast API是不是都会把它自动转成Jason response的？咱去干啥？去浏览器里边看一眼是不是杰森response？

发言人   02:40

首先咱们回到拍top里边来，先给各位同学看一下我们默认的代码。我只保留了一个跟路径响应的结果，是啊python对象message hello word。那咱的项目也在运行的状态，我们就直接去浏览器里边测试了。那接下来咱们回到浏览器里边来，先刷新一下，点开咱们的跟路径，接下来直接try IT1下excel的提交。那接下来咱们就得看看是不是阶层格式，对不对？好，那我们在哪儿看啊？注意了，找到server response或者看底下response也可以，这两个是一样的。那注意来看，在response header这里，这个是响应头，它里边有一项是content type，注意来看。

发言人   03:26

接着那说明当前接口响应的内容类型是啊杰森格式的那这个杰森格式哪来的？咱是不是没有写？Fast API是不是自己转换包装的？这个是fast API默认支持的响应类型Jason格式。当然这个响应类型Jason格式，因为我们已经写过很多接口了，就没有重复的来敲代码。我们看到响应的类型确实是Jason格式就可以了。

发言人   03:53

接下来各位同学也可以来测试一下自己之前写的任意一个接口是不是我们return的。是的对象fast API自动帮我们转成了杰森格式。好，看完之后不要走开，下一小节咱们就来测试另一种响应类型，HTMR race month. 我们下一小节儿不见不散。