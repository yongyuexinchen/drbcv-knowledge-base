14-FastAPI基础入门-异常响应处理_原文

2026年07月21日 12:50

发言人   00:00

零基础学AI月薪过万就来黑马程序员。Hello, 各位同学，这一节咱们来学习fast API处理异常响应的功能。那什么时候用到？就比如说咱们用户通过一个接口来查询数据，如果数据存在是不是肯定要正常响应？但怕就怕数据要是没找到，不存在怎么办？是不是也应该响应一个错误信息给用户，那咋办呢？在fast API里边有一个HTTP exception，它可以中断正常的处理流程，就是遇到没有找到的情况，咱们把正常的是不是要给它中断，然后返回一个错误响应。

发言人   00:42

我们来看一下具体的写法。首先咱们要想用HTTP exception，先要从fast API里面把它导进来。其次我们按照自己的需求写判断条件，一旦判断到异常直接rise，抛出HTTP exception。这个HDDP exception上有一个必填参数status code。显然是状态码是不是？比如说客户端错误一般就是400开头，服务器错误那就五百开头。比如说如果是资源未找到，那咱就状态码为404是不是就可以了？

发言人   01:17

好，逗号隔开还有一个detail。这个detail它是可选参数，是异常信息。如果像这样的接口发请求的话，真的出现异常了，会把追detail参数以及值以Jason的方式响应给客户端，用户也就能看到错误信息了。这个是HTTP exception的用法，接下来咱们也到代码当中来试一下。

发言人   01:44

来我们回到python里边来，接下来允许我先定个需求，我的需求就是我有一个接口，这个接口要按ID来查询新闻，但是我有额外要求，ID值是1到6多了没有如果是1到6直接响应正常新闻信息。如果不是，抛出HDDP exception，这个异常响应是不是就可以了？好，那想要用这个HDDP exception，是不是得先导进来？那我们从最上边，从fast API里边找到HTTP exception，把它导入。导入之后写接口是不是就可以了。

发言人   02:23

这个接口写过很多遍了，大家说APP点儿get，路径的话比如说咱们就叫news了。对，来个路径参数ID。好，那接下来处理函数，DEF get缪斯小括号冒号写完了没有？还没有，是不是得来形参，因为咱是路径参数，同名形参，来个int类型了。那接下来我们如果用户查找的ID存在，我是不是就正常return这条新闻信息就可以了。

发言人   02:59

那我们写的简单点，比如说就是ID那值就是形参ID的值，当然这是正常的。现在别管是110万，只要是ID来了是不是都能响应回去。但我们说咱的新闻ID就是1到6多了，是不是没有？如果不在这个范围，那就是异常了，是不是？

发言人   03:18

好，那咋办？来允许我先来一个，比如说ID list一个列表，把咱们有的ID先给它存上，那就是123456，是不是说好的1到6好，那有了之后，如果在列表里边，那就正常return，不在是不是就要rise HTTP exception。好，那咋办？这是不是典型的衣服？If咱们收到的ID not in ID list，一旦条件成立，是不是证明用户输入的ID不在范围内，那咋办？对，rise抛出异常，抛出HTTP exception异常，来个小括号，那接下来鼠标滑到这个HTTP exception上面，大家来看，这里status code是不是int类型，那底下的detail headers都加了optional，那代表可选是不是？当然我们一般state code和detail都给它设置上。

发言人   04:26

好，那接下来我们来写一下status code。那没找到是不是来个404就可以了，逗号隔开是不是再来一个detail，那来个字符串，就是您查找的新闻不存在是不是就OK了。好，那接口写完了，接下来咱们到浏览器当中来试一下。

发言人   04:48

回到浏览器里面来，允许我们先刷新一下，news带ID路径参数的接口已经有了，那接下来我们try IT1下。比如说我输入一个，先来一个正确的，比如说2 excute来看是不是OK没问题，正常响应了这条新闻数据给客户端了。好，那接下来我们再次，比如说来个20 excuse来看，您查找的新闻是不是不存在？而且状态码是2404。那我们目前在这个交互式文档里边是不是能看到状态码，能看到detail。

发言人   05:25

在这儿我还想要给大家看一下，如果是访问路由的话，结果是什么样。比如说我来个127，先访问到80，那接下来我们再访问news。比如说斜杠我来一个直接来一个错的了，比如说来个十大家来看，detail您查找的新闻不存在，那这样是不是就是真的出现异常了？Detail以及值是不是以Jason的方式给到客户端了？这个就是咱们fans API处理异常响应的HTTP exception的用法了。

发言人   05:57

接下来咱们回到代码里边，把这个个重点关键点给各位同学来看一下。首先要用异常处理HTTP exception，异常响应处理是不是先导进来。接下来按我们的需求写判断条件，一旦判断到异常了，rise抛出是不是就可以抛出HTTP exception。那状态码是不是必须得写？Detail也尽量写，因为将来用户是不是就指着看他了？好，这个是HTTV exception的用法。接下来该各位同学表演了，大家也来动手来试一下，把你的答案留到咱们的评论区，评论区不见不散。