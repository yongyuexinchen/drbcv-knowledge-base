路由传参(一)

路由传参(一)_原文

2026年07月22日 14:58

发言人   00:00

好，各位小伙伴们大家好，我是老肖。接下来我们继续回到bus API的路由器。现在我要开始讲的是路由传参，什么意思呢？大家都知道我们fast的AP主要是开发后端的API接口，对吧？那这个API接口客户端是可以发送请求，HTP也好，HTTPS也好，都可以发请求。发请求的时候，它也有可能会传参，如果我们是通过路由的路径或者路由的UL来携带这个参数的话，那么这样子的传参就称之为叫路由传参。其中这个参数就叫做路由是路由参数。

发言人   00:43

其实这个请求传参有好多种，接下来我讲的是第一种叫路由传参录入船上我们可以通过与python格式化字符串相同的语法来声明参数就可以了。其实我们在之前是提过，比如说我们要查询一个ID唯一的一个学生，我们要查询ID唯一的学生怎么查？首先有一个路由器，这个路由假设叫student对吧？Student没写完，student的地好，然后斜线加个一，通过这种方式来传达。你看这个一是一个参数，这个一这个参数实际上也也是也是组成了我们的路由由路由路径的一部分。就是我现在写的这个实际上是一个路由地址，其中路由地址里面包含了一个参数一对吧？这个一是通过路由携带过来的，这个参数一是通过路由地址携带过来的，所以这样的这种船仓，我们叫路由船舱，其中一这个参数也称之为路由参数。

发言人   01:49

接下来我们再给大家举个例子，让大家体会一下。来我们回到拍time中，比如说我们现在这样子，我们我们以后每次讲一个知识点还是不用换项目了，我们可以换一个什么换一个APP好吧？因为我们讲过这个路由分发现在非常简单了，对吧？好，我们创建一个python pack，这个拍拍过去了，我们讲的是这个陆游传唱，陆游传唱，那我给大家取个名字，我们叫什么呢？叫CH01，叫CH01好不好？但是零一里面我觉得在这儿去写我们的windows。

发言人   02:40

那我一样的，我用fast导入fast API点。点这个API，我们首先是要把直接打import叫API root对吧要创建一个吧要创建一个分路由，对吧，我们讲过的需要创建一个分路由，我们叫CH01算了。三是013是013C31，等于API root，PI fix，你可以有，也可以没有，好像是有一个，我们叫CH一算了，然后这个钱最需要斜线开头，然后加个summer，加个txt tax灯泡，路由挑战。

发言人   03:38

好，然后接下来我们就定义函数假设，假设现在我们要干嘛呢？我这个函数是要需要查询一个ID唯一的员工，查询一个ID为什么什么的员工，不是ID不一定ID为一，好吧？查询员工放的EMPOK，我先把这个函数的写完整，然后接下来我是不是要加I的符号CH一对吧？点我这里查询的话一般是用get请求方式对吧？

发言人   04:17

好，然后斜线我们知道就叫做EMP算了，知道EMP但是它要干嘛呢？它要传一个参数过来，这个参数我就把它通过路由里面携带，那这个就是我们的传参，这个参数我们用花框括起来表示。这里面实际上你要传参的，传到参数名字，我定义在这儿，我参数名字叫EMPID，好吧，叫EMPIDOK。好，这里就写个3M了，3M就等于查询单个员工，这就这就那那这这个参数是通过路由携带过来的，这样的参数我们叫路由传参。同时这个参数也组成了路由的DUR地址的一部分，明白吗？

发言人   05:01

这里要注意，好在fast的API里面，如果你你要传参，那接收怎么接收呢？直接通过函数的形参来接收就可以了。所以在这儿就加上一个形参，那实际上的名字要和你前面定义的这个名字要保持一致，好吧，我们这里也叫EMP下线ID，然后它是什么类型的，我们就可以使用这个类型类型类型的声明。因为我们前面讲过fast的API它是基于我们的3 python 3.7以后的版本，而且是最好是有类型声明的，好吧，有类型提示的。那么有类型提示你就按照这种写法，听到吗？这种写法来做。

发言人   05:43

好了，如果有人把参数传进来了，我这里需要自动的通过这个形参，通过这个函数的形参传进来，明白吗？传进来之后，我打一下。传入的参数为。滑坡括起来EMP小小ID看到没？好吧？好，然后一样的return，包括括起来的。好，然后OK。好了，这就是这个函数。我主要是给大家演示这个传参，以及接怎么样去接收参数。听到吗？

发言人   06:24

好，这个路由，这是子路由，我们写好了，写好之后，需要在这儿是不是要把它加载一下，对不对？Control d，当然这里面还要干嘛？还要导入先导入下from从记住这个打包的话你要注意，是从项目从项目下开始。CH01，然后点views import一个叫CHCS1的一个路由，所以这里面实验室使用CSCS1就可以了，CS这样CS一好，然后把它启动一下。

发言人   07:01

启动没有报错，没有报错之后，然后我们回到浏览器中，从这个接口文档开始说起。然后我们可以看到，有一个路由传上，看到没对吧？查询单个员工这个描述是不是有啊？你看这里是不是有一个传传一个是不是这里有一个parent一个要你传参，对不对？好，点击try that out，try out. 

发言人   07:23

接下来是表示我现在开始发请求了，放学之前你看我这里面是可以传它的。比如说我传一个ID为二，那这句话意思其实就是查询ID为二的员工，对不对？对吧？好，there is cute. 

发言人   07:35

那传进去了没有，这里打印，这里响应的数据是OK，那么传进去了，我其实应该在这儿看，对吧？在这控制上看，因为他只是把这个参数打印出来而已，对吧？打印出来了没？打印出来了叫传入的参数为二，看到没？对不对？

发言人   07:52

好，然后大家发现一下没有，我们的请求路径是什么样子的？是这样的一个请求路径，我把这个请求路径复制一下到这个地方，我们再来支撑一下来大家看一下这情形路径，前面这个CHE是前缀，EMP是这个函数的路由地址，好吧，还有后面的斜线二也是路由地址的一部分是吧？其实应该是这样讲，斜线EMP斜线二是整个我们的函数的路由，前面CH一是前缀对吧？回车键看到没？这样也可以，因为就是get请求，get请求在浏览器的地址栏中也是可以一样的发请求的。

发言人   08:30

好，发请求之后这边又可以传入了一个参数。当然你要传个参数三，你只要把二改成三就可以了。我再回去一下对吧？那就传了一个三，看到没，这就是传三好吧？

发言人   08:44

好，然后继续回到我文档中来。我告诉你在我们的这个在我们的fas API中，我们这个参数，其实如果你声明了类型之后，它还同时提供数据校验功能和类型转换功能，是自动给你提供的。其实我们这里传的参数，在浏览器地址栏中传的参数都是字符串的，在浏览器地址栏中其实都是字符串类型的，但事实上我们打印出来的类型实际上是什么呢？是整数，所以默认情况下，我们的fast API其实已经给我们提供了类型转换以及数据校验功能了。类型转换大家都知道，这类型已经从组串转换成整数了。

发言人   09:28

数据校验可能大家不太明白，来我们乱传一个参数，你看我在这传一个ABC，ABC是一个整数吗？不是，所以我们点击excuse的时候就会报错，报一个什么错在这里有place，什么什么什么有一个验证通不过，对不对？那validation validation error就是验证的错误，看到没有什么错误呢？Value must be a in tiger. 

发言人   09:53

第一个是整整形的意思，就是你传的值必须是一个整数。看到没？它有自动帮你做校验，你只有输一个整数，整数是什么都无所谓，它才正常的能够能够响应得到结果，明白吗？你在这边也是一样的，我们大部分情况下直接在接口文档中去测就行了，以后我就不会再再单独去测了，好吧？好，你看我们的fast的API，前面我们说过的它有一个很多好处，只有一个好处是什么？大大提高了我们的开发效率，因为它自动帮我们做了类型转换，只要你标记的类型，它就会帮你做类型转换。你标记是int自动给你转换成硬件。如果转不了，那一定是你参数传传的有问题，他会帮你做校验，好吧？

发言人   10:39

OK好，接下来有人说老师有没有一种可能你这是你这是你这个接口是查询单个员工的，有没有可能查询所有员工对吧？那查询所有员工你大概你这个函数可能是这样子的，叫find all对放的EMP。但是你的接口你想你的接口的U你这个接口的UR地址，路由的UDR地址还是不变，你还想是EMP行不行？就是要查询所有员工，所有员工怎么给不变可以吗？就我这我这个我这里面这个路由不是这个视图函数，不穿插，或者说我们这个接口不想传它，不用传它对吧？不用传它OK那我这里就不传对吧，这里不用管它。然后打一句话。查询所有员工。

发言人   11:44

这样子行吗？这样子你发现没有？首先你的请求路径这个地方是不是相同，函数是不是都是get对吧？那这个时候会出现什么情况？大家想想这时候会出现这个会会不会会不会有干扰？先问大家这种情况下会不会有干扰？会不会？答案是不会的，这种情况下不会有干扰，来我们试试。放心。

发言人   12:22

好，路由传餐，这是查询所有员工，这是查单个员工，看是不是来了，对不对？好，先查询单个员工的这里面我们点击try out对吧？然后我们输入个33，你看这边是可以正常的。好，把这个收起来，再点击这个查询，所有员工点击try IT out他账号是可以的对吧？他请求地址是没有问题的。

发言人   12:44

你说老师你这样子请求跟在地址栏请求实际上是一样的，get的请求是无所谓的，知道吧？一样的。好，但是接下来我给大家搞一个错误的假设未来我是这样子写。我是我是写这么一个，我把这个find all改成这样子的一个群，把这个接口改成这样的一个路由地址，叫斜线EMP下的OK来我们来运行一下，看看这种情况下有没有什么问题。没有，来我们运行一下，那运行一下的话，那回到这个文档中来刷新。

发言人   13:22

这个是放到all，对不对？点击try IT out，点击excute，excute，报错了对不对？报错了报什么错报什么错。你看这个错误信息它在这儿显示了，所以input should be。我不知道各位看得清楚吗？把它放大一点，input should be对吧，就是说你输入的B应该是一个应该是一个合法的整数，对吧，你你你输入的是一个非法的整数，不能够把它解析成一个字符串，对不对？

发言人   14:00

什么意思呢？就是你发现没有，它根本就不是进到我们当前这个视图函数中来，他好像进到这儿来了，因为进到这儿来他才会做类型的校验，对不对？这里面我们是不需要穿它的，他把这个二当成了参数了，你知道吧？所以如果如果你这边有路由传参，那么就很有可能跟其他的路由的跟其他的跟其他路由会有冲突，这种情况会有冲突，那么这种冲突怎么解决呢？

发言人   14:35

你就要考虑这个顺序问题，因为我们这个路由是按照顺序去一个一个匹配的。先去找第六行这个路由匹不匹配，发现匹配吧。他把这个or当成EMPID这个参数了，对吧？因为它这种是匹配的。但是进了函数里面之后，发现你这个参数要的是整形，然后我传过来的是一个一个单词all，所以它就会出现一个数据校验失败的一个错误，对吧？OK那那我们想办法能不能让这个接口先被匹配了，可以的那你要把它放到前面去，你看我们换一下顺序，放到前面去，然后再启动，这个时候就没问题了。那我们再刷新一下，这个刷新你看我这个在前面的对不对？

发言人   15:29

点击创意的2，然后点击is q你看是不是一切正常，对吧？我代码没动，我就是把它换了一下顺序，各位换了一下顺序就可以了。因为它是按照这个顺序，我们的路由是按照顺序进行匹配的，谁先匹配到就先进入哪一个实数函数，明白吗？

发言人   15:48

进了失传函数，如果发现有错那就报错，没有错就执行，就是正常执行这个制度函数里头的代码，好吧，是这么一个顺序。OK好，所以你千万注意了，当然习惯上我们不应该这么做。因为你要查找所有的员工，你就应该什么跟这里保持一致，不要加，就这样就行了。因为他这样的情况下是不冲突的，不管你的顺序怎么变，他都不冲突的，知道吧？

发言人   16:16

就是同样的一个员工，这个处理员工的真相改查，你用一个前缀用一个前缀，你后面可以传参，后面的都都把它当成路由参数就行了，明白吗？包括未来我要删除一个ID为什么什么的员工，那我要写一个视图函数，怎么写呢？记住，我是要删除，那删除的话这个函数叫D地址，对不对？吧？我也这样写，叫删除单个员工，删除单个员工明白吗？这还当然这函数名字还是不一样。Yeah delete. 

发言人   17:00

你看这是删除某一个员工的对吧？删除某个员工我也是这样写。那这样写的话，老师你这不是一模一样的吗？是他的请求的路由地址确实是一模一样，但是请求函数不相同，一个是get，一个是delete。所以它会自动帮我们区分的，我们不用管。

发言人   17:23

还记得吗？我们说过我们的路由映射实际上是由三个部分，哪有三个部分组成，一个是请求路径，一个是请求方法，千万别忘记了，还有一个请求方法，好吧？好，然后才是找到对应的函数。好，这节课我们先讲到这儿，讲了比较基本的基础的一个路由船舱的这么一种方式。然后路由船舱还没有讲完，后面还给大家讲一些高级的路由船舱的一些操作。好，谢谢大家，再见。