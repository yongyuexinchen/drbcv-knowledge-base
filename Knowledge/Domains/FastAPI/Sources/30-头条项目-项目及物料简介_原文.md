30-头条项目-项目及物料简介_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，很开心可以在评论区看到大家发出的练习代码以及催更留言。那么我来了，从这一刻起，我们要一块进入到项目环节了。我们从第一个项目AI掘金头条开始逐步搭建web服务器。

发言人   00:18

这一小节跟各位同学汇报一下，我们先不写代码干啥呢？我们先从整体看一下AI绝境头条这个项目包含哪些主要功能呢？那这些功能又是怎么设计的呢？以及我给大家准备的配套物料，给各位同学来看一下。首先咱们先看项目的主要功能，那看项目功能咱是不是得到运行好的项目里边，那项目我已经提前跑起来了，我们直接到浏览器里边来看一下。

发言人   00:50

咱们整个AI掘金头条这个项目，围绕着新闻功能跟用户功能两大功能展开。首先新闻新闻从上是新闻的分类，底下是不是就是一条一条的新闻。分类包含头条、社会、国内是支持用户点击切换分类的。好，每个分类之下都有很多条新闻。我们任意点击一条新闻就进到了新闻的详情页面，可以看到新闻的所有内容。稍微向下滚动，还有一个相关推荐，这个相关推荐展示的就是其他的同类新闻。这个是新闻模块大致的功能。

发言人   01:32

我们说除了新闻是不是还有一个用户模块？用户模块无非就是登录注册，修改用户信息等等等等。用户登录之后，还可以对新闻进行收藏，还能展示自己的浏览历史记录。当然新闻跟用户是两大核心功能，我们还有一个AI问答。这个AI问答在最开始跟各位同学介绍过，是不是调用的是阿里百炼的千问模型实现这个问答功能，当然我们在这里是不是调用千问模型，后边是不是咱自己训练部署一个模型。好，那这些是咱们眼睛可以看到的AI掘金头条这个项目的实际功能。

发言人   02:18

那这些功能都是怎么实现的呢？是不是需要我们去规划设计一下？那我这里提前把这个项目从0到11个规划流程设计思想整理成了一个文档。我们一块儿来看一下。首先大家看到的是一个项目后端设计说明文档。这个文档就是记录咱们这个项目的整体架构，比如说包含项目的概述、技术战模块的划分、数据库表的设计、API接口说明等等等等信息。简单来说，这个文档其实就是咱们这个项目的一个说明书。

发言人   02:57

我们就一块儿从宏观的角度来看一下这个项目的整体设计思路。我们从最上来我这儿把这个文字稍微放大一点。大家来看整个咱们这个项目仿的是今日头条，就是相当于是一个新闻资讯类的一个后端API服务。好框架采用异步的python框架fast API。那我们前两章节是不是学的就是fast API？好，再往后数据库mysql l当然咱们不用手写SQL语句，通过ORM的方式来操作数据是不是就可以了。项目的主要功能，刚才我们也看到了像用户功能的新闻功能、收藏和历史记录等等等等。要完成这样的一个系统，咱们要储备的技术栈在这里给各位同学列了一下。

发言人   03:48

Fast API，框架就不用说了，肯定要有mysql数据库，当然咱们采用ORM的方式去操作数据库。好，再有就是异步的数据库驱动。咱们在讲这个circle alchemy ORM的时候，是不是就已经涉及到异步的方式，异步数据库驱动了？

发言人   04:07

好，那接下来再往下，往下有一个密码加密，就是咱们有用户功能，用户要登录要注册，是不是就要有密码？那肯定不能明文，那就不安全是不是？所以咱们这里用pcl lab这个模块对密码进行加密。

发言人   04:25

那再往下缓存系统，咱们这个项目是设计了缓存的。缓存功能是干啥？就是对一些高频访问的数据，咱们可以先把它放到缓存里面。我们发请求不直接请求mysql数据库，而是先看看缓存里边有没有，有就从缓存里面加载，缓存里边没有是不是再发请求。那这样的话，咱们整个系统的性能是不是更高一点，那circle数据库的压力是不是就能降低了？好，这是咱们设计的一个缓存系统。好，那看完了技术站了，咱们再往下，顺序来到下一个，就是项目结构。这个项目结构，我先简单说一下，咱们待会儿开一章节单独的去搭项目结构。

发言人   05:11

项目结构的意思就是我们做一个项目代码肯定很多。我们原来学基础知识是不是放到一个PY文件里边？那现在项目这么庞大，这么多代码放到一个PY文件里边还合适不，是不是就不合适了，那怎么办呢？拆分拆到不同的文件里边，对不对？拆分我们可以按照这个软件的功能进行拆分。在这里我不详细展开说，待会儿咱们单独来一章节把项目结构打出来给大家讲明白。每个文件每个文件夹儿代表是啥意思。好，项目结构咱们知道要拆分就可以。

发言人   05:47

好，我们再往下就是项目的功能，这个项目功能相信同学们已经比较熟悉了。我们这里是不是有用户功能，用户登录注册、获取信息、修改信息等等。新闻就是分类的获取，新闻列表、新闻详情，当然还有一个浏览量。好，那再往下就是收藏模块，可以添加收藏，可以取消收藏新闻，可以看收藏列表，还能清空，还可以检查收藏的状态。好，再有浏览历史，这个浏览历史就是用户看了哪些新闻，可以看这个历史记录，还能删除。

发言人   06:27

好，那再往下就是缓存，我们这里设计就是新闻详情做缓存，还有新闻列表分类数据，用户的历史记录做缓存。那这些我们设计了缓存功能，为什么他们要设计缓存功能呢？大家想，用户登进来是不是看新闻的，频繁访问的数据，先看缓存有没有，没有是不是在向数据库发请求，拿数据库里边的数据就行了，那这样咱的系统性能才高。好，这是咱们的缓存设计。好，那看完了功能了，是不是包含用户新闻、收藏、浏览历史和缓存，对不对？

发言人   07:08

好，接下来到数据库的设计了。来大家来看看，在这里主要给各位同学介绍六张数据库表。我们来看看为啥是这六张。首先大家这么看啊，有一张用户表儿，是不是还有一个用户令牌表？用户表肯定不能少，因为我们有用户模块，存用户信息对不对？这个我们肯定能理解。

发言人   07:33

好，那下来再有用户令牌，那啥是令牌？就是用户的登录凭证，做用户登录认证用的。这里主要涉及到两个数据，一个是令牌，一个是这个令牌的过期时间。将来就是看这张表里边有没有令牌，有没有过期。有令牌没过期，那说明这个用户是登录的状态，否则是不是就没有登录。那你没有登录，假如说你要看收藏，看历史记录，看个人信息，那是不是不能让他看啊，得让他回退到登录。好，那么这是用户令牌表，做用户认证功能用的。好，那这两个表大家是不是清楚干什么用的了，对应着上边的功能模块来设计的。

发言人   08:22

好，接下来到新闻模块，新闻有分类，我们要有分类表，我们有新闻还要有新闻表。好，我们有收藏跟历史记录，对应收藏表跟历史记录表也跑不掉，也要有啊。好，这六张表是不是对应着上面的功能模块得出的结论？

发言人   08:45

了解了数据库表了，再往下缓存的设计，缓存层咱们用redis，至于redis该怎么用，等我们讲到这个缓存功能的时候再说。现在大家知道我们要有缓存策略，让咱们的系统性能更好一些，响应速度更快一些。什么样的数据我们要进行缓存呢？高频访问的来看，新闻的详情，新闻的列表分类，这个是新闻分类，还有就是用户历史记录，这四大项我们做缓存的处理。在这里给各位同学规划了，比如说缓存，其实就是把数据先给它存到redis里边一下，那存的话就要有对应的K名。在这里稍微设计了一下K名，就是每一个要存储的内容是不是我都设计了一个K名，到时候咱们对应着来使用就可以了。除了K名，还有这个过期的时间等等，这个是咱们的缓存策略。在这儿大家先知道这个项目，咱们设计了缓存策略就可以，具体该咋实现，咱们后边慢慢来。

发言人   09:55

好，那接下来再往下，再往下来看API接口说明。在这里提前说这个不是全量接口，我隐藏了一些信息，先给大家看一部分。提前跟大家说，API接口咱们设计的是目前咱们在企业当中常用的风格restful风格。大家来看，首先每个模块它的请求路径，前边儿来看这个前缀标识是不是都是一样的。我一看到API user就知道它是用户模块的相关接口。

发言人   10:30

好，那再往下看新闻，新闻来看看API news，API news。API news也是一样，就是咱们的路径前缀，每个接口是不是都是一样的标识。好，那也有请求的方法，是不是提前规划好了。那大家来看，用户模块的、新闻模块的、收藏的、浏览历史记录的，是不是全都规划好了。当然在这里我是隐藏了向请求的参是啊响应的结果等等。待会儿我们会有一个专门的API接口规范文档，这里边只记录接口的细节，像路径方法。参数响应的结果等等等等，都会给大家标注明确。在这儿我们先知道接口规划，我们也采用restful风格就可以了。

发言人   11:20

好，那接下来再往下，认证机制，咱们刚刚说是不是采用令牌，这里的令牌我们就是token。Token用户登录成功之后会返回一个token。其实就是在我们的那个令牌表里边，创建了token，设置了过期时间。那同时前端会怎么做？前端注意会在请求头里边设置一个authorization字段，把咱们的令牌给它存起来。这个是认证的机制，前后端约定好了。好，再有就是错误的处理，像一些认证失败，不存在，那我们就四百开头，比如说返回401。那如果是服务器这边的错误，那我们就是500的状态码。

发言人   12:05

好，再有就是开发的规范，我们一律采用异步数据库操作。当然我们从学ORM的时候，是不是就已经是异步数据库操作了。再有就是密码，咱们加密处理，pass lab模块。好，接口返回的格式也是统一的，响应的都是阶层格式，接口文档跟示例待会儿我会详细的给大家展示。再有就是能抽取能封装的，咱们一律抽取封装，方便去复用调用。

发言人   12:37

好，再往下，最后一个就是性能优化。我们这里涉及到的性能优化就是redis缓存热点数据，高频访问的数据，还有就是异步数据库操作。这个是咱们整个这个项目从0到1从头开始规划的时候，一个设计说明文档。大白话来说，是不是就是一个说明书，包含了咱们从零开始规划的整体细节。希望给大家看完这个文档之后，大家对咱们AI掘金头条这个项目整体架构、功能模块、实现方案等等等等有更清晰一点的了解。

发言人   13:16

好，那看完了这个设计说明文档了，接下来我要给各位同学来看一下，我给大家准备好的咱这个项目要开发使用的配套物料。好，那我们来到一个文件夹里面，大家来看，首先这个说明文档我是不是放在这里了，在它的旁边还有三个文件夹。这三个文件夹就是我给大家准备好的配套物料，是我们后边做项目开发必不可少的。

发言人   13:44

首先第一个就是接口规范文档。我们刚刚在看这个设计说明文档的时候，我说它里边有接口，但是这个接口我只列了请求的路径和方法，对不对？那像参数和响应结我都是忽略的那我会是不是给大家详细版本，那就在这里了。大家进到文件夹里边是不是有一个API接口规范文档，那么它打开就能看到咱们接口的详细规范了。那我们来看一下，来找到这个API接口文档。概述不说了，UIL就是本机是不是好认证方式，如果需要认证token的，请求头里边添加authorization这个字段，存的就是咱们的token值。

发言人   14:29

那接下来响应格式都是统一的杰森格式，刚刚咱们在说明文档里边是不是看到过，那这个杰森包含啥内容呢？来看看code状态码，message提示是成功还是失败，data是不是具体咱们要的数据，那这是咱们的响应格式，都是统一的这样格式。那通常就是data里边的内容是不是有可能是不一样的，有可能是新闻分类或者是用户信息，对不对？

发言人   15:01

好，接下来重点看接口的详情。比如说用户管理向用户注册来post方法，API user register，那前面的是不是每个模块统一的路径前缀，对不对？好，请求的接口地址看到了，来往下看请求参数，那用户要注册是不是携带用户名和密码，那这两个参数是啥类型呢？说明就是啥作用，是不是全都列的很清楚。

发言人   15:37

好，那接下来再往下来看，响应示例是不是就是响应回来的结果。首先来看code message data，data里面包含啥？注册了，注册完是不是立刻登录？好，有一个token，就是用户的那个访问令牌。再有就是咱们这个用户的详细信息，包含他的比如说名字、简介等等等等，是不是？好，那这是咱们用户的一个注册。那接下来来看登录也是一样，接口地址参数响应结果的示例是不是全都有。

发言人   16:14

好，那我们再看看新闻，挑一下看看新闻模块。比如说新闻分类，新闻分类请求的地址是不是规划好的？前端未来发请求是这个路径，咱后端提供API服务是不是也是这个路径方法，是不是get好参数呢？两个类型是不是都是整型，而且有默认值？

发言人   16:40

好，那响应的结果code message data。那注意来看，这个data是不是有一点不一样，跟用户不一样了。因为我们要获取的是新闻分类列表，我们响应的这个data是不是也是一个列表？那列表里面包含就是咱们的一个的分类了。每个分类是不是一个字典？这个是咱们新闻分类列表这个接口。

发言人   17:08

当然再往下也是一样，比如说像新闻详情，大家来看接口地址参数响应的结果示例是不是都是有的。那未来咱们就参照着这个接口规范文档来写咱们的路由，按要求来响应，结果给到前端是不是就OK了？咱们就简单看这几个接口，其他的也都是一样，都是有路径、有参数、有响应示例，就不一个一个看了。等后边咱们开发到哪个功能来具体分析就可以了。

发言人   17:41

好，那现在问大家，咱要开发API接口规范文档是不是必须要有没有他？这咋写？是不是？好，那第一个物料给各位同学看了一下。

发言人   17:53

接下来咱们回到刚刚的文件夹里边，返回到上一层下一个数据库circle文件。那这是干啥的？先跟各位同学汇报，咱是不是一个新闻资讯类的web服务器，是不是真的得查新闻分类，是不是得真的能查出来新闻？这些新闻我提前给大家准备好了。我们直接把这个circle导入，那表就有了，那就是新闻那个表就有了，表里边的数据也有，那咱是不是就能查了？所以这个circle文件，待会儿咱是不是导入到circle当中去？

发言人   18:31

好，那这个要有啊，要不然查不出来新闻，咱还得一个一个添加，是不是自己模拟，那有点麻烦。所以咱借助一个circle文件，目的是要那些新闻相关的数据。好，我再点一个返回。

发言人   18:46

接下来还有一个03前端项目代码，那这是干啥用的呢？这个提前跟大家说，前后端分离开发这个代码帮咱们做测试用的。就是它它有了前端界面了，那后端是不是咱们提供数据，是不是写路由响应结果，响应结果给谁用？是不是就是给这份前端代码用的？他能请求到数据是不是就能看见新闻，看见分类了。

发言人   19:16

好，那这个代码大家把它下载好，要用。那这个代码是我提前整理好的，是前端的VUE框架。未来就是这份前端代码向我们开发的这个后端服务发请求。拿咱们的数据，这个项目大家不用看原码，能把它运行起来就可以。

发言人   19:38

接下来同学们重点来了重点来了，重点来了，怎么运行？我说一下分两步，分两步。首先第一步安装node，第二步执行一个命令就可以。那安装node咱得先下载。那回来我在这里是打开了node的官网，各位同学也可以联网，来到node的官网，百度搜node，去官网就可以点到下载。接下来可以选择你自己的电脑操作系统，选择版本，那往下针对自己的系统去点下载就可以了。那我这里就不点了，因为我已经提前安装好了。安装它就是双击下一步下一步下一步下一步完成就可以。

发言人   20:25

好，node下载安装完之后，那回到刚刚咱们的这个前端代码里面来，我说一下是不是要把这个项目跑起来，那怎么跑？运行它要靠命令。如果是windows操作系统，那太简单了，直接在这个文件夹里边按着shift键，是不是就能打开那个power share或者是这个命令提示行。那如果是迈克操作系统，那咱就找终端CD的方式切到咱们这个文件夹里边，就是咱这个项目目录里边，我这儿是不是就已经切过来了？新闻资讯news是不是？好，那切过来之后运行的命令，各位同学来看一下，NPM run DEV。好，那我们敲回车来看是不是他给到了我们一个地址，local host 5173，这是view运行的接口，5173好，那接下来大家要做的就是回到浏览器里边来开一个local host 5173，敲回车大家来看。

发言人   21:37

我右键点个检查，因为我们是这个模拟的移动端的页面，我们右键点检查，然后把右上角这个给它点开，给它点开点开之后，大家可能看着右边这个报错是不是有点胆儿小。那我说一下，不要怕，正常的，因为我们后端没有开服务，换句话说咱后端是不是一个代码都没有写，那这个前端频繁的找咱们的后端发请求，发请求是不是一直报错？那正常的，那目前大家看到就是分类有一点点，这儿是不是一直加载，但是不是啥也没有，为啥？因为后端服务没开，所以这个给到的是一个前端的代码。未来咱们后端比如说分类写完，那分类就会完整的展示出来，这只是写了默认的七个，未来我们不止这七个分类。

发言人   22:31

好，那未来比如说这个新闻列表这个接口写完，那这里边就会有数据了。那未来比如说详情写完，详情就能有数据。所以目前大家把这个NPM run DV跑起来之后，发现它是空的。还哇哇哇哇疯狂爆炸，正常的，因为咱后端，不好意思，没有，对不对？

发言人   22:54

还没有好，那以后咱们就是从pym里边噔噔噔携带面开发接口接口开发完，交互式文档一看没有问题，咱就得到这个前端页面里边刷新，看看数据能不能正常显示出来。如果能，哦耶这个接口功能是不是就搞定了。那这个就是咱们未来写代码的一个测试流程。

发言人   23:18

好，那截止到现在，咱们整个项目的功能看到了，项目的规划文档，看到了配套物料，大家是不是也知道有啥了？那我回到这个配套物料里面来。在这儿温馨提示各位同学要在咱们的视频下方来领取这个项目的配套物料，这里面包含刚刚给大家看到的后端设计说明文档，就是咱这个项目的说明书，还有三个文件夹，接口文档、circle l文件、前端配套的代码，这三项。这三个文件夹是不是就是咱们待会儿写代码要用到的配套物料？那大家记得点视频下方来领取这个配套物料。

发言人   24:03

好，还有就是这个第三，前端项目稍微复杂一点，安装一个node切到咱们的项目目录里面，打开终端NPM run是不是就能跑起来了。各位同学在咱们视频下方来领取资料，顺便把前端这个代码让它运行起来。我们做好准备，从下一小节开始，咱们搭建工程结构，就开始搭后端服务了。我们下一集不见不散。