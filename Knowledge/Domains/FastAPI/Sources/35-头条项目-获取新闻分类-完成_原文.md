35-头条项目-获取新闻分类-完成_原文

2026年07月21日 12:49

发言人   00:01

Hello, 各位同学，上一节咱们写完了获取新闻分类的模块化路由了。那这一节我们继续向下来查询数据库里边新闻分类响应结果。但是我们说查询数据库或者将来的增删改查都OK，是不是我们要基于ORM工具来操作数据库里边的数据，那我们就得先有啊模型类。所以咱们先定义模型类，然后来查询新闻分类，接下来路由调用响应结果。

发言人   00:30

那我们来看一下模型类该怎么定义，大家还有印象吗？模型类是不是我们需要两个，一个鸡肋，一个数据库表对应的鸡肋。有要求继承declare base。那数据库表对应的模型类要继承这个鸡肋，当然所有的属性和字段都不能凭空捏造。是不是我们要参照数据库设计文档，或者是像咱这个项目，数据库已经提前有了，那我们就参照表结构来定义模型类是不是就可以了。接下来咱们就到payton m当中定义模型类，基于模型类查询新闻分类来响应结果。

发言人   01:09

我们回到拍场里面来，各位同学注意，我们上一节儿是不是写好了获取分类这个路由了，那我们接下来要干啥呢？注意，这是要获取新闻分类，但我们知道它是不是应该先要去查询数据库里边的数据，接下来才能return响应结果。所以我在这个地方先打一个注释，我们是不是要先获取数据库里面新闻分类数据，是不是才能下一步return响应结果。我要获取数据库里边的新闻分类，那我们基于ORM操作，所以是不是就得先定义模型类才可以？好，有了模型类了，咱们就来封装查询数据的方法，封装查询数据的方法。那这个方法在哪儿调呢？是不是就在我们注释这个地方调用就OK了？

发言人   02:08

好，那我们先从第一步定义模型类来，那要写类写代码了，先找个地儿放一下呗，放在哪儿呢？Models里面是不是比较合适，模型类就往这儿放，那我们新建一个python file起个名字，关于新闻模块要用到的模型类全往这里边放，直接回车。那我先倒个包，像那个declare base是不是得提前倒包？Circle alchemy点ORM impart dick lariated base当然没完。还有一个map是不是设置属性类型用的，还有一个mapped colum映射字段用的。好，那接下来我们来写类，这个类咱说了不能瞎写，参照哪里写？

发言人   02:54

对表，那我们把database打开，找到尼欧斯开它里边是分类，是不是头条、社会财经等等分类，八个分类都在这儿。好，我先关掉。咱是不是参照这个columns里边这五个字段来写类就可以了。那这五个字段，咱们稍微插一下，create at update at创建更新时间是不是一般是通用的那待会儿新闻不瞒大家说，展开news来看看新闻，里面也有创建更新时间。那通用的就往base里边放，表自己的独立的独有的就往数据库表对应的模型类里边放，是不是就可以了。

发言人   03:42

好，接下来我们专注在分类这里。好，那把确定了，把这个create at update at这两个时间放到base里面。好，那我们class一下base。注意了看啊通义灵码是不是发功了，那我直接tab一下了哦啊那这个pass我不要，我是不是把俩时间给它放进来，那我就直接写create了，CREATE create at那帮我生成了时间。那来看这个data time是不是没有，那我给它导包导一下，从这个data time里边，from data time in part data time给它导进来。后边这个大写的data time它是circle alchemy里面的，我们也把它导包导进来，找到circle alchemy里面的data time。

发言人   04:32

好，那来检查一下，这个default默认值是不是就是时间，OK comment创建时间没问题。好，那现在有了通义灵码，我这个小助手是不是直接把我的创建时间就搞定了。那下一步来看apple date at是不是更新时间走你检查一下类型是小写data字段，类型是大写data time，默认值和更新的时间是不是用的都是now，comment说明是更新时间，OK是不是没问题。

发言人   05:07

好，那现在我的base两个时间是不是搞定了？那接下来我们往下敲回车，我们是不是就要有表对应的类了？好，那class一下，来个名字，我这里得是factory，CATE factory。来我直接tape一下了。那tape之后大家帮我看看table name表名字charactery行不行？这个可不能乱写，它是不是得跟我们的表名字是一样的，所以我们稍微把它改一下。News下划线category不是司，是fact。

发言人   05:49

好，那表名字搞定了，下一步就是咱们的表自己独有的字段了。那这个字段一个一个来，ID是int类型是不是没问题？映射字段是主键是不是也没问题，自增是不是也没问题？那差类型对不对？我们这样导一个类型进来，from circle acoming import igor, 我给它导一下，这个是整形。好，那接下来类型ID的类型integer写一下，逐渐有了自增有了，那我再给它加一个common common，common的话比如说这就是分类的ID好，那接下来ID搞定，往下name检查一下咱是不是也有name，但只不过我们要按50个字符。好，那类型是STR没问题。那映射字段的类型我们要导一下，从circle alchemy里面导入大写的S随便。

发言人   06:51

好，那接下来往下走，map的么类型写一个string小括号，那我们要50个字符，我写一个五十，那我再加一些限制。Uni q唯一我写一个true，因为我们的分类名字是不是不能重复。那uni QE我来一个true，那再有now able允许为空吗？我来一个false，是不是不允许为空？好，那再往下，咱们comment是分类名称，是不是没问题？

发言人   07:23

好，接下来咱们往下走下一个属性description，大家帮我检查一下需要吗？没有description，有一个salt order是排序，我们把这个description删掉，改成salt ORDER salt order。来tape一下，map的类型是int类型，大家看一下int类型是不是没问题。映射字段的类型是in tiger来整形默认值是零是不是没问题？靠近说明是排序也OK。好，那我加一个加一个脑able为false，就是不允许为空。那现在咱们这个分类表的字段是不是也搞定了？那我把database这个插件儿先关掉了。

发言人   08:11

好，那搞定之后，最后我再加一个小方法，不知道各位同学还记不记得REPR用来干啥的？打印对象可以用到咱们return的这个结果。这样的话打印出来是不是能看到咱们这个ID值、name值，还有就是排序规则。如果没有这个方法，打印出来的是对象所在的内存地址，那是不是不容易观察具体的数据，所以我在这儿加一个方法。好，那现在咱们的模型类是不是就搞定了？

发言人   08:46

搞定了模型类了，各位同学来看看我们下一步要干啥。封装数据库查询数据的方法对不对？那我们这个方法是不是要再找一个地儿来写，不能放到models reuters里面是不是？好，那展开咱们的目录结构放在哪儿呢？对，CRUD是不是放数据库增删改查代码的文件夹？那我们去新建一个新建一个python file，因为咱们现在做的是新闻模块，那我就叫news。关于新闻模块数据库增删改查的方法就封装到这个文件夹里边了，封装到CRUD的news里面来。

发言人   09:29

好，那我来个方法，数据库操作肯定是异步的那我就think DF我就叫get characterises，那我直接tab一下了，允许我先写一个pass。好，那大家来看我的通义灵码帮我生成了两个参数，一个skip一个limit，是不是做分页用。而且大家看着这两个参数熟悉吗？对，在rotor路由里边是不是也有这两个参数，那他俩是不是一样的？一会儿这里的参数值是不是就传到get charactery这个方法里面来用，是不是就OK了？

发言人   10:11

好，那我们现在有了方法了，有了两个参数了。但提前跟大家说，我们还需要一个参数，还需要一个形参，就是数据库。因为咱们这里是数据库的增删改查操作，是不是得有数据库绘画，所以我们再准备一个形参，比如说我就叫DB了，DB逗号隔开，当然我们是不是也可以写一个类型，大家还记得那个类型咋写吗？允许我导个包，circle alchemy，点EXT，点a think IO里面import大写的think session类型。那咱的DB冒号我就用这个think section了。当然这里提示后边两个是默认参数，那前面这一个是不是没有默认值，所以我得把这个DB是不是放到默认参数的前面来。

发言人   11:06

好，那接下来咱们就回归到pass这里，是不是写功能，咱要干啥？是不是select查询，然后执行这条语句拿结果是不是就OK了？好，那接下来我写一下，首先是不是select要基于模型类是不是去查询，那我这个select它没有import到circle alchemy里边的select，那我这儿是不是写模型类，模型类是不是在models里边，那我们是不是得把这个factory给它导包导进来？好，那接下来回到咱们的CRUD里面，同样导个包，from models点儿news或者是from me models import整个news是不是也可以？我就models点news，in part我那个fact给它导进来。好，那我的select是不是就能写了？Select这个factory模型类就有了。

发言人   12:06

好，那接下来正常写分页的话，是不是offset一下，然后再点儿limit一下是不是就可以了。那我的outside值limit值在这里，我是不知道是什么的，我直接用这两个参数值是不是就可以了。好，那在这儿那就skip一下limit小括号的参数，就是limit是不是就OK了。那这一条是不是咱们的查询，那我把这条语句儿来存一下，STMT等号赋值，那有了语句了是不是要执行一下？那我await是不是用咱们的DB，就是未来传给我的数据库绘画，对不对？点excute执行咱们上边这句select，执行完是不是会拿到一个结果，那我给它存一下，result一下，那result里边儿是不是来提取，那我先tap一下，result点scale as点，但凡查出来的是不是全拿过来，当然我这儿就不再存charactery了，直接return了。好，那我们这个方法是不是就是查询数据库的语句儿，让DB数据库绘画执行。然后接下来提取结果并且return，是不是return到调用这个方法的位置？

发言人   13:27

好，那当然这个方法在哪里调用呢？允许我先把这个方法截一下，截一下钉在咱们的屏幕上，放到右下角来回到rooters里边。The news是不是在咱们路由处理函数这个位置，要调用刚刚封装的这个查询方法？

发言人   13:45

好，那接下来我要调用方法，是不是还是得先倒包？那我们来到上面，那from一下from CRUD import and news，那我们往下走，在空行这里，我是不是就得调用news里边的get factories这个方法。那这个方法skip limit是不是要传下去？但是在这儿跟各位同学说一下，因为咱只有八个分类，其实也不需要做分页，对不对？但是我这里留了两个参数，为了将来分类，比如说确实很多了，是不是可以做分页？那我们这里就偷个懒，简单一点，直接把这两个参数传下去。那个分页的处理计算，咱们这个接口就先不做了。

发言人   14:33

好，我直接把两个参数的值传到get charactery里面。当然大家来看，我是不是还差一个DB那这个DB哪来？是不是咱们要注入一下数据库，咱们的配置是不是已经配好了？有一个依赖项叫get DB大家还记得吗？我们是不是就把这个get DB注入到路由处理函数是不是就可以了？

发言人   15:02

好，那接下来我要为难一下，大家还记得怎么注入吗？Dependence是不是？那dependence得先倒包，从fast API里边导入dependence，那下一步路由处理函数我是不是就来一个参数？比如说就看我的小助手又上线了DB类型dependence，get DB是不是都有了好类型导包，导包导进来。那这个get DB也是一样，从config里边是不是给它导进来，来往上检查一下，从config DB config里边是不是import了依赖项。好，那接下来我们往下news点get factories方法里边是不是得按顺序先把DB传过去，然后再传skip和limit。好，那回到方法调用这里，我们把DB是不是给它传下去。

发言人   16:00

当然咱们这一句还没有写完，我们get charactery是不是数据库操作。所以我们采用异步的方式await它一下。那await之后是不是能拿结果？那我在这儿来一个characterises把它存一下，开个JORIES把它存一下。接下来响应结果注意return的data，就不是写死的新闻分类列表了，而是characterises是不是就可以了。

发言人   16:31

那我们现在定义了模型类，封装了查询数据库的方法，是不是现在也拿到了结果响应了。那我们来验证一下能不能拿到新闻分类。好，我们回到浏览器里边来先刷新一下了，找到分类，try IT1下参数，我就用默认值了。Execute见证时刻的奇迹。往下走好像两百好像成功了。来看所有的分类八个是不是全拿到了。那我们现在的获取新闻分类的接口是不是就OK搞定了。

发言人   17:05

接下来搞定了接口功能了，我们回到代码当中稍微总结提炼一下。各位同学来看一下，我们刚刚完成的是不是接口实现流程的234这3个步骤。我们为了要连接数据库，基于ORM来查询数据。所以是不是先在models里边定义了模型类，有机类有表儿对应的模型类。好，那有了模型类了，我们是不是就可以封装查询方法了？还封装了一个get charactery。注意我们是不是要有一个数据库绘画这个形参，因为将来就指着它是不是去execute执行select。好，那执行完就有结果了，我们return结果，return从result里边用scale as all是不是来提取，这样就拿到了查询到的所有数据。

发言人   18:02

好，那这个方法我们在哪里调用路由处理函数当中是不是要做真正的查询？所以我们在路由处理函数里边是不是去调用方法，切忌把DB数据库这个绘画依赖项是不是传到咱们的CRUD方法里边去。那我们采用异步的方式去执行数据库操作，拿到结果响应给前端是不是就OK了。

发言人   18:29

这个是咱们的新闻分类功能的实现流程，大家往咱们的接口实现流程上来看，先有路由，然后定义模型类。因为咱都是ORM操作数据库，所以定义模型类。下一步CRUD文件夹里边封装函数来增删改查操作数据。最后在路由处理函数里面调用封装好的方法，响应结果是不是就OK了。

发言人   18:55

这个接口的实现流程基本上适用于咱们的所有接口，后续咱们还会逐步的去用这个流程复现功能。那现在咱们一块儿获取新闻分类列表就已经成功了，接下来就该同学们的时间了，大家写代码能够查询到所有的新闻分类就可以。在这里温馨提示，先不要跑咱们的前端代码，先不要打开前端代码，向后端characterises这个路径发请求。因为直接用前端来访问咱们的后端，还有一个小问题没有解决，等我们解决完，他才能实现真正的前后端交互。接下来同学们来动手获取一下自己的新闻分类，下一小节我们来动手解决我说的那个小问题，我们下一小节儿不见不散。