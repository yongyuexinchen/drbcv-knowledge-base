13-FastAPI基础入门-自定义响应数据格式_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，这一节咱们来学习自定义响应数据格式有什么用呢？简单来讲，就是当一个接口响应的数据格式是Jason时，那我们可以自定义一个数据格式，其实就是一个类，用这个类来约定相应的数据包含哪些属性，是什么类型。光这么说，大家感觉可能没有那么明显。我们拿一个底下这个news接口来分析一下。我们现在的news接口响应回来的是一个python对象，fast的API，是不是帮咱包装成Jason格式？那目前的杰森肯定是包含一个ID。

发言人   00:40

接下来问大家，我能不能再加一个抬头？那可以，那我能不能再加一个content也能行，那想加啥是不是随便？大家有啥想法？我能随便了，但是这对吗？哼也不是不行，也不是不行。当然如果我们想要把接口返回的数据格式给它提前固定下来，我们就可以自定义数据格式，用这个自定义的类来约束响应的数据格式。

发言人   01:17

那怎么做呢？来咱们一块儿来看一下，首先要自定义响应数据格式，我们要给装饰器，就比如咱的get post，再加一个参数叫response杠model。那它的值是一个基于python tic的类型。那未来这个接口就得听这个类型的话，这个类里边有啥属性，是啥类型，那这个接口就能返回啥样的格式的数据。那我们具体来看一下写法。

发言人   01:49

注意首先第一步，我们要自定义响应数据的格式。第一步要先准备一个基于python单体的类。好，那比如说我这里是不是有ID有title，有contente。那假设我有一个接口响应的数据格式，就是这三个属性，且是相应的类型，那怎么办呢？在装饰器注意get方法是不是先写请求的路径，第二个参数继续往后加，来往下加一个参数，response杠model值赋值为我们的类就可以了。目前这个接口响应的数据格式就只能是这个news类型，如果不是直接就会报错。那接下来咱们就到代码当中动手来试一下。

发言人   02:37

那我们回到py top里边来，首先先让我明确一下需求，来需求我们的需求是定义一个新闻的接口。那么这个接口要响应数据，对不对？响应数据的格式要包含比如说ID，再包含一个title，再包含一个content，要这三个。那我们既然要约定数据格式，是不是就得先定义类型？那这个类型要基于python deny是不是？那我们先导一下，那我们from python tic impart，base model这个鸡肋，那往下走定义类型class咱就直接叫news了，我们让他继承base model这个鸡肋，它要包含我们预期的三个属性，是不是ID title content l，那我就快速写了。ID我要int类型，title我要spring类型，content我也要随便类型。现在这个类型定义完了，也就是我们要的数据格式是不是已经有了，那就差把它用上是不是？

发言人   03:40

好，那我们来写接口，艾特APP get一下这个路径，我就叫news了，斜杠来个路径参数，比如说ID那路径写完了，逗号隔开是不是设置数据的格式？那是response，杠model那只写啥？咱们定义的是啥？这儿是不是就写啥？我们希望它带有ID title content，那我们就把这个news这个类名给它搁在这儿，是不是就OK了。好，那下一步写处理函数，异步一下，a think DF get我就叫get news了，小括号冒号一下。我们因为有这个ID路径参数，所以是不是就得有一个ID同名ID这个形参？那我来一个int类型。

发言人   04:30

好，接下来我return响应结果。在这儿跟各位同学汇报一下，我们先响应正确的，就是按要求包含这哥仨，看到结果了，咱们再把它改的，比如说少一个或者多一个，看看是不是会报错。来那我们写return，直接来一个大括号，python的对象了。ID是不是得要着值，就要ID形参了，title TITLE也得要着，那我们格式化一个。比如说这个是第某某本书，那这个某某的话咱们就把它格式化成形参ID是不是就行了？那逗号一下，再来一个content，CONTENT，那冒号一下，这个简单点来写死了，这是一本好书。好，接下来保存一下。

发言人   05:27

我们目前接口写完了，下一步是不是用这个接口来发请求，来测试一下。那我们回到浏览器里边来，允许我先刷新一下news带ID参数的接口已经有了，那我们把它展开try IT1下，输入一个ID，比如说666。那接下来excute来提交，来看看正常的响应结果是不是已经拿到了。那这样是咱们听话约定了格式是news是不是真的响应回来了？News格式的数据，那接下来咱要试试，如果不听话，来回到拍唱片里边来。比如说我把这个糠炭的给他拿掉了，那接下来保存再回到浏览器里边刷新一下。同样还是这个接口，咱们还是比如说来个66 excute一下来看看，是不是直接报错了。但是这个报错只能看到是international server是不是error，但不知道具体的问题是不是？

发言人   06:28

那我说一下具体的问题。在patch m里边打开拍场不来找哪里？注意了，找到项目运行的这个窗口，就是这个小三角形，我们把它点开，大家来帮我看看，这个type是不是类型错误，missing是不是丢失的意思，丢失了啥呢？content。那我们现在是不是就看到了，如果咱们约定了响应的数据格式，响应的数据就必须是这个格式。如果不是，不好意思，稍微报错一下下。那接下来我control z把咱们这个content给它还回来。那接下来我再次保存，再次保存，把这个窗口滚到最底下，这个报错是不是就被覆盖掉了？

发言人   07:17

好，那接下来回到浏览器里边，咱们也刷新一次。Try IT输入一个11 execute来看还有报错吗？没有了，那同时回到pid里边检查一下有报错吗？没有，是不是都是200OK？那这个就是response杠model约定一个类型响应的，就得是这个类型。

发言人   07:41

好，了解了response和model了，接下来咱们回到PPT里边，把所有的响应咱们来总结提炼一下重点。截止到现在，咱已经了解到fast API它支持很多种响应类型，那其中最常用的大家觉得是哪个？对，Jason格式这个Jason格式fast API是不是能帮我们去包装转换，不需要程序员去做额外的操作。当然这里边有一个细节点，就是如果响应的是Jason格式，我想要约定Jason里边有啥属性，是啥类型，也就是自定义响应数据的格式。

发言人   08:18

那有一个关键的参数大家记得吗？对response杠model。具体的做法来看一下，我们是不是要基于python deny定义一个类型。那在路由请求方法这里添加上response到model这个参数，等号赋值为咱们的类型的名字。那这样的话这个接口响应的数据是不是就得是咱们定义的这个类型，多一个少一个是不是都会报错的那这个是咱们的杰森格式。那接下来再来，我们说开发当中是不是还有可能用到HTML response file response，一个返回HTML内容，一个返回文件。我们说如果是默认的Jason，fast API帮咱干活，那如果不是，是不是需要咱程序员干了？

发言人   09:08

那我的下一个问题就来了，在fans的API里面怎么设置响应类型？不是结算了咱怎么办？两种方法，要么在装饰器里边设置响应类，要么返回我们想要的响应对象。那我们看一下具体的代码，如果是这种简单的固定的那是不是直接在装饰器这儿设置成response class，等于我们对应的响应类是不是就可以了。比如说像HTML纯文本是不是咱都这么干。好，另外再有，如果是像一些文件、流媒体或者是音视频等等等等，这种需要动态的响应，那我们就不直接指定死response class了，而是在return这里直接响应对象就可以了。这个是咱们响应相关的最最常用的知识。

发言人   09:59

说到这儿，咱就已经说完了，同学们不要走开，下一小节儿咱们要来做异常处理了。就是有正常的现象，也允许有异常错误，那如果遇到错误该怎么处理呢？同学们别走开，咱们下一小节儿不见不散。