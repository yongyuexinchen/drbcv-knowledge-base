25-FastAPI进阶-ORM操作数据-查询总结_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，截止到现在，咱们ORM里面常用的查询功能就已经学完了。那这一小节咱们一块儿来总结提炼一下重点。咱们一块儿回忆一下，咱学了查询是不是select可以查get，还能根据主键值来查。当然这两个里边，大家想想开发当中哪个应该是更常用一些，是不是select？你看咱们前边学查询的时候，是不是select用的次数是不是最多了？

发言人   00:31

那select咋用的呢？首先select，这是基本写法，它是一条ORM语句，对不对？那我们得excute执行它，执行完会得到啥？对ORM对象。那我们有了这个ORM对象是不是就能提取结果？提取到结果是不是就能响应结果给客户端了，这是select查询功能的大致实现思路。

发言人   00:57

当然我们知道select能查，是不是还支持更复杂的查询写法，比如说可以添加where做条件查询，查询出来满足我们条件的数据还能加function，这是不是聚合查询？其实聚合查询是不是就是做统计计算用的，还有分页，分页查询是不是就是用户指定想要哪一页，每页有多少条数据。那我们是不是就按照用户的需求去查库，给他响应结果？那这三种查询方式，每个是不是又涉及到了不同的写法？那咱们一个一个来看一下，比如说条件外，那条件都能写啥呢？大小相等的比较判断可以like模糊查询是不是可以与或非与非判断逻辑运算符是不是也行，判断是不是包含，包含的就找，不包含的是不是就不返回了。好，这是条件的写法。

发言人   02:01

再有function做聚合计算，那聚合计算大家想想涉及到的方法是不是也挺多。比如说有coin可以统计数据库里边到底有多少行数据，有多少数据量。是不是还可以针对于某个字段值求最大最小值，求平均值，求和是不是都可以。

发言人   02:22

好，再有分页查询，分页查询的写法是select offset做跳过，统计出来跳过的条数，limit是每页的数据量。那其实用户要几页跟每页的数据量是不是用户指定的重点，是不是这个offset底下有我们讲课的时候学过的这个offset的计算方式，它要跳过多少？我要第三页儿，是不是前两页的都要跳过，那跳过的是不是就是前面页时候包含的数据量？前面页儿数怎么求，当前页儿减一是不是就是前面一页儿数乘上每页的数量，是不是就是要跳过的那有了跳过了，再根据用户指定的，比如说这个页码，每页的数据量是不是就能做分页查询了？这是select不同查询方式大致的写法。

发言人   03:17

那有了select了，咱们excute执行之后，我们说是不是会得到一个ORM对象？我们基于这个ORM对象来提取数据是不是就行？那我们可以提取所有，是不是也能提取单条？提取所有的scale as all是不是就是所有那单条的scale as first，那你找出来很多，我是不是只要第一个，那scale one or nine是不是有就提取出来，这个没有那我就OK了。当然还有一个scale，注意这个没有S，它是提取标量值。其实就是提取一个数据，通常就是配合这个聚合查询。假设你统计最大值是多少，这是一个数，是不是用scale来获取就可以了？那有了结果了，是不是就可以return想给客户端了？

发言人   04:06

这是咱们的select，select是查询execute执行，执行完之后有ORM对象，基于这个对象是不是去获取数据，能获取完就能响应了。好，同学们，这个是咱们查询功能相关的知识，咱们这一节一起来梳理一下。梳理完，希望同学们对于查询功能的知识脉络可以更清晰一些，希望对同学们的学习有所帮助。我们现在查询是不是学完了，那接下来下一小节儿，我们就要继续往后学习ORM操作数据的下一个方式。下一个方式是I的新增，咱们下一节见。