12-FastAPI基础入门-响应类型-文件格式_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，这一节咱们来学习file response，来响应文件内容。并且咱们这个响应类不再是添加到装饰器里边，而是直接返回响应对象。那我们一块儿来看一下，首先file response它是一个响应类，是fast API提供的返回文件内容的。那么这里的文件内容像图片、PDF、excel音频视频等等等等都是支持的那用法跟我们之前学的HTML response也很像。

发言人   00:32

首先从fast API response里边导入，注意fail response。导入之后，我们就可以在处理函数当中用了return的时候直接写file response小括号。这就构造了一个响应对象，通过return响应给客户端了。当我们向这个接口发起请求的时候，就能拿到对应的图片了。

发言人   00:57

了解了大致的用法了，接下来咱们也到代码当中来试一下。回到pat tom里边来，接下来我来定义一个需求，那我要有一个接口我要有一个接口，这个接口它是返回，比如说一张图片内容图片内容。图片我已经提前准备好了，给各位来看一下。我准备了一个files文件夹，里边有一个1点JPG是一张猫，那我们就定义接口来响应这张图片就可以了。

发言人   01:26

好，接下来我们来写代码了。首先我们要响应的是文件内容，应该先导入，是不是导入响应类？那from fast API点response，import的导入file response。接下来回到底下，咱们来写路由了，那我敲个回车，艾特APP1下，点get定义一个路径，比如说就是范围了。好，那接下来敲回车，视图函数，异步函数拿出来，DEF我就叫five了，小括号冒号一下参数就不写了，直接敲回车。

发言人   02:04

那我们未来是不是要响应给客户端一张图片？那我们是不是就是return这个file response构造出来的响应对象，当然这个响应对象要用到刚刚的图片吧，那我们是不是就得把图片的路径告诉file response这个响应类才可以。好，那怎么办？那我提前定一个字符串，比如说就叫pass等号赋值值就是当前项目目录files里边的一点勾PEG是不是就OK了？那我们就这么写，当前目录写一个点儿杠，files文件夹再写个杠1，注意来看是不是有一点勾PEG，那我们直接回车选中它。

发言人   02:48

好，那现在咱们路径已经有了，那我们的文件对象要用到哪张图？是不是在pass这个变量里面存着？那我们把pass的这个变量放到file response小括号里边来，从而就构造好了当前一这张图的文件对象了。通过return把一这张图这个文件对象返回给客户端是不是就OK了？

发言人   03:14

好，那现在咱的代码是不是已经写完了？那接下来咱们到浏览器里边来试一下。回到浏览器里边来，那我直接访问咱们的接口了，那我来一个斜杠file敲回车来看，这只猫是不是就已经有了。那图片能打开，说明return的文件对象是不是已经成功了。那以后在开发当中，如果响应类型是文件格式，那我们就按刚刚的操作是不是先导入five response。那一般文件咱是不是采用返回对象的方式？好了，那这个就是咱们的file response这个响应类以及直接返回响应对象的写法。接下来各位同学也来动手来响应一个文件对象给客户端，大家来试一下。