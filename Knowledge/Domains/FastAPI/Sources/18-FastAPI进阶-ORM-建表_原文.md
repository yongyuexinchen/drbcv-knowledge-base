18-FastAPI进阶-ORM-建表_原文

2026年07月21日 12:50

发言人   00:00

Hello, 各位同学，上一小节咱们提到要想使用circle acme，咱们要先安装，然后建库建表，接下来就可以操作数据了。那么上一小节咱们是不是已经安装了circle ACME了？那这一小节咱们顺序向下就开始建库建表了。首先创建数据库，咱们还就用circle的方式，great database加上我们要的数据库名字，分号一下是不是就OK了。

发言人   00:25

那有了数据库了，重点就来了，是不是要建表？建表特意为各位同学梳理了一个流程，先创建数据库引擎，其实在这儿就是把数据库最重要的连接地址给它搞定。好，那接下来连接上数据库了不定义模型类。

发言人   00:45

为什么是定义模型类呢？因为在上一小节，咱是不是提到过ORM，其实就是面向对象的方式操作数据库，操作数据库的表。所以咱们要有模型类来定义是啥表，有啥字段，是啥类型。这是第二步，有模型类，那接下来模型类也有了。咱们在第三步启动fast API应用的时候就可以建表了。那这三步走完之后，我们就可以得到一张数据库表了。

发言人   01:15

好，那接下来这三步对应了不同的代码，那咱分别来看一下。首先第一步创建数据库引擎。创建数据库引擎的话，我们要通过一个方法create a think engineer，异步引擎，那当然要用它，得先倒包从circle alchemy EXT think IO里边把它导进来。那导进来之后，调用方法配置一下就能得到一个异步引擎了。

发言人   01:44

那当然这个配置第一个参数注意重点来了，数据据库的链接。那这个UL包含啥呢？来往上抬头来看，它有一个固定的公式，数据库加上驱动冒号斜杠斜杠这个root，用户名冒号123456密码。但是用户名密码是不是得按自己的数据库走？艾特local host是地址，冒号3306是咱们mysql数据库的端口号，是不是？那接下来斜杠跟上的是啥？数据库的名字，问号叉set UTF8，那就是字符编码了。这个字符编码可以省略，也可以换成别的。

发言人   02:26

好，最重要的一个参数，数据库的UL地址有了，那往下这三个参数稍微了解一下。ECHO等于出它是可选的是专门用来输出circle日志的。就是我们说咱ORM是不是面向对象的编程方式，不写circle了是不是？但每句代码对应的是circle的啥功能呢？如果这一项开启了，那么会输出一些细节给我们看啊。好，那接下来再来put size和max overflow这两个放到一起来看。Positive它是咱们连接池当中允取得活跃的连接数，那这儿我是不是写的十？

发言人   03:03

那max overflow是除了这个十，还允许额外配置多少个连接数，就是允许多少个超出。那我这是不是写的是20？那咱这个数据库最大的允许的连接数是10加20，也就是30个。当然这个数字不是固定的。未来根据咱们项目的用户量，服务器的情况，可以去灵活调整。那当然一会儿咱们在代码当中也就写10跟20了，为啥呢？因为咱个人电脑配置也没有那么高，写太高了电脑是不是也受不了？

发言人   03:36

这个是咱们有了数据库，创建数据库引擎的方式，那接下来咱是不是也要试一下？当然提前跟各位同学汇报一下，数据库我偷摸建好了，叫fast API first，是不是跟咱当前这个测试项目的名字是一样的？数据库有了，咱就走流程，重点来建表，是不是就行？但是咱现在是建表的第一步，是不是创建异步引擎？那接下来咱们到py cha m里边来试一下，打开python，首先我就往上写了，比如说在这儿我敲一点回车，加一个小注释，我们现在是要干啥？是第一步是不是来创建异步的引擎？那这个引擎咱是不是得先给它导包导进来？Rome circle alchemy点EXT点think IO里边import导入，导入的是create a think engineer，那创建异步引擎的方法有了，那我们下一步是不是调用这个方法？

发言人   04:39

那这个方法一定要用，我们是不是会得到这个引擎，那我们起个名字把它存上，比如说我就叫think下划线ENGINE，等号赋值。那我们的a think engineer是不是就是异步引擎了？接下来我们回到create这个小括号里边来配置参数。首先第一个参数，我把鼠标放上来，大家帮我看看是不是UL，就是数据库的地址是不是？那我们提前把数据库的地址给它搞定一下，允许我起个名字，咱这个就来大写了。因为它这个常量不变，那我就叫ASYNC，是不是a think database，DATABASE database，下划线URL等号赋值值，我们刚刚说是不是有一个固定的结构，先放上数据库再加上驱动，那咱是异步的对不对？I think IO my circle一下。

发言人   05:39

好，那冒号斜杠要放什么？数据库的用户名，然后冒号放上密码，对不对？123456，注意这是我的账号跟密码，将来是不是要替换成你自己的那接下来还没完，艾特是不是写地址？Local host host冒号一下是不是放上3306，咱这个端口号。

发言人   06:03

好，那接下来斜杠放什么？是不是就是咱们数据库的名字？大家还记得我准备好的数据库名字是fast API下划线FIRST，就是跟咱们当前项目的名字是不是一样的？好，那接下来在问号是不是放上字符编码，那个字符编码是叉set等号，UTF8就可以了。当然这个UTF8可以换成其他的，甚至把叉sit去掉都可以。

发言人   06:33

好，那现在咱们的数据库UR地址搞定了，那我们把它填到create a think engineer方法参数一的位置，那think大写a think database UL好，参数一搞定了，我们下一个是不是就是ECHO等号赋值为true，逗号隔开一下，那我在这儿加一个注释，这是不是可选的？它是输出circle日志用的是不是？好，那敲个回车，接下来是不是就是连接池的数量了，那就是POOL size，我跟PPT里边一样写实了，那逗号隔开还有一个超出的数量是不是max over flow FLOW等于比如说一个20，那这俩配合起来我们加注释。第一个是设置设置连接池活跃的连接数量。好，那是十个是不是？好，那下一个就是允许的允许额外的连接数。那目前是不是就是10加20，最多有30个连接数？那配置到现在，咱们的异步引擎是不是就已经有了？

发言人   07:46

那有了之后，下一步咱是不是要定义模型类？那怎么定义呢？来咱们回到PPT里边来看一下。定义模型类的话分两步，第一步先有鸡类，这个鸡肋放通用的属性跟字段。接下来你要有几个表，表里边要有啥字段，还要放上一个数据库表对应的模型类。我们看一下细节，首先鸡肋的话它有要求要继承一个类declare base。比如说将来我们各个表都有的，像创建的时间，更新的时间，假如说所有表都要，那咱就可以把这两个属性字段放到鸡肋base里面来。

发言人   08:27

好，那有了鸡肋了，下一步你是啥表？表里边有啥字段？那一个表再是一个模型类，比如说我要有书籍表，那我定义一个类，那table name注意是下划线开头，两个下划线开头，两个下划线结尾，table name等号赋值来一个表明。接下来就是左边是属性map的杠colum，就是映射的字段。这个就是咱们定义模型类的方式。第一先有鸡类，第21个表对应一个模型类是不是就可以了？那接下来咱们也到pym里边来试一下，回到咱们的拍上里边来，允许我先打一个小注释，打一个注释。

发言人   09:12

我们现在是不是到第二步了，叫定义模型类，是不是？模型类我们说了先有鸡类再有一个表，对应的模型类是不是这么干？好，那我们具体规划一下，鸡肋里边包含啥呢？就是创建时间和这个更新时间，更新时间然后分号一下，再有就是表对应的，咱们一会儿有一个书籍表，书籍表里边有ID书名，再有就是比如说这个作者价格，再来一个出版是咱就要这几个字段。好，那需求各位同学清楚了吗？OK是不是？

发言人   09:56

好，那我们开始定义模型类了。当然首先第一步，咱是不是得先把那个declaration base那个类给它导进来，那我们导一下from circle alchemy点ORM导入谁呢？Import d大D开头，declare base。当然一会儿咱们的map是约定属性类型的，mapped column是映射字段的那我们把这俩也同步导进来，省得我来来回回去滚动代码了。

发言人   10:28

好，那接下来回到咱们第二步的位置，是不是把俩类定义出来。首先class是不是先有一个鸡肋，比如说我就叫base了，它要继承declare base，冒号我先写一个pass，好，鸡肋是不是有了？接下来再来是不是来一个表对应的那这个表对应的我就起名就叫book了。书籍括号继承谁呢？继承base来冒号一下，pass放上来。

发言人   10:53

那我们现在两个类是不是已经有了，咱们往里边来填充字段。我先从鸡肋开始，鸡肋pass干掉。我们说是不是要有创建跟更新时间，那我就起个字段名，我叫create time，就是创建时间CREATETIME。

发言人   11:13

然后接下来冒号一下，注意map的M大写的，中括号一下写类型。那类型是啥呢？就是data time一下。当然这个包需要咱们导进来，鼠标放上来impart，从data time里面给它导进来，那等号赋值，map的column来映射字段。那这个字段的话注意也要先写上类型。那这个类型我写data time，这个data time用的就是circle alchemy里面的了。那我import把它找到circle action里面的给它导进来。好，那类型写好了，逗号隔开一下。

发言人   11:49

那我们是不是要真的插入时间？那我放上insert default，这个时间我用FUNC点儿now这个方法来获取时间，来鼠标悬停上来把它导进来，那逗号再隔开default，我设置一个默认值，那这个默认值FUNC点now啥时候创建对象，啥时候插入真正的时间，那我再逗号隔开一下来个comment，说明就是这个字段的说明。比如说我放上这个是创建时间，是不是创建时间？好OK，那我现在的创建时间这个属性搞定了，那我再来一个更新时间。Update time，照猫画虎大家来帮我，是不是我们属性的类型，还是data time这个类型就可以了。

发言人   12:38

等号赋值为map的colum，是不是映射字段？那它的类型是啥？是不是还是circle atomy里边的data time类型？同时insert default我们也要着它是不是就是插入的时间。当然默认值我也来一个，因为比如说我第一次没有修改，那我就这个时间也作为更新时间出现一次，对不对？好，那还没完，再来一个on update，那么时间的话就是FUNC点now就可以了。那最后我也来一个，比如说comment，那我就来一个，这个是修改的时间是不是？好，那OK现在咱们的这个base鸡肋是不是已经搞定了？

发言人   13:22

那搞定之后是不是到咱们表对应的模型类了？那么接下来我就来注意，第一先放上表名下划线table name，等号赋值值。咱们就比如说就是输几遍，那就叫book了。好，那接下来敲回车，是不是把咱们这五个字段给它搞定。

发言人   13:43

好，接下来我就开始了。在这里ID一下map的类型，我中括号一下，那就int类型是不是就可以映射字段，那我就来一个主键了，PR primary k等号来一个true。好，那可以加，是不是也可以加这个command说明对不对？就是书籍的ID好，那接下来再敲回车，是不是要有一个书名，那我就叫book name了。

发言人   14:11

Mapped一下map的书名儿的话，咱是不是来的类型比较合适？等号赋值映射字段，那字段是不是得整个类型？那这个类型用circle alchemy里边的spring，我们限制一下字符数量，比如说250 22222255。好，那我们把这个string给它导进来，导的是circle economy里面的。

发言人   14:35

好，那我们也可以给它加一个说明comment。这个就是书名，书名好，OK肖薇正，那还要用啥来着？作者AUTHOR author，那我们来个map还是string是不是就可以了？映射字段那咱也就string了。String类型比如说人名有长有短是不是？那再来一个comment，这个是作者。

发言人   15:05

好OK那再来Price PRICE，这个大家就稳住了。Price一般，是不是？应该是float浮点型比较合适，对不对？我就写一个float等号赋值映射字段，这个字段类型是FLOAT，注意大写F它是来自于circle ACM里面的，来把它导进来，我们逗号隔开一下。Comment，这个是咱们书籍的价格，是不是？

发言人   15:32

好，最后一个，咱们再来一个这个出版社，PUBLISHER，迈浦特是不是还是来一个string类型，等号赋值map的column，还是string是不是就可以？括号书名有长有短，咱们尽量给长一点，comment这个是出版社是不是就可以了。我们现在关于模型类当中的鸡肋表对应的类是不是都搞定了？

发言人   16:00

那下一步是不是就是启动fast API的时候，咱们要调用建表的函数给它建表，是不是就ok那具体该怎么做呢？来咱们回到PPT里边来。首先想要建表分成两小步。第一步从连接池里边获取异步连接，开启事务，create all来建表就可以了。那啥时候执行咱们这个建表呢？就是fast API在启动的时候调用这个建表的方法，那咱们数据库表是不是就被创建了？接下来咱们回到拍tom当中来动手试一下。

发言人   16:35

来回到拍里边来，我们向下滚动一点，这里是不是完成的是第二步，定义模型类，那我们就顺序向下了，敲一个注释，我们现在要完成的是第三步，第三步要干啥？建表对不对？那这个建表，咱们定义函数来建表，那具体怎么做呢？是不是我们在fast API这个起动启动的时候，调用建表的函数是不是就OK了？好，那接下来咱们开始，首先我先有一个建表的函数，我think异步一下，因为咱们要数据库的写入了，那肯定是异步的操作方式。比如说我建表我就叫create tables了。CREATE create下划线，TABLES create tables.

发言人   17:26

还不知道写啥，先来一个pass。好，那接下来我们再往下来，界面的函数有了，啥时候要调用这个create tables呢？是不是fast API启动的时候，那咱们来个装饰器，艾特APP，是不是咱们的fast API实例点儿，来个on event。那什么时候？打引号start START start up就是启动的时候，那启动的时候咱们装饰器有了定义函数，a think DF，我就叫start up EVENT了。来小括号冒号a wait过来调用上边的create tables，来create tables就OK了。当然create tables这个方法咱们是不是还空着呢？那我们把pass给它干掉。

发言人   18:15

重点来了，在这里是不是咱们要获取那个数据库的异步引擎？因为异步引擎里边是不是有链接，获取过来然后创建事物来干啥呢？建表就OK了。那个异步引擎大家还记得叫啥吗？如果忘了没关系，咱们向上找。这里是不是创建异步引擎存到了think engineer里边，那它是不是就是异步引擎？来咱们向下走，向下缩进一下，a think异步一下，那就是with一下，a think engineer点儿begin。

发言人   18:58

如果创建了事物了，那这太长了。咱们来一个别名，CONN冒号一下，那我们await一下，还是异步的方式。COONN点儿注意run think调用这个方法去创建。写的时候没有提示，咱就直接手写就可以了。

发言人   19:19

SYNC1下小括号，那小括号里边要干啥？使用模型类来创建，使用base模型类的原数据来创建。那具体怎么写？注意就是找到咱们的那个鸡肋base，因为咱自己的模型类是不是继承贝斯基类的？好，找到咱们这个贝斯基类点儿meta data点creal，但注意create all在这里我一敲回车是不是有小括号代表是一个方法，把这个小括号去掉啊不要立刻调用起来。什么时候调用呢？当启动应用的时候，调用create tables，再来调用create all，是不是才OK？所以这里大家写的时候，把那个小括号也给它干掉。

发言人   20:12

好，那现在咱们的第三步定义函数建表是不是也搞定了？那一旦咱们启动fast API是不是就要调用create tables，是不是真的就要建表了？那是不是意味着数据库里边真的会有出来？当然前提如果无bug的情况是不是好，那接下来咱们启动来试一下好不好，来启动看看有没有报错。首先好像没有报错，来往上来看，是不是一些circle的细节也给我们了，看是不是因为咱们输出了那些circle的日志。

发言人   20:47

好，那看起来好像没问题，是不是？那我们是不是就得检查一下数据库里边到底有没有book，这个表字段到底对不对呢？咱们得检查，那咱们检查，在这儿我说一下，肯定不是原生circle的方式了。咱们的排插M，它有一个插件叫database，来把它点开，点开，点这儿加号添加一个数据源。那咱的数据库是不是mysql l，你其他的数据库就在底下找就行了。那我就点到mysql，其他都不用变。我们的用户名我是root密码，我的123456。

发言人   21:23

来接下来点个OK那OK之后，是不是就有了一个本地的连接？但是在这儿点就是底下这儿是不是有3个点点，咱们把这个数据库给它加载过来，点一下，找到咱们自己的fast API first，把它勾上。那另外的默认的就不要了，点一下其他位置来fast API first是不是已经有了？来展开表有没有？好像有，是不是来看book，是不是在这儿双击点开book。来来来，同学们来看一下ID是不是有书名，是不是有作者，是不是有Price，出版社创建时间、更新时间是不是全都有。

发言人   22:06

那现在这张表有了，是不是说明刚才咱们的流程三步骤，来回到咱们的main点PY里面，把底下的面板关掉了，来说明咱们刚刚的建表操作没有问题。第一，创建异步引擎添加的数据库UL是不是没问题？那定义了两个模型类，是不是一个base一个book，就是咱们的表对应的模型类。当然这里温馨提示，因为是表对应的模型类，是不是得先放上table name，给这个表起个名字。

发言人   22:41

好，那第三步是不是定义建表函数fast API在启动的时候调用这个函数是不是就OK了？好，这个就是咱们ORM来建表的流程了。那么接下来也该各位同学来练习了。我有一个小练习要求，就是创建数据库表，创建谁呢？用户表。通过circle argue ORM来创建用户表，我这里有几个建议的字段，比如说用户ID，ID为主键，用户名、密码、创建时间、更新时间，或者大家还可以酌情添加字段。比如说昵称、性别、手机号等等等等都可以。接下来各位同学来动手使用ORM的方式来创建一个用户表，动手来试一下。