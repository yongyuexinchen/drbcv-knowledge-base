04-FastAPI基础入门-路由_原文

2026年07月21日 12:51

发言人   00:02

Hello, 各位同学，这一小节儿咱们一块来学习陆游的写法。陆游有什么用呢？陆游可以帮助我们访问不同的地址，拿到不同的结果。就比如上一节，我们访问了跟路径，是不是能拿到message hello word的这个结果？那这个功能用啥来实现呢？其实就是路由，路由它是UL地址跟处理函数之间的映射关系。大白话来讲也就是当我们访问某个网址的时候，那服务器执行哪些代码，返回什么结果给我们，这样是路由的作用。

发言人   00:39

那么想要写路由，我们首先要有一个函数，未来要执行的代码放到函数里边是不是就可以了。但是还有一个问题，访问某个网址是不是要执行这个函数？那网址跟这个函数怎么关联起来呢？

发言人   00:55

这里我们要借助python的装饰器写法，具体的写法就是注意来看艾特APP。这个APP指代的是当前fast API实例。大家还有印象这个名字吗？

发言人   01:08

好，接下来再往后点儿get小括号，这个get是一个请求方法。那我们都知道是不是还会有post、put、delete等等等等请求方法，在这里都支持。好，那接下来再往后看，小括号里边目前是不是放了一个斜杠，这个代表的是请求路径斜杠是不是代表跟路径？未来斜杠后边还能加其他的，那有了这个装饰器了当我们请求这个路径的时候，就执行下方的这个root函数。

发言人   01:42

这个函数里面有一个return，return我们都知道它是函数的返回值，那在这里也就是请求当前这个路径的时候，返回message hello word这个信息。这里的返回值咱们专业一点叫响应结果。这样就是咱们路由的写法，用装饰器装饰上函数，当请求这个路径的时候，执行下方这个函数。好，那了解了陆游的写法了，接下来咱们也到拍场当中写一个陆游来试一下。

发言人   02:14

那接下来咱们回到拍场里边来试一下，首先要写陆游了，对不对？我提一个需求，我加一个注释。比如说我们要访问斜杠hello这个路径，那我们的响应结果注意响应结果比如说是MSG，接下来冒号，你好，fast API咱们这么来。

发言人   02:38

好，那我们要写这个路由是不是先放上装饰器，再放上处理函数。那装饰器对，艾特APP，这里的APP是不是就是上边这个fast API实例？好，那接下来点儿请求方法我们就也写get了。接下来小框号小括号里边是不是放请求的路径，那路径是咱这里是斜杠。

发言人   03:05

Hello HELLO好，那现在装饰器写好了，敲回车是不是放函数？那我们也可以放上a link，是不是？就是fast API的异步函数EEF。然后我们起个函数名，比如说我就叫get下划线，hello小括号一下冒号敲回车。那我们没有其他的处理逻辑，就直接return响应结果就可以了。注意我们要来一个对象，对象里边是不是键值对的格式，那我们来一个比如说MSG冒号一下方商，你好，fast API是不是？那我偷个懒直接control c control v了。

发言人   03:44

好，那现在我们的路由是不是就已经定义完了？那接下来我们保存一下直接运行项目来访问试一下。好，那我们是不是打开127冒号8000这个端口，那现在默认访问的是跟路径，那我们可以在8000的后边继续写上hello，但注意后边这个python没有。好，敲回车我们来看MSG你好，fast API是不是已经得到了。那我们现在直接访问路由，拿到了响应结果，当然我们也可以访问一下这个自动生成的可交互式文档，来看一看有没有hello这个请求路径。

发言人   04:22

好，那我们来访问DOCS，敲回车来看有没有斜杠。Hello有点开，try out execute一下来看看。你好，fast API是不是也拿到了？好，这个就是咱们fast API当中路由的写法。用一个装饰器装饰上处理函数，从而实现UL地址跟处理函数之间的映射关系。

发言人   04:47

了解了路由的写法了，接下来咱们回到PPT当中总结提炼一下重点。我有两个问题，第一，什么是路由？路由就是UIL地址跟处理函数之间的映射关系。我下边有一个代码，大家帮我解释一下。

发言人   05:04

首先第一行，它是一个装饰器艾特APP，这个APP是fast API的实例get。请求方法将来能不能换其他的？能小括号这个引号斜杠它是请求的路径是不是？好，那底下a think DF root这是什么处理函数，那return就是响应结果了。那有了咱们的路由了，是不是就可以在访问这个路径成功之后，拿到return这个响应结果了？

发言人   05:37

好，知道陆游写法了，接下来就该同学们的表演时间了。我有一个小练习，当然肯定是陆游，是不是？我的需求是访问路径斜杠，user斜杠，hello, 响应结果是MSG，我正在学习fast API。各位同学来动手试一下。大家写完之后可以在评论区留下你的答案，我们在评论区不见不散。