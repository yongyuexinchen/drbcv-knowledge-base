01-FastAPI从入门到实战导学课程_原文

2026年07月21日 12:51

发言人   00:00

零基础学AI月薪过万就来黑马程序员。Hello, 各位同学，今天黑马给大家带来了一套python web开发fast API框架，从入门到实战课程。学完这堂课，大家可以构建一个自己的大模型，并且提供出一个可以访问的地址供其他应用接入。咱这个大模型就比如我们日常用过的deep sick、豆包，或者是软件当中集成的智能聊天机器人等等，都是构建大模型，也就是训练大模型。通过外部能力将模型服务化，提供一个可以访问的地址。其他的应用就可以通过这个地址来调用大模型了。

发言人   00:39

如果以后咱们也要开发AI大模型，想要让其他应用接入咱的大模型，同学们可以围绕以下三部分开始学习。首先就是python语言基础了，python语言是AI大模型开发最常用的语言。相信很多同学了解过python，学会一点点python语言了。有了python基础了，下一步学服务化。所谓服务化就是用python web框架把程序封装成接口，让模型不仅能跑，还能被别人访问，被其他系统调用。这一步就是把代码变成了服务。这一项也是咱们这堂课要学的核心内容。

发言人   01:12

有了前两部分了，我们就可以正式接触到大模型开发了。像模型训练、构建、微调、优化等等。咱们这堂课也会涉及模型训练的内容。那么具备了这三大能力，就能成为一名合格的AI大模型工程师了。所以黑马重磅推出了增强模型服务化能力的课程，python web开发fast API框架。从入门到实战课程。我们用fast API框架把训练的大模型服务化，供其他应用来访问。

发言人   01:44

那python框架千千万万，fast API框架是首选，为什么呢？因为fast API框架有着先天的优势。比如它天生支持异步，性能高，而且是内置a link awaited or支持异步，不需要安装额外的扩展。其次fast API还支持python单体和自动类型校验，这样的话就省去了程序员手动校验类型的情况，开发效率是不是就变高了。再有fast API还支持自动生成可交互式的文档，那我们程序员就可以用这个可交互式的文档测试接口了，是不是很方便？

发言人   02:21

因为fast API有着这么多优势，所以我说python框架千千万，fast API是首选。以后我们要是想搭建后端服务，或者是开发公司的内部系统，亦或是我想要服务化部署一个大模型了，或者是构建微服务API，那fast API这个框架是最好的选择。咱们也来学一学，咱们这堂课可不仅仅只学框架。除了框架，还为同学们设计了两个项目，这是咱们首次一套课设计两个项目。我们先学fast API框架基础知识，接下来学项目一。项目一是AI掘金投，是一个web项目，重点学习封装接口的能力。

发言人   03:03

那么学完的项目一，紧跟着我们学习第二个项目。第二个项目就是把模型服务化，也叫模型部署的项目了。当然咱们是先带着大家训练一个模型，再来部署模型。这两个项目我已经提前准备好了，并且已经运行起来了。那我们一块儿到浏览器当中看一下。

发言人   03:21

来咱们来到浏览器，目前大家看到的是项目一AI掘金头条是一个新闻资讯类的web项目，首页面展示的就是新闻分类以及新闻裂变。我们可以点一条新闻看详情，也能点右上角进行收藏新闻。好，接下来我们点一个返回，新闻功能看到了再来顺着菜单往右还有一个AI问答功能。这个AI问答接入的是阿里百炼的千问模型，这里调用的是别人的模型。好，那接下来比如说我们来一个，你好，看看有没有反馈，收到了千问模型的反馈了，那接下来我们正式提问一下，比如说我们要关于fast API的新闻。好，小回车稍等一下。关于fast API的新闻是不是就已经给到我们了，那我这里就不等了，说明我们能拿到新闻就是接入千问模型是不是已经成功了。

发言人   04:12

好，那接下来再点到我的我的用户模块，像常用的登录注册、退出登录功能都有。而且我们还有收藏历史等等功能。比如说这个收藏，刚才咱是不是收藏了一条新闻，确实是不是收藏成功了，还可以点叉删除这条收藏。那我们确认一下，取消收藏是不是也成功了？这个就是咱们项目一的三大核心功能，新闻展示、AI问答和用户模块。那看完了项目一了，接下来咱们来到第二个标签页，是项目二。项目二是采用随机森林算法，结合数据构建了一个模型来解决文本分类问题。

发言人   04:53

有了模型了，咱们用fast API来部署模型提供服务。前端这个简易界面，我是用stream lit构建的那我们用这个界面来调用通过fast的API部署的大模型，拿到文本分类预测的结果。那我们来试一下，比如说我这里有一份文字，只需要点预测按钮就能调用模型来进行文本分类了，那我们来试一下，点预测education大家帮我看看这份文字是教育类O不OK，可以的吧？好，那我们再换一份文字来试一下好不好？那文本的话我就就地取材了，来到新闻新闻首页。比如说我来一个发射卫星的这个来点开，我们就用这个标题文字了。右键点检查，来到右边把文字control c复制一下，回到咱们的项目二文本的位置给它粘进来。

发言人   05:42

接下来重新预测了，大家不要眨眼，来预测同学们这单词啥意思？科学是不是？我们发射通信试验卫星属于科学分类，是不是妥妥的没毛病。

发言人   05:57

到这里，咱们从AI大模型的训练到通过fast API来部署服务，接下来前端来调用模型，是不是一套流程就打通了。当然咱们这里算法，我说是采用随机森林，对不对？那后面我们还可以换成机器学习，深度学习的其他算法都可以。这个就是咱们的两个项目了，后续在课程当中咱们从0到1逐步去实现功能。

发言人   06:22

接下来我们回到PPT里边来看一下每一部分要学习的时长。首先fast API框架咱们预备学习两个章节的时长，AI掘金头条要学习四个章节的时长，那模型训练加部署学习一个章节的时长。经过这七个章节，我们就可以把自己训练的模型用fast API服务化提供地址，给其他应用接入了。

发言人   06:45

整套课程除了学会faas的API框架服务化之外，还为同学们准备了很多惊喜。比如首次双项目驱动，一个web项目一个AI项目，满足大家的不同需求。其次就是讲练结合，咱们每个知识点都有配套的代码练习，学完就练，这样咱们印象更深刻。其次就是避坑，在课程当中我会尽可能的穿插一些常见的报错，帮助大家去排错。

发言人   07:12

再有AI时代写程序，有AI插件来辅助。我们用的AI插件是通义灵码，用通义灵码来提升的我们的开发效率，学完这堂课，大家可以独立搭建web后端服务，比如说工具系统都可以独立完成。其次还掌握AI大模型服务化的能力，我们可以把自己的大模型服务化提供地址给别人用。其次我会逐步的锻炼大家的编程思维跟工程化意识。再有AI时代，咱们写代码也通过AI插件通义灵码来辅助加持，让咱的开发效率更快。

发言人   07:47

接下来话不多说，咱们一起开始学习python web开发，将大模型服务化供其他应用接入，调用咱们的大模型。咱们马上开始课程，点赞收藏加关注，持续学习不迷路。同学们的肯定是我们更新课程的动力。黑马程序员为千万学生少走弯路，我们一直在努力。

发言人   08:11

我是黑马程序员创办的大学，我是8090后创办的大学，因为看不惯，所以要创办，五年超清北，十年成名校。你可以嘲笑我的自大，我可怜你总是等待。你可以轻视我们的现在，我会颠覆你们的过去。办大学是注定孤独的旅行，路上少不了质疑和嘲笑，但那又怎样，哪怕遍体鳞伤。也要办成名校。我是大同数据科技职业学院，我为自己代言。