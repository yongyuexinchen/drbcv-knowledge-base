47-头条项目-用户登录_原文

2026年07月21日 12:54

发言人   00:01

Hello, 各位同学，这一节咱们来实现用户模块的登录功能。这个登录功能是非常常见的。目前年代好多软件系统是不是都有登录功能。那大家想想，这个登录是不是就是用户名，比如说输入用户名、密码，还有些系统是输入手机号，对不对？输入用户名密码，接下来如果输入的正确，用户名注册过密码也是对的，是不是就能登录了。

发言人   00:29

这一节，咱把咱自己的登录功能也来实现一下。那咱老规矩，先分析基础路由，然后再来实现功能。基础路由的话咱就比着接口文档来分析是不是就OK了。我们从最上来，首先接口地址这里提示我们请求方法是啊post，那也就意味着我们要提交用户名和密码。好，那接下来路径API斜杠user斜杠log in，那我们直接写log in这个路径是不是就可以了。因为前半部分API user是不是写到了路由前缀里边了。好，那现在接口地址这一行我们得到了关键信息请求方法和路径。

发言人   01:09

再往下参数，因为我们是post请求，user name和password是不是得用请求体参数类型，那就是python单体类型。那在这里考考大家，这个类型还需不需要再定义？非常好，不需要。因为在注册那个接口，咱是不是也是用的用户名和密码，这个python单体的类型，那我们还用它是不是就OK了？好，那现在参数也搞定了，那再往下响应结果不用操心了，为啥？因为咱们前边是不是把通用的格式已经封装好了，那我们就整合需要的token，需要的user info响应是不是就OK了。好，这个是咱们用户登录这个接口的基础路由。大家来看看这个路由OK，我相信是不是难不倒大家了。

发言人   01:59

接下来咱们回到拍摄当中，先把这个路由给它搞定一下。来回到咱们的拍场当中来，找到rooters里边的users，点PY，我们贴着register，贴着register咱们往下写，log in。好，那我来一个艾特root一下，点儿是不是还是post方法？我tag一下log in这个路径也是OK的。然后我们a think一下，a think DEF log in是不是也没问题。那接下来user data类型是user request，是不是跟咱们这个注册它的类型是一样的，都包含用户名和密码。好，那接下来DB，这个DB是不是就是数据库绘画依赖项，那咱肯定也要。好，那接下来我们这个函数里边功能一会儿再写，接下来是不是就return结果就行，那么大家还是return大括号吗？

发言人   02:54

不是了，不是了，我们比着register注意来看是不是调用用了我们封装的这个统一格式的响应结果这个函数对不对？传入我们需要的message值和data值是不是就OK了？好，那我们也来，那就是circus ze response data，我们还不知道是啥，但message是不是可以写一写？

发言人   03:17

那我就来一个，登录成功了。好，control s一下。那现在基础路由是不是搞定了，那我们来浏览器刷新试一下，来回到浏览器里边来找到交互式文档，我们来刷新一下。Log in有了，try艾特一下，输入个用户名，你就随便来，因为这儿还没有写逻辑。大邱，接下来密码是123456，来excute来看一下，code message data是不是全都有了？登录成功了，是不是也是咱们设置的这个message信息，那能拿到这个统一格式的响应结果，那说明咱们这个路由器没问题。前面封装的那个统一的成功响应格式是不是太好用了。

发言人   04:06

好，那现在基础路由搞定了，咱是不是来分析一下这个登录流程是啥，该怎么实现？来，接下来咱们还是回到PPT里面来，回到PPT那我们继续往下，我们说刚刚写了路由了，一旦进入到请求是不是携带着用户名和密码信息的那进入请求之后，我们说是不是应该验证用户名，你注册过没有，就是在数据库里面有没有？如果有，我再看看密码对不对，对，是不是才能登录。好，所以进入请求之后，下一步我们要检查用户在不在数据库里面，那就查库是不是就OK了。

发言人   04:44

那这儿就有两种情况有可能不存在。那不存在，我就返回一个，那为的是啥？就是我加if条件判断，如果没有user那就rise抛异常是不是就可以了。

发言人   04:57

那当然这是一种情况不存在的时候，那如果存在？那用户名存在就已经注册过了，那我们是不是就得看看密码对不对？这个密码大家要留意数据库里边存的是不是都是哈希加密的。但这一次用户登录，它输入123456是不是明文的那我们是不是就得验证一下明文跟密文是不是一致的？

发言人   05:22

那怎么验证呢？基于咱们那个密码加密上下文有一个verify方法，那传入明文跟密文的这个密码一致会返回出，不一致会返回boss，这是verify这个方法的功能。好，那当然verify的结果我们说是不是有true false的两种情况。如果是false是不是就是不一致不一致return，那为的是写一幅判断，没有这样的user，那就rise抛出异常。那当然如果一致？

发言人   05:54

用户名有密码，还是跟数据库里边一样的，是不是就应该能登录了？但是咱这里登录，大家想象登录成功之后，后续还有其他的操作。比如说查个人信息，就是看个人信息，或者是修改个人信息。那是不是就得有啊token证明他登录了才能查，才能改个人信息。所以下一步我们要生成访问令牌，就是生成一个token返回给前端。好，那最终我们响应结果给到前端就可以了。

发言人   06:27

这个响应结果里面就包含token还有用户的关键信息。好，这个是咱们用户登录的逻辑思路，进入请求，携带用户名密码，判断用户在不在数据库如果在那就看看密码，用户新输入的跟库里边是不是一样的？如果是一样的，生成令牌响应结果是不是就OK，那都生成了令牌了，是不是说明它已经登录成功了？好，那这个思路各位同学还OK吗？OK的话咱们写代码来试一下。

发言人   07:00

来，咱们回到py top当中来。首先我把这个log in打一点注释，我们把这个登录的逻辑先给它放在这儿。登录的逻辑我直接先top一下，是不是验证用户是不是存在，然后验证密码是不是跟库里边一致，生成token响应结果是不是就OK了。

发言人   07:19

那大家还记得吗？这个响应结果我们现在是不是基本上就搞定了，生成token这我怎么觉得咱做过呢？注册的时候是不是就写过了，咱们在这儿还调用一次是就OK。大家抬头看，这create token是不是就是生成token的，我们在这儿也调用一次，是不是就OK。那我们现在差的是不是就是验证用户在不在库里边，明文密文、密码是不是一致的，是不是就差这里了。那接下来我们是不是就得回到CRUD里边查库验证用户验证密码了？

发言人   07:54

好，那接下来回到咱们的CRUD，我来写代码，敲一个回车。我要定义方法来验证用户名和密码，那我起个函数名a think DEF。比如说我要认证用户，验证用户，对不对？我就叫authenticate，我就叫authenticity cate user了。这么拼AUTH单词有点长，稍等一下。Authenticity cate user，就是这个单词，让我tape一下。

发言人   08:21

那接下来函数名有了DB是不是数据库绘画依赖项？参数有user name和password是不是没问题？这俩我们都要要验证用户名在库里边有没有密码，是不是一致的，所以这俩参数是不是一个都跑不了。

发言人   08:38

好，那接下来我们往下继续来写。首先是不是看看库里边这个user name有没有，那怎么看呢？来抬头get user by user name，是不是查库，看看里边有没有咱们输入的那个名字。我们把这个方法再调用一次是不是就OK了？好，所以在这里await get user by user name，那括号里边传入DB参数，传入user name用户名是不是就OK了？

发言人   09:09

那这个方法是不是会return一个结果，结果是用one RN去提取的那是不是有可能有有可能没有？那接下来我拿一个变量给它存一下，比如说我就叫user，那这个user我们说它一定有吗？那不一定是不是？那没关系，我写个if呗。如果没有这个user我return一个NONEN。那这里return难是为了干啥？我这个方法将来是不是要在路由处理函数当中调用？如果拿到了难，就证明这个用户是不是不存在，那我就写if not user rise，抛出异常是不是就可以了。

发言人   09:49

好，接下来回到CRUD，我们继续。还没完，这是用户名有没有？那我是不是还要验证密码是不是一致的，对不对？密码是不是一致涉及到明文和密文的对比，是不是我们要用那个密码加密上下文了？那那怎么办？大家别慌，我们之前在UTS里边有一个security，那它里边是不是就是咱们的密码加密上下文，我们得到过哈希密码，那我们是不是还有一个verify方法在这儿？我们封装一个方法，将来导入调用是不是就OK了，来验证这个密码，允许我打个注释，现在我要验证密码，DEF verify password，明文的密文密码，那我直接return了，return密码加密上下文very fine。传入明文和密文密码，如果一致它就return true，否则就return false。

发言人   10:46

那在这儿我稍微写一下，这个very fine，very fine。它的返回值是布尔型，是布尔类型。那我们是不是就拿着这个布尔类型来做文章就可以了。那允许我。

发言人   11:00

把这个方法名来control c一下，回到咱们的CRUD里面来。那我再往下写一个if not not not a security verify，导入一下security点verify传入现在的密码以及数据库里边的密码。这个user点password，user是不是就是上面我们查库得到的user？那所以这里边，就是加密的密码。当然如果是明文跟密文不一致，这里是false，not false是不是就是if true才执行return nine，如果这个very fine password的得到的是false，那就return now，就证明没有这样的用户，是不是好OK，这是我考虑的错误的情况，就是没有这样的用户名，也没有这样的密码，密码明文密文不一致，那都return the为的是抛异常。

发言人   12:00

当然我们说是不是还有一种情况，就是我们想见到的。我直接return一个user，就是用户名在库里边也有，没有执行第一个衣服，且这个明文密文也是一致的那就没有执行第二个if，那我就直接查出来了，这样的user return是不是就OK了？好，那咱们现在用户验证的这个方法是不是就定义完了？那允许我来截一个屏，我们去路由里边去调用。这样的话我们能对照着传参来稍微缩小一点，放到屏幕右下角来。那接下来咱们回到reuters里边来调用这个t cate user来验证用户和密码，是不是就OK了。

发言人   12:44

好，那我们来写代码。好，那接下来我await一下调用users里面的ONTK user，那我先tab一下，传入数据库绘画依赖项没问题，是不是也要传入请求体参数user data里边的用户名和密码是不是也没问题？那我们调用这个OSNDK的user这个方法是不是会得到或者那或者是一个用户。所以接下来我拿一个小变量，比如说我就用一个user把它存一下。好，那接下来我是不是就可以写if判断了，那就是if not user是不是rise来抛异常就可以了。那异常的状态码detail详情就是用户名或者是密码错误了。当然这个状态码我稍微改一改，我给它改成这个401了，401而且找到这个unauthorized就是未经授权的，未经批准的，那我直接回车一下。

发言人   13:47

好，那回车之后，用户认证完了，下一步是不是要生成token？那生成token的话，咱们直接调用方法是不是就OK了。那我tap一下users里面的create token传入地B传入当前用户的ID，那是不是会返回一个token给我们？好，那token也有了，下一步我们是不是就可以响应结果了？这个响应结果重点是不是咱们要有code message和data，咱这里是不是没有data呢？那我们把这个data给它搞定一下，那怎么做呢？大家如果忘了抬头看注册data的值是不是一个变量名。

发言人   14:25

Response data它是用user of response这个模型类来实现出来的对象，它里边包含token和user info，用户关键信息。那user info里边是不是又包含了好多好多数据？它有一个类叫user info response点model validate。大家还记得这个model validate吗？就是允许从ORM对象当中取值。哪个ORM对象呢？是不是咱们查库查出来的这个user？

发言人   14:57

好，那在咱这儿也是一样的，再写一遍，我在这里来一个response，data data等号赋值值是不是就先user of response，它里边要包含token和user info。User info的类型是user response validate model validate，提取ORM对象user当中的属性值。好，那我们data的数据已经有了，是不是在response杠data这个变量当中，我们是不是就可以响应了？好，在函数success response这个成功结果统一的格式这里的后面，我们再给它加一个data，直接tab了，data的值，就是response data上边这个变量是不是就OK了。

发言人   15:43

好，那接下来我们的响应的data也有了，下一步咱们就到浏览器当中来试一下了，我们保存回到浏览器当中来，允许我先刷新一下，那我们来一个try IT登录。这回有功能了，输入的用户名密码我们先来。对的，比如说大邱，就大邱，来123456来excute，我们来看看是不是登录成功了，有了token，有了user info这个关键信息，那当然这是用户名，对，密码也是对的，是不是登录成功。但如果不对，来咱们来一个，比如说我来个大球100，那我excute一下来看看，401是不是用户名或密码错误，data也是一个。

发言人   16:31

Now好，那现在咱们这个登录功能是不是就OK搞定了？那接下来我们拿着咱这个前端页面来真正输入用户名、密码来登录一次。来我们点去登录，输入用户名。那这里比如说我来个大邱222，这个咱们也注册过，密码1123456。来我点登录是不是登录成功，那我们去我的来检查一下大邱222是不是OK搞定。

发言人   16:59

那现在我们搞定了登录功能了，咱们回到代码当中梳理一下思路。那回到拍照本当中来，我们要实现登录是不是要验证用户名，在库里边有没有验证用户这次输入的密码跟数据库里边是不是一致。接下来如果一致，我们生成token响应结果是不是就OK了。当然如果前两步都是否定的，我们直接在CRUD里面是不是都是return的none，那也就是整个authenticated user这个方法是不是得到的是难。

发言人   17:33

假设说得到的是难，我们回到root，我是不是就if not user是不是就没有user，那直接rise抛异常是不是就OK了。在这儿我不知道各位同学会不会有疑惑，说我们上一小节儿不是封装过这个统一的全局异常处理器吗？那在这儿为什么还要抛异常呢？我说一下，异常处理器它是规定返回什么样的格式的信息，比如说包含错误的类型、错误的位置、详细信息等等等等。但我们这里是具体的业务错误，由我们这里加if条件判断来返回具体的业务错误信息。

发言人   18:13

那大家想想，如果要是没有这个if not user的判断，那假如说真的没有这个用户，那是不是就还是创建头看啊。这事儿想想是不是就很可怕。我没有这个用户还去创建token，那user ID打哪来？那没有这个ID数据库操作是不是肯定报错了？那咱的程序是不是甚至都极有可能崩溃掉？所以咱们这里具体的业务错误还是要咱自己来抛一场。全局异常处理器是规定响应的异常格式，是通用的格式。咱们走异常处理器来，那具体的业务错误，咱们自己写一幅判断就OK了。

发言人   18:51

好，那这是咱们的log in登录功能。接下来各位同学也来动手把自己项目的登录功能来实现一下。另外实现完之后不要走开，下一小节咱们在登录的基础之上获取用户的个人信息，那下一小节咱们不见不散。