26-FastAPI进阶-ORM操作数据-新增数据_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，这一节咱们来学习ORM操作数据库的新增功能。新增对应的方法是爱的ad的小括号有要求，他要填一个ORM对象，所以要真的新增数据，那我们是不是得先得到一个ORM对象，下一步才能爱的。这个爱的它只是添加对象到事物，并没有真的提交到数据库。那想要提交到数据库需要commit才行。这个是新增大致的实现思路了。

发言人   00:30

那有了思路了，咱们来看看代码该咋写呢？来上代码，先明确需求，这个接口实现的功能就是添加图书到数据库表。那咱们直接看处理函数的里边，首先有一个不可OB勾，它是啥？是通过一系列转换得到的ORM对象。先由ORM对象然后add添加到事物，接下来commit提交是不是就OK了。

发言人   01:01

那在这里边关键点是ORM对象该怎么得到。因为这个I的跟commit是不是调个方法就行了，它并不难。好，那我们来看这第一句，怎么得到的这个ORM对象呢？首先这里是不是有一个book，这个book代表的是用户输入的这个图书信息。比如说书的ID、名字、作者等等等数据。那这个数据是不是它肯定不是ORM对象，那咱是不是就得转换？

发言人   01:31

那怎么转？先把book通过下划线dict，后边还俩下划线转成字典。那有了字典了还没完，星星是不是展开运算，展开这个字典是不是就拿到了所有的键值对，把这个键值对用模型类book来转换一下。这个book是模型类，是不是就能把刚刚得到的兼职队转成ORM对象了？那有了ORM对象了，是不是就能爱的了那爱的添加到事物了，commit一提交是不是就完活了？这个是新增功能的实现方法，先有ORM，接下来去艾特commit提交是不是就OK了。

发言人   02:16

了解了实现方法了，接下来咱们也到代码当中来试一下。那我们回到py cho p里边来，在python里边我准备了一个post请求的接口。因为我们是不是要提交数据到数据库了，所以咱们这是post，那接口是添加图书。那接下来咱们明确一下需求，明确一下需求是不是就是用户输入图书的信息。然后接下来我们去新增到数据库是不是就可以了。这个图书信息我来规范一下。比如说就包含这个书的ID书名，还有作者，还有价格，还有出版社。

发言人   03:00

在这儿同学们说时间，咱原来是不是还有创建时间、更新时间，那这里需不需要让用户输入呢？注意不需要，为啥？来往上咱们去瞅一眼咱们之前的模型类，那两个时间是不是在base里面？Create time和update time，注意是不是有default默认值。这个默认值的功能就是真的当用户去create或者去update修改的时候，它是不是自动插入的是系统时间。所以这两个时间咱们不用管，来我把它折叠掉了。也就是说用户输入的是图书的基本信息，是不是就可以了？那接下来明确用户要输入的了。

发言人   03:43

接下来我们还得解决一个问题，那现在要用户输入是不是？那用户咋说呢？咱是不是提供个参数，参数是用户输入的图书信息是不是就可以了？那现在还问题又来了，那这个参数咱是要路径参数，还是查询参数，还是请求体参数？显然是不是得是请求体参数。因为我们要把这些信息是不是整体post提交到后台，所以它应该是请求体参数。

发言人   04:20

那请求体参数咋写呢？是不是也得在处理函数这儿定一个参数名，然后加个类型？比如说我这参数名我就叫book了。好，那接下来冒号是不是跟上一个类型？那接下来同学们，这个类型是不是要包含噔噔噔噔图书这些信息，那咋整？

发言人   04:42

是不是定义一个类，还记不记得请求体参数？要定义一个类，是不是它还要继承一个base model这个鸡肋，那我们先把这个base model给它导进来，那我们去最上面来检查一下有没有base model？现在来看是不是没有，那咱们就给它倒一下。大家还记得在哪里导吗？注意pydna in part base model还有印象吗？没印象没关系，咱在这儿就再记一遍，是不是就可以了。

发言人   05:15

好，来往下走，往下走，我们这敲个回车，来往下走，往下走。接下来来到这儿咱是不是得定义这个python dented的类型了？那咱是不是就这样写，从需求这儿往底下放。好，敲点回车。可lass一下，比如说我就叫book base了，最基础的。好，括号一下是不是要继承贝斯model，贝斯model冒号一下。

发言人   05:45

好，那包含啥属性？是不是上面写的ID啥的，是不是ID咱就是int类型。然后再有比如说还有book name，是不是应该是string类型。再有auto AUOTHOR author是不是也是string类型？还有Price是不是PRICE Price，注意是不是应该是float类型F典型。再来一个出版社PUBLISHER publisher，string类型是不是就OK了。

发言人   06:16

好，那现在咱们的python单体和类型定义好了，也就是用户的请求体参数的类型是不是有了，那咱们把它替换一下，这个叉叉干掉是不是变成book，base就OK了。那用户输入的是不是就到行参book里边，也就是这个请求体参数book里边，那我们就提取它的所有属性，转成ORM是不是就OK？好，那我接下来把这个pass干掉，把pass干掉。

发言人   06:46

我们说一下大致的思路。首先是不是咱们要定义这个ORM类型，对不对？先有ORM对象，那有了它之后，接下来去add，再接下来去commit提交就OK，那响应我这就不写了。

发言人   07:02

好，那来怎么去转呢？首先用户输入的是不是在book这个参数里面点儿下划线dict是不是就得到这样的字典了。那我是不是要把它展开转ORM，那我允许我剪切一下，剪切一下那模型类是不是book？我要展开走，是不是给搁进去，就是把用户输入的这个字典展开，是不是用book模型类给它转一下，是不是就变成ORM对象了？那我们起个名字，比如说就是book下划线，OB勾一下。

发言人   07:49

好，那ORM对象有了是不是就好办了，下一步是不是就添加就行了？怎么添加？数据库DB点儿at艾特谁呢？不可OB勾，是不是添加这个OM对象，完了添加完还没完，是不是得提交才行？因为我们说这一步，它就是添加对象到事物并没有真正提交。好，那我们真的数据库写入，咱们得异步的方式，await DB点儿commit提交。那接下来咱们也return一个响应，咱就不写多复杂，直接就添加哪本书，响应回来哪本书就可以了。Return这个不可回去。

发言人   08:26

好，那咱们现在添加图书这个接口是不是就封装完了？那接下来咱们到浏览器里面试一下，那咱的项目也是正在运行的状态，那咱直接回到浏览器，接下来刷新一下。爱的book在这里try eat一下。用户输入比如说这个ID，我原来是六本书来着，我这ID我就来个七书名，瞎编一个了，这是一本好书，作者是我，价格66，出版社黑马出版社。好，用户输入完了，excute提交一下来看一下。这一本书响应回来了，那能响应说明刚刚的添加应该没问题。但是我们不放心，是不是得去那个数据库里边看一眼。

发言人   09:19

来回到咱们的python里边，展开咱们的book表，来看看大邱的这是一本好书，是不是已经添加进来了。看到这本书了，说明咱们的新增功能是不是欧耶已经成功。好，那成功了之后，咱们来捋一下思路，怎么做的。首先我们这里是不是设计了一个请求体参数，因为用户要提交数据到后台，所以我们是设计请求体参数。好，那接下来有请求体参数了，我们是不是得到这样的一个字典？有了字典了，咱们还没完，展开得到所有的键值对，是不是用book这个模型类把它转ORM对象。因为我们爱的添加的得是ORM对象，所以转ORM对象转完去爱的提交到事务commit，提交到数据库是不是就OK了。这个也是咱们ORM新增数据的写法。接下来是不是该大家的表演时间了。大家也基于自己的模型类，自己的数据库表去新增一条数据到数据库，我们可以把这个代码答案添加到评论区，咱们在评论区核对正确答案。