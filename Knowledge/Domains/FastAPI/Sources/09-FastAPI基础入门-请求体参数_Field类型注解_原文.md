09-FastAPI基础入门-请求体参数_Field类型注解_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，咱们之前学过的路径参数查询参数都有两种类型注解方式。一种python原生，第二种是导入外部的函数。那咱的请求体参数也一样，上一节咱们用的是python原生的方式做类型注解，这一小节咱们来导入外部函数来做类型注解。

发言人   00:20

那咱们请求你参数用到的外部函数，是python deny里面的。注意，不再是fast API里面的，是python dentist面的feel的函数。那这个feel的函数，它的用法跟之前的pass query有异曲同工之妙。都是先导入进来，导入之后等号赋值为对应的view的函数就可以了。Feel的函数的小括号填参数，来往右边看参数，很熟悉，必填大小判断，描述长度的限制，只不过多了一个大地方falt它是默认值。这个是python dented的feel函数做类型注解的写法。

发言人   01:06

接下来咱们也到代码当中来试一下。回到python里面来，我们就用着上一节的这个用户名和密码，我们把这两个数据继续做额外的声明。比如说提个要求，用户名咱们来一个默认值，来个长度的限制。好，那写法注意了，是不是先倒包倒进来那from past dental，咱们把刚刚所说的这个field f大写FRFIELD给它导进来。

发言人   01:41

接下来在python原生类型注解的基础之上，等号赋值为feel的这个函数。如果加3个点点代表的就是必填，当然大家也不用一个一个记，我们鼠标放上来来看一下。比如说我们刚刚提到的discription是不是在这里，那向下滑动一下，像大小的限制的，再往下看，长度的限制的是不是都在这里。好，那接下来咱们回到few的函数里边，我先把3个点点取消掉了。刚刚我说是不是带一个默认值，那默认值是defauts，那打个DE有提示直接回车。那默认值比如说咱们来个张三，另外人名咱们来限制一下长度，比如说mini length，咱们来个二，max lens咱们来一个十。那接下来保存一下，咱们回到浏览器里边来试一下。

发言人   02:36

好，打开浏览器咱们刷新一下，找到registered这个接口，try eat一下来，大家看哪力有不同了。显然现在张三这个默认值是不是已经有了。那接下来比如说我先来一个错的，就是张一个字是不是不满足最小长度的限制？那接下来execute一下来看看有没有报错哟是不是string too short太短了，是不是要大于两个字符？那接下来咱们继续回到代码里边来。比如说咱们再加一个description是啥？描述是不是？那我就说这个是用户名长度要求是不是2到10个字，十个字。

发言人   03:22

好，那现在user name假设已经注解好了，那我们再把password来设置一下。比如说password，咱们简单一点来就来一个长度的限制，那怎么写？等号赋值为few的函数就可以了。那密码是不是没有默认值，对不对？那我们直接比如说mini length，咱们来一个3 max length，再来一个二十，那这个description，这个描述我就偷个懒儿不写了。

发言人   03:52

那接下来回到浏览器里边来，我们先刷新一下，同样register再次try IT1下。比如说用户名这次我填对的，那密码我们要求是不是3到20个字符，那我就填个两个，比如说一二来excute一下来往下来看看，又报错了，是不是咱们这个string is too short，来看24君to shot是不是要至少三个字符。好，那我们整体来一个对的，往上来比如说要123456，那接下来再次excute一下来看，我们的张3123456这个密码是不是都响应回来了？

发言人   04:34

这个就是咱们python dental的field函数，给请求体参数做额外的声明，做额外的类型注解。说到这儿，请求体参数常用的功能咱们就已经说完了。接下来我们回到PPT里边总结提炼一下重点。

发言人   04:50

我们回到PPT里边来，我有三个问题。第一，请求体参数有啥作用？创建更新资源，向服务端提供数据的时候，我们就用请求体参数，比如说注册是不是叫新增用户，创建用户是不是？比如说新增图书，修改图书信息等等，是不是都可以用请求体参数，请求体参数不再拼接在UIL里面，是不是出现在消息题里面，更安全一点。

发言人   05:19

好，第二个问题怎么定义使用请求体参数？对，先基于python deny导入鸡肋base model，那我们自己的类型继承base model是不是就OK好，有了自己的类型了，在处理函数这里把参数冒号写上咱自己的类型，是不是就OK，那在这里是不是就完成了这个类型的校验了？那当然这是python原生的方式做类型注解。

发言人   05:46

那紧跟着下一个问题就是我想问的，请求体参数怎么加类型注解？Python原生和feld函数是不是都可以？这个feel的函数大家还记得吗？从哪儿导进来的？也是py森单体壳里边非常好啊。好，这个就是咱们请求体参数的核心知识了，各位同学OK了吗？那OK之后就该大家的表演时间了。

发言人   06:14

那我们把上一节的新增图书是不是有书名、作者、出版社和售价，我们要做额外的类型注解。当然这里是不是就用feel的函数了。我的要求是书名不能为空，长度2到20，作者长度是2到10，出版社有默认值是黑马出版社，售价不能为空，要求价格大于零元，不能来个负的，是不是？接下来各位同学动手把咱们这个请求体参数用fid函数做一下类型注解。另外写完之后不要走开，答案留到评论区，咱们在评论区见。