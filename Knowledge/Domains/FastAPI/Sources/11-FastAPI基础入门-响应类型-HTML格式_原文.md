11-FastAPI基础入门-响应类型-HTML格式_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，经过上一小节，咱们已经了解到fast API支持的响应类型是很多的。比如说Jason fast API是不是自动帮咱转换，但如果不是杰森，我们该怎么办？这个时候需要程序员来设置响应类型，怎么设置呢？Fas ABI提供了两种方式。第一种是在装饰器当中来指定响应类。第二种是返回的时候，返回我们需要的响应对象。那么这两种有啥区别呢？

发言人   00:32

我们先来看场景，如果返回的是相对固定的简单的，比如说HTML代码纯文本，那这一种我们就采用在装饰器当中直接指定响应类的方式。怎么指定呢？就是在我们的请求方法路径的后面加上response class等号赋值为我们想要的响应类。这种看起来非常的直观清晰，但它有一个问题就是一旦在这里指定了响应类了，那当前的接口返回的就得是这个类型，它不能是别的类型了。好，这是第一种方式。

发言人   01:10

第二种方式，我们说是返回的时候，也就是return后面跟上我们需要的响应对象。那啥时候用到呢？就比如说文件的下载，图片流媒体这种需要用迭代器。如果是逐步返回的，或者是根本就不确定响应类型，比如说按条件，条件A成立就返回A类型，条件B成立就返回B类型。那这个接口返回的数据它不固定类型不固定，那我们就用返回对象的方式，这样会更灵活一点，那写法我们也来看一下，注意直接看最后一行return，这里写的是five response，其实这是啥？它就是一个文件响应对象。这是咱们fast API返回除了Jason以外其他类型的方法，这两种方法咱肯定是都要学。

发言人   02:05

那我们从简单的来这一小节，先来在装饰器当中指定响应类。好，我们就拿着这个HTML response返回HTML内容当练习。我们知道返回HTML内容它相对固定和简单，那我们就采用在装饰器当中来指定响应类是不是就可以了？

发言人   02:26

写法注意来看一眼。首先我们需要的响应类是HTML response，需要先导入在fast API response里边导入HTML response。下一步我们就在装饰器路径的后边，跟一个参数response class是不是响应类型，等号赋值为HTML response。那这样的话咱们这个接口就可以响应HTML代码了。这个引号里面像尖括号H1，斜杠H1，这就是HTML代码。它表示的意思是大标题，就是一级标题，字比较大，还有加粗的效果。这个接口咱们如果拿浏览器去访问的话，是真的可以看到HTML这种大字加粗的效果的。这是我们第一种设置响应类型的方式。

发言人   03:18

接下来咱们也到拍场当中动手来试一下，回到咱们的代码当中来，首先我们的需求注意定个需求，我们要干啥呢？是不是来一个接口来一个接口，这个接口它去响应HTML是不是代码？那怎么办呢？我们是不是得先导入HTML response？这个类大家还记得在哪导吗？对，非常好。Fast API点response里边import导入HTML response。

发言人   03:51

好，那下一步咱们是不是开始写接口了？那艾特APP get一下那路径，比如说咱就斜杠HTML1下。好，那接下来逗号隔开是不是设置race base class为HTML race bx，那这样的话咱这个接口就可以return HTML代码了。好，那异步函数DEF，比如说就是get HTML小括号，冒号参数咱不写了，直接return了。比如说我来一个引号，咱HTML代码要以字符串的方式出现。HTML它的语法规则就是尖括号，尖括号里边放名字H一是大标题，当然它要闭合就是现在的H一是开始还要有一对尖括号加上斜杠H1，这样的话这个大标题就闭合了。两对尖括号之间加内容，那我就说这是一级标题。

发言人   04:49

代码写完了，接下来咱们来运行看效果了。注意来看我们现在的项目是正在运行的状态，那我们就直接去浏览器里边了，来回到浏览器里边来，这回我暂时先不看文档，我们新开一个标签页，咱们直接来访问12780，这是个路径。当然咱们刚刚HTML路径是不是这个斜杠HTML敲回车，大家来看这是一级标题，这个字儿还是很大的加粗的效果。那说明我们响应类型是HTML response，是不是已经设置成功了？

发言人   05:24

那成功之后，我们再回到左边这个dos交互式文档里边来来，我刷新一下，给同学们看个小细节，我们找到HTML这个接口，try IT1下，直接excute来往下来看啊，响应回来的内容是不是就是我们的HTML代码H一大标题。接下来往下看响应的内容类型tax HTML是不是就是HTML类型的那现在咱们的第一种方式，直接在装饰器当中指定响应类的方式，是不是已经ok那这种方式怎么做？首先第一步，从fast API response里面把我们需要的类型是不是给它导进来。导进来之后，在装饰器就是请求路径后面加上response class HTML response是不是就OK了。那这种返回的内容相对简单的固定的，咱就用它。但是我们说也有一个弊端，我们指定了HTM response，那如果不改代码，咱这个接口是不是就只能响应HTML代码，它不能响应别的了。这个是装饰器当中指定响应类的方法，接下来各位同学也来动手来试一下。