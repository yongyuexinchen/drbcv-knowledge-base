15-FastAPI进阶-中间件_原文

2026年07月21日 12:50

发言人   00:00

零基础学AI月薪过万就来黑马程序员。Hello, 各位同学，这一小节咱们来学习fast API的中间件。开发当中，咱们可以用中间件给每个请求的前后添加统一的逻辑代码。那啥时候会用到？我们举个例子，比如说我有多个接口，这些接口都要验证用户身份。我有多个接口，每一个接口都要记录访问的日志，性能数据等等等等。那咱们怎么办？有同学说我会咱C位，又想了想这不科学，是不是？那咋办？

发言人   00:38

在咱们fast API里边，可以用中间件把这些通用的逻辑，用中间件封装起来。这样的话请求前响应前会自动执行这些代码的那这个就是中间件大致的作用了。在咱们实际开发当中，像刚刚提到的日志记录、身份认证、性能监控，我们是不是可以用中间件解决？而且注意跨域处理，咱们做项目肯定会涉及到跨域的处理的，我们也可以用中间件来完成。像一些响应头的添加设置、处理等等等，也可以用中间件，那在开发当中这么多地方都会用到中间件，是不是？

发言人   01:18

那咱得学呀，那啥是中间键呢？注意了，中间键在fast API里边推荐就是一个函数的写法，它就是一个函数就行。那这个函数会在每次进入fast API应用的时候会自动执行。具体啥时候执行呢？请求前跟响应前都会执行。这里我画了一幅图，咱们来具体看一下执行流程。

发言人   01:45

我们原来说客户端是不是要向服务器发请求，其实就是发请求到某一个接口，某一个路由了。那收到请求之后是不是要返回响应结果？但那我们说现在是不是有中间件了，中间件在请求响应前执行。那我这儿模拟了两个中间件，那实际开发当中按需求可以有多个中间件，我模拟了两个。那有了中间件了，这个请求响应的流程变成什么样呢？

发言人   02:14

来注意仍然是客户端发起请求。现在发起请求是不是先到达第一个中间件？那这个时候第一个中间件请求前的那个统一代码是不是要执行，执行完之后下一步向下传递这个请求到下一个中间键。那第二个中间键，请求前的那个统一代码是不是也要执行？执行完之后，是不是继续向后传递？那现在我就模拟了两个中间件，是不是那没有中间件了，接下来是不是就传递请求到路由处理函数？

发言人   02:47

路由处理函数收到请求了，我们要干啥？是不是要返回响应？那这个响应肯定不能直接到客户端，对不对？那也要过中间件，那返回响应到第二个中间键，那到达这个中间键了，先执行响应前统一的代码，然后传递响应到第一个中间件。那老规矩，响应前的那个统一代码执行，接下来返回响应到客户端就OK了。

发言人   03:16

这个是有中间件的请求和响应大致的执行流程。当然这中间有一个细节，就是我说这里我是不是定义了两个中间件，那谁是A谁是B先执行谁呢？注意按代码顺序自下而上，这是fast API的执行顺序，待会儿咱们写代码去实验。

发言人   03:38

好，了解了faas API中间件的执行流程了，那我来看看中间键该怎么写？注意中间键，我们说fast API推荐是一个函数，对不对？它是特殊功能的函数，顶部要使用一个装饰器，艾特APP middle war小括号HTTP，用这个装饰器装饰的函数就会变成中间件了。那我们看具体的写法来看，先有装饰器，接下来是不是就是一个函数。好，那这个函数它有一个特殊的地方，两个位置参数request代表的是请求。那connect它是一个函数功能，就是向下传递请求。如果没有connex请求传不过去就崩掉了，那这两个参数咋用呢？

发言人   04:29

注意往下来看啊，await是不是等待一步等待connex小括号request是不是就是向下传递。这个请求当然传递给路由处理函数了，它是不是会返回一个响应给我们的？那我们拿一个变量，比如说我叫response接收，那未来有了响应了，我们是不是要把响应是不是返回给客户端？所以最终return response响应结果就可以了。这个就是fast的API里边中间件的写法了。

发言人   05:00

接下来咱们也要写代码做实验了，测试写法看执行流程。我们回到payout里面来，首先我保留了一个跟路由请求跟路由响应message hello word，我们就拿它来做实验了，那我们直接上手写中间件来叫回车中间件。咋写？一个装饰器装饰的函数，装饰器是艾特APP写上middle wear，小括号里边是HTTP。接下来写异步函数DEF，那我就起名字就叫middle WARE middle了，当然我一会儿可能有一个两个多个中间件，那我就按序号米诺维尔一二来小括号冒号一下。小括号参数有几个来着？对，两个，一个请求，一个传递请求的那请求我就叫request了。接下来逗号隔开传递请求的那我们就跟官方文档一样了，叫靠nex了。

发言人   06:00

好，那接下来函数里边是不是封装请求前响应前要执行的代码，以及是不是把这个请求给它传下去。好，这个逻辑代码我们不写多复杂了，我们简单一点来就放个print打印。比如说咱们模拟一下，这个就是中间键一开始了，我们来个start CV一下，这个就是中间键一N的结束了。那么在开始结束之间要干啥？传请求传下去，是不是等待这个结果？那等待结果注意了，咱们要用异步的写法，那怎么写呢？Await一下，注意a think代表是异步函数，真的有异步代码，开头要加上await，await后边放要异步执行的代码。好，那额外的啥呢？

发言人   06:55

就是connex传递请求，并且要等待结果回来是不是？好，那我把request请求传下去。当然一层一层传下去之后，是不是总归要拿到一个响应结果的那我们用一个变量把它存上，比如说响应我就叫response了，PONSE response。那有了响应结果了，我们是不是要把这个响应是不是返回给客户端？所以在最后我们写一个return这个response响应结果。

发言人   07:27

好，那现在咱们的一个中间件就已经定义完了，那我们是不是来运行来试一下。接下来咱们来运行项目来看一下，来我们运行点链接来请求是不是已经发送成功，达到了响应结果了。那这个print打印在哪儿看呢？来咱们回到拍唱片里边来来看看中间键一开始结束，这两个打印能执行，说明未来我们要是封装这个统一的请求前，响应前的这个逻辑代码是不是也没问题？

发言人   07:59

好，那现在咱们一个中间键是不是能正常执行了，接下来咱们要来试一下，如果我有多个中间件，执行顺序，刚刚PPT里边说是不是自底向上来执行的，那我们来看看是不是。接下来我把这第一个中间键control c一下，偷个懒儿。来，接下来往下control v粘贴，咱们改吧，下边的这个我就定义成middle wear，提示词就是中间键二开始了，中间键二结束了。那接下来我们保存一下，是不是要重新发请求来回到浏览器里边，我们来刷新响应结果仍然能拿到，说明请求和响应没问题。那回到咱们的paid time里边来来重点看打印，注意来看中间键。

发言人   08:49

二开始中间键一开始这是不是模拟的，就是请求前的那个统一的代码先二后一是不是？为啥是先二呢？来往上看代码，因为按代码的顺序是不是从底下向上这个顺序执行自底向上。好，那接下来再往下看，中间键一结束了，中间键二结束了，那响应前的代码是不是也执行到了？那这个执行顺序是不是就是收到请求先过中间件2，然后传到中间键一，自下而上。好，那返回响应是不是先中间键一结束了，然后中间键二是不是也结束了。这个就是咱们多个中间件自下而上的这个执行顺序。

发言人   09:52

当然如果我们不放心，可以把一二这两个中间键再颠倒一下顺序来试一下。比如说我剪切中间键2，咱们往上放来试一下。来小哥哥把这个回车多的这个换行给它干掉，来control v粘贴一下。好，注意看咱们现在代码顺序，是不是先是二再是一。那接下来我们保存一下，保存一下control s那回到咱们的浏览器里边来刷新一次，请求响应是不是没问题。来回到代码里边来看中间键一，中间键二是不是先到达的中间键一，那我们看看为啥先到中间键一呢？代码顺序一在底下自下而上这个顺序是不是仍然适用。这样就是咱们的中间件的写法以及中间件的执行流程。

发言人   10:46

了解了写法跟执行流程了，接下来咱们回到PPT当中总结提炼一下重点，我有三个问题，第一个问题中间件作用是啥？对，给每个请求添加统一的处理逻辑。比如说开发当中需要记录日志、身份认证、跨域、性能监控响应头等等等等，都可以用中间键。

发言人   11:12

好，第二个问题，中间键怎么定义？对函数这个函数的顶部我们用一个装饰器艾特APP middle war，我们可以写一个HTTP，因为咱们这儿是HTTP请求是不是？好，那具体的写法来看看，先写装饰器，然后放上异步函数。这个函数两个参数，一个是请求，一个是传递请求的函数名。那未来我们就可以在函数里面封装请求前响应前的统一逻辑的代码了。好，那在请求前和响应前之间，我们是不是就要传递请求，connex把request传下去，并且是i await注意异步的写法，那我们。在这里等待响应回来的结果是不是用变量response我给存上了。那最终我们要把响应结果是不是传递回去，所以return这个response响应结果。

发言人   12:11

好，接下来第三个问题，如果有多个中间件执行顺序是什么？对，自下而上按代码顺序，自下而上。好，那这就是咱们的中间键。接下来就该同学们动手了，定义中间件先定义一个看能不能执行到，然后再定义多个，比如说定义俩，看是不是自下而上这个执行顺序。接下来各位同学来动手，把中间键的写法儿来试一下。