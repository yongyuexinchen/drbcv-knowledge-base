42-头条项目-用户注册-基础路由_原文

2026年07月21日 12:54

发言人   00:01

Hello, 各位同学，从这一小节儿开始，咱们来到掘金头条项目的用户模块了。用户模块核心功能有用户注册、用户登录、获取个人信息、修改个人信息。咱们这个项目涉及到的修改个人信息，包含个人简介的修改和密码的修改。这个是咱们用户模块的核心功能，这些功能咱们都要做，咱一个一个来。

发言人   00:26

先从用户注册功能开始入手。那么用户注册指代的是用户在前端界面输入用户名密码，确认密码。当他点注册按钮的时候，向后端接口注册接口发一个请求。那咱这个接口的功能，就是把用户输入的用户名、密码存储到数据库里面去。这个是注册接口的核心功能。

发言人   00:50

那这一小节，咱们就开始制作注册接口的功能了。那在这儿问大家，咱要开发一个接口，在新闻模块的时候，咱是不是有一个流程来着？大家还记得那个流程先干啥吗？对对对，先有路由器，有了路由之后定义模型类，因为咱们都是通过ORM的方式操作数据库，所以是不是得先有模型类才能封装增删改查操作数据库的方法。当然在咱们这儿，注册账户是不是把用户输入的用户名、密码要添加到数据库？所以咱们这里涉及到的是数据库的新增。那封装好了CRUD方法了，下一步在路由处理函数当中调用CRUD方法，响应结果是不是就可以了？

发言人   01:38

这个是接口的实现流程。那像咱们上一章节做新闻模块，是不是基本上是按照这个流程来的那同样的流程，咱们再到用户模块来试一下。也就是说咱们要做注册功能了，是不是得先有模块化路由器？那在这儿问大家，这个模块化路由是不是得参照一个文档来写来着？大家还有印象吗？接口规范文档那里边是不是明确要求地址，参数响应结果啥的那同样咱要写注册也是一样，先找接口规范文档，来，咱们打开接口规范文档，从接口详情里边找到用户管理，找到用户注册。

发言人   02:20

那接下来往右扭头来看，接口第地址这一行请求方法是post是不是提交数据，咱是不是就是提交用户名密码到数据库里面。所以请求方法注意post跟我们原来的那个新闻模块是不是不太一样了，新闻模块全是获取，对不对？好，那继续往下，路径是API user斜杠register。这个registered是不是注册的意思？但那个API user对它应该是路由前缀，对不对？我们来检查一下，来看第二个接口，用户登录来看是不是也有API user。那我们再往下来看看，还有获取用户信息是不是也是以API user开头。所以现在咱就知道了这个API user是不是模块化路由的前缀。那真正咱们写路径，写后半部分是不是就OK了？好，我们还是回到这个用户注册的位置。

发言人   03:21

现在确定了请求方法和路径了，那下一步来到参数这里。参数是不是user name，是用户名，string类型。那password是密码也是string类型。只不过在这儿温馨提示一下，咱是post请求，还注意，post请求那参数写啥类型呢？路径参数查询参数，请求体参数哪一种？对，请求体参数。

发言人   03:55

那在这儿就又得回忆，这个请求体参数怎么校验类型。对对对，python单体类型。所以咱们是不是得把用户名密码回头封装到一个类里面，然后用这个派森单体的类型做类型校验，是不是就OK了？好，在这儿大家先知道username、password, 咱是不是python dental做类型校验。

发言人   04:19

好，那接下来再往下响应示例，响应的结果包含code、message、data这三个属性，是不是跟咱新闻模块是一样的？那接下来往data里边看，有token和user info。这个user info不用我解释是不是用户的关键信息，但这个token是咱们这个项目当中第一次见，这token是啥？他说叫用户访问令牌，那这到底是啥呢？我大白话解释一下，用户访问令牌其实就是用户是否登录的一个小标识。

发言人   04:55

如果有token且没有过期，那说明用户登录了，登录了可以看个人信息，可以更改信息，可以修改密码。但假设说没有token或者是过期了，就代表他没有登录。没有登录还能不能看个人信息？不能对不对？那不能看是不是也更不能改了？所以在这里咱们有一个token的返回，用token去验证用户有没有登录，登录了可以做什么事儿，没登录就不能做什么事儿，这是token的作用。

发言人   05:31

像在咱们用户模块，好多接口都涉及到token的响应，那在咱们的注册这个接口，也要返回一个token。那同学们可能说我不是注册吗？为什么要返回token呢？其实是在注册的时候，返回了token，前端拿到token他直接登录了。那这样的话用户操作是不是就更方便了？那用户体验更好，那这是咱们注册接口返回token的作用。在这儿token的作用大家稍微知道一下。至于怎么创建token，怎么返回，咱们回头到代码当中详细去说。

发言人   06:06

好，那现在咱们注册接口所有的要求基本都看到了，那下一步咱是不是可以对照着这个要求写基本的路由了？咱们还是老规矩，先把基本的路由给它写出来，没问题了，是不是？再往里边去细化功能就可以了。

发言人   06:23

好，那咱们写基础路由。首先这个请求的地址容我偷个懒儿直接复制了。好，那回到咱们的拍场当中来写路由，对不对？注意，咱现在是用户模块了，需要在reuters里边新建一个文件了，不能再往news里边放，news是新闻。好，那reuters新建python文件，起个名字，比如就叫users。

发言人   06:47

好，那接下来是不是写模块化路由，大家还记得要干啥吗？导入创建API root实例是不是写路由？那我from first API import API router。好，那下一步创建API router实例是不是这么写？API router小括号，然后存到一个变量里边来。那这个API root小括号是不是prefix填前缀tags是不是写分组，那这个分组我就不动了，就是users了。那前缀容我先粘贴一下这个register，剪切走前缀是不是就是API user register，是不是写到路由的装饰器里面来？

发言人   07:31

好，那现在API root实例是不是有了，下一步我们是不是就可以写装饰器，写路由处理函数了？好，那接下来艾特root or点儿提示我的也是post，我直接tab了。好，那接下来双引号里面放上斜杠register这个路径，那我们再往下写处理函数，直接tab了来看DEF register，这个参数。我先干掉，咱还没有先给它干掉。然后接下来响应的结果也不是我们想要的给它干掉响应的结果。我还是偷个懒儿，回到API接口规范文档里边直接复制一下，那来到这个文档，我们找到响应示例，直接选择来control c，回到咱的pyt m直接return control v一下。我们稍微把它对齐一点儿，来tab缩进一下，来敲一个换行符。那现在注意来看，register这个装饰器，registered这个路由处理函数是不是写完了那个参数，咱们等一会儿再说。

发言人   08:33

好，那我的这个基础的路由有了，下一步是不是把它挂载到APP上去？那挂载的话我们就得回到main点PY了。来看，这里我们还有注释，AAPP点in可录的rotor是不是就是在挂载路由，是哪个模块下的哪个API router实例。咱是不是得把user里边的给它导进来？好，那我们从上从root里边import，我们逗号隔开，把users给它导入进来。接下来咱们来到底下，同样APP点in可录的rotor，写上users里面的rotor是不是就OK了？

发言人   09:16

好，那写完之后，咱们的项目也是在运行的状态，那我们就直接来到浏览器里边来测试了。那我们刷新一下来看registered是不是已经有了，那我们点开直接try IT。咱们没写参数，就是没有参数的状态。

发言人   09:31

Execute执行来看响应的结果是不是已经拿到了。那能拿到这个结果，说明咱们的基础路由是不是没有问题。当然路由咱是不是还没有写完，咱是不是还没有参数？那回到咱们的代码当中，咱们写一下这个参数，那大家还记得我们需要啥参数吗？

发言人   09:53

对，容我打个注释，是不是得有用户的信息，还得有一个对数据库绘画依赖项，对不对？那我先把这个数据库给它搞定了，DB一下冒号，直接tab一下了，类型没问题。Dependence依赖注入是不是也没问题？那我们依次倒包一下，think session dependence也得给它导进来。稍等一下，in part找到fast API里边的dependence get DB大家还记得吗？是不是在configure里面直接导进来？这个user的话我们先等一会儿，直接把它干掉一下。干掉一会儿咱们自己写。

发言人   10:35

好，那现在DB是不是搞定了？那接下来就是咱们这个接口要求的用户名、密码这个用户信息了。大家还记得吗？分析接口文档的时候，我们说咱是post请求提交数据，这个用户名、密码咱们得python tic做类型校验，对不对？那怎么办呢？爱写class，那我找个地儿，schemers大家还记得吗？咱们在工程化的时候就说scams放的就是。Python单体类型，做数据类型校验的对不对？

发言人   11:13

好，那我们右键去新建一个python file，那我写上这就是users，也叫users了，是scams里面的那我们要做类型校验，是不是依赖于python单体，所以我先F让python tic import。大家还记得base model吗？我们先导入base model，接下来咱的类继承base model。好，那我们来写一下，class一下那空格，比如说起个名字，我叫user REQUEST request，我tab一下了。User是用户request，是请求是不是就是用户请求相关的那继承base model是不是就属于python单体壳类型了？好，那下一步user name password as string类型，那我敲一个回车。

发言人   12:08

好，那现在咱们的派森单体的类型是不是写完了，那下一步参数是不是就能写了也能有类型校验了。那回到咱们的reuters里面来，我往前写了，比如说我就叫user杠data，冒号，我分号一下，这个类型各位同学写谁啊？对，是不是咱们刚刚定义的user request，当然咱是不是得导包先给它导进来。好，那我们导入一下rome scams点user import谁呢？User request, 那我们的类型是不是就是user request？

发言人   12:47

好，那现在咱的参数搞定了。那一会儿我们刷新交互式文档，是不是就得用户输入用户名密码了？那我咋验证一下它有没有输入成功呢？如果用一下，来看我这儿是不是有一个username，写死了一个字符串，我把这个写死的字符串先给它干掉，换成参数user data里面的username。一会儿能拿到用户名，那这个密码是不是也就能拿到？好，那现在保存，咱们再去刷新测试一下这个路由。

发言人   13:19

来回到浏览器里边来，我们刷新一下，来看try IT1下，是不是已经有了参数username password了。好，那我来一个，比如说大秋密码123456，来我点XQ的提交来看看有没有用户名。已经拿到了，那这里能拿到用户名咱也就踏实了。将来密码是不是也能拿到？那咱现在这个请求体参数是不是已经搞定了？那拿到用户名密码了，下一步咱们是不是就可以写注册的逻辑了？那注册的逻辑，咱们下一小节再说，这一小节儿先搞定基础路由就行。

发言人   14:02

那搞定了路由了，咱们回到代码当中稍微整理一下重点。来回到payton m这一节，我相信基础的模块化路由大家肯定没问题，重点就在哪儿呢？好久是不是没写过请求体参数了，请求你参数怎么用？是不是先定义python dente类型，从python dentil里边导入base model，接下来自己的类继承base model，写我们的需要的参数是不是就OK了。

发言人   14:35

咱们的user request就是一个python dental类型，那我们需要传参用户名、密码，那是不是也就定义一个参数，冒号写上python单体壳类型，那用户传递的数据是不是就是我们刚刚定义的类型了？那这样的话就完成了post请求方法请求体参数了。这个也是咱们registered注册这个接口基础的路由。接下来各位同学也来动手把自己的注册接口基础路由来搞定一下。搞定之后不要走开，下一小节儿咱们开始分析注册的功能，实现注册功能，下一小节不见不散。