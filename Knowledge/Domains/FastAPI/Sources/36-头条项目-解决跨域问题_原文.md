36-头条项目-解决跨域问题_原文

2026年07月21日 12:49

发言人   00:01

Hello, 各位同学，在上一节的最后我抛出了一个问题，让大家先不要运行咱们的前端项目。因为在默认情况下，咱们前端view项目直接请求后端fast API项目会有问题。那这一节咱们先来看问题，然后把这个问题给它解决掉。

发言人   00:18

我们把前端view项目运行起来，咱们先看问题。首先我是通过CD的方式，已经切到了前端这个项目目录里边。接下来通过命令NPM run DV把前端项目是不是跑起来了。那接下来咱们到浏览器里面输入local host 173，先看一下这个项目。

发言人   00:39

好，那项目打开之后，我右键点检查一下，大家来看前端页面，上边这是不是新闻分类，看起来没啥问题。底下的列表之所以没有，是不是因为咱后端还没写接口？那接下来大家看浏览器右边这里，来看是不是疯狂报错。那同学们可能会想海报，就是接口没写全列表接口是不是还没有，所以是不是报错？其实我说一下，这个报错是一部分原因。

发言人   01:09

我们先看分类，这里给大家看一个神奇的事情，我们右键再点检查一次，点1下更多。大家看分类有，但全吗？不全。咱记不记得数据库有几个分类？对，有八个。现在是不是只有七个分类，那说明啥？数据不对是不是？

发言人   01:34

好，提前跟各位同学说，这七个分类是我在前端写的默认的。如果请求后端失败了，就显示这七个默认的分类，那也就说明咱其实characterises分类接口写了，但前端请求成功了吗？根本没有，对不对？

发言人   01:54

那为什么没成功呢？咱们是不是得在报错提示里边去找答案？当然我说一下咱这个报错的这个提示太多了，我就任意选一个告诉大家问题。

发言人   02:08

大家来看在这里有一条has been blocked by CORS，这是什么意思？Hesba blocked，已经被终止了，阻止了拦截了，被什么拦截了？Cos好，允许我把这一条报错，先给它截一个屏，钉在这儿。来我截一个屏，在这里。好，截屏定下来。那我把我的浏览器这个窗口先关掉，因为它疯狂的报错比较占用电脑的资源，所以我先给它关掉。我把这个报错稍微放大一点，大家来看，报错的关键点是不是这里有一个cos但是同学们别担心，说这个接口是不是不对，那没关系，就是所有的接口如果你一个儿一个的去看，全都有这一句报错，全都有啊。

发言人   03:00

那现在我们发现问题了，但是不是不知道是啥意思。首先一个关键点，被拦截、被终止，被谁终止？是不是cos那我们是不是得先弄明白到底cos是什么呢？这个问题该怎么解决呢？那我们一块儿到讲义里边来找答案。那来到讲义，首先cos它叫跨域资源共享，是浏览器的一种安全机制。浏览器要求前后端同源才能正常发请求。如果前后端不同源，那需要服务器授权才能发起请求，也就是服务器要注明哪些前端可以访问。

发言人   03:44

那现在问题就来了，什么是同源，不同源了该怎么授权？我们一个一个来看。首先同源要同时满足三个条件。注意要同时满足的条件，协议域名端口号要一致才叫同源，否则其中任何一个不同都叫跨域。那跨越了就会触发浏览器的这个安全机制，那咱的请求就会被拦截。

发言人   04:14

我准备了几个例子，咱们来分析到底是同源还是跨越。来上注意第一个前端地址是local host 5173协议是HTTP，后端协议HTTP local host 8000同源还是跨域？跨域啥不同了，是不是端口不同？好，那再看下一个HTTP example点com，HTTPAPI example点com，这是同源还是跨域？还是跨域什么不同了，是不是域名不同了？好，那再看下一个，HTTP example点com后端还是HTDP example点com那是同源还是跨域？这个没跨域，是同源的那了解了同源跟跨域了，咱得看看咱这个代码。

发言人   05:09

我们的前端是view，后端是fast API运行地址。我们就说默认前端view是不是HTTP local host 5173，后端fast API是HTTP127，8000端口号同源还是跨域？跨域了，所以是不是被浏览器的安全机制给他拦截了？

发言人   05:32

但是在这儿我多问一嘴，这个跨域同学们是啥不同导致的跨域，注意端口是不是肯定不同域名？有同学可能说老师local host是本机127是不是也是本机域名，这是不是相同的呢？注意域名不同，local host 127在浏览器眼中他俩可不一样。所以咱们的项目前端view后端fast API只有协议相同，域名端口全都不同，所以跨域了。那这个请求就被浏览器的cos安全机制给它拦截了，所以是不是就出现报错了，这个就是咱们问题发生的原因，那找到问题了是不是得解决问题，那怎么解决？

发言人   06:27

最后说一下添加cos中间件，因为中间件是不是可以封装在每个请求进入之前要执行的代码。那咱封装的代码其实是不是就类似于授权。我的服务器要授权，哪个前端可以访问，那么进入请求之前看前端地址是不是被授权了，授权的可以继续请求，没授权的那就拦截呗。

发言人   06:52

那这个代码该怎么写呢？提前说，在fast API里边不用咱们自己定义cos中间键，因为fast API它内置好了。那我们来看一下写法，注意首先导包从fast API middle are cows里边导入cos middle are就是cos中间键。好，那接下来添加这个中间键，怎么加呢？来注意基于fast API实例点admired war是不是添加中间键的意思？那添加的是哪个中间键？就是我们刚刚导入的cos middle YE。

发言人   07:30

好，那添加中间件了，后边还有四个参数我们稍微看一下。第一allow origins，就是允许访问的源。因为我们说cos中间件核心功能是干啥？是不是我要授权哪些前端能访问我，对不对？其实这一句就是授权可以访问的来源。

发言人   07:52

我这里写了一个列表，列表里面放了一个星号，表示的是所有。在这儿我要说一嘴，大家要注意，我们开发当中写星号没问题，因为这个代码我们没有正式上线。咱写啥都可以写心儿，那就所有是不是都能访问，但是真正上线可不能写心儿，这个风险是不是太高了？真正上线怎么写？大家往上看啊，我提前定一个列表，把允许的圆给它写在这儿，将来把这个列表心儿就换成上边列表的名字，是不是就OK了。

发言人   08:28

好，第一个就是允许访问的源我们搞定了。那再往下是否允许携带cookie那为触就是允许，那下一个是允许的方法写星号，就是所有像我们的get post put delete啥的心儿是不是全都包含了。好，再往下allow headers就是允许的请求头，那这里信号是所有，那我们未来的token是不是都是放在请求头里边的，所以这里我们是允许了所有的请求头了，好，那这个就是咱们cos中间件来解决浏览器安全机制这个跨域问题。这个添加中间件的代码，注意放到main点PY主入口文件里边就行。因为咱们每个接口是不是都涉及到同源跨域的问题，所以在主入口配置一次全局生效就行了。这个就是咱们的跨域问题，添加cos中间件来解决。

发言人   09:23

了解了写法了，接下来咱们也得解决一下这个问题。那我们来到代码当中来，在咱们的代码里边要添加中间件解决跨越问题，是不是？那我们得先干啥，打包好。From fast API点儿middle war点儿cos import的谁，cos middle war倒班倒进来之后，下一步基于fast API实例去添加中间件是不是就可以了。大家来看我的小助手又上线了，直接给他采纳一下。

发言人   09:57

我们来看看代码APP face的AP实例添加中间件添加的就是cos middle are，是不是就是刚刚导进来的。好，那下一步允许的原是不是星号？所有再强调一遍，咱开发这写星号没问题。真正企业当中要上线是不是得定义一个授权列表？好，那我们在这儿加一点小注释，这个是允许的允啊允许的允允许的允的切一下输入法，允许的允那逗号，那默认咱这儿是不是芯儿了，那我们开发当中是不是允许所有逗号隔开一下，生产环境我们要指定，生产环境就是上线咱们要指定。好，那这个允许的圆搞定了，下一个我们再打一个注释，这个是允许携带cookie那再往下method的这一句是按允许的请求方法，我们写星号。是也是所有因为咱们将来肯定是get post，delete啥的是不是全都有，所以这儿直接写心儿了。好，那再下一个，allow headers是允许的请求投，那将来咱们的token是不是就是加在请求头里边的，所以我们这儿先写一个星儿。

发言人   11:17

好，那现在咱们的cos middle are这个中间键是不是就添加完了？那我们是不是得验证一下，它能不能解决咱们的跨越问题。好，那我们回到浏览器里边来，同样我们的项目还是在运行的状态。我们是不是得乐扣后45173来访问一下？首先看两点，一是那个分类全不全，二是看cos那个提示还有没有。好，那我们来看一下，logo host 5173，先允许我右键点个检查来到更多。大家看真是神奇，财经是不是已经出来了八个分类，是不是全都有了。那说明现在是不是才是真正的前端view向咱characterises这个接口发请求，是不是拿到了接口响应的结果。

发言人   12:09

好，数据有了之后，我们顺便看一下报错提示，还有没有cos相关的提示呢？这里如果大家有耐心，你就上下滚动着来看，一定是没有cos的提示了。所以现在咱的跨域问题是不是就已经解决掉了？那我们添加了中间件分类，是不是全都能获取到了？而且在这里跟大家说，因为我们是全局配置，一劳永逸，其他的接口也都能正常被前端发请求了。好，这个就是咱们的中间件cos middle war是不是解决跨越问题的那问题解决了，接下来咱们回到PPT当中总结提炼一下重点。

发言人   12:53

好，大家帮我分析下边浏览器这个报错的原因。主要看这一句has been blocked by course。那这个报错的原因是是不是触发了浏览器的安全机制了，允许同源，请求不同源的，是不是得让服务器去授权？

发言人   13:15

好，那接下来第二个问题，同源三个条件是啥？协议、域名、端口都相同才是同源，哪怕只有一个不同，是不是也是跨域？一旦跨域是不是就得解决问题，怎么解决对全局配置cos中间键是不是就可以了？这个中间件是fast API内置的，对不对？来我们上斜方，首先导包cos me wear，接下来基于fast API实例来添加中间件，添加的中间件就是cosme w刚才导班导进来的，那么设置我们允许访问的源设置能不能携带cookie设置请求方法，请求投。那当然在这儿再提示一下，就是这个授权的允许访问的来源能不能一直写心儿？咱开发写心儿没问题，上线是不是得换成真正的，你允许哪些前端来源，是不是写谁不能写星儿，风险太高了。好，这个就是咱们的cos中间件解决前后端跨越问题。

发言人   14:28

接下来也该各位同学来动手了，大家先把前端项目跑起来，看看是不是有cos问题。接下来添加中间件解决问题，看一下是不是添加完中间件之后，那个分类就是数据库里边的八个新闻分类。另外也去浏览器那个提示信息里边看看添加完中间键还有没有那个浏览器cos的提示。那接下来大家来动手来添加中间键来试一下。