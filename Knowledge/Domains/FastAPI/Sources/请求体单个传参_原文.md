请求体单个传参

请求体单个传参_原文

2026年07月22日 14:53

发言人   00:00

好，各位小伙伴们大家好，我是老肖。接下来我们继续回到这个请求体传参。上节课我给大家演示了一个案例，对吧？这个请求体船舱确实未来在企业开发过程中很有用。它可以把从前端或者从浏览器发送过来的这个数据，节省格式的数据。把它转换成一个python的对象，这是EMP对象。好转换成python的EMP对象之后，那这个对象我们就我们处我们要处理起来就非常的方便了。

发言人   00:31

但是有这么一种极特别的情况，就是我不想把它转换成对象，我就因为我传的数据比较少，比如说接下来我从这个浏览器或者通过GS传过来的数据，是一个非常简单的阶层格式的数据，它里面我用多行注释。它里面是这么一个数据用阶层格式的都是画括起来对吧？传了一个name。然后冒号，然后一个值，那这个值是什么？我们暂时不管它，它还传了一个IG，二级里面也有对应一个值，这个值是什么？我暂时不管它。各位完了没了，他就传了这么一个非常简单的节省格式的数据，里面就只有简简单单的两个值是吧？

发言人   01:27

这样的节省格式的数据，难道我还有必要给他定义一个模型类吗？我觉得这种情况可能很多同学可能考虑一下不是说不能定义成模型来定义成模型的代码写的多，那能不能解，能不能直接把它变成一个一个的函数中的形参来接收了也是可以的。假设你的请求体就传这么一个参数，请求T叉叉这个阶就是我们的阶层数据，我们今天数据就这么简单。那这个时候你要定义的一个，我们再定一个函数，这样我们直接抄过来，从这抄过来，这粘贴好跳过来之后，我这边放一下，叫test，这边也要开始一个特殊的一个也叫添加员工测试添加员工。好，那他传过来了，就这么简单，一个name一个edge没了。

发言人   02:27

我我我懒得去定义模型类的，好不好？你说老师那上面这个EMP的模型类可以用吗？当然可以用，因为这里面包含了name和edge对吧？老师那那上面这还多了两个了，多两个没关系，因为只要这两个了，是可穿可不穿的对吧？那都可以。我现在不用上面那个模型的了，我就这样定。这个STR对不对？

发言人   02:55

然后等于这个时候等于什么呢？等于body。因为它是请求传统保底，保底也是来自于fast的API里面的包底然后这里面假设你给他一个默认值，默认值是那对吧？假设你还可以给他一个description描述这个测试测试的信乱写的逗号。然后接下来edge，因为你两个参数对吧？进来ins类型的，然后一样的等于保底。

发言人   03:28

记住，我总结一下之前我们的路由传参用的是什么pass对吧？那个对象然后我们还讲过URL的传参是不是？那我们用的是query。现在我们讲请求体的单个传传参，那么用的是body好不好？所谓叫单个就是一个一个的从阶层格式中一个一个的值，一个一个的接收这样单个传单，那我们用body好吧？当特别多的话，我还是建议大家，尤其还有这种嵌套的方式，我还是建议大家使用这个模型类好吧？

发言人   04:01

好，然后接下来我们开始把这个写完整，一样的default，我们等它等于18，我们冒险的description就是测试的年龄，我把这个接收过来的参数打印一下，把name也打印一下，IG也打印一下之后，我返回一个一个随便一个什么信息，message SG冒号，然后OK打起来。OK好，可以了，好，非常简单的。好，我现在剪完了。剪完之后，我们现在启动一下，走启动之后，回到这个接口文档中刷新一下。

发言人   04:44

刷新之后，我们还是一样找到请求体状态，这里面是多了一个接口，对吧？这是上上节课那个接口。好，我们点击这个，你看点击chat out，其实我们可以点击schema在这里看啊。我们这个description没有描述吗？我们的描述我是写的对吧？Description scheme描述不起作用，body description测试的姓名，测试的年龄，在我们这个比较特殊了。你看它这里面没有没有描述对吧？没有描述的话，我们先测一下看看能不能接受参数。

发言人   05:27

然后我们点击try IT out，传一个加3。好这样传，传一个19岁，19岁。好点击excuse走是吧，是不是可以了，对不对？没问题。

发言人   05:45

当然我们还有一种我们之前忘记给大家讲了，其实我们还有一种接口文档，这个文档是不支持交互式的，叫REDOC，叫REDOC回撤。当然了这个又出现了一个什么？因为这个接口文档它也需要去请求远程的，所以就是国外的服务器，远程服务器上的一些数据，我们可以刷新一下。那这个国外的服务器的数据，就你看这个GS，它就请求不了，对吧？我们可以看一下他的请求地址是这个，对不对，包括这个CSS也是的啊啊啊，包括这个图片，那么还是解决方法还是一样的。

发言人   06:27

上周我们之前说了的，这也是一个，当然这个不是交互式的一个接口文档。那这个时候你要想解决了，还是一样先翻墙，然后再把这些CSS文件，GS文件下载下来。好好，这就是单个处理，就是在请求体传上的时候，单个我们把节省格式的数据一个一个的单独来接受，那就采用这种方式就可以了然后当然这个用的不多，这极特殊情况下才会用到，就是作为一个特你好，接下来我们其实已经讲了各种各样的请求传参的方式，这些三种请求的传参的方式，未来你一定会用到。其中其中就是URL的传参稍微用少一点，路由传参以及这个请求对传参稍微用的多一点。

发言人   07:19

那么接收过来的参数，我们往往需要和数据库进行打交道，对吧？需要和数据库进行侦探改查。所以接下来我们就要暂停fast的API了。因为如果我们接下来就要开始去处理数据的话，还需要学另外一个和fast API这个框架无关的无关的一个ORM的框架。我们采用的是circ aci所以从下节课开始，我们要讲一下这个third army。讲完certain army之后，再回过头来把third army和fast的API结合在一起。你就知道了在fast的API中我们收到了请求的参数之后，怎么样和数据库打交道。

发言人   08:03

好，这节课我们就到这儿，谢谢大家，再见。好，我做一个补充，其实上节课我们可以看到这个description，只是我没有给它展开你看我们展开之后，这不是可以出现我们的description，对吧？包括这边展开也是可以的，default它跟前不一样，它需要展开一下。

发言人   08:22

好，我们回到这里来。这里我们在这个scheme中，其实也要展开才能出现每个字段的注释，对吧？那么整个这个对象的整个这个类的EMP这个类的注释直接可以在这上面看得到。但是每个字段的注释了你要展开一下，那跟上面的这个实际上是一样的，这里也是要展开一下才能看到这个主持，不是说看不到，是有的。好，另外还有这个REDOC这个它纯粹的是一个接口文档。

发言人   08:52

现在你看我们可以访问了，是因为我这里打开了方向，这里面是不能交付，其他的都能做。你看我比如说我们的请求的传有两个接口，对不对？一个是添加员工，一个是测试添加员工，这个就不用展开了，对不对？是不是要传的参数，你看这里就有这个参数的描述，以及这个接口的描述，这里都有，这其实应该叫杰克的摘要，杰克的描述我没有写，没有写，那他没有注释，好吧。另外这里面你看这里面就是默认的或者说给你一个例子，给你一个案例，就是请求的案例，那你可以把这个请求体，就是阶层格式的数据点击拷贝一下，再放到其他地方，放到专门的一个测试工具里面去做做测试也是可以的，好吧？

发言人   09:39

好，总而言之，这个REDOC这个接口文档是不支持在页面上直接进行接口测试的。那么在这个DOCS这个接口文档中，是可以直接在页面上对接口进行测试的。因为这是基于一个底层，是基于一个swagga的一个接口文档，swagger这个接口文档很著名的在java语言中，go语言中以及python语言中都是都是通用的，这里做一下补充，谢谢大家。