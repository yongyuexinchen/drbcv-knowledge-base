23-FastAPI进阶-ORM操作数据-聚合查询_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，这一节咱们学习ORM的聚合查询。聚合查询功能是用来做统计的。就比如说我要统计数据总量，我要求和平均值、最大最小值，我们就用聚合查询就可以了。那聚合查询写法就是function FUNC点方法名小括号，小括号里面放上模型类点属性。写了哪个属性，则根据这个属性对应的那个字段值来做统计。常用的统计方法有，比如说coin统计数据总量，AV这是求平均值，max mini就是最大最小值，some就是求和了，这是function点方法。这个聚合计算的写法。

发言人   00:46

那这个聚合计算写在哪儿呢？来我们看一下写的位置，注意来看slike，它的小括号里面写聚合查询的代码，fk tion点儿AVJAV这是不是求平均值？那么依赖于哪个字段的值求平均值呢？来看book点Price，那是不是就是价格。那XQ的执行完这个select查询之后，是不是会返回平均值给我们，那我们要提取结果，注意这里提取的结果是不是就是一个数字平均值。那我们用scale小括号，注意没有S，那提取到结果了，我们return给客户端是不是就可以了。这个是咱们ORM当中聚合查询的写法了，那么接下来咱们也来试一下，来回到咱们的拍场当中来，我准备了一个最基础的路由，请求路径是不可控，方法是get point，但是代码还没写。那接下来咱们来试一下聚合查询，我们分别来试一下。

发言人   01:50

首先聚合查询是不是写select小括号，里边是不是放FUNC点某个方法名。比如说我们先来一个coin小括号，那你要按哪个字段统计？比如说我写上book表，咱们的模型类点，比如说咱就按ID了，那这个是一句ORM语句，咱是不是await DB点XQ te要把它执行一下。那执行完是不是有一个结果？那我拿一个result存上，那存上之后依赖于result点儿scale，注意没有s scale小括号把它提取出来。那么提取出来之后是不是会给我们一个数字？比如说我就叫number等号来一个赋值，最终我们把这个number返回给客户端。

发言人   02:38

接下来咱们到浏览器当中来试一下，来刷新一下，点开point try IT excute一下来看数据是啊6，那这个六表示的是啊表里边是不是一共有六行数据，对不对呢？来咱们回到这个拍照P当中来找到不可表六行数据妥妥的没问题。好，那接下来回到咱们的main点，PY point功能是不是看到了？

发言人   03:05

那接下来我们再测试一下其他的。我偷个懒CV一下，上边这个coin给它干掉，那我们改一个。比如说我想求一下这个价格里边谁的价最高，那是不是max求最大值，当然价格是不是就不能写book点ID了得是book点Price那保存一下。接下来回到咱们的浏览器当中，重新excute一下，来看201是不是价格最高的是201，那对不对呢？来咱们回到pat time当中，找到book表来看价格201是不是最高的，是吧？好，那现在这个max最大值我们测试了，那这个最小值我就不试了，那允许我CV一下，把这个max最大值注释掉，咱们测试一个。

发言人   03:54

比如说求和，我要看看买这么多说要花多少钱，萨姆做求和来保存一下，回到咱们的浏览器当中来，再次提交来看价格一共要花万910，这个应该没问题。咱们要相信计算机，就不一个一个加了。好，那回到咱们的代码当中来，萨姆求和看到了，咱们再试一下那个AVG，就是求平均值。来AVG1下来保存文，回到浏览器重新exclude来看，151.666，平均值是不是也计算出来了？那这个就是咱们的这个聚合查询的写法，是不是方格式？然后点儿方法名儿就可以了，方法名儿就可以了，方法的小括号填上模型类的属性，那我们在这儿稍微加一个，不是，咱们现在学的是聚合查询，那写法是怎么写的？

发言人   04:49

是不是基于select小括号，这个小括号里面放上function点儿点儿方法名儿，再来个小括号，这个小括号里面是模型类点儿属性，是不是就OK了。对于point它是求总量max最大sum求和AVJ平均值，那个迷你最小值我没有测试。好，每个这个聚合查询它是不是返回的是一个数值一个数字结果，那这里我们是不是用scale来提取的那在这儿我加一个注释，这个scale它用来提取一个数值，一个数值它专业一点，这玩意儿叫标量值，就这么单一的一个数值，我们用scale来提取，没有S。好，那这个是咱们的这个聚合查询的写法。接下来该各位同学的表演时间，大家也来动手来测试一下这几个聚合查询的方法。测试完之后，把你的答案留到咱们的评论区，咱们在评论区核对一下。