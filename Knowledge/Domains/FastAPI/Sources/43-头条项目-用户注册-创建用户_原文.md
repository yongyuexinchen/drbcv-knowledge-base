43-头条项目-用户注册-创建用户_原文

2026年07月21日 12:54

发言人   00:01

Hello, 各位同学，上一小节咱们实现了用户注册这个接口的基础路由了。那这一小节咱们来分析实现一下用户注册的功能。大家回忆我们上一小节写路由的时候，是不是携带了用户输入的用户名和密码。但是这个用户名密码，咱是不是用python dente做类型校验的？好，那我们最终的目标是不是把用户输入的用户名密码存储到数据库，响应结果给到前端就可以了。

发言人   00:30

那么具体该怎么实现呢？咱们来思考一下，首先用户输入他期待的用户名。那问是他随便输入一个，我们都能注册，都能写到数据库吗？不是，应该，用户名在这儿是不是不能重复？那当然在这儿多说一下，如果是像互联网上的一些网站，有可能是输入手机号是不是做注册。那么手机号能重复吗？按是不是就不能了？所以咱这儿是输入用户名密码做注册，那用户名是不是就不能重复？

发言人   01:06

那既然不能重复，咱应该先干啥？对，是不是应该先检查一下咱数据库里边这个用户名是不是存在。如果它存在，说明重复了，还能注册吗？不能了，对不对？如果存在，咱们抛出异常，当前用户已经存在了，不能重复注册是不是？

发言人   01:27

好，那如果查库发现不存在，咱是不是就可以来创建用户了？那创建用户是不是就是把用户名密码写入到数据库？那咱是不是就是数据库新增数据就可以了？那新增是不是就是ADD就OK。当然在这儿多说一嘴，咱们在新增之前要把密码进行一个加密处理，用户输入123456能不能直接写入到数据库呢？也能，只不过不安全是不是？所以咱们采用一个pcl lab模块，用这个模块做哈希算法加密处理。那加密之后，再去爱的就OK了。

发言人   02:06

当然有了用户了还没完，咱们下一步还要干啥呢？生成访问令牌，就是那个token为什么要做它？因为我们说咱这个界面，是创建完用户直接就登录。另外像个人信息、修改信息，是不是都要验证用户有没有登录过。所以咱们这里创建完用户之后，同步要在数据库里面生成一个访问令牌。

发言人   02:31

那怎么生成呢？在这儿跟大家说一下，咱们采用一个UUID模块生成一个临时的token。那我们把它转成一个字符串来存储到数据库里边就OK了。那前端拿到这个token是正确的，且没有过期，就说明登录了。那我们就让他做进一步操作，否则他就告诉他是不是登录过期了，我们让他重新登录是不是就OK了。那这样的话，咱们创建了用户了也生成了访问令牌了，下一步响应结果给到前端是不是就OK？

发言人   03:01

这个是咱们用户注册的整体流程，用户输入用户名做注册，所以我们就要检查当前这个用户是不是存在，也就是用户名要唯一。那将来如果是输入手机号注册，我们是不是就要校验手机号的唯一性？如果唯一也就是说在库里边不存在，再往下去，数据库新增生成访问品牌响应结果是不是就OK了？

发言人   03:27

这个是咱们用户注册功能的整体思路，各位同学OK吗？还OK的话，那咱们写代码来试一下。提前跟各位同学说一下，咱们这一小节完成检查用户是否存在以及创建用户这里，下一小节再来生成这个token访问令牌。

发言人   03:46

好，那接下来咱们到代码当中来试一下，来回到咱们的拍场当中来。首先找到咱们这个路由，允许我先加一点点小注释。我们要实现这个用户注册，他的逻辑思路大概是什么样的呢？是不是先要验证用户是否存在？如果存在是不是报错？不存在，那我们是不是再让他去创建用户？

发言人   04:13

好，下一步是不是创建用户。创建用户之后，我们要生成这个token，TOKEN，这个访问令牌。最后我们来响应结果是不是就OK？当然这个响应的结果，咱们底下已经有了一个模板，到时候直接改数据是不是就OK了。

发言人   04:32

这个是我们刚刚在PPT里边分析出来的注册逻辑的实现思路，我们把它放在这儿。放在这儿之后，我们把这个思路转代码。我问大家，验证用户是否存在，是不是要查询数据库？创建用户是不是也要操作数据库？包含这个生成token，都要操作数据库。那我们要操作数据库是不是肯定是ORM的方式，所以肯定得先有啥，对模型类是不是？那这个模型类要写的话得参照啥来写？咱这里是不是就参照数据库的表结构来写？

发言人   05:09

那展开在这里跟各位同学介绍一下，这里有一个user就是用户表。我们当创建用户的时候，就是把用户信息写入到user里面。我们可以先来看一下，点开user来看，user name password。在这儿大家来看一下这个dollar 2b dollar什么什么一大串儿。大家好，你说这个是我真的输入的adin的密码吗？不是，他就是用那个part lab模块哈希算法加密之后的结果。那大家想就拿到这个密码，咱是不是也不知道密码到底是啥？那这个安全性是不是就更好一些了？

发言人   05:48

好，再往后还有这个昵称、头像、性别等等等一系列的信息，那这是用户表创建，用户就往user表里边来存。好，那接下来再往右低头来看，在user表底下还有一个user下划线token，这个就是咱们的那个访问令牌存储的位置。那大家来看，我们现在这个user token它是不是空的。

发言人   06:11

因为我们没有创建过用户，也没有登录过，所以没有任何一个用户有token在，有访问令牌在，所以token表是空的。那也就是说待会儿我们创建了用户或者是登录之后，是不是都是写入或者更新到这个token表里边就OK了。当然我们刚刚说这个token访问令牌，咱们下一小节儿再说。

发言人   06:33

那这一小节儿，咱是不是说关注到创建用户这个逻辑就可以，也就是操作user表。那user表里边的字段有很多，我们要是写模型类的话是不是会有好多个属性，那写起来会比较麻烦，浪费时间。我是提前准备好了，咱们直接复制过来。

发言人   06:50

好，回到咱们的文档里面来，找到ORM相关模板代码。在这里我准备好了一个用户表和用户token表的模型类，我们直接复制到拍场当中再给大家去解释。来我复制一下，回到咱们的拍场当中来。各位同学先帮我找一个地儿，把这些代码粘下来，对模型类是不是放到models里面。那我们右键去新建一个python file，我们是用户模块，那我就叫users了，直接CTRLV粘进来。那么粘进来之后，我把这个小插件儿稍微关一下，大家来瞅一眼现在的代码，注意来看一下，咱们现在有了这个user表，对应的table name是user是不是没问题？

发言人   07:38

那我先把它折叠一下，还有一个user token，这个模型类表名字，就是user token是不是跟咱们的那个表名字都是对应起来的那我们稍微展开一下这个user，我们这一小节关注到user这里。那大家来看，这个还记得是啥吗？Index索引是不是？那就企业开发来看，咱们按用户名去搜索用户，或者是按手机号去查询用户，是不是肯定是那种高频发生的查询场景。所以创建索引是不是就不会全表来搜，查询效率是不是就更高一些？

发言人   08:16

好，接下来再往下来看，下边就是对应的属性和这个表字段名。好，那我们来看看主要在这个地方有一个我们不认识的叫optional an optional。这optional是啥？是可选的，就是填也行，不填也行。当然要不要变可选，还是要根据咱们的数据库表结构来。那我们怎么看是否可选？来在这里教给大家一个经验，展开databased插件，找到这个user，那把这个user的注意选中之后，往上看是不是有一个DDL，点开一下，点开一下，点开之后，这个就是咱们user表。所有字段的解释说明，就是定义它的细节要求都在这儿。

发言人   09:05

那接下来大家往右滑一点，注意来看他是不是有的是not now，not now是不是就是不允许为空，但有的像昵称、头像、性别，是不是写的是now，那就是允许为空。像这种有now的，那我们就把它写成optional，可选就可以了。我们可以对照着这个模型类来看一下，来看那个name是不是可选的，头像性别，然后这个是简介、手机号等等，是不是都有optional，那就是可选。这个大家记着是不是对应着咱们数据库的DDL来的，这个大家知道一下。

发言人   09:45

好，现在咱们的模型类我直接复制过来了，大家也可以直接复制过来。如果大家时间充裕，想练一练这个ORM模型类的写法，可以单独私下去练习。好，在这里我为了节省时间快速进入到用户注册的功能，我就直接偷个懒儿复制过来了。好，那我把这个models先关掉，user这个表也先关掉了。那下一步各位同学我们是不是就检查用户是否存在，创建用户是不是就OK了。当然这属于数据库的CRUD，咱是不是要给它封装CRUD方法，再从路由处理函数当中来调用。

发言人   10:26

好，那接下来又该同学们了，帮我找地儿呗。对，CRUD里面是不是我们去新建一个python file，那我们还是叫user solo。那我们是不是要封装两个方法，一个是根据用户名来查库的。一个是创建用户的，也就是数据库I的新增的，对不对？好，接下来我先打一个注释，我们要根据用户名去查询数据库。好，那接下来我们定义方玩儿，我来一步了，DEF，这个名字还可以，我直接tab一下。

发言人   11:05

大家来看get user是不是获取用户by user name，是不是就是根据用户名来获取用户的意思。那往后我们需要的参数，user name是不是肯定得要着，DB是不是也肯定得要着。当然我习惯这么写，因为咱的DB是不是肯定要传。但传的这个关于用户相关的数据有可能带有默认值，所以我就一律把DB放在参数一的位置，用户的数据放到参数二的位置了，我稍微调一下，这样咱们整体风格是统一的。

发言人   11:37

好，那允许我剪切一下，把这个逗号干掉，然后接下来逗号隔开一下，control v粘贴，我把user name放后边来了。好，那当然这个think session咱是不是要给它导入一下？我把这个注释稍微往下摞一摞，来control v一下，把这个往上挪。

发言人   11:58

好，那接下来再来我们是不是要查询数据库，那select肯定没问题，那我们把它import导入circle alchemy里边的select，那user是不是咱的模型类，那我们也要导入一下，到哪里的呢？Models里边的user是不是就OK了，那我们点一下。好，select user是不是全导进来了？好，那接下来往后，where是不是条件user name等于参数user name，那这句查询语句是不是没问题？那接下来result result就是excute执行的结果。接下来return return的是scala one or nine，是不是也没问题？

发言人   12:40

因为查表有可能有这个用户，有可能没有，所以我们是不是用scala one or nine去提取结果。如果真的有用户，返回的就是一个用户。如果没有用户，是不是就是now了。那我们将来在路由里边是不是就可以写判断，如果没有用户就rise，抛出异常是不是就OK了。

发言人   13:02

好，那现在咱们根据用户名查询数据库的函数是不是就已经封装完了。那下一步咱是不是就得去封装创建用户的方法了？好，那我打一个注释，咱们要创建创建用户，那我们来一个a think，OK没问题，create user是不是OK搞定，那DB也是我们要的数据库绘画依赖项这个参数。好，那下一步用户名密码。这个用户名密码，我直接用咱们之前的python单体个类型了，那我就这么写，把user整个的data数据给它传过来，然后我tab一下。User request是咱们之前定义在skies里面的那接下来我们把它导入一下，impart一下。好，往上来检查是不是从schemers user里边导入user request。

发言人   13:53

好，那我们的函数定义好了，那下一步我们来写功能。这个功能允许我先打一个注释，我们首先大家思考一下，能不能上来就把这个user data里边的用户名密码写入到数据库，还不能是不是？应该是先密码加密，加密处理一下，然后在AADD新增是不是就OK了。那么添加咱们肯定会，但密码加密处理咱是不是还不会？那没事了，咱们来讲义当中找答案走。

发言人   14:30

首先我们要做密码加密处理，咱可以借助一个模块叫pass lab。想要用模块，咱需要先安装一下盘slab。安装之后，咱肯定要导入创建加密上下文。那么依赖于加密上下文调用哈希方法来进行加密，very fine方法来校验。比如说明文跟密文的这个密码是否是一致的。那像这个哈希它是把明文转加密，那我们在注册的时候用，那verify是校验明文跟密文是不是一致的那这个wifi就是登录的时候用，那也就意味着咱们pass lab这个密码加密处理的这些功能，咱注册，登录，最起码这两个接口是不是都要用了。那咱们这么着，把它抽取封装一下，那哪个接口要用，直接调用是不是就OK了？

发言人   15:27

那咱们来试试这个pass lab模块做密码加密处理。我们说首先安装PIP in store pass lab，有依赖b crept，这个是一个依赖，它指代的是哈希加密算法。这个b crept它是依赖于blow fish这个加密算法改进而来的。那我们安装pass lab携带依赖，并且最好指定一个版本。这个版本是官方推荐的稳定版本，那我们就尽可能稳定着来。这个命令我已经提前在咱们的素材文档里边准备好了，咱们直接来到文档，找到02 pass lab加密来看安装包在这儿了，那我直接来一个control c复制，包含使用方法，我是不是也都把代码贴出来了，我们可以直接来复制。好，那回到咱们的pad time当中来找到终端，咱们control v粘贴一下。接下来敲回车，稍等一会儿，联网安装来看successfully是不是安装完了。

发言人   16:29

那安装完之后，下一步咱回到PPT里边来，是不是创建加密上下文，那怎么创建依赖于crept context，那注意，它有两个参数，参数一是加密算法，咱用的加密算法是不是就是这个b crept。第二个参数值是凹凸，它指代的是新旧加密算法，可以自动兼容，自动适应。那有了这个加密上下文了，下一步咱们是注册，是不是把用户输入的名为转加密。所以基于加密上下文点儿哈希，把我们的明文密码传进来，就能得到一个加密的结果了。那这些代码，我们说多接口可能会共同使用，所以咱们就抽取一个工具函数了，那回到咱们的pyx m当中来，抽取工具函数来有一个文件夹告诉我是谁啊。对，youtube工具函数，那我们右键新建一个python file。我们起个名字，咱这个加密是不是相当于是安全相关的那我起个名字，比如说我就叫security，安全、授权，那怎么拼？SECURITYRITY，直接回车，那我们是不是得先倒包倒进来，那注意，phorm pass lab点context import的谁呢？大写的creep context。

发言人   17:56

好，那下一步咱是不是要创建加密上下？那么创建加密上下文，就是依赖于我们导进来的crept context。那我先tab一下。首先加密算法，还有就是新旧加密算法都是自适应的，可以兼容。那前边咱们这个上下文这个变量名字我稍微改一下，比如说我就叫密码context，密码上下文XT好，那我们的密码加密上下文是不是搞定了？容我打一个注，是啊这是创建密码上下文。

发言人   18:32

那有了这个上下文了，我们是不是说可以调用哈希这个方法来把明文转密文？好，那我打一个注释，接下来我们就是做密码加密功能。我定义一个方法，DEF哈希password OK但我这样我加一个get，是不是就是获得哈希密码。那将来调用的话，把用户输入的明文密码传进来，那我们直接return基于加密上下文password context调用哈希方法传入用户的明文密码，就能得到一个按哈希加密之后的密码了。那我们return是不是就return到函数调用的位置了？那这个密码加密处理的这个小方法，我们是不是就定义完了，未来在需要的位置直接导入来调用这个方法，是不是就OK了。

发言人   19:21

那当然咱们在哪儿调用呢？是不是刚才咱们的create user这里是不是先加密处理，那我们就可以调用了，当然得先导入，是不是？那么from IU tus utils，接下来import直接导入security了。好，那接下来我们做加密处理，是不是就是security点儿，get哈希password，那我们是不是就是把这个user data里面的password给它传进来？好，那接下来我缩进一下user data里面的password。那这一句子弹执行完，我是不是就得到加密的密码了？那我给他存一下哈希，我来个ED，是不是就是已经哈希处理过的，pass word，等号赋值。

发言人   20:09

那有了加密之后的密码了，下一步我们是不是就可以按创建用写入到数据库了，那我就直接这么来了，首先我来一个user，那用模型类来创建user。User name就是用户输入的用户名。Password是不是就是我们哈希处理之后的密码。那接下来我们是不是把这个user艾特到数据库，是不是就OK了。那我们就DB点ADD user，当然ADD之后是不是得提交，那提交之后才写入到数据库，那按照常理，我们是不是return一下这个user就可以了。

发言人   20:48

蛋蛋，大家刚刚有没有注意到一个细节，就是我们的通义灵码插件。我在这之后一敲回车，大家来看它提示我一个啥，refresh，refresh这单词是不是好像刷新的意思？那这一句是啥意思？我说一下，它是从从数据库读回读回读读读回最新的这个user为什么需要这一句，我说一下。

发言人   21:18

我们刚刚把user用户名密码写入到数据库，是不是数据库里边的所有字段都要更新了？如果涉及到这个字段，本来没有值，现在重新新增了值的，我们最好refresh一下，确保我们拿到的就是已经写入到数据库里边最新的这个数据。所以以后咱们遇到这种情况也refresh一下。

发言人   21:42

好，那refresh之后，下一步我们就可以return了。那return啥呢？就是把咱们这个user给它返回是不是就可以了。

发言人   21:50

那一会儿咱们在路由当中是不是就可以基于这个user去响应回给用户，比如说用户名、密码、简介、手机号等等关键信息了。那现在各位同学，咱们的创建用户这个功能是不是也搞定了？先哈希加密，然后创建这个user对象。提交到数据库是不是就OK？当然咱们这儿学了个新知识，字段更新了，原来没有值，现在有值。为了确保真的写入到数据库，咱们要读数据库最新的user做返回，所以refresh一下。

发言人   22:25

好，那现在咱们CRUD的方法是不是就已经封装完了？那允许我把这两个方法截一个屏，主要就是参数的顺序。这里来我截一个屏给它钉到咱们屏幕的右下角，稍微的缩小这么一点点。

发言人   22:41

好，那函数封装完了，下一步咱是不是去root里边来调用就好。首先先调用验证用户是否存在的那是哪个方法？Get user by user name是不是？好，那接下来我们得先导入一下，我就说from咱们的这个CRUDI import user是不是就OK了？

发言人   23:07

好，接下来我们要调用查询用户是否存在的那是不是就是users里面的点儿get user by user name，那我们要传入DB还要传入啥？是不是要传入一个用户名？那我们就依赖于参数，请求体参数。User data是不是可以访问到user name？好，那就是user data里面的user name。当然这是数据库查询，咱是不是await异步操作。好，那接下来咱们来个小变量把它存储一下。Exciting NEXISTING下划线user，把它存一下。

发言人   23:48

好，那这个exciting user里面是不是有可能有用户，有可能没有用户？如果要是有了是不是就得报错？有是不是就说明这个用户名在数据库里边已经存过了，用户名已经存在了。好，那接下来我写个if判断，if exciting user，冒号一下咱们要干啥？是不是rise抛异常，那我就HTTP exception了，咱们把它import导进来，导入fast API里边的HTTP exception。那么这个status code状态码，那我们在fast API里边有一个status STATUS，来我type一下，HTTP400 bad request。好，那我把它导一下，注意in part好，导进来之后状态码搞定了，detail用户已经存在，那我就直接这么着了，当然如果在调用get user by name的时候，我们得到的是now，是不是就不会进这个if那是now了，我们是不是就说明这个用户名在数据库里边没有，咱是不是就能正常添加了？

发言人   24:58

好，那接下来我们往下走创建用户的流程，那创建用户是不是就是调用user里边的create user，就是咱们第二个创建用户这个方法。同理，传入数据库绘画依赖项传入请求体参数user data是不是就是用户名和密码了？那这个方法大家还记得吗？是不是return了一个从数据库里边读回的最新的这个用户，那我们把它接一下，在这里比如说我叫user把它存一下，那存完之后，下一步干啥？注意这个生成token的方法，咱是不是还没有封装，咱说下一小节再做，那我们跳过一下，暂时假装它不存在，下一步是不是就可以响应结果了。

发言人   25:43

那响应结果，咱们这里能用到的ID是不是得换换用户名、简介头像是不是得换一下？好，那我们就都依赖于这个user来设置，是不是就OK了。好，那这个ID我们写一下是不是就是user的ID，接下来user name这里也是一样，我们先把原来的这个测试的给干掉了，还是依赖于user，这是数据库里边读回的user点user name，那这个BIO是简介，那还是依赖于user这个对象，找到BIO简介，再往下就是这个头像了。那头像干掉基于user点阿瓦塔来查找用户头像就OK了。

发言人   26:26

各位同学，现在咱们验证用户是否存在，创建用户这两步小操作是不是已经完成了。那咱们来试一下O不OK。那我们回到浏览器里边来找到交互式文档。

发言人   26:39

接下来咱们来刷新一下，register注册try IT1下。比如说我来一个大秋，密码123456，来一个XQ的提交来看一下。大邱这个人很懒，什么都没有留下默认的头像ID是不是全都OK，这个访问令牌咱还没有做。好，那现在是我的数据库里边是不是没有大邱这个账户，我是第一次注册OK，是不是注册成功了。但假设我要再注册一次，来再提交一次，走你我们看看他干啥了，用户是不是已经存在了。那说明咱大邱这个账户是不是已经写到了那个数据库user表里边了。咱们回到py tom里边来检查一下，来找到database，找到user，来看大邱是不是已经进来了，而且密码还是加密的。当然账户创建完了，咱这个token是不是没管，所以这个token注意来看，还是空的，咱们下一小节儿完成token，那我们不着急，这一小节儿咱们能新增用户到数据库表里边了。

发言人   27:49

那我们来总结提炼一下这一小节的重点，来回到咱们的rotor里边来。首先大家回忆咱们整个注册的功能实现思路，是不是得验证用户的唯一性？当然我在这儿多问一嘴，开发当中永远都是验证用户名唯一吗？那不一定。像我们有些用手机号注册的，是不是验证手机号唯一？用邮箱注册的那就验证邮箱唯一。好，那接下来如果是查询用户发现数据库里边没有这个用户，那我们是不是就新增用户，然后生成token。当然咱这一步没做接下来响应结果是不是就OK了。至于咱们这个CRUD的方法，相信大家比如说查询用户select，这个新增爱的大家肯定没问题。

发言人   28:37

但这一节我们涉及到了新知识点，大家还记得吗？对part lab是不是密码加密？来我们看看，首先是不是安装这个模块，安装完之后创建密码加密上下文，依赖于这个加密上下文调用哈希方法，是不是把用户输入的明文转加密。我们说这个哈希，它是注册的时候要用的。咱们PPT里边说是不是还有一个verify，那个是为了验证密码明文跟密文是否一致，是不是？那是登录的时候用的那那个wifi方法，咱们等讲到登录的时候再来这儿来封装。好，这个就是咱们刚刚完成的注册里面的一小部分功能，验证用户是不是存在创建用户这两个步骤。

发言人   29:23

接下来就该同学们的表演时间了。大家也是自己动手，依赖于自己的项目，把注册的前两步完成。当然这里边咱们的密码加密的功能是新的，大家可以记好笔记，方便后续项目来重复使用。好，接下来时间交给同学来动手，把自己的项目实现到创建用户这一步。创建完用户，大家不要走开，下一小节咱们就来生成token，来给用户创建一个访问令牌，我们下一小节儿不见不散。