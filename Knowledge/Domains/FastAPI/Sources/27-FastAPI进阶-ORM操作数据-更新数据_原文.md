27-FastAPI进阶-ORM操作数据-更新数据_原文

2026年07月21日 12:50

发言人   00:00

Hello, 各位同学，这一节咱们来学习ORM操作数据库的更新数据的功能。所谓的更新其实就是修改了，那该怎么做呢？注意想要更新修改，先查找再来修改。找到了那我们就重新赋值来修改就可以了。没找到是不是要报错给用户就行。

发言人   00:23

好，那具体该怎么做呢？注意，刚才我们说了是不是要先查再改，那我们可以用select查询或者get查询都行。好，那找到了之后，把属性重新赋值，最后commit提交到数据库就可以了。这个是大致的实现思路。

发言人   00:39

接下来咱们举个例子来看一下。比如说我要封装一个接口，那这个接口提供修改图书信息等功能。那我们在这里设计一个路径参数，代表的是将来用户要改的这一本书的ID那我们是不是就可以通过get通过这个主键值ID值去查找这本书。如果没有找到rise抛出异常就行了。但是如果要找到了，我们说是不是要修改，修改是不是就是把属性重新赋值就行了。

发言人   01:09

那么负的值是啥呢？来往上看啊，我们准备一个请求体参数，这个参数里边就包含着用户输入的，比如说书名、作者、出版社等等等等信息。那我们找到了这本书了，就把属性重新复制成用户输入的那下一步提交到数据库是不是就可以了？这个是咱们更新操作的实现方法。

发言人   01:33

了解了实现方法了，接下来咱们也来试一下。回到咱们的拍场当中来，我有一个基础的路由，注意这里我用的是put，put是不是代表修改更新的意思？那提供的路径是book下的update a book。那现在咱们先来明确一下需求，来我打个注释，需求是不是就是修改图书的信息题？当然我们说修改是不是得先查再改，对不对？先查再改，那咱们怎么去设计这个需求呢？来小哥回车。

发言人   02:08

那我们的设计思路设计思路，我是不是接口要提供一个路径参数，就是这个书籍的ID是不是方便我们去查找？这个它的作用是查找，查找。那我们说要找到了是不是得改？那要改成啥呢？是不是用户提供要修改的数据，对不对？所以在这里我们再来一个请求体参数。那么这个请求体参数它的作用作用是啊是不是那个新的数据新的数据新的数据。那咱们这个新的数据我稍微规范一下，比如说ID咱们不需要用户输入，比如说书名是不是用户输入，作者用户输入价格他也能改，出版社是不是也能改，那咱就改这四个是不是就可以了。

发言人   02:57

好，这个需求跟大致的设计思路，各位同学还OK吗？OK的话我们往下了，那我们来一下注意。首先路径这里我们说是不是要携带一个路径参数是书籍的ID，那我们就得是不是斜杠下大括号一下。比如说书的ID我就叫book ID了。好，路径参数是不是得在处理函数这里来一个同名的形参，那我给它粘过来，冒号是不是int类型，逗号隔开一下。

发言人   03:28

好，那第一个路径参数搞定了，下一个是不是用户要改，那你改成啥？这个新的数据是不是要包含在请求体参数里面来？好，那我来一个请求体参数，比如说我就叫data了，是不是用户的数据，那冒号是不是也得写一个类型？

发言人   03:44

好，那这个类型咱现在有吗？还没有，是不是还得包含噔噔噔噔，是不是这四个？那咋办？定义类是不是定义python dente类型，在这儿用上这个类型是不是就可以了？好，那python单体和类型咱是不是得先给它导包导进来？好，那我们往上走，往上走，f ROM f ROM python tic import，base model这个类。好，那有base model了，下一步咱们的类型是不是就可以写了？

发言人   04:17

好，那叫个回车，回车，咳class，比如说我就叫book，book咱们是修改的那就update一下。那小括号要继承的是不是这个贝斯model才可以？好，那冒号一下，那我们说要修改的数据是不是有这四条，那咱就来呗，不可name是不是应该是死定类型？

发言人   04:43

作者AUTEHOR作者是不是应该也是string类型？价格Price Price是不是float，浮点型。再有出版社PUBLISHER也是string类型，是不是就OK了？那我们这个更新的这个类型是不是搞定了？那接下来这个data这里是不是换成book update就OK了。

发言人   05:07

好，那现在参数搞定了，咱是不是该写逻辑了？那这个逻辑各位同学咱们要先查是不是再改。那么查询的话，我们是不是可以通过get，select是不是都行？因为我们这里提供的是不是主键值，那就直接get了。好，那我await一下DB点get模型类似book查找的值是不是就是我这个参数book ID呢？那这一句get执行完，是不是会得到一个结果，那我给它存一下，比如说我就叫DB杠book book把它存一下。

发言人   05:42

那问大家这个DB book它一定有书吗？不一定是不是，他有可能有有可能没有。那我们说查到了就改，没查到是不是得报错提示一下用户。

发言人   05:54

好，那我先处理一下这个没查到的情况。If DB book is没有没查到，那它是no is no，那怎么办呢？我们就rise抛出一个异常，这个异常各位同学还记不记得咱们在fast API里边有一个HTTP exception，是不是异常响应？那接下来我们把它导进来，往上走找到fast API，from more fast API它有一个HTTP exception。好，那导进来之后，咱们往底下来，往底下那rise抛异常的位置，咱就用HTTP exception是不是就可以了。那它是不是有俩参数，一个statues code必须要写，那没找到咱就来个404，是不是就是代表未找到的意思？好，还有一个就是detail，detail是不是详情咱尽量也写。因为将来响应给用户的内容是不是就是detail的内容了，我就写，查无此书。好OK好，那现在是不是就是去查找这本书，没有找到是不是就报错提示用户。但是如果找到了，咱们是不是就要修改了？

发言人   07:08

所谓的修改，各位同学咱说了，这里其实是不是就是重新赋值就可以了，那怎么做？比如说DB book它是不是有book name，那我要赋值为用户最新输入的books name是不是就可以了，那就是data里面的book name。好，那其他的是不是也是一样的道理了？DB点儿比如说author作者它就等于data点儿，answer作者。再有查找到的这本书它的价格，咱们也给它改成用户最新输入的价格。最后还有一个出版社，DB点儿，DB book点populist出版社等于用户最新输入的出版社，是不是就可以了？当然在这儿，咱们也可以做成，是如果data点book name有值，再把它走重新赋值这一句就是再写一个if是不是也行，那我这儿就不写了，就直接重新赋值了。

发言人   08:11

好，那问大家赋值了，数据库改没改？没有是不是得commit？那我await一下，await DB点儿commit是不是提交？那提交之后，我们是不是要return响应一个结果给用户，那咱也不响应多复杂，我就直接响应这本书的信息给用户，咱们看一下有没有修改成功，是不是就OK了。好，那现在咱们的修改的接口功能已经封装完了，我们保存一下，接下来到浏览器里边去验证一下。

发言人   08:43

来，我们打开浏览器，接下来刷新一下，注意来看这里是不是有一个up data book，那展开try IT1下。那ID比如说咱们就改，比如说ID为七的这是不是咱们上一节作者是大邱的那本书？来那么修改它，接下来要改成啥呢？书名，比如说这真的是一本特别特别好，哎呀呀特别好。我的书你上一节咱们加的时候，是不是这是一本好书。好，作者是大大邱，大大秋。价格比如说这回变88，涨价了是不是？出版社咱们黑马出版社。

发言人   09:30

好，那接下来exclude提交一下。我们往下来看来看ID为七的作者便大打球价格书名是不是都改成功了。那我们现在看到了响应结果了，接下来回到咱们的拍场品当中，来，打开咱们的book表来验证一下。来打开book来看看，这是一本特别好的书。大大球价格88，是不是OK没问题。那现在我们的更新操作是不是就完活儿了？那怎么做的？注意来往上来看是不是先查再改。

发言人   10:02

那怎么查？我们这里是不是设计了一个路径参数是book ID它是主键，所以是不是get的方式去查找。那在这儿咱们加一个注释，这里是不是咱们的第一步查找图书，查找图书。好，那找到之后有一个结果，它一定有书吗？是不是不一定？那我在这儿打一个注释，如果如果未找到未找到是不是就得抛出异常，抛出异常。

发言人   10:33

好，那如果找到了，那在这儿是不是就已经修改了？好，那在这里我打一个注释，这是咱们的第二步，找到了则是不是修改就可以了。那这里的修改其实就是属性重新赋值就可以。

发言人   10:51

好，那改完了，数据库里边并没有真的变，那咱是不是得commit提交一下才行？好，那这是第三步，是不是咱们去提交到数据库？提交到数据库就OK了。这个是咱们数据库操作更新数据的做法。接下来是不是该同学们来实现了，大家来写代码，来更新一下自己数据库表里面的数据，更新完之后，大家把答案粘贴到评论区，咱们老规矩，评论区核对答案。