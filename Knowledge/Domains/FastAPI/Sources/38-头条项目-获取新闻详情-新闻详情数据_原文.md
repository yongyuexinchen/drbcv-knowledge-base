38-头条项目-获取新闻详情-新闻详情数据_原文

2026年07月21日 12:49

发言人   00:01

Hello, 各位同学，上一节咱们获取了新闻列表了，那这一节咱们来开发获取新闻详情接口。有了这个接口，当点击新闻跳转到详情页的时候，这个界面就不是空的了，就有展示的数据了。具体展示的数据包含新闻的标题、作者、发布时间、浏览量，新闻的详细内容。但是往最下看啊，还有一个相关推荐，这个相关推荐就是同类的其他新闻了。这个是详情接口的大致功能。那具体的功能咱们是不是得拿着这个接口规范文档来分析一下来我们来到接口规范文档，首先从最上看接口地址，这里请求方法是啊get地址，我们直接写detail详情是不是就可以了。好，那再往下请求参数，请求参数就一个是ID代表的是新闻ID因为我们要从数据库里边查找某一个各新闻的详细内容，那我们就按主键ID去搜索是不是就可以了。

发言人   01:05

好，有了参数了，下一步就是响应结果。那这个响应结果仍然包含code message data，data是不是咱们界面要用到的真实数据？这个数据上半部分是新闻的关键数据，我这儿用省略号省略了好几个字段。因为这个数据太多了，PPT里边盛不下了，我就省略了一些，一会儿全量的，咱们到马克档文档里边去看啊。

发言人   01:34

好，有了新闻的关键数据还没完，还有一个views是浏览量。我们每条新闻每看一次浏览量是不是就要加一个，所以我们这里要把浏览量也返回。好，再下一个related news，这个就是相关推荐。我们推荐的肯定不止一条新闻，所以用一个列表来保存，这个是咱们接口的要求，根据这个要求是不是可以推导出基础路由的写法？那就是模块化路由写get方法，路径是detail。接下来定义方法传一个ID是不是就可以了，响应结果就OK。好，那这个路由相信同学们是不是肯定没问题。好，那接下来咱们这样到markdown接口规范文档里边看一下全量的这个响应结果。

发言人   02:24

来我们来到markdown里边来。首先从左边我们找到新闻模块，找到获取新闻详情来看是不是接口地址参数是不是都在这儿。重点看响应的结果，响应结果有ID新闻标题、内容、图片作者、发布时间、分类、ID views和related news。刚刚在PPT里边是不是见过了？好，这个是我们要的全量响应结果。接下来我一不做二不休，直接全选复制。

发言人   02:56

我们来到拍场王好，首先我们要写路由，是不是往routers里面写，咱们仍然是新闻模块，那就还往news点PY里边写是不是就可以了。好，那接下来我们往下来艾特root一下，是不是点get一下，那个路径是啊detail DETAIL。好，那接下来写方法，那我们a think一下，DEF小助手又上线了，是不是？那我直接tab一下。好，那检查get news detail没问题，那我们要的参数是不是news ID还需要数据库绘画依赖项也没问题，那底下的代码我们直接给它干掉，我们是不是先写死个人响应结果？

发言人   03:43

好，那我先来一个return，我们粘贴一下从markdown里边复制过来的这个响应结果，那格式有点不太正常，那我们缩进一下，来选中tab一下。好，在这里另外有俩报错，就是这个now是不是找不着？那没关系，我们现在先他打一个字符串，这样它不报错就可以了。那具体的数据查完数据库再替换是不是就可以了。好，那现在我们的return响应结果是不是搞定了？当然网上还没完，咱们把这个参数要稍微整理一下。那我这里是不是有一个news ID，news ID的话我们给它起个别名，因为我们需要的前后端约定好的是不是就是ID，所以我来一个query，然后这个query里面必填要着他alliance别名写一个ID，那么现在咱们的基础路由是不是就已经搞定了？

发言人   04:35

那我们来到这个交互式文档里边来测试一下。首先我们刷新一下，来看detail是不是已经有了。那接下来我们try IT1下，比如说我来一个随便来一个ID，来excute来看看响应的固定的结果是不是已经拿到了。

发言人   04:51

那基础路由搞定了，按我们之前的接口实现流程是不是该到模型类了。这里要问大家，我们获取取的是新闻详情这个模型类还用不用再定义？不用是不是还是咱们原来的news那个模型类就可以了。这个news回到pym里边，咱是不是早就定义好了，包含新闻ID、标题、描述内容等等一系列的字段是不是都在这儿了。所以我们的新闻详情是不是不需要再写模型类了，那我把我这个model就关掉了。好，那模型类是不是我们直接略过就可以？

发言人   05:28

那下一步是不是就得是增删改查操作数据库里边的数据响应结果了。那么这个操作数据的流程是啥呢？来咱们回到PPT里边来分析一下。

发言人   05:39

来到PPT首先我们要获取新闻详情，我们是不是要获取当前新闻的详细内容？另外每看一次这个新闻的详细内容，浏览量是不是就要加一个，还要获取是不是相关新闻。啥叫相关的？就是同类的。当然我们点的是新闻一，是不是找到这个新闻的同类？

发言人   06:06

好，那具体该怎么做呢？咱们来分析一下，首先一旦进入请求，我们是不是携带了一个新闻ID这个参数。那有了这个参数之后就好办了，我们直接按ID来查新闻是不是就可以了。那查新闻是不是就是select带个条件，news ID等于我们指定的新闻ID是不是就OK了。那这样的话，咱们这一条新闻的详细内容是不是就能查出来了？

发言人   06:34

好，那查出来之后，下一步还没完，我们是不是得操作浏览量加一个，那怎么做呢？这里是不是涉及到数据库的修改了？好，那接下来浏览量加1，这里给同学们扩展一个数据库修改的方法叫update。Update要更新，那当然更新的时候加上一个where，是不是你得找到这条新闻再去更新。好，那接下来values就是指定更新的值，那怎么指定呢？字段等于值就可以了，这个是浏览量加一个的操作。

发言人   07:12

好，有了浏览量了还没完，我们是不是还需要相关的其他新闻，就是同类的其他新闻，那怎么做呢？获取相关其他新闻还是一个select，只不过where条件咱要写一写。我们说什么叫相关，就是同类的，对不对？那所谓同类是不是就是看看表里面谁的分类ID跟咱们这条新闻的分类ID是一样的。那查询条件是不是就是news里边的character，等于我们当前这条新闻的分类ID，那这个是同类，那当然光指定同类还不行。

发言人   07:50

我们现在假如说看的是啊第十条新闻，你还能推荐第十条新闻给我吗？不能了，对不对？所以是不是得排除当前这一个，所以就是news表里边的ID不等于咱们这一条新闻的ID的。那么这两个条件同时满足，就是相关新闻同类的其他新闻。

发言人   08:14

那现在咱们是不是就把当前这条新闻的内容浏览量加一相关其他新闻，是不是这三个数据全都集齐了，那集齐之后是不是就可以响应结果给前端了？这个是咱们获取新闻详情这个接口大致的实现思路。那咱就从第一个就是按ID来查找新闻开始实现。当然在这儿跟各位同学说一下，咱们这一小节能够实现按ID查询新闻，就可以流览量加一跟获取同类其他新闻。这两个咱们留到下一小节儿做，因为在这里面我有新的知识点要给大家讲。

发言人   08:54

好，那接下来咱们就按ID来查找新闻。我们来到拍场当中来，首先咱要写的是数据库查询，那是不是得找到CRUD的news来写代码？那双击打开，那打开之后我们就顺序往下来了。首先数据库操作咱们肯定是异步的那我来一个a think，定一个方法，小助手上线我直接tab一下，get news detail。那么需要的参数有DB是不是数据库绘画依赖项，还有一个news ID是不是就是新闻ID？

发言人   09:30

好，接下来查找select news，模型类是news是不是没问题？Where条件news ID等于用户传进来的news ID那么这条语句我们数据库excuse执行一下，那执行完之后，是不是就能return结果了？那么这个return，我用scala one or nine就有可能是不是查不到？查到就返回这条新闻查不到是不是就是now当然同学们可能说我们是点一点新闻过来的，难道还能查不到吗？其实我们这里就是把功能想的更复杂一点。万一在界面当中哪个地方我们要用到搜索，这个搜索需要让用户输入，那用户输入咱控制不了，万一他输入一条不存在的ID咱是不是就有可能拿不到新闻，那咱就死掉了。玩二难，能查到就返回新闻，查不到是不是就是now就可以了。

发言人   10:27

那现在咱们获取新闻详情这个数据库操作的方法是不是已经封装完了？那允许我把它截一个屏，钉在这里。那钉在这儿之后，这个方法各位同学在哪里调用？回到reuters里面来，那么我们来敲一个回车，我在这儿先打一个注释，我们是不是得获取新闻详情加浏览量加1，还要加上啥呢？就是相关新闻。

发言人   10:59

好，那接下来我们调用方法把这个详情先给它拿过来。我直接tab一下news里边的get news detail传进去数据库绘画依赖项以及news ID保存到news detail里面。当然我们光保存还不行，响应的这个结果是不是得用news ID里边的标题内容啥的那所以咱们得把data里边的数据稍微改一改，这里就是news detail的ID，底下这个是title标题，那就是news detail里边的title。那我快进一下。那现在我们的数据就已经填充完了，接下来咱们到浏览器里边来试一下。

发言人   11:44

那我回到咱们的浏览器刷新一下，那我们找到detail，detail try IT1下，我们来一个ID，比如说XQ的执行，那往下来看看新闻的内容是不是就已经查出来了。当然我这里是输入的新闻ID是一，是不是真的有ID为一的这条新闻，所以是不是能查到新闻？但假设有一个地方要让用户输入ID去查询。比如说用户手一抖输了个1万，不瞒大家说，数据库里边没有ID为一万的新闻。那么执行来看看，直接服务器报错500开头的是不是服务器的错误。那这是不是坏菜了？本质上它不是咱们错了，是不是数据库里边没有ID为一万的新闻，那这种情况怎么办呢？来，我有解决办法，回到咱们的排场里边，找到路由，就是这里造成报错的原因，是不是这个news detail有可能拿不到新闻数据，那如果拿不到，我们就告诉一下用户当前新闻不存在是不是就可以了。

发言人   12:56

那怎么办？来加个if判断，if not news detail，那还没完，我们不是return，那我直接rise抛出异常。大家还记得这个fast API里边有一个HTTP exception吗？抛出异常的是不是？好，那我把它导包导进来，从fast API里边导入HTTP exception。Statues code 404是不是？那查不到就404是不是没问题？Detail详情是新闻不存在。

发言人   13:28

好，那接下来当我们加了这个判断之后，再来试一下，回到浏览器，那仍然注意，还是一万这个ID execute来看，还报错吗？不报错。此时在前端界面是不是就可以看到，新闻不存在，是不是不再是咱服务器的错误了？好，那这是查不到新闻的情况。当然假设说比如说我来一个IDV2的来excuse一下来看看，有这个ID所以是不是能查到这个ID对应的新闻？

发言人   14:02

好，这个是咱们获取新闻详情这个数据的实现流程。为了让功能尽可能的完美，我们在查的时候有数据，是不是死掉了one or nine就能返回这条新闻。如果没有，它返回的是不是就是now？它返回now不要紧，那在我们的reuters里面，一旦这里是now，是不是没有办法继续点ID点title了，是不是报错就是服务器的错误了？为了让功能完善，我们加一个意符判断，如果没有新闻是不是rise抛出异常就可以了。

发言人   14:39

这个是咱们获取新闻详细内容的实现流程。接下来各位同学也来动手，先把这个新闻详细内容这个数据给他获取过来。下一小节咱们再来做浏览量加一跟获取相关的其他新闻，下一小节儿咱们不见不散。