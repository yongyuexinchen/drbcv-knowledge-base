ASGI协议和服务

ASGI协议和服务_原文

2026年07月22日 14:58

发言人   00:00

好，各位小伙伴们大家好，我是老肖。接下来我们继续回到这个fast的API相关概念上。讲第二点概念就是ASGI协议和服务。ASGI要和这个WSGI要一起讲，首先说一下这个WSGI，WSGI叫web server get away interprets，翻译中文叫web服务器的网关接口。这是干嘛的这其实就是一个同学协议，好吧，它是主要是是把把我们的外部应用程序和我们的这个index之间进行通信的一种协议和规范。那么ASGI也是一样的，也是是把我们的外部应用程序和NGX外部服务器之间进行通信的一种规范。ASGI了它叫signalized，server signal sovs get away interface，翻译中文的就叫一步网关协议接口。你发现和这个WS间有什么区别，就多了个一步，是不是？

发言人   01:01

一步那底层是怎么实现的呢？就用python里面的携程多进程以及携程来实现的。当然还有线程，携程一定是要在线程里面的，如果没有线程就不可能有携程。之前我们讲这个python的高级编程中是讲过的。好，那么是一个基于网络协议服务和pya应用程序之间的一个标准接口。你看这也告诉你了，其实这句话翻换一句话，其实是一个介于外部服务器与pya应用程序之间的一个标准结构。

发言人   01:31

OK它能够处理多种通用的协议类型，包括HTTP以及HTTPR还有这个web socket当前运行在ASGI协议上的外部框架了，有fast API以及讲狗3.2之后，各位如果你还是使用这个I的话，是不支持ASGI这样的通讯协议的。OK, 那我为什么要提这两个概念呢？是因为fast的API它默认就是ASGI的。因为fast API它的定义就是什么叫异步外部框架，所以它的通信协议也必须是异步网关协议接口，或者叫异步网络，异步网关接口，那就是这个ASGIASGI了。

发言人   02:21

这个通信协议主要是谁和谁之间的一个通信呢？给大家说一下，你看这首先我们看中间这个叫web服务器，那web服务器的一般是什么呢？一般像是index是吧？Inx是我们目前为止企业里面用最多的一个外部服务器。好吧，这个inx都在我们的课程中也有大家有兴趣的话，你要是不了解，你可以先去看一下。

发言人   02:43

好，index它里面又放了这个静态的服务，也放也是作为一个非常主流的web服务器，也它同时也是作为文件静态文件服务器，那么浏览器发送请求，首先请求这个index，然后index发现N这个请求是请求数据的。好，那请求数据的那交给后端接口去得到数据。因为我们前面说过，我们的后端的API接口就是负责返回给客户端返回数据的对吧？好，那他就把这个请求转给我们的这个web应用服务器，这个web应用服务器里头就是使用这个fast API框架写的一个web项目。

发言人   03:28

好，那么接下来就需要把index和web应用程序之间要实现一个通信，要不要？要不然你怎么你你怎么去请求我这个外部应用程序中的这个API接口的。那么index和web应用程序之间的这个通信的协议就必须是异步的。因为我们的fast的API开发出来的这个框架就是异步的，明白吗？所以我们这个ASGI这个网络通讯协议其实就用在这儿，就用在index和我们的web应用程序之间这样的一个通信协议。

发言人   04:06

由于通讯协议是异步的，同时我们的fast的API开发出来的这个外部应用程序也是异步的。那这样的话，我们的性能，我们前面说了fast API性能是最好的。原因就是因为它整个周边用到了一些技术和协议，全部都是异步的，知道吧？

发言人   04:25

好，目前为止，我们的这个异步的服务在异步的，这个异步的接口就是ASDI接这样的一个网络接口协议，完了，这个协议需要有服务来提供，好吧？那这个目前为止主流的还是用使用什么？看看我下面这个图。如果是异步的网关接口，你要提供这样的服务的话，你要么使用这个叫unique，要么使用这个hyper co hyper con以及uni ico那么WSGI非一步的网关接口的话，你要么使用这个UWSGI好吧，或者是那个口这个叫甘尼克，有些人做甘尼克，甘口，还有先做这个沟内口骨骨利口。好了，这个随你读怎么读了，反正我有时候也读这个咖喱，有时候读这个郭里口。

发言人   05:29

好，但我们现在这个bus的API写的项目，能不能跑到能不能也跑到这里头了？可以，因为这个比较特殊，这个是又支持WSGI，其实也支持ASGR，这个要给大家说一下，好吧？这个good，good是又支持WSGI协议，又支持ASGI协议，知道吧？

发言人   05:54

但是fast的API默认它是或者官方推荐的，是哪一个呢？是这个UV icon，u vico是官方就推荐的。大家可以打开这个fast API的官网，立马就能看到它好吧？

发言人   06:07

好，讲到这个发色API的官网，很多同学说了，这个老师怎么去打开了，对不对？怎么去打开？我们回到浏览器，回到浏览器，我们在这儿给大家打开一下，你在浏览器里面随便找个搜索，在这搜fast API，收到之后一般第一个就是的，好吧？像我这里用的是B浏览器，它的第一个就是的。如果是百度的话，可能是第三个或者是第2个OK你可以它里面有一个lean就是学习的文档。这个学习文档里面的，你看他的这个介绍以及教程，全部都是默认都是采用这个UV口，就是刚刚我给大家说的那个提供的ASSASGI协议的一个服务，好不好？这个服务，我们企业里面也会到用到这个UV口，就这个UV口，我们等会儿在做测试的时候也可以用UV口，好吧？

发言人   07:05

好，然后那关于这个fast的API的相关的一些概念，我就先介绍到这儿。接下来我们马上就要开始进入到这个fast的这个框架，fast API的框架的介绍好吧？好，我们还是一定要把那个文档的最大化一些，要不然大家也看不清楚。

发言人   07:24

Fast API是一个现代的比较新的它的第一个发行版。我说的这个发行版的意思就是说，大家可以从官网上下载来下载下来用了，不是说在内部测试的。第一个发行版大概是在2019年的样子，各位你想想那是很比较新的一门技术了。那稳定版的话，大概是在2020年的样子才出了这个稳定版。

发言人   07:51

那么现在这个fast的APAPI的最新的版本了，好像还是0.11。等会儿我们可以看一下，等会安装的时候你可以看一下，我们按照最新的，为什么不用去记版本呢？因为版本现在fast的API由于现在比较火了，所以它的版本更新比较快。你去记它的版本暂时还没有意义。我一般来说我们记一些大版本，什么大版本？比如说我们在江口里面，你记你知道江高2，江口三江口4，知道这些大版本就行了，知道吧？你这江口3.2.16，这个到底16还是17，其实没什么太太大的区别，明白吗？所以这些版本不用去管它。

发言人   08:27

好，fans的API是一个比较新的，当然比这个江哥也好，托拉德也好，flash也好，都是都新新的很多。如果你把这个江狗比作人的话，八现在七八十岁的老人就是江狗好吧，11R31ICA的小伙子。那就是fast的API同时它是一个高性能的web框架。

发言人   08:53

其实fast API为什么现在越来越火？它最多最重要的一个优势就是高性能，因为它是是异步的，一出生就是异步的。虽然说整个框架3.2之后也支持异步，对吧？但是问题是你还需要它的异步和和和和那个同步的异步和同步的这开发它实际上是要两套代码，但是在faas的API中，它是一套代码知道吧？

发言人   09:22

好，用于构建API的API接口的所以我们主要你看这句话里告诉我们就是发as API主要是做这个后端的API接口开发好吧。它是建立在这个stanic以及PY desk里面的，单独里面的这个state主要是是一个极轻量级的外部框架，PYD是一个专门处理数据的，比如说接收数据，或者对数据进行校验，或者响应数据，或者对数据的类型进行转换这么一个工具，知道吧？那么fast的API主要是基于这两个的，好吧？好，同时fast的API它是基于python 3.7以后的版本。各位如果你是fast 3.7以前的版本，我不建议你使用fast API，用不了，为什么呢？因为fast API框架里头，由于它是基于最新的这个PY dente PY denning里面有什么呢？有这个类型提示以及类型转换。这个类型提示，那必须要3.7之后，才有这个完整的类型提示。

发言人   10:23

什么叫类型提示呢？我们之前在讲python的核心技术编程的时候，当时就说过我们定义一个变量，原来比较老的版本，A等于100，这样定一个B塔就可以了，对吧？那现在如果我们要归要做类型提示的话，应该这样定，A冒号int对吧？然后等号100，那这样子的话其实就声明了A这个变量的类型，那么有了这个A变量这个类型的声明，所以我们后面再写基于这个A做代码开发的时候，就会有类型提示，这就是所谓的类型提示，知道吧？然后在我们的fas API中，后面你要知道我们定义参数的时候，几乎都是需要声明类型的，都是采用这种方式来编程，好吧？那这种方式来编程的话，你的python版本必须要高。就是你拍的解释器的版本，你不能还是3.6，3.6以下的版本，3.6的话好像都不行了。B是3.7还好，我们用的是3.9，所以这点不用管它，好吧？

发言人   11:26

好fast的AP具有这个自动交互式的文档，它基于的是open API。记住，不是OpenAI，是open API，它是一个接口文档，知道吧？像我们的swagger这个接口文档都是基于这个open API的OK。好，然后接下来往下为什么要用这个fast API？也就是说我们为什么要去学这个fast API了，其实我们之所以要学，是因为企业中现在慢慢的大部分项目都在使用发射API了，所以我没办法再学。

发言人   11:58

那为什么企业要用这first API了？其实我说过了，就是它的最大的一个特点，最大的一个优势就是强大的性能。什么叫性能？就是速度快，响应速度快，处理数据的速度非常快。好吧？Fast AP fast的API中使用了dente以及state等现代的python库，它可以实现比比flash jung等传统的框架更快的性能。他的义务他的义务支持使其能够处理大量的并发请求，并在高负载情况下保持稳定的性能，这才是bus的API最大的一个特点。

发言人   12:46

后面的我告诉你不重要，可有可无。你要说自动生成API文档，浆果也可以生成API文档，好吧？然后类型提示以及自动校验。类型提示的话在交互中确实不行，但是自动经验在加入中确实也是可以的，好吧？

发言人   13:03

好，第四点快速开发。说白了快速开发的话，那我我要我我要说一句话了，这个特点是有点扯的。这个讲狗的快速开发就是开发效率一定是比较比fast AP的开发效率更高的。我告诉你为什么呢？因为架构里头有一个叫server list虚拟化器，它又可以做反虚拟化，又可以做虚拟化，它而且可以跟模型结合在一起，它的集成度更高，所以开发效率是理论上来说要更高的。但是说实话化石的API仅次于江广OK，那你要要比要比普拉多要比那还是开发效率要高一点。

发言人   13:46

然后第五个有强大的交互式的API文档，也就是说发射API中有自带的一个API文档，相当于之前我们在讲江口的时候一个API文档类似。但是江口里面那个API文档他要需要和这个swag的API文档整合，要不然不好看OK，它自带的不好看，不好用。但是我们的fast API的又又好看又好用，用起来什么意思？等会儿我们就知道了，好吧？我们直接可以在API文档接口中接口文档中去去点击做测试每一个API的接口。好，关于这个fast API的介绍我就到这儿，谢谢大家，再见。