59-头条项目-获取浏览历史列表-思路分析_原文

2026年07月21日 12:53

发言人   00:00

Hello, 各位同学，这一节咱们来分析获取浏览历史列表这个接口的实现思路。咱们还是老规矩，先分析API接口规范文档的要求，推导路由，然后再来获取浏览历史列表。我们先看接口要求，接口地址这里请求方法是get，那地址我们写list是不是列表就OK了。那接下来请求头仍然需要认证，那就还使用咱们原来的这个get current user，就是我们之前定义好的认证请求头的方法，是不是就OK了。

发言人   00:31

好，那再往下请求参数需要两个，一个配置表示页码，一个是配置size，表示每页的条数。这两个配合起来大家还记得干什么用吗？分页查询是不是？好，配置参数它的默认值是一，配置size默认值是十，最大值是100。

发言人   00:52

那我们未来写参数的时候，是不是就定义为查询参数query导入进来。那导入进来之后D去设定大于等于，那我们的配置要求是不是大于等于一？配置size的话，我们按照python的语法规则写python下划线size。未来query里面定义一个别名是驼峰式的配置size。是不是就OK了？剩下的就是默认值，大于等于一，小于等于100OK这两个参数搞定之后，接下来就看响应结果，响应结果仍然是统一的格式。Code message data.

发言人   01:30

只不过data里边的数据因为返回的是浏览历史列表详细的数据，所以有点复杂。首先有一个浏览历史列表list，list是一个列表，列表里面是字典，字典里面包含的。注意来看，新闻基础数据加上view time。看到这个大家会想起来啥？新闻基础数据是不是来自于news新文表view time是不是来自于浏览历史表？那这里的信息涉及到两个表，说明将来查是不是肯定涉及到连表查询。那列表查询，咱是不是在上一个模块收藏获取收藏列表里边是不是已经讲过了？那这里的做法其实跟获取收藏列表基本是一样的。

发言人   02:25

好，那再往下total是不是就是总量孩子们是否还有更多用来给前端做下拉功能的，是不是？好，那这个获取浏览历史列表接口要求咱们就看到了。那么大家可以回忆一下，跟我们那个收藏列表很像是不是？好，那现在咱们这个API接口要求咱们已经分析出来了，那对应的路由大家是不是应该没问题？

发言人   02:54

好，那路由搞定之后，接下来咱们来看看具体获取浏览历史列表它的实现思路。首先一旦进入请求，我们需要认证token。另外参数我们要求是两个，一个是配置页码，一个是page size，就是每页的条数。进入请求之后，第一件事儿就是验证用户有没有登录，没有登录报错，不给看浏览历史，登录了是不是才能看浏览历史？当然我们的获取浏览历史列表，是不是需要三个数据，一是新闻加浏览数据，二是total总量，三是是否还有更多，然后是不是才能响应。好，那接下来我们是不是就得在整个实现的过程当中想办法获取这三份数据。那我们可以看最简单的统计总量，是不是就是circle action里面的function，调用coin方法是不是就可以统计总量了。

发言人   03:54

好，那有了总量了，下一步咱们连表查询浏览历史。但是这个连表查询是谁跟谁连呢？查询的主体还是新闻，那就是news模型类join要联合谁呢？联合history就是咱们的历史表。当然咱们在这儿查询的时候也要考虑，比如说排序了，那查出来总要有一个顺序，对不对？还要有分页的功能等等等。那写法基本上还是跟咱们那个收藏是一样的，收藏列表是一样的。好，那有了这个连表查询能够查到浏览历史了，下一步还有一个是否还有更多，那就看total这个总量是不是大于当前显示的条目数，大于就是还有更多，不大于是不是就没更多了。

发言人   04:44

好，那现在总量浏览历史还有是否还有更多，这三份数据已经准备好了，那我们是不是就可以响应结果给到前端了？当然大家一定要记着，就是响应结果的时候，因为我我们要的那个数据是不是带有list列表，带有total总量，带有孩子帽，是否还有更多？是不是得先定义模型类，用这个模型类构造出来包含list，包含total，包含孩子帽的数据才OK。所以在响应结果之前，我们需要定义这个模型类才能响应结果。那具体这个模型类的定义方式，我们就参照收藏列表来定义就行。当然我们在收藏列表里边，是不是把新闻最基础的那些属性已经定义好模型类了，我们就还继承就OK了。

发言人   05:37

好，这个是浏览历史列表实现的大致思路。那么分析完之后，大家不难发现，确确实实跟收藏列表非常相似，基本上思路都是一样的。那么接下来老规矩，获取浏览历史列表交给大家来完成。万一遇到问题，我在评论区里面等你。