Web开发模式相关概念

Web开发模式相关概念_原文

2026年07月22日 14:59

发言人   00:05

好，各位小伙伴们大家好，我是老肖。接下来我们开始进入到目前为止，在web框架这个领域中最快的一门技术，叫fast API这个框架。Fast API这个框架，我们会讲这个fast API本身以及fast API和circle action整合对吧？那我们在这个框架里面还会讲这个circa I，那circle action我们是讲最新的2.0的版本的，好吧？那fast的APR也是最新的。

发言人   00:35

好，在先讲fast的API这个框架本身之前，我们先要理解一下关于faas API里面一些相关的概念。首先是web开发模式，这个web开发模式我是简单的介绍，因为很多小伙伴们可能在前面已经学完了这个框架了，好吧？那这个时候你应该能够搞清楚什么叫前后端分离，什么叫前后端不分离。如果你之前没有学过三个框架，那我还是建议你，你可以最可以提前去看一下那个江阁框架的视频。

发言人   01:12

好，我直接回到我们的fast API里面的相关概念。首先给大家讲讲在web开发里面，它有两种常用的开发模式。一个叫前后端不分离。

发言人   01:27

什么叫前后端不分离呢？就是客户端浏览器看到的内容和所有的界面效果都是由同一个服务器提供出来的那浏览器我们都知道，浏览器可以看到界面，可以看到各种特效，对吧？可以点击按钮，可能弹出一些不同的数据，所有你看到在浏览器中看到的内容，包括界面本身，包括这个界面的特效，都是由一个服务器提供的。包括这个界面中所展示的数据，明白吗？数据界面以及界面的特效全部都是由同一个服务器给你提供的响应。这样子的开发模式就叫做前后端不分离的。

发言人   02:15

为什么叫不分离呢？因为我刚刚说了，界面也好，数据也好，以及界面的特效也好，全部都是由一个服务器提供的。在这个服务器里面，你又得写业务逻辑代码，又得写数据的代码，又得写界面的代码，所有的业务逻辑的请求，数据的处理以及界面的渲染，全部都是在一个服务器里面，全部要写完，全部要完成这他们的代码。这样子的话就要前后端不分离的那大家可以看这个图，这个图里面我们浏览器发送一个请求，好吧，请求到我们的应用服务器之后，这个应用服务器他会去数据库中查询数据，那这个绿色的就是数据库，查完数据之后，对数据进行处理。处理完之后，可能还要处理一些相关的逻辑，再渲染成一个HTM码的页面。

发言人   03:10

各位因为如果你要在要在我们的这个浏览器中展示，你肯定是要响应一个HTML的一个界面，对吧？ITML界面其实叫这个过程叫渲染，因为它还需要把数据了也要在ITM中展示出来。整个数据界面以及特效，而都是在这一个应用服务器中给你全部生成好。生成好之后给你返回响应，这个响应其实就是一个完完整整的ITM。好，然后把这个ITML响应到你的浏览器中，浏览器你就能看到了，又看到什么数据界面以及界面的特效，那这样子的程序员开发这样子的一个外部应用程序，这种开发模式叫前后端不分离的。

发言人   03:55

前后的部分离是前几年比较老的一种开发模式了。这种开放模式它有一个问题就是什么呢？就是这个程序员在开发的时候，你的设计界面必须要先出来，要不然我没办法在我的应用服务器里面同时要把数据查出来，还要把数据放到页面中进行渲染。好吧。那这种实际上对开发效率来说是有大大降低的好吧。而且不而且有一个特点，它没办法做这个拆分，所以前后端不分离的，大家只是了解为主，所以我们的课程中重点是前后端分离的项，好吧？

发言人   04:36

好，接下来我们再介绍下一个，就是前后端分离的，前后端分离的，我还是要把它变大。前后端分离的那就是前端的界面效果。前端的界面浏览器的界面主要是由RTM标签以及CS的样式，以及GS对吧？这些这三个部分内容叫前端界面，这个前端界面的技术，我们把它分离到另外一个服务器中。还有一个python的服务器，这个python的服务器专门处理业务逻辑和返回数据OK，也就是说整个开发模式，它把这个拆分了，按照技术拆分了前端的HTMLCSSGS放到一个服务器中，专门开发以及部署那拍的你写好的后端的代码，包括业务逻辑的代码，以及处理数据的代码，包括产生数据的代码，又放到一个专门的python的服务器里面去运行，包括开发。那这样子的开发也是分离的，运行也是分离的，明白吗？就两个服务器了，知道吧？

发言人   05:43

也就是说前后端分离它最大的一个区别就是至少要两个服务器。一个服务器专门处理前端，一个服务器专门处理后端。所以你在开发的时候也可以分开，一个团队专门开发前端，一个团队专门开发后端。在开发后端的时候，前端他的这个团队他可以同时开发。不用说，等着你的界面开发完之后，我这个后端才开始开发。不用，只要需求确定了，那前端的团队以及后端团队可以同时开发。这也是目前为止在企业里面，尤其是这样的外部项目，是最最流行的一种开发模式。几乎是90%以上的项目，就企业级应用项目都是采用这种开发模式，前后端分离的开发模式。

发言人   06:31

好吧？好，我们可以看下面这个图，下面这个图里面看我们浏览器，它如果是去请求的是HTML的界面，那他就请求这个专门的前端服务器。这个前端服务器也称之叫静态文件服务器。好吧。如果你的浏览器专门是想请求数据，那你就请求我的这个python的应用服务器，我这个python应用服务器对外提供统一的API接口。好吧，那根据你的请求操作数据，我给你返回你需要的数据。不过大部分情况下，我们响应的数据就是返回的数据都是节省格式的，因为现在阶层格式最流行，几乎是95%以上的这种外部应用。不单是外部应用，还有一些桌面应用，甚至它服务器返回的，甚至包括安卓IOS这样子的APP的手机，APP的开发，它返服务器返回的数据都是阶层格式的，好吧，所以XM是比较老的一门技术了。

发言人   07:39

现在应该是十年前可能还会有有有些项目使用X，现在大部分都使用节省的，因为节省第一个姐姐，第二个老师他是跨所有语言的，而且操作了非常方便，好吧？好，这个python的这个python我们通过python语言写的这个后端的应用服务器，它只处理数据以及处理业务逻辑。至于你把数据放到哪个地方展示，我不管明白吗？你把数据放到手机APP里面，你把数据放到微信小程序里面，你把数据放到浏览器中都行，你爱放哪放哪。也就是说我的python应用服后端的应用服务器，我只负责处理逻辑以及给你返回数据，明白吗？你拿着数据去做什么事情，那是你客户端的事，懂吗？

发言人   08:31

这就是前后端分离的项目情况下，对于我们的后端程序来说是这样子的那前端程序是什么？前端程序就是专门去做界面，好吧，那个界面的样式，界面的按钮以及界面的排版，界面的特效，专门是有一个叫前端服务。这个前端服务器，里面放的都是一些HTMCSS图，还有一些js文件。这些文件，都是叫静态的文件，所以我们也经常把这样的服务器了，就前端服务器了，称之为叫静态文件服务器。

发言人   09:06

那像这样说的话，是不是如果是浏览器的话，浏览器是不是发很多请求对吧？如想要获得界面去去请求前端服务器或者叫静态服务器，想要获得数据就请求我这个python应用服务器，是不是这样？对，没错，对吧？所以前后端分离的情况下，我们的请求也是分散的，知道吧？就浏览器发的请求也是分散，明白吗？这很正常。

发言人   09:31

好，另外再多说一句，其实我刚刚在说这段话的时候也给大家已经说了的，后端服务器不一定是说只是做外部网站开发。其实你做手机APP开发也需要后端服务器。因为手机APP本质来说就是一个界面。手机APP上展示的数据，很有可能这个数据是从后端服务器中发送一个请求，由后端服务器返回得到的数据。然后你在手机的某一个APP上才能看到这个数据，知道吧？

发言人   10:03

所以目前为止，企业里面的这个服务端开发，主要都是像刚刚我所说的，对外提供统一的API接口。根据你客户端任何一个客户端，手机客户端也行，微信小程序客户端也行，浏览器的客户端也行。你只要发了请求，我就给你返回阶层格式的数据。这就是我们这个后端服务器要做的事。

发言人   10:27

好，了解了，了解完之后，接下来我们再说为什么我们要去谈这两个概念。原因是因为我们的fast的API其实一出生一生来就是为前后端分离的这种前后端分离的这种开发模式提供的。你说老师那用fast的API能不能做这种前后端不分离的呢？可以的，首先fast的API它底层是基于的一个叫，我们往下拉，底下有它。其实fast的API它底层是基于一个叫starlet。Starlet本身它是可确实它study本身一开始一出生做专门提供API接口开发的，就是专门做后端开发的。但事实上它也可以做什么，做这个前后端不分离的。比如说它里面可以兼容或者整合。

发言人   11:24

精英讲二模板引擎，大家都应该听过。之前我们在讲这个，可能有些同学在讲这个，听我们的这个江湖课里面介绍过浆果框架本身是不是也有一个自己的模板引擎？那个模板是干嘛的？就是做x chema界面渲染的，好吧，那只有前后端不分离的情况下，你才需要做界面渲染OK。

发言人   11:48

也就是说fast的API由于是基于这个starlet，所以fast API也可以做前后端不分离的项目。它也可以实现模板的渲染，好吧，也能实现。不过它的主要的功能，你可以去看官方文档，就是fast的API的官文档。它主要它的功能还是做这种前后端分离的项目，还是偏向这种后端开发为主的一个框架。所以fast的API我们可以这样讲，它是未来最主流的，也是速度最快的运行速度最快的一个外部服务的后端框架。他不是说只能做后端，他主要做后端开发，也可以做一定的前端，好吧，也可以做一个结论，所以我们要介绍一下这个前后端分离的问题。

发言人   12:44

然后再说一下刚刚我们提到一个叫统一的API接口。什么叫API接口呢？其实就是我们的这个叫application programming interface，就叫简称API。这个API接口就是应用程序对外提供的一个操作数据的入口。这个入口可以是一个函数，可以是一个类，也可以是一个UL地址或一个网络地址。当客户端想要调用这个入口的时候，您只要客户端发送请求就可以了，明白吗？那这样子的一个东西，我们把它称之为叫API接口。目前为止市面上大部分的公司开发，大部分的项目开发，其实都是后，尤其是后端开发都是采用接口的这这种API接口的方式去实现。

发言人   13:31

现在目前为止主流的API接口的规范有restful以及RPC这两种接口协议。记住这两种，这是你可以理解为这是接口的规范和协议。好吧？像RBC的话不用质疑，它就是一种叫接口开发的协议。这个restful的话一般很多人说叫开发风格是吧？其实开发风格不就是人为规定的一些条文，对吧？一些人为有人为的去规定的一些开他的风格和样式，你也可以认为就是规定，规定其实就是跟协议差不多，好吧？

发言人   14:05

协议不就是人和人之间商量规定的一些，我们俩通信的规则，对吧？这就是协议，这里面都有规则两个字，知道吧？好，现在主流的还是使用这个restful好吧，这个RPC的接口开发了，现在市场占有率不高了，python也可以做RPC的接口开发好吧，但主流的还是这个riser。因为这个risp它可以考虑的是一个方便，第一个是非常方便，第二个安全性也比较高。

发言人   14:37

好，我们介绍这个racial，全称叫present presentation state点翻译成中文叫什么呢？叫其实这个翻译成中文还不好不好翻译。如果直接翻译成中文，其实好像你看直接翻译成中文叫什么？叫具象的状态转换这种。其实刚刚我们看我把这个中文翻译出来，你根本不明白什么意思，其实你不用去明白什么意思，它就是一个名字而已，就像我们这RPC1样，它就是个名字而已好吧。它只是一种接口API接口开发的规范的名称。

发言人   15:18

就好比说我们经常说，全世界有一个叫质量管理体系，叫ISO9001是吧？为什么它叫ISO9002？其实不用管它它为什么叫对吧？只是我们都知道这种质量规范体系有一个质量规划体系的名字，国际质量规范体系的名字叫ISO9001对吧？就可以了。

发言人   15:39

那recipe就是在程序在这个后端API接口开发中，一个国际上主流的一种开发规范风格，名它的名字就叫做restful好吧？它是一种面向资源开发的编程模式。OK他把所有的API接口都看成是一个资源。你请求这个API接口，你就能获得这个资源。资源是什么？资源就是数据好吧，资源就是数据。就他中间他在处理过程中，他一定是有业务逻辑的处理好吧？

发言人   16:11

这种风格的理念认为后端开发任务就是提供数据的，数据就是我们所谓的资源，对外提供的是数据资源的访问接口，所以在定义接口时，客户端访问的UL路径就表示这种操作数据的资源，明白吗？就是我们前面也说了，如果是前后端分离的那后端就是提供数据，明白吗？我把数据看成资源，是不是你这个资源我怎么描述呢？就是有一个UIL的地址，这个地址就对应这个资源，一切我们面向这个资源来进行开发。那对于数据的资源，分别可以使用这几种请求方法，分别是post，delete, get update等等这些请求动作。

发言人   16:58

请动作也是我们经常说的请求方法，它是一个概念。比如说我们要添加一个学生，对吧？那添加一个学生，我们的请求的方法一般是post的请求地址是student，但我只是举例子，我要获得所有的学生，我请求方法就变成get。因为get是获取数据的意思，然后获取一个学生也是用get，那你get里面往往是后面要你这个路请求地址，后面往往要接一个什么，接一个这个学生的ID，获取ID唯一的一个学生对吧？你看这个请求路径跟前面的get请求路径就不一样，是不是好put，就是修改学生修改一个IDV1的学生，delete是删除一个IIDV1的学生。如果你学过这个框架的话，上面这些概念按道理说你是听过的或者稍微了解过的，好吧？所以这节课我是比较简单的快速的给大家普及一下关于这个外部开发模式的这里面相关的一些概念，这节课就先到这，谢谢大家，再见。