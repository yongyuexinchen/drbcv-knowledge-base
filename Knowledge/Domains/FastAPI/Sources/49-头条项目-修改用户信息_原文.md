49-头条项目-修改用户信息_原文

2026年07月21日 12:54

发言人   00:01

Hello, 各位同学，这一小节咱们来实现修改用户信息的功能。我们老规矩，还是先路由再来分析实现思路。我们对照着这个API接口规范文档来提取一下路由的关键信息，首先来看请求方法这回事，pot put表示修改或者更新的意思，那往后看地址是按update更新的意思。

发言人   00:23

好，那接下来看下一行。请求头需要认证，这我怎么觉得这么熟呢？对对对，上一个接口获取用户信息是不是认证了请求头，也就是那个token是不是有效的那一个，对不对？那个代码咱们已经抽取成函数了，那在修改用户信息这里直接依赖注入来使用是不是就OK了？

发言人   00:43

好，那请求头这里我们做过了，那再往下请求参数，是不是用户有可能输入昵称、头像、性别简介、手机号，它输入啥？是不是通过put的方式提交到数据库保存起来？好，那我问大家，你说这些参数们用什么参数类型，路径查询请求体，请求体参数是不是因为我们要提交到数据库？那想要请求体参数，咱是不是就得提前定义python dentical模型类？好，那在这儿大家在多看一眼，必填项这里都是否，那就是非必填。好，那这是咱们的参数，请求体参数。

发言人   01:21

好，那接下来再往下就是响应结果了。响应结果仍然是统一的格式，code message和data。Data里边是用户信息就OK了。那我们就用那个user info对应的模型类来提取数据，是不是就OK了？好，这个是咱们的接口规范。

发言人   01:37

有了这些要求了，问大家，陆游大家会解了不，应该没问题，是不是？我们从新闻模块到现在用户模块，已经写过很多个路由了。那我相信这个基础路由已经难不倒大家了。在这儿我绝对是无条件相信大家。

发言人   01:56

接下来我大胆一点儿，每回咱们都是分析了接口规范，那我们就带着这些要求，是不是先写路由，这我大胆一点，先跳过，先不写了，咱们先把那个修改用户信息的思路分析完，然后整体到代码当中一块来实现路由跟修改的功能。那我大胆一点了，当然在这儿我得做一个准备工作，就是我这些参数名字是不是太多了，我怕我忘，所以我咋办呢？我把它截一个屏，这样给它钉到屏幕上，钉在右下角，保证比如说不遮挡，尽可能的先缩小一点，等看的时候再放大。那这个路由咱们先压一压，待会儿再来写。

发言人   02:41

好，那接下来咱们言归正传，我是不是说分析一下修改信息的思路，分析完整体写代码。好，那接下来我们来看看该怎么修改用户信息。首先子弹进入请求前端是不是就携带了token，我们需要认证，用户有没有登录过。当然这个认证的过程，咱是不是已经写过了，按照直接复用了。但是稍微复习一下，要提取这个请求头里面的token，咱fast ABI是不是用header参数？这个也是我们上一节写的，稍微提一嘴。好，接下来除了获取这个token以外，另外还有就是用户输入的昵称、头像、性别简介、手机号等等等等。好，一会儿我们先定义python单体壳类型。

发言人   03:26

好，进入请求了，获取了token，有了用户输入的这些信息了，那下一步我们需要干啥，是不是认证一下你登录没有，对不对？那就是我们上一节写过的那个token认证的过程，大家还记得吗？先检查你这令牌有效不，要是无效了，那我就return个，那将来是不是就能read抛异常了。那要是有效就是有令牌没过期，那我们是不是就可以查用户了。但是查用户能查出来或者是不是查不出来，两种可能性。那如果查不出来，我们就抛异常，查出来了就干啥，查出来了就修改呗。修改其实是不是就是把用户输入的那个值是不是保存到我们的数据库，也就是执行更新的动作。

发言人   04:16

好，那接下来更新完应该干啥？更新的动作，那提前说这个更新，我们有两种方式，一个update，一个直接赋值。那我这里就用update了，因为这个update写法在这儿更方便一点。

发言人   04:30

好，那接下来再往下更新完用户信息应该干啥？响应结果给前端是不是就OK了？那前端拿到最新的这个用户信息了，渲染到界面，我们是不是就能看到修改之后的信息了？这个是咱们修改用户信息的思路。其中检查令牌有效性，认证这个用户是否登录的这个逻辑，咱是不是已经提前写过了。抽取成了一个函数了，那我们就直接dependence注入来使用，是不是就OK。所以其实在咱这儿是不是把路由搞定，参数搞定，接下来就是修改就OK，然后响应结果是不是就完事儿了。

发言人   05:09

好，那接下来咱们来写代码来试一下了。那回到咱们的拍摄品当中来，允许我先打一个注释，我们现在是不是要修改用户信息？那大致的思路是啥样的？首先验证token，下一步干啥？好，下一步是不是就是修改用户信息，其实就是更新是不是就OK了。那更新完咱们响应结果是不是就可以？

发言人   05:33

当然在这儿多问一嘴更新的是啥？是不是应该是用户输入的那些数据，那用户输入的是不是在右下角还有什么头像、昵称等等等等各种信息。那用户输入的这些信息，我们说是不是要put去提交？所以得它是请求体参数是不是才可以？所以要干啥？定义python antic模型类是不是才OK？好，那接下来也就是说咱们一点一点的分析得到结果。

发言人   06:10

也就是说咱们想要写路由，想要定义这个请求体参数，是不是先把这个派森单体和模型类给它定义出来。咱们这个模型类也是用户的，所以我就定义在scams users里边了。那打开我就顺序往最后写了，那我这儿加一个注释，这就是更新用户的更新更新用户信息的模型类，是不是就OK了？那我起个名字，class，这个名字还不错。User update request接下来我敲一个回车，那是不是就包含nk name？哇哒哒哒哒这五个就OK了，那我就直接写了nk name冒号。咱们简单一点来，就是string等号赋值，我直接来一个NOAE难了。

发言人   06:57

咱们简单一点儿。好，瓦塔头像VATAR我tape一下了，还有gender JENDER还是用类型，BIO是不是简介？那我冒号一下还是spring类型。那最后一个就是PHONE phone手机号还是string类型，默认值为那是不是就OK了。因为咱们这儿不确定用户输入简介还是输入头像，他有可能这五个当中只输入一个，对不对？所以这是给一个默认值为难。

发言人   07:28

好，我们有了这个模型类了，是不是就把路由的参数规定成是这个类型，就OK。那回到咱们的我control c一下，回到咱们的reuters里边，我开始写这个路由了。好，陆游大家说at root一下点儿，注意请求方法，大家还记得吗？对，put是不是？好，那接下来路径，对对对，更新update别写错了。好，那接下来小回车，我们a think一下。DEF方法名是update user info这个名字我喜欢我的通义灵码很懂我。

发言人   08:05

好，那接下来对于代码允许我给它干掉，我先写一个pass，那接下来参数问大家帮我分析一下，你说这个地方它的参数包含啥？参数包含啥？是不是得包含用户输入的，还要有啥？还要有验证登录凭证的，就是验证token的，还要有啥？注意还要有DB就是数据库绘画依赖性。为什么要有它？因为一会儿我在这儿打一个括号，因为一会儿咱们在这里是不是要调用CRUD里面的那个更新新的方法，那你要不要传入数据库绘画依赖项要，所以在这儿DB也得要着三个参数。那我们检查一下user data，那它的类型我们改一下，是不是刚才咱们定义的，我记得是update对不对？Apple date DATERE，走拿出来，当然还没导包，我们impart把它导进来，sky MERS里面的。

发言人   09:10

好，导进来之后，接下来就是user，这个user是不是就是我们验证token的那个，它的写法跟咱们上一个接口来看一下，上一个接口获取用户信息是不是一样的写法。OK搞定，那再下一个DB数据库绘画一来向。好，那现在参数是不是搞定了？那搞定之后，下一步我们一会儿写完了更新的方法，是不是就可以直接return了。那return的是不是还是统一的格式，更新信息成功，data我先给它干掉了，因为我还没有这个更新之后的最新的用户信息，我直接给它干掉了，来删掉。好，那我control s保存一下。路由是不是就写完了。

发言人   09:48

但是在这儿，同学们说那咱咱去浏览器里边试一下呗，别试了。为啥？因为咱这儿是不是有认证token的，需要携带请求头里边的token，交互式文档里边是不是测不出来。所以这儿稳住，咱有信心，这肯定没问题。咱忍住，就不测试基础路由了，直接往下写更新的逻辑，写完了拿着前端项目来测试就可以了。

发言人   10:15

好，那接下来基础路由不错了，咱们往下来，各位同学要干啥？验证token的跳过，咱们直接封装更新的方法是不是就OK了？那就写到CRUD里面来往下往下加一个小注释，这个是更新用户信息，来写一个type一下这个名字我很喜欢。

发言人   10:40

Apple date user好数据库绘画依赖项这个参数是不是肯定得要着？那接下来user data是不是就是应该是用户输入的那些数据。因为我们要更新是不是就把用户输入的提交到数据库？但这个类型不对，它是不是应该是那个update user up date，那我们给它导进来，import一下。

发言人   11:03

好，那接下来再来user ID那我说一下，这个ID咱们就不要了，要个啥，要个user name。因为一会儿咱们是不是要查出来数据库里边最新的用户响应给前端，前端拿到这个用户是不是就能渲染他的个人信息了。但我们之前封装过方法，是get user by user name，是不是根据用户名来查用户？所以在这儿我就不用user ID了，我用user name了。好，那我稍微把它改username的话是不是就得是string类型了？好，那这个参数各位同学OK吗？应该没问题。好，接下来我们往下来写代码来执行更新，是不是就OK了？

发言人   11:47

那这个更新该怎么办？首先这个写法不知道大家还记不记得，update写个模型类where WHERERE把它找出来，然后接下来点儿alias去更新它就OK了。当然update要导个包，update in part一下导入circle alchemy里面的那模型类咱们是用户，所以模型类是user是不是就OK了？那接下来Y那是不是就得是看看用户名是哪一个，根据用户名是不是去查，然后再改是不是就OK了。所以接下来就是user里面的user name，等于参数user name。那接下来再往后就是values值这个值它是不是应该是字段等于值，字段等于值这样的写法儿。它其实相当于这里边就是一个儿的键值对逗号隔开。

发言人   12:43

那么这种情况怎么办？首先大家要明确数据在哪里，用户输入的数据是不是在user data里面，它本身是不是一个python dente类型。那我这里要的是不是应该是这个格式，字段字段等于值。然后接下来逗号隔开，是不是再来一个字段等于值，是不是？应该是这样的。

发言人   13:05

我control c问一下，在这儿我打一个注释，大家注意，我要的是这样的，但是我现在有的是python单体类型的那我咋办？大家想如果如果我要是能把这个python单体的类型转成一个字电，我是不是就可以用星星把它解包出来了？啥意思？

发言人   13:25

User data允许我复制一下，那咱们现在的user data，它是一个python dente类型，python单体类型。那我要得到得到字典，就是python的字典。然后我干啥呢？把它星星是不是就能解包出来，解包出来，那解包之后是不是就长成字段等于值这个样子了？那这是我们的目标，那怎么办？注意我有办法，大家注意，这又是新知识点了。来我把这个字段的等于值给它干掉。

发言人   14:01

大家要清楚user data它是不是python单体的类型。我调用一个方法，model dump啊啊啊过了model dump，它就是把python单体模型转成字典的那转了字典了，下一步我在前面放上两个星号，是不是把字典解包，就相当于把那个python字典的大括号是不是给它干掉，我就能得到字段等于值这样的格式了。好，那写到这儿，大家OK吗？应该OK是不是？但是我说一下，我还没有写完，还没有写完，为啥？那大家还记得吗？这些参数用户是不是可能只输入了一个，比如说我只输了一个简介，我就想改简介，那其他的值是不是都为难？

发言人   14:53

那把难的提交到数据库有意义吗？那没意义，而且还有个要命的，大家想象一下，我原来是不是有一个默认头像是那张小猫，是不是？但我现在默认只是难用户没有给新头像，我咵嚓一下把难提交了会发生啥？对，原来还有一张默认的猫，提交完之后啥也不剩，那合理吗？是不是不合理？

发言人   15:24

那我们应该咋办？来在这儿我打一句注释，是不是应该是没有设置值的，不更新，这是不是我们的目标？没有设置值的不更新，就是用户没有输入头像，那我这头像就不更新，我就在这儿model dump的时候去检查一下哪个值不是难，再把它转字典解包是不是就OK了，那咋办？

发言人   15:51

加参数，注意我开始写了，这个参数叫x cloud，安set和难这俩都到，unset是不是没设置呢？那是不是就是无的，我把这俩都置为true，那逗号再来一个，就是一个思路的来一个，那它的值也为出，那这两个一加，就是设置值的，才会把它提取出来更新到咱们的数据库，把这两个加上。要不然就造成我刚刚说的，本来还有一个默认的头像，结果用户没有输入，一提交啪插头像默认的没有了，这就不合理了。

发言人   16:32

好，那我们现在这个更新的语句就搞定了。那我来一个小变量，叫query或者是STMT，把它保存一下。那下一步干啥？好办了，那我们就excute是不是就OK了。当然excute之后，我来一个result存一下，大家还记得吗？这个更新之后都得检查一下命中一行没有，命中了是不是代表更新了，那要命中了零行是不是就得抛异常了。所以我在这儿把这个XQ的执行结果我给它存一下，那存完之后，我再来一个commit提交，那紧跟着我就要检查更新，在这儿我要检查更新，大家还记得吗？那就是走。

发言人   17:16

If result row can't, 如果等等零是不是代表没有命中数据？没有命中数据要干啥？对rise抛异常是不是就OK了？那HTTP exception导入一下fast API里面的status code，我直接简单点了。那就404，用户不存在就这么放着了。好，假设没有进这个if，那是不是就命中了数据了？那命中了说明真的更新了。

发言人   17:44

那命中之后又要干啥呢？是不是把最新的用户给他返回？因为将来前端是不是得要更新之后的用户信息，所以在这里我获取一下更新的用户信息，也就是这个用户，那咋办？重新查一遍就可以了，那重新查也不用再去select了，而是我们原来有一个方法叫get user by user name。我传入DB传入user name是不是就OK了？好，那接下来我们拿一个小变量把它存一下。Update的user是不是就是更新之后的用户。好，那接下来下一步我们return是不是就OK，return这个apple dated的user就可以了。

发言人   18:32

好，那现在更新用户信息咱就完成了，稍微把这个思路整理一下。首先咱们这里是不是就用的是update来更新，按update来更新，且是更新的是用户真的输入的。好，那更新之后下一步我们干啥？检查是否命中，是否命中就是是否真的更新到一个用户了。好，那再下一步，获取更新后的用户返回是不是就O开了？好，那更新用户信息这个CRUD方法咱们封装完了，接下来我把它截一个屏，截一个屏。好，给它钉在这儿。

发言人   19:07

那问大家我们下一步要干啥？把这个方法放到路由处理函数当中给它调用上，是不是就OK了。来回到reuters里面来，那我们就在return之前给它调用，那就是await users里面的CRUD users里面的update user update。

发言人   19:29

User那传参DB是不是要传？接下来后边的参数我改一改，它不对劲儿，是不是得传个user name username，那user name打哪儿来？注意user里面有，是不是在这个get current user里面，它会返回一个用户，那我就这么写了，user形参点儿user name是不是就OK好，那逗号隔开第三个参数是啥？是不是用户输入的那在咱们这儿是不是就是这个user data？好，接下来逗号隔开user data一下。这个update user要是执行完，是不是就拿到这个方法的return返回值，就是更新之后的用户了。那我们拿一个变量给它存一下，比如说我就叫user，那存完之后各位同学下一步该干啥了？

发言人   20:24

对，return这里光有message不太好，我们是不是来一个data，data仍然是用user info response模型类点model validation这个模型类对不对？对，这是不是用户关键信息的那个python单体的模型类，mode validate是不是从user这个ORM对象当中提取值。好，那我们直接附到data这里来。那整体circus ze response是不是设置了统一格式，return响应给前端就OK了。好，那现在我们的update这个路由是不是也写完了，那我们就到浏览器里边来试一下了。

发言人   21:02

那回到浏览器里边来，切到咱们的这个个人信息。那目前我是登录过了，那我可以先刷新一下，确保是登录的状态。那接下来我们来修改，当然在这儿我说一下前端界面，只在个人简介这一条。加了点击弹窗的功能，就其他的地方他点不了。所以我点个人简介这一行，是不是让我修改个人信息？好，那我改一下，删一下，默认的我就说我喜欢法斯特API。

发言人   21:36

好，确认走你来看是不是修改简介成功，简介的内容是不是就换了。那接下来我点返回，在我的这里是不是也变成了我修改之后的文字，那这个简介是不是就修改成功了？当然在咱们这个地方，大家还记得吗？我们是不是提供了性别、头像等等很多个用户修改。但是咱们这个前端界面，只在个人简介这里提供了这种点击弹窗让我们修改的功能。那其他的位置它没有加功能，但咱后端不影响他子弹加了，咱是不是全能改。因为咱们的请求体参数包含的修改的属性还是很多的。好，那现在咱们的这个修改个人信息的功能就已经实现完了。

发言人   22:24

接下来咱们回到py top当中总结一下思路，来回到paid当中来。首先修改用户信息是不是思路也需要先验证token登录了不登录了才能更新，才能响应结果。那所谓更新怎么更新？是不是把用户输入的put提交到数据库？那既然是提交，那所以是不是设置用户输入的那个头像、简介、性别等等等等是请求体参数。所以咱是不是先定义了python单体壳类型，那我就在这个users里面，steamers里面定义了一个user up data request类型，这些值是不是都置为默认值为难，就是它有可能不输入，对不对？所以我们允许为空，默认值为难。

发言人   23:11

好，那定义好了这个类型了，回到root left里面，我们是不是就可以写路由了。大家还记得吗？为什么这个路由处理函数需要这三个参数，用户输入的请求体参数肯定需要验证是否登录的。这个get current user这个方法是不是也要依赖注入？另外DB咱们这里是不是要调用更新方法？所以数据库绘画依赖项是不是肯定得传参的？所以这个DB是不是也是咱们的一个参数？

发言人   23:41

好，有了这个基础路由了，咱们重点是不是来封装update user这个方法，来更新的方法。那这个更新的逻辑大家来看看，是不是update更新。咱们这儿用update的好处是啥？是不是直接把python dente这个类型通过model dump，是不是把它转成python的字典了。然后星星一解包是不是就OK？当然咱们加了俩参数，确保这个用户真的输入的才提取值，接下来执行update是不是就OK了。

发言人   24:13

好，那update之后，我们要按照惯例是不是检查一下有没有命中某个用户，命中了才能获取命中之后的用户是不是返回给I函数调用的位置。但如果假如说没有命中，直接rise抛异常是不是就OK了？好，那这个是咱们更新用户信息这个大致的实现思路。咱们一块儿实现完了，接下来就该同学们动手把自己的用户信息也来实现一下修改的功能。另外实现完之后不要走开，下一小节儿咱们来实现修改密码的功能，那下一小节咱们不见不散。