06-FastAPI基础入门-路径参数_Path类型注解_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，这一小节咱们给路径参数添加更复杂的类型注解。什么叫更复杂的呢？来我提需求。我们上一节有一个接口带的是输的ID，那被我们用python原生类型注解为int类型。那这个时候这个ID是不是输入1 100、666，它都是可以的。但现在我的要求变得更严格了，这个ID我要限制范围1到100之间，因为我只有100本书，超了100咱没有这个ID那咋办呢？

发言人   00:35

在咱们fast API里边有一个pass函数，可以做路径参数的类型注解，给参数添加额外的信息和校验。也就是说咱们python原生类型可以限制数据类型。想要做进阶的校验，在路径参数这里我们用pass函数。那怎么用呢？来pass函数，需要先导入，我们在fast API里边导入pass函数，接下来做类型的注解，怎么注解？注意来看在python原生类型注解的基础之上，等号赋值为pass函数。那我们是不是要限制范围？那这个范围怎么限制呢？

发言人   01:22

把pass函数继续添加参数，比如说3个点点，GTLTGELE表示的是大于小于、大于等于等等，那描述还能做长度的限制等等。当然我提前说pass的参数不仅仅只有这几个，它很多的那我这儿是列举了几个比较常用的那至于不常用的，待会儿我告诉大家在哪里找。这个是咱们fast API pass函数做类型注解，了解法了，接下来咱们也到代码当中来试一下。

发言人   01:59

我们回到里面来，我们就基于上一节的这个书的ID去做类型注解了。那我们要用pass函数对不对？第一步先干啥？导入从fast API里边导入一下，注意是大写的pass，p大写。

发言人   02:17

好，那导入之后，下一步原生类型注解的基础之上，等号赋值为pass函数。那它的小括号里边是不是写参数就可以了？那这个参数注意我鼠标放到pass上来。大家来看，pass的参数有这么多，咱们不需要背它。

发言人   02:40

大家来看，我们刚刚在PPT里边看到的，比如说3个点点，GTGELTLE max mini lens等等，是不是都在这里。那么这里有我们见过的，也有我们没见过的。我们说见过的是常用的，是不是？稍微记一记。至于没见过的那大家知道在哪里查就可以了。

发言人   03:04

好，我们言归正传，我们pass的小括号里边我先写3个点点代表什么意思？必填逗号隔开，我要限制范围，比如说1到100之间，那我是不是可以写GT等于注意GT是granter赞是大于的意思。好，那我写一个零，因为我要取一，我大于零是不是就能取一了？好，那接下来逗号隔开，再来一个less than LT，它是小于，我要娶到100，我是不是可以写101？当然写LE是不是也可以，那我这就不改了。

发言人   03:40

好，那写完了之后，接下来我们保存一下，是不是要来验证一下这个范围。我们的项目现在是运行的状态，直接回到浏览器里边来，我们刷新。接下来不可带ID的这一个try IT1下，那输入了，比如说我们先输入一个111，是不是大于100了？好，XQ的提交往下来看，这个input should be less than 101，是不是小于101？好，那接下来比如说我们来一个一百，是不是小于101？好，excute来看这是第一百本书，是不是就OK了？好，那范围限制好了，我们回到py top里边来，接下来我再加一个description，跟单词含义一样，就是描述。我打一个引号，这个描述就是方便同事之间配合工作。

发言人   04:36

咱们这个参数它是干什么用的？有什么细节，有什么注意事项，都可以放到描述里面来。比如说咱们这个ID，它是不是就是书籍的ID，书籍的ID那它的取值范围是不是1到100之间？好，那我们保存一下，再次回到浏览器里边来，我们刷新一下来看看description相关的文字，是不是已经在这个输入框的上面展示出来了。那这个对于我们调试接口来说是不是就非常友好了。现在我们范围也限制了，描述信息也添加了。当然我们刚刚还有一个这个长度范围的限制，这个长度范围是不是一般针对字符串来说的那接下来咱们回到py top里边来再写一个接口，让它带一个路径参数，咱们限制一下这个长度范围。

发言人   05:31

好，那我敲回车，我先提一个需求好不好？我的需求是我又要有一个接口了，需要干嘛呢？查找。比如说书籍的作者书籍的作者作者路径参数，比如说我是name是name，但是这个路径参数长度要有范围的限制。比如说人名，我来一个2到10个字，那我的需求各位明白了吗？清楚了吗？好，那OK咱们开始去写接口。

发言人   06:04

好，首先来个装饰器，艾特APP是不是点儿？对，get小括号那引号来个路径，那么我作者的话就是author了，AUTHOR那斜杠是不是得传入？名字是不是？那我就来个name了。好，那装饰器写完了，路径也搞定了，来处理函数。咱们来个a think异步函数，DF比如说就是get name一下小括号冒号，那小括号里边要咋写？

发言人   06:36

要跟路径参数同名儿，咱们得来一个形参是不是？好，那人名的话是不是就是string类型，而且确实我也准备要这个类型，因为要做长度范围的限制。好，那接下来pass做注解，咱们等会儿再说，先把这个return响应结果给它搞定。

发言人   06:57

那我来一个对象，比如说MS记一下，我就来格式化一个字符串，这是某某的信息，那这个某某咱们要替换成是不是name就可以了。那现在我们一个只有python原生类型注解的路径参数这样的一个接口是不是就搞定了。接下来我们保存一下，回到浏览器里边来试一下这个接口，来刷新一下。

发言人   07:27

我们找到author name的这一个接口，try eat一下。比如说输入我的名字，那我的名字是打球execute提交来看看，这是大邱的信息，是不是搞定了。那我现在输入这是大，就这一个字是不是XQ的，这是大的信息，这不科学。人名不能无限制是不是？那我们刚才约定好的长度范围是不是2到10？我们回到拍tom里边，把这个name做一个类型注解。

发言人   08:01

那怎么写？Pass是不是？那我们上面是不是已经导入过pass了？所以这回直接等号赋值，直接用，注意P大写，PATH小括号必填，比如说先把它搁上逗号。隔开max lens和mini lens是不是来限制范围。比如说mini lens来个二多少，隔开max length，我们再来一个十。

发言人   08:26

那接下来保存回到咱们的浏览器里边来刷新一下，还是找到author这一个接口，try IT1下来看。我们这里好像多了点内容，迷你max Lance是不是已经填在这儿了？好，那我们也来输入试一下。我的名字是大邱，是不是？那我就要一个字走你吼报错了，而且是直接弹窗报错，是不是？Must be list两个字符，对不对？还必须是大于两个字符。好，那我要是正常输入一个，大邱好execute，没有报错。

发言人   09:13

往下来看响应的信息是不是也拿到了，那我们的长度范围是不是也就限制好了。这个就是咱们fast API的类型注解，如果只限制类型就用python原生注解方式。如果有额外的限制路径参数，这里是不是导入pass函数，用pass函数来继续做类型注解就可以了。

发言人   09:37

学到这儿，咱们路径参数常用的功能就学的差不多了。接下来咱们回到PPT当中，给路径参数做一个重点提炼。我有两个问题，第一路径参数我们说出现在哪里，它是出现在UIL路径的一部分，就比如说我们写的book斜杠大括号ID，那这个ID它就是一个路径参数。

发言人   10:03

好，第二个问题，怎么给路径参数添加类型注解？一个是python原生的，一个是pass是不是原生的？我们说限制类型，如果是想要有额外的信息，比如说大小的限制、描述长度的限制等等等等。是不是咱们要原生类型注解的基础之上，导入使用pass函数就可以了。那我们搞定了路径参数以及类型注解方式，接下来就该同学们的表演时间了。我有小练习，我要大家自行定义俩接口，自己定义，都要携带路径参数，还要使用pass来实现类型注解，来看具体的要求。

发言人   10:47

第一个接口以新闻分类ID为路径参数，设计一个UL要求ID的范围是到100之间。好，第二个接口以新闻分类名字为参数设计一个UL分类名就比如说体育新闻、娱乐新闻、国际新闻等等等等，分类名称的长度比如说是2到10个字。接下来各位同学来动手定义一下这两个接口。另外定义完之后，大家可以把答案留在评论区，我们在评论区不见不散。