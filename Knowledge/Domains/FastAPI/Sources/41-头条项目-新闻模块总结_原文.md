41-头条项目-新闻模块总结_原文

2026年07月21日 12:54

发言人   00:01

Hello, 各位同学，截止到现在，咱们新闻模块的核心功能就已经开发完了。那这一小节咱们一块来总结一下整个这一章节的核心知识，我们就尽可能的按照讲课顺序来了。首先就是工程结构。大家回忆，我们说开发项目代码很多，肯定不能都放到main点PY。我们是不是按照软件的功能把代码进行拆分？我们拆分出来了一些文件夹，每个文件夹是不是存不同的功能代码？我们一个一个来看一下，比如说CRUD放的是啊数据库增删改查逻辑，models放的是啊数据库的模型类。因为我们这个项目操作数据库是不是采用的是ORM的方式，ORM其实就是面向对象的方式来操作数据库，那是不是就要有模型类？那我们把数据库的模型类放到models文件夹里边来。

发言人   00:57

好，那接下来再往下rooters如我们说是路由层，其实就是模块化路由。我们说那么多个接口不可能都放到main点，PY是不是直接拆分到rooters不同模块之下，那么维护起来是不是更方便？好，那接下来再来steamers，skimmers是数据验证模型，也就是python单体壳类型。当然咱新闻模块是不是还没有用到这个数据验证模型？那么目前这个文件夹是不是空的？那没关系，咱们下一个模块，用户模块就要用到了。好，这是skimmers。再往下utils，utils在咱新闻里边是不是也没有使用？

发言人   01:39

我们说ut s封装工具函数就是一些通用的逻辑。比如说我们后续有好多个接口都需要验证用户是不是登录了，登录了才能查看。比如说浏览历史才能查看用户详情。那么判断用户是否登录的逻辑是需要频繁使用的那我们就可可以把它抽取到UTS里面来。好，我们新闻暂时是空的，下一个模块就用到UTS文件夹了。

发言人   02:06

好，那再往下config是不是配置相关的那我们目前是放了一个数据库配置，那未来咱们有缓存的配置啥的也往config里边放，是不是就OK了？再往下main点PY是咱们程序的主入口文件。那像fast API实例、中间件，还有注册路由的代码，是不是都放到的是main点PY里面？这个是咱们的工程结构，后续开发项目是不是也可以参照这个结构来？

发言人   02:36

好，那有了工程化结构了，接下来再来，我们是不是就可以写代码了。写代码开发接口，我们说是不是可以有一个通用的流程来。第一，模块化路由是不是拆分代码到reuters里面。有了模块化路由，其实本质是不是可以进行数据库的增删改查操作了。但是我们说咱这个项目是ORM的方式操作数据库。所以是不是先有模型类再来封装数据库增删改查的逻辑代码。封装好了之后，这个增生改成的代码在路由处理函数当中一定要用，将来响应结果是不是就OK了？

发言人   03:13

这个是咱们整个的接口开发流程。大家可以回忆一下，我们获取新闻分类、获取新闻列表、获取新闻详情，其实跟这个流程是不是大致是一样的？我们后续开发这个用户模块，还可以参照这个流程来，整个流程有了，我们看看细节。

发言人   03:34

首先咱是不是有一个模块化路由，模块化路由咋写的？对API root or是不是先从fast API里边导入API router，接下来创建API router实例，写路由，注册路由是不是就可以了。那这个API router实例，来大家看这个参数prefix这是啥？对，前缀那就是相同模块每个路径前半部分是不是一样的。那我们直接放到前缀里面，将来写装饰器这个路径的时候，直接写不同的这一部分是不是就OK了。好，接下来再往上看啊，tags分组体现在哪里来着，交互式文档里边它是不是有一个组名，这个组名是不是可以点就能展开折叠当前这个组下的所有接口是不是？好，那我们有了API root实例了，那下一步是不是写路由装饰器就不再是艾特APP，而是艾特这个API root实例的名字。那接下来照着接口写请求方法，写路径，写处理函数，响应结果是不是就OK了。

发言人   04:45

当然问大家，如果只写到这里，咱这个路由能不能用？不能是不是得注册？那怎么注册？在main点PY里边依赖于fast API实例点应可录的rotor。是不是就包含rotor？那包含谁呢？是哪个模块下的哪个API router实例？那这是不是就是main点PY里边注册路由的写法？

发言人   05:12

那有了API root实例了，咱这个模块化路由是不是就写完了？那我们就开始封装功能，响应结果是不是就OK了。但假设这个接口我已经开发完了，交互式文档里边测试是不是没问题了。但大家还记得吗？我们能直接用前端的VOE项目访问后端fast API的这个接口吗？不能，为啥来着？是不是跨域了？

发言人   05:36

为什么跨域了？首先咱前端view项目的运行地址，是不是HTTP local host 5173 fast API是HTTP127点8000。他啥不同了？对域名不同端口是不是还不同？所以他天然跨域的，那跨域不能直接请求对不对？那怎么办来着？是不是cos跨域资源共享，当然我们稍微复习一下这个cos，它是浏览器的安全机制，对不对？简单来说就是同源的，可以发起HTDP请求，不同源需要服务器授权，对不对？

发言人   06:20

那问题就来了，什么叫同源来着？协议域名端口都得相同才叫同源，其中任意一个不同是不是都是跨域？好，那咱的fast API跟这个前端view是不是域名端口都不一样，所以它跨域，那跨域怎么解决？

发言人   06:39

添加啥来着？Fast API里边有cos middle e咱导进来是不是就添加cos中间件就OK了，那怎么添加？先导入导入之后，基于fast API实例来添加cos中间件APP点儿爱的middle are。那么添加的中间件叫啥名？是不是我们导进来的cosme的where？

发言人   07:02

接下来allow origins，这个是不是就是允许访问的源？那这是不是就是开启授权呢？那你允许谁来访问？我们开发是不是写了一个星儿，但真正项目上线是不是要改一个真实的列表，允许谁来访问，把谁放进来。那将来这里就不写列表心了，而是写列表名，是不是就OK。

发言人   07:26

当然剩下的三个参数，允许携带cookie允许请求方法，允许的请求头。当然我们这里是不是直接写的就是星儿了，允许所有请求方法，所有请求头。那像这样添加了cos中间件了，是不是咱前端的view项目就可以向咱的后端fast API接口发HTTB请求了。因为我们开了授权，即便跨域是不是也能资源共享了？

发言人   07:51

好，这是咱们的cos跨域资源共享。其实以后项目当中像前后端交互，它通常都是跨域的。所以这个cos跨域问题都是需要咱们去解决的。

发言人   08:04

好，解决了跨域资源共享问题了，那我们再往下有几个细节小知识点我们一块来看一下。比如说第一个模型类，我们给字段是不是创建过索引，大家还记得吗？为了提升这个查询速度的，这个写法，咱们第一次写一块儿复习一下。首先在模型类里面下划线table下划线ARGS，后边还有两道下划线，等号是不是来个方法，那核心是index index方法，你要添加的索引名是谁？对应是哪个字段？那我们这个index方法是不是就可以把这里就是factory ID是不是创建了索引。

发言人   08:47

当然咱们复习一个经验，咱这个项目为啥把分类ID发布时间创建了索引？我们说按分类去查新闻是不是很很常见的功能？高频查询功能对不对？所以我们创建了索引，创建索引之后再查新闻，比如说按这个ID来查，它就不是全表去搜，而是直接通过分类ID取送查询速度是不是更快。

发言人   09:16

再比如底下这个publish time，这是不是就是发布时间，那为啥把它也创建索引？想看新闻是不是通常你得把新闻找出来之后，按这个发布时间，最新的是不是在最上面，旧的新闻是不是在底下让用户看更新的新闻。所以这个按发布时间排序这个功能是不是也是非常常用的功能。我希望它速度更快一些，所以创建了一个索引。好OK这个是咱们的创建索引，提升查询速度。

发言人   09:50

接下来紧跟着，刚刚我们在发布时间这里是不是涉及到一个排序，这也是咱们ORM当中的一个新知识。这个ORM里边查询的数据排序，大家还记得用啥了吗？对，older by, 那我们先找出来order by写上你的字段和规则，是不是就OK了。这里问大家，这DESC是啥意思，对对对，它是降序，就是如果不写这个order by，它默认是升序排的那我们比如说这个浏览量，是不是谁的高在前面更合适，所以是不是DESC降序排是最合理的那像我们项目当中，是不是views这个浏览量降序排，发布时间是不是也是降序排的？因为发布时间新的时间它的数是不是更大，所以降序排是不是保证最新的在上面？好OK，这个是排序。

发言人   10:45

接下来我还有最后一个，大家还记得吗？我们在整个项目当中涉及到浏览量，是不是看一次新闻就要加一个？那我们是不是修改数据来着？咱们扩展的修改数据的核心方法是啥？

发言人   10:59

对，update, where是不是找出来values就是设置值。像我们那个浏览量，是不是先update更新哪个模型类对应的表，那where就是查询出来，对不对？查出来之后values要更新的字段是谁？值是什么？是不是给它写出来？

发言人   11:21

好，接下来还有一个重要的经验，就是我们说更新之后，确保真的命中了数据，咱要干啥？检查一下对不对？那怎么检查的？注意这里是不是有一个result，大家要清楚这个result是不是上边excute执行后的结果，这是ORM对象。基于这个ORM对象点row point是不是可以获取到命中的数据条数。那我做一个大于零的判断，真的命中了这儿。

发言人   11:52

比如说我们更新这个浏览量，这个point值是不是应该是一I是不是大于0，它是不是就return一个true给到了函数调用的位置。但是假如说没有更新成功，这儿是不是就return的是false？那我们在路由当中，是不是就可以根据这个return的true或者是false去提示用户有没有更新成功了。那以后我们在ORM修改数据的时候也是一样，先去修改，接下来是不是可以检查一下是不是真的命中了数据。

发言人   12:23

好，这些刚刚我们复习的这些是咱们整个新闻模块课程当中涉及到的重点知识和新增的知识点。那我们一块儿复习一下，希望各位同学对这些知识的掌握更清楚一些。当然同学们也可以私下把这些知识记好自己的笔记，方便自己去留档。好，截止到现在，咱们新闻模块就学到这儿，那下一小节我们就开始下一个模块用户模块了，我们下一小节儿不见不散。