53-头条项目-添加收藏_原文

2026年07月21日 12:53

发言人   00:01

Hello, 各位同学，这一节咱们来实现添加收藏这个接口的功能了。咱们老规矩，先按接口要求搭基础路由，然后再来分析添加收藏功能，实现功能是不是就OK了。我们先看接口要求，首先看第一行接口地址，这里请求方法是post，这里大家就要稳住了，请求方法是post。那就代表未来的参数得是什么样的参数，路径查询请求体，请求体参数是不是好，那完是不是就得定义python单体模型，是不是才能写请求体参数？好，这是第一个post请求方法咱们得到的关键信息。那再往下就是请求的路径，我们直接写I的就OK了。

发言人   00:46

好，那看完了接口地址这一行，再往下请求头仍然需要认证。那就是登录了才能把某条新闻添加收藏，否则就不能添加。好，那请求头认证的这个方法咱们还是直接导入来使用，是不是？就OK那接下来再往下请求参数，news ID代表的是要收藏的是哪条新闻，是不是？好，当然这news ID刚刚咱们说了，因为请求方法是post，所以咱们得定义成请求体参数。好，那再往下来看就是响应结果了，仍然是统一的格式code message和data。好，那现在接口的要求咱们看到了，这个基础的路由是不是又能写了，只不过需要留意就是news ID这个参数是请求体参数，需要定义一个python单体类型是不是才可以。好，那接下来咱们就回到py top当中来试一下了。

发言人   01:41

回到咱们的pym里边来。首先我先把那个python dente模型类给它定义出来，要不然咱没法写请求体参数是不是？好，回到S里面找到favorite点PY，那我敲个回车，咱们来一个类，这个类名咱们起个名字，是不是添加收藏请求体参数的类型？我起个名字，FAVORITE favorite，AADD代表添加，但我们是发请求的时候要用的，对不对？请求就是request REQUEST，我直接tab一下，继承base model没问题，那我们要news ID，但field给它起个别名，是不是我们真正需要的news ID就OK了。

发言人   02:27

好，那咱发请求的时候要用的这个请求体参数，这个类型定义好了，那我们下一步是不是就可以写路由了？那回到咱们的root里面来，我顺序往下写了，来一个at root点儿，注意请求方法是啊post，那我tape一下，路径是不是就是ADD，那敲个回车，我多回车一点。好，那接下来来一个think DEF定义路由处理函数，我直接tab一下了。ADD是不是favorite？就是添加收藏。方法具体的功能允许我先pass一下。

发言人   03:03

那接下来我们刚刚的参数是不是要搞定一下？刚刚说需要news ID需要认证token，是不是还需要数据库绘画依赖项？News ID认证token的数据库绘画依赖项是不是没问题？蛋蛋news ID是query查询参数吗？不是，它是不是请求体参数？

发言人   03:30

那咋写？容我删掉类型，咔嚓粘一下。这是不是刚才咱们定义在scammers里面的py森单体和类型的名字？我import t把它导入一下，导入当然这个news ID我改个名字，这名字就不太好。比如说我就给它叫data，将来是不是就得是data点儿news ID才可以？因为你叫news ID，点news ID我都不敢想这个名字是不是好OK。

发言人   03:58

现在咱们的三个参数搞定了，那未来我们调用封装的CRUD添加的方法，接下来是不是return响应结果就ok那我们响应结果是不是用success response来响应？好，那我先写一个message，添加收藏成功。好，那现在大家来看我们的这个基础路由是不是就已经搞定了。那下一步我们就来看看具体添加收藏该咋实现，是不是就OK。

发言人   04:25

那咱们这样回到PPT里边来分析一下添加收藏的实现思路，来回到PPT里边来。接下来大家看啊，当我们进入请求的时候，是不是要认证投肯，还要携带请求体参数news ID。那进入请求了，下一步是不是验证它有没有登录，没登录又要报错，登录了是不是才能执行添加收藏，那添加收藏咋做？是不是就是在咱们favorite表里边爱的一条，爱的条。那ID完之后，响应结果是不是就OK了？那这个思路大家OK吗？So easy是不是？那我们来试一下了，回到咱们的拍摄当中来，我们直接来封装CRUD艾特的方法是不是就OK了。那回到CRUD里面来，敲个回车，我们接下来要添加收藏a think DEFAI的news favorite就要这个方法，名儿是不是还OK？那接下来关于具体的添加咱们自己写，我给它pass掉了。

发言人   05:34

那参数大家看啊，DB数据库绘画依赖项是不是铁定得要着？那接下来user ID，user ID要不要？肯定也得要，是不是得是添加当前用户收藏了某一条新闻，所以表示当前用户的user ID，表示当前新闻的user ID是不是都得要着它？好，那接下来参数搞定了，咱们来实现功能。那功能是不是就是新增就OK了。当然我们是不是得先有一个ORM对象才能给它增进去，是不是，那我就来了，模型类小括号，user ID等于形参，user ID是不是传过来的？User ID表示当前用户是不是news ID等于传过来的参数值news ID那我这样的一个ORM对象是不是构造出来了，我们将来把它艾特到数据库是不是就ok那接下来我来一个变量把它收一下，FAVORITE等号赋值。

发言人   06:42

那接下来同学们要干啥？DBID是不是就OK了？那艾特之后commit提交千万不要忘。另外我们最终是不是要return这个favorite给到函数调用的位置。那接下来我们要return的是不是绝对得是最新的favorite这个对象。那最好最好咱们这样一下DB refresh一下，是不是确保拿到的是最新的数据库里边返回的这个最新的favorite ORM对象，那拿到最新的了，最终在return到函数调用的位置，那我们是不是就拿到了这个ORM对象里边。

发言人   07:22

比如说包含ID用户ID新闻，ID创建时间等等等，是不是好OK，那我们I的数据库新增的这个方法是不是已经搞定了？那允许我把这个函数，尤其是参数顺序这里截一个屏钉在屏幕上。那我们对照着这个参数顺序到路由处理函数当中来调用传参是不是就OK了？

发言人   07:43

来回到咱们的root里面来，那我要调用方法是不是我await一下，调用的是favorite里面的I的这个方法。那传的参数，数据库绘画依赖项是不是要着？那我们还要一个user ID，那是不是就是咱们user里面的ID，user点ID，那我还要传新闻ID那是不是就是咱们的请求体参数data里边有news ID给他传过去是不是就OK了。但是这儿我重新写一下data点儿，妈呀对，data点儿，news ID它一下。好OK那接下来我这一条一旦执行了，是不是会拿到这个方法返回的那个favorite，就是那条新增的对象，那我给它存一下，比如说我就叫result RESULT给它存一下，那我就直接把这个result填到data里面了。

发言人   08:39

大家可能会纠结，就是我们这个ORM对象里边，咱们数据库表里边存的这个create at是创建时间。但大家还记得就是我们的接口文档要求的是create time。但是我说一下，这儿我就直接return这个result了。因为我们在新闻详情界面并没有展示收藏信息的那个位置，所以我在这儿就不管这个create at和create time这两个属性名不一致的关系了，那我就直接把这个result给它响应回去了。也就是说我直接这样写，data等于result SULT。好，接下来我保存一下。那接下来咱们就要看一下能不能真的去通过点那个小星号去添加收藏，来回到咱们的浏览器里边来，允许我先刷新一下。

发言人   09:34

第一条新闻是不是手动收藏的，我们来看第二条新闻，默认是不是没有收藏，我点了，大家不要眨眼，见证时刻的奇迹点成了好，添加收藏是不是成功了，那接下来咱们再试一个。第三条新闻点是不是又成了，添加收藏没问题，当然你再点不管用，因为再点是不是取消收藏，咱又没写功能，是不是？好，那接下来添加收藏成功了，那我们检查一下那个favorite表是不是有三条新闻被收藏了。来回到咱们的这个拍产品当中，来找到favorite表，刷新一下，是不是三条新闻。当前是22这个user ID收藏了1233条新闻。好OK那咱们的收藏添加收藏的功能就实现了。这个接口的功能相对来说比较简单，大家要留意。

发言人   10:37

因为我们的请求方法是post，所以这个news ID是不是得定义为请求体参数。咱们是不是先定义好了python单体的模型类包含news ID，那这样的话咱们的news ID参数是不是就搞定了？搞定之后，我们封装CRUD的方法，其实是不是就是先用模型类构造出一个。对象接下来把它add增加到数据库，是不是就OK这个是咱们添加收藏的实现思路。

发言人   11:07

接下来该同学们的表演时间，大家把自己的这个添加收藏功能来实现一下。另外实现完之后不要走开，能添加收藏，还能取消收藏。咱们下一小节不见不散，一块儿来实现取消收藏的功能。