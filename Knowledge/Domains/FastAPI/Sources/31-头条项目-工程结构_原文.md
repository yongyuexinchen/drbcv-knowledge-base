31-头条项目-工程结构_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，咱们马上要做项目了，那开发项目第一件事是干啥？先创建项目是不是？这一节咱们一块来创建项目，搭一下项目结构。所谓的搭建项目结构其实就是根据功能把代码拆分到不同的文件夹里面，这样方便咱们去维护和扩展。那具体该怎么做呢？首先咱们得创建fast API项目，是不是？那一会儿咱们比如说项目起名就叫头条back end，头条后端的意思，那创建项目了是不是帮咱们会生成一个main点PY它是啥？主入口文件，一般就是创建发丝和API实例，整合路由或者是配置啥的那其他的功能代码咱们就要拆分了，拆分到不同的文件夹里面。

发言人   00:50

那具体怎么拆分，咱们从上到下来看一下。首先最上是CRUD，CRUD就是增删改查的意思，那存啥呢？存的就是数据库增删改查逻辑的代码。那就比如说将来咱们做新闻是不是得查新闻？作为用户模块是不是得查用户修改用户信息，那未来模块不同，咱们新建不同的PY文件，存储数据库增删改查逻辑的代码，这是CRUD，那我们顺序再往下还有一个models，model是不是模型的意思？那保存就是定义的数据库模型类，那还是一样。有新闻那我就有一个新闻对应的模型类文件。

发言人   01:35

有用户模块那我就要有一个用户模块对应的模型类文件，这个是models，那再往下rotors rooters是不是路由的意思？那路由保存的就是路由文件了。那将来我们的接口会有很多，我们会按功能拆分路由。比如说我有新闻模块，那就有新闻模块对应的路由文件。我有用户模块，那就要有一个用户模块对应的路由文件，以此类推。

发言人   02:04

好，那看到了reuters了，顺序再往下还有一个steamers scala，MERS是原型的意思。这里放的就是数据验证模型，就是python单体格类型，用来校验数据格式的。那么未来哪些数据格式需要咱们去校验，那就放到schemers文件夹里面就可以了。

发言人   02:25

好，接下来再往下youtube，youtube是工具的意思，这里放啥？放工具函数，就是一些封装的通用的功能，用来全局调用的。就比如说后边咱们做项目的时候，好多地方都要验证用户是不是登录了。那这个逻辑要重复使用，我们就可以把这个用户认证的代码抽取到u tus里边来，将来全局调用，就是整个项目每个文件里边都能调用它。那具体该怎么写，等咱们讲到用户认证的时候再来说。

发言人   02:59

好，那接下来再往下还有一个config，config是不是就是配置的意思？那这里边保存的就是配置相关的，像数据库的配置，缓存的配置都可以放到config里面来。这个就是咱们企业开发当中项目的工程化思维。咱们按软件功能拆分代码，每个目录都有自己明确的职责，这样方便咱们项目维护扩展，团队之间配合也会更轻松。

发言人   03:25

了解了项目结构的组成了，接下来咱也到拍top当中搭建一下咱们头条项目结构。我们回到pyo m里边来，首先点右上角new project去新建项目。来我们点一下，接下来到项目配置文件这个环节，那我们怎么选？先选择框架fast API，接下来浏览项目的存储位置以及项目名称。好，那这个项目存储位置不变了，我们把名字改一下。咱做头条后端，那我就头条下划线拜克摁一下，头条后端。好，那名字有了往下，切记是不是选择一个虚拟环境。

发言人   04:08

选完之后，我们往右下角点create创建这个按钮。那这个时候我们稍等一下，注意这一步，是不是等待联网下载安装fast API以及相关的服务，那我们稍等一下下。好，各位同学来看，我们现在有了这个main点PY，那说明我们fast API项目是不是已经创建完了。那我们可以运行来试一下。

发言人   04:31

来我们点运行，稍等一下。项目是不是跑起来了，给了我们一个链接。那接下来按control键，点左键来看，能看到hello word，说明咱们的项目是不是没问题。那接下来咱们要做的就是继续回到py COP里面来，是不是把咱的这个功能模块都给它搭建一下。

发言人   04:52

好，那接下来咱就开始了。首先切记点着头条back end的这个根目录咱去创建。那创建的文件夹名字咱参照右边了，咱就自上而下按顺序来了。首先咱们来个CRUD，点着头条根目录右键new选择文件夹来确定一下。那我们来个CRUD，这个是不是就封装的是数据库增删改查相关的代码，那我们确认一下。注意至于里边的什么news点PY user点PY咱们现在先不创建，等我们做到那个模块再来去对应创建就可以了。

发言人   05:33

好，那CRUD有了，我们继续往后，右键去新建一个文件夹，往下是不是就是models模型类MODELS models，那模型类的也有了，再往下新建一个文件夹，该rooters是不是路由层，将来我们的路由也都要拆分，ROUTERS销毁者就我们这一步，只新建项目目录以及这个对应的功能，文件夹，里边的文件回头再说。好，那接下来我们继续，右键去新建一个文件夹，该谁了？Skimmers模型类是不是SSCHEMAS？好，敲回车。好，那接下来再往下还有没有？还有utils是不是工具，utils工具函数。

发言人   06:20

好，那再去新建一个文件夹，还有一个啥配置相关的是不是config？好，那我们敲回车一下。注意，先新建好功能代码保存的目录，文件夹就行。用到啥将来咱们找对应的位置就可以。这就是咱们企业当中开发项目的工程化思维，把代码按照软件的功能拆分到不同的目录，每个目录有自己明确的职责，这样的话方便咱们项目的维护和扩展。同事之间去配合工作也会变得更轻松。想要找哪个功能的代码就去对应的文件夹，是不是就可以了？

发言人   06:58

好，那现在我们一起搭建的项目结构是不是完事儿了？我在这儿就要考考大家了。首先问大家，来这回按我的这个拍场里面的顺序，打乱一下。Config是干啥用的，存的是不是配置相关的，将来数据库缓存的配置是不是往这个config文件夹里面存。那再往下CRUD看封装的数据库增删改查逻辑的代码是不是往CRUD里边存。Models对模型类rett SI拆分出来的路由schemers，对python单体类型做数据校验用的ut工具函数封装的通用的逻辑，对不对？ENV骗不了大家，是不是？ENV是虚拟环境。

发言人   07:47

好，那现在咱们的头条后端的项目结构就已经搭建完了。重点是大家要对应起来每个文件夹存放的是什么功能的代码。将来我们可以写到代码了，就找到对应的文件夹来保存，是不是就可以了。好了，我们一块儿搭建了项目结构了，接下来大家是不是也要有一个自己的项目结构？接下来大家来动手把自己的项目结构也来搭建一下。