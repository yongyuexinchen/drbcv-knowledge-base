57-头条项目-收藏模块-总结_原文

2026年07月21日 12:53

发言人   00:01

Hello, 各位同学，截止到现在，咱们关于收藏模块的核心接口就已经开发完了。那我们一块来总结提炼一下这一章节的内容。首先咱们收藏模块涉及到的主要接口有检查收藏的状态，还有就是添加取消收藏。另外还有获取收藏列表，清空收藏列表。我们在实现这些接口的过程当中，涉及到两个新的知识。一个是ORM连表查询的写法，另外一个就是咱们ORM模型类当中创建索引的时候，引入了一个新的参数。我们要知道这个参数的作用。好，那我们一个一个来。

发言人   00:40

首先关于联表查询，咱们在课程当中是不是就是查询news和收藏表，这两个表联合起来查询出来我们要的新闻信息和收藏信息。那怎么做到的呢？来注意联表查询仍然是select联合哪个表呢？我们要添加一个join方法，那select的小括号填的是查询主体模型类，我们要查的主体是不是就是新闻，因为我们收藏的是不是就是新闻，所以查询的主体模型类我们就写news，新闻表对应的模型类是不是就OK了。当然还允许逗号隔开，把字段重命名。

发言人   01:24

那大家回忆，我们在课上，因为新闻表跟收藏表都涉及到ID字段，都涉及到create这个字段。那么这两个字段名字是一样的，到时候结合起来的话，是不是分不清哪个create艾特属于的是哪个表了？那就乱了，所以怎么办呢？我们可以把其中的一份重命名，那写法就是模型类点字段名，点label写别名是不是就OK了。

发言人   01:53

好，那么另外join我们说join的小括号要先放上join的模型类。咱是不是join就是那个收藏表，所以把收藏表对应的模型类是不是放在这儿了。当然那join的条件是啥？那咱是不是就看看两张表里边都有新闻的ID，那我们就找到这两张表里边新闻的ID是一样的，把它查出来是不是就OK了。好，那么这是连表查询，select填查询的主体模型类join填联合的模型类是不是就OK了？

发言人   02:27

好，那连表查询搞定之后，再有就是咱们模型类里边创建索引的时候，有一个新的字段叫unique e concentrate，它是什么作用注意唯一约束。不知道大家还记不记得，咱们的收藏表里边创建索引的时候，我们加了这个参数，为啥要有唯一约束呢？大家想一个账户对于一条新闻是不是只能收藏一次，那我要收藏多次是不是也不合理？所以use ID news，ID要有唯一约束。这个唯一约束是咱们原来没有写过的，大家在这儿留意一下。

发言人   03:13

这个两个，一个ORM连表查询，一个就是为约束。这两个是我们的新知识，大家也来记好笔记，方便以后去学习。这个是咱们整个收藏模块涉及到的两个新的知识。至于其他，是不是就是按照接口文档的要求来开发接口就行了。

发言人   03:32

我们每次开发接口，这个流程是非常相似的。都是先有模块化路由，然后我们定义模型类，方便我们通过ORM的方式增删改查操作数据库里边的数据。另外封装好了CRUD方法，在路由处理函数当中去调用拿结果响应结果是不是就OK了。咱们的新闻模块、用户模块到现在的收藏模块接口的实现流程，是不是基本都是按照这个流程来的？我们后续开发其他的接口，也会按照这个流程来实现。好了，这个就是咱们收藏模块涉及到的核心内容，带着大家来回顾一下。收藏模块完成了，大家整理好自己的笔记跟代码，下一小节儿我们开始另外一个模块喽，下一小节儿不见不散。