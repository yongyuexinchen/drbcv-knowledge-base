37-头条项目-获取新闻列表_原文

2026年07月21日 12:49

发言人   00:01

Hello, 各位同学，这一节咱们来开发获取新闻列表这个接口。这个接口的核心功能就是查询数据库里边的新闻，响应新闻列表给前端，最终前端界面里边能展示多条新闻就可以了。那我们要实现这个接口功能是不是得有路由，还要通过ORM的方式查询数据库里边的新闻，然后响应结果给前端。那大家想这个接口的实现方法是不是跟我们之前写的那个获取新闻分类基本上是一致的？先定义基础路由，然后要有模型类，接下来封装查询列表的方法，最后在路由当中调用查询方法，响应结果是不是就OK了？

发言人   00:45

好，那我们先从第一步基础路由开始入手了。那写路由咱是不是得先分析一下，根据啥来写路由呢？对API接口规范文档，我已经在PPT里边准备好了新闻列表这个接口的要求了，我们一块儿来分析一下。我们来到PPT首先获取新闻列表，从最上看接口地址，get方法前缀API news路径是i list。我们写装饰器路径直接只写一个list是不是就可以了。

发言人   01:19

好，那有了接口地址跟方法了，下一步参数咱们这里需要三个参数。第一capture ID是不是就是分类ID好，那配置就是页码，默认值是一，代表默认要的是第一页的数据。好，再有配置size，配置size是每页显示的新闻数量，默认值是十，那最大值是100。好，这是三个参数。

发言人   01:45

接下来再往下就是响应的结果示例code message仍然要还有data。Data是不是我们真正前端要用到的数据？这个数据里边list是新闻列表。当然除了list它还有两个。注意了看啊，total是当前分类下新闻的总数量，再往下还有一个孩子们，它的值是个布尔值，要么true要么false，那这是干啥用的？作用就是前端界面篇幅有限，但如果新闻太多展示不全咋办？如果haze mode值是true，允许滑动加载更多好，那这个是咱们响应结果的重要数据，新闻列表总量和是否还有更多好。

发言人   02:33

看到了接口要求了，我们是不是就可以推导出来基础路由的写法了？来我们看路由，首先装饰器，root get路径只写list是不是就可以了？因为前半部分我们是不是已经放到模块化路由前缀里面了。

发言人   02:51

好，定义方法这个方法里边三个参数分类，ID页码和每页的数量。但在这儿我稍微提示一嘴，就是咱python的要求是函数的参数是全小写，但我们跟前端约定的参数名character ID配置size是不是驼峰式的？这个I跟S都是大写，那这种情况怎么办？也好解决。我们这里是query查询参数，那我们导入query允许加a liars设置别名，别名是我们真正要的，这个驼峰式的参数名是不是就OK了？

发言人   03:30

搞定了参数了，下一步响应结果。Code message data，data里边有新闻列表总量，还有是否还有更多新闻。这个是根据咱们的API接口规范文档推导出来的基础路由的写法。各位同学OK吗？OK的话，咱把这个基础路由先给它搭建出来。

发言人   03:51

好，我们来到拍场当中来，首先咱要写路由，大家帮我找个地儿，reuters里面需要新建python文件吗？不需要，因为咱们现在做的是新闻模块，还往news里边放，是不是就可以了？好，那接下来咱们先从装饰器开始入手，来小助手上线，直接让他执行了，采纳他的建议。Rotor get路径是list，是不是OK没问题。

发言人   04:20

好，那a think定义一个方法，a think DEF我就叫get news list了。来，接下来注意小助手提是我参数是不是也有了，这参数有的能用，有的不能用，那我直接tap咱们改一下。那这里允许我敲个回车，这样咱们看的更清楚一点。好，再走，这里还有一个打个冒号，我们来一个pass。

发言人   04:47

好，接下来大家帮我，咱们从最上一个参数来改写。首先factory ID是不是这是python的函数参数，全写，但我们前后端约定的是驼峰的，那我们说怎么办来着？查询参数咱们导个query进来，那也就是这里我来一个query，那往下等号赋值query，我来一个必填，重点是我要有一个a liars别名，我的别名是驼峰式的factory ID好，那参数一搞定了，下一步这个skip大家看看需要吗？不需要，我们来一个配置，是不是页码？那想要第几页数据，默认是第一页。好，那再有再来一个page size，那page size是不是要有默认值，也要设置别名，所以我再用咱们的query，默认值是十，然后liars是不是来一个别名，是配置size。另外最大值是不是100？那最大值就是小于等于，那我来个LE，来个一百。

发言人   05:59

好，那咱们每页的数量是不是搞定了？接下来再来DB数据库绘画这个依赖项是不是也要把它注入进来。好，那现在咱的参数是不是就搞定了？那搞定之后，这个pass我干掉了，咱们来一个return是不是响应结果，那我直接采纳建议了。

发言人   06:19

Code需要message，需要这个data咱们不要了。来一个对象，我们要的是不是列表总量和是否还有更多？好，那咱自己写，list那写死，这个是新闻列表。

发言人   06:34

好，下一个是啥？是不是就是那个总量，那就是TOTAL冒号，我们写死，这是总量。好，那再下一个是是否还有更多，对不对？Has more，来这个是否还有更多？那现在咱们的基础路由是不是就写完了？接下来咱们到交互式文档里边去测试一下，看看能不能拿到这个固定的响应结果。好，我们来到浏览器里边来切到交互式文档，先刷新一下。现在是不是有一个list列表这个接口了，我们点一下try IT，注意来看这里是不是有character ID那假设我来一个一分类ID我来一个一，接下来excute执行它，那我们看响应结果是不是正常拿到了这个写死的响应结果了，那说明咱们的模块化路由是不是已经OK搞定。

发言人   07:28

那搞定了这个路由了，下一步咱是不是要查询新闻了，那咱是不是得先有新闻的ORM模型类才可以。好，这个模型类提前跟大家说新闻需要的字段太多了。为了节省时间，我提前准备好了news模型类。大家来看，表明是news，那么对应的字段来看看这么多，其实这个代码咱是不是老早就会写了，所以我们就直接复制走了，大家也可以在我提供的素材里边来复制。

发言人   08:04

好，接下来我点一个复制，回到咱们的拍场景当中来找地方是不是要粘贴模型类。我不会，往哪儿放？Models里面的news里面，我们双击打开。好，打开之后我就往最后了。最后这个位置我们control v来粘贴一下。粘贴好之后，他有的地方报错，那没关系，我们倒包就是了。

发言人   08:32

首先从最上import咱们的这个circle alchemy里边的index。首先这一句是干啥用的？大家来看，我加了注释是不是创建索引提升查询速度。那这个索引是咋回事儿？我稍微解释一下，这里的索引就相当于把一本书添加了目录，添加了目录，那有目录按目录去找内容是不是速度更快？好，那我们把啥字段插入到目录创建了索引呢？来看分类ID还有发布时间。我们这里调用了index方法，参数一是索引名儿自定义的，参数二是我们要创建索引的字段名。

发言人   09:18

那我这里是不是把分类ID还有发布时间创建了索引？为什么这么多字段选中，分类ID跟发布时间，我说一下，这是咱们做新闻资讯类的一个经验。因为我们按分类ID去查询新闻，是高频查询操作。那么像这种高频的查询，我们去创建索引就不会进行全表搜索了，而是直接定位到captain ID去搜索，是不是速度会更快？

发言人   09:51

好，另外这个发布时间，为啥把发布时间也去创建索引呢？因为很多时候咱们查出来新闻之后，可能会按照这个发布时间进行排序。如果我们不去创建这个索引，会全表去排序，那速度就会慢。所以结合着真正企业开发新闻项目的一个经验，我这里也是把分类ID是高频查询的场景。还有就是这个发布时间，它是一个按发布时间排序的一个刚需场景。所以我就把这两个创建了索引，这样我们的速度会更快一点。好，我在这里打一个小注释，这个是高频查询场景。

发言人   10:40

好，再往下publish time为什么也创建了索引呢？因为这个是咱们的按发布时间去排序，也是咱们新闻系统的刚需，所以这两个我就插入了索引。好，这个索引大家大概知道之后，其实就是提升查询速度，那我们继续往下，像ID title就不说了，description这里有一个optional，它是可选，那我们也要打包给它导进来，导的是typing里面的optional。好，接下来再有就是tax这个类型，倒班来自于circle alchemy里边的tax。这个foreign k就是外建，那我们也把它倒包倒进来。好，那接下来咱们news这个模型类是不是就OK搞定了，没有任何的报错了。

发言人   11:31

好，搞定了news模型类我们就可以干啥了。查库是不是响应结果了。提前跟大家说，咱们这个新闻列表的接口响应的结果有点复杂。那我们回到PPT当中，带着大家去分析一下整个查询和响应结果的流程，我们来到PPT里面，首先明确目标，我们要的数据有新闻列表总量是否还有更多。一会儿我们查询也好，响应结果也好，是不是都要奔着这三个数据努力。那具体该怎么做呢？来咱们一块儿来分析一下。

发言人   12:06

首先子弹进入请求，咱是不是就携带了三个参数分类，ID页码和每页的数量。我们的第一个目标是不是要有新闻列表，把新闻列表响应给前端。那列表咱们要先做分页的处理，因为将来我们的列表是不是要依赖于分页查询？就是offset跳过的数量，limit就是每页的数量。那么每页的数量是不是就是配置size这个参数，但跳过的数量咱是不是要提前处理一下？

发言人   12:40

跳过的数量怎么计算？我要第二页儿，那就跳过第一页的数量。我要第五页，前四页是不是都要跳过？所以跳过的数量是不是就是页码减一乘上每页的数量就可以了？

发言人   12:55

好，那有了跳过的数量了，分页查询是不是就能搞定了？好，那我们的第一个数据，新闻列表这个数据是不是搞定了？好，那搞定之后继续往下就是总量，总量怎么获取呢？注意聚合计算大家还记得吗？Function点point一下是不是就OK了？

发言人   13:16

好，那总量也有了，接下来是不是有一个是否还有更多新闻？这是为了前端界面要不要加下拉功能？好，那怎么去计算是不是还有更多呢？注意，跳过的数量加上当前列表的新闻条数，是不是就是现在我们有的新闻条数，看看是不是小于总量，总量是不是就是前一步这个总数量，如果小于，看说明是不是还有更多，如果不小于，那就还没有更多了，对不对？就不需要再加下拉功能了。

发言人   13:53

好，那现在咱们的新闻列表总量是否还有更多？这三个数据是不是集齐了，集齐之后就可以干啥了？对，响应结果给前端是不是就OK了？

发言人   14:06

那这个是咱们新闻列表整个查询响应结果的实现思路。各位同学这个思路大家OK吗？OK是不是？接下来咱就得来试一下喽。回到咱们的拍场当中来，首先允许我先来到陆游里边一趟，这个路由里边允许我加一点注释，我们把刚刚的思路是不是来整理一下。我首先要处理这个分页的规则，然后接下来干什么，是不是查询新闻列表，然后干什么计算总量计算计算总量，然后去计算是否还有更多，那最后return响应结果是不是就可以了。那这个响应结果我就不写了，因为我这return已经有了。好，那接下来咱们线上边这个思路是不是就OK了。

发言人   15:02

我提前跟大家说，查询新闻列表是不是属于数据库的增删改查操作，那我们一会儿是不是封装到哪里ICRUD好，另外计算总量是不是也要查询数据库里边新闻的总量，所以还是数据库的增删改查，我们把它放到CRUD里面去。至于分页规则跟计算是否还有更多在咱们当前路由里边处理就OK了。好，接下来咱们先把CRUD这两个方法来封装一下。

发言人   15:33

好，跟着我来到CRUD news里边来，定义方法是不是先把这个列表给它获取一下。首先数据库操作咱们肯定是异步的那我来一个DEF，咱们就是get news list了。好，那我直接tab一下。

发言人   15:50

首先DB是不是我仍然需要？好，那下一步我还要有其他的参数，比如说character ID我需要，另外还要有skip SKIP是不是跳过的数量？那这个跳过的数量我来一个默认值，比如说默认值我来一个零，那另外这个配置size我给它改一下，我就叫limit了。那limit我也来一个默认值，比如说我要的就是每页十条数据，那接下来参数搞定了，那我们往下来写功能，这个功能问大家是不是要查指定分类下的所有新闻？我打一个注释，他要查询查询的是指定分类下的所有新闻，是不是要干这件事儿？所以要查询，查询带条件是不是就是等于category ID的？

发言人   16:50

好，那接下来我来写代码了，那我来tab一下，注意来看select查询where是不是带条件。这里他给了我一个news，是不是应该是模型类。那咱那个模型类是不是也确实叫news？那我们直接导入了，来检查一下，从models点news PY里边导入news这个模型类，那我们select的是不是也是它好where news里边的character ID如果等于用户指定的分类ID那满足这个条件的是不是要找出来？那意思是不是就是查询的是指定分类下的所有新闻了。

发言人   17:28

当然我们是不是要做分页查询，那就点offset指定跳过的数量，点limit指定每页的数量是不是就可以了。好，这一个语句有了，那我们是不是要数据库来执行？所以a wait DB excute执行上边这条语句，是不是会得到一个ORM对象？基于这个ORM对象，我们是不是scale as点2来提取所有的结果就可以了。

发言人   17:56

现在咱们列表的查询是不是已搞定了？那搞定之后，我们先不着急往下写，咱们来试试这个列表能不能查出来数据。好，那咋办？注意我这里是不是封装了一个方法了，允许我来截屏。这个方法是不是要在咱们的路由里边给它调用起来？来，我把它截屏定在这儿，那回到咱们的reuters里面来，那我在这里是不是要调用，那是不是就是我await一下数据库操作，await一下news里面的get news list，那允许我先tab一下。

发言人   18:32

首先数据库是不是要传进来？好，那接下来分类的ID是不是也要传下来？这个分类ID是不是就是上边这个查询参数？好，那接下来在这里，这个是不是就是咱们skip的值。那这skip值咱们别直接在这儿写，是不是提前把这个分页的规则给它来一个小变量存一下？好，那我把它剪切一下，那我就叫offset好了。等号赋值粘贴一下配置页码减一乘上配置size是不是就OK了。那我们参数三的位置换上offset。好，那接下来最后一个参数，每页的数量是不是配置size要传到limit这里。

发言人   19:16

好，那数据库查询的这个方法调用起来了，那是不是能拿到return这个返回值，那我们拿一个变量把它存一下，比如说我就叫neos list给它存上。那存上之后下一步我们响应结果list这里就不要写死的新闻列表了，直接把news list这个变量给它放到这儿。接下来我们保存重点来了，见证时刻的奇迹，我们是不是去看一下能不能拿到一个新闻列表。好，那我保存一下，回到咱们的浏览器里边来，切到交互式文档。那云允许我重新提交一次，来执行看看有没有数据，有，且是我们指定的这个character ID1。比如说我们换一个，我指定ID是二来excute执行，来看看查查到的数据是不是character ID就是二了。好，那现在咱们的列表新闻列表是不是已经查到了。

发言人   20:14

那下一步还有下一个数据是不是计算总量？说这个计算总量也是属于数据库里边的查询，那我们是不是也给它封装一下？因为主要我这担心还有其他的位置要用到。好，那我们回到CRUD里面来，get news list，它是获取所有新闻的那我们再来一个总量，那a think数据库我们就异步操作了，DF get比如说我起个名字，咱们叫get news COUNT point是不是数量的意思？好，那接下来我tape一下，注意数据库绘画这个依赖项是不是仍然要，那接下来character ID要不要也要为什么呢？允许我加一个注释，我们这里要查询要查询的是指定分类下的新闻数量，所以我们这个character ID分类ID是不是得要着？

发言人   21:09

好，那接下来参数搞定了，我们往下来看查询的语句。首先select news是不是就不行了？我们要改一改，因为这里我们要做聚合计算，那是不是就是FUNC点儿point一下是不是才可以？当然FUNC需要导包，我们鼠标放上来。Import circle alchemy里边的function。好倒包进来那point的小括号里边，我们查询查哪个字段呢？咱就查那个ID了，因为咱news ID是不是唯一标识？好，那我就来news点儿ID了。

发言人   21:45

好，那聚合计算写完了，当然后边外带条件是不是就是看看表里面的哪个ID跟用户指定的分类ID是一样的。是那是不是我们就给他查出来，那这是查询的语句已经写好了，那下一步是不是DB数据库绘画要执行它，执行之后会得到一个ORM结果。接下来基于这个结果我们来提取数据是不是就可以了。

发言人   22:09

那怎么提取？在这里注意return我们删一下，基于result点儿，我们在之前讲课的时候是不是用了一个sky了，是不是来提取这个聚合计算统计出来的结果，那OK我们还用skyline也没问题。好，接下来我给各位同学扩展一个新的叫sky了。One one是不是一个？那这个scale one表示啥意思？我打一个注释，它是只能有一个结果，注意，只能有一个结果，如果是零个或者是多个，它就报错，否则就报错。但是原来我们用过的那个scala，它是统计数量多个结果也行，它不报错，但scala它就返回一个。

发言人   22:58

所以这个时候，万一一数据库里边是多个数据的话，死掉了是不是不会报错？那我们也不知道在业务上会不会有啥问题，对不对？像scale one只能有一个数据库里边真的有一个结果，那OK没问题。但如果是多个直接报错，那我们是不是就知道或者数据库，或者是哪里的业务是不是设计的不合理了？所以在这儿给各位同学扩展1 sky one。当然在咱们这里，因为数据库铁定没问题，业务设计也没问题，用skyline也OK。

发言人   23:29

好，那现在我们的计算总量这个方法是不是封装完了，允许我截个屏盯一下盯一下。那我们是不是要到路由里边把它调用一下？好，那我们就顺序写，这是不是新闻列表，那我敲个回车，我们是不是要直接type了，news里面的get news point是不是要调用起来？那同样数据库绘画依赖项是不是要传进来？同样capture ID是不是也要拿到？调用了方法得到结果是不是存到了total这个变量里面？那接下来总量这个位置我就放total变量名喽。

发言人   24:09

好，现在搞定了总量了，咱们再来交互式文档里边看看有没有。来回到咱们的浏览器里边来，上一次请求的结果我还没有刷新，我先滚动到最底下给大家看看我们现在的前边儿的是不是写死的总量。如果我重新提交一次，重新提交一次，接下来大家来看，往下走，妈呀话多了，偷偷变多少了，50亿了，说明查库聚合计算是不是已经欧耶成功了？好，那现在咱们的总量也搞定了，是不是还差最后一个啥来着？这个是否还有更多，是不是？好，那回到咱们的代码当中来，那是否还有更多咱就直接在路由里边写就可以，那怎么去做呢？那我说一下，就是跳过的，加上当前列表里面的数量，我们去看看，我写个括号，我们去看看它是不是小于总量，是不是小于总量就可以了。

发言人   25:22

好，那帮我把这一句转换成代码，我先写一个括号，怎么写跳过的是不是of that加上当前列表里面的数量，我们有一个棱方法，是不是可以统计列表的长度，那我直接用上了，来小于总量，总量是不是就是前一个变量total好在这儿，小于total那是不是会得到一个结果？那我存一下，has下划线M给它存上好，那现在是否还有更多也搞定了，那写死的字符串，响应结果这里写死的字符串干掉换成变量。孩子们，接下来我们保存一下，回到浏览器里边来重新提交来试一下，来往上往上提交一次。接下来我们滚动到数据的最底下，现在是不是有一个处了？好，那我们来一个force的情况来看一下，我们总条数是51条，我给他来一个这样配置页码，我要第六页是不是就一个数据？因为我每页数量是十个，好执行。那我们来看一下第六页儿只有一条数据，是否还有更多呢？False是不是没有更多了？那现在咱们新闻列表这个接口要求的list total has more是不是全搞定了？

发言人   26:45

那搞定了之后，见证时刻的奇迹，我们是不是去前端界面刷新来看一下？来到咱们的前端界面，首页执行一下，稍等一下下。大家来看耶新闻列表是不是已经有了，那下滑是不是也有？如果大家有耐心，你就往下滑，滑到最底下就不能滑了。好，那咱们的新闻列表这个接口就搞定了。

发言人   27:14

那我们回到代码当中，带着大家总结一下重点，来回到咱们的代码里面来。我们来到陆游整体来看一下思路。我们的目标是要有列表，有总量，还有一个是否还有更多。所以为了能查出来列表，先处理分页规则，处理完分页规则是不是就能做分页查询了。然后接下来计算总量是不是就OK。那计算总量大家还记得吗？其实是不是就是一个function point聚合计算就OK了。那有了总量了切回来，我们最后再计算一下是不是还有更多，是不是就能响应结果了。

发言人   27:53

这个是咱们这个接口的实现思路。当然我们在实践的过程当中，涉及到有新的知识点。比如说刚才的CRUD里面，我们是不是新学了一个提取结果的scale one，只能有一个结果。如果你给我零个或者给我多个，是不是他直接报错，他跟那个scale是不是就不太一样了，scale它不报错。好，这是一个新的知识点。

发言人   28:17

另外在咱们的models里边是不是还有一个索引，我们说这个索引它就相当于添加了一个目录，这样的话咱们查询的速度是不是更快。同时也跟大家介绍，我们新闻系统的话像按分类ID来查询，它是高频查询场景。所以这个分类ID我们给它加了一个索引。另外就是发布时间，通常会按发布时间来排序，所以为了不全表来查询来排序，所以我们把这个发布时间是不是也创建了一个索引？这个创建索引，它属于咱们项目性能优化的一部分。好，那现在咱们一块儿把获取新闻列表这个接口就搞定了。接下来时间就交给大家了，大家也把自己的新闻列表这个接口来搞定一下。

发言人   29:07

实现的流程是不是先写路由，然后定义模型类。这个模型类咱早就会写了，所以大家可以直接在素材里边来复制。那接下来在CRUD文件夹里边来封装咱们的列表查询，还有总量计算的两个方法。接下来这俩方法在路由当中调用响应结果是不是就可以了。接下来大家来动手把自己的获取新闻列表这个接口来实现一下。实现完之后不要走开，下一小节咱们来获取新闻的详细内容，我们下一小节不见不散。