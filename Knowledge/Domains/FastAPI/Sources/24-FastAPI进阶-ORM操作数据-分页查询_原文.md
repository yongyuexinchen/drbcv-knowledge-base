24-FastAPI进阶-ORM操作数据-分页查询_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，这一节咱们来学习ORM查询当中的分页查询功能。分页查询其实就是用户指定他想要的页码，是第二页还是第三页，那每页需要多少条数据？用户指定了页码和每页的数据量了，接口根据用户指定的去查询，然后响应数据给客户端。

发言人   00:24

分析是查询该怎么实现呢？来看select查询是不是offset limit。这个limit最好解释了它是用户指定的每页的记录数，就是每页有多少条数据。那offset他说是跳过的记录数，那什么叫跳过的？比如说用户要第一页数据，那第一页假设每页我需要十条，那第一页需要跳过吗？不需要，是不是从第一个数据开始响应，给客户端一共享用十条就可以了。

发言人   00:58

好，那假设举例子，用户如果要的是第二页数据，每页十条，看需不需要跳过，需要他需要把第一页的数据是不是都跳过，也就是跳过十条。那以此类推，假如说用户要第三页每页要展示十条，那要不要跳过呢？要跳过多少呢？对，跳过的是不是就是前两页的数据量了。那也就是说在开发当中的分页，这个用户要哪一页，是不是用户指定的？

发言人   01:31

用户要每页显示多少条数据，是不是也是他指定的那需要咱们程序是不是去算出来这个跳过的数值到底是多少？那跳过的该咋算呢？我们发现是不是永远把前面页数的给跳过。那这里我整理一个公式，大家来看一下，跳过的值等于当前页码减1，是不是就是前面的页数乘上每页的数据量，这个limit值。

发言人   01:59

这个跳过的值的算法，各位同学OK吗？这个应该没问题是不是？就是把前面页数的数据是不是都给它offset跳过就可以了，这个是大致的计算思路，如果转换成代码该怎么实现呢？来咱们来看一下。首先分页查询功能重点在这一句，是不是select这一句offset是不是跳过的limit是不是每页的数量。那我们要是能把跳过的值跟每页的数量得到，这个select的查询语句是不是就能执行了。

发言人   02:34

那我们来看看怎么办。首先每页的数量我们是不是用户指定？用户想要一页显示十条还是20条，是不是它可以指定。所以这个每页的数量，我们让它作为一个路由参数让用户传递进来。那具体的值我们可以让用户去设置。

发言人   02:52

好，那么再有用户要第几页的数据，你是要第二页、第三页、第五页、第八页哪一页？到哪一页的数据是不是用户说了算？所以我们再来一个路由参数。比如说是配置是不是代表用户想要的页码。

发言人   03:08

那有了页码了跟每页的数据量了，那跳过的能不能算？那能吗？咱们前面这儿是不是有公式？那就是当前用户想要的减1，是不是得到前面的页数，乘上每页的数据量，是不是就得到了跳过的记录数。那有了跳过的记录数了，咱们的offset的值是不是有了limit的值，是不是用户指定的？好，那咱们这句select是不是就能执行了？执行完是不是就能提取结果响应给客户端了？这是分页查询的实现方式，我们想办法得到offset值跟limit的值，接下来去查询是不是就OK了。

发言人   03:47

好，了解了大致分页查询的实现思路了，接下来咱们写代码来试一下。我们回到py tum当中来，首先我准备好了一个路由，但核心的代码是不是一句都没有写，我先把这个pass干掉，各位同学分页查询那个核心的语句是啊是不是select，我们要查的是book表对不对？把模型类填过来，是不是点儿offset一下点儿limit一下。我们说这个limit是不是用户指定的每一类的数量，这用户指定的我们是不是可以设计一个路由参数。当然除了这个以外，那么为了计算出来跳过的记录数，是不是还得问问用户你要第几页儿？好，所以接下来咱们准备两个参数，那我把咱们这个参数都敲个回车换行了，这样看得更清楚一点。

发言人   04:39

好，首先比如说我来一个配置就是代码int我来一个默认值了，比如说当前我就要第一页好逗号隔开，我是不是还要有一个每页的数量，那我就叫配置size了，也来一个int等号。跟大家汇报一下，我的数据库里边是不是就六条数据，我要是分每一页十条的话，是不是也看不出来分一页功能允许我写小一点。比如说我初始值给一个三，那意思是不是就是每页有三条数据，但这个具体的是要第几页，每页有多少条数据，是不是将来用户说了算？那我这先设计一个默认值。

发言人   05:20

好，那这两个路由参数搞定了，那下一步limit是不是每页的条数，那我们就拿上配置size过来是不是就OK了。接下来关键点来了，outside是不是跳过的那这个怎么算？我们说跳过的，允许我打个注释，它是不是这个页码用户想要的这个页码减一再乘上这个每页数量是不是就OK了。那在咱这儿咋算？扩一下，扩一下页码是不是配置，咱们的参数是不是配置好，那减一是不是就是前面的页儿乘上每页儿的数量，每页数量是啊参数配置size，对不对？

发言人   06:06

好，那算出来之后，比如说我来个skip，跳过这个变量把它收一下。好，那offset的值是不是写skip就可以了。那现在我们这句slack的关键数值是不是都得到了，那我们是不是就可以去执行它了？那这语句稍微有点长了，我这么来，STMT拿个变量收它一下，然后接下来我就await让数据库DB点excute来执行这句。Select是不是就OK，那执行完是不是会给我们一个结果，那我拿result去收一下，那基于result这个结果我们是不是可以提取数据了？那分页是不是有多条数据，那我就scale as点out一下了，那是不是就拿到了书籍，那我存一下。比如说books，将来return这个books给客户端是不是就可以。

发言人   06:56

那现在我们的这个分页功能的接口是不是封装完了，那我们保存一下，回到浏览器当中来试一下，来，我先刷新一下，get book list，我try IT1下。默认是第一页三条数据，每页三条。那我就直接用默认值excute一下来我们来看看数据，果然是不是拿到了，来看一下三条数据，ID12，稍等往下是不是三好，那接下来现在第一页数据是不是没有问题，那我再看看第二页数据，那配置我给它改成2，接下来excute一下来看看分页儿ID4ID5再往下ID6是不是也没问题。好，那这个就是咱们的分页查询的功能。

发言人   07:44

那回到代码当中，咱们来看重点语句。大家告诉我重点语句是哪一行？报左边的行号，对，是不是83行，这句是重点。那我加一点注释来问大家，考考大家这里边offset是什么意？它是跳过的记录数，就是跳过几条数据是不是？

发言人   08:07

好，那再来limit，limit什么意思？微粘贴limit，它是每页的记录数，就是每页有多少条数据，是不是？其中这个limit是不是用户指定，用户想要哪一页是不是也是他自己指定。那唯独这个跳过的记录数是不是需要咱们自己算一下？跳过的是前面所有页的数据，那就是用户要的这一页减一是不是就是前面的，乘上每页的数量是不是就OK了。这是跳过的记录数的算法，那有跳过的了，有每一页的数量了，那我们这个select的查询语句就能执行，就能查询了。这个是咱们分页查询功能的实现方式。

发言人   08:51

那像后边咱们做到项目，比如说新闻，那有很多，咱是不是就会涉及到分页查询了？那到时候这个逻辑，我们还会再把它跑一遍，分页功能咱们一块儿实现了。接下来该同学们动手了，大家来动手，在自己的数据库表基础之上实现一个分页查询功能。实现完，大家把这个代码贴到咱们的评论区，评论区对一下答案。