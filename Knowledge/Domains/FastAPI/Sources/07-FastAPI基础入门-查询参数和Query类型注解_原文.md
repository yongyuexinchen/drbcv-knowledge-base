07-FastAPI基础入门-查询参数和Query类型注解_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，这一小节咱们一块儿来学习第二种常见的参数查询参数。那查询参数长啥样呢？注意查询参数不需要在路径上定义参数，直接给处理函数添加行参，那么这个形参就会被自动解释为查询参数了。而且这里是形参对不对？还允许等号赋默认值，那这个默认值也就是参数的默认值了。

发言人   00:29

那么有了查询参数了，咱们发请求的时候是不是就要传入参数值了？那未来查询参数出现在哪儿呢？注意了，是URL问号之后，拼接规则就是参数名等于参数值K等于Y6的方式。如果有多个参数，中间就用按的符去拼接就可以了。

发言人   00:54

在开发当中，通常查询参数是用来对大量的数据进行过滤、排序、分页等等操作。因为我们这里是不是要获取过滤之后的数据获取排序之后的数据，所以通常的请求方法就是get了。这样是查询参数的写法，只需要给路径参数是不是定义形参就行了，非常方便。

发言人   01:20

了解了查询参数的写法了，接下来咱们也到代码当中来试一下。回到拍top里边来，接下来咱们敲回车先明确一个需求，我们现在要完成的是查询新闻，但是我要有一个分页的功能，那这个分页功能我设计两个参数，就跟PPT里边一样，一个是skip，这个skip它代表的意思就是跳过的记录数。然后再有一个是limit LIMIT limit是啥呢？返回的记录数，那这两个参数怎么配合用？

发言人   02:03

假设举例子，如果我返回的记录数是十，是不是代表每页有十条新闻？好，那第一页是不是1到10这个新闻，那第二页是不是就是11到20了？那这个跳过的记录数，比如说第一页不需要跳过，默认就是零。那第二页需要跳过多少？还是不是把第一页的跳过，是不是就跳过十个？

发言人   02:30

大家现在先了解这两个参数，大概咱们表达的含义就行。至于具体的分页功能，咱们到项目当中去完善。我们这里就是构造两个查询参数，咱们去练习一下查询参数的写法。

发言人   02:46

好，了解了咱们的需求了，接下来我们敲回车，写路由。首先我们是不是要来一个艾特APP点儿请求方法，获取get是不是？那接下来路径，比如说我们就是获取新闻，对不对？但将来我分页是不是返回的是新闻列表，那我就来一个news下划线list。好，那我们现在请求路径是不是写完了？注意，查询参数直接写路径，是不是不需要在路径这里定义参数名？

发言人   03:16

好，那接下来向下敲回车，咱们写处理函数还是采用a think异步的DF，起个名字，get news，list小括号冒号一下。那小括号里边是不是给处理函数加形参，那这个形参就会自动解释为查询参数。那这个形参的话，咱们就直接抄需求了。Skip跳过的记录数，我们注解一下是int类型，逗号隔开，再来一个limit冒号注解为int类型。同时咱们说查询参数是不是允许设置默认值？那我们可以采用等号赋值的方式赋予一个默认值。

发言人   03:56

好，那现在咱们的邢参写好了，也就是查询参数是不是写完了。那复杂的逻辑咱们就不写了，直接return响应结果就可以了。我们来一个对象，咱就简单一点来skip咱们响应回来看看能不能拿到参数值。逗号隔开limit LIMIT，冒号值就是参数limit的值。现在咱们的代码写完了，我的项目也是运行的状态。那我们直接回到浏览器里边来看效果。

发言人   04:29

接下来我们刷新一下，注意来看news list这个接口是不是已经有了。那接下来咱们展开try IT来试一下。好，来看参数skip和limit是不是全有了，而且limit的默认值也是在这里的。那接下来咱们传入参数值来试一下。比如说skip咱们默认第一页就跳过零条，每页的条目数咱们就来十了，excute提交一下，接下来向下没有报错。

发言人   04:59

首先来看查询参数出现在了哪里，URL问号的后面，而且是参数名等号是不是参数值，也就是K等于Y6的方式。因为咱们有两个参数and是不是拼接到一起了。好，那现在验证了咱们查询参数出现的位置确实是UL问号之后。好，那接下来再低头来看响应结果，skip 0 limit默认的十响应结果也是OK的那以后开发当中，咱们对大量数据进行筛选，过滤了，咱们就用查询参数就可以了。

发言人   05:38

当然我们刚刚在写这个查询参数的时候，是不是冒号用python原生类型的方式做了类型的注解。那我们对比一下，回忆一下路径参数是不是允许用pass做额外的注解。那查询参数哦也允许做额外的类型注解，但只不过肯定不是pass了。他是谁啊？是酷睿，是query QUERY查询的意思。那我们也是一样，需要先从fast API里面导入，在原生类型注解的基础之上，等号赋值为query函数就可以了。

发言人   06:20

那么接下来咱们来试一下，首先导入fast API，这里我们逗号同样导入一个query QUERY，注意Q大写。好，接下来咱们做类型的注解。当然我们这里俩参数为了让各位同学看得更清楚一点，允许我敲一个回车敲一个回车来看，这个limit也回车一下，这个括号也回车一下。首先我把skip做一个注解，怎么注解呢？等号赋值为QUERY qui函数就行。我们在pass那里写过的参数，在query这里都适用。鼠标放上来来看一下，首先来找我们认识的description，妈点跑了，来再来一次，再来一次，鼠标放上来来看，description GT，GELE max ellis，mini lace是不是都在这儿了。所以qui的参数写法跟pass的极其的像。

发言人   07:27

好，那我们来试一下。首先query的小括号里边仍然可以写三个点，是不是代表必填？当然它本身就是必填，对不对？因为我们是查询参数，我们说可以设置默认值了。如果只设置默认值，我们最简单的写法就是像limit这样，是不是等号直接赋值，这个十是不是就是默认值了。但假设举例子，我又需要有默认值，又需要加额外的校验，这个时候quality也能实现。那怎么实现？直接把三个点儿这里换成我们需要的，按默认值就可以了。

发言人   08:03

比如说我这里默认值就是零，好，那逗号隔开我们做额外的校验就可以了。比如说我来个description，是不是参数描述？好，那我打一个引号，允许我偷个懒，是不是跳过的记录数，control v给它搁在这儿来。

发言人   08:19

好，再比如说我再加一个LT是不是就是小于的意思？比如说要小于多少呢？小于100，你不能光光跳过100个。那现在query是不是注解了一个skip？那接下来咱们来看一下效果，同样的我们回到浏览器里边来再次刷新一下。那我们try IT1下，来看默认值有了，描述也有了。好，那接下来我们直接点一个提交，来看看skip limit的值是不是OK没问题，且拼接在了问号的后面，而且响应结果也是OK的那接下来我们自己输入一个试试，我们刚才说是不是要小于100，比如说这里我来个166来exclude来看看，报错了，报错了是不是要小于100？那我们这个query注解skip是不是就成功了？

发言人   09:14

接下来咱们再回到pat up里边来，咱把这个limit也注解一下，这limit我想要给它加一个描述信息，那大家帮我怎么做？等号，把这个十就得干掉了，写上cory那这个十怎么办？直接写在query，第一个参数的位置是不是就可以了？

发言人   09:35

逗号隔开，再来一个scription描述。那他的意思就是返回的记录数是不是来直接给它粘过来。那接下来保存一下，咱们回到浏览器里边再次刷新来看一下。注意来看返回的记录数这个描述信息是不是也有了，默认值是十是不是也有了。那这样就是咱们查询参数的写法了。注意路径上不需要再定义参数了，直接给处理函数定义形参。这个形参是不是会被自动解释为查询参数，而且查询参数跟路径参数也是有相似的地方的。

发言人   10:13

云学类型注解原生类型也行，Carry也可以。那了解了查询参数的写法了，接下来回到PPT当中敲黑板划重点了。一查询参数出现在什么位置？对，URL问号之后，而且是参数名等于参数值的方式。如果有多个参数中间用and去拼接的，好，那这是参数出现的位置，接下来怎么给查询参数加类型注解？

发言人   10:49

对，两种方式。原生的也行，是不是query也行。那原生的就是参数名的后边冒号直接写类型。如果需要添加额外的注解，就等号赋值为query函数是不是就可以了。那咱们就这么记，query函数小括号里边的参数其实跟pass是不是非常类似，咱们对比着去记就可以了。这个就是咱们开发当中对大量数据进行筛选、过滤、排序的查询参数了。

发言人   11:19

了解了查询参数了，接下来该同学们的表演时间了，我有小练习来看我的需求。设计接口查询图书要求携带两个查询参数，一个是图书的分类，一个是价格。也就是说我们找某个分类下某个价格区间段的图书，是不是？我的具体要求就是图书的分类，默认值是python开发长度有限制，是五。5到255。也就是说这里不仅仅是数据类型的限制，是不是还需要宽睿做类型终结？好，那再来往下看啊。第二个参数价格，价格的话限制大小范围是50块钱到100块钱就可以了。好，那接下来大家来动手把这个查询参数的小练习来试一下。另外练习完之后，记得把答案放到评论区，我们在评论区见。