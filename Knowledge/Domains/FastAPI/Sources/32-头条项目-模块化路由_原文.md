32-头条项目-模块化路由_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，上一节我们在拆分项目结构的时候，拆分了一个rooters文件夹。大家还记得干什么用的吗？放的是路由代码，而且是按模块拆分的。将来有几个模块是不是就要对应几个路由文件？其实像这种把路由代码拆分到不同文件的做法，就是咱们fast API当中的模块化路由器。每个模块对应一个文件更容易维护。

发言人   00:27

咱们做项目的时候，模块化路由必不可少。当然我也猜到，同学们可能会想，我们原来讲基础的时候也没有拆分，所有的路由代码放到一个PY文件里边不行吗？肯定可以。但是大家想，这么多路由代码都放到一个文件里边，大家觉得怎么样？有点多是不是太乱了？那咱肯定不能这么干，所以做项目肯定要用到模块化路由来拆分路由代码。

发言人   00:58

那什么是模块化路由呢？模块化路由就是把路由代码拆分到独立的文件里边，再到这个main点PY当中挂载一下就可以了。那么由于做了拆分了，咱的项目结构是不是更清晰了？由于把咱们的路由代码拆分到不同的文件了，方便咱们去查找，找到了是不是更容易去修改它。好，再有因为我们做了拆分，代码是不是都不放在main点PY里边了？那这个主入口文件就放fast API实例和一些配置入口文件是不是更清亮了。所以咱们做项目的时候肯定会用到模块化路由。那咱们就来看一下模块化路由该怎么实现。

发言人   01:40

注意上做法分三步。第一，模块化目录结构。其实这一步我们上一节已经做了，是不是有了一个reuters文件夹？那未来有啥模块在新建这个模块对应的PY文件，放它的路由代码是不是就可以了？

发言人   01:57

好，第一步模块化目录结构了，那紧跟着咱们就来写路由，那路由该怎么写呢？来咱们上写法。在这儿我提前跟大家说，其实模块化路由只比原来的路由器多了一个创建实例。我们来看一下细节，首先从fas API里边导入API root，下一步创建API router实例，这是咱们模块化路由的关键点。有了这个实例了，我们再去写这个路由装饰器的时候，不是艾特APP了，而是艾特刚刚的这个实例是不是就可以了。那剩下的请求方法、路径处理函数、响应结果跟原来那个基础路由是一样的写法。所以我们把关注点放到API router这里是不是就OK了。

发言人   02:50

那我们看重点，API root小括号创建API router实例参数。首先prefix，它是路由前缀，那么大家还记不记得这个API斜杠news，API斜杠user是不是咱们同模块这个接口前半部分是不是都是一样的？路径前半部分都是一样的。那既然都是一样的，所以我们可以把它放到前缀里面，那将来再去写这个路由请求路径的时候，只放最后这一部分是不是就可以了？将来这个前缀跟咱们写的这个请求路径，它会自动拼到一起。好，这是第一个参数前缀。

发言人   03:31

下一个tags，tags是分组的意思，这个分组会体现在交互式文档里面。Pax的值它是组名，就点击这个组名，可以折叠的方式显示隐藏这个组下的所有接口。这样的话，咱们交互式文档那个结构是不是就更清晰？我们要去找一个接口是不是就更快更方便了？这个是咱们模块化路由的路由写法。好，那有了这个路由了，下一步在main点PY当中挂载让它生效。

发言人   04:05

那怎么挂载呢？来上代码，基于fast API实例，这个APP是不是fast API实例？点儿in可录的root，包含路由是不是？好，那包含哪个路由呢？你的路由来自于哪个文件？对应是哪个API router实例？导入写在这儿，挂载就成功了。那这三步流程走完，咱的模块化路由就写完了，那就可以正常的向这个接口来发请求了。好，了解了模块化路由的写法了，接下来咱们来试一试。

发言人   04:44

我们一块儿回到拍场当中来。首先我们是不是得先模块化这个目录，模块化目录我们已经有了rootless了，那我们是不是要针对于不同的模块去新建PY文件，对不对？那咱就直接往咱项目上靠，回头咱们项目第一个做的模块是新闻，所以我就先新建一个python file，叫啥呢？叫news点PY是不是新闻呢？好，我回车了。

发言人   05:13

好，那现在我们模块化目录这一步是不是就完成了？我有了一个news点PY文件，放的就是新闻模块的所有路由。好，那第一步完成了，下一步我们干啥？对，是不是写路由？写路由要先干啥？对，倒包of rome fast API import，还记得谁吗？API root, 那接下来怎么办？

发言人   05:40

下一步咱是不是要创建这个？我投个篮儿，创建API router实例，创建API rotor实例。好，那怎么创建大家还记得吗？对，是不是就是API router小括号，那我们把它存一个名字，比如说我就叫rotor路由，那下一步API root小括号里边是不是填前缀跟这个分组？好，那前缀是prefix，那接下来打个引号，那我们就跟咱的那个接口一样了，我就写API斜杠news。

发言人   06:20

好，那再下一步是不是写个tags分组。这个分组你可以起多个名儿。比如说我来个列表，打个引号，那咱也就不起别的名字了，咱要做新闻模块，那就直接叫news了。

发言人   06:32

好，那现在有了API root or实例了，那下一步咱们要干啥？对，是不是正常写路由就可以了，那这个路由咱要写装饰器。那这回艾特谁？对，at router注意，咱们艾特的是API router实例，咱这儿可没有APP了。

发言人   06:51

好，艾特root点儿请求方法。比如说咱就get一下，咱们来个路径，这里路径其实可以随意设置。那比如说我就来个分类了，因为反正咱们待会儿也要获取新闻这个分类是不是？那就charactery，JORIES。那我们现在这个装饰器写完了，那下一步是不是写处理函数？DEF起个名字就叫get分类了。下划线CATEGORIES，小括号参数就不写了，咱直接空着它return直接响应一个结果。咱只要能看响应的这个信息是不是就可以了。

发言人   07:28

好，那我们来一个这就是获取分类，成功了，成功。那么现在咱们这个路由是不是就写完了，那么下一步咱们要干啥？挂载对不对？在哪里挂载？对，main点PY当中去挂在这个跟的默认的这个我留了一个，接下来我们往下来写了。

发言人   07:52

那怎么挂载来着？大家还记得吗？AAPP是不是就是咱们的fast API实例点儿in可录的rotor包含谁？是不是包含的是咱们news里边的这个root？好，那我们先给它导进来。所以from reuters and in part news，我现在是不是把整个news给它导进来了？当然我们这回注册挂载的是不是news里边的这个rotor？

发言人   08:25

好，那现在咱们的挂载注册路由是不是也完事儿了？那我在这儿加一个小注释，这个是挂载路由，或者你叫它为注册路由，是不是也可以？注册路由也可以。好，那现在咱们模块化路由的三步是不是走完了。那接下来我们去看看这个路由能不能正常被请求到，我们项目是正在运行的状态，那我们直接去浏览器里边试一下。

发言人   08:52

来我们回到浏览器里边，我是提前打开了这个交互式文档，我们刷新一下。注意来看news分组下获取的是API news下的character aries路径是不是在这里。那我们先来试试能不能正常发请求，拿响应结果来try IT execute，来看看响应结果。按获取分类成功，那能拿到这个响应结果说明咱们的路由是不是没问题。

发言人   09:20

好，接下来往上来给各位同学看个细节，这个news是不是就是tags分组名，我们说有了这个分组可以咋着来着，点它是不是能折叠显示这个分组下的接口。来我们试试，走你是不是折起来了，再点开是不是也没问题。好，这是咱们自己的一个new分组，大家往上看这儿是不是有一个defauts，这是默认的。就是fast API是不是自带一个default分组，你看它也是可以折叠的那这样的话我们不看谁了，把它折叠起来，整个文档结构是不是会非常清晰那现在咱们的模块化路由是不是也能被正常请求拿响应结果了？那接下来回顾一下咱的代码。首先第一步是不是模块化目录，有个reuters将来有啥模块，是不是新建哪个模块对应的路由文件，那路由文件里边是不是就可以写路由了，导入API router，重点是不是就是创建API root or实例。

发言人   10:27

这里创建了API root实例了，那我加个注释，prefix它是干什么用的，加个注释，这个是啊路由的路由前缀，那具体前缀是啥？将来是不是得去看那个API接口规范文档里面，对不对？好，接下来再有这儿是不是还有一个tags，这个tags它是不是分组或者叫标签的意思？有了它是不是交互式文档结构就更清晰了，可以点它折叠显示隐藏所有接口。好，那咱们有了API root实例了，写装饰器艾特这个API root实例是不是就可以？当然大家别忘，有了路由了，是不是还得在魅里边把它挂载一下？APP应可录的retour把它挂载注册一下才行。

发言人   11:18

好，那现在咱们的模块化路由写法儿就搞定了，那我们回到PPT里边总结提炼一下重点。我有三个问题，什么是模块化路由？它有什么优势？模块化路由就是把路由代码拆到独立的文件里边，再从main点PY里面挂载一下，是不是就OK了？那有啥优势？项目结构更清晰，是不是更容易维护了？

发言人   11:41

好，接下来第二，我有一段代码大家帮我解释一下，来上代码，路由的写法对不对？导入API root or关键点是创建API root or实例。Prefix是啊对前缀儿，这个前缀儿能乱写吗？动将来看API接口规范文档。好，那再来tags对，它是分组。好，那将来写装饰器就艾特咱们的API router实例名是不是就OK了？

发言人   12:13

好，那这样的话咱的一个路由是不是就有了？但它能直接生效吗？肯定不行，所以咱们要注册一下，咋注册？对，在main点PY当中添加a APP，基于fast API实例，我们要挂载也是挂载到fast API实例上面，点in可录的rooter。那你要挂载的是哪个文件下的哪个API router实例？捣进来给它写在这儿是不是就OK了。

发言人   12:42

这个是咱们项目当中模块化路由的写法，我们一块儿学完了，接下来是不是又该同学们的表演时间了。大家基于自己的项目来一个模块化的路由，写完之后去验证能不能正常发请求拿结果。OK都没问题之后，大家把自己的代粘贴到咱们的评论区，咱们互相学习。大家来试一下模块化路由。