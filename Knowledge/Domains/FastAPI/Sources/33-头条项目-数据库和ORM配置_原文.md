33-头条项目-数据库和ORM配置_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，上一节我们学了模块化路由，有了路由咱就可以写接口响应结果了。但是我们都知道这个接口的主要功能是不是就是连接数据库，操作数据库里边的数据，接下来响应结果，那咱马上就要写接口功能了，是不是？还没有数据库，那肯定不行，是不是？所以这一节咱们先来准备一下这个项目相关的数据库环境，那具体都要做啥呢？咱们一块儿来看一下。

发言人   00:30

首先咱肯定是要先创建数据库，那有了数据库是不是就可以操作数据了？但问题是我们说咱就不写circle了，是不是采用ORM的方式来操作数据？所以这一节咱们的重点就是准备数据库配置ORM。至于数据库的话，咱就不create创建了，因为我们是不是需要大量的新闻数据，我们是新闻资讯类操作的，是不是就是新闻数据？那这个新闻数据哪儿来来呢？别担心，我准备好了。我在咱们的配套物料里边准备了一个circle文件，把这个文件在咱们python database那个插件里边导入。导入之后能帮咱们自动创建数据库，建表，还有大量的新闻数据，所以数据库咱就用这个circle文件来解决。

发言人   01:19

好，搞定了数据库了，下一步配置ORM，大家还有印象吗？具体都要干啥？咱们一块儿来复习一下，想要配置ORM咱们分成三步。第一步安装，第二建表，第三是不是就能操作数据了。那我们先来看安装，大家还记得安装要安装啥吗？买两个包，一个是circle actress，当然是异步的，还有一个异步的数据库驱动版。

发言人   01:50

好，那安装完之后，下一步是不是就能见表了？大家还记得建表是不是要写的代码挺多来着？我们先来看一下，比如我们要创建异步引擎定义模型类，还有在fans API启动的时候，是不是调方法来建表。但是大家放轻松，咱刚刚说是不是有个circle文件一导入有数据库有表，是不是还有数据？那也就意味着可以暂时不写模型类，也不需要软think来填表。二三这两小步骤是不是可以省略？当然创建异步引擎了，创建这个绘画了，创建依赖项了，咱们仍然需要。好，那建表这一步搞定了，那后期我们针对不同的接口是不是就可以注入这个数据库依赖项，增删改查操作数据了？

发言人   02:43

这个是咱们ORM整体的使用流程，各位同学还有印象吗？跟我们讲那个ORM的时候，是不是那页总结的PPT是一模一样的？那我们把这个流程大致串一下，接下来咱就两个重点工作。一是不是导入circle文件，拥有数据库表，下一步把ORM来配置一下。接下来咱们到python当中来试一下，那我们回到py time里边来，首先咱是不是得先有数据库再配置ORM，那数据库咱是不是采用导入的方式？

发言人   03:15

那怎么导入？注意跟着我的鼠标来到pat tom右上角database这个插件点开，在这里创建连接导入circle文件。那我们点加号，选择data source数据库，咱是mysql，接下来选中这个mysql，在这个界面要填啥？用户名密码是不是填自己的mysql l用户名密码，稍等一下，那我们点1个OK，那这个时候稍等，链接创建完了，我们是不是要导入circle了？那怎么导入呢？注意，点着这个local host连接的名字，鼠标右键往下找到circle script ts，circle脚本是不是？那下一级菜单选择最后一项run circle script，接下来我选一下，那选中之后，就会跳出来一个浏览窗口，那大家就要自行浏览，找到你的circle文件存在哪儿了，这个要清楚。好，选中这个circle文件，接下来点这个肯定的按钮，确认打开就行了。

发言人   04:23

那接下来我open一下，大家稍等来看，是不是insert into添加了啥来往上看啊，是不是有好多条SQL语句是不是执行出来了。好，其实现在咱的数据库表数据全都有了，那我怎么去验证呢？注意来到database这个插件儿，那现在的话看不见咱的数据库，怎么办？这里链接的底下是不是有3个点点点开数据库的名字叫news APP，注意，叫news APP。所以在这儿大家把它点开勾上news APP来看，这就是咱的数据库。

发言人   05:06

展开一下有几张表大家来看看，八张表点开像收藏、历史、新闻、用户等等表是不是全都有了。比如说来看看news，我说我是不是有很多数据来着？大家来看，这就是我提前给各位同学准备好的数据，咱现在是不是都已经拿到了，那这个是新闻数据，再给各位同学来看一下这个user表，来展开一下。User表里面我有一个测试用户，admin注意了，看密码，加密的。

发言人   05:42

好，那现在咱们的news APP数据库有了，关键的表是不是也有了？那关于数据库是不是准备完了，那么下一步咱是不是采用ORM的方式来操作数据库里面的数据？那下一步配置ORM大家还记得要干啥吗？安装然后配置对不对？接下来咱们先来安装一下，各位同学，我提前准备好了这个安装的包名儿，那我直接复制了PIP install，异步的circle跟数据库异步驱动，直接复制回到咱们的python，找到终端直接control v粘贴。

发言人   06:22

因为我这里是mac操作系统，因为带了这个异步的依赖。这个circle acme这里是不是得打引号？好，那接下来我直接回车联网安装了。那在这里我们稍等一下下，因为这个安装包的话是不是跟网速的快慢就有关系了。

发言人   06:38

那现在咱们已经安装完了，下一步配置ORM是不是就可以了？比如说包含咱的数据库的用户名、密码、链接地址、数据库引擎、绘画依赖项等等等等。这些代码我也是提前准备好了，我们来到素材里边。这个代码各位同学看看还有印象不数库UL异步引擎绘画工厂，依赖性是不是有了这个依赖项，我们就可以在处理函数当中i dependence注入它了。好，这个代码我们就不占用课上时间，带着大家一个一个去写了。如果同学们想要加深印象，可以自己来写一写试试。

发言人   07:19

好，那我这里就直接全选control c复制回到拍照P里边来，关键点就来了，搁哪儿去呢？各位同学看着左边的项目结构，告诉我放到哪里最合适了。对config是不是配置？我们说关于数据库将来的缓存配置，是不是都放到这里面来。所以接下来咱们右键去新建一个python file，那我们起个名字，咱这是不是数据库的配置，那我就叫DB database，对不对？杠config ICONF。接下来我敲一个回车，把咱们的代码直接粘进来。那我们稍微来看一下这个代码，来我把底下折叠来看一下，确保数据库的UL注意root是账户名123456是不是我的密码，这里是不是要改成自己的。好，那这个local host的3306不用变，注意来看数据库。News APP后边这个是不是就是字符编码，我们可以不用动，这个数据库名字是不是也不用动，因为咱用circle导入的是不是都叫news APP，那大家也不用动，注意账号密码改成自己的。

发言人   08:32

好，那再往下看啊，是不是创建异步引擎，用到的是咱们上边设定的这个数据库的UL。好，还有这个连接池的数量，允许溢出的额外的数量，对不对？好，那有了异步引擎了，下一步用这个异步引擎就可以创建绘画工厂了。那有了绘画工厂了，我们依赖于这个绘画工厂是不是就可以创建依赖项。为的是路由处理函数里边是不是可通过依赖注入的方式注入这个依赖项，那是不是就能拿到数据库绘画，就能连接数据库操作数据了？这些代码在咱们讲OM基础的时候，咱就写过一遍，而且是不需要改的，所以我就直接复制过来了。同学们如果想要自己去加深印象，可以自己再写写试一下。

发言人   09:19

好了，各位同学，咱们现在关于数据库ORM的准备工作咱就做完了，那也有两件事儿需要大家去做。是不是通过这个database我们创建连接之后，右键选择到circle script，run circle script是不是导入circle文件？导入了之后，大家就在那个右下角是不是会有一个3个点点选择到咱的数据库。注意来看数据库名字是不是叫news APP。那导入sgl之后，会自动生成这个news APP以及八张表。那有了表了，数据库就搞定了。那接下来就是OM的配置，是不是安装包，下一步在config里边新建一个DB的，把咱们的代码粘过来是不是就OK了。这个就是咱们这一节关于数据库环境的准备工作，接下来就该各位同学也来动手，circle导入把这个ORM配置好。那下一小节儿，咱们就开始写接口来实现新闻相关的功能了，那我们下一小节儿不见不散。