64-头条项目-封装缓存操作方法_原文

2026年07月21日 12:53

发言人   00:01

Hello, 各位同学，上一节咱们安装和配置了redis客户端，创建了redis连接对象。那这一节咱们来封装缓存操作方法。这里的封装缓存操作方法，其实就是把redis连接对象自带的这些方法进行二次封装，来完善功能，方便咱们在项目当中复用。那么接下来，咱们先看redis连接对象自带的方法，再带着大家进行二次封装。

发言人   00:27

那我们先看redis自带的那首先redis自带的方法是围绕着设置缓存、获取缓存、删除、判断等等操作来完成的那我们redis存数据的时候，它有一个原则就是k value的方式。K是自定义的缓存键，自定义就可以了。Value是我们要缓存的数据，那具体该怎么存，怎么读，怎么删呢？来我们看下边的方法。

发言人   00:57

首先我要设置缓存，也就是说写入缓存调用方法set EX传参。注意来看K字符串类型，这是缓存键expire int类型，这个expire是过期时间以秒为单位。那再往下Y6ISTR字符串类型，这是缓存的数据。这里大家就要注意了，我们无论存什么数据到缓存，是不是要把数据先转成字符串类型再来存？好，这是set EX是设置缓存的意思。那再往下get，get是不是得到？

发言人   01:39

这里是读取缓存，根据什么来读呢？传一个参数K是不是根据指定的缓存键来读取数据？好，那再往下delete。对了，删除就是根据指定的缓存键来删除缓存数据，那excise判断当前这个缓存键是否存在，那是否存在返回的就是布尔值了。

发言人   02:05

这几个方法是我们开发当中常用的。接下来我们就把这几个方法来进一步封装一下来完善功能，方便咱们来调用。当然我们提前规划一下这一节，先封装设置缓存跟get读取缓存，后边的咱们慢慢来实现。那接下来咱们回到代码当中来试一下，来回到拍场景当中来，允许我先加一个小注释，我们刚刚说是不是要二次封装设置缓存，还有这个读取缓存的方法。

发言人   02:39

我在这儿先说一下，关于读取缓存，我准备封装两个方法。为什么是两个呢？是因为我们存的数据有可能类型是不一样的。比如说我们存之前，我这个数据它本身就是个字符串，那我读出来还是字符串，哦耶没毛病，是不是可以直接用。但还有一种可能性就是我存进去之前，我们这个数据它是一个列表或者是字典。那我们在真的存的时候，是不是要把这哥俩转成字符串才能存到redis里面，那我取出来是不是还是个字符串？那将来它的长相是啥样的？是不是就是字符串里面包个列表或者是字典，甚至是列表里面包字典。

发言人   03:27

那问大家，如果我直接获得到的是一个这样的字符串，那对于咱们python来说，是不是还要解析一遍？所以如果读出来的是列表或者是字典这样的复杂顺序，那我们就直接在封装的这个读取方法当中把它序列化一下，是不是就OK了？这就是我刚刚说设置，我就一个方法读取，我是两个，一个是处理就读出来的是普通字符串，另一个是为了序列化一下。好，那也就意味着接下来我要带着大家是不是封装三个方法，那我打个注释。

发言人   04:04

首先第一个方法就是我封装一个读取缓存，那我们读的就是这个普通的字符串。好，封装完这一个，我们再往下封装下一个还是读取。那他读的是啥呢？就是列表或者是字典能够序列化的这一种。好，那最后再来一个设置缓存，设置缓存一共这三个方法，先跟大家对齐一下，这三个小方法目标咱们明确了没有？

发言人   04:34

明确了，那我们就开始了。首先第一个是不是就是读取，处理的就是啊直接是字符串类型的数据。好，那我们来封装方法。首先咱是异步的方式，那就a think一下定一个方法名。比如说我就叫get缓存CACHE小括号。当然咱们未来是不是根据K来获取，那我在这儿来一个形参K类型是不是就是STR类型。好，那方法名也有了，功能是啥呢？注意基于连接对象调用盖头方法，是不是就可以根据指定的键来获取值了？

发言人   05:16

那我们开始了redis client，是不是就是咱们上边创建的redis连接对象，打点儿get一下就可以了，get的小括号传入K当然因为咱们是异步的那我们是不是要await执行这个方法？好，那执行完get是不是根据这个K来获取缓存数据？那这个方法一旦能执行，是不是就能获取到缓存数据？那按道理我直接return结果是不是就OK？当然在这儿我问大家一个问题，就这个方法它一定能拿到某个K对应的值吗？不一定。那接下来我要把它做进一步的处理了，就是不要上来直接就执行get有可能成功，有可能失败，各位同学咋办？踹一脚是不是？

发言人   06:13

好，我写了TRY try，我们去尝试执行get我把它复制一下，敲一个回车尝试执行get。那一旦失败我来一个except，比如说exception as一个E冒号一下，那我print一下，那我就说获取缓存失败了，把再把这个E给它格式化一下。那我来一个字符串格式化一下，获取缓存失败来个冒号，然后把咱们这个E给它格式化一下。好，另外return一个，就是我们都失败了，是不是就没有缓存数据，所以return一个N就可以了。

发言人   06:59

好，各位同学来看看，获取缓存我们处理的就是最简单的字符串类型的缓存数据，那就直接基于上一步创建的redis连接对象，调用get方法是不是就能获取了。当然我们说它有可能成功，有可能失败，是不是try except一下，尝试执行。如果成功了，那就return获取到的数据。失败了，提示一下，并且return一个难，是不是就OK。

发言人   07:26

各位同学这个方法的思路还OK吗？是不是这个挺简单的那我们字符串类型的是不是处理完了，那接下来往下是不是就到列表或者是字典类型的数据了。我们同样a think DEF，我就叫get，比如说就是阶层类型的了，CACHE小括号是不是还是按照指定的K来获取就OK了。那根据上一个处理字符串类型的，我们有经验了，是不是有可能成功，有可能失败，那我们这次获取是不是也是一样的？所以我还是把它try尝试执行，尝试执行啥呢？还是去获取，还是获取，那就是await，redis client点get是不是就是根据K来获取缓存数据。

发言人   08:14

当然如果有数据，因为我们处理的是列表或者是字典。那我们想要列表或者是字典，是不是得把redis存的那个字符串给它序列化一下？那我就这么写，比如说我来个data变量把它存一下，if有data，那我就要序列化了，对不对？那我就return一下，return Jason点儿loads，我们把它序列化，填进这个data。那这样的话就会把我们带引号的这个列表或者是字典序列化成我们想要的直接是列表或者是字典。那就相当于把这个引号是不是给它拨掉了。当然咱们现在是如果有数据是不是就序列化了？当然如果没数据那我就直接return一个难，没有数据就返回难是不是就可以了。

发言人   09:02

当然还有一种可能性就是这些尝试执行失败了。那就except exception exception exception as一个E然后冒号一下，那我们就老规矩打印一个错误信息，在return一个N是不是就可以了。那我print，我往上滚动一点，来个F那我就说获取杰森格式的缓存失败了，我把上边这一个偷个懒复制一下control v粘下来，获取Jason缓存失败，并且把这个错误信息给它格式化出来。好，那最后我再return一个难，都失败了就return难，是不是就OK了？那这是处理，我们想要列表或者是字典在这里我们是不是要把它序列化一下，序列化一下OK。好，那现在第二个方法就是我们想要列表或者是字典带序列化功能的读取缓存是不是也OK了？

发言人   10:02

好，那接下来再往下是不是就是设置缓存了？那我封装方法i think DEF起名字我就叫set CACHE缓存了。那么接下来我问大家设置缓存这个方面我应该要啥参数？K要不要？还要还应该要啥？对，Y6还应该要啥过期时间。为啥为啥？因为我们设置缓存是不是set EX，他是不是要传K？要传过期时间是不是EXPIRE过期时间还要传啥？Y6，所以我这个4 at catch是不是也需要这三个参数？好，我写了这个参数顺序就按自己的习惯来就可以了。

发言人   10:50

比如说我要K那它是STR类型，逗号隔开。我们一般人说话是不是k value就挨着，所以我这儿就直接来个value了号。这个我来一个案例类型，就是任何类型是不是都OK来允许我import一下，import导入typing里面的安妮逗号隔开，是不是再来一个过期时间，expire，来一个int类型等号，我负一个初始值了，3600当然它是秒3600秒，就是1个小时。但是在这儿我提前说一下，咱们这里设置一个默认值是3600秒。未来不同类型的数据，我们这个过期时间是不一样的。当我们涉及到具体的业务功能的时候，再给大家去具体设计这个。过期时间。好，那这里我们就写初始值3600秒就可以了。

发言人   11:41

好，接下来我敲个回车问大家set开始设置缓存我们要干啥？其实思路比较像，还是尝试执行。因为我设置缓存是不是也有可能会失败，所以我尝试执行，还是踹他一脚。

发言人   11:57

TRY try尝试执行，尝试执行啥呢？其实是不是就是纯数据。当然大家想我们获取的时候，这个最重要的数据是不是有可能是字符串或者是列表字典。那你说我存的时候有没有可能也要考虑这两种数据类型呢？也要是不是？所以在这里我写一个依附判断，就是如果是列表或者是字典，我们是不是要把它变成字符串。当然如果直接是字符串，嘿嘿直接set EX是不是就可以了。

发言人   12:32

所以我在这里加一个if判断，if我来一个is instance，是判断类型的，把我的value先判断一个类型。Value如果是我括号一下，如果是dict或者是list是不是就是如果这个value是dick的字典或者是list列表，那我干啥？这里是不是要转字符串再存，对不对？那怎么转呢？注意我来一个Jason结算点儿dumps来往下，选择它把我们的value传进去。当然我还加一个参数EN insure，杠ask等于一个false，啥意思？保留中文，不转译。因为转译之后我们就看不懂了，所以在这儿取值为false，就是保留中文，保留中文，中文正常保存。那我把这个转换之后的字符串，拿一个小变量把它存一下。

发言人   13:42

比如说我还就是Y6，那就意味着如果我们的数据类型是字典或者是列表，是不是转字符串我再存。当然如果不是，那我未来存的还是这个参数value是不是就OK了？这样的话我们名字统一直接都存，是不是就OK？好，那接下来我再往下，我要存数据了，怎么存呢？那我await一下。

发言人   14:07

异步操作，基于redis client这个连接对象，调用set EX这个方法，那我们传参了，注意首先是不是得来一个K值，这就是缓存键，是不是那逗号隔开接下来来一个啥？是不是就是时间expire过期时间逗号隔开，最后来一个啥，对，是不是就是我们要存的这个Y6。如果以上执行成功了，那我就return一个true，方便咱们后期去判断。当然except捕获异常，exception as一个E那我们还是老规矩，就提示一个错误信息，是不是就OK了，那我就print f这是不是就是设置缓存失败了，失败了，冒号一下，然后咱们把这个E给它格式化出来。E好，那我再return一个false是不是就OK了。那我们现在这个设置缓存的方法是不是也就封装完了？

发言人   15:07

那我们最开始规划的封装设置和读取缓存的方法，OK搞定。读取咱们是不是有两个，一个是我们想要的直接就是字符串，所以get出来的就是字符串，那就直接return是不是就OK了。当然如果我们读出来，我们想要的是这个列表或者是字典。但是redis里边咱们已经存的是不是字符串了，那怎么办呢？是不是把它序列化出来，那就Jason点路子一下。

发言人   15:35

好，那最后设置缓存，设置缓存的时候，我们说有两种情况，如果直接就是字符串，那就不进这个if类型的判断，是不是就直接set EX存了参数value了。但如果我们检测到这个数据类型是字典或者是列表，是不是要先转字符串再存，那怎么转的呢？杰森dump测一下。为了判断后续到底有没有设置缓存成功，咱是不是分别成功的时候return了true，失败的时候return了一个false，方便咱们去判断。

发言人   16:10

好，这个就是我们刚刚封装的这三个小方块法。这些方法封装好了，各位同学下一步我们是不是就可以调用这些方法。比如说设计个新闻，比如说新闻分类的K名，是不是把新闻分类写入到缓存，或者设计一个新闻列表的K名，是不是把新闻列表设置到缓存了？那接下来各位同学来动手，基于自己的项目把这个读取、设置缓存这三个小方法来封装一下子。另外封装完之后不要走开，下一节我们就基于刚刚封装的这些方法来缓存咱的新闻数据了。下一节我们不见不散。