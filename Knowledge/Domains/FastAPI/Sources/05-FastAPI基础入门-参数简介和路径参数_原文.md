05-FastAPI基础入门-参数简介和路径参数_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，这一小节咱们要学习参数了。有了参数，我们就可以让同一个接口根据参数不同响应回不同的结果，实现接口的动态交互了。什么意思呢？咱们来分析一下。我们知道现在如果我们向服务器发请求，比如说我们请求跟路径返回的信息是不是message hello word。那这个时候，无论我们向跟陆靖发起多少次请求，拿到的结果是不是都是hello word？

发言人   00:30

但现在用户有一个接口提了更高的要求。比如说他有一个book接口，他想通过这个接口得到图书12345任意一本书的信息。比如说他跟我要ID为一的那我就要返回图书一的信息。如果他要book ID为二的，那我就要返回图书二的信息。那这是不是显然用户传入了一个ID值，传入ID为己服务器是不是就要想送回ID为己的图书信息？那这个该怎么实现呢？我们有参数可以用参数来实现，参数就是客户端发请求时附带的额外信息。参数可以实现同一个接口用户输入不同服务器响应回不同的结果，实现接口的动态交互。

发言人   01:22

在实际开发当中，绝大多数接口都是需要参数的。为了满足开发当中各种各样的开发场景，参数也是有多种的那常见的参数分三类，路径参数查询参数、请求体参数。在开发当中，路径参数一般用来定资源，你要哪一本书要找哪一个用户呢？查询参数在开发当中一般用来怎么选？比如说做排序，做分页等等操作。请求体参数通常用来提供数据，也就是给服务器什么。这三种参数出现的位置作用方法都是不同的那我们要一个一个来学习。

发言人   02:11

首先咱们来学习路径参数。首先路径参数出现的位置，各位来看IURL路径的一部分。比如说在底下，我们的UL路径里边用大括号是不是写了一个ID，那这个ID就是路径参数了，它的作用是指向唯一的特定的资源。就比如说要找哪一本书，IDV几的，要找哪个用户，用户的名字是什么，或者ID是什么？像这种找唯一的特定的资源，咱们都可以用路径参数。

发言人   02:46

那么再往下看路径参数，一般请求方法就是get了。这个是咱们的路径参数。但是了解了未知作用了，我们还不知道怎么写，是不是？来咱们上代码看一个写法。首先从最上来看我们的请求路径，这里大括号定义了一个ID那这个ID就是路径参数的名字了。那这个路径参数的值是不是肯定要拿到处理函数当中去使用？那往下来看处理函数要加一个同名的形参，那这样的话咱们的路径参数ID值就会传到处理函数形参ID这里接收到，从而处理函数的内部是不是就能使用这个路径参数的值了？

发言人   03:35

另外再往咱们形参这里看一个细节，ID型参名冒号是不是跟了一个int，那这个int咱们都知道是不是python原生的数据类型，它是整形。那这个冒号int是在干什么呢？注意了，这里是在做参数类型的注解。大白话来说就是约定ID的数据类型能未来开发当中，咱们根据实际需求来变换这个数据类型就可以了。

发言人   04:05

那写到这儿，咱们的路径参数就定义完了，那我们是不是就可以发请求了？那么发请求肯定是要给路径参数，是不是？传值的结果长啥样呢？注意来看，假设用户传入了一个666，那么这个666它也会出现在URL路径的位置。而且咱们的处理函数内部是不是也能拿到这个ID值666？请大家观察一个细节，这个ID的类型是还没有加引号，对不对？它是int整形，为啥是整形呢？因为咱们是不是做过参数类型的注解，用户输入666，那我们处理成int类型。

发言人   04:46

这个是路径参数的写法以及大致的执行流程。接下来咱们也到排场当中动手来实现一个带路径参数的接口。首先咱们定个需求，就跟PPT里边一样了，也是像当不可接口发请求，我们要拿到指定ID的书籍的信息。好，首先写咱们的路由了，写接口了，是不是要带一个装饰器？

发言人   05:11

那艾特APP点请求方法，我们说一般是啊get小括号路径，那我们放上比如说book斜杠一下，是不是来路径参数咋写来着？大括号一下里边放上自定义的路径参数的名字，比如说咱这里也就叫ID了，代表书的ID好，那接下来敲回这该写啥了，处理函数，那咱也来一波函数了，think一下DF起个函数名，比如说就叫get book，小括号冒号一下，那小括号里边大家还记得要干啥吗？定义一个跟路径参数同名的形参，比如说咱这里没有，比如说了只能叫ID了，是不是？好，那接下来冒号一下要干啥？约定类型，那咱们PPT里边是不是写的是int类型，那我们这儿也写int整形了。当然未来看实际需求，比如说需要字符串是不是就写STR，需要布尔那就写布林，当然咱这个ID我就写in的类型了。好，那接下来不写复杂的处理逻辑了，直接return响应结果。

发言人   06:22

那响应的结果咱们拿一个ID就来看一下是不是int类型逗号隔开，再来一个书名title，那这个书名我们就输出这是第比如说五本书，这是第六本书，那就是输入的ID市集咱这就用几，那我来一个F咱们格式化一下，这是第某某本儿书把某某是不是格式化，换成ID就行了，大括号一下换ID。好，那我保存一下。现在咱们一个带路径参数的接口路由是不是就写好了？那我们运行咱们的项目来试一下。那找到咱们的8000端口，这个地址直接点一下，打开了。那接下来咱们直接这样，访问这个dos文档，那来看book斜杠带ID带路径参数的这样的接口是不是有了。那我们直接点开try IT1下，那在这里是不是要输入一个ID值？

发言人   07:24

比如说我们来个666，接下来excute一下来看，首先URL里面是不是有666，那说明路径参数出现的位置是URL里面。好，那接下来低头来看响应的结果，我们的ID是不是拿到了？Title格式化输出的是不是也拿到了。另外注意看细节，IID的值还没有带引号，是不是说明我们类型注解为int类型也已经生效了。

发言人   07:57

那现在我们是不是实现用沪输入了一个ID值666，我们根据用户输入的响应回来结果。那如果用户要是输入888，那我们excuse一下是不是响应回来的就是ID为888的结果了。那这样就是咱们路径参数的作用，根据用户输入的不同响应回不同的结果。咱现在的接口是不是就是动态交互的？

发言人   08:26

当然，了解了路径参数的写法以及作用了，接下来我要讲一个注意事项，就是这个路径参数大家来看ID这里有一个required，什么意思？对，必填那就意味着填了还则罢了，不舔啊哈没啥大事儿。Excuse一下，吼报错了说ID是required的，必填项得填才行。如果真的填了，比如说111 excuse，是不是就不再报错了？好了，那这样就是咱们的路径参数。在请求路径这里，大括号是不是定义路径参数？在处理函数这里定义一个同名的形参，做好类型注解，处理函数的内部是不是就可以使用路径参数的值了？那以后如果我们想要向服务器请求一个特定的资源，就用路径参数就可以了。

发言人   09:26

了解了路径参数了，接下来该同学们的表演时间需求是以用户的ID为路径参数设计一个UL要求响应的结果要包含用户的ID名字就是普通用户加上ID就可以了。至于这个ID类型注解为int或者是死定，不做限制，可以自己发挥。接下来同学们也来动手试试。另外我们可以把练习结果发到评论区，评论区不见不散哦。