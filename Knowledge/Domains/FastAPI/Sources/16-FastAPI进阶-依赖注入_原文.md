16-FastAPI进阶-依赖注入_原文

2026年07月21日 12:50

发言人   00:01

Hello, 各位同学，这一节咱们来学习fast API的依赖注入系统。在开发当中，我们可以用依赖注入系统来共享通用的逻辑，减少代码的重复。那具体啥时候用到呢？我准备了一个场景，咱们来分析一下。比如说我有一个项目有很多个接口，其中有三个接口有通用的逻辑，分别是新闻列表、用户列表、新闻分类列表。这里都有列表分页的逻辑，都涉及。这三个接口有通用的逻辑代码，我们用依赖注入系统更合适。

发言人   00:41

当然在这儿相信同学们可能就会有疑惑了，不对，我们上一节是不是学了中间件了？中间件是不是也能封装通用的逻辑，是不是也能减少代码的重复？那为啥这里用依赖注入系统更合适，不能用中间件呢？由我来慢慢道来。

发言人   01:02

注意，中间键是控制所有请求，每个请求都会自动执行中间件里面封装的代码。但依赖注入就不一样了，依赖注入是人做主，也就是咱们程序员说了算哪个请求需要这些通用的代码，我们就把它注入。注入的接口当然会执行这个通用的代码，那没有注入的是不是就不执行？所以刚刚我们说咱们的一个项目有很多个接口，但共享的分页逻辑是不是这三个接口才需要使用？那我能用中间件吗？那是不是咵嚓一下所有接口都有分页逻辑了，那是不是不合适？所以这里用依赖注入系统更合适，也就是咱们开发当中控制所有请求都有的通用代码，那就中间键，我们想指定哪些接口需要，那就用依赖注入是不是就可以了。

发言人   02:00

好，了解了大致依赖注入系统的使用场景了，咱们来看看依赖注入系统到底是什么。注意依赖注入系统就是我们封装依赖项，其实就是用函数或者是类把重复的代码给它封装起来。好，那依赖注入对不对？注入是咋回事儿呢？就是程序员写代码，把我们封装的依赖项注入到路由的处理函数里面。那fast的ABI会自动调用我们注入的这个代码的那因为我们把重用的代码封装起来了，是不是写一次很多个地方，很多个接口是不是都可以去用它，而且我们能指定哪个接口用，是不是注入到哪个接口里边去。其次，因为我们重用的代码是不是单独封装了，实现了代码的解耦。因为单独封装了，那我们是不是可以轻松的替换依赖性，将来去做各种各样的测试是不是都很方便了。这样就是咱们的依赖注入系统在开发当中还是很常用的。

发言人   03:03

就比如说咱们像一些请求参数要重用，我们可以用依赖注入的方式。像我们刚刚说的业务逻辑，分页逻辑是重用的，对不对？可以用依赖注入。那像数据库的连接，可能多个接口是不是都需要用依赖注入。某些接口需要验证用户的身份、权限、角色等等，我们也可以用依赖注入系统，可见依赖注入在开发当中使用的地方是不是也很多，那我们就得来学习一下该咋用，首先要用依赖注入系统分三步。第一，创建依赖项，其实就是封装个函数，把那些通用的代码给它封装起来。

发言人   03:44

第二，因为我们要注入，对不对？注入要使用fast API的一个dependence，所以咱们得先把它导入进来。好，导入了dependence了，那下一步就能注入了。也就是声明依赖项。所谓的声明依赖项，其实就是在路由处理函数这个参数这个位置来看是不是dependence小括号，小括号里边加上依赖项的名字。这个依赖项的名字其实就是我们封装的这个通用逻辑的这个代码。它的函数名来看，写函数名就OK。未来我们向这个接口发请求，执行到路由处理函数的时候，fast API会自动帮咱们去调用上边依赖项里边封装的这个重复的代码的就是咱们的依赖注入。先用函数创建一个依赖项封装重用的代码，然后用dependence注入是不是就OK了？那了解了依赖注入系统大致的写法了，接下来咱们也到代码当中来试一下。

发言人   04:43

那我们回到代码当中来，在咱们代码里边，我提前准备了两个接口，一个是新闻列表，一个是用户列表。那咱就跟刚刚PPT里边一样，分页的参数逻辑是通用的，这两个接口通用的，我们用依赖注入系统来实现。好，那接下来我们需求明确了，那接下来我加一个注释，是不是把这个分页的参数和逻辑咱们实现共用，是不是就OK了？当然这里共用是不是就是新闻列表和用户列表共用，它不是所有共用。好，那接下来咱是不是得按依赖注入系统的流程定义依赖项，然后注入进来。好，那定义依赖项咱就来个函数是不是就可以？那我们来一个think异步函数DEF起个名字共用的我就叫common，但它是参数逻辑啥的我就叫primary，PARAMETERS。

发言人   05:44

小括号一下，冒号一下。那我们模拟一下这个分页，那分页儿的话我们是不是要有比如说这个SKIP in的类型，就是跳过几条数据，默认第一页是不是就不跳过，逗号隔开一下。再有就是每页最多要多少条数据，是不是也是int类型。当然我们是不是也可以在int这个数据类型的基础之上做额外的约束。比如说默认值大于0，或者是每页最多多少条数据，是不是等等等等。在这里我们就可以借助到比如说fast API里边有一个大家还记得不那个query，query函数是不是可以做额外的类型注解。因为一会儿我们用到这个接口里边的话，它是不是一个查询参数，所以咱就用query了。

发言人   06:38

那在这里我们等号来赋个值，匡瑞比如说默认值，那默认第一页是不是不用跳过？那我们来一个这个呃比如说记忆就是大于多少呢？大于等于一个零。好，那limit也是一样，query等号query函数一下。默认值。比如说我们是每页十条，那再来一个LE是不是就是最大值？最大值咱就来个60条，多了也看不全是不是？

发言人   07:06

好，那现在共用的参数写完了，假装我们哒哒哒写了很多逻辑，最终我return一个结果，那咱也不写多复杂，就把这两个形参给它return出去。Skip值就是skip，再来个limit，LIMIT值就是limit，是不是就OK了？好，那现在各位同学咱们的第一步这个依赖项是不是就已经完事儿了？依赖项已经有了。

发言人   07:35

那有了依赖项了，我们第二步要干啥？是不是导入dependence是fast API里面的。所以在这里我继续逗号隔开，咱们把这个dependence是不是给它导入进来。在这儿我在加一个注释，这是不是就是咱们的第二步，导入dependence DEPENDS。

发言人   07:56

好，那也有dependence了，下一步干啥？需要的接口是不是给它注入就可以了？那假设新闻列表我需要是不是给它注入，大家还记得怎么注入吗？对，是不是在路由处理函数形参这里。比如说我来一个common行参，先给它收一下，等号赋值是不是一个dependence小括号，那小括号里边咋写？注意写名字哦啊写依赖项的名字。好，那就是common primary，但注意啊不要调用。

发言人   08:31

现在我们完成了依赖注入，拿到结果还是comments。那真的拿到了吗？咱们return来试一下，那我直接return comments。这样的话，当我们向这个接口发请求的时候，看响应结果是不是comments，也就是是不是上边return的这个对象，是不是说明依赖注入成功了。那接下来咱们到浏览器里边发请求来试一下，确保项目是正在运行的状态。

发言人   09:00

那我们回到浏览器里边来，接下来咱们直接刷新一下，干脆直接访问这个dos交互式文档，来敲回这那找到news list，这个是不是咱们完成了依赖注入的那try IT execute一下来，接下来往下来看有没有拿到comments这个结果是不是已经拿到了？当然，我们现在是只依赖注入到了news list里面，user list有没有注入？并没有。

发言人   09:32

这个时候你去发请求来看，有那个limit，还有这个skip吗？还没有，是不是那个默认的message？Hello, 这个就是咱们当时在PPT里边说的，注入到哪个处理函数里边，哪个接口是不是才用了这些通用的代码。那user's st没有注入，那当然是不是就用不到上面定义的那些重复的代码了？那当然我们是不是也可以把它注入一下，比如说我们还是叫comments等号赋值，是不是dependence一下？Dependence那怎么写？写名字就行，注意我一回车，它是不是自动帮我把小括号加上，我们删一下。

发言人   10:18

好，那注入完了有没有拿到结果呢？咱们return comments来试一下，来保存。再回到浏览器里边，咱们刷新一下，刷新一下。这回找到user list，try IT1下来excuse来看看有没有拿到依赖项里边return的结果，哎也拿到了。

发言人   10:39

那这个就是咱们的依赖注入，我定义了依赖项了，那接下来注入到哪个处理函数，哪个接口是不是能用到这些依赖项？如果没有注入的，是不是当然就用不到了？我在这里再补一个注释，就是我们在路由处理函数这儿是不是第三步就是声明依赖性。其实大白话来说就是注入了这个依赖，对不对？依赖注入了。好，那这个就是fast API依赖注入系统的写法以及执行结果了。

发言人   11:14

学完了写完了，接下来我们回到PPT当中总结提炼一下重点。我有两个问题，第一，在fast API里面依赖注入系统有什么用？对，它是抽取可重用的组件，是不是实现了代码的复用解耦解耦了是不是可以轻松替换进行测试了？好，第二个问题，怎么使用依赖注入系统？对三步走创建依赖项导入dependence是不是声明依赖项其实就是dependence，注入是不是就OK了？好，那具体的代码咱们来回顾一下，当然在这里倒包是不是放在最上面了。

发言人   11:53

好，首先定义依赖项，我们来个函数就行了。把重用的这些代码是不是给它封装起来。那在需要的接口这个路由处理函数里边是不是使用dependence小括号，小括号里边注意写的是不是那个依赖项的名字啊不要写函数调用，因为在这儿写了依赖项的名字。当我们向这个接口发请求的时候，fast API是不是自动找到这个名字，找到对应的依赖项，重复的代码是不是会被自动调用起来的那整个代码截图就是依赖注入系统的写法了。

发言人   12:27

接下来是不是该同学们动手了？那大家来动手，把依赖注入系统的使用流程来实现一下。另外实现的时候要注意观察是不是注入到哪个接口，哪个接口就能用这些重复的代码拿到这个依赖项的结果。那接下来各位同学来动手试一下依赖注入。