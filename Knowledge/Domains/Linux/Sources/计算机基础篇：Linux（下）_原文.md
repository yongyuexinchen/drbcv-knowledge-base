计算机基础篇：Linux（下）

计算机基础篇：Linux（下）_原文

2026年07月22日 14:53

发言人   00:01

好，同学们，这节课我们来讲这个dota指令，还有一些其他指令。Dota指令它也是最为复杂的一些指令。那在讲dot指令之前，先给大家讲一下dota工具它是干啥的，以及这个工具是为什么被开发出来，它是需要去解决哪些问题。我们还有前两节课那个房子来举例，给大家讲一下这个倒车工具。

发言人   00:32

假如说我们有一个房子，我们很精心的装修了这个房子，房子里面的设施也是非常的豪华。那我们想旅游的时候，也住自己的房子怎么办呢？我就不想住宾馆或酒店，就想住自己的房子怎么办呢？相当于是他们打比方，就是把你的房子装了一个房车里头，房车特别大能把你的房子容纳下。这样我们在旅游的时候，不管你在任何地方都可以住在自己的房子里。

发言人   01:03

这些dota它也就是类似的功能，就比如说我们在自己电脑里有这项目，这些项目它可能需要很多工具才能跑起来，那我也想在别的电脑里跑这些项目怎么办？那我们就可以用刀片工具来装载这些项目，把项目成功的迁移到别的机子上，也就是房车迁移到别的地点，也就是房车把我们和我们的房子迁移到别的地点，你可以把它理解成车子装着你的房子去别的地方，你还可以住自己家，这意思，这个就是刀叉工具。如果有熟悉虚拟机的朋友可能会比较了解一下，你可以把刀砍列为一个小型的虚拟机。但是这些虚拟机和正经的虚拟机还不一样。虚拟机它是一个操作系统里该有的内容它都有。而docker不一样，它会省略很多部分，它只需要一些必要的组件，这就是刀片工具，接下来给大家讲一下docker它是如何安装的。

发言人   02:09

Doctor安装是一个固定的流程，大家需要执行这些指令就可以，包括后面咱们需要执行一些docker代码之类，大家也不用自己写，都给大家写好了。就是我们做这些指令，这些基础教学课的意义是什么？就是我希望大家先播一遍，然后后面边跑项目边反过头来再品这三节课。这样的话大家可以理解每一步的它的作用，从而避免咱们去盲目的无脑去跑项目，这样的话是不行的。同学们首先我们要更新一下软件包，我们下载最新的这些docker接下来我们安装软件，你也可以复习一下这些指令，速度是不安全的，安全线APT空格excel也就是安装软件包，后面的是软件包的包的名称，我们直接安装就可以。

发言人   03:17

接下来我们要执行这条指令，这条指令不是很常见，给大家讲一下这条指令这个开头call这个指令，你可以把它理解为你在网上点了一个链接，就是这些动作。执行这条指令，这里一定要输入OK才证明成功了，接下来我们再执行这一条指令。

发言人   03:50

这里按回车。

发言人   04:02

我们再次更新一下软件包的。所以。

发言人   04:10

现在就是安装doctor了，执行APP控制，install doctor以及doctor的其他一些工具。

发言人   04:53

就安装成功了。安装成功好之后，我们再检查一下它是否安装成功。这些指令的大家在后面应该会经常碰到。这些指令它分为两部分，前面是一个软件包的名字，后面是这个版本。

发言人   05:12

这条指令就是查看这个软件包的版本。一般是用来检查软件是否安装成功了。如果软件安装成功，它就会弹出软件的版本。如果没有安装成功，它就不会弹出。当然你也可以用它来看软件版本，你可以看到它弹出了多少版本，对不对？那就证明它安装成功了。接下来我们安装doctor couples。

发言人   05:52

在这儿给这个dr compos一个执行权限。看一下到时compose安装成功没，可以看到这里，后面这些部分是跟上面的指令是一样的，这里输出了docta compose的版本就证明成功了。

发言人   06:12

也给大家讲一下这个doctor和doctor compose它们的一个区别。Doctor它是专注于这房车，你的房车里是不是有你的屋子，对不对？那可能就有厨房、有卫生间、有客厅。我们只需要把这个房车给运到指定的地点就行了，或者说就是其他的一些电脑的机子上，我们这一个单子车就叫doctor容器。那dot compos就不一样了，它是要管理这车队。这个车队里面它可能会有不同种类的车，可能有厨房车、卫生间车，还有车型这种车，每种车它又有很多种车组成。就比如说厨房车可能就有五辆，卫生间车也有五辆，还有客厅车也有五辆，他们整个被运送到另外一个地方。这里面的每这车被叫做一个服务，每种服务有很多dota容器，它doctor x pose一般是用在一个项目要启动很多服务的情况下，主要是用docker一个启动很麻烦，这是docker和docker trump s的区别。Dota和dr p post安装好之后我们就去用一个具体例子给大家来演示一下。首先我们要先撤拢一个项目。

发言人   07:38

我们再看一下有哪些项目我们要对应，我们要去对应的目录底下把项目给启动起来。

发言人   07:57

这就是项目的目录，我们可以看一下它底下有哪些目录。一般用刀叉启动的项目，它底下都有一个刀砍文件夹。我们进入到dollar文件夹里，看一下doctor文件夹里它有些什么。一般用dot compose启动的项目，它都会有一个docker账compose点yml这些文件，我们把它提供一下。

发言人   08:25

大家先不用着急，我们一会儿会给大家详细讲解指令，现在执行的这些操作，你可以把它理解为房车在搬家的一个过程，因为我们用的是doctor compose列指令。所以他可能所以他这里可能就会有几十辆的厨房的车，卫生间的车有十几辆，所以他可能就是有十几辆的厨房车，也有十几辆的卫生间车，也就是NYSQLG的服务。这是不同的服务，也就是对应不同类型的车。可以这样理解，我们可以等待它拉取一下。好，我们的docker镜像就拉就好了，服务也成功执行了，也就是整个项目就启动起来了。

发言人   09:15

我们接下来给大家讲解一下这些指令。首先是docker空格PS指令，dota PSG指针是查看运行中的服务。我们来看一下，你可以看到这些服务，一次、两次、三次的。

发言人   09:32

那又如何去理解这些正在运行中的服务呢？你可以把它理解为整个车队的这些房车，比如说厨房车、卫生间车，有没有正在使用，有没有在工作，有没有在工作中。也就是说厨房里有没有人做饭，卫生间里有没有人上厕所。如果有人在用的话，他就会把这些房间里列出来。也就是这些服务它已经启动了，那就会把这个服务给列出来。当然如果没有启动的话，它是不会列出来的这是doctor空格PS的这指令。

发言人   10:07

接下来是docker images，这两个是有点区别的。大家讲一下这个docker images这个是啥东西？就是它直接展示的有哪些容器。不管你这个容器有没有运行一下跳转到容器的概念，有些同学可能反应不过来。不过也没有关系，还是那句话，大家先熟悉一下后面跑一跑项目，跟着我跑一跑项目，再回过头来细品这几节课就明白了。

发言人   10:35

再给大家强调一下，容器是单个的车，也就是刀片启动的容器。服务是一种类型的车，使用dota compose启动一些服务，服务里可能会有很多容器，我们再返回到dota image。在用房车的例子，就比如你有一个房车的车队，但是你这个房车里有些车是没有被人使用的，有些车是正在使用的那docker images这些指令，它是会把所有的房车都列出来，不管你有没有正在使用这些房车，doctor空格PS它会列出来正在被使用的房车，也就是正在运行的服务，这是他俩的一个区别。

发言人   11:19

Docta PSD指令，它平时就是我们应该会用到，就是在排查项目不涨的过程中会使用到它，也就是会检查项目有没有被成功的运行。就比如说你启动一些项目，可能要有很多的服务器，可能很多服务器都会同时启动。那有时候这项目打不开了，那怎么办呢？我们查看一下有没有可能哪些服务它没有被成功启动，或者是缺少哪些服务，那我们就需要用到doctor风格PS看一下倒车PS。

发言人   11:55

接下来是docker碰着tempos，碰着APP，碰着账地。这些指令就是启动docker couples所包含的这些服务的这些指令，这些指令我们可以先往旁边放一放，我们先用这个指令先停止这些服务。重新给大家讲一下，我们把这些服务都先停止，doctor jumps down，也就是把这些服务全部停止的意思。我们再用doctor comes up down d把这些服务在全部的重新启动起来，这就是质量指令，也就是用来停止所有服务的，一定是用来启动所有服务的。而且质量指令在使用的时候，一般在有doctor杠compose点yml的目录下来执行。Dot compose指令，你不能在任意的目录下执行。

发言人   12:58

接下来是docker compose restart，我们来看一下这个指令它是用来重启某个服务的。就比如有可能这些服务它运行成功了，你可以看到它正在运行。但是它可能运行的时候会有点小毛病，而你不知道问题出在哪儿了。但是你觉得它就是有毛病，那我们就把它重启一下。我们就用doctor compose restart这些指令。这些指令必须在有dota compose点yml这个文件的目录下来执行这个指令后面要跟服务的名称。

发言人   13:41

上面这些是所有已经启动了的服务。你要启动一个服务，不能直接用docker NG杠1这名字，这是容器名字，它不是服务的名字，F的名字是中间这段。如果就是你听不懂的话，你就记住，只用中间这段就行，也就是docta problem restart，后面的服务的名称。我们执行看一下。可以看到它就重启了，这是doctor champs resort指令。

发言人   14:20

接下来是这个。Doctor restart也就是重启doctor重启服务这个意思。Doctor restart它和这个doctor compose restart它是有本质的区别的。Doctor指令可以无视目录来重启对应的容器，而doctor composite的指令它必须在对应的目录下来，都是composter，他就没有目录的限制。给大家看一下。如果是doctor start后面，它必须跟容器名称，也就是全称。不能用服务器的名称。这是另外一个区别。

发言人   15:15

接还是doctor转，运行一个新的dota容器。我们来给大家演示一下，这里有一段这个刀塔传，一般是用来创建新的dot容器的时候会拿这个指令，不过用的时候用的机会和用的地方比较少。OK成功拉取过来了，执行了一个hello world。它就是用来拉取镜像，也就相当于是房车在跑的过程中，也就是防止在跑，然后我们接下来创建一个新的容器。接下来是这个docker start启动服。

发言人   16:11

在用doctor start这些指令之前，我们先用这个docker风格PS空格杠A指令看一下刚才提到的服务，需要用他的名字。

发言人   16:28

需要用他的名字来启动这个项目，来需要用他的名字来启动这个容器，也就是到star的后面的名字，名字可以在name字段下寻找。用这个name字段启动刚才我们用dota run创建的容器，你可以看到，它就成功的启动了。

发言人   16:53

这是doctor star的资料，接下来是doctor stop，dota stop也就是停止的服务，它的用法和dota stop是一样的。

发言人   17:14

复制一下这个名字，把它停止掉。好，这样就停止了这个容器了。接下来是这两个指令，这两指令我在前面也已经讲过了，也就是用来查看这些doctor或者是doctor compos有没有安装成功。接下来是docker空格RM，顾名思义，同学们前面不是也讲RM了，对不对？那dot空格RM也就是删除的容器。就好比你有这房车，它到了一个固定的地点了，你不想要这房子怎么办？你想把扔怎么办？我们就用docker LM加这个容器名字。

发言人   18:09

把它直接删除掉。删除之后我们就不能doctor that了，就不能把这个容器倾斜了。我们可以试想，因为这房车已经没了，我已经把它扔掉了，所以我们就没有办法再去重新开他了。他可能已经被别人捡走了，就不停的运行起来了，所以就会报错。

发言人   18:32

接下来是docker restart加一个符号的指令，这个符号的指令它会重启所有的倒车服务。不管是由doctor空格champs或者是doctor创建的，它会重启所有doctor服务。只要是在运行中的房车，或者是有人在干活的房车，它会让所有的房车重新启动。这是这个指令的一个功能。如果再用docta创建项目的时候，你感觉这个项目有点问题，但你又不知道这个问题出在哪服务器里。那你就用这个指令把所有的服务器全部冲进医院，这就是这个指令的一个作用。接下来就是这个SSH这个指令，这个指令咱们后面会经常遇到它它这个指令我们一般在用auto DL这个云云服务器的时候会经常用到，我们也给大家演示一下。

发言人   20:01

我们在这儿租一个机子。那SSHD指令，它是帮我们远程登录，我们在云服务器里租的一个disease指令。就是我们在本地可以来执行我们租的这个服务器。给大家讲一下这个怎么使用，当你要远程登录机器的时候，你需要首你首先需要把这指令你首先需要把这个登录指令来复制下来，粘贴到这里。那我们怎么编写SSH这个指令呢？你需要把这部分。和这里进行一个替换。把这串数字替换到这里，这样就完成了SSH指令的一个编写。

发言人   20:54

那我们怎么用这指令呢？我们首先同学们需要来到我们的电脑的左下角，然后搜索一下CMD或者是windows power show。然后把这条指令给粘贴进去，回车。这会儿他就会问你要不要连接是yes or no，输yes就代表我们要链接，然后稍等片刻，然后再他问我们要密码，然后这时候我们再输密码，密码从哪里获得呢？在这里，同学们这里会有密码，我们把它复制下来。

发言人   21:34

把它粘贴到这里，在这儿输入你的密码。在这输入密码的时候，是区分大小写的，而且书的时候任何内容它都是不显示的。我们可以看到它这是一个密码保护一个措施和机制。如果你在输的过程中是没有显示任何东西的话，是正常的。因为他在保护我的密码，保护我们的密码。

发言人   21:59

这些指令可能还有一些地方需要修改，也就是这部分。一般我们启动这个项目的时候，它会弹出这样一串数字，就比如说这就比如说是这个。Local URL。后面再标一个这样的6006，启动成功后，它会显示这样一串数字。那我们怎么用SSH指令登到我们的远程的机子，也就是我们远程云服务器的机子来看到我们这项目的，如果他这local URL后面冒号后面真的是6006的话，那我们就把这一串替换到这里。如果后面真的是8501的话，那我们就把这串替换到这儿，替换成8501，这样就可以。这也就是SSH遂川指令的用法。

发言人   23:06

大家如果这条指令看的有点迷糊的话，也不用管，因为后面以实操为准，就是带大家过一遍。很多同学没有接触过电脑，没有接触过这种云服务器，还是以云端听这些可能会有点听天书，我问题不大。后面跑一跑项目，你再配合前面的这节课反复观看，大家就悟了，瞬间开悟。

发言人   23:26

OK，这节课就是docker指令和SSHG6G6以及一些相关的指令。今天的课程就先到这里了，咱们我们再见。然后后面会带大家来一步一步的实操操作的。也希望大家在这个学习过程中，可以保持一种比较谦卑的心，来持续的来进行学习和成长进步。也祝大家一切好运。有任何问题就在学习群里艾特老师就可以了。好，同学们，再见。