# 021 3.6.3 VLAN的基本概念与基本原理 (1)

> Source: 021 3.6.3 VLAN的基本概念与基本原理 (1)_原文.docx

021 3.6.3 VLAN的基本概念与基本原理 (1)_原文

2026年07月19日 11:53

发言人   00:00

各位同学大家好，在这个视频中我们要探讨V链虚拟局域网技术。我们知道链是局域网的一个英文缩写，然后这儿的V是virtual，对应的就是虚拟。好，虚拟局域网在大纲当中处于这个位置，我们的王道书是按照大纲的这些标题顺序来编排的。所以在王道书上，以太网的后面跟的是这个i triple e802.11无线局域网。但是我们现在先跳过无线局域网这个部分，因为V链虚拟局域网和以太网是一脉相承的东西，所以我们把这两个部分的内容连起来学，效果会更好一些。威廉这个部分是2022年才加入考纲的，所以属于一个还比较新的考点。在i triple e802委员会当中，v line这项技术主要由802.1Q这个工作组去负责。所以和微链相关的很多技术标准都是以802.1Q去冠名的。

发言人   01:04

好，在探讨这个虚拟局域网之前，我们先来看一下，一个传统的这种大型局域网可能会面临什么问题。我们以大家熟悉的校园网为例，很多学校的校园网就是一整个大型的局域网。在校园网内部会通过很多交换机去把各种节点连接起来。比如说学生的电脑以及学校的数据中心，还有校园网服务器等等。那通过之前的学习，我们知道通过交换机连接的各个节点，它们属于同一个广播域，对吧？这就意味着在这个网络内任何一个节点发送一个广播帧，这个广播帧都会被交换机广播到与之相连的所有节点。经过逐层的广播之后，最终整个校园网，整个局域网内部的所有节点都会收到这个广播。真好。

发言人   02:03

现在问题出现了，如果这个局域网规模比较大，连接的节点数比较多，那是不是意味着在这个局域网内广播帧出现的可能性很高。而一旦广播帧变多，那么整个网络的数据传输的负载，以及每个节点接收并且处理广播帧的这种给节点带来的负载都会变得很高。甚至在某些情况下可能出现大规模的广播风暴，就是整个网络的负载可能会被广播帧给占满。好，这是一个大型局域网面临的第一个问题。

发言人   02:42

接下来第二个方面也会有安全性的问题。可以看到一个校园网内部可能会有一些节点，它是高敏感的节点。比如说校园网的服务器以及学校的数据中心，这些高敏感的节点会暴露在整个局域网上，会被局域网内的其他节点探测到，并且很可能受到黑客的攻击。好，这是一个大型局域网面临的第二个问题，就是安全性。

发言人   03:12

那怎么解决这些问题呢？I triple e802.1Q工作组提出了V虚拟局域网的这种解决方案。我们可以把一个局域网内部的节点重新进行分组，把这些节点归为不同的虚拟局域网。接下来为了简化探讨，我们引入这样的一个模型。两台交换机连接了多个节点，交换机和交换机之间用一条干线链路进行连接。干线链路通常是更快速的网线，而节点和交换机之间连接的这种普通链路，我们用普通的网线就行了。好，那么在现在这个模型当中，所有的节点都属于同一个局域网，对吧？也就就是说所有的节点都处于同一个广播域，并且局域网内各个节点之间是可以相互发现的。

发言人   04:08

好，那么接下来我们引入虚拟局域网。我们不妨把所有的这些节点分成两个分组。V链十这几个节点把它们划分为同一个虚拟桔网，V链二十这几个节点划分为另一个虚拟局域网。这样的话每个v an每个虚拟局域网就是一个独立的广播域。并且刚才我们提到的V30VN20 10和20，这个参数我们把它定义为VID，也就是虚拟局域网的ID好，在这种情况下，假设A结点发送一个广播帧，那么这个广播帧最终只会被从属于同一个虚拟局域网的节点所接收。也就是BCD还有HI这几个结点可以收到A结点发送的广播帧。但是VID等于20的这个虚拟局域网，这几个节点就接收不到A节点发送的广播。

发言人   05:13

真不知道大家有没有关注过windows电脑的一个功能，叫做网上邻居。你在你的电脑上可以看到你的网上邻居，而所谓的网上邻居就是和你从属于同一个局据网的，并且没有隐身的那些设备。那么划分了VN之后，比如说对于A结点而言，BCDHI是他的网上邻居。也就是说A结点可以探测到BCDHI这几个结点的存在。但是EFGJKL这几个节点是A结点探测不到的那这样的话其实就可以提升一个局域网的安全性，对吧？

发言人   06:00

比如说使用van技术，我们可以把一些敏感的节点划分为同一个v an而其他的这些节点在划分更多的微量。只要这些终端节点和敏感节点不属于同一个微量，那么这些终端节点对敏感节点的访问就是受到限制的。好，所以划分微链之后，我们可以把一个很大的广播域拆分成若干个更小的广播域。同时不同的V链是相互独立的，一个节点只能探测到和自己从属于同一个V链的节点，而不能探测到其他V链的节点，从而就提升了网络的安全性。

发言人   06:44

值得一提的是，划分微链这个事情需要支持微信功能的以太网交换机来实现。普通的以太网交换机是不支持微链功能的。比如这是一台支持微链功能的交换机，它总共有八个接口，交换机的管理员可以去配置如何划分v an好，接下来我们要展开探讨划分V链的3种方式。

发言人   07:11

第一种方式我们可以基于接口去划分v an比如对于刚才这个例子来说，我们可以在交换机一和交换机二的后台进行配置。这是管理员做的事情，做什么样的配置呢？它可以设置2个VID分别是十和20，也就是说要划分为2个VN那对于交换机一而言，VID10对应的接口号是1234。也就是说只要连接了1234这几个接口的节点，我会把它划分为VN10。好，除了1234之外，交换机二这边也会有VN10相关的节点。那么对于交换机一而言，它是通过八号接口和交换机二进行连接的，所以这儿也需要把八号接口和VID10进行一个绑定，这么做是有好处的。

发言人   08:14

比如说A结点发送一个广播帧，交换机一收到这个广播帧，并且这个广播帧是从一号接口进来的。那么检查VID和接口号的这种映射关系之后，交换机一就能够知道和一号接口从属于同一个VN的节点，分别连接在二号接口、三号接口、四号接口和8号接口。因此它会从二号、三号、四号和八号这几个接口去转发A节点发来的广播真好。

发言人   08:52

对于交换机二来说也是一样的，它也需要绑定VID和接口号的这种映射关系。当交换机二从一号接口收到一个VID等于十的这种广播帧的时候，他就知道这个广播帧需要从二号接口和三号接口再转发出去。那这样的话就实现了广播帧的传输。好，细心的同学刚才可能会产生这样的一个疑问，一个广播帧从交换机一传输给交换机2，那交换机二怎么去判断这个广播帧到底是发送给哪一个VN的呢？这个问题我们一会儿会来探讨。

发言人   09:37

好，总之我们可以基于接口去划分。VN交换机的管理员需要把VID和VID对应的接口号进行绑定。那么在这种方案中，如果一台计算机它换了一个连接的接口，比如说I这台计算机它不通过三号接口连接，而是连到了八号接口。那么I这台计算机就不再属于v an 10。因为在交换机的后台，它是根据这个接口号去划分Willy的。所以只要连接的接口一变，那么这个节点它从属于哪个V量就有可能发生改变。这样其实不太方便对吧？那能不能让一个节点即便换了接口，也可以不改变它从属的VI呢？

发言人   10:29

接下来我们提出另一种微链的划分方式，可以基于mac地址。如果采用这种方式，那么交换机的管理员在后台配置的就是VID和各个设备的mac地址之间的这种映射关系。每个节点都会有自己独一无二的mac地址。那在交换机一的后台，我们可以把VN10的这几个节点它们的mac地址和VID10进行一个绑定。这样的话即便其中的某一个节点他改了一个接口，只要它的mac地址不变，那么这个节点依然从属于同一个位置。

发言人   11:14

好在这种情况下交换机是怎么工作的呢？比如A结点现在发送一个广播帧，那么交换机会检查这个广播帧的发送方是谁。根据发送方的mac地址去判断这个节点它从属于哪个位置。好，既然A结点属于vn 10，因此A结点发送的广播帧就应该转发给BCDHI这几个节点。交换机能够知道这些节点分别对应哪一个接口。比如说H和I这两个结点，它们对应八号接口，所以A结点发送的广播帧会通过干线链路传给交换机2，那交换机二又会把这个广拨针转发给H和I这两个结点。好，这是第二种V类的划分方式，基于mac地址。

发言人   12:09

接下来第三种划分方式基于埃批地址。在一个局域网内部，每一个节点都会有自己的埃批地址。当然IP地址是属于网络层的概念，那么和mac地址类似，我们也可以通过IP地址和VID进行绑定，用这样的方式去实现v line的划分。由于涉及到埃批地址，因此这种划分方式需要网络层功能的支持。同时这种划分方式甚至可以让微量的范围跨越路由器，让多个局域网的主机组成一个微栏。

发言人   12:48

什么意思呢？比如说交换机2，它连接了一个路由器，而路由器的另一端又连接了交换机以及连接了多个节点。之前我们说过路由器的每一个接口对应的应该是不同的局域网，对吧？也就是左边这部分属于同一个局域网，右边这个小部分又属于一个局域网。如果我们使用IP地址去划分V量的话，我们可以做到说，比如说把右边这个局域网里边的某几个节点也划分为v an 10。那这样的话我们就把两个局域网内的这些节点组成了一个虚拟局域网，这就是可以跨越路由器的意思。如果要基于埃批地址划分v line，那么这些交换机它不仅是要实现物理层和数据链路层的功能，还需要实现一部分网络层的功能，就会使得这个交换机变得更复杂。好，这是第三种方式。

发言人   13:53

接下来我们来填一个坑，之前我们说A结点发送一个广播帧之后，交换机会把这个广播帧转发给与之直接相连的BCD这几个节点。与此同时也会把这个广播帧发给交换机2。那么交换机二如何去判断这个广播帧到底是从属于哪一个微链呢？关键就在这个地方，交换机和交换机之间传输的这个真它的格式并不是标准以太网帧的格式，而是802.1Q帧的格式。

发言人   14:32

802.1Q帧是在标准以太网帧的基础上演变而来的，它会在标准以太网帧的中间这个位置插入四个字节的信息。而这四个字节当中包含了VID的信息。也就是说假如现在A节点发送了一个广播帧，那么交换机一先判断A结点它的VID等于多少。好，知道这个VID等于十之后，由于交换机二这边也涉及到V链10，所以交换机一会在这个标准以太网帧的内部插入四个字节，其中包含了VID等于十这个信息。把这个真交给交换机2，那么交换机二收到这个针之后，就可以把这四个字节拆出去，同时去检查VID等于多少。那只要确定了VID交换机2，接下来就知道自己应该从哪几个端口去转发这个广播帧了。

发言人   15:39

好，所以主机和交换机之间还是以之前我们学过的那种标准以太网帧的格式去进行传输。而交换机和交换机之间的这种传输，为了让对方能够知道我这个针到底从属于哪一个危难，所以需要在其中插入关键的VID的信息。因此交换机和交换机之间的传输是以802.1Q这种格式的帧去进行的。

发言人   16:09

好，那么接下来我们不妨拆开来看一下这个802.1Q帧它的构成。标准以太网帧的构成是662N4收发斜竖宴，而802.1Q帧会在标准以太网帧的基础上增加四个字节，在中间编了一个新的口诀，6642N4收发V斜竖烟。新增的这四个字节我们把它称为v line标签。因此在标准以太网帧的这个原地址字段的后面插入V栏标签之后，就可以得到802.1Q帧。

发言人   16:48

对于这四个字节来说，它又有这样的几个部分组成。首先最前面的这两个字节它是固定死的，这个固定值是指明这个真的类型是802.1Q真好，这是前两个字节。在后两个字节当中，前面的四位没有任何意义，写什么值都行。比如说我们可以填入四个0或者四个一都可以，反正它毫无意义。那末尾的这12位关键的信息来了，这12位就是VID。

发言人   17:21

要指明这个帧的发送方，它所属的VVID等于多少，怎么判断发送方所属的VID等于多少呢？这就可以回到之前介绍的VN划分方式。在每一台交换机的内部，它肯定能够通过自己的这些内部的数据结构去判断他所收到的一个数据帧发送方到底从属于哪一个VID。好，这就是802.1Q帧的构成。主机和交换机之间以标准的以太网帧格式进行数据传输，而交换机和交换机之间会把这个标准以太网帧内部再插入四个字节。很显然新加入这四个字节之后，末尾的这个CRC循环冗余校验码就需要重新生成，对吧？因为循环冗余校验是针对前面这一整个部分去计算的校验，对吧？

发言人   18:23

好的，在这个视频中我们探讨了虚拟局域网。在一个传统的大型局域网当中，我们会面临比如说广播风暴以及安全性等等一系列的挑战。所以我们有必要把一个大型局域网内部划分成更小的虚拟局域网。虚拟局域网有这样的一些特点。首先每一个虚拟局域网构成了一个独立的广播域，同时我们需要使用支持虚拟局域网的以太网交换机去进行配置。另外每一个虚拟局域网都会对应一个VID。

发言人   18:59

我们介绍了三种划分微链的方法。如果要基于接口划分V链，那么在交换机内部就需要记录VID和接口号的一个映射关系。如果要基于mac地址，那么就是记录VID和mac地址的映射关系。而如果基于IP地址的话也是一样的，VID和IP地址的映射关系就需要被记录下来。最后这种V链的划分方式可以允许让V链跨越路由器，让多个局域网内的主机组成一个V最后我们还介绍了802.1Q帧，主机和交换机之间还是以标准的以太网帧去进行数据的传输。标准以太网帧的构成是662N4收发斜竖验。而交换机和交换机之间的传输，也就是在干线链路上的传输，我们需要以802.1Q帧的格式去进行。

发言人   19:51

需要在66的后面加四个字节，就是V栏标签的信息。而V栏标签的构成是这样的，四个字节微量标签总共对应32比特。这32比特被拆分成了16、42这样的三个部分。最开始的16个比特是固定的，写死的，然后中间四个比特没有任何意义，随便写什么都行。最后的12比特最重要，它表示的是这个帧的发送方属于哪一个VID。好的，以上就是VN这个考点。