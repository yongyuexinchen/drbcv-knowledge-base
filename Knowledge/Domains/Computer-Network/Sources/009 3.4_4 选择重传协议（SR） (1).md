# 009 3.4_4 选择重传协议（SR） (1)

> Source: 009 3.4_4 选择重传协议（SR） (1)_原文.docx

009 3.4_4 选择重传协议（SR） (1)_原文

2026年07月19日 11:52

发言人   00:00

各位同学大家好。在这个视频中我们要探讨选择重传协议。选择重传协议和停止等待协议以及后退N帧协议有很多相似的方面。选择重传协议的英文缩写叫SR selective repeat，它的滑动窗口大小有这样的特点。首先发送窗口和接收窗口都要大于一，其次它在滑动窗口上有一个特殊的要求，就是接收窗口不能比发送窗口大。一会儿我们再解释为什么要这么规定。好，另一方面和停止等待协议后退N帧协议类似，如果我们用N个比特给臻进行编号，那么必须满足WT加WR小于等于2的N次方，要满足这样的一个条件。

发言人   00:47

好，另外在确认机制方面，首先由于选择重传协议，它的发送窗口的大小是大于一的，所以发送方可以连续的给接收方发送多个真。在后退N帧协议当中，接收方如果连续接收到多个正确真，那么可以采用累计确认的这种方式，也就是只返回最大的正确帧的帧号就可以，这是后退N帧协议。而对于选择重传协议来说，它并不支持这种累计确认的方式。必须每收到一个正确真就返回一个ACK好。这一点是选择重传协议和后拖延真协议不同的地方。另一方面在选择重传协议当中，如果数据的接收方收到的这个真检测出差错，那么接收方会丢弃这个帧，并且给发送方返回否认真。用否认帧NAK主动的去请求发送方重新发送哀号真。那这一点也是选择重传协议和之前的两个协议不同的地方。

发言人   01:59

之前学习的两个协议，在重传机制方面只有超时重传这种规则。也就是发送方超时没有收到ACKI的时候，再重传I号真。而选择重传协议当中，这个重传机制除了支持超时重传这种规则之外，还支持请求重传，就是会有数据的接收方主动的用NAK这种否认真请求发送方去重传爱好。真这样的话，发送方就不用非得等到这个超时再进行重传，可以让重传的效率进一步的得到提升。

发言人   02:38

好，这是选择重传协议的一些要点。接下来我们会结合一些例子带大家感受这些要点。首先我们来探讨选择重传协议的这个窗口大小的限制条件。假设我们用三个比特给这些帧进行编号，所以这些帧的编号就是先从零再到7，然后再回到01234567用这样的方式给这些帧编号。那接收方这边也是一样的，给每个帧用0到7再回到零这样的方式进行编号。

发言人   03:09

好，刚才我们说在选择重传协议当中，窗口大小的限制有这样的两个条件必须满足。首先第一个条件，如果用N个比特给帧编号，那么发送窗口和接收窗口的这个总大小要小于等于2的N次方。第二条件，接收窗口的大小要小于等于发送窗口的大小。那么对于刚才给的这个例子，如果我们用三个比特给帧编号的话，我们可以取发送窗口，大小等于4，接收窗口的大小等于32的3次方等于8。满足第一个条件，发送窗口和接收窗口的大小之和小于等于8。同时也满足第二个条件，接收窗口要小于发送窗口。所以如果这么设计，那么就符合选择重传协一的这个窗口大小的两个限制条件。

发言人   04:04

好，接下来假设我们把接收窗口的大小改为四，那么此时同样满足条件一和条件2，那这种设计也是合法的。好，再接下来如果我们把发送窗口的大小改为三，那么此时不满足条件2，我们说接收窗口必须小于或者等于发送窗口的大小。那由于不满足第二个条件，所以这样的设计是不被允许的。

发言人   04:33

好，我们简单解释一下为什么要规定接收窗口一定要小于或等于发送窗口。回到前面这个例子，接收窗口等于3，发送窗口等于4。那在这种情况下，发送方显然可以连续的发送四个真，对吧？连续的发送四个真，那么接收方在接收到第一个帧的时候，它的这个接收窗口是不是就可以往右开始往右滑动了，对吧？那只要往右滑动一格，接下来后面的这三个真陆续到达，也可以被顺利的接收。这是接收窗口小于发送窗口的情况。我们可以充分的利用发送窗口和接收窗口的这个容量。

发言人   05:18

好，再回到这个例子，在这个例子当中接收窗口要比发送窗口更大。那在这种情况下，发送方最多只能给他先发三个真，对吧？那么接收方连续接收到这三个帧之后，他又会给发送方依次返回这三个真的ACK确认的信息。那么只有发送方接收到这些确认信息之后，发送方的窗口才可以向右滑动。只有窗口向右滑动，它才可以继续发送后一个也就是三号真。所以在刚才这个例子中，我们可以感受到，如果接收方的窗口大于发送方的窗口，那么接收方更大的这些小格子它就会长期保持为空，利用率不高。所以如果接收窗口大于发送窗口的话，那么这两个窗口的性能就没办法得到完全的发挥。好，所以在选择重传协议当中，我们必须满足第一个条件和第二个条件。

发言人   06:20

好，那么在实际应用当中，选择重传协议的发送窗口和接收窗口通常会取相等的这种情况。比如说用3比特给帧编号，那么我们可以取发送窗口和接收窗口的大小都等于4。在这种情况下两个窗口相互配合效率会更高。

发言人   06:41

好，接下来我们用几个事例来感受选择重传协议的工作过程。发送窗口和接收窗口的大小都等于4。此时发送方可以给接收方连续的发送零号、一号、二号和三号，这几个真好。接收方会陆续的收到这几个帧，并且要给发送方返回这几个真的ACK。同时接收窗口可以向右滑动，可以看到接收方每收到一个正确帧，都要给发送方返回这个正确帧所对应的ACK。相比之下，在后退N帧协议当中，接收方可以累积确认。如果是后退N帧协议，像刚才这种情况，连续收到0123这几个真的话，那么后退N帧协议可以只返回ACK3对吧？好，这一点大家需要注意，两种协议的确认机制是有一些区别的。

发言人   07:41

好，我们让故事继续。发送方接下来会陆续的收到ACK0123。确认了这几个真都已经被正确的接收之后，发送方的发送窗口就可以向右滑动。那接下来发送方就可以发4567这几个真。

发言人   07:59

下面我们看一种异常情况的处理。假设在接下来这个发送过程当中产生了数据真的丢失，那么情况就是这样的。假设接下来发送了4567这几个帧，每发送一个帧也会给这个帧设置计时器，也就是一个倒计时。假设其中的五号真因为通信线路的故障而丢失了，那这样的话接收方就只能接收到四六七这几个真。那么收到这几个帧之后，接收方的这个接收窗口可以向右滑动一格。与此同时把四六七这几个真的ACK返回给发送方，那么发送方接下来会收到467的ACK。在这种情况下，发送窗口只能往右滑动一格，因为五号真此时还没有被正确的接收好。此时由于零号帧已经在发送窗口之内，所以可以发送零号帧，并且设置零号真的计时器。

发言人   09:04

随着时间的推移，五号真迟迟没有收到ACK，所以五号真的计时器会超时。在这种情况下，发送方会重传五号帧，并且重置五号真的计时器。好，接下来接收方就可以收到零号帧和5号帧，并且顺利的把这两个真人接收好。现在接收窗口可以向右滑动，与此同时接收方给发送方返回ack 0和K5。那这次ACK0和ACK5都可以在超时之前被发送方收到。那确认这两个真被接收之后，发送窗口就可以继续向右滑动。

发言人   09:47

好，刚才这个例子当中展示了数据帧丢失的时候，选择重传协议会如何处理。主要就是依靠超时重传机制。每个帧被发出的时候都会设置一个计时器，如果计时器超时还没有收到对应的ACK，那么就重传这个真好，这是数据帧丢失的处理。

发言人   10:10

接下来看另一种情况，我们退回到前面这一步。接下来我们假设接收方可以收到数据帧，但是由于检测出数据帧的错误而把数据帧主动的丢弃。那在这种异常情况下选择重传协议又是如何处理的呢？

发言人   10:27

在这个图示当中，现在发送方可以连续的传输4567这几个帧，并且每个帧被发出的都会设置一个计时器。那现在假设所有的帧都可以被接收方收到，但是五号真因为检测出差错而被接收方丢弃，那此时接收窗口只可以往右移动一格。那与此同时接收方会给发送方返回四号、六号、七号真的ACK以及五号真的NAK。NAK指的是否认真，就是告诉发送方说，五号真出问题了，你给我重新传一个，就是这样的一个作用。NAK是negative acknowledge的缩写，acknowledge是确认的意思，negative是否定负面的意思。英文缩写取了NAK这几个字母。

发言人   11:25

好，接下来发送方会陆续的收到四号真的确认、五号真的否认和6号7号真的确认。当收到四号真的确认之后，发送窗口就可以向右滑动一格。与此同时，由于收到五号真的否认，所以发送方会在这个计时器超时之前，主动的把这个五号帧重传一遍。那重传五号真的同时也会重置五号真的这个计时器。好，与此同时由于发送窗口刚才已经向右滑动了一格，所以零号帧此时也落入了这个发送窗口之内，因此零号帧也可以紧接着被发出。发出零号帧的同时也要设置零号真的计时器，接下来接收方可以顺利的收到五号真和零号针。

发言人   12:15

这一次重新收到5号帧之后，我们假设并没有检测出差错，那么五号真就可以被正确的接收。好在这种情况下接收窗口就可以向右滑动四格。由于是先收到五号真，所以会先返回五号真的ACK，紧接着收到零号帧，再返回零号真的ACK发送方收到ACK5之后，滑动窗口可以向右移动。在收到ACK0之后，滑动窗口可以继续往后移动。

发言人   12:48

好，刚才这个例子当中我们展示了，如果数据帧因为出现差错而被主动丢弃的时候会进行的处理。在选择重传协议当中，除了超时重传机制之外，还引入了请求重传机制。如果接收方发现一个有差错的真，那么除了丢弃这个真之外，还会主动的给发送方返回这个帧的NAK，主动的去请求发送方重传这个哀号真。那这样的话发送方就不必等到计时器超时再重传，而是收到否认帧就立即重传。这样的话就可以提升选择重创协议的效率。

发言人   13:32

接下来我们再看第三种异常的情况如何处理。我们假设在接下来的例子当中确认真有可能会丢失。好，回到最初的起点，现在发送方可以连续发送零号、一号、二号、三号帧，接收方也可以顺利的陆续收到这几个真。根据收到这些帧的顺序，接收方会给发送方陆续的返回ACK0、ACK1、ACK2和ACK3。

发言人   14:00

那现在情况发生了，假设ACK1和ACK3因为通信故障而半路丢失了，那发送方就只能收到ACK0和ACK2。那在这种情况下，发送方的发送窗口只能往右滑动一格。好，往右滑动之后，现在四号真又落入了这个发送窗口之内，所以可以发送四号真好。与此同时，如果一号臻和三号真的这个计时器超时了，那么发送方就会重传一号臻和3号帧，重传的同时也会重置这两个计时器。好，那么回到接收方这边，首先接收方收到了这个四号真，那四号真刚好也落在这个接收窗口之内，所以四号真可以被正确的接收，并且会给发送方返回ACK4。与此同时这个接收窗口可以再向右滑动一格。好，紧接着接收方还会收到一号和三号数据真。

发言人   15:00

由于这两个数据帧落在了接收窗口之外，所以接收方会认为这两个数据帧肯定是重复的那收到这种重复帧的时候，接收方会给发送方返回这两个重复帧的ACK好，这一次确认真并没有在中途丢失，所以发送方可以顺利的收到ACK4、ACK1和ACK3。那此时发送窗口就可以往右滑动。好，那刚才我们探讨了确认帧丢失的时候，选择重传协议是如何处理的。首先确认帧丢失会引起发送方超时重传，超时重传之后又会导致接收方收到重复真。那接收方判别重复帧的这个方法，就是看这个真是否落在了接收窗口之外。如果在接收窗口之外，那么就说明它肯定是一个重复真。每收到一个重复帧，接收方就会给发送方再一次返回这个重复帧的ACK，那用这样的方式就可以解决确认帧丢失的问题。

发言人   16:12

好，那么接下来我们要探讨这个窗口的大小，如果不满足这个条件会有什么问题。假设帧序号是三个比特，现在我们故意违反这个规定，我们让发送窗口等于5，接收窗口等于4，二者之和大于8，也就是不满足这个条件。好在这种情况下我们看会发生什么。首先发送方可以给接收方发送01234这几个真，那接收方首先会收到零号帧，只要零号帧被正确的接收，那接收窗口就可以向右滑动一格，与此同时给发送方返回ACK0。好，接下来1234这几个真也会陆续的到达，那接收方也会陆续的接收到这几个真，与此同时接收窗口向右滑动，滑到五这个位置。

发言人   17:06

好，那么接下来意外发生了。假设因为通信故障，ACK0到4全部都丢失了。那么发送方这边01234这几个真的计时器会陆续的超时，超时之后它又会重传零号、一号、二号、三号、四号。真此时零号真的重传就出现了问题。按照刚才这个例子，我们接收窗口往右滑动之后，零号是在接收窗口之内的。所以当接收方收到零号帧的时候，他会认为这个零号帧是合法的，并不是重复真。那此时接收方就会错误的接收零号帧。那对于1234这几个真，它们是落在了这个接收窗口之外，所以接收方会认为这几个真是重复真好。

发言人   17:54

回到刚才这个问题，如果不满足这个条件，就会导致发送方和接收方无法正确的判别哪些是重复真好。这就是必须要满足这些条件的原因。好的，在这个小节中我们介绍了选择重传协议，它和停止等待协议、后退N帧协议有很多相似的地方。比较特别的点，我用这种红色的这种框框给它框起来了。

发言人   18:24

选择重传协议的确认机制比较特别，当接收方收到正确真的时候，会返回这个帧的ACK。而当接收方收到一个错误的有差错的真的时候，会返回NAK否认真。用NAK否认帧来主动的请求发送方重传对应的这个I号真好。另外在选择重传协议当中要注意这个条件，接收窗口不能大于发送窗口的大小。那选择授权协议最需要注意的要点就是，首先是刚才说的NAK否认帧，用NAK来主动的请求重传。另外我们强调选择重传协议不支持累计确认，必须一帧一确认，这点是和后退N帧协议不一样的。好的，这就是选择重传协议。