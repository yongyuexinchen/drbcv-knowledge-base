# 010 1.2.2 计算机网络分层结构（下）

> Source: 010 1.2.2 计算机网络分层结构（下）_原文.docx

010 1.2.2 计算机网络分层结构（下）_原文

2026年07月19日 11:55

发言人   00:00

好的，继续来看计算机网络的分层结构。在之前的学习中我们介绍了计算机网络的分层结构以及各层之间的关系。我们把工作在某一层的软件和硬件模块称为这一层的实体。为了完成这一层的某些功能，同一层的实体和实体之间需要遵循某种通信协议。也就是说协议这个东西，它是约束了水平方向的通信规则。

发言人   00:32

为了让大家体会到协议的作用，我们不妨从水平的视角来看一下数据是怎么传输的。之前我们简单的介绍过数据传输的过程。从垂直的视角来看，发送方这边的数据会从最顶层开始逐层的往下传递。每一层都会对数据进行相应的处理，然后再放到物理传输媒体上进行传输。在接收方这边，当他收到数据之后，又会从最底层开始进行逐层的逆处理，最终获得原始数据。

发言人   01:08

我们不妨假设应用层需要实现LMN这几个功能。那N这个功能，我们不妨假设它是要把数据进行压缩，然后再传输，从而可以节省流量。基于这样的设计，发送方的第五层实体需要把数据进行压缩，压缩完了之后再交给下面的层次去处理和传输。而接收方的第五层实体对等的这个实体，它又需要把数据进行解压缩，解压了之后再把它上交给上层的用户。因此从水平视角来看，这两个用户他们感受不到数据被压缩和解压的过程。在他们俩看来，数据似乎是被原模原这样的传输的。但事实上发送方的第五层实体和接收方的第五层实体分别做了压缩和解压缩的过程。

发言人   02:10

好，那么为了支持这样的功能，我们不妨制定一个协议。这个协议的名字叫做YSCS协议。这是压缩传输的首字母的缩写，比较土的一个取名方式。

发言人   02:25

之前我们说过一个协议，它是规定了水平方向上的两个对等实体，他们之间的通信规则？那么为了实现压缩传输这个功能，我们需要在协议当中规定发送方的第五层实体。首先需要把这个数据进行压缩。数据压缩的算法有很多种，对吧？为了让这个接收方能够用对应的这个解压算法去解压，因此我们需要增加一个手部的信息，也就是一部分控制信息发送方的应用层实体。先把数据用某一种压缩算法进行压缩，然后在手部这个位置指明自己采用了哪一种压缩算法。这样的话，当接收方的第五层实体收到这个数据之后，他是不是就可以从手部这个位置去获取压缩算法的信息。紧接着再采用对应的解压算法把数据进行解压，再交还给上层的用户。

发言人   03:40

好，从这个例子中我们可以感受到，从水平视角来看，对等的两个实体之间，他们的通信需要遵循某种协议，而遵循这个协议的目的是为了实现某一种功能。那一个协议可能会规定在数数据之前增加一个首部，当然有的协议也可以再增加一个尾部的信息。反正无论是头部还是尾部，这些都属于协议的控制信息，是为了实现协议的功能而存在的控制信息。那具体首部和尾部需要多少个字节，以及每个字节具有什么含义，这些都需要在协议当中去给它规定清楚。好，理解了这些之后，我们就可以更深刻的体会到为什么要分层，以及为什么要制定协议了。我们说计算机网络功能是很复杂的，所以我们需要先把计算机网络需要支持的功能ABCDFG先给它罗列出来，然后再把这些功能安排到合适的层次当中。处于同一个层次的对等实体需要去配合着实现这一层当中的这些功能。因此我们需要去制定协议，去约束对等实体之间的这种通信规则。

发言人   05:07

好，了解了协议存在的意义之后，接下来我们再从垂直的视角再来看一下数据传输的整个过程。假设左边这个用户要给右边这个用户发送一些数据，那这些数据首先会被交给应用层进行处理，应用层负责实现某一些功能。为了实现这些功能，应用层和应用层之间的通信需要遵循某种协议。因此就需要给这些数据增加协议的首部，我们不妨把它称为应用层的首部。好，应用层按照协议的规定做了这些处理之后，又会把这一整块的数据作为一个整体，把它交给下面这一层传输层。传输层和传输层之间同样需要遵循某一些通信协议议，因此传输层也会增加协议控制信息，也就是手部的这部分数据。好，接下来传输层又把这一整块的数据作为一个整体交给下面这一层网络层。

发言人   06:15

同样的道理，网络层和网络层之间也需要遵循某种通信协议。因此网络层的实体也需要增加一个手部的信息。好，接下来网络层把这些数据交给数据链路层。数据链路层为了实现本层的功能，它需要增加首部和尾部的信息。无论是头部还是尾部，它最终都是为实现协议的功能去服务的。好，最后数据链路层再把这些数据交给物理层。在这种五层的体系结构当中，物理层并不会对这些数据进行任何处理。

发言人   06:52

他只负责把这些二进制的数据原模原样的传输到这个物理传输媒体上，这样就可以让数据到达下一个节点。好，下一个节点接收到这些数据之后，首先交给物理层，紧接着物理层，再交给数据链路层。这一层的实体会根据本层的协议规定去检查首部以及尾部的这些信息。然后根据这些控制信息完成数据链路层需要完成的一些事情。比如说完成差错控制的功能。好，如果这些数据没有什么问题的话，这层的实体会把本层的首部和尾部拆掉，然后再交给上一层进行处理。

发言人   07:39

第三层网络层它通过本层的这个协议控制信息去实现本层应该负责的功能。具体来说这一层要实现的就是路由，也就是数据分组的存储转发。所以第三层的这个首部信息，它其实会包含发送方的IP地址以及接收方的IP地址这些信息。好，那么路由器的第三层实体就可以根据这些信息去决定，这个数据分组下一跳应该把它转发到什么位置。

发言人   08:14

路由器的第三层实体会对H三这个部分的控制信息进行一些简单的处理。具体是什么处理我们现在先不展开。总之处理完了之后，他又会把这一整块的数据交给下一层，也就是数据链路层。数据链路层又会根据对等实体之间的这个协议去增加首部和尾部的信息，然后再把这些数据交给物理层，物理层再把这些数据通过物理传输媒体传给下一个节点。接收方的主机，它的第一层物理层收到这些信息之后，就会把这些信息交给数据链路层。

发言人   08:54

后面的这个过程就是类似的逐层的往上处理，每一层的实体只关注本层的这个首部信息。逐层往上处理，逐层的把这些首部信息给拆掉。最终交给用户的就是一个干净的没有手部信息的一个数据。这样我们就完成了数据的传输。这就是从垂直视角来看数据的一个传输过程。同一层次的对等实体之间，他们需要遵循某一种通信协议，而这个协议本质上是为了某一些功能而服务的那要让这个协议规定的这些事情可以落地，我们就需要在数据之前增加一个首部的信息，每一层只关心自己的手部信息。

发言人   09:48

好，缕清这个过程之后，接下来我们引入几个概念，分别是协议数据单元、服务数据单元和协议控制信息。英文缩写是PDU、SDU和PCR。首先看协议数据单元对等层次之间传送的数据单位就是这一层的协议数据单元。好比如说第五层和第五层之间，他们传送的数据就是原始数据加一个H五这个头部对吧？所以这一整块东西就是第五层的协议数据单元。好，类似的第三层和第三层之间它们传送的数据是这一整块，对吧？所以这一整块就是第三层的协议数据单元。

发言人   10:38

协议数据单元由服务数据单元和协议控制信息组成。协议控制信息指的是控制协议操作的信息，说白了就是刚才我们提到的这个首部第五层的协议控制信息也就是第五层的PCI就是第五层的首部。第四层的PCI就是第四层的首部。第二层的PCI包含了首部和尾部两部分的信息，所以首部和尾部加起来共同组成了第二层的PCI，也就是协议控制信息。

发言人   11:16

去除协议控制信息之外，剩下的这个部分就是本层的服务数据单元，也就是SDU这个概念。服务数据单元是为了完成上一层实体所要求的功能而传送的数据。这句话读起来比较拗口，但是通过之前的讲解相信不难理解。好，没有跟上的同学我在这儿给大家罗列了一些例子，大家可以暂停来感受一下。总之这三个概念之间的关系就是某一层的服务数据单元加上这一层的协议控制信息，共同组成了这一层的协议数据单元。而这一层的协议数据单又会作为下一层的服务数据单元。大家可以暂停来体会一下。

发言人   12:07

好，最后我们再来介绍协议的三要素。我们要制定一个完整的协议，那需要规定这个协议的语法、语义和同步分别是什么。这就是所谓的协议三要素。

发言人   12:21

首先第一个要素，所谓的语法指的是数据与控制信息的格式。比如说我们需要规定这个协议的控制信息也就首部，总共几个字节，每个字节的含义是什么？以及协议的数据部分，也就是SDU那个部分最多有多少个字节，这就是所谓的语法要规定数据的格式。

发言人   12:45

第二个要素语义指的是需要发出何种控制信息，完成何种动作以及做出何种应答。听起来比较抽象，举个例子，比如刚才我们提到压缩传输的功能。AB两台主机的五层应用层的实体要实现压缩传输功能，那么它们之间就需要遵循压缩传输协议。除了规定这个协议的数据控制信息格式之外，我们还需要规定在双方进行数据传输的过程当中，双方需要完成哪些动作以及做出哪些应答。比如说当发送方发完数据之后，接收方是否需要给它一个应答的信号，以及这个应答的信号有哪些种类。比如说我给他应答一个传输成功，或者给他应答一个传输失败，那这就是所谓的语义要描述清楚的东西。

发言人   13:42

好，最后第三个要素叫做同步，又叫做时序。在历年真题当中出现的是时序这个术语。那时序这个要素需要描述清楚，通信双方执行各种操作的条件，时序关系等等，是对事件实现顺序的详细说明。

发言人   14:03

比如说AB双方要遵循压缩传输的这个协议，那么这个协议当中我们要求发送方首先要给接收方发送数据。接收方接收完数据之后，需要立即给发送方一个回应。比如说传输成功，当然也可以回应传输失败，对吧？那如果发送方在10秒之内还没有收到这个接收方的传输成功应答的话，发送方会再次尝试重发重新发送这个文件的数据。所以我们用这样的一个时序图去描述清楚了发送方和接收方他们的这些动作之间的时序关系。好，所以语义这个要素是描述了发送方和接收方有可能需要做的动作有哪些。而时序这个要素是需要描述清楚这些动作的发生顺序。这三个要素相辅相成，共同的组成了协议。

发言人   15:15

在这门课的后续章节中，我们还会详细学习很多种协议。学完这门课之后再回来看这几个概念，相信大家也会有更清晰的认知。如果现在没有办法完全理解协议三要素的含义，这很正常。等大家学完整门课再回来看这个部分会觉得很简单。

发言人   15:35

好的，在这两个视频中我们介绍了计算机网络的分层结构。我们说计算机网络的功能十分复杂。为了让计算机网络看起来结构清晰，并且易于维护，易于实现，因此我们引入了分层设计的思想。把计算机网络分成多个层次，每一个层次负责实现若干个功能。对于同一层次中的活动元素，我们把它称为这一层的实体。我们可以把处于同一层次的实体称为对等实体。为了实现一个层次中的各种功能，对等实体之间需要根据某种协议的规定去进行通信。所以协议规定的是水平方向对等实体之间的这种通信的规则。

发言人   16:29

在一个分层结构的计算机网络当中，上一层的实体会通过接口去请求下一层实体的服务。而所谓的服务就是为上一层提供的功能调用。所以接口和服务这两个概念是垂直方向的，它描述的是相邻的上层和相邻的下一层之间发生的一个交互关系。服务可以按照不同的维度进行分类，比如可靠的不可靠好的，有连接的、没有连接的，以及有响应的和没有响应的那关于服务的分类，在初学这个阶段可以先不了解。随着这门课后续章节的学习，我们对服务的分类会有更直观的认知。

发言人   17:15

在这节课当中我们也介绍了协议数据单元、服务数据单元和协议控制信息这几个概念处于同一层的对等实体之间，以协议数据单元为单位去交换数据。协议数据单元由服务数据单元和协议控制信息组成。所谓的协议控制信息就是首部和尾部，而服务数据单元就是上一层的实体委托给下一层的实体传输的那部分数据。而某一层的协议数据单元又会作为下一层的服务数据单元。这几个概念比较容易混淆，建议大家看图复习反而会更清晰。

发言人   18:03

好，在这节课中我们还介绍了协议的三要素，分别是语法、语义和时序。语法描述了协议的数据与控制信息的格式，也就是描述了一个协议的SDU和PCI的格式。协议的第二个要素语义描述了使用协议的这些对等实体之间需要发出什么样的控制信息，以及需要完成什么样的动作，做出什么样的回应。简言之，语义描述了对等实体之间需要做什么。而最后一个要素时序，它需要描述这些对等实体做的这些动作触发条件是什么，以及先后顺序是什么，也就是需要描述清楚什么时候做。由于到目前为止，我们暂时还没有接触任何一个具体的协议，所以这几个概念现在听起来比较抽象是很正常的。通过这门课后续章节的学习，对这几个概念的理解会更加深刻。

发言人   19:12

好，在上一节课当中我们也介绍了计算机网络体系结构的概念。当我们要设计计算机网络体系结构的时候，我们需要精确的定义，层次的划分以及各层的功能，各层的协议等等。只需要给出精确的定义就可以，而不需要涉及到具体的实现。相当于我们设计一个体系结构，只是给计算机网络的软硬件开发者搭建了一个框架。开发者们只需要按照体系结构的这些定义去进行开发。那么你开发的软件和硬件就可以在我这个网络上去正常的运行。好，所以具体的实现这个问题是交给软硬件开发者的，体系结构的设计者只需要给出定义就可以。

发言人   20:04

在上节课中我们也简单介绍了三种常见的网络体系结构。分别是OSI参考模型、TCPIP模型以及我们教学使用的五层模型。OSI参考模型是法律标准，是由一个国际标准化组织提出来的。但是现在的互联网世界，网络的体系结构是TCPIP模型，也就是分为了四层。所以TCPIP模型它是事实上的标准。在下节课中我们会展开介绍OSI参考模型和TCPIP模型。而在这门课程的后续几个章节中，我们会更详细的去研究五层模型的各个层次。好的，以上就是这个视频的全部内容。