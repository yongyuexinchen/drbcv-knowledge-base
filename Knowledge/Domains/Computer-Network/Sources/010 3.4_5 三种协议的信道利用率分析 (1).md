# 010 3.4_5 三种协议的信道利用率分析 (1)

> Source: 010 3.4_5 三种协议的信道利用率分析 (1)_原文.docx

010 3.4_5 三种协议的信道利用率分析 (1)_原文

2026年07月19日 11:53

发言人   00:00

好的，在这个视频中我们要探讨三种协议的信道利用率。包括停止等待协议、后腿延伸协议和选择重传协议。后退N帧协议和选择重传协议的信道利用率分析思路是完全一致的。所以这两种协议我们会放在一起来探讨。另外在这个视频中我们也会补充几个重要的术语，这些术语可能在看书做题的时候会碰到。好，由于信道利用率这个部分是考研小题的命题重点，所以在这个视频中我们也会穿插一些例题来帮助大家了解这个部分的考察风格。

发言人   00:37

好，首先我们来看停止等待协议的信道利用率。我们知道在停止等待协议当中，发送方和接收方发送数据的过程是这样的。首先发送方要给接收方发送一个数据帧，接收方收到这个数据帧并且检查无误之后，会给发送方返回这个数据真的确认证对应的ACK。由于停止等待协议的发送窗口和接收窗口的大小都等于一，所以每一次只能发一个数据帧，返回一个确认帧，再发送下一个数据帧，再返回下一个确认帧，就是这样的一个过程。

发言人   01:18

好，为了方便探讨信道利用率，我们不妨假设这样的一些前提条件。现在假设发送方和接收方之间的这个信道数据传输速率是1KBPS数据帧的长度是4KB那显然发送一个数据帧就需要4秒钟的时间。一个确认帧的长度是一KB所以发送一个确认帧需要耗时一秒钟。好，另外我们再假设信道的单向传播时间等于7秒钟。那基于上面这些条件，发送方和接收方之间使用停止等待协议去传输这个数据帧的过程就是这样的。

发言人   01:59

在这个图当中，上面这条线表示的是发送方，下面这条线表示的是接收方，横轴表示时间T并且横向的一个小格子对应的是一秒钟的时间。好，那么来看一下，首先发送方给接收方发送第一个数据帧耗时总共需要4秒钟，所以对应横向的12344个小格子。由于信道的单向传播实验是7秒钟，所以这个数据帧的最后一个比特从被发出到被接收总共中间经历了7秒钟。好，那么收到这个数据帧之后，假设这个数据真没有检测出任何的差错，那么此时接收方就可以给发送方返回一个确认真ACK。

发言人   02:49

那刚才我们说了确认真的发送时延是一秒钟的时间。好，那么这个确认帧在信道上传播也需要7秒钟。因此从确认真的第一个比特被发出到第一个比特被发送方接收，总共中间经过了七个小格子。好，那么再经过一秒钟的时间，发送方就可以完整的收到这个ACK。真接下来从这个时间点开始，发送方就可以发送第二个数据帧。第二组数据帧和确认真的这个传输过程是完全一致的。所以理想情况下之后会一直重复前边的这个过程。

发言人   03:30

可以看到从发出一个数据帧到收到这个数据帧的ACK，整个过程至少需要花4秒，再加上2乘以7，再加上最后的一秒钟，这是总耗时。在这一整段时间内，发送信道上只有4秒钟是有数据通过的。所以发送信道的信道利用率就应该是4除以刚才的这个总时间就等于21%点。那这就是发送信道的利用率。

发言人   04:02

注意这个地方我们探讨的是发送信道的利用率。也就是说发送方和接收方之间的数据传输，我们把它看作是两条单向的信道。发送方给接收方发送数据的这条信道叫做发送信道。而接收方给发送方返回ack k的这条信道，我们把它视为接收信道。由于我们探讨的是发送信道的信道利用率，所以我们认为发送信道上只有4秒钟的时间是有数据通过的。传输ACK的这一秒钟是接收信道上有数据通过的时间。

发言人   04:42

好，理解清楚这一点，接下来我们对信道利用率的这个计算进行一个小总结。我们把一个数据帧的传输时延记作TD，把两倍的单向传播时延记作RTT，然后把一个确认帧的传输时间记作TA那么在停止等待协议当中，理想情况下信道利用率U就应该等于数据帧的传输实验除以数据帧的传输实验，加上两倍的单向传播实验，再加上这个确认帧的传输实验。结合下面这张图应该不难理解。

发言人   05:24

对了，这个地方为什么要强调是理想情况下呢？因为我们知道信道的传输有时候是不稳定的对吧？那所谓理想的情况就是说这个信道的传输不会丢帧，这个真的内部也不会出现比特差错。每一个数据帧和每一个确认帧总是能顺利的发送和接收。所以这个地方我们是基于这种理想情况来探讨信道利用率的。好，这是停止等待协议的信道利用率。

发言人   05:55

好，接下来我们探讨另一种情况，我们假设确认真非常短，那确认真非常短意味着确认真的这个传输时间也会非常短。我们可以把这个传输时延忽略不计。在这种条件下，我们可以把这个确认帧的传输化成这样的一条线。也就是说它是没有宽度的。我们把这个确认帧的传输看作是一瞬间就可以完成，因为它非常短。好，所以改变这样的一个条件之后，信道利用率就变成了这个样子。发送信道上有4秒钟的时间是有数据通过的那从第一个数据帧发出到下一个数据帧可以发出，总共需要经过4加上14这么长的时间。所以信道利用率就应该等于22.2%。

发言人   06:49

在这种情况下，刚才我们总结的这个信道利用率的计算公式同样适用。只不过这儿的TA我们把它设为零就可以了。好，接下来看一个真题，2018年的36题，假设主机甲采用停止等待协议向主机乙发送数据，数据的传输速率是3KBPS单向的传播时延，单向的传播时延是200毫秒，忽略确认帧的传输实验。这个是什么意思？就是刚才我们说的那个条件对吧？就是说确认真非常短，我们可以认为它的传输是在一瞬间就完成的。也就是说我们可以忽略确认帧的传输实验，但是我们不能忽略确认真的传播实验，对吧？这是两回事。

发言人   07:38

好，题目说当信道利用率等于40%的时候，数据帧的长度应该是多少？好，那么停止等待协议的这个信道利用率，我们刚才总结过，等于数据帧的传输时延加上数据帧的传输时延，加两倍的单向传播时延，再加确认帧的传输时延。在这个题目当中确认帧的传输时延等于0，那数据帧的传输时延应该是等于数据帧的长度除以这个信道的速率。我们最终要求的是数据帧的长度，对吧？那把题目给的条件带进去之后，我们应该算得这个信道利用率要等于40%，这也是题目的条件。好，接下来就可以解这个方程，最终可以算的数据帧的长度应该等于800比特。提醒大家需要注意，我们这儿带入的单向传播实验单位是毫秒，得把它换算成秒为单位才可以和前边这个BPS相匹配。

发言人   08:46

好，这是一个很典型的例题。考察停止等待协议的信道利用率。有必要提醒部分同学很多同学看到这样的一个公式，他会选择去背。其实很简单，信道利用率的分子表示的是一个数据帧的传输时间，对吧？而它的分母指的是从一个数据帧刚开始发出到下一个数据帧可以发出，中间总共经历的总时间。在发送信道上这一段总时间内只有其中的一小部分有数据通过。那有数据通过的这个时间的比例就是信道利用率。

发言人   09:31

所以我们要理解这个公式背后的逻辑，而不是去死记硬背。死记硬背的公式很难去应对考试当中变化的条件。好，这是要提醒大家注意的，要理解而不是死记硬背。

发言人   09:46

好，接下来看后退原装协议和选择重装协议的信道利用率。在理想情况下，这两种协议的信道利用率分析方法是完全一样的。这二者有一个共同点，就是发送窗口的大小要大于一。虽然这两种协议的接收窗口大小不一样，但是在理想情况下，我们认为发送窗口内的N个数据真可以被连续的发出，并且被接收方顺利的接收。因为不用考虑帧丢失、帧错误这些问题，所以在理想情况下，这两种协议的信道利用率其实都只受发送窗口的大小影响，和接收窗口的大小没有关系。

发言人   10:31

为了讨论方便，我们也假设一些前提。我们不妨假设发送窗口的大小是4。信道的传输速率，还有数据帧长度，确认帧长度以及信道的单向传播实验，和之前的那个假设都是一致的那在这种情况下，数据的传输会变成这个样子，可以看到一个数据帧的传输时间应该是等于4秒。由于发送窗口的大小是4，所以无论是采用后退N帧还是选择重传，反正最多可以连续的发出四个数据帧，对吧？那我们不妨把这些数据真编号为0123，每一个数据帧的传输时间都是4秒钟。由于我们讨论的是理想情况，所以这些数据帧接下来都可以顺利的被接收方所接收。好，第一个数据帧经过7秒钟的时间之后被这个接收方所接收，那接收方会立即返回第一个确认帧。这个确认帧的传输时间也是一秒钟，而传播时延同样是7秒。

发言人   11:45

对于数据的发送方而言，当它收到第一个数据帧的ACK之后，它的发送窗口就可以往前滑动一格，对吧？也就是说收到第一个确认帧之后，就可以紧接着发送下一个数据帧。同样的道理，收到第二个确认帧之后，又可以发送再往后一个的数据真，那后面的过程就是完全一致的。所以我们要分析信道利用率，我们只需要看这样的一个周期就可以了，也就是关注这个灰色的虚线框内，发送信道上有百分之多少的时间是有数据通过的。好，结合这个图示可以得到发送信道的利用率应该是等于4乘以4。也就是总计16秒的时间，发送信道上是有数据通过的这16秒传输了N个数据真好。那么从发出这一组数据帧到下一组数据帧，可以被发出，中间所经历的时长就应该是第一个数据帧的传输时延加上两倍的单向传播时延，再加上这个确认帧的传输实验，也就是这个式子的分母部分，计算结果是84%。

发言人   13:03

那我们不妨把这个式子进行整理。同样的用TD表示一个数据帧的传输实验，用RTT去表示两倍的单向传播实验，用TA去表示确认帧的传输实验。那这样的话信道利用率就应该等于N倍的。

发言人   13:21

TD除以TD加RTT再加TA同样的这个式子不用解，关键是理解它背后的逻辑。上面这个N乘以TD表示的是把发送窗口内的这N个数据帧连续的发出所需要的时间。而下面分母的这个部分表示的是从发出第一个数据真到接收到第一个确认帧中间所需要的总耗时。因为收到第一个确认帧之后，接下来就可以顺利的往后发送后一组数据帧对吧？之后的过程就是不断的在重复，所以分母我们取了这样的一个值。好的，那这是发送窗口等于四的情况。

发言人   14:08

接下来我们把条件改一下，让发送窗口的大小等于5，其他的条件不变。好，改变这样的条件之后，大家可以代进去算一下，发送窗口等于五算出来的信道利用率会大于一。但是我们知道信道利用率的定义是指一条信道上有百分之多少的时间是有数据通过的对吧？所以信道利用率这个指标它不可能大于一，因此生搬硬套这个公式就不太适用了。

发言人   14:40

好，来看一下发送窗口变大之后，具体发生了什么。发送窗口大小等于五意味着可以连续的发出五个数据帧，对吧？那每个数据帧的传输时间是4秒钟，也就是说这五个数据帧全部发出都需要经过20秒的时间。

发言人   15:00

回头看第一个数据帧，它的ACK在20秒之前就会被收到，对吧？也就是说从这个时间点开始，理论上发送窗口就可以往后滑动。但现实情况就是在这个时间点这儿，第五个数据帧还没有发完。所以第五个数据帧还需要再继续发一段时间。那只有第五个数据帧发完了之后，才可以接着发后一组的数据真那在这种情况下可以看到发送信道上总是有这个数据正在通过，所以发送信道的信道利用率等于一好。因此要提醒大家注意，这个信道利用率是不能超过一的。

发言人   15:43

在这个公式当中，如果分子的值大于分母的值，那最终我们应该取最大信道利用率等于一好，这是做题的时候需要注意的地方。接下来看一个例题，14年的36题。假设主机甲和乙之间使用后退N帧协议传输数据，发送窗口的大小是1000，数据帧的长度是一千字节，注意字节是大B信道的带宽是100兆BPS。主机以每收到一个数据帧，会立即用一个短针进行确认。

发言人   16:22

题目明确的告诉我们，这个ACK帧确认帧的传输时延是可以忽略不计的。如果甲乙之间的单向传播时间是50毫秒，那么主机甲可以达到的最大平均数据传输速率约等于多少？好，根据题目条件，一个数据帧的长度是1000大B而信道的带宽是100MBPS，所以传输一个数据帧所需要的时间就是0.08毫秒。注意大B要乘以8换算成小B那如果画图表示就应该是这个样子，一个数据帧它的传输时间应该是等于0.08毫秒。那由于发送窗口的大小是1000，所以就意味着主机甲最多可以连续的发送1000个数据帧。我们在这个图当中表示为0到999，发送这一千个数据帧的耗时就是80毫秒。也就是从第一个数据真发出一直到最后一个数据帧发送完毕，这个过程总共耗时80毫秒。

发言人   17:30

好，那么在理想情况下，主机以每收到一个数据帧，就会立刻用一个短帧进行ACK。所以第一个数据帧被接收之后立即返回一个ACK由于单向的传播时间是50毫秒，因此这个ACK针从发出到被接收中间应该是经历了50毫秒。同样的道理，当接收方收到第二个数据帧的时候，它又会立即返回第2个ACK对吧？那理想情况下，从这个时间点开始，发送方就可以连续的发送后面的1000个数据帧对吧？也就是再次重复前面这个周期所发生的事情。

发言人   18:16

好，所以我们只需要考虑这样的一个周期之内，它到底传输了多少数据？用这样的一个周期内实际传输的数据量除以这一整个周期的时长，就可以得到发送方的最大平均数据传输速率。好，那么发送的数据总量是1000个数据帧，每个数据帧的大小是一千大B刚才提到的这一整个周期时长应该是两倍的单向传播时延，再加上0.08毫秒，结合这个图示应该不难理解为什么要这么计算。好，那么最后的计算结果应该是80MBPS。所以这题目的答案选择C选项可以看到14年的这个题目，它并没有直接考什么信道利用率这个东西，对吧？也就是说如果我们只会生搬硬套公式，而没有理解这个公式背后的这个分析过程，那么这个题目肯定是做不出来的。所以最重要的还是要理解我们在背后分析的这种方法和过程。

发言人   19:21

好，对于这个题目来说，我们不妨用我们计算出来的这个实际的数据传输速率去除以这个信道的最大带宽80除以100。用这样的方式也可以算出信道利用率等于80%。所以信道利用率的计算方法有很多，我们可以用实际的数据传输速率除以最大的带宽，用这样的方式计算出信道利用率。当然也可以用我们刚才总结的U等于N倍的TD除以TD加RTT再加上TA，也可以用这样的方式计算，算出来的结果是一样的。这两种算法在背后表达的是同一个意思。

发言人   20:07

好，这是14年的36题。好，接下来再看一个题目。15年的35题说主机甲通过这么快的一条卫星链路，采用滑动窗口协议向主机乙发送数据，给了链路的单向传播时间，给了数据帧的长度，并且告诉我们不考虑确认帧的开销。为了确保这条链路，也就是这条信道的利用率不小于80%，问我们帧序号的比特数至少应该是多少？好，这个题目是有一定难度的。它不仅考察信道利用率，而且还结合帧序号的比特数来进行出题。

发言人   20:51

好，另外这个题目提到一个术语叫做滑动窗口协议。此处我们需要给大家补充，所谓的滑动窗口协议，它指的是后退N帧协议或者选择重传协议。我们学过停止等待、后退N帧和选择重传这样的三个协议。为了便于大家理解，所以在讲解这几种协议的原理的时候，我们把这三者视为都采用了滑动窗口机制，对吧？只不过这三种协议的发送窗口和接收窗口的大小有所区别而已。所以在原理上我们可以把这三种协议都视为采用了滑动窗口机制。

发言人   21:31

但是在考试的时候，如果遇到滑动窗口协议这个名词，那你要知道它指的是发送窗口大于一的这两种协议。也就是说停止等待协议不属于滑动窗口协议的范畴。它虽然也使用了滑动窗口机制这种原理。

发言人   21:52

好，所以这个术语是需要给大家补充的那顺道的我们再补充另一个术语，叫做ARQ协议和连续ARQ协议。所谓的ARQ指的是automatic repeat request，通常会把ARQ翻译为自动重传请求协议。自动重传这个特性，我们在这三种协议当中都有感受到，对吧？无论是哪种协议都有这样的一个规定。就是发送方给接收方发送一个数据帧之后，如果长时间没有收到ACK长时间没有收到ACK，那么发送方就会自动的去重传这个数据帧。好，所以这三种协议都有自动重传的特性。因此如果大家在看书或者做题的时候看到说两个节点之间采用ARQ协议。那么你要知道他在背后指代的有可能是停止等待，有可能是GBN，也有可能是选择重传。

发言人   22:50

好，与之相关联的还有一个术语叫做连续ARQ协议。为什么叫连续ARQ？因为后退N帧和选择重传协议它的发送窗口都大于一对吧？也就是说在这两种协议当中，如果连续的发出多个数据帧，但是这些数据真的ACK都没有被顺利的接收。那么这两种协议会连续的自动去重传多个数据帧。所以如果遇到连续ARQ协议这个术语，那你要知道它指代的有可能是后退N帧和选择重传停止等待协议，不属于连续ARQ的范畴。好，这是需要给大家补充的术语。

发言人   23:34

接下来我们回到刚才这个题目，这个题目说主机甲和主机乙之间采用滑动窗口协议。那现在你知道了它指的是主机甲和乙之间，可能使用的是GBN当然也有可能使用的是选择重传，对吧？那无论是GBN还是选择重传，在理想情况下，这两种协议的信道利用率计算的方法都是一样的。题目要求这个信道利用率不小于80%，所以这个式子最终我们希望它大于等于80%对吧？

发言人   24:07

好，接下来我们把这个式子里边的变量进行一个填充。首先这个大N它指的是发送窗口的大小，题目并没有给这个条件，但是我们知道发送窗口的大小它受到帧序号的比特数影响，对吧？所以只要我们确定了这个发送窗口的大小，当它等于多少的时候可以满足这个条件。这样的话我们就可以继续去推算帧序号到底应该占多少个比特。好，这个坑我们一会儿再来填，我们先把后面这几个变量给填上，这儿的TD指的是发送一个数据帧所需要的时间，数据帧的长度是1000个字节，一千大B除以这个链路的带宽128KBPS，计算结果是这样的。

发言人   24:58

好，接下来在分母这儿也用到了TD刚才已经算出来了，RTT指的是两倍的单向传播时延。那题目告诉我们单向传播时延是250毫秒，两倍的单向传播实验就应该是0.5秒。好，最后这个变量TA指的是确认帧的发送实验。那这个题目告诉我们不考虑确认帧的开销，对吧？也就是说确认真非常短，所以TA我们可以忽略不计，因认为它的发送时间等于0。好，现在在这个式子当中只有大N的值是未知的，其他几个变量的值都是已知的那解这个不等式得到的结论就是大N应该大于等于7.2。满足这个条件的时候，就可以确保这个链路的利用率不低于80%。

发言人   25:50

那大N表示的是发送窗口的大小，这个题目最终是让我们求帧序号的比特数至少应该为多少。在之前的学习中我们知道GBN协议的发送窗口大于一，接收窗口等于一，而选择重传的发送窗口和接收窗口都要大于一。并且无论是哪个协议，他们都必须满足这样的一个条件。当我们用N个比特给帧进行编号的时候，我们需要满足发送窗口和接收窗口的大小要小于等于2的N次方对吧？

发言人   26:25

好，接下来出现了一些区别，JBN协议的接收窗口大小等于一，而选择重传协议接收窗口是大于一的。所以显然如果N比特这个N确定的情况下，那么GBN协议的WT也就是发送窗口可以更大对吧？我们不妨把接收窗口等于一给带进去，就意味着发送窗口WT加上一应该小于等于2的N次方。那刚才我们已经计算出发送窗口的大小应该大于等于7.2。那发送窗口的大小不可能是一个小数，对吧？肯定是一个整数。所以这个式子意味着发送窗口的大小至少应该等于8。那我们不妨把八代进去，8加1小于等于二分之N这里的N也必须取整数，所以这个式子推出的结果就是N要大于等于4，因此帧序号的比特数至少应该等于4。

发言人   27:28

这个题目选择B选项。好，那么在这个题目当中我们要强调这个结论。当我们用N个比特给帧进行编号的时候，GBN协议的发送窗口肯定要比选择重传协议的发送窗口更大对吧？因为选择重传协议的WR要更大，那相应的在满足这个式子的条件下，选择重双协议的WT就会更小。

发言人   27:57

在这个题目当中，无论是GBN协议还是SR协议，反正当N等于4的时候，肯定都能满足题目要求的这些事情。但如果我们把条件稍微改造一下，我们就可以体会到刚才强调的这个结论它的作用。我们不妨把这个80%变小一点，我们把它变成75%好了，反正就是把这个要求变低一点。这样的话我们推出的这个大N它可能大于等于7就可以了。改成75%之后，是不是大于等于7，这个我不确定，反正就是我们假设改了条件之后，得到的结论是发送窗口要大于等于7。那基于这样的条件，如果采用GBN协议，那么GBN协议应该满足发送窗口加上接收窗口要小于等于2的N次方，对吧？要满足这个条件，那推出来的结果就是N大于等于三就可以。也就是说如果采用GBN协议，那么我们只需要用3比特给真进行编号就OK。

发言人   29:05

好，现在再来看SR协议，我们要确保发送窗口的大小至少要等于7，再加上接收窗口的大小要小于等于2的N次方。那我们知道选择重传协议，它的接收窗口肯定是要大于一的对吧？所以接收窗口的大小至少应该等于2。那7加2小于等于2的N次方，那这个式子推出来N应该要大于三才对。也就是说如果采用选择重传协议，并且要保证发送窗口的大小大于等于7的话，那么我们至少要用四个比特给帧进行编号。所以如果这个题目的条件稍微做一下更改，那么采用GBN协议和采用SR协议得到的这个结果结论就是完全不一样的。好，所以通过这个真题要理解这一点。

发言人   29:58

好的，在这个视频中我们探讨了三种协议在理想情况下信道利用率等于多少。其实信道利用率的计算本身并不复杂，但是从刚才那几个真题当中可以看到，这个部分的考察还是可以做的比较综合的。停止等待协议的信道利用率考察相对来说会简单一些。后退N帧和选择重创协议就很喜欢结合用N比特给帧编号这个知识点进行考察。当题目对信道利用率做出限制的时候，本质上就是对发送窗口做出了一定的限制。那么发送窗口的大小又受限于N比特，就是给帧编号的这个比特数。同时当我们采用JBN协议或者选择重创协议的时候，因为二者对于这个接收窗口的大小要求不一致。

发言人   30:54

所以采用这两种协议的时候，我们去判断这个比特数也需要做出一定的调整。同样用N个比特给帧编号的时候，JBN协议的发送窗口会更大，相应的GBN协议的信道利用率也会更高。好，另外在这个视频中我们也补充了几个，大家看书做题的时候可能会遇到的术语。分别是滑动窗口协议，它指的是GBN协议或者SR协议。ARQ协议包含了停止等待、后退N帧和选择重传这三个协议，而连续ARQ指的是后退N帧和选择重装协议。好的，大家可以再结合这个思维导图来回顾一下，以上就是这视频的全部内容。