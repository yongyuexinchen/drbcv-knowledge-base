# 008 3.4_3 后退N帧协议（GBN） (1)

> Source: 008 3.4_3 后退N帧协议（GBN） (1)_原文.docx

008 3.4_3 后退N帧协议（GBN） (1)_原文

2026年07月19日 11:52

发言人   00:00

好的，接下来看第二个基于滑动窗口机制的协议，叫做后退N帧协议，英文缩写叫GBN。GBN分别取了go back n的首字母。那么相比于停止等待协议，后退N帧协议有这样的一些不同。首先在滑动窗口方面，后退N帧协议的发送窗口大小是大于一的，接收窗口的大小和停止等待协议一样都是等于一好。另外还有一个比较特殊的规则，就是关于确认真。在停止等待协议当中，每一个数据帧都需要一次确认。但是在后退N帧协议当中，接收方可以累积确认，也就是连续收到多个数据帧的时候，只返回最后一个真的ACK。一会儿我们会用一个例子带大家感受这个特点。

发言人   00:57

好，另一方面关于超时重传的规则也有一些不一样。如果说发送方超时没有收到I号真的ACK，那么在后退N帧协议当中会要求重新传输I昊祯以及I号真之后的所有真。同样的一会儿会用例子带大家感受，我们现在先建立起一个框架。好，接下来我们先探讨滑动窗口和帧编号这两个问题。后退N帧协议规定发送窗口的大小要大于一。比如说我们可以取发送窗口的大小等于3，而接收窗口的大小等于一。那么如果我们要用N个比特给各个帧进行编号的话，我们必须满足发送窗口和接收窗口的大小小于等于2的N次方这个条件。对于这个例子来说就是3加1要小于等于2的N次方，所以可以推出N要大于等于2，也就是说我们至少要用两个比特给帧编号，那两个比特可以表示的编号就是0123这几个数。

发言人   02:05

我们会用这样的方式给各个帧进行编号。第一个真是零号帧，接下来一二三好，再往后一个真又回到0号123，接下来又回到零号0123。好。那么对于接收方来说，它的这些帧序号也是同样的规则。

发言人   02:25

好，接下来基于这样的条件，我们来看后退N帧协议是如何工作的。现在发送窗口框住了012这几个真，所以发送方可以在信道上连续的传输这样的三个真好，接收方首先会收到零号帧。如果按照停止等待协议的规则，每当收到一个帧，接收方就需要返回这个帧的ACK对吧？但是对于后退N帧协议来说，我们不需要给每一个帧都返回一个ACK。

发言人   02:59

好，接下来可以这么做。收到零号帧之后，接收方的滑动窗口向右滑动，并且准备好接收一号真。现在一号臻也已经正确的接收，那滑动窗口继续往右移动，现在收到了二号真。那么在二号帧之后，信道上已经没有连续传输的数据了。此时接收方可以给发送方返回，最后收到的这个帧所对应的确认真，也就是ACK2。这就是所谓的累计确认机制。

发言人   03:34

当接收方给发送方返回ACKI的时候，表示它已经正确的接收到爱好真以及它之前的所有其他帧了。那对于刚才这个例子来说，返回ACK2意味着它已经收到二号贞以及二号真之前的所有帧了。好，那么发送方收到这个确认帧之后，就可以确认零号、一号、二号已被正确地接收。接下来发送窗口就可以向后移动。

发言人   04:06

刚才我们举的这个例子是一种没有发生异常的情况。发送方连续发送了三个帧，并且三个帧都被顺利地接收。接下来我们看一种异常的情况，假设其中某一个数据帧丢失，我们应该如何处理好？

发言人   04:23

现在发送方的窗口框住了301这几个真，首先发送三号真，紧接着零号，最后是一号，这些帧的数据依次被发到新道上，那么第一个到达的帧肯定是三号真。我们假设三号真顺利到达，并且数据没有错误，可以被正确的接收。那此时接收方的滑动窗口会向右移动。假设信道发生了一些故障，导致这个零号针在中途丢失。或者我们也可以假设我们可以顺利的收到零号帧，但是在零号帧当中检测出差错，因此零号帧被接收方主动丢弃。反正不管是因为线路的原因导致这个真丢失，还是说接收方主动的丢弃了这个真，接下来的处理都是类似的。

发言人   05:15

好，后面的故事是这样的，零号帧丢失之后，一号臻还可以顺利的传到接收方这边。但是接收方检查这个帧的序号并没有落在接收窗口之内。我现在期待接收到的是零号帧，但现在我收到的是一号真。那这种情况下我们肯定得拒绝接收一号臻，直接把它丢弃。与此同时接收方会给发送方返回最后收到的这个正确真的ACK那目前收到的最后一个正确真是三号真，所以应该给发送方返回ack 3让发送方知道这个三号真之前的所有帧我都已经顺利的结束了，对吧？

发言人   06:03

好，现在发送方收到ACK3之后，它就可以让发送窗口往右移动一格。那与此同时，由于迟迟没有收到零号帧一号真的这个确认信息，所以零号真的计时器它会超时。而零号帧超时之后，发送方会把零号帧及其后面的所有帧都重新传一遍。虽然一号臻此时暂时还没有超时，但是既然接收方没有给我返回AK0，那么就意味着林浩真有可能没有被正确的接收，对吧？那在后退N帧协议当中，接收方只能顺序的接收各个真好。所以对于发送方而言，如果说零号帧超时了，那么就需要把零号帧及其后面的所有帧都统一的做重传的处理。零号帧和一号臻需要重传，而二号真是第一次进行传输。好，讲到这儿我们就能理解为什么后退N帧协议要叫后退N帧了。

发言人   07:13

之前发送方的这个发送窗口框住的是301这几个真，对吧？301这几个真原本都已经发给接收方了，但是现在由于零号帧超时，所以发送方不得不退回到林皓祯，从林皓祯再重新进行发送。因此由于需要退回到某一个帧重新进行发送，因此这个协议的名字叫做后退N帧协议。注意，这个后退并不是指滑动窗口往后退，而是原本已经发送出去的这些真需要退回来重新发一次。这样的特性也实现了流量控制。

发言人   07:56

我们可以想象一下，如果发送方给接收方发送真的数据太快，而导致接收方来不及接收，所有的帧已经发出。但是来不及接收的那些真肯定迟早会超时，对吧？那超时之后，发送方又会把超时的帧以及后面的所有帧再重新发一遍。也就是说发送方给接收方发送数据的速率是被接收方所控制的那这就是后退N帧协议实现流量控制的一个原理。

发言人   08:29

好，接下来我们让故事继续。发送方重传了零号帧和一号真，那么被重传的这些真需要重置计时器，同时新发送的这个二号真也需要给它设置一个计时器。首先到达的是零号数据帧，收到这个帧之后，接收窗口可以向右滑动。与此同时一号臻二号真也顺利的到达，并且顺利的被接收，那接收窗口就可以到达三这个位置。在连续收到几个帧之后，接收方会把最后这个真的ACK返回给发送方，最后这个真的真号是2，所以返回的就是ACK2。那么对于发送方而言，他说到ACK2就知道二号贞以及它前面的所有帧都已经被顺利的接收，因此发送方的窗口也可以继续往右滑动。好，刚才这个例子当中我们看到了在GBN协议当中，如果数据帧丢失，发送方和接收方会如何处理。

发言人   09:34

接下来我们看另一种异常的情况，我们看确认帧丢失的情况，我们把传输的过程退回到这一步，假设现在发送方已经给接收方发了两个数据帧，并且这两个真已经被顺利的接收。紧接着发送方又发送了二号数据帧，同时这个真也被顺利的接收。好，那么连续收到3个帧之后，接收方的窗口会向右滑动，与此同时会给发送方返回最后收到的这个正确真它的确认信息，也就是ACK2。

发言人   10:09

接下来出现了一点小状况，假设这个确认帧在传输的过程中丢失了，那这样的话发送方就一直收不到ACK2，对吧？不过好在发送方在每一个帧被发出的时候都会设置计时器，那零号真是最先被发出的，所以它的计时器会第一个超时。那么零号帧超时之后，零号帧及其之后的所有帧都需要重新发送。所以接下来发送方会把零号帧一号臻二号贞全部进行重传。

发言人   10:49

好，现在问题来到了接收方这边，接下来它会陆续的收到零号、一号、二号这几个真，但是这几个真落在了接收窗口之外。好，那么当接收方收到这种非法的真的时候，会给发送方返回目前已经收到的最后一个正确真的ACK那现在已经收到的最后一个正确真应该是二号真对吧？所以接收方会给发送方返回ACK2，那接收方会把这些重复收到的帧直接丢弃，之前已经收到过了对吧？不用重新再接收。好，那么这一次返回的确认真，ACK2中途没有丢失，被发送方顺利的收到。那发送方就可以知道在二号贞及其之前的所有这些帧已经被正确的接收，因此它可以把滑动窗口滑到三开始的这个位置。

发言人   11:46

好，刚才这个例子中我们探讨了确认帧丢失的时候，发送方和接收方会怎么处理。好，接下来我们探讨下一个关键的问题。我们强调过无论是停止等待协议，还是后退N帧协议，还是之后将要学习的这个选择重传协议，这三种协议都必须满足发送窗口和接收窗口小于等于2N这个条件。其中N指的是给甄进行编号的比特数。

发言人   12:18

好，现在我们探讨一下后退N帧协议，如果不满足这个条件。那么会有什么问题？我们假设使用两个比特给这些帧进行编号，但是发送窗口的大小我们调到4，那么发送窗口和接收窗口的大小已经大于2的N次方了，对吧？也就是不满足这个条件。

发言人   12:41

好在这种情况下我们看会有什么问题。现在假设发送方给接收方连续发送了零号、一号、二号、三号，真因为这几个真都落在了发送窗口之内，对吧？所以按照滑动窗口机制的规则，这几个真都是可以发给接收方的。好，现在假设接收方这边也没有出现任何的问题，它顺利的接收了0123这几个真。同时接收方的这个接收窗口也滑动到了后一个帧，也就是零号这个位置。好，接下来问题发生了，接收方最后接收到的真是三号，对吧？因此按照后退N帧协议的规则，接收方会给发送方返回最后这个真的ACK。但是这个ACK3在传输的过程当中丢失了，那么对于发送方而言，它就永远收不到这个ACK3。发送方给每个帧都设置了计时器，由于零号真是最先被发出的，所以零号真的计时器会首先超时。零号帧超时之后，发送方会把0123这些真全部再重新传输给接收方。

发言人   13:57

首先进行重传的是零号帧。现在问题发生了，接收方的接收窗口它落在了零号这个位置。与此同时发送方给过来的这个真它的编号也等于0，刚好可以落在接收窗口之内，对吧？所以接收方会把这个真的数据完整的接收下来。但是通过上帝视角我们可以看到，发送方这次传过来的零号帧，并不是他真正想接收的这个零号真，而是对前面这个零号真的重传好。所以如果发送窗口和接收窗口的大小小于等于2N这个条件不满足的话，在传输的过程中这个帧序号可能会错乱，导致发送方接收方无法判别每一个帧序号真正对应的到底是哪一个。真好，所以必须满足这样的条件，这个条件也十分重要。在历年真题当中很多题目就是以这个条件作为解题的切入点。

发言人   15:04

好的，那么这个小节当中我们探讨了后退N帧协议，英文缩写叫GBN。后退N帧协议的发送窗口大于一，接收窗口等于一，那发送窗口和接收窗口的大小相加要小于等于2N这里的N指的是给帧进行编号的比特数。在后退N帧协议当中，确认机制有一些特殊的地方。我们不需要给每个帧都返回一个ACK，我们可以采用累计确认的这种方式。当接收方收到连续的多个帧的时候，可以只返回最后一个真的ACK。所以对于发送方而言，当他收到ACKI的时候，表示爱好真和爱好之前的所有帧都已经被顺利的接收了。

发言人   15:57

好，那么除了确认机制之外，重传机制，后退N帧协议也会有一些特殊的地方。如果I号真超时，那么发送方除了重传I昊祯之外，还需要重传I号帧之后的所有真。这是后退N帧协议比较特别的两个地方。

发言人   16:19

好，最后我们再总结一些后退N帧协议的要点。首先当接收方收到一个非法针的时候，接收方会把这个针给丢弃。那非法真是我的说法官方并没有这样的术语。所谓的非法针指的就是落在了接收窗口之外的真，或者落在窗口之内但是被检测出差错的，这些真反正就是有问题的帧，我们把它称为非法真。接收方会丢弃这种非法帧，并且给发送方返回目前已经接收的最后一个正确真的ACK。那么对于发送方而言，他收到ACKI之后就意味着爱好真以及爱好之前的所有帧已经被正确的接收。

发言人   17:10

如果爱好真的后一个真出现超时的情况，那么数据的发送方就需要后退回I加一号真，把这个帧以及后面的所有帧都重新发送。那严格来说我们不应该把它称为I加一号真更准确的说法应该是I加一再对2的N次方取一。因为我们知道用N比特给帧编号，那么帧的编号就会呈现出0123，一直到2的N次方减1，然后再回到0123，这是真编号的一个规律对吧？所以I号真的后面一个真，它的编号应该是I加1，然后对二的N次方取余。当然这个地方大家能够理解，I加一号臻指的就是I昊祯后面一个帧就可以了。

发言人   18:08

好，这样的特性也使得后退N帧协议有一个明显的缺点。如果接收方接收帧的速度很慢，也就是说发送方给你连续发送了多个真，但是接收方可能只来得及接收第一个帧，后面这些帧都来不及接收。那么对于发送方而言，后面这些帧它迟早会超时，对吧？而只要其中一个帧超时，那发送方就需要把这个真及其后面的所有帧都重新发一遍。那当接收方收帧的速度很慢的时候，发送方的这个发送进度是不是就很有可能需要频繁的往后退？我需要重复的传送很多真，那这样就会导致传输效率低下。

发言人   18:54

另一方面，如果说信道的误码率很高的话，也会导致传输效率低下。同样的道理，发送方可以给接收方连续的发送多个帧，但是由于信道误码率很高，所以接收方收到的第一个真很有可能就会有差错，对吧？而只要第一个真有差错，后面这些帧都不可能被顺利的接收。因此发送方又需要退回到第一个真，逐一的再重新发一遍。所以在信道误码率很高的情况下，也会导致后退N帧协议的这种传输效率非常低。好，这是后退N帧协议明显的一个缺点。在之后我们还会学习选择重传协议，在选择重传协议中我们会尝试着解决这种需要频繁后退的问题。好的，大家可以再结合这一页的总结，再回顾一下后退验证协议。以上就是这视频的全部内容。