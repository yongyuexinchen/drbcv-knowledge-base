# 006 5.3.3_1 TCP连接管理（建立连接）

> Source: 006 5.3.3_1 TCP连接管理（建立连接）_原文.docx

006 5.3.3_1 TCP连接管理（建立连接）_原文

2026年07月19日 12:20

发言人   00:00

各位同学大家好，在这节课我们要探讨TCP连接管理这个考点在大纲当中处于这个位置。我们知道TCP它是提供面向连接的服务，所以在上节课当中我们介绍过TCP协议它会经历三大阶段。第一大阶段就是需要先通过三次握手来建立连接。这三次握手分别对应3个TCP报文段。主动发起第一次握手请求的这一方，我们把它称为客户端，而被动响应连接请求的这一方，我们把它称为服务器。

发言人   00:38

当连接建立完毕之后，双方就可以进行双向的数据传输。如果其中一方已经传输完所有的数据，那么他就可以向对方发出挥手。经过两次挥手，可以关闭一个单向的数据传输通道。而另一个方向的这个数据传输通道还会继续保持，直到另一个进程他也发出挥手。总共经过2加2次挥手之后，双方的通信就彻底结束，也就是连接也彻底的断开被释放。好，这是TCP协议工作的三大阶段。

发言人   01:18

另外在上节课我们也详细学习了一个TCP报文段的构成，分为TCP首部和携带的这个数据部分。其中在TCP的首部有ACKSYN和FIN这样的三个标志位。我们需要特别注意，在这节课当中，这三个标志位也是我们探讨的核心。除了这三个标志位之外，在这节课当中我们还会频繁的涉及到序号和确认号这两个字段。所以如果不熟悉这三个标志位以及这两个字段的同学，可以先暂停来看一下这页PPT。在这页PPT当中我已经把最重要的东西都整理进来了。

发言人   02:01

好，接下来我们正式进入TCP连接管理这个考点的学习。这个考点本质上就是要考察三次握手和四次挥手它背后的一些细节。通常来说有这样的三种命题角度。第一个命题角度，题目很喜欢考察在这三次握手和四次握手当中，这三个标志位和sequence ACK sequence这两个字段，它们的值分别是多少。我们要能够根据题目给的初始条件去推算这些字段的值。好，这是最常考的一种命题角度，在大题小题当中都有可能考察。

发言人   02:39

好，接下来第二个命题角度，通过接下来的学习，我们会认识到在三次握手和4次挥手期间，进程的这个TCP状态是会发生转换的。每发出一个握手或者挥手报文段，或者每收到一个握手挥手报文段，都会影响进程的PCP状态。那么它的状态如何受到这些报文段的影响，这个有可能在选择题或者大题当中作为一个小问来进行考察。

发言人   03:11

第三个命题角度，题目有可能让我们去分析在建立连接和断开连接，也就释放连接的那个过程当中，最短耗时等于多少。这节课的讲解也会以这三种考法作为一个核心的主线。掌握这三种考法的同时，你也就理解了TCP连接管理背后所做的一些事情。

发言人   03:34

好，首先我们看建立连接这个阶段要经过三次握手。我们来看这三次握手当中SYNACKFIN这三个标志位以及sequence序号，还有ACK sequence。就是确认好这两个字段他们分别是怎么变化的。我给大家设计了一个详细的例子。由于每页课件想让大家体会到的重点不一样，所以我会在一张图的右侧这个地方去提醒大家，你在这张图当中应该重点关注的是什么东西。比如这张图体现的是三次握手，在这张图当中想让大家重点关注的是SYNACK sequence和ACK sequence这几个字段。我也会在这个图示当中用一些高亮颜色来吸引大家的注意力。

发言人   04:28

好，再换一张图，在这张图当中我想让大家重点体会到的是TCP的状态是如何变化的。所以我把中间的这些信息全部变成了黑白的字体。好，所以大家在听课还要回顾课件的时候，一定要注意我这儿给大家写的这个文字，提示每一个例子聚焦于一个具体的小问题。

发言人   04:50

好，首先看这张图，在这张图当中我们要重点关注SYN和ACK这两个标志位。首先通过之前的学习，我们知道ACK这个标志位只有握手一是等于零的，其他所有的TCP报文段ACK都等于1。而SYN这个标志位只有握手一和握手二是等于一，其他的报文段都等于0。最后FIN finish这个标志位只有挥手一和挥手3，FIN等于1，其他的所有报文段FIN都等于0。

发言人   05:27

我们在这个图当中也体现了这三个标志位的细节。首先SYN这个标志位表示的是当前的这个TCP报文段，它是不是在请求连接，或者是不是在同意请求连接，那只有握手，一是在请求连接，只有握手二是在同意请求连接。因此只有这2个TCP段它的SYN等于1，那再往后所有的TCP段SYN就都等于0。

发言人   06:00

好，接下来ACK这个标志位，它表示的是ACK sequence，也就是确认号这个字段是否有效。确认号这个字段是希望向对方反馈当前已接收的数据有多少。在刚开始建立连接的时候，由于通信双方还没有任何的数据传输的动作，对吧？所以握手一的这个确认号肯定是无效的。客户进程暂时还没有接收服务器进程发来的任何东西，对吧？所以他给服务器进程发的这个确认号肯定就要设置为无效，那对应的ACK这个标志位就得设为零，这两个是配套的东西，对吧？

发言人   06:42

好，接下来握手一当中还有一个字段叫做sequence。这个字段的值是客户端进程他自己决定的。握手一当中的这个sequence表示的是起始序号。那我们不妨假设客户进程，它规定接下来自己发送的这些数据是从666开始编号的。好，这是握手一。那接下来服务器进程收到握手一之后，就立即回复一个握手2，其中也包含了sequence和ACK。我们先来看确认号这个字段，可以看到服务器给客户返回的这个确认号，刚好是客户发过来的这个序号再加一。

发言人   07:28

有这样一点需要特别强调，握手一和握手二这2个TCP报文段都是不能携带数据的，但是仍然要消耗一个序号，什么意思呢？上节课我们说一个TCP段由首部和数据部分组成。握手一和握手二这2个TCP报文段，它们的数据部分都为空，也就是只有TCP首部。正常来说首部当中的序号这个字段它表示的是在这个TCP报文段当中的第一个字节，它的序号等于多少对吧？那相应的数据的接收方返回的这个确认号，它反馈的是我希望接收到的下一个字节，它的序号等于多少，对不对？照理说握手一当中既然没有携带任何数据，那么服务器进程返回的这个ACK也应该等于666才对，是不是？但是TCP协议特别规定，握手一一定要消耗一个序号，所以这个地方服务器返回的确认号在握手一的这个起始序号的基础上加了一好。

发言人   08:48

再来看握手二当中的序号这个字段。握手二当中的序号是服务器进程向客户端进程说，接下来我给你发的这些数据的起始序号从五十开始，这个值是由服务器进程它自己设定的，注意体会别和握手一的序号搞混了。握手一这个序号是客户进程给自己的数据确定的一个起始序号，而握手二当中的这个序号是服务器进程给自己的数据确定的一个起始序号。

发言人   09:29

好，那么继续往后，当客户进程收到握手二之后，会立即回复一个握手3。首先观察握手三当中的这个确认号，是在握手二的序号基础上加一对吧？那刚才我们说过握手2当中不能携带任何数据，也就是只包含一个手部。但是握手二仍然要消耗一个序号，所以客户进程收到握手二之后，他返回的这个确认号应该是在上面这个起始序号的基础上加1，它要固定的消耗一个序号。

发言人   10:10

好，接下来看握手三的序号字段，677就是客户端进程数据部分的真正的起始序号。梳理一下客户进程，在握手一当中选定了自己的起始序号等于666。虽然握手一不携带任何数据，但是它依然会消耗一个序号。接下来客户端进程真正开始发送的这些数据，它的序号就是从667开始的，也就是对应这个值。好，所以一定要注意这一点。握手一握手二虽然不携带数据，但是他们仍然要消耗一个序号。好，接下来在这一页课件当中我们会看到握手三它可以不携带数据，也可以选择携带数据。和握手一、握手二不一样的是，如果握手三不携带数据就不用消耗序号。

发言人   11:11

来看左边这张图，我们假设这个握手三它也只有手部没有携带任何数据。那么当服务器进程收到这个TCP报文段的时候，他给客户端返回的确认号就是667。你看这个握手三没有消耗序号，对吧？它和前面的握手一不一样，握手一同样没有携带数据，但是返回的这个握手二当中，确认号就变成了667，要消耗一个序号。

发言人   11:45

好，再看右边这个例子，在右边这个例子当中，我们假设握手三它携带的数据并且是100个字节。那你会看到此时握手三具体消耗了多少个序号，跟它的这个携带的数据量是完全相关的。携带100个字节，那么它就需要消耗100个序号。

发言人   12:08

我们可以结合之前这个视角来解释。在握手一这客户端进程规定自己的起始序号是666。虽然握手一没有携带任何数据，但是它也需要消耗一个序号。因此客户端进程接下来真正要发送的这些数据应该是从667才开始编号的那在握手三当中携带了100个字节的数据。那当服务器进程收到这100个字节之后，他给客户端反馈的确认号应该表达的是什么？应该表达的是接下来我期待收到的那个数据，它的序号是从多少开始的对吧？那显然再往后一个字节的这个数据，它的编号就应该是从767开始的这就意味着序号在767之前的所有数据已经被我服务器进程给顺利的结束了。

发言人   13:08

好，所以通过这张图左右两个例子，我们要重点强调的是握手三它可以不携带数据，也可以携带数据。如果它不携带数据，那么它就不会消耗序号，这一点和握手一二是有区别的。好，接下来我们看一个例题，2011年的真题。39题说主机甲向主机乙发送了这样的一个TCP报文段，期望与主机乙建立TCP连接。问我们如果主机已接受连接请求，那么主机已向主机甲发送的这个正确TCP段可能是什么？好，大家先暂停做一下，十秒钟之后公布答案。321这个题目选择C选项。

发言人   13:58

好，来看一下为什么说主机甲向主机乙发送这样的一个TCP报文段。那么由于SYN这个标志位等于一，我们可以推断出这个TCP报文段要么是握手一，要么是握手2，对不对？在TCP协议的全过程当中，只有这两个报文段的SYN是等于一的。

发言人   14:21

好，再往后看说主机甲期望与主机乙建立TCP连接。这句话的背后隐含的信息是什么意思？就是说主机甲是主动发起连接请求的那一方，对不对？也就意味着主机甲肯定是客户进程。那既然他是客户进程，他给主机乙发送的肯定就只会是握手一对不对？那么服务器也就是主机乙这边，它收到握手一之后，如果它接受该连接请求的话，他应该给主机甲返回的TCP段就肯定是握手2。那既然是握手2，SYN就肯定等于一了。所以AD2个选项首先排除它们的SYN都等于0。

发言人   15:07

好，接下来比较有迷惑性的是B和C两个选项。我们说握手二是服务器给客户主机返回的TCP报文段。在握手二当中，sequence序号这个字段表示的是服务器进程的起始序号，对吧？它表达的是这样的一个含义。那服务器进程的起始序号到底等于多少，这个是由服务器进程去决定的，对不对？所以这个值到底等于多少其实无所谓，具体就看这个服务器进程，它如何去设置自己的起始序号。

发言人   15:42

那有所谓的是什么呢？就是这个确认号在握手2当中，服务器给客户端返回的这个确认号应该是等于握手一的这个起始序号再加一。因为之前我们强调过，虽然握手一他不携带数据，但是它固定的会消耗一个序号。所以握手二返回的这个确认号就应该是11220，再加一对不对，要固定的消耗一个序号。好，那这样的话B选项是不是就排除了，剩下的就是C选项，这个确认号的值是正确的。好，这是11年的39题，这个题目当中服务器给客户端返回的这个序号字段，它其实是一个干扰信息。如果你没有理解序号确认号背后的那些逻辑的话，那么这种干扰选项很有可能就把你带到坑里去。好，所以这个部分考的还是很细的，一定要能够推算出握手一二三当中的这几个字段的信息分别等于多少。

发言人   16:51

好，现在我们已经搞定了第一种考法，接下来我们来关注第二种考法，有可能会考察进程的TCP状态转换。直接给个图。在建立连接的过程当中，客户进程刚开始是连接关闭的状态。在真题当中通常用英文表示就是close状态。当客户进程发出握手一之后，它的状态就从close变成了pyro ize sent，就是同步已发送。而当客户进程收到握手二之后，它就会进入established以建立连接这样的一个状态。好，这是客户进程在建立连接的阶段会经历的三种状态。

发言人   17:34

再来看服务器进程，刚开始服务器进程也是close这个状态。接下来如果服务器进程允许其他的客户进程建立连接的话，那么它就会监听自己的端口。于是服务器进程进入了listen监听的状态。好，当服务器进程收到握手一之后，它又会从监听状态转换到同步已接收的状态。SYS对应的是psychotic zia同步，然后RCVD对应的是received。好，这是同步已接收的状态。那么再往后当服务器进程收到握手三的时候，它就进入了established已建立连接的状态。

发言人   18:18

好状态转换的这种题目一般来说会怎么考呢？来看21年的38题，这个题目它考的是释放连接阶段的状态转换。但是从这个题目当中可以看到，建立连接的这个过程也有可能出现类似的考题，对吧？你看这个题目探讨的就是在四次挥手的那个阶段。如果客户端服务器巴拉巴拉发了一堆挥手信息之后，客户的这个TCP状态会转换成ABCD会转换成哪种状态？它的这些选项就是用英文来表示的那类似的考法也很有可能在建立连接这个阶段来进行考察。

发言人   19:00

说我可以这么考，你当服务器进程收到握手一之后，它会转换为什么状态？那答案就是进入同步已接收状态。好，那么这种考点它就属于记忆型的考点，建议大家考前几天回顾一下，利用短时记忆去做题就可以了。说实话这个状态转换的过程，如果你下周问我，我很有可能也忘了。所以这种考点它本身逻辑不是特别复杂，关键在于你记不记得住这种东西，给自己写一个备忘录，考前几天回顾一下就可以了。

发言人   19:33

好，这是建立连接阶段这个TCP状态的转换。接下来看建立连接这个阶段的最短耗时分析。这种考法可能会这么考察。比如说题目可能会问你从客户端发出握手一到客户端进程可以开始发送数据至少需要多久？那答案就是一倍的RTT。

发言人   19:56

RTT是往返时延对吧？对于客户端进程来说，当他收到握手二的那一刻开始，他就可以发送数据了。如果用进程的TCP状态去解释的话，就是一旦这个进程它的TCP连接进入到了establish的这个状态，那么他就可以开始给对方发送数据了。所以从发出握手一直到他可以正式的发送数据，整个过程最短耗时就是一个RTT对吧？RTT对应的就是这个TCP报文段的往返时延。

发言人   20:35

好，我们换一个进程。对于服务器进程来说，从客户端发出握手一开始，一直到服务器进程可以开始发送数据至少需要过多久。那显然就是1.5倍的RTT从发出握手一开始，握手一到达服务器，握手2到达客户端，这儿总共花费了一倍的RTT对吧？接下来握手三还需要经过单向的传播时延到达服务器B所以握手三的传输过程又需要消耗0.5倍的RTT，那加起来不就是1.5倍的RTT吗？如果考这种考题的话，题目肯定会给你RTT的值等于多少，把它带进去算就可以了。那我是不是还可以这么问，从服务器进程发出握手二开始，直到服务器进程可以开始发送数据，整个过程耗时是多少？那么显然就是一倍的RTT对吧？

发言人   21:32

好，所以其实这个耗时分析不要去记忆，关键是理解它背后的逻辑。最核心的点就是客户端进程是从收到握手二才可以开始发送数据的，而服务器进程是从收到握手三这个时间点才可以开始发送数据的。只要理解了这点，那么管它题目怎么去变，你肯定都能自己分析好，所以不要记忆，要理解。

发言人   22:01

好的。那么这节课当中我们主要探讨的是在连接建立的阶段，也就是三次握手的阶段，这三类题型我们需要注意什么？由于内容比较多，释放连接也就是四次挥手的阶段。我们留在下个视频再来探讨。好的，以上就是这个视频的全部内容。