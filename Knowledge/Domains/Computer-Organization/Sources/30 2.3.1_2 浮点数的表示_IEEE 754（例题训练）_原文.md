30 2.3.1_2 浮点数的表示_IEEE 754（例题训练）_原文

2026年08月03日 23:46

好，继续来看754浮点数的表示。这个视频中我们会引入几个重要的例题。看第一个例题，假如你是李华，是一个计算机高手，你的加拿大笔友Thomas正在参加408考试，他想请教你，要将十进制数负的8.25转换成一个i triple e754单精度浮点数，请你告诉他如何处理。好，大家可以暂停动手写一下，十秒钟之后公布答案。321。最后得到的二进制串是这个样子。如果用16进制的记法来表示就是这个样子。

首先第一步，根据题意，由于是单精度浮点型float型，所以我们首先要确定它的这个结构？是一加8加23分别对应符号阶码和尾数。第二步我们要把十进制真值转换成二进制的普通技法。负的8.25用二进制表示的话就应该是负的1000.01。第三步我们把二进制的普通记法转换成所谓的二进制科学计数法。需要注意的是，我们要把尾数这个部分给规格化，也就是小数点的前面必须有且仅有一个一，因此这个二进制数就可以表示为负的1.00001乘以2的3次方阶码等于3，意味着我们需要在这个小数点的实际位置应该往后移三位。

好，写出了这种二进制科学计数法之后，其实符号位应该填多少，以及尾数部分应该填多少就已经可以确定下来了对吧？那剩下的就是阶码这个部分它应该是什么样子。由于阶码是用移码的方式去存储的，因此我们需要把这个阶码的增值3按照乙码的规则把它转换成巴比特。

转换的方法很简单，用阶码的真值加上乙码的偏置值。对于float型变量来说，一码的偏置值是多少？应该是二的N减1次方再减一对吧？这儿的N也就是阶码的位数应该是8，所以就应该等于2的7次方减1，也就是127。好，所以真值加上偏置值也就是3加上127，那3加127等于130。最后我们再把130按照无符号整数的规则，把它转换成巴比特的这种形式。这样我们就得到了八比特的一码。

最后我们再把确定的这八比特填到中间这个位置不就OK了。好，所以最后一步我们把符号为八比特解码以及23比特的尾数，把它们拼起来就行了。再次强调这个规格化的尾数，我们只保存小数点后面的这些部分，小数点前面的这个一我们会隐含表示。

好，这是第一个例题。接下来我们把这个例题改成双精度浮点型，十进制真值不变，大家可以暂停练练手。好，321公布答案，double型应该长这个样子。其实符号位的转换以及尾数部分的转换和float型没有什么区别。只不过尾数部分我们需要多凑一些0，把它凑足52比特，仅此而已对吧？

那和float型相比，转换起来唯一有区别的就是阶码部分。Double型的阶码部分是用11比特移码去表示的，并且偏置值是1023。所以在确定这个部分的时候，我们应该用这个阶码的真值3加上1023偏置值得到1026，再把1026按照无符号整数的规则转换成11比特。得到这一串之后，填进去之后，大功告成，转换成16进制记法，就应该是这个样子。这就是负的8.25这个数值用double型变量保存在计算机内部的一个样子。

好，刚才我们的这个例题一其实是来源于11年的真题十三。希望经过之前的学习，这类题目你可以轻松拿捏，没跟上的同学或者算错的同学可以暂停下来再重新做一下这道题。好，这个题目选择A选项。

好，再看一道题，同样考察真值转换成浮点数。22年的14题把这样一个数转换成单精度浮点数，请大家暂停视频动手做训练，321公布答案。这道题应该选择A选项，解题的思路一模一样，只不过是这个真值发生了一些变化而已。对于这个题目来说，有一部分同学可能会被卡在这一步，就是0.4375。好复杂的一个小数对吧？怎么把这个数转换成我们的二进制定点积法呢？好，那可以这么去处理。

如果题目给了你一个很复杂的小数，把它转换成二进制可以这么做。首先第一种方法，拼凑法。首先在草稿纸上写出小数点的前面各个比特，以及小数点的后面各个比特，它的权值等于多少。然后再尝试用各个比特的这个权值，用它们去拼凑出0.4375这样的一个值。那刚好0.25加上0.125，再加上0.0625加和的值就是4375。所以这个值转换成二进制就应该是负的0.011。好，这是第一种方法。这种方法对于一个比较长的这种小树，其实拼凑起来也没那么容易，对吧？

所以下次遇到这种比较长的小数，你可以尝试用第二种方法，叫做乘积取整法。我们把题目给的十进制数，它的小数部分给抠出来，然后用乘积取整的方式确定每个比特应该是多少。所谓的乘积就是乘以基数，那二进制的基数等于2。所以你会看到在第一轮处理当中，我们用这个十进制的真值，这个符号位我们先不考虑，0.4375，0.4375乘以基数二得到了0.875。好，这是乘积这一步。取整的意思就是说整数的这个部分把它留下作为这个比特的值，然后小数的部分把它用作下一轮的处理。在下一轮的处理当中，同样的就是把剩下的小数部分乘以基数等于1.75。

好，把整数部分一给留下，然后小数部分0.75再作为下一轮处理的这个素材，0.75再乘以二等于1.5，把整数留下，小数0.5给它放到下一轮继续使用。最后0.5乘以二等于1.0，到这一步你会发现小数部分已经等于零了，对吧？整数部分把它留下来，那整个转换就可以到此为止。最开始取的这个整数就是小数点后面的一个比特，也就是更高位，然后越往下就是越低位。所以对于这种比较复杂的小数来说，乘积取整法来转换二进制可能会更快一些。好，这些都是我们曾经学过的东西，忘了的同学可以借此机会回顾一下。这种转换技巧在考试的时候还是用得到的。

好，所以对于22年的这道真题来说，只要确定了这个十进制数的二进制表示，那接下来再把二进制表示转换成所谓的二进制科学计数法，并且确保尾数是规格化的。只要到这一步，我们就可以确定符号位应该怎么写，以及尾数应该怎么写。最后就是在确认解码应该怎么写。按照float型的规则，用偏置值127这样的规则去转换成巴比特的移码，最后拼凑出来就得到了最终的结果。好，这是22年的这道题，选择A选项。

接下来第三个例题，反过来把浮点数转换成真值，这是13年的十三题，题目给了单精度浮点数的16进制表示，问这个数的数值它的真值应该是多少？好，请各位考生开始应答，3秒钟后公布答案。321这个题目应该选择A选项，你做对了吗？

好，来看一下怎么处理。首先因为是单精度浮点数，所以我们先把单精度浮点数的结构给回顾一下，1加8加23。接下来第二步肯定要把题目给的这个16进制数，把它展开成32比特的二进制对吧？好，二进制串确定了之后，再结合float型的这个具体的结构，是不是就可以拆分出符号阶码和尾数分别是多少。接下来就是要依次确定符号的真实含义，还有这个阶码的真值以及尾数部分在高位再补一个一，是不是就OK了。

符号很简单，零正一负这个题目它的符号等于一，说明它是一个负数对吧？好，阶码部分我们需要把这个阶码转换成十进制的增值，转换的方法就是增值转移码的逆向的过程，对吧？首先先把这个移码按照无符号整数的规则把它解读一下。按照无符号数的规则解读，这八个比特应该对应的是140。然后第二步我们需要减掉已码的偏置值，140减掉127等于13。好，这样我们就得到了阶码的真值，对吧？

那确定了符号，确定了解码的真值，再拼接上尾数，是不是就可以得到二进制的科学计数法。由于这串尾数的前面小数点的前面隐含了一个一对吧？所以我们再把这一串尾数转换回二进制科学计数法的时候，需要在小数点的前面补一个一，以及符号是负的对吧？那就是负的1.1乘以二的多少次方呢？13次方，这个十三来自于刚才转换出的这个阶码的真值。

好，最后我们再把这个二进制的科学计数法再把它转换成十进制。好，到这一步我们再观察题目给的各个选项，它都是1.50.5乘以二的多少次方，对吧？那既然是1.50.5，说明这个题目的答案当中，它的这个尾数部分它是用十进制去表示的。

好，所以我们再把这个1.1这个二进制给它转换成十进制的形式。那1.1就应该对应十进制的1.5，那最终就得到了选项A负的1.5乘以2的13次方。好，这是13年的13题最后一个例题，20年的十三题同样的暂停训练。321公布答案，这个题目应该选择A选项。

好，这个题目同样是给了一个32比特的机器数，但是他说这个32比特的机器数它有可能是一个带符号整数。那带符号整数是用补码表示的，以及它也有可能是一个float型的这种浮点数。问我们这串机器数它的真值到底是多少？好，由于这个小节我们重点讨论的是浮点数，所以我们先尝试着把这个二进制串转换成浮点数。那它对应的浮点数臻值应该是负的二的17次方。

同样的先展开成二进制串，然后把符号解码以及尾数三个部分拆分出来。符号很简单，尾数也很简单。只需要把这个解码转换成十进制真值，然后得到二进制科学计数法。二进制的1.0转换成十进制的话，它也是1.0，所以最后就得到了十进制真值应该等于负的二的17次方。ABCD几个选项当中都没有这样的一个值。

好，所以题目给的这几个选项，如果我们按照float型去解读的话，都对不上。那float行不行？我们就只能按照int型，也就是带符号的整数去解读它。由于带符号整数都是用补码去保存的那首先观察最高位一，说明它是一个负数。要解读这个补码的真值，我们需要把补码先转换成源码。

一个负数的补码转源码方法很简单，从右往左扫到第一个比特一好，在这个比特一的左边画一条线，这条线的再往左的部分，除了符号位之外，零变一一变零，也就是100。这三个比特零变一一变零变成011。好，符号位保持不变，以及刚才这条线右边的所有的比特都保持不变。这样我们就把补码转换成了源码。

变成源码之后再解读它的真值就变得很简单了。首先它是一个负数，这个很简单。然后有三个一，这三个一它们的权值分别是二的29、28和27次方。把这三个比特的权值分别加起来，再结合它的符号再进行一些化简，就可以得到最终的正确答案，也就是A选项。好，这是2020年的题目，它不仅考察了float型，也考察了int型。考察的角度都是把二进制转换成十进制的真值。

好的，在这个视频中我们介绍了i trip e754浮点数的表示。我们通过大家熟悉的十进制科学计数法引入了一些比较重要的概念，分别是符号尾数、阶码基数以及什么叫尾数的规格化。紧接着我们介绍了单精度浮点型和双精度浮点型它们的结构单精度浮点总共占32比特，符号阶码和尾数分别占1823这么多个比特。需要注意的是单精度浮点型的这个阶码，它是用移码表示的，偏置值是127点。那双精度浮点型它的偏置值是1023。另一方面双精度浮点型的符号阶码和尾数分别是一比特、11比特和52比特。

无论是单精度还是双精度浮点，我们都需要特别的注意我们保存的这个尾数部分。由于这个尾数通常是规格化的，所以我们会隐含小数点前面的那个一。这么做的好处就是可以让尾数部分的实际精度更高一点。

好，由于浮点数这个部分，它几乎是每年必考的一个考点。解决浮点数的这个相关考题最重要的基本功就是能不能够把真值转换成浮点数，以及能不能够把浮点数的二进制表示转换成十进制的真值。这个是所有的同学都必须掌握的。这个基本功没打牢，那更难的浮点数题目你肯定做不出来。好，所以在这个视频中我们引入了比较多的练习题，带大家一边学一边练，趁热打铁。在练习的过程中，我们也介绍了一些做题的技巧，移码和真值之间怎么去转换，这个也要熟练。另外如果遇到复杂的这种十进制小数，那把它转换成二进制可以采用乘积取阵法。好的，以上就是这一小节的全部内容。