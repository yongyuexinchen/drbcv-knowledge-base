36 2.3.2_4 浮点数的加减运算（溢出问题）_原文

2026年08月03日 23:47

好的，继续来看浮点数加减运算的最后一个问题，就是溢出判断。我们说进行浮点数加减的时候，需要进行五个步骤。第一个步骤叫对接，原则是小街向大街看齐。第二个步骤是尾数加减。第三个步骤尾数的规格化，可能需要进行左归或者右归丸，确保尾数是规格化的那所谓规格化的尾数就是一点叉叉这样的一个形态。第四个步骤是舍入，把多余的尾数位给舍弃。最后第五个步骤就是溢出判断了。在这节课我们主要探讨的就是第五个问题。

好，首先给大家写了一个程序，在这个程序当中可以看到加减运算发生上亿的情况。在这儿定义了一个变量X这一长串数字，它的值就是二的127次方。下面Y的值对应的是二的127加2的126次方，紧接着Z等于X加Y最后打印出XYZ的值。那可以看到Z的运算结果是infinity，也就是无穷。

如果我们监测XYZ的这个二进制表示，那么可以看到XYZ分别是这样的值。重点关注Z它的阶码部分是巴比特全一，以及尾数部分是23比特的全0。另外它的符号位等于0。之前我们学过单精度浮点数，如果符号为零阶码全一而尾数全零的话，表示这是一个正无穷大。好，所以在这个例子中，X加Y的值超出了单精度浮点数可以表示的最大范围，因此发生了上亿运算，结果记录为正无穷。

我们快速回顾一下上亿的概念，我们说规格化的浮点数可以表示的范围是有限的。如果说超出了正的规格化浮点数可以表示的最大范围，那此时我们称发生了正上移。另外如果负值太大，那么就会发生负上亿。正上亿的运算结果通常会记为正无穷，负上亿的运算结果通常会记为负无穷。正上溢和负上意统称为上意，有时也会简单的翻译为溢出。发生溢出的时候，运算结果会设置为正无穷或者负无穷。与此同时浮点数溢出异常这个标志位会被置为一。但是i triple e754规定默认情况下我们不响应浮点数溢出异常，发生这种异常的时候不中断程序，除非程序员手动的响应这个一出一场。

好，接下来我们从二进制的视角分析一下X加Y它的背后发生了什么。大家先暂停自己动手算一遍，3秒钟后公布答案321。好，首先把X和Y的二进制给写出来，它们的阶码部分都是7个11个0，二者的阶码相等。尾数部分X是23比特权0，Y是一个1加22比特的全0，符号位都为正好。接下来把符号位和尾数补全，在小数点的前面隐含了一个一把这个一给补上好，接下来正式开始加法运算。

第一步对接，用X和Y的阶码相减等于0，说明二者的阶码相等，所以无需对接。于是我们可以把X和Y的尾数直接进行加减运算。在这个题目当中我们是要进行加法运算，把这两个尾数相加。那么相加的结果就是这样的，一串正的10.100000。

好，现在对于这种情况，显然我们需要进行右归？向右移动一位完成尾数的规格化。右归的同时需要把阶码加一，原本这个阶码值是七个一，一个零再加一之后，运算结果就等于八比特全一。

之前我们说过，对于单精度浮点数来说，规格化浮点数的阶码的合法范围是00000001一直到11111110，这是规格化浮点数的阶码的范围。但是现在又归之后阶码来到了巴比特全一这个值。计算机硬件检测到这种情况的时候，它就可以知道此时发生了溢出。具体来说是发生了正上移。因为这个尾数的运算结果是正的对吧？与此同时阶码是巴比特权益，所以发生了正上移。在这种情况下，这个加法运算的结果就是尾数部分会被强行的置为23比特的全0。

我们知道在没有发生善意的时候，尾数舍入的这个环节，通常来说我们是把小数点后面的23比特保留下来，对吧？多余的比特位把它砍掉，采用零舍一入，然后100特殊处理。一般来说是采用这样的原则，但是一旦发生了这种阶码的上移，那么这个尾数的运算结果我们会强行的把它置为全0。你看这儿原本有一个，现在被我们置为了全0。符号位为零，阶码为全一，而尾数又是全0。这个浮点数在754标准当中对应的就是正无穷这样的一个增值。我们可以得到这样的结论，这个结论也是选择题或者简答题当中比较常考的一个点。

浮点数的溢出并不是用尾数的溢出来判断的。比如说X和Y的尾数相加之后得到的这个新的尾数，你看最高位这已经溢出了，对吧？但是这种尾数的溢出我们可以通过右归，通过右归来进行纠正。尾数每右归一位，阶码相应的就要加一。那什么时候点数真正的发生了一处呢？就是指数这个部分，也就是阶码这个部分发生了上亿变成了权益。所以浮点数的上亿是通过指数是否上亿，是否来到了巴比特权益来进行判断的。一旦发生了上亿，那么就把这个运算结果记为无穷。好，这是第一个例子，我们可以看到上亿是怎么发生的，以及计算机硬件是怎么去检测上亿的。

接下来第二个例子，我们会看到加减运算发生下溢的情况，同样给大家写了一段程序，在这段程序当中调用了这样的一个函数。大家如果有兴趣自己去写一下的话，这个函数被包含在了max点H这个头文件当中。所以大家需要include mass点H那这个函数它计算的就是1.0乘以2的负126次方。右边这个就是1.0乘以2的负149次方。所以在我这儿X的值就是二的-116加上二的-149，那么Y的值它是等于负的2的负126次方。那Z的值我是要把X和Y相加，相加之后真值应该是等于二的负149次方。由于这个小数非常小哈，所以在打印运算结果的时候，我在这个地方小数点后面打印了60位，这样我们就可以看出XYZ的值。

用我们熟悉的十进制的表示，如果监控XYZ的这个二进制形式，我们可以看到Z的二进制表示是0。巴比特全0，然后00000，最后有一个一阶码全0，而尾数不全为零，这在浮点数的标准当中表示这是一个非规格化的浮点数。由于符号为正，所以这是非规格化的正数。此时这个浮点数的真值应该解读为二的负126次方乘以零点F这个F就是我们的23比特的尾数，大家可以自己验证一下，这个真值对应的就是二的负149次方。

好，我们回顾一下下义是怎么回事。我们说规格化的浮点数可以表示的范围是有限的。如果一个浮点数它的绝对值很小，已经低于规格化浮点数可以表示的最小范围。那么假如它是正数的话，此时就发生了正下一。而如果是负数，那么就发生负下邑。发生下溢的时候，如果可以用非规格化的浮点数表示，那么这个运算结果就会用非规格化的浮点数。而如果非规格化浮点数还不能表示，还要更小，那么此时就会被当作机器零来处理。那H会议规定发生这种浮点数下异常的时候，通常来说也不中断程序不会特意的去处理这种异常，除非程序员手动的去响应这种下溢异常。

好，接下来我们从二进制的视角看一下X加Y这个过程发生了什么。大家先暂停自己练习一下，3秒钟后公布答案，321。好，应该是这样的。首先把X和Y的二进制给写出来，阶码都是七个0，一个一尾数部分X这是22比特的0，最后有一个一Y的尾数部分是23比特全0。好，那我们把这个尾数和符号位给它写出来，并且补上小数点前面隐含的这个一。

接下来进行第一步对接。由于二者的阶码相减等于0，说明解码相等，无需对接，于是我们可以直接对X和Y的这个尾数进行加法运算。那Y的尾数是一个负值，所以X加Y相当于X的尾数减掉Y的这个尾数的绝对值。我们用手算的方式可以逐位相减，最后一个比特1减0等于一，前面这些比特都是0减0等于0，对吧？都是零。小数点前面这儿一减掉一同样等于0。好，所以X的尾数加上Y的尾数，最终的运算结果就应该是正的0.0000，最后还剩一个一好，那么我们把这个运算结果写出来。

现在由于小数点的前面这一位不是一，所以按照常规的流程，我们需要进行尾数的规格化，对吧？这种情况我们通常需要用左归的方式去处理，尾数每左移一位阶码就要减1。然而现在问题发生了，当前这个解码就是规格化浮点数的最小的一个解码值。如果尾数继续左移，然后阶码继续减1，那么阶码就会落到00000这样的一个值。而一旦阶码变成全0，那么这个浮点数就变成了一个非规格化浮点数，我们就要用另一种规则去解读它。好，所以现在我们对尾数进行左归这个动作其实有点矛盾的对吧？我们要对尾数进行规格化，但是当尾数左归一位的时候，整个浮点数就会变成非规格化的浮点数。好，所以此时我们不能按照普通的左归方式去处理。

计算机的处理方法是这样的，如果当前的这个阶码值已经是000001了，但是此时这个尾数还需要继续左移，才有可能实现规格化。如果发生这种情况，那么计算机会直接把阶码置为全0，并且这个尾数不一位，注意尾数不移位，保持原样解码。虽然置为了全0，但是尾数保持不变。然后直接把这个尾数就是小数点后面的这23位保存到float型浮点数的里边。好，此时符号位为零，阶码全0，尾数是22比特的0，再加上一个一。当阶码变成全零的时候，说明发生了下意识。

而当前这个尾数不全为零，说明此时是下移到了非规格化浮点数的区间。如果阶码全0，并且留下的这些尾数位也是全0，那么就是直接下移为了机器0。好，那么对于这个例子，我们尝试着用非规格化浮点数的规则去解读一下。它的真值应该是对应正的二的负126次方乘以0.00000000001，那最终对应的结果就是二的负149次方。这样我们就理解了前面这个例子，X加Y的运算结果下移到了非规格化浮点数的区间。

好的，这就是浮点数加减运算的溢出判断问题。我们通过两个例子感受了上亿以及下意这两种情况。计算机硬件对浮点数溢出的判断主要是根据阶码来进行的。如果右归的同时阶码加一阶码的值来到了巴比特全一的情况，那么就说明发生了上一。而如果阶码的值当前已经是00000001，但是尾数仍然需要继续左归，那么此时就会直接把码制为全0，并且尾数保持不变。而一旦阶码全0，说明发生了下意识。那此时运算的结果有可能会下移到非规格化浮点数的这个区间。对非规格化浮点数的增值这个尾数部分的解读，大家需要注意，非规格化浮点数的小数点前面隐含的不是一而是零。好的，那么以上就是浮点数加减运算这个部分的全部内容。大家可以再结合课件回顾一下我设计的这些例子。