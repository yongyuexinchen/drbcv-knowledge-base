60 4.3.3 循环语句的机器级表示_原文

2026年08月04日 00:23

好的，在这个视频中我们会学习如何用汇编语言实现我们熟悉的循环语句，也就是for while这些循环语句。使用我们之前学过的条件转移类指令，其实就可以实现循环。来看这样的一个简单的例子，在这个循环当中我们实现了一直加到100这样的一个功能。我们使用result这个变量来存储求和的结果。这是for循环的写法，我们也可以把它写成while循环对吧？都是等价的。

好，这种循环语句用汇编语言实现也很简单。好，为了方便讨论，我们现在假设result这个变量，它是被存储在EAX这个寄存器当中，初始值为零。所以刚开始这儿我们用了一个move语句，把EAX的值赋为零。而EDX这个寄存器会用于保存I这个变量，它的初始值是一。所以这一句我们把一赋值给EDX寄存器。那我们每一轮循环之前需要判断I的值是否小于等于100对。而如果I的值一旦大于100，是不是就应该跳过这个循环，往后继续执行。

好，所以这个地方你会看到两个你熟悉的条件，转移指令。首先compile指令对比了EDX和100，也就是对比了I和100的值。如果i greater than 100，也就是I大于100的话，那么会jump跳转到L2所指的这个位置，也就是直接把这个循环体直接给跳过。好，所以在第一次进入循环体之前，需要先进行一次判断是否需要直接把这个循环给跳过。有可能出现这样的情况对吧？那这个逻辑用我们熟悉的条件转移指令就可以实现。

好，再来看循环体的内部怎么实现每一轮循环。我们做的事情是让result的当前值和I的值相加，相加的结果存放回result。那这个逻辑是不是用一个add指令就可以很简单的实现add EAXEDX，把这两个寄存器里的值相加，相加的结果再存放回EAX当中。而我们刚才说EAX保存的是result这个变量，所以用这样的一句简单的汇编语言就可以实现result加等于I这个逻辑。

好，这是循环里面的处理。那每一轮循环快结束的时候，我们是不是需要例行的让I进行一个加加的操作？好，所以这个操作可以用一个increase，也就是自增的指令来实现对EDX里边的值进行加加。而EDX寄存器对应的就是变量I好，那么I加加之后是不是就需要判断还要不要进行下一轮循环？判断的方法也是使用一个company指令和一个条件转移指令，对比EDX和100的值，如果I小于等于100，我们就继续让它循环。所以这儿我们是用了JLE这个指令jump when less than or equal to，当I的值小于或等于100的时候进行跳转。那跳转回哪呢？跳转回L1，也就是这个循环刚开始的这一句。

我们对这个循环的实现进行总结，要用条件转移类指令实现循环，我们需要这样的四个部分来构成。首先第一部分要进行循环前的初始化工作，也就是这个部分I和result这两个变量是循环里边需要使用到的变量。那我们在循环开始之前需要对这些变量进行一个初始化。好，第二个部分，在我们开始第一次循环之前，我们需要先判断一下是否有可能直接跳过循环。如果I的值刚开始就不满足循环的条件，那直接就需要跳过对吧？好，所以第二部分我们需要加这样的一部分代码。

好，第三部分接下来就到了循环的主体，那每一轮循环做了哪些处理？这些处理的相关逻辑就写在第三部分。而第四部分每一轮循环的主体处理结束之后，我们都需要例行的判断是否还需要继续进行下一轮循环。只需要使用compile指令和一个条件转移指令，然后再结合一个标号，就是标记出这个循环的起始位置，用这样的方式就可以实现第四个部分。

好，所以如果大家在考试的时候遇到这种循环类的语句，那么首先你需要根据C语言去理解这个循环的逻辑，包括循环前需要做什么，循环的主体当中主要做的是什么样的处理，以及是否需要继续循环的这个条件是什么样的。你需要确定这样的几个问题，然后再结合我们这儿给出的这种框架，去分析题目当中给出的这个汇编语言指令。好，这就是用条件转移指令实现循环的一种方法。除了条件转移指令之外，在叉八六的这个汇编语言当中还有一个指令叫做loop指令，让大家看一下你就知道是什么意思了。我们在写程序的时候是不是经常会用到这样的处理，比如说你就规定死了，我要循环500轮或者要循环N轮之类的。

好，那先来看一下这个C语言，我们先初始化了一个变量I用于计数，初始值为500。然后每一轮循环当中I会减，如果I还大于零的话，那么就继续进行循环。好，所以这儿的变量它是一个计数的一个功能。

来看一下右边这个代码，首先这儿用了一个move指令，把500这个值赋给了ECX这个寄存器。所以相当于这儿我们是用ECX寄存器作为循环的计数器。好，接下来这个部分就是循环的主体laptop这个标号标记了循环的开始位置，然后中间这些部分就是循环里边要做什么事情，你自己去填充。

好，最后这个地方使用到了loop指令，loop到哪呢？Loop到loop top，听起来像是一个绕口令。好，那这一句指令它所做的事情就是首先会对ECX寄存器里的值自动的减，然后判断ECX的值是否等于0。如果不等于0，那么就继续循环，也就是继续跳转回lop top所标明的这个位置继续循环。

好，所以这条loop指令它就等价于先对ECX的寄存器进行decrease，进行简的操作。然后对比ECX和0的值。如果ECX和0 not equal不相等的话，那么就jump回lop top所指的这个位置。好，所以loop指令和条件转移指令它的底层逻辑没有任何区别，唯一多做的一个事情就是会自动的对ECX寄存器进行减的操作。

这个地方有必要跟大家解释一下，在叉八六里边我们知道有这样的四个通用寄存器，对吧？分别叫EAX、EBX、ECX和EDX。然后ECX这个寄存器它的一个特殊功能就是作为循环的计数器。所以在这个地方我们不可以用什么EAX、EBX, 就是其他的这些寄存器来代替ECX loop指令。它就是默认指定了对ECX进行减减好。因此如果要使用loop指令，那么肯定需要配合着使用ECX这个寄存器，用ECX里的值来判断是否还需要继续进行循环。好，这就是loop指令。

理论上来说，只要能用loop指令实现的功能，就一定可以用条件转移指令来实现。那既然如此，为什么叉86汇编语言里面需要引入live指令来增加这些指令的复杂性呢？原因也很简单，因为我们使用loop指令可能会更简洁一些，对吧？代码要更简洁而且更清晰一些。当我们在汇编语言代码当中看到J叉叉叉这种条件转移类指令的时候，就是作为程序员而言，你看到J开头的这种指令，第一反应可能会觉得说它应该是一个分支结构，这是我们的条件反射，对吧？那如果你在汇编语言代码里边看到loop指令，那你肯定会第一反应就觉得它肯定是个循环，而不是一个简单的分支结构，对吧？好，所以在汇编语言里边使用P指令，其实可以让整个汇编语言的代码逻辑更清晰，更简洁，这就是loop指令存在的意义。

好，最后再跟大家补充一个东西，除了我们这儿提到的loop指令之外，叉八六里面还有一些叫做loop叉，我把它称为loop叉这一类的指令。这儿的loop差就有点类似于条件转移指令里边的J叉，反正这儿的差指的是某一种条件。比如举两个例子，loop NZ和loop z那这儿的NZ指的是not zero。Loop NZ这条指令的执行效果就是在loop指令的基础之上，再多判断一下ZF标志位是否等于0。ZF标志位等于零就意味着最近的一次运算，不管是加法、减法、乘法、除法，反正最近的一次运算的结果not zero不是0，对吧？好，所以loop NZ这条指令它同样会做ECX减减，同样会判断ECX是否不等于0。当ECX不等于零并且还同时满足ZF等于0的时候，会继续循环。

相反的loop z这儿的Z指的就是zero。如果最近的一次运算结果为零，那么就继续循环。好，所以loop z这条指令，它除了判断ECX的值之外，还会判断ZF标志位是否等于一。因为如果最近一次运算的结果是0，那么ZF标志位一定是一，在这种情况下就继续循环。好，除了loop NZ和loop z这两条指令之外，还有可能会出现其他的loop叉这种形式。这只是给大家进行一个拓展。

万一你在考试当中遇到了loop叉这一类的指令，那么你不用害怕，你只需要知道反正它就是实现循环的那它这个实现循环的背后的这个逻辑需要判断哪些条件呢？你是不是可以结合C语言程序来倒推这条loop指令，它需要满足哪些条件？好，所以我们没有必要去尝试掌握所有的叉86语言相关的指令，包括其他的什么loop叉或者J叉叉叉。所有的这些指令都要掌握的话，你需要花很多时间，个人认为没有必要。在考试的时候，其实你只需要能够结合C语言给出的这个代码，然后去分析这个汇编语言它每一个部分大概做了哪些事情。具体一条指令它的判断逻辑条件，判断的逻辑是什么？你是不是可以根据这C语言程序来逆推就可以了。

好，所以我们只要大概知道loop指令，还有这种条件转移指令，它长什么样子，有什么特征。你在题目当中看见，你知道它是个条件转移指令，你知道它是一个loop指令就可以了。好的，以上就是这小节的全部内容。我们介绍了两种实现循环的方式，一种是用loop指令，而另一种是用条件转移类的指令。大家可以再结合课件自己消化一下。