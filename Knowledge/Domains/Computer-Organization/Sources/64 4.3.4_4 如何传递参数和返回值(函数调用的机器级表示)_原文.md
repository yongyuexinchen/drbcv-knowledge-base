64 4.3.4_4 如何传递参数和返回值(函数调用的机器级表示)_原文

2026年08月04日 00:23

好，继续看函数调用的机器级表示。通过之前的学习，我们知道函数调用需要用到两条很关键的指令，分别是call和return。在函数调用发生的时候，每一层函数在这个函数调用栈里边都会有一个与之对应的战争。而战争里边保存了和这一层函数相关的各种各样的信息。并且我们已经知道在函数调用发生的时候，以及函数调用返回的时候，如何去切换战争，以及我们如何去访问各个战争里边的数据。

在这个视频中我们要回答剩下的两个关键问题。首先每一个函数战争里边它到底包含了哪些内容呢？第二，我们如何通过这个函数的栈帧去传递调用参数，以及函数调用返回的时候，返回值是怎么带回给上一层函数的。

好，首先探讨第一个问题，在一个函数的战争内部可能会包含哪些内容呢？我们以color这个函数为例，通过之前的学习，我们知道每一个函数战争底部的这四个字节一定是保存了上一层战争的基地值。对于color函数来说，它的战争底部需要保存P这个函数的战争基地值。好，除此之外我们还可以确定任何一个函数在调用下一层函数的时候，一定会使用到call这个指令。而靠指令必然会把IP寄存器的值压到栈顶去保存。所以任何一个战争的顶部一定是保存了函数的返回地址，也就是IP寄存器的旧址。

好，接下来看一下color函数的这个代码，它里边定义了三个局部变量，分别叫tep 1、tep 2和sum。这三个局部变量分别在这几个位置。对于任何一个函数来说，在它的内部定义的这些局部变量都会被集中的存储在这个函数的栈帧靠近底部的这一片区域。另外注意观察在color这个函数里边，三个局部变量在代码当中出现的顺序，先是ten p然后是ten p2，最后是some。而在这个战争底部，如果我们从上往下看的话，最先出现的是sum然后是temple，接下来才是ten PC语言里面越靠前定义的这个局部变量会越靠近栈顶好，这是用于存储局部变量的区域，会靠近战争的底部。

那我们不妨思考一下站在汇编语言的视角，如何去访问函数内部定义的这几个局部变量呢？很简单对吧？我们只需要基于这个函数的EBP指针减4就可以找到，第一个局部变量减8就可以找到，第二个局部变量，减12可以找到第三个局部变量。因此以后大家阅读汇编语言代码的时候，如果你看到汇编语言当中去访问了EBP减4或者去访问了EBP减8，那么你要知道这里边存储的肯定是函数内部定义的某一个局部变量。如果结合C语言来分析的话，EBP减四这个位置肯定是存储了C语言当中最后一个出现的变量。而EBP减八这个位置，它对应的应该是C语言当中倒数第二个被定义的局部变量。

好，现在我们知道了战争的底部这一片区域是用于存储局部变量的。接下来在战争的顶部这片区域会用于存储函数调用的参数。可以看到color函数在调用I的函数的时候，需要给它传两个参数，分别是X和Y这两个参数在战争内部是存储在这个地方，靠近战争的顶部。同时在参数列表当中越先出现的参数越靠近顶部的位置。

好，基于这样的特点，我们不妨思考一下，如果现在正在执行的是add函数，那么EBP指针是指向了add函数的这个占比的位置，对吧？那么在add函数当中如何去访问上一层函数给它传过来的两个参数呢？是不是很简单？我们只需要用EBP加8，EBP加12，就可以分别找到X和Y这两个参数。所以当你以后阅读汇编语言代码的时候，如果看到EBP加八这个地址，那你就要知道这个地址所对应的一定是当前执行的这个函数，它的参数列表当中的第一个参数。而EBP加12对应的是当前执行的这个函数，它参数列表当中的第二个参数。好的，这是函数调用参数所存储的位置。

最后我们发现color函数的栈帧，它中间还有四个字节是我们没有使用的空闲区域，这是正常现象。因为当我们使用GCC编译器把C语言编译为汇编语言的时候，为了追求数据对齐，GCC编译器会规定每一个战争的大小应该是16字节的整数倍。像color函数它的栈帧大小就是32个字节，也就是16字节的两倍。那P函数和main函数肯定也是一样的，任何一个函数的栈帧大小都是16字节的整数倍。只有当前正在运行的这个函数不受刚才说的条件限制。也就是说如果现在正在运行的是I的函数，那么它的战争可以是四字节，可以是八字节，都可以。但是只要这个函数需要再调用下一层函数，那么这个函数的战争一定需要给它凑齐到16字节的整数倍。好，现在我们知道为什么会有这片空闲区域了，相当于一个用不完的零头。

对了，刚才说到这个GCC编译器，有兴趣的同学可以自己下去查一下。GCC编译器是linux世界里边最常用的一个编译器。在程序员的世界里面，GCC编译器非常流行。由于这个编译器是开源的，而且任何人都可以自由使用。所以绝大多数的教材在讲叉86汇编语言的时候，通常都是用GCC编译器把C语言翻译成汇编语言。好，有兴趣的同学可以自己下去了解一下。

好的，我们对刚才的内容进行总结。一个战争的内部可能会包含这样的一些区域。首先底部的四字节是上一层战争的基地址。接下来靠近底部的这一片区域会用于存储局部变量，而靠近战争顶部的这片区域会用于存储函数调用的参数。显然对于任何一个函数战争来说，底部的这四个字节一定是存在的。一定要存储上一层函数的战争机制，不然你就没法恢复上一层函数的战争了，对吧？接下来存储局部变量的这片区域，并不是每一个函数战争都有，因为有的函数它内部可能不定义局部变量，有这样的可能性对吧？

好，那再来看战争顶部的这个东西。可以肯定的是，只要我们发生了函数调用，那么顶部的这四个字节一定是用于存储IP寄存器的旧址的这是返回地址必须存储，而上面的这片区域就不一定存在，因为有的函数调用可以不传参数。好，最后中间可能会有一片区域是没有被使用到，不过这片区域也不一定存在。因为如果其他这些数据刚好可以凑足16字节的整数倍，那么中间这个零头就不会存在，那这就是一个战争的内部可能包含的内容。在这个视频的开始，我们提过一个问题，函数调用的时候如何传递调用参数呢？现在答案已经很明显了，在靠指令发起函数调用之前，我们需要把调用的参数写入到当前这个函数的战争顶部这片区域一定是这样的对吧？

好，接下来我们分析一下color和ad两个函数的汇编语言代码，来验证一下刚才我们学习的东西。好，首先在color函数之前有一个函数叫做PP函数调用了color函数。现在假设P函数的战争是这样的一个范围，那么当P函数调用color函数的时候，color函数的前两句汇编语言指令会切换战争，对吧？首先要把P函数的战争基地址给push压栈保存，然后再让EBP寄存器指向color函数的战争底部，那执行的效果就是这个样子。好，接下来这条指令会让ESP减24，也就是变成这样。EBP和ESP指明了当前执行的color函数，它的战争范围相当于画了一块自己的地盘，对吧？

好，接下来看一下这个C语言代码，color函数它定义了三个局部变量，其中碳P和探盘是有初始值的。好，那来看一下接下来这一句指令会把125写到EBP减12这个位置，那EBP减12应该是在这个地方对吧？那这句指令的执行就是完成了ten p1变量的初始化，它的初值是125。再往后一句把80写到EBP减8，那EBP减8对应这个位置，因此下面这条指令就是完成了type 2等于80这个操作。

好，另外我们还有一个局部变量叫sum，但是这个局部变量它没有初始值。接下来需要调用add函数，把I的函数的返回值赋给sum变量，对吧？那通过之前的学习，我们知道sum变量它对应的存储位置应该是EBP减四这个地方，只不过它的值是没有被初始化的。好，可以看到刚才的这两步操作完成了局部变量的初始化。工作站在战争的视角就是要往战争底部的这片区域写入相应的数据。

好，继续往后。接下来需要调用add函数，同时需要给add函数传递两个参数。Team p ten 8在靠指令发起调用之前，显然我们需要往战争顶部的这片区域写入两个参数。那来看接下来这句指令，把EBP减8的值写到EAX寄存器当中。那EBP减8对应的是type 2对吧？所以就是要把80这个数据先给写到EAX寄存器当中，接下来再把EAX里的值写到ESP加4的位置。那ESP加4应该是这个地方。所以这条指令的执行效果就是把80写到这个位置，也就是传递了Y这个参数。

这个地方。大家可能会奇怪，为什么要先把80写到EAX当中，再从EAX寄存器写回内存呢？难道我们不能用一条move指令就把数据直接从EBP减八复制到ESP加四这儿吗？好，这是不可以的。大家可以回去复习一下，我们讲move指令的时候强调过move指令不支持两个操作数都同时来自于主存。所以我们只能先从主存复制到寄存器，再从寄存器复制回主存。好，接下来这条指令，把EBP减12的值复制到EAX当中，125复制过来，然后把EAX的值复制到ESP所指的那个主存地址。执行的效果就是这样子。

接下来就可以通过靠指令发起函数调用。我们知道靠指令会把IP的旧值压到栈顶，同时会让程序的执行流转移到add函数的第一条指令。艾特函数前两条指令它也是要切换战争，对吧？Push指令会把color函数的战争基地值给保存下来，然后move指令会让EBP指针指向当前函数的这个基地址。

好，来看一下at函数做了什么，我们会使用X和Y这两个参数对它们执行加法，并且把相加的结果返回给上一层函数。那么在add函数内部如何去访问X和Y这两个参数呢？这个问题我们探讨过，通过当前这一层函数的基地址EBP加8可以访问到第一个参数，加12可以访问到第二个参数。所以来看后一句指令，从EBP加12把值复制到EAX当中，也就是把80复制到EAX寄存器当中，接下来EBP加捌复制到EDX寄存器当中，所以执行的效果就是这样的。好，再往后这条指令是一个加法指令，EAX和EDX的值相加，并且把结果写回EAX，那80加125等于205，所以执行的效果就是这样的。

好，最后执行leave指令，切换回上一层函数的战争，然后再执行return指令，让程序的执行流回到上一层函数。可以看到刚才add函数把返回值写在了EIX寄存器当中。那对于color函数来说，它要把add函数的返回值复制到sum变量这儿对吧？所以来看这条指令，把EAX的值复制到EBP减4。

刚好EBP减4对应的就是sum这个变量，那执行的效果就是这样的。好，接下来color函数需要把sum的值返回给再上一层函数，对吧？来看一下它是怎么做的。下面这条指令把EBP减4，也就是sum的值复制到了EAX寄存器当中。所以color函数给上一层函数返回值的时候，同样也是通过EAX寄存器来返回这个结果的。

我们知道在C语言里面，一个函数可能会有多个参数，但是它的返回值永远只会有一个。我们没有见过同时返回两个值这种写法，对吧？好，所以函数调用的时候，多个参数可以通过函数调用栈来传递。而函数调用返回的时候，由于只需要有一个返回值，因此通常我们都是把返回值写到e ax寄存器当中。这样当我们返回上一层函数的时候，它只需要去EAX寄存器当中去取这个值，是不是就可以获得函数的返回结果了？

好的，那我们再对之前的总结进行一个拓展。函数调用的机器级表示，首先是有两个关键的指令，分别是call和return。这两条指令会改变程序的执行流。通过这个小节的学习，我们知道在执行call指令之前，我们需要把调用的参数写入到当前函数的战争顶部那片区域。把参数压入栈顶的动作可以用push指令或者move指令来实现。接下来call指令会把返回地址压栈保存，并且跳转到被调用函数的第一条指令。首先需要进行战争的切换，接下来如果这个函数里边定义了一些局不变量，那么我们通常会把EBP减4、EBP减8、EBP减12这些位置的值给进行初始化。这些位置存储的就是当前函数的局部变量。

好，接下来经过一系列的逻辑处理之后，当前这个函数需要给上一层函数返回一个结果，对吧？那我们可以通过EAX寄存器来传递这个返回值。在return返回之后，这个调用者就可以通过EAX寄存器去使用返回值。

好，最后我们还要补充一个小细节，思考这样的问题。假设在发生函数调用的时候，这个调用者他有一些运算结果被存在了某一些寄存器当中，比如EDX、ECX或者EAX。那么当函数调用发生的时候，被调用者这个函数是不是也有可能会使用到这几个寄存器？那就有可能把调用者的这些中间结果给覆盖掉，就有可能会导致数据的丢失，对吧？

好，那怎么解决这个问题呢？是不是很简单？如果有必要的话，在发起函数调用之前，我把我所需要的这些寄存器值压栈保存，是不是就OK了。在函数调用返回之后，我再把这些寄存器的值从栈恢复到寄存器，这样就可以保证寄存器里面的这些数据不会丢失。

好，因此我们对刚才的这个图进行一个拓展的补充。有的函数战争里边可能会有这样的一片区域，用于保存部分的寄存器值。当然这片区域不一定存在，因为如果这些寄存器的值对我这层函数接下来的运行没有意义，那我就不用保存，对吧？好，所以具体情况具体分析。总之在函数调用之前，如果某一些寄存器的值有必要保存的话，那么就可以把这些寄存器的值压栈。然后在函数调用返回之后，再通过栈恢复这些寄存器的值。

好，这就是函数调用的机器集，表示这页PPT可以作为大家阅读汇编语言代码的一个分析框架。你要知道在靠指令之前需要做什么，在return指令之前需要做什么。好，最后我们对函数调用的机器级代码表示进行一个总结。

首先是战争，在函数调用栈里边一个战争会对应一层函数。那一个栈帧内部包含哪些内容呢？最底部肯定是EBP的旧值，接下来可能会存储若干个局部变量。接下来由于每个栈帧的大小是16字节的整数倍，因此可能会有一些没有被使用到的碎片。好，再往战争顶部走，就可能会有一片区域用于存储部分寄存器的值。因为在发起函数调用之后，有一些寄存器的值可能会被覆盖掉，有数据丢失的风险。所以有的时候函数调用之前，需要把部分的寄存器值给保存到战争里边。

好，接下来就可以往战争里边压入这个调用参数，最后再使用靠指令发起函数调用，而靠指令会把函数调用的返回地址给压到战争的顶部。那需要注意栈底的方向是高地址的方向，而栈顶的方向是低地址的方向。并且在叉86计算机当中，通常以四个字节为单位去操作栈。真在叉86CPU内部有EBP和ESP2个寄存器，分别标记了当前执行的这个函数战争的底部四字节和顶部四字节。我们可以使用push和pop指令结合，ESP也就是栈顶的指针去操作栈顶的数据。Push指令就是把数据压入栈顶，pop指令是把栈顶的数据给出战。当然这两条指令会让ESP指针自动的减4或者加4。

除了push和pop指令之外，我们也可以用我们最熟悉的move指令结合ESP或者结合EBP指针去访问战争里面的某一个数据。由于一个CPU只有一个EBP和一个ESP，因此当函数调用发生的时候，我们需要改变EBP和ESP的值，从而完成战争的切换。在每个函数的开头一定需要例行的执行enter指令，以及在每个函数return返回之前一定需要例行执行。例如指令，这两条指令可以完成战争的切换。当然我们说过可以用push和more指令去等价的实现这两条指令的功能。

我们发起函数调用需要使用call指令，而函数调用的返回需要使用return指令。在函数调用之前，我们需要先传递参数传递参数的方法，就是在靠指令之前把若干个参数写入到当前战争的顶部区域。参数入栈之后才可以执行coach令。执行code令首先会把IP寄存器的旧值压栈保存，也就是要保存函数的返回地址。接下来coach令还会把IP寄存器指向被调用函数的第一条指令，也就是设置一个新值。

第二个效果相当于执行了一个无条件转移指令，这是函数调用的时候需要做的事情。首先把参数压栈，接下来执行靠指令接下来。如果一个函数执行结束，就需要用return指令返回上一层函数。那return指令的效果就是从战争的顶部找到IP旧址，然后恢复IP寄存器的值。

最后还有一个问题值得我们注意，如何去访问战争内部的数据呢？特别是局部变量和上一层函数传过来的参数，如何去访问呢？由于局部变量会集中保存在靠近战争底部的那片区域，因此可以通过EBP减4、EBP减8、EBP减12，通过这几个地址分别找到一个函数内部定义的局部变量。而调用参数会保存在上一层函数的战争顶部的位置。所以我们可以从当前这一层函数的基地址往上偏移八个单位，或者往上偏移12个单位，就可以找到第一个参数，第二个参数。好的，以上就是这一部分的全部内容。