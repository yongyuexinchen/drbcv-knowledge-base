34 2.3.2_2 浮点数的加减运算（例题训练）_原文

2026年08月03日 23:46

好，继续学习浮点数的加减运算。接下来看一个实例，我们会看到浮点数的加法是如何实现的。浮点数的加减运算通常会基于float型来进行考察。因为double型和float型它的原理完全一致，所以如果作为考题的话，其实float型完全能够考察出大家对浮点数加减运算原理的理解了。就没有必要为了复杂而复杂，故意的去考double型对吧？那在历年真题当中也体现了这个特点，所以接下来的加减运算我们主要基于float型来进行探讨。

好，首先看这个例子，有XY2个float型的浮点数符合754标准，它们的十进制真值分别是1.5和0.5。然后让我们分析X加Y等于Z这个过程。好，首先我们把这两个数转换成float型二进制的形式，大家可以先暂停练练手。转换的结果应该是这样的，float型由一比特的符号为八比特的阶码和23比特的尾数组成。其中阶码这个部分的偏置值是127。前面这个部分我是用二进制科学计数法来记录的。我们只要先写出二进制科学计数法，然后就可以推出float型的二进制表示。数值转换这个部分之前已经练过很多，这儿就不再赘述。

好，接下来我们把符号位和尾数单独的提取出来。我们知道规格化的这个754浮点数，它在尾数的小数点前面还隐含了一个有效位一对吧？所以为了让接下来这个尾数的加减运算保持正确性，我们把这个小数点前面引盘的一给补上。另外Y这个数同样的在小数点前面补一个一，与此同时前面的这个符号位是正还是负，我们也把它给写上。因为加减运算我们是需要考虑符号位的。好，这样我们就得到了加数被加数的尾数的源码表示，包括隐含位和符号位。好，接下来正式开始加法运算，我们说需要经过五个步骤，分别是对接尾数、加减尾数、规格化尾数的舍入和溢出判断这样的五个步骤。

首先第一个步骤对接，我们说要让小街向大街靠齐。那计算机是怎么判断两个数的阶数谁大谁小的呢？很简单，只需要把两个数的阶码部分相减就行了。X的阶码减掉Y的阶码等于一，说明X的解码比Y的解码更大一从左边这个科学计数法，我们也可以很直观的看到这一点，X的阶码更大，Y的阶码更小，并且它们的差值是一。那么既然Y的阶码更小，那我们让小街向大街靠齐，就得让Y的尾数向右移一位，与此同时让阶码加一，这样就可以让二者的阶码对齐。

好，所以X的尾数这个部分保持不变，Y的尾数要右移一位，就得到了这样的一个值。小数点固定不变，尾数整体右移。那么右移多出来的这一位，我们先把它保留下来。与此同时更高的这一位，高位空出来的这一位，我们补0，符号位保持不变。

好，这个地方值得一提的是i triple e754的标准。它会规定就是在这个尾数右移的过程当中，右移移出的这个比特我们至少要额外的保留三位。那现在我们只移出了一位对吧？所以这个比特我们要把它保留下来，思考一下保留下来有什么作用，可以提高运算的精度，对吧？像这个例子当中，我们移出的这一位是零，这个零保留或者不保留其实无所谓。但如果说最后的这一比特原本是一，移出的是一个比特的一，那么这个一我们把它先保留下来，参与接下来的尾数加减运算，这就会比较有意义，可以使得加减运算的精度更高对吧？好，所以i triple e有这样的规定，就是右移移出的比特至少要额外的保留三位。

好，那么Y的尾数右移一位之后，相当于X和Y的阶码就对齐了，阶码都统一为了0，这个值小阶向大阶看齐。好，那么阶码对齐之后，接下来就可以进行尾数的加减运算。由于我们要实现的是加法，所以接下来只需要让这两个尾数进行相加。那我们说两个浮点数，它们的尾数是用源码的规则去表示的。计算机硬件实现源码的加减运算，本质上是把源码先转换成补码，用补码实现了加减之后再转回源码，这是计算机硬件实现源码加运算的原理。

好，那么我们用手算的方式来计算，观察两个数都是正值。那我们不妨把每一个比特对应的比特位全部相加，最后符号保持为正就行了对吧？那后面的这些比特全部是零，那这两个比特一加一等于0，往更高位进一个一对吧？那么小数点前面一加0再加一等于0，往更高位进一个一。所以两个数相加的结果就是正的10.0000000，后面全部是零，得到这样的一个加法结果。好，这是我们手算得到的结果，计算机计算得到的结果和我们手算的结果是完全一致的。如果不信的话，大家可以自己试一下，把这两个源码先转换成补码，就是把符号位变成0，然后用补码相加的规则看一下最后得到的结果是不是和我们一样OK。

到这一步我们实现了两个尾数的相加。现在得到的这个尾数显然它并不符合规格化的规定，对吧？规格化的尾数必须是第一个有效数值位，让它在小数点的前面一个位置。显然对于这个尾数来说，我们需要对它进行右归于是。

来到了第三步，让小数点固定不变，尾数向右移一位。尾数右移一位就得到这样的一个值正的1.000000。尾数每又归一位，相应的这个阶码就应该加1。

由于前面对接的这个步骤，我们已经让小阶码向大阶码看齐了。二者的阶码此时都是统一为零对吧？和大阶码保持一致。那么在这个步骤尾数右移一位的时候，我们就需要把原本已经统一的这个大阶码让它再加一。于是可以看到更大的这个解码加一得到1000000。好，这样我们就完成了规格化。

接下来第四个步骤，尾数的舍入处理。多余的尾数位我已经用这种灰色字体下划线给它标注出来了。在二进制浮点数的舍入这个动作，我们通常采用的是零舍一入，可以简单的记为零舍一入的这样的一个原则，就是要观察被舍弃的这个更高位。它如果是零，那么就直接舍掉就行了。如果是一，那么就要考虑往更高位入一个一。在这个例子当中，我们不需要往更高位进一，所以直接保留下24比特的有效尾数位，这样就完成了舍入的动作。

好，到这一步我们就得到了浮点数的每一个部分。首先尾数加减之后得到的运算结果为正，所以最后的运算结果符号位应该等于0。好，另外尾数这个部分我们保留了1加23比特的有效尾数位。小数点前面的这个一可以隐含的表示小数点后面的这23比特，需要完整的保存到型变量当中，所以你会看到23比特的0。

与此同时阶码这个部分，刚才我们进行尾数的规格化之后，阶码的值也进行了相应的调整对吧？尾数右移阶码加了一，那么阶码最终的值就会变成一个一，后面七个0，这就是X加Y的运算结果。大家可以先暂停把这个float型数转换成十进制，看一下是不是等于2.0这个值。好，大家可以先暂停转换一下这个数值。好，得到这个运算结果之后，对于计算机硬件而言，它还需要进行溢出判断。看一下刚才这个加法或者减法运算有没有超出float型可以表示的范围。

通过之前的学习，我们知道规格化的浮点数，它的阶码不全为零，也不全为一对吧？如果阶码全为一，那么整个浮点数的值可能表示的就是正无穷或者负无穷，那此时说明发生了上亿。而如果阶码全为零，阶码全为零，那么就说明已经超出了规格化浮点数可以表示的范围。此时这个浮点数就会下移到这个非规格化浮点数的区间。所以计算机判断是否发生溢出，主要就是看运算结果的这个解码部分。如果不全为一，也不全为零，那么就说明既没有发生上移，也没有发生下移。那对于这个例子而言，显然没有发生溢出，对吧？

好，那么这个二进制浮点数把它转换成十进制真值对应的就是二和我们期待的运算结果是完全一致的。好，大家可以再结合这些课件感受一下这五个步骤分别做了什么。好，接下来再看一个例子，浮点数的减法给出了XY的真值分析，X减Y的过程同样分为12345这样的五个步骤。好，第一步首先大家暂停把这两个数转换成float型的二进制形式，3秒钟之后公布答案。321转换的结果应该是这样的，其中左边这个数我是用二进制科学计数法来表示的。

好，接下来我们把两个数的尾数给提取出来，注意23比特的尾数前面应该隐含一个一对吧？与此同时最高位这个比特表示的是尾数的正负性，所以我们用正号去表示。这样我们得到了两个数的尾数源码，接下来正式开始减法运算。

首先第一步同样的是对接，我们要让小街向大街靠齐。我们说过计算机判断阶数大小，方法很简单，把二者的阶码相减，X的阶码减Y的解码等于-2。大家可以计算一下，这两串这两个数把它进行相减等于-2，那说明X阶码要更小对吧？并且阶码的差值是2，于是我们需要把阶码更小的这个数X它的尾数右移两位，这样就可以对齐阶。

好，那么X的尾数右移两位之后，得到这样的一个值，右移多出来的这两个比特我们把它保留。因为我们说过i trip e规定右移移出的这些比特至少要额外的保留三位。那现在只移出了两位对吧？这两位我们都把它保留下来。与此同时右移空出的高两个比特补零就行了，符号位保持不变。好，Y的尾数保持不变。经过这样的操作，X和Y的阶码就对齐到了负一这个值。好阶码对齐之后就可以进行尾数的加或者减。这道题当中我们需要用X的尾数减掉Y的尾数。那对于计算机而言，这两个尾数源码的减法运算，计算机会先把源码转补码，补码加减之后再转回源码。

我们可以模仿一下计算机的做法，画一条横线，右边的这些位全部都是零，所以我们就不模拟了。我们只考虑前边的这些比特，把符号位正号变成0，那么X的补码就是0.01，Y的补码就是01.00。最高的这个比特是符号位X要减掉Y对于计算机硬件而言，这个补码的减法运算就是把减数全部为按位取反，末位加1。那全部位按位取反，得到的就是10.11对吧？末位再加一个一，末位再加1，那这样的话减法就可以变成加法。好，最后的这三个比特1加1再加一，本位留一往更高位进个1 0加1再加一，本位由零往更高位进一个1 0加0再加一等于1 0加1等于一好，那这样的话就得到了X减Y的值。但是现在这个得到的结果是补码表示，对吧？

我们还需要把补码转换成源码。负数的补码转源码很简单，这个符号位一保持不变，然后从右往左找到第一个一，这个一的右半部分保持不变，左半部分0釆11变0，所以就变成了10.11对吧？那最高位符号位等于一表示它是一个负的，所以X减Y的运算结果就应该是负的。0.11后面全部都是零，这个是源码，我们已经把补码转换成了源码。好，这是尾数相减得到的一个运算结果，没跟上同学可以暂停动手算一下。

总之通过这个例子我们就能感受到，计算机硬件做这种源码的减法，本质上就是把源码先转成补码。加减之后先得到补码的运算结果，然后再把补码转换为原码。好，那么目前得到的这个尾数显然没有规格化，对吧？规格化的尾数的特点就是小数点的前面有且只有一个一。那对于现在这个尾数，我们应该进行左归，小数点固定不变，尾数整体左移一位，那佐归一位得到的就是负的1.1，后面全部都是零。

好，尾数左归一位，与此同时阶码就应该减1。之前X和Y的阶码已经对齐到了负一这个值，对吧？也就是对齐到了更大的这个解码。所以这儿的阶码减一就是在原本的这个大阶码的基础上进行减一的动作。好，原本的大解码减一就等于这样的一个值，这种简单的减法运算我们就不再赘述。好，这样我们就完成了尾数的规格化。

注意尾数的规格化这一步，在对尾数进行左移或者右移的同时，阶码的值也要相应的减或者加。好，接下来到了第四步尾数的舍入，我们只保留1加23比特的有效尾数。多余的这些比特就把它舍弃，采用零舍一入的原则。由于舍弃的这些都是零，直接把它干掉就行了，不需要往更高位进一，最终尾数就留下了这样的24比特。

好，最后我们把运算的结果拼接起来。由于运算结果是一个负值，所以符号位是一。而运算结果的阶码应该是这样的一个值。尾数部分我们把小数点前的这一个比特隐含，只把更低的23比特保存到float型变量当中，这样就得到了运算的结果。大家可以暂停尝试一下，把这个二进制串转换成十进制真值等于多少？3秒钟之后公布答案，结果应该是对应负的0.375，和我们这儿期待的X减Y的结果是能够对应上的。好，这是减法运算的例子。

接下来再来训练一个例子，希望大家可以先暂停自己把12345这几个步骤先完整的梳理一遍。好，十秒钟之后我再公布答案。愣着干嘛动手？哈哈哈好，先把XY两个数转换成float型，先转换成二进制科学计数法，再把科学计数法转换成float型，这是第一步。这步就搞错的同学请把不愧是我打在公屏上好，接下来我们把两个数的尾数提取出来，注意小数点的前面隐含了一个，一把它补上，与此同时符号位也把它补上好，接下来开始对接。

从人类视角可以很简单的看出来，X阶码要比Y的阶码小3，对吧？所以小阶向大家看齐，我们就需要把X的尾数右第三位。我们没必要把这个二进制的阶码带进去做减法运算，这是计算机做的事情，我们是人不是人机。所以直接从人类视角，就二进制科学计数法的这个视角去看就行了。二者的差值是3，那么我们把X的尾数右移三位。你看X的尾数右移三位，那多出来的这三位先把它保留下来，更高的三位补0。好，Y的尾数保持不变。

好，现在解码对齐了，下一步要进行尾数的减法，对吧？两个二进制的源码进行减法运算，怎么减？计算机的做法是把源码转补码，然后用补码完成加减运算，最后再转回源码。好，那么这两个源码的减法运算结果X减Y应该等于负的一点。哒哒哒大家得到这样的一个值，做错的同学抽自己一边子。好，那么减法运算得到的这个尾数天然的就已经规格化了，对吧？所以接下来第三步规格化的这个动作，我们就可以直接跳过尾数，既不需要左移也不需要右移。所以阶码也可以保持不变，就是保持为原本更大的这个解码就行了，保持不变。

好，接下来第四步尾数的舍入，尾数我们只保留有效的1加23比特对吧？那更多余的这三个比特就全部把它舍弃。那么采用零舍一入的原则，由于舍弃的最高位等于0，所以不需要向更高位进一，于是保留这样的一个尾数，总共是1加23比特。好，最后就得到了运算结果，运算结果为负，符号位等于一，阶码是这样的一个值。

好，最后尾数23比特，我们把小数点前的这个一隐含小数点后面的23比特保留下来，这样就得到了我们的float型。如果把它转换成十进制真值，那么对应的应该是负的110.125。大家可以用计算机算一下，X减Y的值刚好就是这样的一个值。好的，这是浮点数的减法运算。其实理解这个部分最好的方式就是自己动手。算几个例子。